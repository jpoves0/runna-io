var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __esm = (fn, res) => function __init() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except3, desc3) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except3)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc3 = __getOwnPropDesc(from, key)) || desc3.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// worker/node_modules/@libsql/core/lib-esm/api.js
var LibsqlError, LibsqlBatchError;
var init_api = __esm({
  "worker/node_modules/@libsql/core/lib-esm/api.js"() {
    LibsqlError = class extends Error {
      static {
        __name(this, "LibsqlError");
      }
      /** Machine-readable error code. */
      code;
      /** Extended error code with more specific information (e.g., SQLITE_CONSTRAINT_PRIMARYKEY). */
      extendedCode;
      /** Raw numeric error code */
      rawCode;
      constructor(message, code, extendedCode, rawCode, cause) {
        if (code !== void 0) {
          message = `${code}: ${message}`;
        }
        super(message, { cause });
        this.code = code;
        this.extendedCode = extendedCode;
        this.rawCode = rawCode;
        this.name = "LibsqlError";
      }
    };
    LibsqlBatchError = class extends LibsqlError {
      static {
        __name(this, "LibsqlBatchError");
      }
      /** The zero-based index of the statement that failed in the batch. */
      statementIndex;
      constructor(message, statementIndex, code, extendedCode, rawCode, cause) {
        super(message, code, extendedCode, rawCode, cause);
        this.statementIndex = statementIndex;
        this.name = "LibsqlBatchError";
      }
    };
  }
});

// worker/node_modules/@libsql/core/lib-esm/uri.js
function parseUri(text3) {
  const match2 = URI_RE.exec(text3);
  if (match2 === null) {
    throw new LibsqlError(`The URL '${text3}' is not in a valid format`, "URL_INVALID");
  }
  const groups = match2.groups;
  const scheme = groups["scheme"];
  const authority = groups["authority"] !== void 0 ? parseAuthority(groups["authority"]) : void 0;
  const path = percentDecode(groups["path"]);
  const query = groups["query"] !== void 0 ? parseQuery(groups["query"]) : void 0;
  const fragment = groups["fragment"] !== void 0 ? percentDecode(groups["fragment"]) : void 0;
  return { scheme, authority, path, query, fragment };
}
function parseAuthority(text3) {
  const match2 = AUTHORITY_RE.exec(text3);
  if (match2 === null) {
    throw new LibsqlError("The authority part of the URL is not in a valid format", "URL_INVALID");
  }
  const groups = match2.groups;
  const host = percentDecode(groups["host_br"] ?? groups["host"]);
  const port = groups["port"] ? parseInt(groups["port"], 10) : void 0;
  const userinfo = groups["username"] !== void 0 ? {
    username: percentDecode(groups["username"]),
    password: groups["password"] !== void 0 ? percentDecode(groups["password"]) : void 0
  } : void 0;
  return { host, port, userinfo };
}
function parseQuery(text3) {
  const sequences = text3.split("&");
  const pairs = [];
  for (const sequence of sequences) {
    if (sequence === "") {
      continue;
    }
    let key;
    let value;
    const splitIdx = sequence.indexOf("=");
    if (splitIdx < 0) {
      key = sequence;
      value = "";
    } else {
      key = sequence.substring(0, splitIdx);
      value = sequence.substring(splitIdx + 1);
    }
    pairs.push({
      key: percentDecode(key.replaceAll("+", " ")),
      value: percentDecode(value.replaceAll("+", " "))
    });
  }
  return { pairs };
}
function percentDecode(text3) {
  try {
    return decodeURIComponent(text3);
  } catch (e) {
    if (e instanceof URIError) {
      throw new LibsqlError(`URL component has invalid percent encoding: ${e}`, "URL_INVALID", void 0, void 0, e);
    }
    throw e;
  }
}
function encodeBaseUrl(scheme, authority, path) {
  if (authority === void 0) {
    throw new LibsqlError(`URL with scheme ${JSON.stringify(scheme + ":")} requires authority (the "//" part)`, "URL_INVALID");
  }
  const schemeText = `${scheme}:`;
  const hostText = encodeHost(authority.host);
  const portText = encodePort(authority.port);
  const userinfoText = encodeUserinfo(authority.userinfo);
  const authorityText = `//${userinfoText}${hostText}${portText}`;
  let pathText = path.split("/").map(encodeURIComponent).join("/");
  if (pathText !== "" && !pathText.startsWith("/")) {
    pathText = "/" + pathText;
  }
  return new URL(`${schemeText}${authorityText}${pathText}`);
}
function encodeHost(host) {
  return host.includes(":") ? `[${encodeURI(host)}]` : encodeURI(host);
}
function encodePort(port) {
  return port !== void 0 ? `:${port}` : "";
}
function encodeUserinfo(userinfo) {
  if (userinfo === void 0) {
    return "";
  }
  const usernameText = encodeURIComponent(userinfo.username);
  const passwordText = userinfo.password !== void 0 ? `:${encodeURIComponent(userinfo.password)}` : "";
  return `${usernameText}${passwordText}@`;
}
var URI_RE, AUTHORITY_RE;
var init_uri = __esm({
  "worker/node_modules/@libsql/core/lib-esm/uri.js"() {
    init_api();
    __name(parseUri, "parseUri");
    URI_RE = (() => {
      const SCHEME = "(?<scheme>[A-Za-z][A-Za-z.+-]*)";
      const AUTHORITY = "(?<authority>[^/?#]*)";
      const PATH = "(?<path>[^?#]*)";
      const QUERY = "(?<query>[^#]*)";
      const FRAGMENT = "(?<fragment>.*)";
      return new RegExp(`^${SCHEME}:(//${AUTHORITY})?${PATH}(\\?${QUERY})?(#${FRAGMENT})?$`, "su");
    })();
    __name(parseAuthority, "parseAuthority");
    AUTHORITY_RE = (() => {
      return new RegExp(`^((?<username>[^:]*)(:(?<password>.*))?@)?((?<host>[^:\\[\\]]*)|(\\[(?<host_br>[^\\[\\]]*)\\]))(:(?<port>[0-9]*))?$`, "su");
    })();
    __name(parseQuery, "parseQuery");
    __name(percentDecode, "percentDecode");
    __name(encodeBaseUrl, "encodeBaseUrl");
    __name(encodeHost, "encodeHost");
    __name(encodePort, "encodePort");
    __name(encodeUserinfo, "encodeUserinfo");
  }
});

// worker/node_modules/js-base64/base64.mjs
var version, VERSION, _hasBuffer, _TD, _TE, b64ch, b64chs, b64tab, b64re, _fromCC, _U8Afrom, _mkUriSafe, _tidyB64, btoaPolyfill, _btoa, _fromUint8Array, fromUint8Array, cb_utob, re_utob, utob, _encode, encode, encodeURI2, re_btou, cb_btou, btou, atobPolyfill, _atob, _toUint8Array, toUint8Array, _decode, _unURI, decode, isValid, _noEnum, extendString, extendUint8Array, extendBuiltins, gBase64;
var init_base64 = __esm({
  "worker/node_modules/js-base64/base64.mjs"() {
    version = "3.7.8";
    VERSION = version;
    _hasBuffer = typeof Buffer === "function";
    _TD = typeof TextDecoder === "function" ? new TextDecoder() : void 0;
    _TE = typeof TextEncoder === "function" ? new TextEncoder() : void 0;
    b64ch = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=";
    b64chs = Array.prototype.slice.call(b64ch);
    b64tab = ((a) => {
      let tab = {};
      a.forEach((c, i) => tab[c] = i);
      return tab;
    })(b64chs);
    b64re = /^(?:[A-Za-z\d+\/]{4})*?(?:[A-Za-z\d+\/]{2}(?:==)?|[A-Za-z\d+\/]{3}=?)?$/;
    _fromCC = String.fromCharCode.bind(String);
    _U8Afrom = typeof Uint8Array.from === "function" ? Uint8Array.from.bind(Uint8Array) : (it) => new Uint8Array(Array.prototype.slice.call(it, 0));
    _mkUriSafe = /* @__PURE__ */ __name((src) => src.replace(/=/g, "").replace(/[+\/]/g, (m0) => m0 == "+" ? "-" : "_"), "_mkUriSafe");
    _tidyB64 = /* @__PURE__ */ __name((s) => s.replace(/[^A-Za-z0-9\+\/]/g, ""), "_tidyB64");
    btoaPolyfill = /* @__PURE__ */ __name((bin) => {
      let u32, c0, c1, c2, asc3 = "";
      const pad = bin.length % 3;
      for (let i = 0; i < bin.length; ) {
        if ((c0 = bin.charCodeAt(i++)) > 255 || (c1 = bin.charCodeAt(i++)) > 255 || (c2 = bin.charCodeAt(i++)) > 255)
          throw new TypeError("invalid character found");
        u32 = c0 << 16 | c1 << 8 | c2;
        asc3 += b64chs[u32 >> 18 & 63] + b64chs[u32 >> 12 & 63] + b64chs[u32 >> 6 & 63] + b64chs[u32 & 63];
      }
      return pad ? asc3.slice(0, pad - 3) + "===".substring(pad) : asc3;
    }, "btoaPolyfill");
    _btoa = typeof btoa === "function" ? (bin) => btoa(bin) : _hasBuffer ? (bin) => Buffer.from(bin, "binary").toString("base64") : btoaPolyfill;
    _fromUint8Array = _hasBuffer ? (u8a) => Buffer.from(u8a).toString("base64") : (u8a) => {
      const maxargs = 4096;
      let strs = [];
      for (let i = 0, l = u8a.length; i < l; i += maxargs) {
        strs.push(_fromCC.apply(null, u8a.subarray(i, i + maxargs)));
      }
      return _btoa(strs.join(""));
    };
    fromUint8Array = /* @__PURE__ */ __name((u8a, urlsafe = false) => urlsafe ? _mkUriSafe(_fromUint8Array(u8a)) : _fromUint8Array(u8a), "fromUint8Array");
    cb_utob = /* @__PURE__ */ __name((c) => {
      if (c.length < 2) {
        var cc = c.charCodeAt(0);
        return cc < 128 ? c : cc < 2048 ? _fromCC(192 | cc >>> 6) + _fromCC(128 | cc & 63) : _fromCC(224 | cc >>> 12 & 15) + _fromCC(128 | cc >>> 6 & 63) + _fromCC(128 | cc & 63);
      } else {
        var cc = 65536 + (c.charCodeAt(0) - 55296) * 1024 + (c.charCodeAt(1) - 56320);
        return _fromCC(240 | cc >>> 18 & 7) + _fromCC(128 | cc >>> 12 & 63) + _fromCC(128 | cc >>> 6 & 63) + _fromCC(128 | cc & 63);
      }
    }, "cb_utob");
    re_utob = /[\uD800-\uDBFF][\uDC00-\uDFFFF]|[^\x00-\x7F]/g;
    utob = /* @__PURE__ */ __name((u) => u.replace(re_utob, cb_utob), "utob");
    _encode = _hasBuffer ? (s) => Buffer.from(s, "utf8").toString("base64") : _TE ? (s) => _fromUint8Array(_TE.encode(s)) : (s) => _btoa(utob(s));
    encode = /* @__PURE__ */ __name((src, urlsafe = false) => urlsafe ? _mkUriSafe(_encode(src)) : _encode(src), "encode");
    encodeURI2 = /* @__PURE__ */ __name((src) => encode(src, true), "encodeURI");
    re_btou = /[\xC0-\xDF][\x80-\xBF]|[\xE0-\xEF][\x80-\xBF]{2}|[\xF0-\xF7][\x80-\xBF]{3}/g;
    cb_btou = /* @__PURE__ */ __name((cccc) => {
      switch (cccc.length) {
        case 4:
          var cp = (7 & cccc.charCodeAt(0)) << 18 | (63 & cccc.charCodeAt(1)) << 12 | (63 & cccc.charCodeAt(2)) << 6 | 63 & cccc.charCodeAt(3), offset = cp - 65536;
          return _fromCC((offset >>> 10) + 55296) + _fromCC((offset & 1023) + 56320);
        case 3:
          return _fromCC((15 & cccc.charCodeAt(0)) << 12 | (63 & cccc.charCodeAt(1)) << 6 | 63 & cccc.charCodeAt(2));
        default:
          return _fromCC((31 & cccc.charCodeAt(0)) << 6 | 63 & cccc.charCodeAt(1));
      }
    }, "cb_btou");
    btou = /* @__PURE__ */ __name((b) => b.replace(re_btou, cb_btou), "btou");
    atobPolyfill = /* @__PURE__ */ __name((asc3) => {
      asc3 = asc3.replace(/\s+/g, "");
      if (!b64re.test(asc3))
        throw new TypeError("malformed base64.");
      asc3 += "==".slice(2 - (asc3.length & 3));
      let u24, r1, r2;
      let binArray = [];
      for (let i = 0; i < asc3.length; ) {
        u24 = b64tab[asc3.charAt(i++)] << 18 | b64tab[asc3.charAt(i++)] << 12 | (r1 = b64tab[asc3.charAt(i++)]) << 6 | (r2 = b64tab[asc3.charAt(i++)]);
        if (r1 === 64) {
          binArray.push(_fromCC(u24 >> 16 & 255));
        } else if (r2 === 64) {
          binArray.push(_fromCC(u24 >> 16 & 255, u24 >> 8 & 255));
        } else {
          binArray.push(_fromCC(u24 >> 16 & 255, u24 >> 8 & 255, u24 & 255));
        }
      }
      return binArray.join("");
    }, "atobPolyfill");
    _atob = typeof atob === "function" ? (asc3) => atob(_tidyB64(asc3)) : _hasBuffer ? (asc3) => Buffer.from(asc3, "base64").toString("binary") : atobPolyfill;
    _toUint8Array = _hasBuffer ? (a) => _U8Afrom(Buffer.from(a, "base64")) : (a) => _U8Afrom(_atob(a).split("").map((c) => c.charCodeAt(0)));
    toUint8Array = /* @__PURE__ */ __name((a) => _toUint8Array(_unURI(a)), "toUint8Array");
    _decode = _hasBuffer ? (a) => Buffer.from(a, "base64").toString("utf8") : _TD ? (a) => _TD.decode(_toUint8Array(a)) : (a) => btou(_atob(a));
    _unURI = /* @__PURE__ */ __name((a) => _tidyB64(a.replace(/[-_]/g, (m0) => m0 == "-" ? "+" : "/")), "_unURI");
    decode = /* @__PURE__ */ __name((src) => _decode(_unURI(src)), "decode");
    isValid = /* @__PURE__ */ __name((src) => {
      if (typeof src !== "string")
        return false;
      const s = src.replace(/\s+/g, "").replace(/={0,2}$/, "");
      return !/[^\s0-9a-zA-Z\+/]/.test(s) || !/[^\s0-9a-zA-Z\-_]/.test(s);
    }, "isValid");
    _noEnum = /* @__PURE__ */ __name((v) => {
      return {
        value: v,
        enumerable: false,
        writable: true,
        configurable: true
      };
    }, "_noEnum");
    extendString = /* @__PURE__ */ __name(function() {
      const _add = /* @__PURE__ */ __name((name, body) => Object.defineProperty(String.prototype, name, _noEnum(body)), "_add");
      _add("fromBase64", function() {
        return decode(this);
      });
      _add("toBase64", function(urlsafe) {
        return encode(this, urlsafe);
      });
      _add("toBase64URI", function() {
        return encode(this, true);
      });
      _add("toBase64URL", function() {
        return encode(this, true);
      });
      _add("toUint8Array", function() {
        return toUint8Array(this);
      });
    }, "extendString");
    extendUint8Array = /* @__PURE__ */ __name(function() {
      const _add = /* @__PURE__ */ __name((name, body) => Object.defineProperty(Uint8Array.prototype, name, _noEnum(body)), "_add");
      _add("toBase64", function(urlsafe) {
        return fromUint8Array(this, urlsafe);
      });
      _add("toBase64URI", function() {
        return fromUint8Array(this, true);
      });
      _add("toBase64URL", function() {
        return fromUint8Array(this, true);
      });
    }, "extendUint8Array");
    extendBuiltins = /* @__PURE__ */ __name(() => {
      extendString();
      extendUint8Array();
    }, "extendBuiltins");
    gBase64 = {
      version,
      VERSION,
      atob: _atob,
      atobPolyfill,
      btoa: _btoa,
      btoaPolyfill,
      fromBase64: decode,
      toBase64: encode,
      encode,
      encodeURI: encodeURI2,
      encodeURL: encodeURI2,
      utob,
      btou,
      decode,
      isValid,
      fromUint8Array,
      toUint8Array,
      extendString,
      extendUint8Array,
      extendBuiltins
    };
  }
});

// worker/node_modules/@libsql/core/lib-esm/util.js
function transactionModeToBegin(mode) {
  if (mode === "write") {
    return "BEGIN IMMEDIATE";
  } else if (mode === "read") {
    return "BEGIN TRANSACTION READONLY";
  } else if (mode === "deferred") {
    return "BEGIN DEFERRED";
  } else {
    throw RangeError('Unknown transaction mode, supported values are "write", "read" and "deferred"');
  }
}
function rowToJson(row) {
  return Array.prototype.map.call(row, valueToJson);
}
function valueToJson(value) {
  if (typeof value === "bigint") {
    return "" + value;
  } else if (value instanceof ArrayBuffer) {
    return gBase64.fromUint8Array(new Uint8Array(value));
  } else {
    return value;
  }
}
var supportedUrlLink, ResultSetImpl;
var init_util = __esm({
  "worker/node_modules/@libsql/core/lib-esm/util.js"() {
    init_base64();
    supportedUrlLink = "https://github.com/libsql/libsql-client-ts#supported-urls";
    __name(transactionModeToBegin, "transactionModeToBegin");
    ResultSetImpl = class {
      static {
        __name(this, "ResultSetImpl");
      }
      columns;
      columnTypes;
      rows;
      rowsAffected;
      lastInsertRowid;
      constructor(columns, columnTypes, rows, rowsAffected, lastInsertRowid) {
        this.columns = columns;
        this.columnTypes = columnTypes;
        this.rows = rows;
        this.rowsAffected = rowsAffected;
        this.lastInsertRowid = lastInsertRowid;
      }
      toJSON() {
        return {
          columns: this.columns,
          columnTypes: this.columnTypes,
          rows: this.rows.map(rowToJson),
          rowsAffected: this.rowsAffected,
          lastInsertRowid: this.lastInsertRowid !== void 0 ? "" + this.lastInsertRowid : null
        };
      }
    };
    __name(rowToJson, "rowToJson");
    __name(valueToJson, "valueToJson");
  }
});

// worker/node_modules/@libsql/core/lib-esm/config.js
function expandConfig(config, preferHttp) {
  if (typeof config !== "object") {
    throw new TypeError(`Expected client configuration as object, got ${typeof config}`);
  }
  let { url, authToken, tls, intMode, concurrency } = config;
  concurrency = Math.max(0, concurrency || 20);
  intMode ??= "number";
  let connectionQueryParams = [];
  if (url === inMemoryMode) {
    url = "file::memory:";
  }
  const uri = parseUri(url);
  const originalUriScheme = uri.scheme.toLowerCase();
  const isInMemoryMode = originalUriScheme === "file" && uri.path === inMemoryMode && uri.authority === void 0;
  let queryParamsDef;
  if (isInMemoryMode) {
    queryParamsDef = {
      cache: {
        values: ["shared", "private"],
        update: /* @__PURE__ */ __name((key, value) => connectionQueryParams.push(`${key}=${value}`), "update")
      }
    };
  } else {
    queryParamsDef = {
      tls: {
        values: ["0", "1"],
        update: /* @__PURE__ */ __name((_, value) => tls = value === "1", "update")
      },
      authToken: {
        update: /* @__PURE__ */ __name((_, value) => authToken = value, "update")
      }
    };
  }
  for (const { key, value } of uri.query?.pairs ?? []) {
    if (!Object.hasOwn(queryParamsDef, key)) {
      throw new LibsqlError(`Unsupported URL query parameter ${JSON.stringify(key)}`, "URL_PARAM_NOT_SUPPORTED");
    }
    const queryParamDef = queryParamsDef[key];
    if (queryParamDef.values !== void 0 && !queryParamDef.values.includes(value)) {
      throw new LibsqlError(`Unknown value for the "${key}" query argument: ${JSON.stringify(value)}. Supported values are: [${queryParamDef.values.map((x) => '"' + x + '"').join(", ")}]`, "URL_INVALID");
    }
    if (queryParamDef.update !== void 0) {
      queryParamDef?.update(key, value);
    }
  }
  const connectionQueryParamsString = connectionQueryParams.length === 0 ? "" : `?${connectionQueryParams.join("&")}`;
  const path = uri.path + connectionQueryParamsString;
  let scheme;
  if (originalUriScheme === "libsql") {
    if (tls === false) {
      if (uri.authority?.port === void 0) {
        throw new LibsqlError('A "libsql:" URL with ?tls=0 must specify an explicit port', "URL_INVALID");
      }
      scheme = preferHttp ? "http" : "ws";
    } else {
      scheme = preferHttp ? "https" : "wss";
    }
  } else {
    scheme = originalUriScheme;
  }
  if (scheme === "http" || scheme === "ws") {
    tls ??= false;
  } else {
    tls ??= true;
  }
  if (scheme !== "http" && scheme !== "ws" && scheme !== "https" && scheme !== "wss" && scheme !== "file") {
    throw new LibsqlError(`The client supports only "libsql:", "wss:", "ws:", "https:", "http:" and "file:" URLs, got ${JSON.stringify(uri.scheme + ":")}. For more information, please read ${supportedUrlLink}`, "URL_SCHEME_NOT_SUPPORTED");
  }
  if (intMode !== "number" && intMode !== "bigint" && intMode !== "string") {
    throw new TypeError(`Invalid value for intMode, expected "number", "bigint" or "string", got ${JSON.stringify(intMode)}`);
  }
  if (uri.fragment !== void 0) {
    throw new LibsqlError(`URL fragments are not supported: ${JSON.stringify("#" + uri.fragment)}`, "URL_INVALID");
  }
  if (isInMemoryMode) {
    return {
      scheme: "file",
      tls: false,
      path,
      intMode,
      concurrency,
      syncUrl: config.syncUrl,
      syncInterval: config.syncInterval,
      readYourWrites: config.readYourWrites,
      offline: config.offline,
      fetch: config.fetch,
      authToken: void 0,
      encryptionKey: void 0,
      remoteEncryptionKey: void 0,
      authority: void 0
    };
  }
  return {
    scheme,
    tls,
    authority: uri.authority,
    path,
    authToken,
    intMode,
    concurrency,
    encryptionKey: config.encryptionKey,
    remoteEncryptionKey: config.remoteEncryptionKey,
    syncUrl: config.syncUrl,
    syncInterval: config.syncInterval,
    readYourWrites: config.readYourWrites,
    offline: config.offline,
    fetch: config.fetch
  };
}
var inMemoryMode;
var init_config = __esm({
  "worker/node_modules/@libsql/core/lib-esm/config.js"() {
    init_api();
    init_uri();
    init_util();
    inMemoryMode = ":memory:";
    __name(expandConfig, "expandConfig");
  }
});

// worker/node_modules/@libsql/isomorphic-ws/web.mjs
var _WebSocket;
var init_web = __esm({
  "worker/node_modules/@libsql/isomorphic-ws/web.mjs"() {
    if (typeof WebSocket !== "undefined") {
      _WebSocket = WebSocket;
    } else if (typeof global !== "undefined") {
      _WebSocket = global.WebSocket;
    } else if (typeof window !== "undefined") {
      _WebSocket = window.WebSocket;
    } else if (typeof self !== "undefined") {
      _WebSocket = self.WebSocket;
    }
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/client.js
var Client;
var init_client = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/client.js"() {
    Client = class {
      static {
        __name(this, "Client");
      }
      /** @private */
      constructor() {
        this.intMode = "number";
      }
      /** Representation of integers returned from the database. See {@link IntMode}.
       *
       * This value is inherited by {@link Stream} objects created with {@link openStream}, but you can
       * override the integer mode for every stream by setting {@link Stream.intMode} on the stream.
       */
      intMode;
    };
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/errors.js
var ClientError, ProtoError, ResponseError, ClosedError, WebSocketUnsupportedError, WebSocketError, HttpServerError, ProtocolVersionError, InternalError, MisuseError;
var init_errors = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/errors.js"() {
    ClientError = class extends Error {
      static {
        __name(this, "ClientError");
      }
      /** @private */
      constructor(message) {
        super(message);
        this.name = "ClientError";
      }
    };
    ProtoError = class extends ClientError {
      static {
        __name(this, "ProtoError");
      }
      /** @private */
      constructor(message) {
        super(message);
        this.name = "ProtoError";
      }
    };
    ResponseError = class extends ClientError {
      static {
        __name(this, "ResponseError");
      }
      code;
      /** @internal */
      proto;
      /** @private */
      constructor(message, protoError) {
        super(message);
        this.name = "ResponseError";
        this.code = protoError.code;
        this.proto = protoError;
        this.stack = void 0;
      }
    };
    ClosedError = class extends ClientError {
      static {
        __name(this, "ClosedError");
      }
      /** @private */
      constructor(message, cause) {
        if (cause !== void 0) {
          super(`${message}: ${cause}`);
          this.cause = cause;
        } else {
          super(message);
        }
        this.name = "ClosedError";
      }
    };
    WebSocketUnsupportedError = class extends ClientError {
      static {
        __name(this, "WebSocketUnsupportedError");
      }
      /** @private */
      constructor(message) {
        super(message);
        this.name = "WebSocketUnsupportedError";
      }
    };
    WebSocketError = class extends ClientError {
      static {
        __name(this, "WebSocketError");
      }
      /** @private */
      constructor(message) {
        super(message);
        this.name = "WebSocketError";
      }
    };
    HttpServerError = class extends ClientError {
      static {
        __name(this, "HttpServerError");
      }
      status;
      /** @private */
      constructor(message, status) {
        super(message);
        this.status = status;
        this.name = "HttpServerError";
      }
    };
    ProtocolVersionError = class extends ClientError {
      static {
        __name(this, "ProtocolVersionError");
      }
      /** @private */
      constructor(message) {
        super(message);
        this.name = "ProtocolVersionError";
      }
    };
    InternalError = class extends ClientError {
      static {
        __name(this, "InternalError");
      }
      /** @private */
      constructor(message) {
        super(message);
        this.name = "InternalError";
      }
    };
    MisuseError = class extends ClientError {
      static {
        __name(this, "MisuseError");
      }
      /** @private */
      constructor(message) {
        super(message);
        this.name = "MisuseError";
      }
    };
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/encoding/json/decode.js
function string(value) {
  if (typeof value === "string") {
    return value;
  }
  throw typeError(value, "string");
}
function stringOpt(value) {
  if (value === null || value === void 0) {
    return void 0;
  } else if (typeof value === "string") {
    return value;
  }
  throw typeError(value, "string or null");
}
function number(value) {
  if (typeof value === "number") {
    return value;
  }
  throw typeError(value, "number");
}
function boolean(value) {
  if (typeof value === "boolean") {
    return value;
  }
  throw typeError(value, "boolean");
}
function array(value) {
  if (Array.isArray(value)) {
    return value;
  }
  throw typeError(value, "array");
}
function object(value) {
  if (value !== null && typeof value === "object" && !Array.isArray(value)) {
    return value;
  }
  throw typeError(value, "object");
}
function arrayObjectsMap(value, fun) {
  return array(value).map((elemValue) => fun(object(elemValue)));
}
function typeError(value, expected) {
  if (value === void 0) {
    return new ProtoError(`Expected ${expected}, but the property was missing`);
  }
  let received = typeof value;
  if (value === null) {
    received = "null";
  } else if (Array.isArray(value)) {
    received = "array";
  }
  return new ProtoError(`Expected ${expected}, received ${received}`);
}
function readJsonObject(value, fun) {
  return fun(object(value));
}
var init_decode = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/encoding/json/decode.js"() {
    init_errors();
    __name(string, "string");
    __name(stringOpt, "stringOpt");
    __name(number, "number");
    __name(boolean, "boolean");
    __name(array, "array");
    __name(object, "object");
    __name(arrayObjectsMap, "arrayObjectsMap");
    __name(typeError, "typeError");
    __name(readJsonObject, "readJsonObject");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/encoding/json/encode.js
function writeJsonObject(value, fun) {
  const output = [];
  const writer = new ObjectWriter(output);
  writer.begin();
  fun(writer, value);
  writer.end();
  return output.join("");
}
var ObjectWriter;
var init_encode = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/encoding/json/encode.js"() {
    ObjectWriter = class {
      static {
        __name(this, "ObjectWriter");
      }
      #output;
      #isFirst;
      constructor(output) {
        this.#output = output;
        this.#isFirst = false;
      }
      begin() {
        this.#output.push("{");
        this.#isFirst = true;
      }
      end() {
        this.#output.push("}");
        this.#isFirst = false;
      }
      #key(name) {
        if (this.#isFirst) {
          this.#output.push('"');
          this.#isFirst = false;
        } else {
          this.#output.push(',"');
        }
        this.#output.push(name);
        this.#output.push('":');
      }
      string(name, value) {
        this.#key(name);
        this.#output.push(JSON.stringify(value));
      }
      stringRaw(name, value) {
        this.#key(name);
        this.#output.push('"');
        this.#output.push(value);
        this.#output.push('"');
      }
      number(name, value) {
        this.#key(name);
        this.#output.push("" + value);
      }
      boolean(name, value) {
        this.#key(name);
        this.#output.push(value ? "true" : "false");
      }
      object(name, value, valueFun) {
        this.#key(name);
        this.begin();
        valueFun(this, value);
        this.end();
      }
      arrayObjects(name, values, valueFun) {
        this.#key(name);
        this.#output.push("[");
        for (let i = 0; i < values.length; ++i) {
          if (i !== 0) {
            this.#output.push(",");
          }
          this.begin();
          valueFun(this, values[i]);
          this.end();
        }
        this.#output.push("]");
      }
    };
    __name(writeJsonObject, "writeJsonObject");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/encoding/protobuf/util.js
var VARINT, FIXED_64, LENGTH_DELIMITED, FIXED_32;
var init_util2 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/encoding/protobuf/util.js"() {
    VARINT = 0;
    FIXED_64 = 1;
    LENGTH_DELIMITED = 2;
    FIXED_32 = 5;
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/encoding/protobuf/decode.js
function readProtobufMessage(data, def) {
  const msgReader = new MessageReader(data);
  const fieldReader = new FieldReader(msgReader);
  let value = def.default();
  while (!msgReader.eof()) {
    const key = msgReader.varint();
    const tag = key >> 3;
    const wireType = key & 7;
    fieldReader.setup(wireType);
    const tagFun = def[tag];
    if (tagFun !== void 0) {
      const returnedValue = tagFun(fieldReader, value);
      if (returnedValue !== void 0) {
        value = returnedValue;
      }
    }
    fieldReader.maybeSkip();
  }
  return value;
}
var MessageReader, FieldReader;
var init_decode2 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/encoding/protobuf/decode.js"() {
    init_errors();
    init_util2();
    MessageReader = class {
      static {
        __name(this, "MessageReader");
      }
      #array;
      #view;
      #pos;
      constructor(array3) {
        this.#array = array3;
        this.#view = new DataView(array3.buffer, array3.byteOffset, array3.byteLength);
        this.#pos = 0;
      }
      varint() {
        let value = 0;
        for (let shift = 0; ; shift += 7) {
          const byte = this.#array[this.#pos++];
          value |= (byte & 127) << shift;
          if (!(byte & 128)) {
            break;
          }
        }
        return value;
      }
      varintBig() {
        let value = 0n;
        for (let shift = 0n; ; shift += 7n) {
          const byte = this.#array[this.#pos++];
          value |= BigInt(byte & 127) << shift;
          if (!(byte & 128)) {
            break;
          }
        }
        return value;
      }
      bytes(length3) {
        const array3 = new Uint8Array(this.#array.buffer, this.#array.byteOffset + this.#pos, length3);
        this.#pos += length3;
        return array3;
      }
      double() {
        const value = this.#view.getFloat64(this.#pos, true);
        this.#pos += 8;
        return value;
      }
      skipVarint() {
        for (; ; ) {
          const byte = this.#array[this.#pos++];
          if (!(byte & 128)) {
            break;
          }
        }
      }
      skip(count) {
        this.#pos += count;
      }
      eof() {
        return this.#pos >= this.#array.byteLength;
      }
    };
    FieldReader = class {
      static {
        __name(this, "FieldReader");
      }
      #reader;
      #wireType;
      constructor(reader) {
        this.#reader = reader;
        this.#wireType = -1;
      }
      setup(wireType) {
        this.#wireType = wireType;
      }
      #expect(expectedWireType) {
        if (this.#wireType !== expectedWireType) {
          throw new ProtoError(`Expected wire type ${expectedWireType}, got ${this.#wireType}`);
        }
        this.#wireType = -1;
      }
      bytes() {
        this.#expect(LENGTH_DELIMITED);
        const length3 = this.#reader.varint();
        return this.#reader.bytes(length3);
      }
      string() {
        return new TextDecoder().decode(this.bytes());
      }
      message(def) {
        return readProtobufMessage(this.bytes(), def);
      }
      int32() {
        this.#expect(VARINT);
        return this.#reader.varint();
      }
      uint32() {
        return this.int32();
      }
      bool() {
        return this.int32() !== 0;
      }
      uint64() {
        this.#expect(VARINT);
        return this.#reader.varintBig();
      }
      sint64() {
        const value = this.uint64();
        return value >> 1n ^ -(value & 1n);
      }
      double() {
        this.#expect(FIXED_64);
        return this.#reader.double();
      }
      maybeSkip() {
        if (this.#wireType < 0) {
          return;
        } else if (this.#wireType === VARINT) {
          this.#reader.skipVarint();
        } else if (this.#wireType === FIXED_64) {
          this.#reader.skip(8);
        } else if (this.#wireType === LENGTH_DELIMITED) {
          const length3 = this.#reader.varint();
          this.#reader.skip(length3);
        } else if (this.#wireType === FIXED_32) {
          this.#reader.skip(4);
        } else {
          throw new ProtoError(`Unexpected wire type ${this.#wireType}`);
        }
        this.#wireType = -1;
      }
    };
    __name(readProtobufMessage, "readProtobufMessage");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/encoding/protobuf/encode.js
function writeProtobufMessage(value, fun) {
  const w = new MessageWriter();
  fun(w, value);
  return w.data();
}
var MessageWriter;
var init_encode2 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/encoding/protobuf/encode.js"() {
    init_util2();
    MessageWriter = class _MessageWriter {
      static {
        __name(this, "MessageWriter");
      }
      #buf;
      #array;
      #view;
      #pos;
      constructor() {
        this.#buf = new ArrayBuffer(256);
        this.#array = new Uint8Array(this.#buf);
        this.#view = new DataView(this.#buf);
        this.#pos = 0;
      }
      #ensure(extra) {
        if (this.#pos + extra <= this.#buf.byteLength) {
          return;
        }
        let newCap = this.#buf.byteLength;
        while (newCap < this.#pos + extra) {
          newCap *= 2;
        }
        const newBuf = new ArrayBuffer(newCap);
        const newArray = new Uint8Array(newBuf);
        const newView = new DataView(newBuf);
        newArray.set(new Uint8Array(this.#buf, 0, this.#pos));
        this.#buf = newBuf;
        this.#array = newArray;
        this.#view = newView;
      }
      #varint(value) {
        this.#ensure(5);
        value = 0 | value;
        do {
          let byte = value & 127;
          value >>>= 7;
          byte |= value ? 128 : 0;
          this.#array[this.#pos++] = byte;
        } while (value);
      }
      #varintBig(value) {
        this.#ensure(10);
        value = value & 0xffffffffffffffffn;
        do {
          let byte = Number(value & 0x7fn);
          value >>= 7n;
          byte |= value ? 128 : 0;
          this.#array[this.#pos++] = byte;
        } while (value);
      }
      #tag(tag, wireType) {
        this.#varint(tag << 3 | wireType);
      }
      bytes(tag, value) {
        this.#tag(tag, LENGTH_DELIMITED);
        this.#varint(value.byteLength);
        this.#ensure(value.byteLength);
        this.#array.set(value, this.#pos);
        this.#pos += value.byteLength;
      }
      string(tag, value) {
        this.bytes(tag, new TextEncoder().encode(value));
      }
      message(tag, value, fun) {
        const writer = new _MessageWriter();
        fun(writer, value);
        this.bytes(tag, writer.data());
      }
      int32(tag, value) {
        this.#tag(tag, VARINT);
        this.#varint(value);
      }
      uint32(tag, value) {
        this.int32(tag, value);
      }
      bool(tag, value) {
        this.int32(tag, value ? 1 : 0);
      }
      sint64(tag, value) {
        this.#tag(tag, VARINT);
        this.#varintBig(value << 1n ^ value >> 63n);
      }
      double(tag, value) {
        this.#tag(tag, FIXED_64);
        this.#ensure(8);
        this.#view.setFloat64(this.#pos, value, true);
        this.#pos += 8;
      }
      data() {
        return new Uint8Array(this.#buf, 0, this.#pos);
      }
    };
    __name(writeProtobufMessage, "writeProtobufMessage");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/encoding/index.js
var init_encoding = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/encoding/index.js"() {
    init_decode();
    init_encode();
    init_decode2();
    init_encode2();
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/id_alloc.js
var IdAlloc;
var init_id_alloc = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/id_alloc.js"() {
    init_errors();
    IdAlloc = class {
      static {
        __name(this, "IdAlloc");
      }
      // Set of all allocated ids
      #usedIds;
      // Set of all free ids lower than `#usedIds.size`
      #freeIds;
      constructor() {
        this.#usedIds = /* @__PURE__ */ new Set();
        this.#freeIds = /* @__PURE__ */ new Set();
      }
      // Returns an id that was free, and marks it as used.
      alloc() {
        for (const freeId2 of this.#freeIds) {
          this.#freeIds.delete(freeId2);
          this.#usedIds.add(freeId2);
          if (!this.#usedIds.has(this.#usedIds.size - 1)) {
            this.#freeIds.add(this.#usedIds.size - 1);
          }
          return freeId2;
        }
        const freeId = this.#usedIds.size;
        this.#usedIds.add(freeId);
        return freeId;
      }
      free(id) {
        if (!this.#usedIds.delete(id)) {
          throw new InternalError("Freeing an id that is not allocated");
        }
        this.#freeIds.delete(this.#usedIds.size);
        if (id < this.#usedIds.size) {
          this.#freeIds.add(id);
        }
      }
    };
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/util.js
function impossible(value, message) {
  throw new InternalError(message);
}
var init_util3 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/util.js"() {
    init_errors();
    __name(impossible, "impossible");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/value.js
function valueToProto(value) {
  if (value === null) {
    return null;
  } else if (typeof value === "string") {
    return value;
  } else if (typeof value === "number") {
    if (!Number.isFinite(value)) {
      throw new RangeError("Only finite numbers (not Infinity or NaN) can be passed as arguments");
    }
    return value;
  } else if (typeof value === "bigint") {
    if (value < minInteger || value > maxInteger) {
      throw new RangeError("This bigint value is too large to be represented as a 64-bit integer and passed as argument");
    }
    return value;
  } else if (typeof value === "boolean") {
    return value ? 1n : 0n;
  } else if (value instanceof ArrayBuffer) {
    return new Uint8Array(value);
  } else if (value instanceof Uint8Array) {
    return value;
  } else if (value instanceof Date) {
    return +value.valueOf();
  } else if (typeof value === "object") {
    return "" + value.toString();
  } else {
    throw new TypeError("Unsupported type of value");
  }
}
function valueFromProto(value, intMode) {
  if (value === null) {
    return null;
  } else if (typeof value === "number") {
    return value;
  } else if (typeof value === "string") {
    return value;
  } else if (typeof value === "bigint") {
    if (intMode === "number") {
      const num = Number(value);
      if (!Number.isSafeInteger(num)) {
        throw new RangeError("Received integer which is too large to be safely represented as a JavaScript number");
      }
      return num;
    } else if (intMode === "bigint") {
      return value;
    } else if (intMode === "string") {
      return "" + value;
    } else {
      throw new MisuseError("Invalid value for IntMode");
    }
  } else if (value instanceof Uint8Array) {
    return value.slice().buffer;
  } else if (value === void 0) {
    throw new ProtoError("Received unrecognized type of Value");
  } else {
    throw impossible(value, "Impossible type of Value");
  }
}
var minInteger, maxInteger;
var init_value = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/value.js"() {
    init_errors();
    init_util3();
    __name(valueToProto, "valueToProto");
    minInteger = -9223372036854775808n;
    maxInteger = 9223372036854775807n;
    __name(valueFromProto, "valueFromProto");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/result.js
function stmtResultFromProto(result) {
  return {
    affectedRowCount: result.affectedRowCount,
    lastInsertRowid: result.lastInsertRowid,
    columnNames: result.cols.map((col) => col.name),
    columnDecltypes: result.cols.map((col) => col.decltype)
  };
}
function rowsResultFromProto(result, intMode) {
  const stmtResult = stmtResultFromProto(result);
  const rows = result.rows.map((row) => rowFromProto(stmtResult.columnNames, row, intMode));
  return { ...stmtResult, rows };
}
function rowResultFromProto(result, intMode) {
  const stmtResult = stmtResultFromProto(result);
  let row;
  if (result.rows.length > 0) {
    row = rowFromProto(stmtResult.columnNames, result.rows[0], intMode);
  }
  return { ...stmtResult, row };
}
function valueResultFromProto(result, intMode) {
  const stmtResult = stmtResultFromProto(result);
  let value;
  if (result.rows.length > 0 && stmtResult.columnNames.length > 0) {
    value = valueFromProto(result.rows[0][0], intMode);
  }
  return { ...stmtResult, value };
}
function rowFromProto(colNames, values, intMode) {
  const row = {};
  Object.defineProperty(row, "length", { value: values.length });
  for (let i = 0; i < values.length; ++i) {
    const value = valueFromProto(values[i], intMode);
    Object.defineProperty(row, i, { value });
    const colName = colNames[i];
    if (colName !== void 0 && !Object.hasOwn(row, colName)) {
      Object.defineProperty(row, colName, { value, enumerable: true, configurable: true, writable: true });
    }
  }
  return row;
}
function errorFromProto(error) {
  return new ResponseError(error.message, error);
}
var init_result = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/result.js"() {
    init_errors();
    init_value();
    __name(stmtResultFromProto, "stmtResultFromProto");
    __name(rowsResultFromProto, "rowsResultFromProto");
    __name(rowResultFromProto, "rowResultFromProto");
    __name(valueResultFromProto, "valueResultFromProto");
    __name(rowFromProto, "rowFromProto");
    __name(errorFromProto, "errorFromProto");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/sql.js
function sqlToProto(owner, sql3) {
  if (sql3 instanceof Sql) {
    return { sqlId: sql3._getSqlId(owner) };
  } else {
    return { sql: "" + sql3 };
  }
}
var Sql;
var init_sql = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/sql.js"() {
    init_errors();
    Sql = class {
      static {
        __name(this, "Sql");
      }
      #owner;
      #sqlId;
      #closed;
      /** @private */
      constructor(owner, sqlId) {
        this.#owner = owner;
        this.#sqlId = sqlId;
        this.#closed = void 0;
      }
      /** @private */
      _getSqlId(owner) {
        if (this.#owner !== owner) {
          throw new MisuseError("Attempted to use SQL text opened with other object");
        } else if (this.#closed !== void 0) {
          throw new ClosedError("SQL text is closed", this.#closed);
        }
        return this.#sqlId;
      }
      /** Remove the SQL text from the server, releasing resouces. */
      close() {
        this._setClosed(new ClientError("SQL text was manually closed"));
      }
      /** @private */
      _setClosed(error) {
        if (this.#closed === void 0) {
          this.#closed = error;
          this.#owner._closeSql(this.#sqlId);
        }
      }
      /** True if the SQL text is closed (removed from the server). */
      get closed() {
        return this.#closed !== void 0;
      }
    };
    __name(sqlToProto, "sqlToProto");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/queue.js
var Queue;
var init_queue = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/queue.js"() {
    Queue = class {
      static {
        __name(this, "Queue");
      }
      #pushStack;
      #shiftStack;
      constructor() {
        this.#pushStack = [];
        this.#shiftStack = [];
      }
      get length() {
        return this.#pushStack.length + this.#shiftStack.length;
      }
      push(elem) {
        this.#pushStack.push(elem);
      }
      shift() {
        if (this.#shiftStack.length === 0 && this.#pushStack.length > 0) {
          this.#shiftStack = this.#pushStack.reverse();
          this.#pushStack = [];
        }
        return this.#shiftStack.pop();
      }
      first() {
        return this.#shiftStack.length !== 0 ? this.#shiftStack[this.#shiftStack.length - 1] : this.#pushStack[0];
      }
    };
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/stmt.js
function stmtToProto(sqlOwner, stmt, wantRows) {
  let inSql;
  let args = [];
  let namedArgs = [];
  if (stmt instanceof Stmt) {
    inSql = stmt.sql;
    args = stmt._args;
    for (const [name, value] of stmt._namedArgs.entries()) {
      namedArgs.push({ name, value });
    }
  } else if (Array.isArray(stmt)) {
    inSql = stmt[0];
    if (Array.isArray(stmt[1])) {
      args = stmt[1].map((arg) => valueToProto(arg));
    } else {
      namedArgs = Object.entries(stmt[1]).map(([name, value]) => {
        return { name, value: valueToProto(value) };
      });
    }
  } else {
    inSql = stmt;
  }
  const { sql: sql3, sqlId } = sqlToProto(sqlOwner, inSql);
  return { sql: sql3, sqlId, args, namedArgs, wantRows };
}
var Stmt;
var init_stmt = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/stmt.js"() {
    init_sql();
    init_value();
    Stmt = class {
      static {
        __name(this, "Stmt");
      }
      /** The SQL statement text. */
      sql;
      /** @private */
      _args;
      /** @private */
      _namedArgs;
      /** Initialize the statement with given SQL text. */
      constructor(sql3) {
        this.sql = sql3;
        this._args = [];
        this._namedArgs = /* @__PURE__ */ new Map();
      }
      /** Binds positional parameters from the given `values`. All previous positional bindings are cleared. */
      bindIndexes(values) {
        this._args.length = 0;
        for (const value of values) {
          this._args.push(valueToProto(value));
        }
        return this;
      }
      /** Binds a parameter by a 1-based index. */
      bindIndex(index, value) {
        if (index !== (index | 0) || index <= 0) {
          throw new RangeError("Index of a positional argument must be positive integer");
        }
        while (this._args.length < index) {
          this._args.push(null);
        }
        this._args[index - 1] = valueToProto(value);
        return this;
      }
      /** Binds a parameter by name. */
      bindName(name, value) {
        this._namedArgs.set(name, valueToProto(value));
        return this;
      }
      /** Clears all bindings. */
      unbindAll() {
        this._args.length = 0;
        this._namedArgs.clear();
        return this;
      }
    };
    __name(stmtToProto, "stmtToProto");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/batch.js
function executeRegular(stream, steps, batch) {
  return stream._batch(batch).then((result) => {
    for (let step = 0; step < steps.length; ++step) {
      const stepResult = result.stepResults.get(step);
      const stepError = result.stepErrors.get(step);
      steps[step].callback(stepResult, stepError);
    }
  });
}
async function executeCursor(stream, steps, batch) {
  const cursor = await stream._openCursor(batch);
  try {
    let nextStep = 0;
    let beginEntry = void 0;
    let rows = [];
    for (; ; ) {
      const entry = await cursor.next();
      if (entry === void 0) {
        break;
      }
      if (entry.type === "step_begin") {
        if (entry.step < nextStep || entry.step >= steps.length) {
          throw new ProtoError("Server produced StepBeginEntry for unexpected step");
        } else if (beginEntry !== void 0) {
          throw new ProtoError("Server produced StepBeginEntry before terminating previous step");
        }
        for (let step = nextStep; step < entry.step; ++step) {
          steps[step].callback(void 0, void 0);
        }
        nextStep = entry.step + 1;
        beginEntry = entry;
        rows = [];
      } else if (entry.type === "step_end") {
        if (beginEntry === void 0) {
          throw new ProtoError("Server produced StepEndEntry but no step is active");
        }
        const stmtResult = {
          cols: beginEntry.cols,
          rows,
          affectedRowCount: entry.affectedRowCount,
          lastInsertRowid: entry.lastInsertRowid
        };
        steps[beginEntry.step].callback(stmtResult, void 0);
        beginEntry = void 0;
        rows = [];
      } else if (entry.type === "step_error") {
        if (beginEntry === void 0) {
          if (entry.step >= steps.length) {
            throw new ProtoError("Server produced StepErrorEntry for unexpected step");
          }
          for (let step = nextStep; step < entry.step; ++step) {
            steps[step].callback(void 0, void 0);
          }
        } else {
          if (entry.step !== beginEntry.step) {
            throw new ProtoError("Server produced StepErrorEntry for unexpected step");
          }
          beginEntry = void 0;
          rows = [];
        }
        steps[entry.step].callback(void 0, entry.error);
        nextStep = entry.step + 1;
      } else if (entry.type === "row") {
        if (beginEntry === void 0) {
          throw new ProtoError("Server produced RowEntry but no step is active");
        }
        rows.push(entry.row);
      } else if (entry.type === "error") {
        throw errorFromProto(entry.error);
      } else if (entry.type === "none") {
        throw new ProtoError("Server produced unrecognized CursorEntry");
      } else {
        throw impossible(entry, "Impossible CursorEntry");
      }
    }
    if (beginEntry !== void 0) {
      throw new ProtoError("Server closed Cursor before terminating active step");
    }
    for (let step = nextStep; step < steps.length; ++step) {
      steps[step].callback(void 0, void 0);
    }
  } finally {
    cursor.close();
  }
}
function stepIndex(step) {
  if (step._index === void 0) {
    throw new MisuseError("Cannot add a condition referencing a step that has not been added to the batch");
  }
  return step._index;
}
function checkCondBatch(expectedBatch, cond) {
  if (cond._batch !== expectedBatch) {
    throw new MisuseError("Cannot mix BatchCond objects for different Batch objects");
  }
}
var Batch, BatchStep, BatchCond;
var init_batch = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/batch.js"() {
    init_errors();
    init_result();
    init_stmt();
    init_util3();
    Batch = class {
      static {
        __name(this, "Batch");
      }
      /** @private */
      _stream;
      #useCursor;
      /** @private */
      _steps;
      #executed;
      /** @private */
      constructor(stream, useCursor) {
        this._stream = stream;
        this.#useCursor = useCursor;
        this._steps = [];
        this.#executed = false;
      }
      /** Return a builder for adding a step to the batch. */
      step() {
        return new BatchStep(this);
      }
      /** Execute the batch. */
      execute() {
        if (this.#executed) {
          throw new MisuseError("This batch has already been executed");
        }
        this.#executed = true;
        const batch = {
          steps: this._steps.map((step) => step.proto)
        };
        if (this.#useCursor) {
          return executeCursor(this._stream, this._steps, batch);
        } else {
          return executeRegular(this._stream, this._steps, batch);
        }
      }
    };
    __name(executeRegular, "executeRegular");
    __name(executeCursor, "executeCursor");
    BatchStep = class {
      static {
        __name(this, "BatchStep");
      }
      /** @private */
      _batch;
      #conds;
      /** @private */
      _index;
      /** @private */
      constructor(batch) {
        this._batch = batch;
        this.#conds = [];
        this._index = void 0;
      }
      /** Add the condition that needs to be satisfied to execute the statement. If you use this method multiple
       * times, we join the conditions with a logical AND. */
      condition(cond) {
        this.#conds.push(cond._proto);
        return this;
      }
      /** Add a statement that returns rows. */
      query(stmt) {
        return this.#add(stmt, true, rowsResultFromProto);
      }
      /** Add a statement that returns at most a single row. */
      queryRow(stmt) {
        return this.#add(stmt, true, rowResultFromProto);
      }
      /** Add a statement that returns at most a single value. */
      queryValue(stmt) {
        return this.#add(stmt, true, valueResultFromProto);
      }
      /** Add a statement without returning rows. */
      run(stmt) {
        return this.#add(stmt, false, stmtResultFromProto);
      }
      #add(inStmt, wantRows, fromProto) {
        if (this._index !== void 0) {
          throw new MisuseError("This BatchStep has already been added to the batch");
        }
        const stmt = stmtToProto(this._batch._stream._sqlOwner(), inStmt, wantRows);
        let condition;
        if (this.#conds.length === 0) {
          condition = void 0;
        } else if (this.#conds.length === 1) {
          condition = this.#conds[0];
        } else {
          condition = { type: "and", conds: this.#conds.slice() };
        }
        const proto = { stmt, condition };
        return new Promise((outputCallback, errorCallback) => {
          const callback = /* @__PURE__ */ __name((stepResult, stepError) => {
            if (stepResult !== void 0 && stepError !== void 0) {
              errorCallback(new ProtoError("Server returned both result and error"));
            } else if (stepError !== void 0) {
              errorCallback(errorFromProto(stepError));
            } else if (stepResult !== void 0) {
              outputCallback(fromProto(stepResult, this._batch._stream.intMode));
            } else {
              outputCallback(void 0);
            }
          }, "callback");
          this._index = this._batch._steps.length;
          this._batch._steps.push({ proto, callback });
        });
      }
    };
    BatchCond = class _BatchCond {
      static {
        __name(this, "BatchCond");
      }
      /** @private */
      _batch;
      /** @private */
      _proto;
      /** @private */
      constructor(batch, proto) {
        this._batch = batch;
        this._proto = proto;
      }
      /** Create a condition that evaluates to true when the given step executes successfully.
       *
       * If the given step fails error or is skipped because its condition evaluated to false, this
       * condition evaluates to false.
       */
      static ok(step) {
        return new _BatchCond(step._batch, { type: "ok", step: stepIndex(step) });
      }
      /** Create a condition that evaluates to true when the given step fails.
       *
       * If the given step succeeds or is skipped because its condition evaluated to false, this condition
       * evaluates to false.
       */
      static error(step) {
        return new _BatchCond(step._batch, { type: "error", step: stepIndex(step) });
      }
      /** Create a condition that is a logical negation of another condition.
       */
      static not(cond) {
        return new _BatchCond(cond._batch, { type: "not", cond: cond._proto });
      }
      /** Create a condition that is a logical AND of other conditions.
       */
      static and(batch, conds) {
        for (const cond of conds) {
          checkCondBatch(batch, cond);
        }
        return new _BatchCond(batch, { type: "and", conds: conds.map((e) => e._proto) });
      }
      /** Create a condition that is a logical OR of other conditions.
       */
      static or(batch, conds) {
        for (const cond of conds) {
          checkCondBatch(batch, cond);
        }
        return new _BatchCond(batch, { type: "or", conds: conds.map((e) => e._proto) });
      }
      /** Create a condition that evaluates to true when the SQL connection is in autocommit mode (not inside an
       * explicit transaction). This requires protocol version 3 or higher.
       */
      static isAutocommit(batch) {
        batch._stream.client()._ensureVersion(3, "BatchCond.isAutocommit()");
        return new _BatchCond(batch, { type: "is_autocommit" });
      }
    };
    __name(stepIndex, "stepIndex");
    __name(checkCondBatch, "checkCondBatch");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/describe.js
function describeResultFromProto(result) {
  return {
    paramNames: result.params.map((p) => p.name),
    columns: result.cols,
    isExplain: result.isExplain,
    isReadonly: result.isReadonly
  };
}
var init_describe = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/describe.js"() {
    __name(describeResultFromProto, "describeResultFromProto");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/stream.js
var Stream;
var init_stream = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/stream.js"() {
    init_batch();
    init_describe();
    init_result();
    init_sql();
    init_stmt();
    Stream = class {
      static {
        __name(this, "Stream");
      }
      /** @private */
      constructor(intMode) {
        this.intMode = intMode;
      }
      /** Execute a statement and return rows. */
      query(stmt) {
        return this.#execute(stmt, true, rowsResultFromProto);
      }
      /** Execute a statement and return at most a single row. */
      queryRow(stmt) {
        return this.#execute(stmt, true, rowResultFromProto);
      }
      /** Execute a statement and return at most a single value. */
      queryValue(stmt) {
        return this.#execute(stmt, true, valueResultFromProto);
      }
      /** Execute a statement without returning rows. */
      run(stmt) {
        return this.#execute(stmt, false, stmtResultFromProto);
      }
      #execute(inStmt, wantRows, fromProto) {
        const stmt = stmtToProto(this._sqlOwner(), inStmt, wantRows);
        return this._execute(stmt).then((r) => fromProto(r, this.intMode));
      }
      /** Return a builder for creating and executing a batch.
       *
       * If `useCursor` is true, the batch will be executed using a Hrana cursor, which will stream results from
       * the server to the client, which consumes less memory on the server. This requires protocol version 3 or
       * higher.
       */
      batch(useCursor = false) {
        return new Batch(this, useCursor);
      }
      /** Parse and analyze a statement. This requires protocol version 2 or higher. */
      describe(inSql) {
        const protoSql = sqlToProto(this._sqlOwner(), inSql);
        return this._describe(protoSql).then(describeResultFromProto);
      }
      /** Execute a sequence of statements separated by semicolons. This requires protocol version 2 or higher.
       * */
      sequence(inSql) {
        const protoSql = sqlToProto(this._sqlOwner(), inSql);
        return this._sequence(protoSql);
      }
      /** Representation of integers returned from the database. See {@link IntMode}.
       *
       * This value affects the results of all operations on this stream.
       */
      intMode;
    };
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/cursor.js
var Cursor;
var init_cursor = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/cursor.js"() {
    Cursor = class {
      static {
        __name(this, "Cursor");
      }
    };
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/ws/cursor.js
var fetchChunkSize, fetchQueueSize, WsCursor;
var init_cursor2 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/ws/cursor.js"() {
    init_errors();
    init_cursor();
    init_queue();
    fetchChunkSize = 1e3;
    fetchQueueSize = 10;
    WsCursor = class extends Cursor {
      static {
        __name(this, "WsCursor");
      }
      #client;
      #stream;
      #cursorId;
      #entryQueue;
      #fetchQueue;
      #closed;
      #done;
      /** @private */
      constructor(client, stream, cursorId) {
        super();
        this.#client = client;
        this.#stream = stream;
        this.#cursorId = cursorId;
        this.#entryQueue = new Queue();
        this.#fetchQueue = new Queue();
        this.#closed = void 0;
        this.#done = false;
      }
      /** Fetch the next entry from the cursor. */
      async next() {
        for (; ; ) {
          if (this.#closed !== void 0) {
            throw new ClosedError("Cursor is closed", this.#closed);
          }
          while (!this.#done && this.#fetchQueue.length < fetchQueueSize) {
            this.#fetchQueue.push(this.#fetch());
          }
          const entry = this.#entryQueue.shift();
          if (this.#done || entry !== void 0) {
            return entry;
          }
          await this.#fetchQueue.shift().then((response) => {
            if (response === void 0) {
              return;
            }
            for (const entry2 of response.entries) {
              this.#entryQueue.push(entry2);
            }
            this.#done ||= response.done;
          });
        }
      }
      #fetch() {
        return this.#stream._sendCursorRequest(this, {
          type: "fetch_cursor",
          cursorId: this.#cursorId,
          maxCount: fetchChunkSize
        }).then((resp) => resp, (error) => {
          this._setClosed(error);
          return void 0;
        });
      }
      /** @private */
      _setClosed(error) {
        if (this.#closed !== void 0) {
          return;
        }
        this.#closed = error;
        this.#stream._sendCursorRequest(this, {
          type: "close_cursor",
          cursorId: this.#cursorId
        }).catch(() => void 0);
        this.#stream._cursorClosed(this);
      }
      /** Close the cursor. */
      close() {
        this._setClosed(new ClientError("Cursor was manually closed"));
      }
      /** True if the cursor is closed. */
      get closed() {
        return this.#closed !== void 0;
      }
    };
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/ws/stream.js
var WsStream;
var init_stream2 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/ws/stream.js"() {
    init_errors();
    init_queue();
    init_stream();
    init_cursor2();
    WsStream = class _WsStream extends Stream {
      static {
        __name(this, "WsStream");
      }
      #client;
      #streamId;
      #queue;
      #cursor;
      #closing;
      #closed;
      /** @private */
      static open(client) {
        const streamId = client._streamIdAlloc.alloc();
        const stream = new _WsStream(client, streamId);
        const responseCallback = /* @__PURE__ */ __name(() => void 0, "responseCallback");
        const errorCallback = /* @__PURE__ */ __name((e) => stream.#setClosed(e), "errorCallback");
        const request = { type: "open_stream", streamId };
        client._sendRequest(request, { responseCallback, errorCallback });
        return stream;
      }
      /** @private */
      constructor(client, streamId) {
        super(client.intMode);
        this.#client = client;
        this.#streamId = streamId;
        this.#queue = new Queue();
        this.#cursor = void 0;
        this.#closing = false;
        this.#closed = void 0;
      }
      /** Get the {@link WsClient} object that this stream belongs to. */
      client() {
        return this.#client;
      }
      /** @private */
      _sqlOwner() {
        return this.#client;
      }
      /** @private */
      _execute(stmt) {
        return this.#sendStreamRequest({
          type: "execute",
          streamId: this.#streamId,
          stmt
        }).then((response) => {
          return response.result;
        });
      }
      /** @private */
      _batch(batch) {
        return this.#sendStreamRequest({
          type: "batch",
          streamId: this.#streamId,
          batch
        }).then((response) => {
          return response.result;
        });
      }
      /** @private */
      _describe(protoSql) {
        this.#client._ensureVersion(2, "describe()");
        return this.#sendStreamRequest({
          type: "describe",
          streamId: this.#streamId,
          sql: protoSql.sql,
          sqlId: protoSql.sqlId
        }).then((response) => {
          return response.result;
        });
      }
      /** @private */
      _sequence(protoSql) {
        this.#client._ensureVersion(2, "sequence()");
        return this.#sendStreamRequest({
          type: "sequence",
          streamId: this.#streamId,
          sql: protoSql.sql,
          sqlId: protoSql.sqlId
        }).then((_response) => {
          return void 0;
        });
      }
      /** Check whether the SQL connection underlying this stream is in autocommit state (i.e., outside of an
       * explicit transaction). This requires protocol version 3 or higher.
       */
      getAutocommit() {
        this.#client._ensureVersion(3, "getAutocommit()");
        return this.#sendStreamRequest({
          type: "get_autocommit",
          streamId: this.#streamId
        }).then((response) => {
          return response.isAutocommit;
        });
      }
      #sendStreamRequest(request) {
        return new Promise((responseCallback, errorCallback) => {
          this.#pushToQueue({ type: "request", request, responseCallback, errorCallback });
        });
      }
      /** @private */
      _openCursor(batch) {
        this.#client._ensureVersion(3, "cursor");
        return new Promise((cursorCallback, errorCallback) => {
          this.#pushToQueue({ type: "cursor", batch, cursorCallback, errorCallback });
        });
      }
      /** @private */
      _sendCursorRequest(cursor, request) {
        if (cursor !== this.#cursor) {
          throw new InternalError("Cursor not associated with the stream attempted to execute a request");
        }
        return new Promise((responseCallback, errorCallback) => {
          if (this.#closed !== void 0) {
            errorCallback(new ClosedError("Stream is closed", this.#closed));
          } else {
            this.#client._sendRequest(request, { responseCallback, errorCallback });
          }
        });
      }
      /** @private */
      _cursorClosed(cursor) {
        if (cursor !== this.#cursor) {
          throw new InternalError("Cursor was closed, but it was not associated with the stream");
        }
        this.#cursor = void 0;
        this.#flushQueue();
      }
      #pushToQueue(entry) {
        if (this.#closed !== void 0) {
          entry.errorCallback(new ClosedError("Stream is closed", this.#closed));
        } else if (this.#closing) {
          entry.errorCallback(new ClosedError("Stream is closing", void 0));
        } else {
          this.#queue.push(entry);
          this.#flushQueue();
        }
      }
      #flushQueue() {
        for (; ; ) {
          const entry = this.#queue.first();
          if (entry === void 0 && this.#cursor === void 0 && this.#closing) {
            this.#setClosed(new ClientError("Stream was gracefully closed"));
            break;
          } else if (entry?.type === "request" && this.#cursor === void 0) {
            const { request, responseCallback, errorCallback } = entry;
            this.#queue.shift();
            this.#client._sendRequest(request, { responseCallback, errorCallback });
          } else if (entry?.type === "cursor" && this.#cursor === void 0) {
            const { batch, cursorCallback } = entry;
            this.#queue.shift();
            const cursorId = this.#client._cursorIdAlloc.alloc();
            const cursor = new WsCursor(this.#client, this, cursorId);
            const request = {
              type: "open_cursor",
              streamId: this.#streamId,
              cursorId,
              batch
            };
            const responseCallback = /* @__PURE__ */ __name(() => void 0, "responseCallback");
            const errorCallback = /* @__PURE__ */ __name((e) => cursor._setClosed(e), "errorCallback");
            this.#client._sendRequest(request, { responseCallback, errorCallback });
            this.#cursor = cursor;
            cursorCallback(cursor);
          } else {
            break;
          }
        }
      }
      #setClosed(error) {
        if (this.#closed !== void 0) {
          return;
        }
        this.#closed = error;
        if (this.#cursor !== void 0) {
          this.#cursor._setClosed(error);
        }
        for (; ; ) {
          const entry = this.#queue.shift();
          if (entry !== void 0) {
            entry.errorCallback(error);
          } else {
            break;
          }
        }
        const request = { type: "close_stream", streamId: this.#streamId };
        const responseCallback = /* @__PURE__ */ __name(() => this.#client._streamIdAlloc.free(this.#streamId), "responseCallback");
        const errorCallback = /* @__PURE__ */ __name(() => void 0, "errorCallback");
        this.#client._sendRequest(request, { responseCallback, errorCallback });
      }
      /** Immediately close the stream. */
      close() {
        this.#setClosed(new ClientError("Stream was manually closed"));
      }
      /** Gracefully close the stream. */
      closeGracefully() {
        this.#closing = true;
        this.#flushQueue();
      }
      /** True if the stream is closed or closing. */
      get closed() {
        return this.#closed !== void 0 || this.#closing;
      }
    };
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/shared/json_encode.js
function Stmt2(w, msg) {
  if (msg.sql !== void 0) {
    w.string("sql", msg.sql);
  }
  if (msg.sqlId !== void 0) {
    w.number("sql_id", msg.sqlId);
  }
  w.arrayObjects("args", msg.args, Value);
  w.arrayObjects("named_args", msg.namedArgs, NamedArg);
  w.boolean("want_rows", msg.wantRows);
}
function NamedArg(w, msg) {
  w.string("name", msg.name);
  w.object("value", msg.value, Value);
}
function Batch2(w, msg) {
  w.arrayObjects("steps", msg.steps, BatchStep2);
}
function BatchStep2(w, msg) {
  if (msg.condition !== void 0) {
    w.object("condition", msg.condition, BatchCond2);
  }
  w.object("stmt", msg.stmt, Stmt2);
}
function BatchCond2(w, msg) {
  w.stringRaw("type", msg.type);
  if (msg.type === "ok" || msg.type === "error") {
    w.number("step", msg.step);
  } else if (msg.type === "not") {
    w.object("cond", msg.cond, BatchCond2);
  } else if (msg.type === "and" || msg.type === "or") {
    w.arrayObjects("conds", msg.conds, BatchCond2);
  } else if (msg.type === "is_autocommit") {
  } else {
    throw impossible(msg, "Impossible type of BatchCond");
  }
}
function Value(w, msg) {
  if (msg === null) {
    w.stringRaw("type", "null");
  } else if (typeof msg === "bigint") {
    w.stringRaw("type", "integer");
    w.stringRaw("value", "" + msg);
  } else if (typeof msg === "number") {
    w.stringRaw("type", "float");
    w.number("value", msg);
  } else if (typeof msg === "string") {
    w.stringRaw("type", "text");
    w.string("value", msg);
  } else if (msg instanceof Uint8Array) {
    w.stringRaw("type", "blob");
    w.stringRaw("base64", gBase64.fromUint8Array(msg));
  } else if (msg === void 0) {
  } else {
    throw impossible(msg, "Impossible type of Value");
  }
}
var init_json_encode = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/shared/json_encode.js"() {
    init_base64();
    init_util3();
    __name(Stmt2, "Stmt");
    __name(NamedArg, "NamedArg");
    __name(Batch2, "Batch");
    __name(BatchStep2, "BatchStep");
    __name(BatchCond2, "BatchCond");
    __name(Value, "Value");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/ws/json_encode.js
function ClientMsg(w, msg) {
  w.stringRaw("type", msg.type);
  if (msg.type === "hello") {
    if (msg.jwt !== void 0) {
      w.string("jwt", msg.jwt);
    }
  } else if (msg.type === "request") {
    w.number("request_id", msg.requestId);
    w.object("request", msg.request, Request2);
  } else {
    throw impossible(msg, "Impossible type of ClientMsg");
  }
}
function Request2(w, msg) {
  w.stringRaw("type", msg.type);
  if (msg.type === "open_stream") {
    w.number("stream_id", msg.streamId);
  } else if (msg.type === "close_stream") {
    w.number("stream_id", msg.streamId);
  } else if (msg.type === "execute") {
    w.number("stream_id", msg.streamId);
    w.object("stmt", msg.stmt, Stmt2);
  } else if (msg.type === "batch") {
    w.number("stream_id", msg.streamId);
    w.object("batch", msg.batch, Batch2);
  } else if (msg.type === "open_cursor") {
    w.number("stream_id", msg.streamId);
    w.number("cursor_id", msg.cursorId);
    w.object("batch", msg.batch, Batch2);
  } else if (msg.type === "close_cursor") {
    w.number("cursor_id", msg.cursorId);
  } else if (msg.type === "fetch_cursor") {
    w.number("cursor_id", msg.cursorId);
    w.number("max_count", msg.maxCount);
  } else if (msg.type === "sequence") {
    w.number("stream_id", msg.streamId);
    if (msg.sql !== void 0) {
      w.string("sql", msg.sql);
    }
    if (msg.sqlId !== void 0) {
      w.number("sql_id", msg.sqlId);
    }
  } else if (msg.type === "describe") {
    w.number("stream_id", msg.streamId);
    if (msg.sql !== void 0) {
      w.string("sql", msg.sql);
    }
    if (msg.sqlId !== void 0) {
      w.number("sql_id", msg.sqlId);
    }
  } else if (msg.type === "store_sql") {
    w.number("sql_id", msg.sqlId);
    w.string("sql", msg.sql);
  } else if (msg.type === "close_sql") {
    w.number("sql_id", msg.sqlId);
  } else if (msg.type === "get_autocommit") {
    w.number("stream_id", msg.streamId);
  } else {
    throw impossible(msg, "Impossible type of Request");
  }
}
var init_json_encode2 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/ws/json_encode.js"() {
    init_json_encode();
    init_util3();
    __name(ClientMsg, "ClientMsg");
    __name(Request2, "Request");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/shared/protobuf_encode.js
function Stmt3(w, msg) {
  if (msg.sql !== void 0) {
    w.string(1, msg.sql);
  }
  if (msg.sqlId !== void 0) {
    w.int32(2, msg.sqlId);
  }
  for (const arg of msg.args) {
    w.message(3, arg, Value2);
  }
  for (const arg of msg.namedArgs) {
    w.message(4, arg, NamedArg2);
  }
  w.bool(5, msg.wantRows);
}
function NamedArg2(w, msg) {
  w.string(1, msg.name);
  w.message(2, msg.value, Value2);
}
function Batch3(w, msg) {
  for (const step of msg.steps) {
    w.message(1, step, BatchStep3);
  }
}
function BatchStep3(w, msg) {
  if (msg.condition !== void 0) {
    w.message(1, msg.condition, BatchCond3);
  }
  w.message(2, msg.stmt, Stmt3);
}
function BatchCond3(w, msg) {
  if (msg.type === "ok") {
    w.uint32(1, msg.step);
  } else if (msg.type === "error") {
    w.uint32(2, msg.step);
  } else if (msg.type === "not") {
    w.message(3, msg.cond, BatchCond3);
  } else if (msg.type === "and") {
    w.message(4, msg.conds, BatchCondList);
  } else if (msg.type === "or") {
    w.message(5, msg.conds, BatchCondList);
  } else if (msg.type === "is_autocommit") {
    w.message(6, void 0, Empty);
  } else {
    throw impossible(msg, "Impossible type of BatchCond");
  }
}
function BatchCondList(w, msg) {
  for (const cond of msg) {
    w.message(1, cond, BatchCond3);
  }
}
function Value2(w, msg) {
  if (msg === null) {
    w.message(1, void 0, Empty);
  } else if (typeof msg === "bigint") {
    w.sint64(2, msg);
  } else if (typeof msg === "number") {
    w.double(3, msg);
  } else if (typeof msg === "string") {
    w.string(4, msg);
  } else if (msg instanceof Uint8Array) {
    w.bytes(5, msg);
  } else if (msg === void 0) {
  } else {
    throw impossible(msg, "Impossible type of Value");
  }
}
function Empty(_w, _msg) {
}
var init_protobuf_encode = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/shared/protobuf_encode.js"() {
    init_util3();
    __name(Stmt3, "Stmt");
    __name(NamedArg2, "NamedArg");
    __name(Batch3, "Batch");
    __name(BatchStep3, "BatchStep");
    __name(BatchCond3, "BatchCond");
    __name(BatchCondList, "BatchCondList");
    __name(Value2, "Value");
    __name(Empty, "Empty");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/ws/protobuf_encode.js
function ClientMsg2(w, msg) {
  if (msg.type === "hello") {
    w.message(1, msg, HelloMsg);
  } else if (msg.type === "request") {
    w.message(2, msg, RequestMsg);
  } else {
    throw impossible(msg, "Impossible type of ClientMsg");
  }
}
function HelloMsg(w, msg) {
  if (msg.jwt !== void 0) {
    w.string(1, msg.jwt);
  }
}
function RequestMsg(w, msg) {
  w.int32(1, msg.requestId);
  const request = msg.request;
  if (request.type === "open_stream") {
    w.message(2, request, OpenStreamReq);
  } else if (request.type === "close_stream") {
    w.message(3, request, CloseStreamReq);
  } else if (request.type === "execute") {
    w.message(4, request, ExecuteReq);
  } else if (request.type === "batch") {
    w.message(5, request, BatchReq);
  } else if (request.type === "open_cursor") {
    w.message(6, request, OpenCursorReq);
  } else if (request.type === "close_cursor") {
    w.message(7, request, CloseCursorReq);
  } else if (request.type === "fetch_cursor") {
    w.message(8, request, FetchCursorReq);
  } else if (request.type === "sequence") {
    w.message(9, request, SequenceReq);
  } else if (request.type === "describe") {
    w.message(10, request, DescribeReq);
  } else if (request.type === "store_sql") {
    w.message(11, request, StoreSqlReq);
  } else if (request.type === "close_sql") {
    w.message(12, request, CloseSqlReq);
  } else if (request.type === "get_autocommit") {
    w.message(13, request, GetAutocommitReq);
  } else {
    throw impossible(request, "Impossible type of Request");
  }
}
function OpenStreamReq(w, msg) {
  w.int32(1, msg.streamId);
}
function CloseStreamReq(w, msg) {
  w.int32(1, msg.streamId);
}
function ExecuteReq(w, msg) {
  w.int32(1, msg.streamId);
  w.message(2, msg.stmt, Stmt3);
}
function BatchReq(w, msg) {
  w.int32(1, msg.streamId);
  w.message(2, msg.batch, Batch3);
}
function OpenCursorReq(w, msg) {
  w.int32(1, msg.streamId);
  w.int32(2, msg.cursorId);
  w.message(3, msg.batch, Batch3);
}
function CloseCursorReq(w, msg) {
  w.int32(1, msg.cursorId);
}
function FetchCursorReq(w, msg) {
  w.int32(1, msg.cursorId);
  w.uint32(2, msg.maxCount);
}
function SequenceReq(w, msg) {
  w.int32(1, msg.streamId);
  if (msg.sql !== void 0) {
    w.string(2, msg.sql);
  }
  if (msg.sqlId !== void 0) {
    w.int32(3, msg.sqlId);
  }
}
function DescribeReq(w, msg) {
  w.int32(1, msg.streamId);
  if (msg.sql !== void 0) {
    w.string(2, msg.sql);
  }
  if (msg.sqlId !== void 0) {
    w.int32(3, msg.sqlId);
  }
}
function StoreSqlReq(w, msg) {
  w.int32(1, msg.sqlId);
  w.string(2, msg.sql);
}
function CloseSqlReq(w, msg) {
  w.int32(1, msg.sqlId);
}
function GetAutocommitReq(w, msg) {
  w.int32(1, msg.streamId);
}
var init_protobuf_encode2 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/ws/protobuf_encode.js"() {
    init_protobuf_encode();
    init_util3();
    __name(ClientMsg2, "ClientMsg");
    __name(HelloMsg, "HelloMsg");
    __name(RequestMsg, "RequestMsg");
    __name(OpenStreamReq, "OpenStreamReq");
    __name(CloseStreamReq, "CloseStreamReq");
    __name(ExecuteReq, "ExecuteReq");
    __name(BatchReq, "BatchReq");
    __name(OpenCursorReq, "OpenCursorReq");
    __name(CloseCursorReq, "CloseCursorReq");
    __name(FetchCursorReq, "FetchCursorReq");
    __name(SequenceReq, "SequenceReq");
    __name(DescribeReq, "DescribeReq");
    __name(StoreSqlReq, "StoreSqlReq");
    __name(CloseSqlReq, "CloseSqlReq");
    __name(GetAutocommitReq, "GetAutocommitReq");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/shared/json_decode.js
function Error2(obj) {
  const message = string(obj["message"]);
  const code = stringOpt(obj["code"]);
  return { message, code };
}
function StmtResult(obj) {
  const cols = arrayObjectsMap(obj["cols"], Col);
  const rows = array(obj["rows"]).map((rowObj) => arrayObjectsMap(rowObj, Value3));
  const affectedRowCount = number(obj["affected_row_count"]);
  const lastInsertRowidStr = stringOpt(obj["last_insert_rowid"]);
  const lastInsertRowid = lastInsertRowidStr !== void 0 ? BigInt(lastInsertRowidStr) : void 0;
  return { cols, rows, affectedRowCount, lastInsertRowid };
}
function Col(obj) {
  const name = stringOpt(obj["name"]);
  const decltype = stringOpt(obj["decltype"]);
  return { name, decltype };
}
function BatchResult(obj) {
  const stepResults = /* @__PURE__ */ new Map();
  array(obj["step_results"]).forEach((value, i) => {
    if (value !== null) {
      stepResults.set(i, StmtResult(object(value)));
    }
  });
  const stepErrors = /* @__PURE__ */ new Map();
  array(obj["step_errors"]).forEach((value, i) => {
    if (value !== null) {
      stepErrors.set(i, Error2(object(value)));
    }
  });
  return { stepResults, stepErrors };
}
function CursorEntry(obj) {
  const type = string(obj["type"]);
  if (type === "step_begin") {
    const step = number(obj["step"]);
    const cols = arrayObjectsMap(obj["cols"], Col);
    return { type: "step_begin", step, cols };
  } else if (type === "step_end") {
    const affectedRowCount = number(obj["affected_row_count"]);
    const lastInsertRowidStr = stringOpt(obj["last_insert_rowid"]);
    const lastInsertRowid = lastInsertRowidStr !== void 0 ? BigInt(lastInsertRowidStr) : void 0;
    return { type: "step_end", affectedRowCount, lastInsertRowid };
  } else if (type === "step_error") {
    const step = number(obj["step"]);
    const error = Error2(object(obj["error"]));
    return { type: "step_error", step, error };
  } else if (type === "row") {
    const row = arrayObjectsMap(obj["row"], Value3);
    return { type: "row", row };
  } else if (type === "error") {
    const error = Error2(object(obj["error"]));
    return { type: "error", error };
  } else {
    throw new ProtoError("Unexpected type of CursorEntry");
  }
}
function DescribeResult(obj) {
  const params = arrayObjectsMap(obj["params"], DescribeParam);
  const cols = arrayObjectsMap(obj["cols"], DescribeCol);
  const isExplain = boolean(obj["is_explain"]);
  const isReadonly = boolean(obj["is_readonly"]);
  return { params, cols, isExplain, isReadonly };
}
function DescribeParam(obj) {
  const name = stringOpt(obj["name"]);
  return { name };
}
function DescribeCol(obj) {
  const name = string(obj["name"]);
  const decltype = stringOpt(obj["decltype"]);
  return { name, decltype };
}
function Value3(obj) {
  const type = string(obj["type"]);
  if (type === "null") {
    return null;
  } else if (type === "integer") {
    const value = string(obj["value"]);
    return BigInt(value);
  } else if (type === "float") {
    return number(obj["value"]);
  } else if (type === "text") {
    return string(obj["value"]);
  } else if (type === "blob") {
    return gBase64.toUint8Array(string(obj["base64"]));
  } else {
    throw new ProtoError("Unexpected type of Value");
  }
}
var init_json_decode = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/shared/json_decode.js"() {
    init_base64();
    init_errors();
    init_decode();
    __name(Error2, "Error");
    __name(StmtResult, "StmtResult");
    __name(Col, "Col");
    __name(BatchResult, "BatchResult");
    __name(CursorEntry, "CursorEntry");
    __name(DescribeResult, "DescribeResult");
    __name(DescribeParam, "DescribeParam");
    __name(DescribeCol, "DescribeCol");
    __name(Value3, "Value");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/ws/json_decode.js
function ServerMsg(obj) {
  const type = string(obj["type"]);
  if (type === "hello_ok") {
    return { type: "hello_ok" };
  } else if (type === "hello_error") {
    const error = Error2(object(obj["error"]));
    return { type: "hello_error", error };
  } else if (type === "response_ok") {
    const requestId = number(obj["request_id"]);
    const response = Response2(object(obj["response"]));
    return { type: "response_ok", requestId, response };
  } else if (type === "response_error") {
    const requestId = number(obj["request_id"]);
    const error = Error2(object(obj["error"]));
    return { type: "response_error", requestId, error };
  } else {
    throw new ProtoError("Unexpected type of ServerMsg");
  }
}
function Response2(obj) {
  const type = string(obj["type"]);
  if (type === "open_stream") {
    return { type: "open_stream" };
  } else if (type === "close_stream") {
    return { type: "close_stream" };
  } else if (type === "execute") {
    const result = StmtResult(object(obj["result"]));
    return { type: "execute", result };
  } else if (type === "batch") {
    const result = BatchResult(object(obj["result"]));
    return { type: "batch", result };
  } else if (type === "open_cursor") {
    return { type: "open_cursor" };
  } else if (type === "close_cursor") {
    return { type: "close_cursor" };
  } else if (type === "fetch_cursor") {
    const entries = arrayObjectsMap(obj["entries"], CursorEntry);
    const done = boolean(obj["done"]);
    return { type: "fetch_cursor", entries, done };
  } else if (type === "sequence") {
    return { type: "sequence" };
  } else if (type === "describe") {
    const result = DescribeResult(object(obj["result"]));
    return { type: "describe", result };
  } else if (type === "store_sql") {
    return { type: "store_sql" };
  } else if (type === "close_sql") {
    return { type: "close_sql" };
  } else if (type === "get_autocommit") {
    const isAutocommit = boolean(obj["is_autocommit"]);
    return { type: "get_autocommit", isAutocommit };
  } else {
    throw new ProtoError("Unexpected type of Response");
  }
}
var init_json_decode2 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/ws/json_decode.js"() {
    init_errors();
    init_decode();
    init_json_decode();
    __name(ServerMsg, "ServerMsg");
    __name(Response2, "Response");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/shared/protobuf_decode.js
var Error3, StmtResult2, Col2, Row, BatchResult2, BatchResultStepResult, BatchResultStepError, CursorEntry2, StepBeginEntry, StepEndEntry, StepErrorEntry, DescribeResult2, DescribeParam2, DescribeCol2, Value4;
var init_protobuf_decode = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/shared/protobuf_decode.js"() {
    Error3 = {
      default() {
        return { message: "", code: void 0 };
      },
      1(r, msg) {
        msg.message = r.string();
      },
      2(r, msg) {
        msg.code = r.string();
      }
    };
    StmtResult2 = {
      default() {
        return {
          cols: [],
          rows: [],
          affectedRowCount: 0,
          lastInsertRowid: void 0
        };
      },
      1(r, msg) {
        msg.cols.push(r.message(Col2));
      },
      2(r, msg) {
        msg.rows.push(r.message(Row));
      },
      3(r, msg) {
        msg.affectedRowCount = Number(r.uint64());
      },
      4(r, msg) {
        msg.lastInsertRowid = r.sint64();
      }
    };
    Col2 = {
      default() {
        return { name: void 0, decltype: void 0 };
      },
      1(r, msg) {
        msg.name = r.string();
      },
      2(r, msg) {
        msg.decltype = r.string();
      }
    };
    Row = {
      default() {
        return [];
      },
      1(r, msg) {
        msg.push(r.message(Value4));
      }
    };
    BatchResult2 = {
      default() {
        return { stepResults: /* @__PURE__ */ new Map(), stepErrors: /* @__PURE__ */ new Map() };
      },
      1(r, msg) {
        const [key, value] = r.message(BatchResultStepResult);
        msg.stepResults.set(key, value);
      },
      2(r, msg) {
        const [key, value] = r.message(BatchResultStepError);
        msg.stepErrors.set(key, value);
      }
    };
    BatchResultStepResult = {
      default() {
        return [0, StmtResult2.default()];
      },
      1(r, msg) {
        msg[0] = r.uint32();
      },
      2(r, msg) {
        msg[1] = r.message(StmtResult2);
      }
    };
    BatchResultStepError = {
      default() {
        return [0, Error3.default()];
      },
      1(r, msg) {
        msg[0] = r.uint32();
      },
      2(r, msg) {
        msg[1] = r.message(Error3);
      }
    };
    CursorEntry2 = {
      default() {
        return { type: "none" };
      },
      1(r) {
        return r.message(StepBeginEntry);
      },
      2(r) {
        return r.message(StepEndEntry);
      },
      3(r) {
        return r.message(StepErrorEntry);
      },
      4(r) {
        return { type: "row", row: r.message(Row) };
      },
      5(r) {
        return { type: "error", error: r.message(Error3) };
      }
    };
    StepBeginEntry = {
      default() {
        return { type: "step_begin", step: 0, cols: [] };
      },
      1(r, msg) {
        msg.step = r.uint32();
      },
      2(r, msg) {
        msg.cols.push(r.message(Col2));
      }
    };
    StepEndEntry = {
      default() {
        return {
          type: "step_end",
          affectedRowCount: 0,
          lastInsertRowid: void 0
        };
      },
      1(r, msg) {
        msg.affectedRowCount = r.uint32();
      },
      2(r, msg) {
        msg.lastInsertRowid = r.uint64();
      }
    };
    StepErrorEntry = {
      default() {
        return {
          type: "step_error",
          step: 0,
          error: Error3.default()
        };
      },
      1(r, msg) {
        msg.step = r.uint32();
      },
      2(r, msg) {
        msg.error = r.message(Error3);
      }
    };
    DescribeResult2 = {
      default() {
        return {
          params: [],
          cols: [],
          isExplain: false,
          isReadonly: false
        };
      },
      1(r, msg) {
        msg.params.push(r.message(DescribeParam2));
      },
      2(r, msg) {
        msg.cols.push(r.message(DescribeCol2));
      },
      3(r, msg) {
        msg.isExplain = r.bool();
      },
      4(r, msg) {
        msg.isReadonly = r.bool();
      }
    };
    DescribeParam2 = {
      default() {
        return { name: void 0 };
      },
      1(r, msg) {
        msg.name = r.string();
      }
    };
    DescribeCol2 = {
      default() {
        return { name: "", decltype: void 0 };
      },
      1(r, msg) {
        msg.name = r.string();
      },
      2(r, msg) {
        msg.decltype = r.string();
      }
    };
    Value4 = {
      default() {
        return void 0;
      },
      1(r) {
        return null;
      },
      2(r) {
        return r.sint64();
      },
      3(r) {
        return r.double();
      },
      4(r) {
        return r.string();
      },
      5(r) {
        return r.bytes();
      }
    };
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/ws/protobuf_decode.js
var ServerMsg2, HelloErrorMsg, ResponseErrorMsg, ResponseOkMsg, ExecuteResp, BatchResp, FetchCursorResp, DescribeResp, GetAutocommitResp;
var init_protobuf_decode2 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/ws/protobuf_decode.js"() {
    init_protobuf_decode();
    ServerMsg2 = {
      default() {
        return { type: "none" };
      },
      1(r) {
        return { type: "hello_ok" };
      },
      2(r) {
        return r.message(HelloErrorMsg);
      },
      3(r) {
        return r.message(ResponseOkMsg);
      },
      4(r) {
        return r.message(ResponseErrorMsg);
      }
    };
    HelloErrorMsg = {
      default() {
        return { type: "hello_error", error: Error3.default() };
      },
      1(r, msg) {
        msg.error = r.message(Error3);
      }
    };
    ResponseErrorMsg = {
      default() {
        return { type: "response_error", requestId: 0, error: Error3.default() };
      },
      1(r, msg) {
        msg.requestId = r.int32();
      },
      2(r, msg) {
        msg.error = r.message(Error3);
      }
    };
    ResponseOkMsg = {
      default() {
        return {
          type: "response_ok",
          requestId: 0,
          response: { type: "none" }
        };
      },
      1(r, msg) {
        msg.requestId = r.int32();
      },
      2(r, msg) {
        msg.response = { type: "open_stream" };
      },
      3(r, msg) {
        msg.response = { type: "close_stream" };
      },
      4(r, msg) {
        msg.response = r.message(ExecuteResp);
      },
      5(r, msg) {
        msg.response = r.message(BatchResp);
      },
      6(r, msg) {
        msg.response = { type: "open_cursor" };
      },
      7(r, msg) {
        msg.response = { type: "close_cursor" };
      },
      8(r, msg) {
        msg.response = r.message(FetchCursorResp);
      },
      9(r, msg) {
        msg.response = { type: "sequence" };
      },
      10(r, msg) {
        msg.response = r.message(DescribeResp);
      },
      11(r, msg) {
        msg.response = { type: "store_sql" };
      },
      12(r, msg) {
        msg.response = { type: "close_sql" };
      },
      13(r, msg) {
        msg.response = r.message(GetAutocommitResp);
      }
    };
    ExecuteResp = {
      default() {
        return { type: "execute", result: StmtResult2.default() };
      },
      1(r, msg) {
        msg.result = r.message(StmtResult2);
      }
    };
    BatchResp = {
      default() {
        return { type: "batch", result: BatchResult2.default() };
      },
      1(r, msg) {
        msg.result = r.message(BatchResult2);
      }
    };
    FetchCursorResp = {
      default() {
        return { type: "fetch_cursor", entries: [], done: false };
      },
      1(r, msg) {
        msg.entries.push(r.message(CursorEntry2));
      },
      2(r, msg) {
        msg.done = r.bool();
      }
    };
    DescribeResp = {
      default() {
        return { type: "describe", result: DescribeResult2.default() };
      },
      1(r, msg) {
        msg.result = r.message(DescribeResult2);
      }
    };
    GetAutocommitResp = {
      default() {
        return { type: "get_autocommit", isAutocommit: false };
      },
      1(r, msg) {
        msg.isAutocommit = r.bool();
      }
    };
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/ws/client.js
var subprotocolsV2, subprotocolsV3, WsClient;
var init_client2 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/ws/client.js"() {
    init_client();
    init_encoding();
    init_errors();
    init_id_alloc();
    init_result();
    init_sql();
    init_util3();
    init_stream2();
    init_json_encode2();
    init_protobuf_encode2();
    init_json_decode2();
    init_protobuf_decode2();
    subprotocolsV2 = /* @__PURE__ */ new Map([
      ["hrana2", { version: 2, encoding: "json" }],
      ["hrana1", { version: 1, encoding: "json" }]
    ]);
    subprotocolsV3 = /* @__PURE__ */ new Map([
      ["hrana3-protobuf", { version: 3, encoding: "protobuf" }],
      ["hrana3", { version: 3, encoding: "json" }],
      ["hrana2", { version: 2, encoding: "json" }],
      ["hrana1", { version: 1, encoding: "json" }]
    ]);
    WsClient = class extends Client {
      static {
        __name(this, "WsClient");
      }
      #socket;
      // List of callbacks that we queue until the socket transitions from the CONNECTING to the OPEN state.
      #openCallbacks;
      // Have we already transitioned from CONNECTING to OPEN and fired the callbacks in #openCallbacks?
      #opened;
      // Stores the error that caused us to close the client (and the socket). If we are not closed, this is
      // `undefined`.
      #closed;
      // Have we received a response to our "hello" from the server?
      #recvdHello;
      // Subprotocol negotiated with the server. It is only available after the socket transitions to the OPEN
      // state.
      #subprotocol;
      // Has the `getVersion()` function been called? This is only used to validate that the API is used
      // correctly.
      #getVersionCalled;
      // A map from request id to the responses that we expect to receive from the server.
      #responseMap;
      // An allocator of request ids.
      #requestIdAlloc;
      // An allocator of stream ids.
      /** @private */
      _streamIdAlloc;
      // An allocator of cursor ids.
      /** @private */
      _cursorIdAlloc;
      // An allocator of SQL text ids.
      #sqlIdAlloc;
      /** @private */
      constructor(socket, jwt) {
        super();
        this.#socket = socket;
        this.#openCallbacks = [];
        this.#opened = false;
        this.#closed = void 0;
        this.#recvdHello = false;
        this.#subprotocol = void 0;
        this.#getVersionCalled = false;
        this.#responseMap = /* @__PURE__ */ new Map();
        this.#requestIdAlloc = new IdAlloc();
        this._streamIdAlloc = new IdAlloc();
        this._cursorIdAlloc = new IdAlloc();
        this.#sqlIdAlloc = new IdAlloc();
        this.#socket.binaryType = "arraybuffer";
        this.#socket.addEventListener("open", () => this.#onSocketOpen());
        this.#socket.addEventListener("close", (event) => this.#onSocketClose(event));
        this.#socket.addEventListener("error", (event) => this.#onSocketError(event));
        this.#socket.addEventListener("message", (event) => this.#onSocketMessage(event));
        this.#send({ type: "hello", jwt });
      }
      // Send (or enqueue to send) a message to the server.
      #send(msg) {
        if (this.#closed !== void 0) {
          throw new InternalError("Trying to send a message on a closed client");
        }
        if (this.#opened) {
          this.#sendToSocket(msg);
        } else {
          const openCallback = /* @__PURE__ */ __name(() => this.#sendToSocket(msg), "openCallback");
          const errorCallback = /* @__PURE__ */ __name(() => void 0, "errorCallback");
          this.#openCallbacks.push({ openCallback, errorCallback });
        }
      }
      // The socket transitioned from CONNECTING to OPEN
      #onSocketOpen() {
        const protocol = this.#socket.protocol;
        if (protocol === void 0) {
          this.#setClosed(new ClientError("The `WebSocket.protocol` property is undefined. This most likely means that the WebSocket implementation provided by the environment is broken. If you are using Miniflare 2, please update to Miniflare 3, which fixes this problem."));
          return;
        } else if (protocol === "") {
          this.#subprotocol = { version: 1, encoding: "json" };
        } else {
          this.#subprotocol = subprotocolsV3.get(protocol);
          if (this.#subprotocol === void 0) {
            this.#setClosed(new ProtoError(`Unrecognized WebSocket subprotocol: ${JSON.stringify(protocol)}`));
            return;
          }
        }
        for (const callbacks of this.#openCallbacks) {
          callbacks.openCallback();
        }
        this.#openCallbacks.length = 0;
        this.#opened = true;
      }
      #sendToSocket(msg) {
        const encoding = this.#subprotocol.encoding;
        if (encoding === "json") {
          const jsonMsg = writeJsonObject(msg, ClientMsg);
          this.#socket.send(jsonMsg);
        } else if (encoding === "protobuf") {
          const protobufMsg = writeProtobufMessage(msg, ClientMsg2);
          this.#socket.send(protobufMsg);
        } else {
          throw impossible(encoding, "Impossible encoding");
        }
      }
      /** Get the protocol version negotiated with the server, possibly waiting until the socket is open. */
      getVersion() {
        return new Promise((versionCallback, errorCallback) => {
          this.#getVersionCalled = true;
          if (this.#closed !== void 0) {
            errorCallback(this.#closed);
          } else if (!this.#opened) {
            const openCallback = /* @__PURE__ */ __name(() => versionCallback(this.#subprotocol.version), "openCallback");
            this.#openCallbacks.push({ openCallback, errorCallback });
          } else {
            versionCallback(this.#subprotocol.version);
          }
        });
      }
      // Make sure that the negotiated version is at least `minVersion`.
      /** @private */
      _ensureVersion(minVersion, feature2) {
        if (this.#subprotocol === void 0 || !this.#getVersionCalled) {
          throw new ProtocolVersionError(`${feature2} is supported only on protocol version ${minVersion} and higher, but the version supported by the WebSocket server is not yet known. Use Client.getVersion() to wait until the version is available.`);
        } else if (this.#subprotocol.version < minVersion) {
          throw new ProtocolVersionError(`${feature2} is supported on protocol version ${minVersion} and higher, but the WebSocket server only supports version ${this.#subprotocol.version}`);
        }
      }
      // Send a request to the server and invoke a callback when we get the response.
      /** @private */
      _sendRequest(request, callbacks) {
        if (this.#closed !== void 0) {
          callbacks.errorCallback(new ClosedError("Client is closed", this.#closed));
          return;
        }
        const requestId = this.#requestIdAlloc.alloc();
        this.#responseMap.set(requestId, { ...callbacks, type: request.type });
        this.#send({ type: "request", requestId, request });
      }
      // The socket encountered an error.
      #onSocketError(event) {
        const eventMessage = event.message;
        const message = eventMessage ?? "WebSocket was closed due to an error";
        this.#setClosed(new WebSocketError(message));
      }
      // The socket was closed.
      #onSocketClose(event) {
        let message = `WebSocket was closed with code ${event.code}`;
        if (event.reason) {
          message += `: ${event.reason}`;
        }
        this.#setClosed(new WebSocketError(message));
      }
      // Close the client with the given error.
      #setClosed(error) {
        if (this.#closed !== void 0) {
          return;
        }
        this.#closed = error;
        for (const callbacks of this.#openCallbacks) {
          callbacks.errorCallback(error);
        }
        this.#openCallbacks.length = 0;
        for (const [requestId, responseState] of this.#responseMap.entries()) {
          responseState.errorCallback(error);
          this.#requestIdAlloc.free(requestId);
        }
        this.#responseMap.clear();
        this.#socket.close();
      }
      // We received a message from the socket.
      #onSocketMessage(event) {
        if (this.#closed !== void 0) {
          return;
        }
        try {
          let msg;
          const encoding = this.#subprotocol.encoding;
          if (encoding === "json") {
            if (typeof event.data !== "string") {
              this.#socket.close(3003, "Only text messages are accepted with JSON encoding");
              this.#setClosed(new ProtoError("Received non-text message from server with JSON encoding"));
              return;
            }
            msg = readJsonObject(JSON.parse(event.data), ServerMsg);
          } else if (encoding === "protobuf") {
            if (!(event.data instanceof ArrayBuffer)) {
              this.#socket.close(3003, "Only binary messages are accepted with Protobuf encoding");
              this.#setClosed(new ProtoError("Received non-binary message from server with Protobuf encoding"));
              return;
            }
            msg = readProtobufMessage(new Uint8Array(event.data), ServerMsg2);
          } else {
            throw impossible(encoding, "Impossible encoding");
          }
          this.#handleMsg(msg);
        } catch (e) {
          this.#socket.close(3007, "Could not handle message");
          this.#setClosed(e);
        }
      }
      // Handle a message from the server.
      #handleMsg(msg) {
        if (msg.type === "none") {
          throw new ProtoError("Received an unrecognized ServerMsg");
        } else if (msg.type === "hello_ok" || msg.type === "hello_error") {
          if (this.#recvdHello) {
            throw new ProtoError("Received a duplicated hello response");
          }
          this.#recvdHello = true;
          if (msg.type === "hello_error") {
            throw errorFromProto(msg.error);
          }
          return;
        } else if (!this.#recvdHello) {
          throw new ProtoError("Received a non-hello message before a hello response");
        }
        if (msg.type === "response_ok") {
          const requestId = msg.requestId;
          const responseState = this.#responseMap.get(requestId);
          this.#responseMap.delete(requestId);
          if (responseState === void 0) {
            throw new ProtoError("Received unexpected OK response");
          }
          this.#requestIdAlloc.free(requestId);
          try {
            if (responseState.type !== msg.response.type) {
              console.dir({ responseState, msg });
              throw new ProtoError("Received unexpected type of response");
            }
            responseState.responseCallback(msg.response);
          } catch (e) {
            responseState.errorCallback(e);
            throw e;
          }
        } else if (msg.type === "response_error") {
          const requestId = msg.requestId;
          const responseState = this.#responseMap.get(requestId);
          this.#responseMap.delete(requestId);
          if (responseState === void 0) {
            throw new ProtoError("Received unexpected error response");
          }
          this.#requestIdAlloc.free(requestId);
          responseState.errorCallback(errorFromProto(msg.error));
        } else {
          throw impossible(msg, "Impossible ServerMsg type");
        }
      }
      /** Open a {@link WsStream}, a stream for executing SQL statements. */
      openStream() {
        return WsStream.open(this);
      }
      /** Cache a SQL text on the server. This requires protocol version 2 or higher. */
      storeSql(sql3) {
        this._ensureVersion(2, "storeSql()");
        const sqlId = this.#sqlIdAlloc.alloc();
        const sqlObj = new Sql(this, sqlId);
        const responseCallback = /* @__PURE__ */ __name(() => void 0, "responseCallback");
        const errorCallback = /* @__PURE__ */ __name((e) => sqlObj._setClosed(e), "errorCallback");
        const request = { type: "store_sql", sqlId, sql: sql3 };
        this._sendRequest(request, { responseCallback, errorCallback });
        return sqlObj;
      }
      /** @private */
      _closeSql(sqlId) {
        if (this.#closed !== void 0) {
          return;
        }
        const responseCallback = /* @__PURE__ */ __name(() => this.#sqlIdAlloc.free(sqlId), "responseCallback");
        const errorCallback = /* @__PURE__ */ __name((e) => this.#setClosed(e), "errorCallback");
        const request = { type: "close_sql", sqlId };
        this._sendRequest(request, { responseCallback, errorCallback });
      }
      /** Close the client and the WebSocket. */
      close() {
        this.#setClosed(new ClientError("Client was manually closed"));
      }
      /** True if the client is closed. */
      get closed() {
        return this.#closed !== void 0;
      }
    };
  }
});

// worker/node_modules/cross-fetch/dist/browser-ponyfill.js
var require_browser_ponyfill = __commonJS({
  "worker/node_modules/cross-fetch/dist/browser-ponyfill.js"(exports, module) {
    var __global__ = typeof globalThis !== "undefined" && globalThis || typeof self !== "undefined" && self || typeof global !== "undefined" && global;
    var __globalThis__ = (function() {
      function F() {
        this.fetch = false;
        this.DOMException = __global__.DOMException;
      }
      __name(F, "F");
      F.prototype = __global__;
      return new F();
    })();
    (function(globalThis2) {
      var irrelevant = (function(exports2) {
        var g = typeof globalThis2 !== "undefined" && globalThis2 || typeof self !== "undefined" && self || // eslint-disable-next-line no-undef
        typeof global !== "undefined" && global || {};
        var support = {
          searchParams: "URLSearchParams" in g,
          iterable: "Symbol" in g && "iterator" in Symbol,
          blob: "FileReader" in g && "Blob" in g && (function() {
            try {
              new Blob();
              return true;
            } catch (e) {
              return false;
            }
          })(),
          formData: "FormData" in g,
          arrayBuffer: "ArrayBuffer" in g
        };
        function isDataView(obj) {
          return obj && DataView.prototype.isPrototypeOf(obj);
        }
        __name(isDataView, "isDataView");
        if (support.arrayBuffer) {
          var viewClasses = [
            "[object Int8Array]",
            "[object Uint8Array]",
            "[object Uint8ClampedArray]",
            "[object Int16Array]",
            "[object Uint16Array]",
            "[object Int32Array]",
            "[object Uint32Array]",
            "[object Float32Array]",
            "[object Float64Array]"
          ];
          var isArrayBufferView = ArrayBuffer.isView || function(obj) {
            return obj && viewClasses.indexOf(Object.prototype.toString.call(obj)) > -1;
          };
        }
        function normalizeName(name) {
          if (typeof name !== "string") {
            name = String(name);
          }
          if (/[^a-z0-9\-#$%&'*+.^_`|~!]/i.test(name) || name === "") {
            throw new TypeError('Invalid character in header field name: "' + name + '"');
          }
          return name.toLowerCase();
        }
        __name(normalizeName, "normalizeName");
        function normalizeValue(value) {
          if (typeof value !== "string") {
            value = String(value);
          }
          return value;
        }
        __name(normalizeValue, "normalizeValue");
        function iteratorFor(items) {
          var iterator = {
            next: /* @__PURE__ */ __name(function() {
              var value = items.shift();
              return { done: value === void 0, value };
            }, "next")
          };
          if (support.iterable) {
            iterator[Symbol.iterator] = function() {
              return iterator;
            };
          }
          return iterator;
        }
        __name(iteratorFor, "iteratorFor");
        function Headers4(headers) {
          this.map = {};
          if (headers instanceof Headers4) {
            headers.forEach(function(value, name) {
              this.append(name, value);
            }, this);
          } else if (Array.isArray(headers)) {
            headers.forEach(function(header) {
              if (header.length != 2) {
                throw new TypeError("Headers constructor: expected name/value pair to be length 2, found" + header.length);
              }
              this.append(header[0], header[1]);
            }, this);
          } else if (headers) {
            Object.getOwnPropertyNames(headers).forEach(function(name) {
              this.append(name, headers[name]);
            }, this);
          }
        }
        __name(Headers4, "Headers");
        Headers4.prototype.append = function(name, value) {
          name = normalizeName(name);
          value = normalizeValue(value);
          var oldValue = this.map[name];
          this.map[name] = oldValue ? oldValue + ", " + value : value;
        };
        Headers4.prototype["delete"] = function(name) {
          delete this.map[normalizeName(name)];
        };
        Headers4.prototype.get = function(name) {
          name = normalizeName(name);
          return this.has(name) ? this.map[name] : null;
        };
        Headers4.prototype.has = function(name) {
          return this.map.hasOwnProperty(normalizeName(name));
        };
        Headers4.prototype.set = function(name, value) {
          this.map[normalizeName(name)] = normalizeValue(value);
        };
        Headers4.prototype.forEach = function(callback, thisArg) {
          for (var name in this.map) {
            if (this.map.hasOwnProperty(name)) {
              callback.call(thisArg, this.map[name], name, this);
            }
          }
        };
        Headers4.prototype.keys = function() {
          var items = [];
          this.forEach(function(value, name) {
            items.push(name);
          });
          return iteratorFor(items);
        };
        Headers4.prototype.values = function() {
          var items = [];
          this.forEach(function(value) {
            items.push(value);
          });
          return iteratorFor(items);
        };
        Headers4.prototype.entries = function() {
          var items = [];
          this.forEach(function(value, name) {
            items.push([name, value]);
          });
          return iteratorFor(items);
        };
        if (support.iterable) {
          Headers4.prototype[Symbol.iterator] = Headers4.prototype.entries;
        }
        function consumed(body) {
          if (body._noBody) return;
          if (body.bodyUsed) {
            return Promise.reject(new TypeError("Already read"));
          }
          body.bodyUsed = true;
        }
        __name(consumed, "consumed");
        function fileReaderReady(reader) {
          return new Promise(function(resolve, reject) {
            reader.onload = function() {
              resolve(reader.result);
            };
            reader.onerror = function() {
              reject(reader.error);
            };
          });
        }
        __name(fileReaderReady, "fileReaderReady");
        function readBlobAsArrayBuffer(blob3) {
          var reader = new FileReader();
          var promise = fileReaderReady(reader);
          reader.readAsArrayBuffer(blob3);
          return promise;
        }
        __name(readBlobAsArrayBuffer, "readBlobAsArrayBuffer");
        function readBlobAsText(blob3) {
          var reader = new FileReader();
          var promise = fileReaderReady(reader);
          var match2 = /charset=([A-Za-z0-9_-]+)/.exec(blob3.type);
          var encoding = match2 ? match2[1] : "utf-8";
          reader.readAsText(blob3, encoding);
          return promise;
        }
        __name(readBlobAsText, "readBlobAsText");
        function readArrayBufferAsText(buf) {
          var view = new Uint8Array(buf);
          var chars = new Array(view.length);
          for (var i = 0; i < view.length; i++) {
            chars[i] = String.fromCharCode(view[i]);
          }
          return chars.join("");
        }
        __name(readArrayBufferAsText, "readArrayBufferAsText");
        function bufferClone(buf) {
          if (buf.slice) {
            return buf.slice(0);
          } else {
            var view = new Uint8Array(buf.byteLength);
            view.set(new Uint8Array(buf));
            return view.buffer;
          }
        }
        __name(bufferClone, "bufferClone");
        function Body() {
          this.bodyUsed = false;
          this._initBody = function(body) {
            this.bodyUsed = this.bodyUsed;
            this._bodyInit = body;
            if (!body) {
              this._noBody = true;
              this._bodyText = "";
            } else if (typeof body === "string") {
              this._bodyText = body;
            } else if (support.blob && Blob.prototype.isPrototypeOf(body)) {
              this._bodyBlob = body;
            } else if (support.formData && FormData.prototype.isPrototypeOf(body)) {
              this._bodyFormData = body;
            } else if (support.searchParams && URLSearchParams.prototype.isPrototypeOf(body)) {
              this._bodyText = body.toString();
            } else if (support.arrayBuffer && support.blob && isDataView(body)) {
              this._bodyArrayBuffer = bufferClone(body.buffer);
              this._bodyInit = new Blob([this._bodyArrayBuffer]);
            } else if (support.arrayBuffer && (ArrayBuffer.prototype.isPrototypeOf(body) || isArrayBufferView(body))) {
              this._bodyArrayBuffer = bufferClone(body);
            } else {
              this._bodyText = body = Object.prototype.toString.call(body);
            }
            if (!this.headers.get("content-type")) {
              if (typeof body === "string") {
                this.headers.set("content-type", "text/plain;charset=UTF-8");
              } else if (this._bodyBlob && this._bodyBlob.type) {
                this.headers.set("content-type", this._bodyBlob.type);
              } else if (support.searchParams && URLSearchParams.prototype.isPrototypeOf(body)) {
                this.headers.set("content-type", "application/x-www-form-urlencoded;charset=UTF-8");
              }
            }
          };
          if (support.blob) {
            this.blob = function() {
              var rejected = consumed(this);
              if (rejected) {
                return rejected;
              }
              if (this._bodyBlob) {
                return Promise.resolve(this._bodyBlob);
              } else if (this._bodyArrayBuffer) {
                return Promise.resolve(new Blob([this._bodyArrayBuffer]));
              } else if (this._bodyFormData) {
                throw new Error("could not read FormData body as blob");
              } else {
                return Promise.resolve(new Blob([this._bodyText]));
              }
            };
          }
          this.arrayBuffer = function() {
            if (this._bodyArrayBuffer) {
              var isConsumed = consumed(this);
              if (isConsumed) {
                return isConsumed;
              } else if (ArrayBuffer.isView(this._bodyArrayBuffer)) {
                return Promise.resolve(
                  this._bodyArrayBuffer.buffer.slice(
                    this._bodyArrayBuffer.byteOffset,
                    this._bodyArrayBuffer.byteOffset + this._bodyArrayBuffer.byteLength
                  )
                );
              } else {
                return Promise.resolve(this._bodyArrayBuffer);
              }
            } else if (support.blob) {
              return this.blob().then(readBlobAsArrayBuffer);
            } else {
              throw new Error("could not read as ArrayBuffer");
            }
          };
          this.text = function() {
            var rejected = consumed(this);
            if (rejected) {
              return rejected;
            }
            if (this._bodyBlob) {
              return readBlobAsText(this._bodyBlob);
            } else if (this._bodyArrayBuffer) {
              return Promise.resolve(readArrayBufferAsText(this._bodyArrayBuffer));
            } else if (this._bodyFormData) {
              throw new Error("could not read FormData body as text");
            } else {
              return Promise.resolve(this._bodyText);
            }
          };
          if (support.formData) {
            this.formData = function() {
              return this.text().then(decode2);
            };
          }
          this.json = function() {
            return this.text().then(JSON.parse);
          };
          return this;
        }
        __name(Body, "Body");
        var methods = ["CONNECT", "DELETE", "GET", "HEAD", "OPTIONS", "PATCH", "POST", "PUT", "TRACE"];
        function normalizeMethod(method) {
          var upcased = method.toUpperCase();
          return methods.indexOf(upcased) > -1 ? upcased : method;
        }
        __name(normalizeMethod, "normalizeMethod");
        function Request6(input, options) {
          if (!(this instanceof Request6)) {
            throw new TypeError('Please use the "new" operator, this DOM object constructor cannot be called as a function.');
          }
          options = options || {};
          var body = options.body;
          if (input instanceof Request6) {
            if (input.bodyUsed) {
              throw new TypeError("Already read");
            }
            this.url = input.url;
            this.credentials = input.credentials;
            if (!options.headers) {
              this.headers = new Headers4(input.headers);
            }
            this.method = input.method;
            this.mode = input.mode;
            this.signal = input.signal;
            if (!body && input._bodyInit != null) {
              body = input._bodyInit;
              input.bodyUsed = true;
            }
          } else {
            this.url = String(input);
          }
          this.credentials = options.credentials || this.credentials || "same-origin";
          if (options.headers || !this.headers) {
            this.headers = new Headers4(options.headers);
          }
          this.method = normalizeMethod(options.method || this.method || "GET");
          this.mode = options.mode || this.mode || null;
          this.signal = options.signal || this.signal || (function() {
            if ("AbortController" in g) {
              var ctrl = new AbortController();
              return ctrl.signal;
            }
          })();
          this.referrer = null;
          if ((this.method === "GET" || this.method === "HEAD") && body) {
            throw new TypeError("Body not allowed for GET or HEAD requests");
          }
          this._initBody(body);
          if (this.method === "GET" || this.method === "HEAD") {
            if (options.cache === "no-store" || options.cache === "no-cache") {
              var reParamSearch = /([?&])_=[^&]*/;
              if (reParamSearch.test(this.url)) {
                this.url = this.url.replace(reParamSearch, "$1_=" + (/* @__PURE__ */ new Date()).getTime());
              } else {
                var reQueryString = /\?/;
                this.url += (reQueryString.test(this.url) ? "&" : "?") + "_=" + (/* @__PURE__ */ new Date()).getTime();
              }
            }
          }
        }
        __name(Request6, "Request");
        Request6.prototype.clone = function() {
          return new Request6(this, { body: this._bodyInit });
        };
        function decode2(body) {
          var form = new FormData();
          body.trim().split("&").forEach(function(bytes) {
            if (bytes) {
              var split = bytes.split("=");
              var name = split.shift().replace(/\+/g, " ");
              var value = split.join("=").replace(/\+/g, " ");
              form.append(decodeURIComponent(name), decodeURIComponent(value));
            }
          });
          return form;
        }
        __name(decode2, "decode");
        function parseHeaders(rawHeaders) {
          var headers = new Headers4();
          var preProcessedHeaders = rawHeaders.replace(/\r?\n[\t ]+/g, " ");
          preProcessedHeaders.split("\r").map(function(header) {
            return header.indexOf("\n") === 0 ? header.substr(1, header.length) : header;
          }).forEach(function(line) {
            var parts = line.split(":");
            var key = parts.shift().trim();
            if (key) {
              var value = parts.join(":").trim();
              try {
                headers.append(key, value);
              } catch (error) {
                console.warn("Response " + error.message);
              }
            }
          });
          return headers;
        }
        __name(parseHeaders, "parseHeaders");
        Body.call(Request6.prototype);
        function Response3(bodyInit, options) {
          if (!(this instanceof Response3)) {
            throw new TypeError('Please use the "new" operator, this DOM object constructor cannot be called as a function.');
          }
          if (!options) {
            options = {};
          }
          this.type = "default";
          this.status = options.status === void 0 ? 200 : options.status;
          if (this.status < 200 || this.status > 599) {
            throw new RangeError("Failed to construct 'Response': The status provided (0) is outside the range [200, 599].");
          }
          this.ok = this.status >= 200 && this.status < 300;
          this.statusText = options.statusText === void 0 ? "" : "" + options.statusText;
          this.headers = new Headers4(options.headers);
          this.url = options.url || "";
          this._initBody(bodyInit);
        }
        __name(Response3, "Response");
        Body.call(Response3.prototype);
        Response3.prototype.clone = function() {
          return new Response3(this._bodyInit, {
            status: this.status,
            statusText: this.statusText,
            headers: new Headers4(this.headers),
            url: this.url
          });
        };
        Response3.error = function() {
          var response = new Response3(null, { status: 200, statusText: "" });
          response.ok = false;
          response.status = 0;
          response.type = "error";
          return response;
        };
        var redirectStatuses = [301, 302, 303, 307, 308];
        Response3.redirect = function(url, status) {
          if (redirectStatuses.indexOf(status) === -1) {
            throw new RangeError("Invalid status code");
          }
          return new Response3(null, { status, headers: { location: url } });
        };
        exports2.DOMException = g.DOMException;
        try {
          new exports2.DOMException();
        } catch (err) {
          exports2.DOMException = function(message, name) {
            this.message = message;
            this.name = name;
            var error = Error(message);
            this.stack = error.stack;
          };
          exports2.DOMException.prototype = Object.create(Error.prototype);
          exports2.DOMException.prototype.constructor = exports2.DOMException;
        }
        function fetch4(input, init) {
          return new Promise(function(resolve, reject) {
            var request = new Request6(input, init);
            if (request.signal && request.signal.aborted) {
              return reject(new exports2.DOMException("Aborted", "AbortError"));
            }
            var xhr = new XMLHttpRequest();
            function abortXhr() {
              xhr.abort();
            }
            __name(abortXhr, "abortXhr");
            xhr.onload = function() {
              var options = {
                statusText: xhr.statusText,
                headers: parseHeaders(xhr.getAllResponseHeaders() || "")
              };
              if (request.url.indexOf("file://") === 0 && (xhr.status < 200 || xhr.status > 599)) {
                options.status = 200;
              } else {
                options.status = xhr.status;
              }
              options.url = "responseURL" in xhr ? xhr.responseURL : options.headers.get("X-Request-URL");
              var body = "response" in xhr ? xhr.response : xhr.responseText;
              setTimeout(function() {
                resolve(new Response3(body, options));
              }, 0);
            };
            xhr.onerror = function() {
              setTimeout(function() {
                reject(new TypeError("Network request failed"));
              }, 0);
            };
            xhr.ontimeout = function() {
              setTimeout(function() {
                reject(new TypeError("Network request timed out"));
              }, 0);
            };
            xhr.onabort = function() {
              setTimeout(function() {
                reject(new exports2.DOMException("Aborted", "AbortError"));
              }, 0);
            };
            function fixUrl(url) {
              try {
                return url === "" && g.location.href ? g.location.href : url;
              } catch (e) {
                return url;
              }
            }
            __name(fixUrl, "fixUrl");
            xhr.open(request.method, fixUrl(request.url), true);
            if (request.credentials === "include") {
              xhr.withCredentials = true;
            } else if (request.credentials === "omit") {
              xhr.withCredentials = false;
            }
            if ("responseType" in xhr) {
              if (support.blob) {
                xhr.responseType = "blob";
              } else if (support.arrayBuffer) {
                xhr.responseType = "arraybuffer";
              }
            }
            if (init && typeof init.headers === "object" && !(init.headers instanceof Headers4 || g.Headers && init.headers instanceof g.Headers)) {
              var names = [];
              Object.getOwnPropertyNames(init.headers).forEach(function(name) {
                names.push(normalizeName(name));
                xhr.setRequestHeader(name, normalizeValue(init.headers[name]));
              });
              request.headers.forEach(function(value, name) {
                if (names.indexOf(name) === -1) {
                  xhr.setRequestHeader(name, value);
                }
              });
            } else {
              request.headers.forEach(function(value, name) {
                xhr.setRequestHeader(name, value);
              });
            }
            if (request.signal) {
              request.signal.addEventListener("abort", abortXhr);
              xhr.onreadystatechange = function() {
                if (xhr.readyState === 4) {
                  request.signal.removeEventListener("abort", abortXhr);
                }
              };
            }
            xhr.send(typeof request._bodyInit === "undefined" ? null : request._bodyInit);
          });
        }
        __name(fetch4, "fetch");
        fetch4.polyfill = true;
        if (!g.fetch) {
          g.fetch = fetch4;
          g.Headers = Headers4;
          g.Request = Request6;
          g.Response = Response3;
        }
        exports2.Headers = Headers4;
        exports2.Request = Request6;
        exports2.Response = Response3;
        exports2.fetch = fetch4;
        return exports2;
      })({});
    })(__globalThis__);
    __globalThis__.fetch.ponyfill = true;
    delete __globalThis__.fetch.polyfill;
    var ctx = __global__.fetch ? __global__ : __globalThis__;
    exports = ctx.fetch;
    exports.default = ctx.fetch;
    exports.fetch = ctx.fetch;
    exports.Headers = ctx.Headers;
    exports.Request = ctx.Request;
    exports.Response = ctx.Response;
    module.exports = exports;
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/queue_microtask.js
var _queueMicrotask;
var init_queue_microtask = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/queue_microtask.js"() {
    if (typeof queueMicrotask !== "undefined") {
      _queueMicrotask = queueMicrotask;
    } else {
      const resolved = Promise.resolve();
      _queueMicrotask = /* @__PURE__ */ __name((callback) => {
        resolved.then(callback);
      }, "_queueMicrotask");
    }
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/byte_queue.js
var ByteQueue;
var init_byte_queue = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/byte_queue.js"() {
    ByteQueue = class {
      static {
        __name(this, "ByteQueue");
      }
      #array;
      #shiftPos;
      #pushPos;
      constructor(initialCap) {
        this.#array = new Uint8Array(new ArrayBuffer(initialCap));
        this.#shiftPos = 0;
        this.#pushPos = 0;
      }
      get length() {
        return this.#pushPos - this.#shiftPos;
      }
      data() {
        return this.#array.slice(this.#shiftPos, this.#pushPos);
      }
      push(chunk) {
        this.#ensurePush(chunk.byteLength);
        this.#array.set(chunk, this.#pushPos);
        this.#pushPos += chunk.byteLength;
      }
      #ensurePush(pushLength) {
        if (this.#pushPos + pushLength <= this.#array.byteLength) {
          return;
        }
        const filledLength = this.#pushPos - this.#shiftPos;
        if (filledLength + pushLength <= this.#array.byteLength && 2 * this.#pushPos >= this.#array.byteLength) {
          this.#array.copyWithin(0, this.#shiftPos, this.#pushPos);
        } else {
          let newCap = this.#array.byteLength;
          do {
            newCap *= 2;
          } while (filledLength + pushLength > newCap);
          const newArray = new Uint8Array(new ArrayBuffer(newCap));
          newArray.set(this.#array.slice(this.#shiftPos, this.#pushPos), 0);
          this.#array = newArray;
        }
        this.#pushPos = filledLength;
        this.#shiftPos = 0;
      }
      shift(length3) {
        this.#shiftPos += length3;
      }
    };
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/http/json_decode.js
function PipelineRespBody(obj) {
  const baton = stringOpt(obj["baton"]);
  const baseUrl2 = stringOpt(obj["base_url"]);
  const results = arrayObjectsMap(obj["results"], StreamResult);
  return { baton, baseUrl: baseUrl2, results };
}
function StreamResult(obj) {
  const type = string(obj["type"]);
  if (type === "ok") {
    const response = StreamResponse(object(obj["response"]));
    return { type: "ok", response };
  } else if (type === "error") {
    const error = Error2(object(obj["error"]));
    return { type: "error", error };
  } else {
    throw new ProtoError("Unexpected type of StreamResult");
  }
}
function StreamResponse(obj) {
  const type = string(obj["type"]);
  if (type === "close") {
    return { type: "close" };
  } else if (type === "execute") {
    const result = StmtResult(object(obj["result"]));
    return { type: "execute", result };
  } else if (type === "batch") {
    const result = BatchResult(object(obj["result"]));
    return { type: "batch", result };
  } else if (type === "sequence") {
    return { type: "sequence" };
  } else if (type === "describe") {
    const result = DescribeResult(object(obj["result"]));
    return { type: "describe", result };
  } else if (type === "store_sql") {
    return { type: "store_sql" };
  } else if (type === "close_sql") {
    return { type: "close_sql" };
  } else if (type === "get_autocommit") {
    const isAutocommit = boolean(obj["is_autocommit"]);
    return { type: "get_autocommit", isAutocommit };
  } else {
    throw new ProtoError("Unexpected type of StreamResponse");
  }
}
function CursorRespBody(obj) {
  const baton = stringOpt(obj["baton"]);
  const baseUrl2 = stringOpt(obj["base_url"]);
  return { baton, baseUrl: baseUrl2 };
}
var init_json_decode3 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/http/json_decode.js"() {
    init_errors();
    init_decode();
    init_json_decode();
    __name(PipelineRespBody, "PipelineRespBody");
    __name(StreamResult, "StreamResult");
    __name(StreamResponse, "StreamResponse");
    __name(CursorRespBody, "CursorRespBody");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/http/protobuf_decode.js
var PipelineRespBody2, StreamResult2, StreamResponse2, ExecuteStreamResp, BatchStreamResp, DescribeStreamResp, GetAutocommitStreamResp, CursorRespBody2;
var init_protobuf_decode3 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/http/protobuf_decode.js"() {
    init_protobuf_decode();
    PipelineRespBody2 = {
      default() {
        return { baton: void 0, baseUrl: void 0, results: [] };
      },
      1(r, msg) {
        msg.baton = r.string();
      },
      2(r, msg) {
        msg.baseUrl = r.string();
      },
      3(r, msg) {
        msg.results.push(r.message(StreamResult2));
      }
    };
    StreamResult2 = {
      default() {
        return { type: "none" };
      },
      1(r) {
        return { type: "ok", response: r.message(StreamResponse2) };
      },
      2(r) {
        return { type: "error", error: r.message(Error3) };
      }
    };
    StreamResponse2 = {
      default() {
        return { type: "none" };
      },
      1(r) {
        return { type: "close" };
      },
      2(r) {
        return r.message(ExecuteStreamResp);
      },
      3(r) {
        return r.message(BatchStreamResp);
      },
      4(r) {
        return { type: "sequence" };
      },
      5(r) {
        return r.message(DescribeStreamResp);
      },
      6(r) {
        return { type: "store_sql" };
      },
      7(r) {
        return { type: "close_sql" };
      },
      8(r) {
        return r.message(GetAutocommitStreamResp);
      }
    };
    ExecuteStreamResp = {
      default() {
        return { type: "execute", result: StmtResult2.default() };
      },
      1(r, msg) {
        msg.result = r.message(StmtResult2);
      }
    };
    BatchStreamResp = {
      default() {
        return { type: "batch", result: BatchResult2.default() };
      },
      1(r, msg) {
        msg.result = r.message(BatchResult2);
      }
    };
    DescribeStreamResp = {
      default() {
        return { type: "describe", result: DescribeResult2.default() };
      },
      1(r, msg) {
        msg.result = r.message(DescribeResult2);
      }
    };
    GetAutocommitStreamResp = {
      default() {
        return { type: "get_autocommit", isAutocommit: false };
      },
      1(r, msg) {
        msg.isAutocommit = r.bool();
      }
    };
    CursorRespBody2 = {
      default() {
        return { baton: void 0, baseUrl: void 0 };
      },
      1(r, msg) {
        msg.baton = r.string();
      },
      2(r, msg) {
        msg.baseUrl = r.string();
      }
    };
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/http/cursor.js
var HttpCursor;
var init_cursor3 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/http/cursor.js"() {
    init_byte_queue();
    init_cursor();
    init_decode();
    init_decode2();
    init_errors();
    init_util3();
    init_json_decode3();
    init_protobuf_decode3();
    init_json_decode();
    init_protobuf_decode();
    HttpCursor = class extends Cursor {
      static {
        __name(this, "HttpCursor");
      }
      #stream;
      #encoding;
      #reader;
      #queue;
      #closed;
      #done;
      /** @private */
      constructor(stream, encoding) {
        super();
        this.#stream = stream;
        this.#encoding = encoding;
        this.#reader = void 0;
        this.#queue = new ByteQueue(16 * 1024);
        this.#closed = void 0;
        this.#done = false;
      }
      async open(response) {
        if (response.body === null) {
          throw new ProtoError("No response body for cursor request");
        }
        this.#reader = response.body[Symbol.asyncIterator]();
        const respBody = await this.#nextItem(CursorRespBody, CursorRespBody2);
        if (respBody === void 0) {
          throw new ProtoError("Empty response to cursor request");
        }
        return respBody;
      }
      /** Fetch the next entry from the cursor. */
      next() {
        return this.#nextItem(CursorEntry, CursorEntry2);
      }
      /** Close the cursor. */
      close() {
        this._setClosed(new ClientError("Cursor was manually closed"));
      }
      /** @private */
      _setClosed(error) {
        if (this.#closed !== void 0) {
          return;
        }
        this.#closed = error;
        this.#stream._cursorClosed(this);
        if (this.#reader !== void 0) {
          this.#reader.return();
        }
      }
      /** True if the cursor is closed. */
      get closed() {
        return this.#closed !== void 0;
      }
      async #nextItem(jsonFun, protobufDef) {
        for (; ; ) {
          if (this.#done) {
            return void 0;
          } else if (this.#closed !== void 0) {
            throw new ClosedError("Cursor is closed", this.#closed);
          }
          if (this.#encoding === "json") {
            const jsonData = this.#parseItemJson();
            if (jsonData !== void 0) {
              const jsonText = new TextDecoder().decode(jsonData);
              const jsonValue = JSON.parse(jsonText);
              return readJsonObject(jsonValue, jsonFun);
            }
          } else if (this.#encoding === "protobuf") {
            const protobufData = this.#parseItemProtobuf();
            if (protobufData !== void 0) {
              return readProtobufMessage(protobufData, protobufDef);
            }
          } else {
            throw impossible(this.#encoding, "Impossible encoding");
          }
          if (this.#reader === void 0) {
            throw new InternalError("Attempted to read from HTTP cursor before it was opened");
          }
          const { value, done } = await this.#reader.next();
          if (done && this.#queue.length === 0) {
            this.#done = true;
          } else if (done) {
            throw new ProtoError("Unexpected end of cursor stream");
          } else {
            this.#queue.push(value);
          }
        }
      }
      #parseItemJson() {
        const data = this.#queue.data();
        const newlineByte = 10;
        const newlinePos = data.indexOf(newlineByte);
        if (newlinePos < 0) {
          return void 0;
        }
        const jsonData = data.slice(0, newlinePos);
        this.#queue.shift(newlinePos + 1);
        return jsonData;
      }
      #parseItemProtobuf() {
        const data = this.#queue.data();
        let varintValue = 0;
        let varintLength = 0;
        for (; ; ) {
          if (varintLength >= data.byteLength) {
            return void 0;
          }
          const byte = data[varintLength];
          varintValue |= (byte & 127) << 7 * varintLength;
          varintLength += 1;
          if (!(byte & 128)) {
            break;
          }
        }
        if (data.byteLength < varintLength + varintValue) {
          return void 0;
        }
        const protobufData = data.slice(varintLength, varintLength + varintValue);
        this.#queue.shift(varintLength + varintValue);
        return protobufData;
      }
    };
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/http/json_encode.js
function PipelineReqBody(w, msg) {
  if (msg.baton !== void 0) {
    w.string("baton", msg.baton);
  }
  w.arrayObjects("requests", msg.requests, StreamRequest);
}
function StreamRequest(w, msg) {
  w.stringRaw("type", msg.type);
  if (msg.type === "close") {
  } else if (msg.type === "execute") {
    w.object("stmt", msg.stmt, Stmt2);
  } else if (msg.type === "batch") {
    w.object("batch", msg.batch, Batch2);
  } else if (msg.type === "sequence") {
    if (msg.sql !== void 0) {
      w.string("sql", msg.sql);
    }
    if (msg.sqlId !== void 0) {
      w.number("sql_id", msg.sqlId);
    }
  } else if (msg.type === "describe") {
    if (msg.sql !== void 0) {
      w.string("sql", msg.sql);
    }
    if (msg.sqlId !== void 0) {
      w.number("sql_id", msg.sqlId);
    }
  } else if (msg.type === "store_sql") {
    w.number("sql_id", msg.sqlId);
    w.string("sql", msg.sql);
  } else if (msg.type === "close_sql") {
    w.number("sql_id", msg.sqlId);
  } else if (msg.type === "get_autocommit") {
  } else {
    throw impossible(msg, "Impossible type of StreamRequest");
  }
}
function CursorReqBody(w, msg) {
  if (msg.baton !== void 0) {
    w.string("baton", msg.baton);
  }
  w.object("batch", msg.batch, Batch2);
}
var init_json_encode3 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/http/json_encode.js"() {
    init_json_encode();
    init_util3();
    __name(PipelineReqBody, "PipelineReqBody");
    __name(StreamRequest, "StreamRequest");
    __name(CursorReqBody, "CursorReqBody");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/http/protobuf_encode.js
function PipelineReqBody2(w, msg) {
  if (msg.baton !== void 0) {
    w.string(1, msg.baton);
  }
  for (const req of msg.requests) {
    w.message(2, req, StreamRequest2);
  }
}
function StreamRequest2(w, msg) {
  if (msg.type === "close") {
    w.message(1, msg, CloseStreamReq2);
  } else if (msg.type === "execute") {
    w.message(2, msg, ExecuteStreamReq);
  } else if (msg.type === "batch") {
    w.message(3, msg, BatchStreamReq);
  } else if (msg.type === "sequence") {
    w.message(4, msg, SequenceStreamReq);
  } else if (msg.type === "describe") {
    w.message(5, msg, DescribeStreamReq);
  } else if (msg.type === "store_sql") {
    w.message(6, msg, StoreSqlStreamReq);
  } else if (msg.type === "close_sql") {
    w.message(7, msg, CloseSqlStreamReq);
  } else if (msg.type === "get_autocommit") {
    w.message(8, msg, GetAutocommitStreamReq);
  } else {
    throw impossible(msg, "Impossible type of StreamRequest");
  }
}
function CloseStreamReq2(_w, _msg) {
}
function ExecuteStreamReq(w, msg) {
  w.message(1, msg.stmt, Stmt3);
}
function BatchStreamReq(w, msg) {
  w.message(1, msg.batch, Batch3);
}
function SequenceStreamReq(w, msg) {
  if (msg.sql !== void 0) {
    w.string(1, msg.sql);
  }
  if (msg.sqlId !== void 0) {
    w.int32(2, msg.sqlId);
  }
}
function DescribeStreamReq(w, msg) {
  if (msg.sql !== void 0) {
    w.string(1, msg.sql);
  }
  if (msg.sqlId !== void 0) {
    w.int32(2, msg.sqlId);
  }
}
function StoreSqlStreamReq(w, msg) {
  w.int32(1, msg.sqlId);
  w.string(2, msg.sql);
}
function CloseSqlStreamReq(w, msg) {
  w.int32(1, msg.sqlId);
}
function GetAutocommitStreamReq(_w, _msg) {
}
function CursorReqBody2(w, msg) {
  if (msg.baton !== void 0) {
    w.string(1, msg.baton);
  }
  w.message(2, msg.batch, Batch3);
}
var init_protobuf_encode3 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/http/protobuf_encode.js"() {
    init_protobuf_encode();
    init_util3();
    __name(PipelineReqBody2, "PipelineReqBody");
    __name(StreamRequest2, "StreamRequest");
    __name(CloseStreamReq2, "CloseStreamReq");
    __name(ExecuteStreamReq, "ExecuteStreamReq");
    __name(BatchStreamReq, "BatchStreamReq");
    __name(SequenceStreamReq, "SequenceStreamReq");
    __name(DescribeStreamReq, "DescribeStreamReq");
    __name(StoreSqlStreamReq, "StoreSqlStreamReq");
    __name(CloseSqlStreamReq, "CloseSqlStreamReq");
    __name(GetAutocommitStreamReq, "GetAutocommitStreamReq");
    __name(CursorReqBody2, "CursorReqBody");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/http/stream.js
function handlePipelineResponse(pipeline, respBody) {
  if (respBody.results.length !== pipeline.length) {
    throw new ProtoError("Server returned unexpected number of pipeline results");
  }
  for (let i = 0; i < pipeline.length; ++i) {
    const result = respBody.results[i];
    const entry = pipeline[i];
    if (result.type === "ok") {
      if (result.response.type !== entry.request.type) {
        throw new ProtoError("Received unexpected type of response");
      }
      entry.responseCallback(result.response);
    } else if (result.type === "error") {
      entry.errorCallback(errorFromProto(result.error));
    } else if (result.type === "none") {
      throw new ProtoError("Received unrecognized type of StreamResult");
    } else {
      throw impossible(result, "Received impossible type of StreamResult");
    }
  }
}
async function decodePipelineResponse(resp, encoding) {
  if (encoding === "json") {
    const respJson = await resp.json();
    return readJsonObject(respJson, PipelineRespBody);
  }
  if (encoding === "protobuf") {
    const respData = await resp.arrayBuffer();
    return readProtobufMessage(new Uint8Array(respData), PipelineRespBody2);
  }
  await resp.body?.cancel();
  throw impossible(encoding, "Impossible encoding");
}
async function errorFromResponse(resp) {
  const respType = resp.headers.get("content-type") ?? "text/plain";
  let message = `Server returned HTTP status ${resp.status}`;
  if (respType === "application/json") {
    const respBody = await resp.json();
    if ("message" in respBody) {
      return errorFromProto(respBody);
    }
    return new HttpServerError(message, resp.status);
  }
  if (respType === "text/plain") {
    const respBody = (await resp.text()).trim();
    if (respBody !== "") {
      message += `: ${respBody}`;
    }
    return new HttpServerError(message, resp.status);
  }
  await resp.body?.cancel();
  return new HttpServerError(message, resp.status);
}
var import_cross_fetch, HttpStream;
var init_stream3 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/http/stream.js"() {
    import_cross_fetch = __toESM(require_browser_ponyfill(), 1);
    init_errors();
    init_encoding();
    init_id_alloc();
    init_queue();
    init_queue_microtask();
    init_result();
    init_sql();
    init_stream();
    init_util3();
    init_cursor3();
    init_json_encode3();
    init_protobuf_encode3();
    init_json_encode3();
    init_protobuf_encode3();
    init_json_decode3();
    init_protobuf_decode3();
    HttpStream = class extends Stream {
      static {
        __name(this, "HttpStream");
      }
      #client;
      #baseUrl;
      #jwt;
      #fetch;
      #remoteEncryptionKey;
      #baton;
      #queue;
      #flushing;
      #cursor;
      #closing;
      #closeQueued;
      #closed;
      #sqlIdAlloc;
      /** @private */
      constructor(client, baseUrl2, jwt, customFetch, remoteEncryptionKey) {
        super(client.intMode);
        this.#client = client;
        this.#baseUrl = baseUrl2.toString();
        this.#jwt = jwt;
        this.#fetch = customFetch;
        this.#remoteEncryptionKey = remoteEncryptionKey;
        this.#baton = void 0;
        this.#queue = new Queue();
        this.#flushing = false;
        this.#closing = false;
        this.#closeQueued = false;
        this.#closed = void 0;
        this.#sqlIdAlloc = new IdAlloc();
      }
      /** Get the {@link HttpClient} object that this stream belongs to. */
      client() {
        return this.#client;
      }
      /** @private */
      _sqlOwner() {
        return this;
      }
      /** Cache a SQL text on the server. */
      storeSql(sql3) {
        const sqlId = this.#sqlIdAlloc.alloc();
        this.#sendStreamRequest({ type: "store_sql", sqlId, sql: sql3 }).then(() => void 0, (error) => this._setClosed(error));
        return new Sql(this, sqlId);
      }
      /** @private */
      _closeSql(sqlId) {
        if (this.#closed !== void 0) {
          return;
        }
        this.#sendStreamRequest({ type: "close_sql", sqlId }).then(() => this.#sqlIdAlloc.free(sqlId), (error) => this._setClosed(error));
      }
      /** @private */
      _execute(stmt) {
        return this.#sendStreamRequest({ type: "execute", stmt }).then((response) => {
          return response.result;
        });
      }
      /** @private */
      _batch(batch) {
        return this.#sendStreamRequest({ type: "batch", batch }).then((response) => {
          return response.result;
        });
      }
      /** @private */
      _describe(protoSql) {
        return this.#sendStreamRequest({
          type: "describe",
          sql: protoSql.sql,
          sqlId: protoSql.sqlId
        }).then((response) => {
          return response.result;
        });
      }
      /** @private */
      _sequence(protoSql) {
        return this.#sendStreamRequest({
          type: "sequence",
          sql: protoSql.sql,
          sqlId: protoSql.sqlId
        }).then((_response) => {
          return void 0;
        });
      }
      /** Check whether the SQL connection underlying this stream is in autocommit state (i.e., outside of an
       * explicit transaction). This requires protocol version 3 or higher.
       */
      getAutocommit() {
        this.#client._ensureVersion(3, "getAutocommit()");
        return this.#sendStreamRequest({
          type: "get_autocommit"
        }).then((response) => {
          return response.isAutocommit;
        });
      }
      #sendStreamRequest(request) {
        return new Promise((responseCallback, errorCallback) => {
          this.#pushToQueue({ type: "pipeline", request, responseCallback, errorCallback });
        });
      }
      /** @private */
      _openCursor(batch) {
        return new Promise((cursorCallback, errorCallback) => {
          this.#pushToQueue({ type: "cursor", batch, cursorCallback, errorCallback });
        });
      }
      /** @private */
      _cursorClosed(cursor) {
        if (cursor !== this.#cursor) {
          throw new InternalError("Cursor was closed, but it was not associated with the stream");
        }
        this.#cursor = void 0;
        _queueMicrotask(() => this.#flushQueue());
      }
      /** Immediately close the stream. */
      close() {
        this._setClosed(new ClientError("Stream was manually closed"));
      }
      /** Gracefully close the stream. */
      closeGracefully() {
        this.#closing = true;
        _queueMicrotask(() => this.#flushQueue());
      }
      /** True if the stream is closed. */
      get closed() {
        return this.#closed !== void 0 || this.#closing;
      }
      /** @private */
      _setClosed(error) {
        if (this.#closed !== void 0) {
          return;
        }
        this.#closed = error;
        if (this.#cursor !== void 0) {
          this.#cursor._setClosed(error);
        }
        this.#client._streamClosed(this);
        for (; ; ) {
          const entry = this.#queue.shift();
          if (entry !== void 0) {
            entry.errorCallback(error);
          } else {
            break;
          }
        }
        if ((this.#baton !== void 0 || this.#flushing) && !this.#closeQueued) {
          this.#queue.push({
            type: "pipeline",
            request: { type: "close" },
            responseCallback: /* @__PURE__ */ __name(() => void 0, "responseCallback"),
            errorCallback: /* @__PURE__ */ __name(() => void 0, "errorCallback")
          });
          this.#closeQueued = true;
          _queueMicrotask(() => this.#flushQueue());
        }
      }
      #pushToQueue(entry) {
        if (this.#closed !== void 0) {
          throw new ClosedError("Stream is closed", this.#closed);
        } else if (this.#closing) {
          throw new ClosedError("Stream is closing", void 0);
        } else {
          this.#queue.push(entry);
          _queueMicrotask(() => this.#flushQueue());
        }
      }
      #flushQueue() {
        if (this.#flushing || this.#cursor !== void 0) {
          return;
        }
        if (this.#closing && this.#queue.length === 0) {
          this._setClosed(new ClientError("Stream was gracefully closed"));
          return;
        }
        const endpoint = this.#client._endpoint;
        if (endpoint === void 0) {
          this.#client._endpointPromise.then(() => this.#flushQueue(), (error) => this._setClosed(error));
          return;
        }
        const firstEntry = this.#queue.shift();
        if (firstEntry === void 0) {
          return;
        } else if (firstEntry.type === "pipeline") {
          const pipeline = [firstEntry];
          for (; ; ) {
            const entry = this.#queue.first();
            if (entry !== void 0 && entry.type === "pipeline") {
              pipeline.push(entry);
              this.#queue.shift();
            } else if (entry === void 0 && this.#closing && !this.#closeQueued) {
              pipeline.push({
                type: "pipeline",
                request: { type: "close" },
                responseCallback: /* @__PURE__ */ __name(() => void 0, "responseCallback"),
                errorCallback: /* @__PURE__ */ __name(() => void 0, "errorCallback")
              });
              this.#closeQueued = true;
              break;
            } else {
              break;
            }
          }
          this.#flushPipeline(endpoint, pipeline);
        } else if (firstEntry.type === "cursor") {
          this.#flushCursor(endpoint, firstEntry);
        } else {
          throw impossible(firstEntry, "Impossible type of QueueEntry");
        }
      }
      #flushPipeline(endpoint, pipeline) {
        this.#flush(() => this.#createPipelineRequest(pipeline, endpoint), (resp) => decodePipelineResponse(resp, endpoint.encoding), (respBody) => respBody.baton, (respBody) => respBody.baseUrl, (respBody) => handlePipelineResponse(pipeline, respBody), (error) => pipeline.forEach((entry) => entry.errorCallback(error)));
      }
      #flushCursor(endpoint, entry) {
        const cursor = new HttpCursor(this, endpoint.encoding);
        this.#cursor = cursor;
        this.#flush(() => this.#createCursorRequest(entry, endpoint), (resp) => cursor.open(resp), (respBody) => respBody.baton, (respBody) => respBody.baseUrl, (_respBody) => entry.cursorCallback(cursor), (error) => entry.errorCallback(error));
      }
      #flush(createRequest, decodeResponse, getBaton, getBaseUrl, handleResponse, handleError) {
        let promise;
        try {
          const request = createRequest();
          const fetch4 = this.#fetch;
          promise = fetch4(request);
        } catch (error) {
          promise = Promise.reject(error);
        }
        this.#flushing = true;
        promise.then((resp) => {
          if (!resp.ok) {
            return errorFromResponse(resp).then((error) => {
              throw error;
            });
          }
          return decodeResponse(resp);
        }).then((r) => {
          this.#baton = getBaton(r);
          this.#baseUrl = getBaseUrl(r) ?? this.#baseUrl;
          handleResponse(r);
        }).catch((error) => {
          this._setClosed(error);
          handleError(error);
        }).finally(() => {
          this.#flushing = false;
          this.#flushQueue();
        });
      }
      #createPipelineRequest(pipeline, endpoint) {
        return this.#createRequest(new URL(endpoint.pipelinePath, this.#baseUrl), {
          baton: this.#baton,
          requests: pipeline.map((entry) => entry.request)
        }, endpoint.encoding, PipelineReqBody, PipelineReqBody2);
      }
      #createCursorRequest(entry, endpoint) {
        if (endpoint.cursorPath === void 0) {
          throw new ProtocolVersionError(`Cursors are supported only on protocol version 3 and higher, but the HTTP server only supports version ${endpoint.version}.`);
        }
        return this.#createRequest(new URL(endpoint.cursorPath, this.#baseUrl), {
          baton: this.#baton,
          batch: entry.batch
        }, endpoint.encoding, CursorReqBody, CursorReqBody2);
      }
      #createRequest(url, reqBody, encoding, jsonFun, protobufFun) {
        let bodyData;
        let contentType;
        if (encoding === "json") {
          bodyData = writeJsonObject(reqBody, jsonFun);
          contentType = "application/json";
        } else if (encoding === "protobuf") {
          bodyData = writeProtobufMessage(reqBody, protobufFun);
          contentType = "application/x-protobuf";
        } else {
          throw impossible(encoding, "Impossible encoding");
        }
        const headers = new import_cross_fetch.Headers();
        headers.set("content-type", contentType);
        if (this.#jwt !== void 0) {
          headers.set("authorization", `Bearer ${this.#jwt}`);
        }
        if (this.#remoteEncryptionKey !== void 0) {
          headers.set("x-turso-encryption-key", this.#remoteEncryptionKey);
        }
        return new import_cross_fetch.Request(url.toString(), { method: "POST", headers, body: bodyData });
      }
    };
    __name(handlePipelineResponse, "handlePipelineResponse");
    __name(decodePipelineResponse, "decodePipelineResponse");
    __name(errorFromResponse, "errorFromResponse");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/http/client.js
async function findEndpoint(customFetch, clientUrl) {
  const fetch4 = customFetch;
  for (const endpoint of checkEndpoints) {
    const url = new URL(endpoint.versionPath, clientUrl);
    const request = new import_cross_fetch2.Request(url.toString(), { method: "GET" });
    const response = await fetch4(request);
    await response.arrayBuffer();
    if (response.ok) {
      return endpoint;
    }
  }
  return fallbackEndpoint;
}
var import_cross_fetch2, checkEndpoints, fallbackEndpoint, HttpClient;
var init_client3 = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/http/client.js"() {
    import_cross_fetch2 = __toESM(require_browser_ponyfill(), 1);
    init_client();
    init_errors();
    init_stream3();
    checkEndpoints = [
      {
        versionPath: "v3-protobuf",
        pipelinePath: "v3-protobuf/pipeline",
        cursorPath: "v3-protobuf/cursor",
        version: 3,
        encoding: "protobuf"
      }
      /*
      {
          versionPath: "v3",
          pipelinePath: "v3/pipeline",
          cursorPath: "v3/cursor",
          version: 3,
          encoding: "json",
      },
      */
    ];
    fallbackEndpoint = {
      versionPath: "v2",
      pipelinePath: "v2/pipeline",
      cursorPath: void 0,
      version: 2,
      encoding: "json"
    };
    HttpClient = class extends Client {
      static {
        __name(this, "HttpClient");
      }
      #url;
      #jwt;
      #fetch;
      #remoteEncryptionKey;
      #closed;
      #streams;
      /** @private */
      _endpointPromise;
      /** @private */
      _endpoint;
      /** @private */
      constructor(url, jwt, customFetch, remoteEncryptionKey, protocolVersion = 2) {
        super();
        this.#url = url;
        this.#jwt = jwt;
        this.#fetch = customFetch ?? import_cross_fetch2.fetch;
        this.#remoteEncryptionKey = remoteEncryptionKey;
        this.#closed = void 0;
        this.#streams = /* @__PURE__ */ new Set();
        if (protocolVersion == 3) {
          this._endpointPromise = findEndpoint(this.#fetch, this.#url);
          this._endpointPromise.then((endpoint) => this._endpoint = endpoint, (error) => this.#setClosed(error));
        } else {
          this._endpointPromise = Promise.resolve(fallbackEndpoint);
          this._endpointPromise.then((endpoint) => this._endpoint = endpoint, (error) => this.#setClosed(error));
        }
      }
      /** Get the protocol version supported by the server. */
      async getVersion() {
        if (this._endpoint !== void 0) {
          return this._endpoint.version;
        }
        return (await this._endpointPromise).version;
      }
      // Make sure that the negotiated version is at least `minVersion`.
      /** @private */
      _ensureVersion(minVersion, feature2) {
        if (minVersion <= fallbackEndpoint.version) {
          return;
        } else if (this._endpoint === void 0) {
          throw new ProtocolVersionError(`${feature2} is supported only on protocol version ${minVersion} and higher, but the version supported by the HTTP server is not yet known. Use Client.getVersion() to wait until the version is available.`);
        } else if (this._endpoint.version < minVersion) {
          throw new ProtocolVersionError(`${feature2} is supported only on protocol version ${minVersion} and higher, but the HTTP server only supports version ${this._endpoint.version}.`);
        }
      }
      /** Open a {@link HttpStream}, a stream for executing SQL statements. */
      openStream() {
        if (this.#closed !== void 0) {
          throw new ClosedError("Client is closed", this.#closed);
        }
        const stream = new HttpStream(this, this.#url, this.#jwt, this.#fetch, this.#remoteEncryptionKey);
        this.#streams.add(stream);
        return stream;
      }
      /** @private */
      _streamClosed(stream) {
        this.#streams.delete(stream);
      }
      /** Close the client and all its streams. */
      close() {
        this.#setClosed(new ClientError("Client was manually closed"));
      }
      /** True if the client is closed. */
      get closed() {
        return this.#closed !== void 0;
      }
      #setClosed(error) {
        if (this.#closed !== void 0) {
          return;
        }
        this.#closed = error;
        for (const stream of Array.from(this.#streams)) {
          stream._setClosed(new ClosedError("Client was closed", error));
        }
      }
    };
    __name(findEndpoint, "findEndpoint");
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/libsql_url.js
var init_libsql_url = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/libsql_url.js"() {
    init_errors();
  }
});

// worker/node_modules/@libsql/hrana-client/lib-esm/index.js
function openWs(url, jwt, protocolVersion = 2) {
  if (typeof _WebSocket === "undefined") {
    throw new WebSocketUnsupportedError("WebSockets are not supported in this environment");
  }
  var subprotocols = void 0;
  if (protocolVersion == 3) {
    subprotocols = Array.from(subprotocolsV3.keys());
  } else {
    subprotocols = Array.from(subprotocolsV2.keys());
  }
  const socket = new _WebSocket(url, subprotocols);
  return new WsClient(socket, jwt);
}
function openHttp(url, jwt, customFetch, remoteEncryptionKey, protocolVersion = 2) {
  return new HttpClient(url instanceof URL ? url : new URL(url), jwt, customFetch, remoteEncryptionKey, protocolVersion);
}
var import_cross_fetch3;
var init_lib_esm = __esm({
  "worker/node_modules/@libsql/hrana-client/lib-esm/index.js"() {
    init_web();
    init_client2();
    init_errors();
    init_client3();
    init_client2();
    init_web();
    import_cross_fetch3 = __toESM(require_browser_ponyfill(), 1);
    init_client();
    init_errors();
    init_batch();
    init_libsql_url();
    init_sql();
    init_stmt();
    init_stream();
    init_client3();
    init_stream3();
    init_client2();
    init_stream2();
    __name(openWs, "openWs");
    __name(openHttp, "openHttp");
  }
});

// worker/node_modules/@libsql/client/lib-esm/hrana.js
async function executeHranaBatch(mode, version5, batch, hranaStmts, disableForeignKeys = false) {
  if (disableForeignKeys) {
    batch.step().run("PRAGMA foreign_keys=off");
  }
  const beginStep = batch.step();
  const beginPromise = beginStep.run(transactionModeToBegin(mode));
  let lastStep = beginStep;
  const stmtPromises = hranaStmts.map((hranaStmt) => {
    const stmtStep = batch.step().condition(BatchCond.ok(lastStep));
    if (version5 >= 3) {
      stmtStep.condition(BatchCond.not(BatchCond.isAutocommit(batch)));
    }
    const stmtPromise = stmtStep.query(hranaStmt);
    lastStep = stmtStep;
    return stmtPromise;
  });
  const commitStep = batch.step().condition(BatchCond.ok(lastStep));
  if (version5 >= 3) {
    commitStep.condition(BatchCond.not(BatchCond.isAutocommit(batch)));
  }
  const commitPromise = commitStep.run("COMMIT");
  const rollbackStep = batch.step().condition(BatchCond.not(BatchCond.ok(commitStep)));
  rollbackStep.run("ROLLBACK").catch((_) => void 0);
  if (disableForeignKeys) {
    batch.step().run("PRAGMA foreign_keys=on");
  }
  await batch.execute();
  const resultSets = [];
  await beginPromise;
  for (let i = 0; i < stmtPromises.length; i++) {
    try {
      const hranaRows = await stmtPromises[i];
      if (hranaRows === void 0) {
        throw new LibsqlBatchError("Statement in a batch was not executed, probably because the transaction has been rolled back", i, "TRANSACTION_CLOSED");
      }
      resultSets.push(resultSetFromHrana(hranaRows));
    } catch (e) {
      if (e instanceof LibsqlBatchError) {
        throw e;
      }
      const mappedError = mapHranaError(e);
      if (mappedError instanceof LibsqlError) {
        throw new LibsqlBatchError(mappedError.message, i, mappedError.code, mappedError.extendedCode, mappedError.rawCode, mappedError.cause instanceof Error ? mappedError.cause : void 0);
      }
      throw mappedError;
    }
  }
  await commitPromise;
  return resultSets;
}
function stmtToHrana(stmt) {
  let sql3;
  let args;
  if (Array.isArray(stmt)) {
    [sql3, args] = stmt;
  } else if (typeof stmt === "string") {
    sql3 = stmt;
  } else {
    sql3 = stmt.sql;
    args = stmt.args;
  }
  const hranaStmt = new Stmt(sql3);
  if (args) {
    if (Array.isArray(args)) {
      hranaStmt.bindIndexes(args);
    } else {
      for (const [key, value] of Object.entries(args)) {
        hranaStmt.bindName(key, value);
      }
    }
  }
  return hranaStmt;
}
function resultSetFromHrana(hranaRows) {
  const columns = hranaRows.columnNames.map((c) => c ?? "");
  const columnTypes = hranaRows.columnDecltypes.map((c) => c ?? "");
  const rows = hranaRows.rows;
  const rowsAffected = hranaRows.affectedRowCount;
  const lastInsertRowid = hranaRows.lastInsertRowid !== void 0 ? hranaRows.lastInsertRowid : void 0;
  return new ResultSetImpl(columns, columnTypes, rows, rowsAffected, lastInsertRowid);
}
function mapHranaError(e) {
  if (e instanceof ClientError) {
    const code = mapHranaErrorCode(e);
    return new LibsqlError(e.message, code, void 0, void 0, e);
  }
  return e;
}
function mapHranaErrorCode(e) {
  if (e instanceof ResponseError && e.code !== void 0) {
    return e.code;
  } else if (e instanceof ProtoError) {
    return "HRANA_PROTO_ERROR";
  } else if (e instanceof ClosedError) {
    return e.cause instanceof ClientError ? mapHranaErrorCode(e.cause) : "HRANA_CLOSED_ERROR";
  } else if (e instanceof WebSocketError) {
    return "HRANA_WEBSOCKET_ERROR";
  } else if (e instanceof HttpServerError) {
    return "SERVER_ERROR";
  } else if (e instanceof ProtocolVersionError) {
    return "PROTOCOL_VERSION_ERROR";
  } else if (e instanceof InternalError) {
    return "INTERNAL_ERROR";
  } else {
    return "UNKNOWN";
  }
}
var HranaTransaction;
var init_hrana = __esm({
  "worker/node_modules/@libsql/client/lib-esm/hrana.js"() {
    init_lib_esm();
    init_api();
    init_util();
    HranaTransaction = class {
      static {
        __name(this, "HranaTransaction");
      }
      #mode;
      #version;
      // Promise that is resolved when the BEGIN statement completes, or `undefined` if we haven't executed the
      // BEGIN statement yet.
      #started;
      /** @private */
      constructor(mode, version5) {
        this.#mode = mode;
        this.#version = version5;
        this.#started = void 0;
      }
      execute(stmt) {
        return this.batch([stmt]).then((results) => results[0]);
      }
      async batch(stmts) {
        const stream = this._getStream();
        if (stream.closed) {
          throw new LibsqlError("Cannot execute statements because the transaction is closed", "TRANSACTION_CLOSED");
        }
        try {
          const hranaStmts = stmts.map(stmtToHrana);
          let rowsPromises;
          if (this.#started === void 0) {
            this._getSqlCache().apply(hranaStmts);
            const batch = stream.batch(this.#version >= 3);
            const beginStep = batch.step();
            const beginPromise = beginStep.run(transactionModeToBegin(this.#mode));
            let lastStep = beginStep;
            rowsPromises = hranaStmts.map((hranaStmt) => {
              const stmtStep = batch.step().condition(BatchCond.ok(lastStep));
              if (this.#version >= 3) {
                stmtStep.condition(BatchCond.not(BatchCond.isAutocommit(batch)));
              }
              const rowsPromise = stmtStep.query(hranaStmt);
              rowsPromise.catch(() => void 0);
              lastStep = stmtStep;
              return rowsPromise;
            });
            this.#started = batch.execute().then(() => beginPromise).then(() => void 0);
            try {
              await this.#started;
            } catch (e) {
              this.close();
              throw e;
            }
          } else {
            if (this.#version < 3) {
              await this.#started;
            } else {
            }
            this._getSqlCache().apply(hranaStmts);
            const batch = stream.batch(this.#version >= 3);
            let lastStep = void 0;
            rowsPromises = hranaStmts.map((hranaStmt) => {
              const stmtStep = batch.step();
              if (lastStep !== void 0) {
                stmtStep.condition(BatchCond.ok(lastStep));
              }
              if (this.#version >= 3) {
                stmtStep.condition(BatchCond.not(BatchCond.isAutocommit(batch)));
              }
              const rowsPromise = stmtStep.query(hranaStmt);
              rowsPromise.catch(() => void 0);
              lastStep = stmtStep;
              return rowsPromise;
            });
            await batch.execute();
          }
          const resultSets = [];
          for (let i = 0; i < rowsPromises.length; i++) {
            try {
              const rows = await rowsPromises[i];
              if (rows === void 0) {
                throw new LibsqlBatchError("Statement in a transaction was not executed, probably because the transaction has been rolled back", i, "TRANSACTION_CLOSED");
              }
              resultSets.push(resultSetFromHrana(rows));
            } catch (e) {
              if (e instanceof LibsqlBatchError) {
                throw e;
              }
              const mappedError = mapHranaError(e);
              if (mappedError instanceof LibsqlError) {
                throw new LibsqlBatchError(mappedError.message, i, mappedError.code, mappedError.extendedCode, mappedError.rawCode, mappedError.cause instanceof Error ? mappedError.cause : void 0);
              }
              throw mappedError;
            }
          }
          return resultSets;
        } catch (e) {
          throw mapHranaError(e);
        }
      }
      async executeMultiple(sql3) {
        const stream = this._getStream();
        if (stream.closed) {
          throw new LibsqlError("Cannot execute statements because the transaction is closed", "TRANSACTION_CLOSED");
        }
        try {
          if (this.#started === void 0) {
            this.#started = stream.run(transactionModeToBegin(this.#mode)).then(() => void 0);
            try {
              await this.#started;
            } catch (e) {
              this.close();
              throw e;
            }
          } else {
            await this.#started;
          }
          await stream.sequence(sql3);
        } catch (e) {
          throw mapHranaError(e);
        }
      }
      async rollback() {
        try {
          const stream = this._getStream();
          if (stream.closed) {
            return;
          }
          if (this.#started !== void 0) {
          } else {
            return;
          }
          const promise = stream.run("ROLLBACK").catch((e) => {
            throw mapHranaError(e);
          });
          stream.closeGracefully();
          await promise;
        } catch (e) {
          throw mapHranaError(e);
        } finally {
          this.close();
        }
      }
      async commit() {
        try {
          const stream = this._getStream();
          if (stream.closed) {
            throw new LibsqlError("Cannot commit the transaction because it is already closed", "TRANSACTION_CLOSED");
          }
          if (this.#started !== void 0) {
            await this.#started;
          } else {
            return;
          }
          const promise = stream.run("COMMIT").catch((e) => {
            throw mapHranaError(e);
          });
          stream.closeGracefully();
          await promise;
        } catch (e) {
          throw mapHranaError(e);
        } finally {
          this.close();
        }
      }
    };
    __name(executeHranaBatch, "executeHranaBatch");
    __name(stmtToHrana, "stmtToHrana");
    __name(resultSetFromHrana, "resultSetFromHrana");
    __name(mapHranaError, "mapHranaError");
    __name(mapHranaErrorCode, "mapHranaErrorCode");
  }
});

// worker/node_modules/@libsql/client/lib-esm/sql_cache.js
var SqlCache, Lru;
var init_sql_cache = __esm({
  "worker/node_modules/@libsql/client/lib-esm/sql_cache.js"() {
    SqlCache = class {
      static {
        __name(this, "SqlCache");
      }
      #owner;
      #sqls;
      capacity;
      constructor(owner, capacity) {
        this.#owner = owner;
        this.#sqls = new Lru();
        this.capacity = capacity;
      }
      // Replaces SQL strings with cached `hrana.Sql` objects in the statements in `hranaStmts`. After this
      // function returns, we guarantee that all `hranaStmts` refer to valid (not closed) `hrana.Sql` objects,
      // but _we may invalidate any other `hrana.Sql` objects_ (by closing them, thus removing them from the
      // server).
      //
      // In practice, this means that after calling this function, you can use the statements only up to the
      // first `await`, because concurrent code may also use the cache and invalidate those statements.
      apply(hranaStmts) {
        if (this.capacity <= 0) {
          return;
        }
        const usedSqlObjs = /* @__PURE__ */ new Set();
        for (const hranaStmt of hranaStmts) {
          if (typeof hranaStmt.sql !== "string") {
            continue;
          }
          const sqlText = hranaStmt.sql;
          if (sqlText.length >= 5e3) {
            continue;
          }
          let sqlObj = this.#sqls.get(sqlText);
          if (sqlObj === void 0) {
            while (this.#sqls.size + 1 > this.capacity) {
              const [evictSqlText, evictSqlObj] = this.#sqls.peekLru();
              if (usedSqlObjs.has(evictSqlObj)) {
                break;
              }
              evictSqlObj.close();
              this.#sqls.delete(evictSqlText);
            }
            if (this.#sqls.size + 1 <= this.capacity) {
              sqlObj = this.#owner.storeSql(sqlText);
              this.#sqls.set(sqlText, sqlObj);
            }
          }
          if (sqlObj !== void 0) {
            hranaStmt.sql = sqlObj;
            usedSqlObjs.add(sqlObj);
          }
        }
      }
    };
    Lru = class {
      static {
        __name(this, "Lru");
      }
      // This maps keys to the cache values. The entries are ordered by their last use (entires that were used
      // most recently are at the end).
      #cache;
      constructor() {
        this.#cache = /* @__PURE__ */ new Map();
      }
      get(key) {
        const value = this.#cache.get(key);
        if (value !== void 0) {
          this.#cache.delete(key);
          this.#cache.set(key, value);
        }
        return value;
      }
      set(key, value) {
        this.#cache.set(key, value);
      }
      peekLru() {
        for (const entry of this.#cache.entries()) {
          return entry;
        }
        return void 0;
      }
      delete(key) {
        this.#cache.delete(key);
      }
      get size() {
        return this.#cache.size;
      }
    };
  }
});

// worker/node_modules/promise-limit/index.js
var require_promise_limit = __commonJS({
  "worker/node_modules/promise-limit/index.js"(exports, module) {
    function limiter(count) {
      var outstanding = 0;
      var jobs = [];
      function remove() {
        outstanding--;
        if (outstanding < count) {
          dequeue();
        }
      }
      __name(remove, "remove");
      function dequeue() {
        var job = jobs.shift();
        semaphore.queue = jobs.length;
        if (job) {
          run(job.fn).then(job.resolve).catch(job.reject);
        }
      }
      __name(dequeue, "dequeue");
      function queue(fn) {
        return new Promise(function(resolve, reject) {
          jobs.push({ fn, resolve, reject });
          semaphore.queue = jobs.length;
        });
      }
      __name(queue, "queue");
      function run(fn) {
        outstanding++;
        try {
          return Promise.resolve(fn()).then(function(result) {
            remove();
            return result;
          }, function(error) {
            remove();
            throw error;
          });
        } catch (err) {
          remove();
          return Promise.reject(err);
        }
      }
      __name(run, "run");
      var semaphore = /* @__PURE__ */ __name(function(fn) {
        if (outstanding >= count) {
          return queue(fn);
        } else {
          return run(fn);
        }
      }, "semaphore");
      return semaphore;
    }
    __name(limiter, "limiter");
    function map2(items, mapper) {
      var failed = false;
      var limit = this;
      return Promise.all(items.map(function() {
        var args = arguments;
        return limit(function() {
          if (!failed) {
            return mapper.apply(void 0, args).catch(function(e) {
              failed = true;
              throw e;
            });
          }
        });
      }));
    }
    __name(map2, "map");
    function addExtras(fn) {
      fn.queue = 0;
      fn.map = map2;
      return fn;
    }
    __name(addExtras, "addExtras");
    module.exports = function(count) {
      if (count) {
        return addExtras(limiter(count));
      } else {
        return addExtras(function(fn) {
          return fn();
        });
      }
    };
  }
});

// worker/node_modules/@libsql/client/lib-esm/ws.js
function _createClient(config) {
  if (config.scheme !== "wss" && config.scheme !== "ws") {
    throw new LibsqlError(`The WebSocket client supports only "libsql:", "wss:" and "ws:" URLs, got ${JSON.stringify(config.scheme + ":")}. For more information, please read ${supportedUrlLink}`, "URL_SCHEME_NOT_SUPPORTED");
  }
  if (config.encryptionKey !== void 0) {
    throw new LibsqlError("Encryption key is not supported by the remote client.", "ENCRYPTION_KEY_NOT_SUPPORTED");
  }
  if (config.scheme === "ws" && config.tls) {
    throw new LibsqlError(`A "ws:" URL cannot opt into TLS by using ?tls=1`, "URL_INVALID");
  } else if (config.scheme === "wss" && !config.tls) {
    throw new LibsqlError(`A "wss:" URL cannot opt out of TLS by using ?tls=0`, "URL_INVALID");
  }
  const url = encodeBaseUrl(config.scheme, config.authority, config.path);
  let client;
  try {
    client = openWs(url, config.authToken);
  } catch (e) {
    if (e instanceof WebSocketUnsupportedError) {
      const suggestedScheme = config.scheme === "wss" ? "https" : "http";
      const suggestedUrl = encodeBaseUrl(suggestedScheme, config.authority, config.path);
      throw new LibsqlError(`This environment does not support WebSockets, please switch to the HTTP client by using a "${suggestedScheme}:" URL (${JSON.stringify(suggestedUrl)}). For more information, please read ${supportedUrlLink}`, "WEBSOCKETS_NOT_SUPPORTED");
    }
    throw mapHranaError(e);
  }
  return new WsClient2(client, url, config.authToken, config.intMode, config.concurrency);
}
var import_promise_limit, maxConnAgeMillis, sqlCacheCapacity, WsClient2, WsTransaction;
var init_ws = __esm({
  "worker/node_modules/@libsql/client/lib-esm/ws.js"() {
    init_lib_esm();
    init_api();
    init_config();
    init_hrana();
    init_sql_cache();
    init_uri();
    init_util();
    import_promise_limit = __toESM(require_promise_limit(), 1);
    init_api();
    __name(_createClient, "_createClient");
    maxConnAgeMillis = 60 * 1e3;
    sqlCacheCapacity = 100;
    WsClient2 = class {
      static {
        __name(this, "WsClient");
      }
      #url;
      #authToken;
      #intMode;
      // State of the current connection. The `hrana.WsClient` inside may be closed at any moment due to an
      // asynchronous error.
      #connState;
      // If defined, this is a connection that will be used in the future, once it is ready.
      #futureConnState;
      closed;
      protocol;
      #isSchemaDatabase;
      #promiseLimitFunction;
      /** @private */
      constructor(client, url, authToken, intMode, concurrency) {
        this.#url = url;
        this.#authToken = authToken;
        this.#intMode = intMode;
        this.#connState = this.#openConn(client);
        this.#futureConnState = void 0;
        this.closed = false;
        this.protocol = "ws";
        this.#promiseLimitFunction = (0, import_promise_limit.default)(concurrency);
      }
      async limit(fn) {
        return this.#promiseLimitFunction(fn);
      }
      async execute(stmtOrSql, args) {
        let stmt;
        if (typeof stmtOrSql === "string") {
          stmt = {
            sql: stmtOrSql,
            args: args || []
          };
        } else {
          stmt = stmtOrSql;
        }
        return this.limit(async () => {
          const streamState = await this.#openStream();
          try {
            const hranaStmt = stmtToHrana(stmt);
            streamState.conn.sqlCache.apply([hranaStmt]);
            const hranaRowsPromise = streamState.stream.query(hranaStmt);
            streamState.stream.closeGracefully();
            const hranaRowsResult = await hranaRowsPromise;
            return resultSetFromHrana(hranaRowsResult);
          } catch (e) {
            throw mapHranaError(e);
          } finally {
            this._closeStream(streamState);
          }
        });
      }
      async batch(stmts, mode = "deferred") {
        return this.limit(async () => {
          const streamState = await this.#openStream();
          try {
            const normalizedStmts = stmts.map((stmt) => {
              if (Array.isArray(stmt)) {
                return {
                  sql: stmt[0],
                  args: stmt[1] || []
                };
              }
              return stmt;
            });
            const hranaStmts = normalizedStmts.map(stmtToHrana);
            const version5 = await streamState.conn.client.getVersion();
            streamState.conn.sqlCache.apply(hranaStmts);
            const batch = streamState.stream.batch(version5 >= 3);
            const resultsPromise = executeHranaBatch(mode, version5, batch, hranaStmts);
            const results = await resultsPromise;
            return results;
          } catch (e) {
            throw mapHranaError(e);
          } finally {
            this._closeStream(streamState);
          }
        });
      }
      async migrate(stmts) {
        return this.limit(async () => {
          const streamState = await this.#openStream();
          try {
            const hranaStmts = stmts.map(stmtToHrana);
            const version5 = await streamState.conn.client.getVersion();
            const batch = streamState.stream.batch(version5 >= 3);
            const resultsPromise = executeHranaBatch("deferred", version5, batch, hranaStmts, true);
            const results = await resultsPromise;
            return results;
          } catch (e) {
            throw mapHranaError(e);
          } finally {
            this._closeStream(streamState);
          }
        });
      }
      async transaction(mode = "write") {
        return this.limit(async () => {
          const streamState = await this.#openStream();
          try {
            const version5 = await streamState.conn.client.getVersion();
            return new WsTransaction(this, streamState, mode, version5);
          } catch (e) {
            this._closeStream(streamState);
            throw mapHranaError(e);
          }
        });
      }
      async executeMultiple(sql3) {
        return this.limit(async () => {
          const streamState = await this.#openStream();
          try {
            const promise = streamState.stream.sequence(sql3);
            streamState.stream.closeGracefully();
            await promise;
          } catch (e) {
            throw mapHranaError(e);
          } finally {
            this._closeStream(streamState);
          }
        });
      }
      sync() {
        throw new LibsqlError("sync not supported in ws mode", "SYNC_NOT_SUPPORTED");
      }
      async #openStream() {
        if (this.closed) {
          throw new LibsqlError("The client is closed", "CLIENT_CLOSED");
        }
        const now = /* @__PURE__ */ new Date();
        const ageMillis = now.valueOf() - this.#connState.openTime.valueOf();
        if (ageMillis > maxConnAgeMillis && this.#futureConnState === void 0) {
          const futureConnState = this.#openConn();
          this.#futureConnState = futureConnState;
          futureConnState.client.getVersion().then((_version) => {
            if (this.#connState !== futureConnState) {
              if (this.#connState.streamStates.size === 0) {
                this.#connState.client.close();
              } else {
              }
            }
            this.#connState = futureConnState;
            this.#futureConnState = void 0;
          }, (_e) => {
            this.#futureConnState = void 0;
          });
        }
        if (this.#connState.client.closed) {
          try {
            if (this.#futureConnState !== void 0) {
              this.#connState = this.#futureConnState;
            } else {
              this.#connState = this.#openConn();
            }
          } catch (e) {
            throw mapHranaError(e);
          }
        }
        const connState = this.#connState;
        try {
          if (connState.useSqlCache === void 0) {
            connState.useSqlCache = await connState.client.getVersion() >= 2;
            if (connState.useSqlCache) {
              connState.sqlCache.capacity = sqlCacheCapacity;
            }
          }
          const stream = connState.client.openStream();
          stream.intMode = this.#intMode;
          const streamState = { conn: connState, stream };
          connState.streamStates.add(streamState);
          return streamState;
        } catch (e) {
          throw mapHranaError(e);
        }
      }
      #openConn(client) {
        try {
          client ??= openWs(this.#url, this.#authToken);
          return {
            client,
            useSqlCache: void 0,
            sqlCache: new SqlCache(client, 0),
            openTime: /* @__PURE__ */ new Date(),
            streamStates: /* @__PURE__ */ new Set()
          };
        } catch (e) {
          throw mapHranaError(e);
        }
      }
      async reconnect() {
        try {
          for (const st of Array.from(this.#connState.streamStates)) {
            try {
              st.stream.close();
            } catch {
            }
          }
          this.#connState.client.close();
        } catch {
        }
        if (this.#futureConnState) {
          try {
            this.#futureConnState.client.close();
          } catch {
          }
          this.#futureConnState = void 0;
        }
        const next = this.#openConn();
        const version5 = await next.client.getVersion();
        next.useSqlCache = version5 >= 2;
        if (next.useSqlCache) {
          next.sqlCache.capacity = sqlCacheCapacity;
        }
        this.#connState = next;
        this.closed = false;
      }
      _closeStream(streamState) {
        streamState.stream.close();
        const connState = streamState.conn;
        connState.streamStates.delete(streamState);
        if (connState.streamStates.size === 0 && connState !== this.#connState) {
          connState.client.close();
        }
      }
      close() {
        this.#connState.client.close();
        this.closed = true;
        if (this.#futureConnState) {
          try {
            this.#futureConnState.client.close();
          } catch {
          }
          this.#futureConnState = void 0;
        }
        this.closed = true;
      }
    };
    WsTransaction = class extends HranaTransaction {
      static {
        __name(this, "WsTransaction");
      }
      #client;
      #streamState;
      /** @private */
      constructor(client, state, mode, version5) {
        super(mode, version5);
        this.#client = client;
        this.#streamState = state;
      }
      /** @private */
      _getStream() {
        return this.#streamState.stream;
      }
      /** @private */
      _getSqlCache() {
        return this.#streamState.conn.sqlCache;
      }
      close() {
        this.#client._closeStream(this.#streamState);
      }
      get closed() {
        return this.#streamState.stream.closed;
      }
    };
  }
});

// worker/node_modules/@libsql/client/lib-esm/http.js
function _createClient2(config) {
  if (config.scheme !== "https" && config.scheme !== "http") {
    throw new LibsqlError(`The HTTP client supports only "libsql:", "https:" and "http:" URLs, got ${JSON.stringify(config.scheme + ":")}. For more information, please read ${supportedUrlLink}`, "URL_SCHEME_NOT_SUPPORTED");
  }
  if (config.encryptionKey !== void 0) {
    throw new LibsqlError("Encryption key is not supported by the remote client.", "ENCRYPTION_KEY_NOT_SUPPORTED");
  }
  if (config.scheme === "http" && config.tls) {
    throw new LibsqlError(`A "http:" URL cannot opt into TLS by using ?tls=1`, "URL_INVALID");
  } else if (config.scheme === "https" && !config.tls) {
    throw new LibsqlError(`A "https:" URL cannot opt out of TLS by using ?tls=0`, "URL_INVALID");
  }
  const url = encodeBaseUrl(config.scheme, config.authority, config.path);
  return new HttpClient2(url, config.authToken, config.intMode, config.fetch, config.concurrency, config.remoteEncryptionKey);
}
var import_promise_limit2, sqlCacheCapacity2, HttpClient2, HttpTransaction;
var init_http = __esm({
  "worker/node_modules/@libsql/client/lib-esm/http.js"() {
    init_lib_esm();
    init_api();
    init_config();
    init_hrana();
    init_sql_cache();
    init_uri();
    init_util();
    import_promise_limit2 = __toESM(require_promise_limit(), 1);
    init_api();
    __name(_createClient2, "_createClient");
    sqlCacheCapacity2 = 30;
    HttpClient2 = class {
      static {
        __name(this, "HttpClient");
      }
      #client;
      protocol;
      #url;
      #intMode;
      #customFetch;
      #concurrency;
      #authToken;
      #remoteEncryptionKey;
      #promiseLimitFunction;
      /** @private */
      constructor(url, authToken, intMode, customFetch, concurrency, remoteEncryptionKey) {
        this.#url = url;
        this.#authToken = authToken;
        this.#intMode = intMode;
        this.#customFetch = customFetch;
        this.#concurrency = concurrency;
        this.#remoteEncryptionKey = remoteEncryptionKey;
        this.#client = openHttp(this.#url, this.#authToken, this.#customFetch, remoteEncryptionKey);
        this.#client.intMode = this.#intMode;
        this.protocol = "http";
        this.#promiseLimitFunction = (0, import_promise_limit2.default)(this.#concurrency);
      }
      async limit(fn) {
        return this.#promiseLimitFunction(fn);
      }
      async execute(stmtOrSql, args) {
        let stmt;
        if (typeof stmtOrSql === "string") {
          stmt = {
            sql: stmtOrSql,
            args: args || []
          };
        } else {
          stmt = stmtOrSql;
        }
        return this.limit(async () => {
          try {
            const hranaStmt = stmtToHrana(stmt);
            let rowsPromise;
            const stream = this.#client.openStream();
            try {
              rowsPromise = stream.query(hranaStmt);
            } finally {
              stream.closeGracefully();
            }
            const rowsResult = await rowsPromise;
            return resultSetFromHrana(rowsResult);
          } catch (e) {
            throw mapHranaError(e);
          }
        });
      }
      async batch(stmts, mode = "deferred") {
        return this.limit(async () => {
          try {
            const normalizedStmts = stmts.map((stmt) => {
              if (Array.isArray(stmt)) {
                return {
                  sql: stmt[0],
                  args: stmt[1] || []
                };
              }
              return stmt;
            });
            const hranaStmts = normalizedStmts.map(stmtToHrana);
            const version5 = await this.#client.getVersion();
            let resultsPromise;
            const stream = this.#client.openStream();
            try {
              const sqlCache = new SqlCache(stream, sqlCacheCapacity2);
              sqlCache.apply(hranaStmts);
              const batch = stream.batch(false);
              resultsPromise = executeHranaBatch(mode, version5, batch, hranaStmts);
            } finally {
              stream.closeGracefully();
            }
            const results = await resultsPromise;
            return results;
          } catch (e) {
            throw mapHranaError(e);
          }
        });
      }
      async migrate(stmts) {
        return this.limit(async () => {
          try {
            const hranaStmts = stmts.map(stmtToHrana);
            const version5 = await this.#client.getVersion();
            let resultsPromise;
            const stream = this.#client.openStream();
            try {
              const batch = stream.batch(false);
              resultsPromise = executeHranaBatch("deferred", version5, batch, hranaStmts, true);
            } finally {
              stream.closeGracefully();
            }
            const results = await resultsPromise;
            return results;
          } catch (e) {
            throw mapHranaError(e);
          }
        });
      }
      async transaction(mode = "write") {
        return this.limit(async () => {
          try {
            const version5 = await this.#client.getVersion();
            return new HttpTransaction(this.#client.openStream(), mode, version5);
          } catch (e) {
            throw mapHranaError(e);
          }
        });
      }
      async executeMultiple(sql3) {
        return this.limit(async () => {
          try {
            let promise;
            const stream = this.#client.openStream();
            try {
              promise = stream.sequence(sql3);
            } finally {
              stream.closeGracefully();
            }
            await promise;
          } catch (e) {
            throw mapHranaError(e);
          }
        });
      }
      sync() {
        throw new LibsqlError("sync not supported in http mode", "SYNC_NOT_SUPPORTED");
      }
      close() {
        this.#client.close();
      }
      async reconnect() {
        try {
          if (!this.closed) {
            this.#client.close();
          }
        } finally {
          this.#client = openHttp(this.#url, this.#authToken, this.#customFetch, this.#remoteEncryptionKey);
          this.#client.intMode = this.#intMode;
        }
      }
      get closed() {
        return this.#client.closed;
      }
    };
    HttpTransaction = class extends HranaTransaction {
      static {
        __name(this, "HttpTransaction");
      }
      #stream;
      #sqlCache;
      /** @private */
      constructor(stream, mode, version5) {
        super(mode, version5);
        this.#stream = stream;
        this.#sqlCache = new SqlCache(stream, sqlCacheCapacity2);
      }
      /** @private */
      _getStream() {
        return this.#stream;
      }
      /** @private */
      _getSqlCache() {
        return this.#sqlCache;
      }
      close() {
        this.#stream.close();
      }
      get closed() {
        return this.#stream.closed;
      }
    };
  }
});

// worker/node_modules/@libsql/client/lib-esm/web.js
function createClient(config) {
  return _createClient3(expandConfig(config, true));
}
function _createClient3(config) {
  if (config.scheme === "ws" || config.scheme === "wss") {
    return _createClient(config);
  } else if (config.scheme === "http" || config.scheme === "https") {
    return _createClient2(config);
  } else {
    throw new LibsqlError(`The client that uses Web standard APIs supports only "libsql:", "wss:", "ws:", "https:" and "http:" URLs, got ${JSON.stringify(config.scheme + ":")}. For more information, please read ${supportedUrlLink}`, "URL_SCHEME_NOT_SUPPORTED");
  }
}
var init_web2 = __esm({
  "worker/node_modules/@libsql/client/lib-esm/web.js"() {
    init_api();
    init_config();
    init_util();
    init_ws();
    init_http();
    init_api();
    __name(createClient, "createClient");
    __name(_createClient3, "_createClient");
  }
});

// worker/node_modules/drizzle-orm/entity.js
function is(value, type) {
  if (!value || typeof value !== "object") {
    return false;
  }
  if (value instanceof type) {
    return true;
  }
  if (!Object.prototype.hasOwnProperty.call(type, entityKind)) {
    throw new Error(
      `Class "${type.name ?? "<unknown>"}" doesn't look like a Drizzle entity. If this is incorrect and the class is provided by Drizzle, please report this as a bug.`
    );
  }
  let cls = Object.getPrototypeOf(value).constructor;
  if (cls) {
    while (cls) {
      if (entityKind in cls && cls[entityKind] === type[entityKind]) {
        return true;
      }
      cls = Object.getPrototypeOf(cls);
    }
  }
  return false;
}
var entityKind, hasOwnEntityKind;
var init_entity = __esm({
  "worker/node_modules/drizzle-orm/entity.js"() {
    entityKind = Symbol.for("drizzle:entityKind");
    hasOwnEntityKind = Symbol.for("drizzle:hasOwnEntityKind");
    __name(is, "is");
  }
});

// worker/node_modules/drizzle-orm/column.js
var Column;
var init_column = __esm({
  "worker/node_modules/drizzle-orm/column.js"() {
    init_entity();
    Column = class {
      static {
        __name(this, "Column");
      }
      constructor(table, config) {
        this.table = table;
        this.config = config;
        this.name = config.name;
        this.keyAsName = config.keyAsName;
        this.notNull = config.notNull;
        this.default = config.default;
        this.defaultFn = config.defaultFn;
        this.onUpdateFn = config.onUpdateFn;
        this.hasDefault = config.hasDefault;
        this.primary = config.primaryKey;
        this.isUnique = config.isUnique;
        this.uniqueName = config.uniqueName;
        this.uniqueType = config.uniqueType;
        this.dataType = config.dataType;
        this.columnType = config.columnType;
        this.generated = config.generated;
        this.generatedIdentity = config.generatedIdentity;
      }
      static [entityKind] = "Column";
      name;
      keyAsName;
      primary;
      notNull;
      default;
      defaultFn;
      onUpdateFn;
      hasDefault;
      isUnique;
      uniqueName;
      uniqueType;
      dataType;
      columnType;
      enumValues = void 0;
      generated = void 0;
      generatedIdentity = void 0;
      config;
      mapFromDriverValue(value) {
        return value;
      }
      mapToDriverValue(value) {
        return value;
      }
      // ** @internal */
      shouldDisableInsert() {
        return this.config.generated !== void 0 && this.config.generated.type !== "byDefault";
      }
    };
  }
});

// worker/node_modules/drizzle-orm/column-builder.js
var ColumnBuilder;
var init_column_builder = __esm({
  "worker/node_modules/drizzle-orm/column-builder.js"() {
    init_entity();
    ColumnBuilder = class {
      static {
        __name(this, "ColumnBuilder");
      }
      static [entityKind] = "ColumnBuilder";
      config;
      constructor(name, dataType, columnType) {
        this.config = {
          name,
          keyAsName: name === "",
          notNull: false,
          default: void 0,
          hasDefault: false,
          primaryKey: false,
          isUnique: false,
          uniqueName: void 0,
          uniqueType: void 0,
          dataType,
          columnType,
          generated: void 0
        };
      }
      /**
       * Changes the data type of the column. Commonly used with `json` columns. Also, useful for branded types.
       *
       * @example
       * ```ts
       * const users = pgTable('users', {
       * 	id: integer('id').$type<UserId>().primaryKey(),
       * 	details: json('details').$type<UserDetails>().notNull(),
       * });
       * ```
       */
      $type() {
        return this;
      }
      /**
       * Adds a `not null` clause to the column definition.
       *
       * Affects the `select` model of the table - columns *without* `not null` will be nullable on select.
       */
      notNull() {
        this.config.notNull = true;
        return this;
      }
      /**
       * Adds a `default <value>` clause to the column definition.
       *
       * Affects the `insert` model of the table - columns *with* `default` are optional on insert.
       *
       * If you need to set a dynamic default value, use {@link $defaultFn} instead.
       */
      default(value) {
        this.config.default = value;
        this.config.hasDefault = true;
        return this;
      }
      /**
       * Adds a dynamic default value to the column.
       * The function will be called when the row is inserted, and the returned value will be used as the column value.
       *
       * **Note:** This value does not affect the `drizzle-kit` behavior, it is only used at runtime in `drizzle-orm`.
       */
      $defaultFn(fn) {
        this.config.defaultFn = fn;
        this.config.hasDefault = true;
        return this;
      }
      /**
       * Alias for {@link $defaultFn}.
       */
      $default = this.$defaultFn;
      /**
       * Adds a dynamic update value to the column.
       * The function will be called when the row is updated, and the returned value will be used as the column value if none is provided.
       * If no `default` (or `$defaultFn`) value is provided, the function will be called when the row is inserted as well, and the returned value will be used as the column value.
       *
       * **Note:** This value does not affect the `drizzle-kit` behavior, it is only used at runtime in `drizzle-orm`.
       */
      $onUpdateFn(fn) {
        this.config.onUpdateFn = fn;
        this.config.hasDefault = true;
        return this;
      }
      /**
       * Alias for {@link $onUpdateFn}.
       */
      $onUpdate = this.$onUpdateFn;
      /**
       * Adds a `primary key` clause to the column definition. This implicitly makes the column `not null`.
       *
       * In SQLite, `integer primary key` implicitly makes the column auto-incrementing.
       */
      primaryKey() {
        this.config.primaryKey = true;
        this.config.notNull = true;
        return this;
      }
      /** @internal Sets the name of the column to the key within the table definition if a name was not given. */
      setName(name) {
        if (this.config.name !== "")
          return;
        this.config.name = name;
      }
    };
  }
});

// worker/node_modules/drizzle-orm/table.utils.js
var TableName;
var init_table_utils = __esm({
  "worker/node_modules/drizzle-orm/table.utils.js"() {
    TableName = Symbol.for("drizzle:Name");
  }
});

// worker/node_modules/drizzle-orm/pg-core/foreign-keys.js
var ForeignKeyBuilder, ForeignKey;
var init_foreign_keys = __esm({
  "worker/node_modules/drizzle-orm/pg-core/foreign-keys.js"() {
    init_entity();
    init_table_utils();
    ForeignKeyBuilder = class {
      static {
        __name(this, "ForeignKeyBuilder");
      }
      static [entityKind] = "PgForeignKeyBuilder";
      /** @internal */
      reference;
      /** @internal */
      _onUpdate = "no action";
      /** @internal */
      _onDelete = "no action";
      constructor(config, actions) {
        this.reference = () => {
          const { name, columns, foreignColumns } = config();
          return { name, columns, foreignTable: foreignColumns[0].table, foreignColumns };
        };
        if (actions) {
          this._onUpdate = actions.onUpdate;
          this._onDelete = actions.onDelete;
        }
      }
      onUpdate(action) {
        this._onUpdate = action === void 0 ? "no action" : action;
        return this;
      }
      onDelete(action) {
        this._onDelete = action === void 0 ? "no action" : action;
        return this;
      }
      /** @internal */
      build(table) {
        return new ForeignKey(table, this);
      }
    };
    ForeignKey = class {
      static {
        __name(this, "ForeignKey");
      }
      constructor(table, builder) {
        this.table = table;
        this.reference = builder.reference;
        this.onUpdate = builder._onUpdate;
        this.onDelete = builder._onDelete;
      }
      static [entityKind] = "PgForeignKey";
      reference;
      onUpdate;
      onDelete;
      getName() {
        const { name, columns, foreignColumns } = this.reference();
        const columnNames = columns.map((column) => column.name);
        const foreignColumnNames = foreignColumns.map((column) => column.name);
        const chunks = [
          this.table[TableName],
          ...columnNames,
          foreignColumns[0].table[TableName],
          ...foreignColumnNames
        ];
        return name ?? `${chunks.join("_")}_fk`;
      }
    };
  }
});

// worker/node_modules/drizzle-orm/tracing-utils.js
function iife(fn, ...args) {
  return fn(...args);
}
var init_tracing_utils = __esm({
  "worker/node_modules/drizzle-orm/tracing-utils.js"() {
    __name(iife, "iife");
  }
});

// worker/node_modules/drizzle-orm/pg-core/unique-constraint.js
function uniqueKeyName(table, columns) {
  return `${table[TableName]}_${columns.join("_")}_unique`;
}
var UniqueConstraintBuilder, UniqueOnConstraintBuilder, UniqueConstraint;
var init_unique_constraint = __esm({
  "worker/node_modules/drizzle-orm/pg-core/unique-constraint.js"() {
    init_entity();
    init_table_utils();
    __name(uniqueKeyName, "uniqueKeyName");
    UniqueConstraintBuilder = class {
      static {
        __name(this, "UniqueConstraintBuilder");
      }
      constructor(columns, name) {
        this.name = name;
        this.columns = columns;
      }
      static [entityKind] = "PgUniqueConstraintBuilder";
      /** @internal */
      columns;
      /** @internal */
      nullsNotDistinctConfig = false;
      nullsNotDistinct() {
        this.nullsNotDistinctConfig = true;
        return this;
      }
      /** @internal */
      build(table) {
        return new UniqueConstraint(table, this.columns, this.nullsNotDistinctConfig, this.name);
      }
    };
    UniqueOnConstraintBuilder = class {
      static {
        __name(this, "UniqueOnConstraintBuilder");
      }
      static [entityKind] = "PgUniqueOnConstraintBuilder";
      /** @internal */
      name;
      constructor(name) {
        this.name = name;
      }
      on(...columns) {
        return new UniqueConstraintBuilder(columns, this.name);
      }
    };
    UniqueConstraint = class {
      static {
        __name(this, "UniqueConstraint");
      }
      constructor(table, columns, nullsNotDistinct, name) {
        this.table = table;
        this.columns = columns;
        this.name = name ?? uniqueKeyName(this.table, this.columns.map((column) => column.name));
        this.nullsNotDistinct = nullsNotDistinct;
      }
      static [entityKind] = "PgUniqueConstraint";
      columns;
      name;
      nullsNotDistinct = false;
      getName() {
        return this.name;
      }
    };
  }
});

// worker/node_modules/drizzle-orm/pg-core/utils/array.js
function parsePgArrayValue(arrayString, startFrom, inQuotes) {
  for (let i = startFrom; i < arrayString.length; i++) {
    const char = arrayString[i];
    if (char === "\\") {
      i++;
      continue;
    }
    if (char === '"') {
      return [arrayString.slice(startFrom, i).replace(/\\/g, ""), i + 1];
    }
    if (inQuotes) {
      continue;
    }
    if (char === "," || char === "}") {
      return [arrayString.slice(startFrom, i).replace(/\\/g, ""), i];
    }
  }
  return [arrayString.slice(startFrom).replace(/\\/g, ""), arrayString.length];
}
function parsePgNestedArray(arrayString, startFrom = 0) {
  const result = [];
  let i = startFrom;
  let lastCharIsComma = false;
  while (i < arrayString.length) {
    const char = arrayString[i];
    if (char === ",") {
      if (lastCharIsComma || i === startFrom) {
        result.push("");
      }
      lastCharIsComma = true;
      i++;
      continue;
    }
    lastCharIsComma = false;
    if (char === "\\") {
      i += 2;
      continue;
    }
    if (char === '"') {
      const [value2, startFrom2] = parsePgArrayValue(arrayString, i + 1, true);
      result.push(value2);
      i = startFrom2;
      continue;
    }
    if (char === "}") {
      return [result, i + 1];
    }
    if (char === "{") {
      const [value2, startFrom2] = parsePgNestedArray(arrayString, i + 1);
      result.push(value2);
      i = startFrom2;
      continue;
    }
    const [value, newStartFrom] = parsePgArrayValue(arrayString, i, false);
    result.push(value);
    i = newStartFrom;
  }
  return [result, i];
}
function parsePgArray(arrayString) {
  const [result] = parsePgNestedArray(arrayString, 1);
  return result;
}
function makePgArray(array3) {
  return `{${array3.map((item) => {
    if (Array.isArray(item)) {
      return makePgArray(item);
    }
    if (typeof item === "string") {
      return `"${item.replace(/\\/g, "\\\\").replace(/"/g, '\\"')}"`;
    }
    return `${item}`;
  }).join(",")}}`;
}
var init_array = __esm({
  "worker/node_modules/drizzle-orm/pg-core/utils/array.js"() {
    __name(parsePgArrayValue, "parsePgArrayValue");
    __name(parsePgNestedArray, "parsePgNestedArray");
    __name(parsePgArray, "parsePgArray");
    __name(makePgArray, "makePgArray");
  }
});

// worker/node_modules/drizzle-orm/pg-core/columns/common.js
var PgColumnBuilder, PgColumn, ExtraConfigColumn, IndexedColumn, PgArrayBuilder, PgArray;
var init_common = __esm({
  "worker/node_modules/drizzle-orm/pg-core/columns/common.js"() {
    init_column_builder();
    init_column();
    init_entity();
    init_foreign_keys();
    init_tracing_utils();
    init_unique_constraint();
    init_array();
    PgColumnBuilder = class extends ColumnBuilder {
      static {
        __name(this, "PgColumnBuilder");
      }
      foreignKeyConfigs = [];
      static [entityKind] = "PgColumnBuilder";
      array(size) {
        return new PgArrayBuilder(this.config.name, this, size);
      }
      references(ref, actions = {}) {
        this.foreignKeyConfigs.push({ ref, actions });
        return this;
      }
      unique(name, config) {
        this.config.isUnique = true;
        this.config.uniqueName = name;
        this.config.uniqueType = config?.nulls;
        return this;
      }
      generatedAlwaysAs(as) {
        this.config.generated = {
          as,
          type: "always",
          mode: "stored"
        };
        return this;
      }
      /** @internal */
      buildForeignKeys(column, table) {
        return this.foreignKeyConfigs.map(({ ref, actions }) => {
          return iife(
            (ref2, actions2) => {
              const builder = new ForeignKeyBuilder(() => {
                const foreignColumn = ref2();
                return { columns: [column], foreignColumns: [foreignColumn] };
              });
              if (actions2.onUpdate) {
                builder.onUpdate(actions2.onUpdate);
              }
              if (actions2.onDelete) {
                builder.onDelete(actions2.onDelete);
              }
              return builder.build(table);
            },
            ref,
            actions
          );
        });
      }
      /** @internal */
      buildExtraConfigColumn(table) {
        return new ExtraConfigColumn(table, this.config);
      }
    };
    PgColumn = class extends Column {
      static {
        __name(this, "PgColumn");
      }
      constructor(table, config) {
        if (!config.uniqueName) {
          config.uniqueName = uniqueKeyName(table, [config.name]);
        }
        super(table, config);
        this.table = table;
      }
      static [entityKind] = "PgColumn";
    };
    ExtraConfigColumn = class extends PgColumn {
      static {
        __name(this, "ExtraConfigColumn");
      }
      static [entityKind] = "ExtraConfigColumn";
      getSQLType() {
        return this.getSQLType();
      }
      indexConfig = {
        order: this.config.order ?? "asc",
        nulls: this.config.nulls ?? "last",
        opClass: this.config.opClass
      };
      defaultConfig = {
        order: "asc",
        nulls: "last",
        opClass: void 0
      };
      asc() {
        this.indexConfig.order = "asc";
        return this;
      }
      desc() {
        this.indexConfig.order = "desc";
        return this;
      }
      nullsFirst() {
        this.indexConfig.nulls = "first";
        return this;
      }
      nullsLast() {
        this.indexConfig.nulls = "last";
        return this;
      }
      /**
       * ### PostgreSQL documentation quote
       *
       * > An operator class with optional parameters can be specified for each column of an index.
       * The operator class identifies the operators to be used by the index for that column.
       * For example, a B-tree index on four-byte integers would use the int4_ops class;
       * this operator class includes comparison functions for four-byte integers.
       * In practice the default operator class for the column's data type is usually sufficient.
       * The main point of having operator classes is that for some data types, there could be more than one meaningful ordering.
       * For example, we might want to sort a complex-number data type either by absolute value or by real part.
       * We could do this by defining two operator classes for the data type and then selecting the proper class when creating an index.
       * More information about operator classes check:
       *
       * ### Useful links
       * https://www.postgresql.org/docs/current/sql-createindex.html
       *
       * https://www.postgresql.org/docs/current/indexes-opclass.html
       *
       * https://www.postgresql.org/docs/current/xindex.html
       *
       * ### Additional types
       * If you have the `pg_vector` extension installed in your database, you can use the
       * `vector_l2_ops`, `vector_ip_ops`, `vector_cosine_ops`, `vector_l1_ops`, `bit_hamming_ops`, `bit_jaccard_ops`, `halfvec_l2_ops`, `sparsevec_l2_ops` options, which are predefined types.
       *
       * **You can always specify any string you want in the operator class, in case Drizzle doesn't have it natively in its types**
       *
       * @param opClass
       * @returns
       */
      op(opClass) {
        this.indexConfig.opClass = opClass;
        return this;
      }
    };
    IndexedColumn = class {
      static {
        __name(this, "IndexedColumn");
      }
      static [entityKind] = "IndexedColumn";
      constructor(name, keyAsName, type, indexConfig) {
        this.name = name;
        this.keyAsName = keyAsName;
        this.type = type;
        this.indexConfig = indexConfig;
      }
      name;
      keyAsName;
      type;
      indexConfig;
    };
    PgArrayBuilder = class extends PgColumnBuilder {
      static {
        __name(this, "PgArrayBuilder");
      }
      static [entityKind] = "PgArrayBuilder";
      constructor(name, baseBuilder, size) {
        super(name, "array", "PgArray");
        this.config.baseBuilder = baseBuilder;
        this.config.size = size;
      }
      /** @internal */
      build(table) {
        const baseColumn = this.config.baseBuilder.build(table);
        return new PgArray(
          table,
          this.config,
          baseColumn
        );
      }
    };
    PgArray = class _PgArray extends PgColumn {
      static {
        __name(this, "PgArray");
      }
      constructor(table, config, baseColumn, range) {
        super(table, config);
        this.baseColumn = baseColumn;
        this.range = range;
        this.size = config.size;
      }
      size;
      static [entityKind] = "PgArray";
      getSQLType() {
        return `${this.baseColumn.getSQLType()}[${typeof this.size === "number" ? this.size : ""}]`;
      }
      mapFromDriverValue(value) {
        if (typeof value === "string") {
          value = parsePgArray(value);
        }
        return value.map((v) => this.baseColumn.mapFromDriverValue(v));
      }
      mapToDriverValue(value, isNestedArray = false) {
        const a = value.map(
          (v) => v === null ? null : is(this.baseColumn, _PgArray) ? this.baseColumn.mapToDriverValue(v, true) : this.baseColumn.mapToDriverValue(v)
        );
        if (isNestedArray)
          return a;
        return makePgArray(a);
      }
    };
  }
});

// worker/node_modules/drizzle-orm/pg-core/columns/enum.js
function isPgEnum(obj) {
  return !!obj && typeof obj === "function" && isPgEnumSym in obj && obj[isPgEnumSym] === true;
}
var isPgEnumSym, PgEnumColumnBuilder, PgEnumColumn;
var init_enum = __esm({
  "worker/node_modules/drizzle-orm/pg-core/columns/enum.js"() {
    init_entity();
    init_common();
    isPgEnumSym = Symbol.for("drizzle:isPgEnum");
    __name(isPgEnum, "isPgEnum");
    PgEnumColumnBuilder = class extends PgColumnBuilder {
      static {
        __name(this, "PgEnumColumnBuilder");
      }
      static [entityKind] = "PgEnumColumnBuilder";
      constructor(name, enumInstance) {
        super(name, "string", "PgEnumColumn");
        this.config.enum = enumInstance;
      }
      /** @internal */
      build(table) {
        return new PgEnumColumn(
          table,
          this.config
        );
      }
    };
    PgEnumColumn = class extends PgColumn {
      static {
        __name(this, "PgEnumColumn");
      }
      static [entityKind] = "PgEnumColumn";
      enum = this.config.enum;
      enumValues = this.config.enum.enumValues;
      constructor(table, config) {
        super(table, config);
        this.enum = config.enum;
      }
      getSQLType() {
        return this.enum.enumName;
      }
    };
  }
});

// worker/node_modules/drizzle-orm/subquery.js
var Subquery, WithSubquery;
var init_subquery = __esm({
  "worker/node_modules/drizzle-orm/subquery.js"() {
    init_entity();
    Subquery = class {
      static {
        __name(this, "Subquery");
      }
      static [entityKind] = "Subquery";
      constructor(sql3, selection, alias, isWith = false) {
        this._ = {
          brand: "Subquery",
          sql: sql3,
          selectedFields: selection,
          alias,
          isWith
        };
      }
      // getSQL(): SQL<unknown> {
      // 	return new SQL([this]);
      // }
    };
    WithSubquery = class extends Subquery {
      static {
        __name(this, "WithSubquery");
      }
      static [entityKind] = "WithSubquery";
    };
  }
});

// worker/node_modules/drizzle-orm/version.js
var version2;
var init_version = __esm({
  "worker/node_modules/drizzle-orm/version.js"() {
    version2 = "0.39.3";
  }
});

// worker/node_modules/drizzle-orm/tracing.js
var otel, rawTracer, tracer;
var init_tracing = __esm({
  "worker/node_modules/drizzle-orm/tracing.js"() {
    init_tracing_utils();
    init_version();
    tracer = {
      startActiveSpan(name, fn) {
        if (!otel) {
          return fn();
        }
        if (!rawTracer) {
          rawTracer = otel.trace.getTracer("drizzle-orm", version2);
        }
        return iife(
          (otel22, rawTracer22) => rawTracer22.startActiveSpan(
            name,
            (span) => {
              try {
                return fn(span);
              } catch (e) {
                span.setStatus({
                  code: otel22.SpanStatusCode.ERROR,
                  message: e instanceof Error ? e.message : "Unknown error"
                  // eslint-disable-line no-instanceof/no-instanceof
                });
                throw e;
              } finally {
                span.end();
              }
            }
          ),
          otel,
          rawTracer
        );
      }
    };
  }
});

// worker/node_modules/drizzle-orm/view-common.js
var ViewBaseConfig;
var init_view_common = __esm({
  "worker/node_modules/drizzle-orm/view-common.js"() {
    ViewBaseConfig = Symbol.for("drizzle:ViewBaseConfig");
  }
});

// worker/node_modules/drizzle-orm/table.js
function getTableName(table) {
  return table[TableName];
}
function getTableUniqueName(table) {
  return `${table[Schema] ?? "public"}.${table[TableName]}`;
}
var Schema, Columns, ExtraConfigColumns, OriginalName, BaseName, IsAlias, ExtraConfigBuilder, IsDrizzleTable, Table;
var init_table = __esm({
  "worker/node_modules/drizzle-orm/table.js"() {
    init_entity();
    init_table_utils();
    Schema = Symbol.for("drizzle:Schema");
    Columns = Symbol.for("drizzle:Columns");
    ExtraConfigColumns = Symbol.for("drizzle:ExtraConfigColumns");
    OriginalName = Symbol.for("drizzle:OriginalName");
    BaseName = Symbol.for("drizzle:BaseName");
    IsAlias = Symbol.for("drizzle:IsAlias");
    ExtraConfigBuilder = Symbol.for("drizzle:ExtraConfigBuilder");
    IsDrizzleTable = Symbol.for("drizzle:IsDrizzleTable");
    Table = class {
      static {
        __name(this, "Table");
      }
      static [entityKind] = "Table";
      /** @internal */
      static Symbol = {
        Name: TableName,
        Schema,
        OriginalName,
        Columns,
        ExtraConfigColumns,
        BaseName,
        IsAlias,
        ExtraConfigBuilder
      };
      /**
       * @internal
       * Can be changed if the table is aliased.
       */
      [TableName];
      /**
       * @internal
       * Used to store the original name of the table, before any aliasing.
       */
      [OriginalName];
      /** @internal */
      [Schema];
      /** @internal */
      [Columns];
      /** @internal */
      [ExtraConfigColumns];
      /**
       *  @internal
       * Used to store the table name before the transformation via the `tableCreator` functions.
       */
      [BaseName];
      /** @internal */
      [IsAlias] = false;
      /** @internal */
      [IsDrizzleTable] = true;
      /** @internal */
      [ExtraConfigBuilder] = void 0;
      constructor(name, schema, baseName) {
        this[TableName] = this[OriginalName] = name;
        this[Schema] = schema;
        this[BaseName] = baseName;
      }
    };
    __name(getTableName, "getTableName");
    __name(getTableUniqueName, "getTableUniqueName");
  }
});

// worker/node_modules/drizzle-orm/sql/sql.js
function isSQLWrapper(value) {
  return value !== null && value !== void 0 && typeof value.getSQL === "function";
}
function mergeQueries(queries) {
  const result = { sql: "", params: [] };
  for (const query of queries) {
    result.sql += query.sql;
    result.params.push(...query.params);
    if (query.typings?.length) {
      if (!result.typings) {
        result.typings = [];
      }
      result.typings.push(...query.typings);
    }
  }
  return result;
}
function isDriverValueEncoder(value) {
  return typeof value === "object" && value !== null && "mapToDriverValue" in value && typeof value.mapToDriverValue === "function";
}
function sql(strings, ...params) {
  const queryChunks = [];
  if (params.length > 0 || strings.length > 0 && strings[0] !== "") {
    queryChunks.push(new StringChunk(strings[0]));
  }
  for (const [paramIndex, param2] of params.entries()) {
    queryChunks.push(param2, new StringChunk(strings[paramIndex + 1]));
  }
  return new SQL(queryChunks);
}
function fillPlaceholders(params, values) {
  return params.map((p) => {
    if (is(p, Placeholder)) {
      if (!(p.name in values)) {
        throw new Error(`No value for placeholder "${p.name}" was provided`);
      }
      return values[p.name];
    }
    if (is(p, Param) && is(p.value, Placeholder)) {
      if (!(p.value.name in values)) {
        throw new Error(`No value for placeholder "${p.value.name}" was provided`);
      }
      return p.encoder.mapToDriverValue(values[p.value.name]);
    }
    return p;
  });
}
var FakePrimitiveParam, StringChunk, SQL, Name, noopDecoder, noopEncoder, noopMapper, Param, Placeholder, IsDrizzleView, View;
var init_sql2 = __esm({
  "worker/node_modules/drizzle-orm/sql/sql.js"() {
    init_entity();
    init_enum();
    init_subquery();
    init_tracing();
    init_view_common();
    init_column();
    init_table();
    FakePrimitiveParam = class {
      static {
        __name(this, "FakePrimitiveParam");
      }
      static [entityKind] = "FakePrimitiveParam";
    };
    __name(isSQLWrapper, "isSQLWrapper");
    __name(mergeQueries, "mergeQueries");
    StringChunk = class {
      static {
        __name(this, "StringChunk");
      }
      static [entityKind] = "StringChunk";
      value;
      constructor(value) {
        this.value = Array.isArray(value) ? value : [value];
      }
      getSQL() {
        return new SQL([this]);
      }
    };
    SQL = class _SQL {
      static {
        __name(this, "SQL");
      }
      constructor(queryChunks) {
        this.queryChunks = queryChunks;
      }
      static [entityKind] = "SQL";
      /** @internal */
      decoder = noopDecoder;
      shouldInlineParams = false;
      append(query) {
        this.queryChunks.push(...query.queryChunks);
        return this;
      }
      toQuery(config) {
        return tracer.startActiveSpan("drizzle.buildSQL", (span) => {
          const query = this.buildQueryFromSourceParams(this.queryChunks, config);
          span?.setAttributes({
            "drizzle.query.text": query.sql,
            "drizzle.query.params": JSON.stringify(query.params)
          });
          return query;
        });
      }
      buildQueryFromSourceParams(chunks, _config) {
        const config = Object.assign({}, _config, {
          inlineParams: _config.inlineParams || this.shouldInlineParams,
          paramStartIndex: _config.paramStartIndex || { value: 0 }
        });
        const {
          casing,
          escapeName,
          escapeParam,
          prepareTyping,
          inlineParams,
          paramStartIndex
        } = config;
        return mergeQueries(chunks.map((chunk) => {
          if (is(chunk, StringChunk)) {
            return { sql: chunk.value.join(""), params: [] };
          }
          if (is(chunk, Name)) {
            return { sql: escapeName(chunk.value), params: [] };
          }
          if (chunk === void 0) {
            return { sql: "", params: [] };
          }
          if (Array.isArray(chunk)) {
            const result = [new StringChunk("(")];
            for (const [i, p] of chunk.entries()) {
              result.push(p);
              if (i < chunk.length - 1) {
                result.push(new StringChunk(", "));
              }
            }
            result.push(new StringChunk(")"));
            return this.buildQueryFromSourceParams(result, config);
          }
          if (is(chunk, _SQL)) {
            return this.buildQueryFromSourceParams(chunk.queryChunks, {
              ...config,
              inlineParams: inlineParams || chunk.shouldInlineParams
            });
          }
          if (is(chunk, Table)) {
            const schemaName = chunk[Table.Symbol.Schema];
            const tableName = chunk[Table.Symbol.Name];
            return {
              sql: schemaName === void 0 || chunk[IsAlias] ? escapeName(tableName) : escapeName(schemaName) + "." + escapeName(tableName),
              params: []
            };
          }
          if (is(chunk, Column)) {
            const columnName = casing.getColumnCasing(chunk);
            if (_config.invokeSource === "indexes") {
              return { sql: escapeName(columnName), params: [] };
            }
            const schemaName = chunk.table[Table.Symbol.Schema];
            return {
              sql: chunk.table[IsAlias] || schemaName === void 0 ? escapeName(chunk.table[Table.Symbol.Name]) + "." + escapeName(columnName) : escapeName(schemaName) + "." + escapeName(chunk.table[Table.Symbol.Name]) + "." + escapeName(columnName),
              params: []
            };
          }
          if (is(chunk, View)) {
            const schemaName = chunk[ViewBaseConfig].schema;
            const viewName = chunk[ViewBaseConfig].name;
            return {
              sql: schemaName === void 0 || chunk[ViewBaseConfig].isAlias ? escapeName(viewName) : escapeName(schemaName) + "." + escapeName(viewName),
              params: []
            };
          }
          if (is(chunk, Param)) {
            if (is(chunk.value, Placeholder)) {
              return { sql: escapeParam(paramStartIndex.value++, chunk), params: [chunk], typings: ["none"] };
            }
            const mappedValue = chunk.value === null ? null : chunk.encoder.mapToDriverValue(chunk.value);
            if (is(mappedValue, _SQL)) {
              return this.buildQueryFromSourceParams([mappedValue], config);
            }
            if (inlineParams) {
              return { sql: this.mapInlineParam(mappedValue, config), params: [] };
            }
            let typings = ["none"];
            if (prepareTyping) {
              typings = [prepareTyping(chunk.encoder)];
            }
            return { sql: escapeParam(paramStartIndex.value++, mappedValue), params: [mappedValue], typings };
          }
          if (is(chunk, Placeholder)) {
            return { sql: escapeParam(paramStartIndex.value++, chunk), params: [chunk], typings: ["none"] };
          }
          if (is(chunk, _SQL.Aliased) && chunk.fieldAlias !== void 0) {
            return { sql: escapeName(chunk.fieldAlias), params: [] };
          }
          if (is(chunk, Subquery)) {
            if (chunk._.isWith) {
              return { sql: escapeName(chunk._.alias), params: [] };
            }
            return this.buildQueryFromSourceParams([
              new StringChunk("("),
              chunk._.sql,
              new StringChunk(") "),
              new Name(chunk._.alias)
            ], config);
          }
          if (isPgEnum(chunk)) {
            if (chunk.schema) {
              return { sql: escapeName(chunk.schema) + "." + escapeName(chunk.enumName), params: [] };
            }
            return { sql: escapeName(chunk.enumName), params: [] };
          }
          if (isSQLWrapper(chunk)) {
            if (chunk.shouldOmitSQLParens?.()) {
              return this.buildQueryFromSourceParams([chunk.getSQL()], config);
            }
            return this.buildQueryFromSourceParams([
              new StringChunk("("),
              chunk.getSQL(),
              new StringChunk(")")
            ], config);
          }
          if (inlineParams) {
            return { sql: this.mapInlineParam(chunk, config), params: [] };
          }
          return { sql: escapeParam(paramStartIndex.value++, chunk), params: [chunk], typings: ["none"] };
        }));
      }
      mapInlineParam(chunk, { escapeString }) {
        if (chunk === null) {
          return "null";
        }
        if (typeof chunk === "number" || typeof chunk === "boolean") {
          return chunk.toString();
        }
        if (typeof chunk === "string") {
          return escapeString(chunk);
        }
        if (typeof chunk === "object") {
          const mappedValueAsString = chunk.toString();
          if (mappedValueAsString === "[object Object]") {
            return escapeString(JSON.stringify(chunk));
          }
          return escapeString(mappedValueAsString);
        }
        throw new Error("Unexpected param value: " + chunk);
      }
      getSQL() {
        return this;
      }
      as(alias) {
        if (alias === void 0) {
          return this;
        }
        return new _SQL.Aliased(this, alias);
      }
      mapWith(decoder) {
        this.decoder = typeof decoder === "function" ? { mapFromDriverValue: decoder } : decoder;
        return this;
      }
      inlineParams() {
        this.shouldInlineParams = true;
        return this;
      }
      /**
       * This method is used to conditionally include a part of the query.
       *
       * @param condition - Condition to check
       * @returns itself if the condition is `true`, otherwise `undefined`
       */
      if(condition) {
        return condition ? this : void 0;
      }
    };
    Name = class {
      static {
        __name(this, "Name");
      }
      constructor(value) {
        this.value = value;
      }
      static [entityKind] = "Name";
      brand;
      getSQL() {
        return new SQL([this]);
      }
    };
    __name(isDriverValueEncoder, "isDriverValueEncoder");
    noopDecoder = {
      mapFromDriverValue: /* @__PURE__ */ __name((value) => value, "mapFromDriverValue")
    };
    noopEncoder = {
      mapToDriverValue: /* @__PURE__ */ __name((value) => value, "mapToDriverValue")
    };
    noopMapper = {
      ...noopDecoder,
      ...noopEncoder
    };
    Param = class {
      static {
        __name(this, "Param");
      }
      /**
       * @param value - Parameter value
       * @param encoder - Encoder to convert the value to a driver parameter
       */
      constructor(value, encoder = noopEncoder) {
        this.value = value;
        this.encoder = encoder;
      }
      static [entityKind] = "Param";
      brand;
      getSQL() {
        return new SQL([this]);
      }
    };
    __name(sql, "sql");
    ((sql22) => {
      function empty() {
        return new SQL([]);
      }
      __name(empty, "empty");
      sql22.empty = empty;
      function fromList(list) {
        return new SQL(list);
      }
      __name(fromList, "fromList");
      sql22.fromList = fromList;
      function raw2(str) {
        return new SQL([new StringChunk(str)]);
      }
      __name(raw2, "raw");
      sql22.raw = raw2;
      function join(chunks, separator) {
        const result = [];
        for (const [i, chunk] of chunks.entries()) {
          if (i > 0 && separator !== void 0) {
            result.push(separator);
          }
          result.push(chunk);
        }
        return new SQL(result);
      }
      __name(join, "join");
      sql22.join = join;
      function identifier(value) {
        return new Name(value);
      }
      __name(identifier, "identifier");
      sql22.identifier = identifier;
      function placeholder2(name2) {
        return new Placeholder(name2);
      }
      __name(placeholder2, "placeholder2");
      sql22.placeholder = placeholder2;
      function param2(value, encoder) {
        return new Param(value, encoder);
      }
      __name(param2, "param2");
      sql22.param = param2;
    })(sql || (sql = {}));
    ((SQL22) => {
      class Aliased {
        static {
          __name(this, "Aliased");
        }
        constructor(sql22, fieldAlias) {
          this.sql = sql22;
          this.fieldAlias = fieldAlias;
        }
        static [entityKind] = "SQL.Aliased";
        /** @internal */
        isSelectionField = false;
        getSQL() {
          return this.sql;
        }
        /** @internal */
        clone() {
          return new Aliased(this.sql, this.fieldAlias);
        }
      }
      SQL22.Aliased = Aliased;
    })(SQL || (SQL = {}));
    Placeholder = class {
      static {
        __name(this, "Placeholder");
      }
      constructor(name2) {
        this.name = name2;
      }
      static [entityKind] = "Placeholder";
      getSQL() {
        return new SQL([this]);
      }
    };
    __name(fillPlaceholders, "fillPlaceholders");
    IsDrizzleView = Symbol.for("drizzle:IsDrizzleView");
    View = class {
      static {
        __name(this, "View");
      }
      static [entityKind] = "View";
      /** @internal */
      [ViewBaseConfig];
      /** @internal */
      [IsDrizzleView] = true;
      constructor({ name: name2, schema, selectedFields, query }) {
        this[ViewBaseConfig] = {
          name: name2,
          originalName: name2,
          schema,
          selectedFields,
          query,
          isExisting: !query,
          isAlias: false
        };
      }
      getSQL() {
        return new SQL([this]);
      }
    };
    Column.prototype.getSQL = function() {
      return new SQL([this]);
    };
    Table.prototype.getSQL = function() {
      return new SQL([this]);
    };
    Subquery.prototype.getSQL = function() {
      return new SQL([this]);
    };
  }
});

// worker/node_modules/drizzle-orm/utils.js
function mapResultRow(columns, row, joinsNotNullableMap) {
  const nullifyMap = {};
  const result = columns.reduce(
    (result2, { path, field }, columnIndex) => {
      let decoder;
      if (is(field, Column)) {
        decoder = field;
      } else if (is(field, SQL)) {
        decoder = field.decoder;
      } else {
        decoder = field.sql.decoder;
      }
      let node = result2;
      for (const [pathChunkIndex, pathChunk] of path.entries()) {
        if (pathChunkIndex < path.length - 1) {
          if (!(pathChunk in node)) {
            node[pathChunk] = {};
          }
          node = node[pathChunk];
        } else {
          const rawValue = row[columnIndex];
          const value = node[pathChunk] = rawValue === null ? null : decoder.mapFromDriverValue(rawValue);
          if (joinsNotNullableMap && is(field, Column) && path.length === 2) {
            const objectName = path[0];
            if (!(objectName in nullifyMap)) {
              nullifyMap[objectName] = value === null ? getTableName(field.table) : false;
            } else if (typeof nullifyMap[objectName] === "string" && nullifyMap[objectName] !== getTableName(field.table)) {
              nullifyMap[objectName] = false;
            }
          }
        }
      }
      return result2;
    },
    {}
  );
  if (joinsNotNullableMap && Object.keys(nullifyMap).length > 0) {
    for (const [objectName, tableName] of Object.entries(nullifyMap)) {
      if (typeof tableName === "string" && !joinsNotNullableMap[tableName]) {
        result[objectName] = null;
      }
    }
  }
  return result;
}
function orderSelectedFields(fields, pathPrefix) {
  return Object.entries(fields).reduce((result, [name, field]) => {
    if (typeof name !== "string") {
      return result;
    }
    const newPath = pathPrefix ? [...pathPrefix, name] : [name];
    if (is(field, Column) || is(field, SQL) || is(field, SQL.Aliased)) {
      result.push({ path: newPath, field });
    } else if (is(field, Table)) {
      result.push(...orderSelectedFields(field[Table.Symbol.Columns], newPath));
    } else {
      result.push(...orderSelectedFields(field, newPath));
    }
    return result;
  }, []);
}
function haveSameKeys(left, right) {
  const leftKeys = Object.keys(left);
  const rightKeys = Object.keys(right);
  if (leftKeys.length !== rightKeys.length) {
    return false;
  }
  for (const [index, key] of leftKeys.entries()) {
    if (key !== rightKeys[index]) {
      return false;
    }
  }
  return true;
}
function mapUpdateSet(table, values) {
  const entries = Object.entries(values).filter(([, value]) => value !== void 0).map(([key, value]) => {
    if (is(value, SQL) || is(value, Column)) {
      return [key, value];
    } else {
      return [key, new Param(value, table[Table.Symbol.Columns][key])];
    }
  });
  if (entries.length === 0) {
    throw new Error("No values to set");
  }
  return Object.fromEntries(entries);
}
function applyMixins(baseClass, extendedClasses) {
  for (const extendedClass of extendedClasses) {
    for (const name of Object.getOwnPropertyNames(extendedClass.prototype)) {
      if (name === "constructor")
        continue;
      Object.defineProperty(
        baseClass.prototype,
        name,
        Object.getOwnPropertyDescriptor(extendedClass.prototype, name) || /* @__PURE__ */ Object.create(null)
      );
    }
  }
}
function getTableColumns(table) {
  return table[Table.Symbol.Columns];
}
function getTableLikeName(table) {
  return is(table, Subquery) ? table._.alias : is(table, View) ? table[ViewBaseConfig].name : is(table, SQL) ? void 0 : table[Table.Symbol.IsAlias] ? table[Table.Symbol.Name] : table[Table.Symbol.BaseName];
}
function getColumnNameAndConfig(a, b) {
  return {
    name: typeof a === "string" && a.length > 0 ? a : "",
    config: typeof a === "object" ? a : b
  };
}
function isConfig(data) {
  if (typeof data !== "object" || data === null)
    return false;
  if (data.constructor.name !== "Object")
    return false;
  if ("logger" in data) {
    const type = typeof data["logger"];
    if (type !== "boolean" && (type !== "object" || typeof data["logger"]["logQuery"] !== "function") && type !== "undefined")
      return false;
    return true;
  }
  if ("schema" in data) {
    const type = typeof data["logger"];
    if (type !== "object" && type !== "undefined")
      return false;
    return true;
  }
  if ("casing" in data) {
    const type = typeof data["logger"];
    if (type !== "string" && type !== "undefined")
      return false;
    return true;
  }
  if ("mode" in data) {
    if (data["mode"] !== "default" || data["mode"] !== "planetscale" || data["mode"] !== void 0)
      return false;
    return true;
  }
  if ("connection" in data) {
    const type = typeof data["connection"];
    if (type !== "string" && type !== "object" && type !== "undefined")
      return false;
    return true;
  }
  if ("client" in data) {
    const type = typeof data["client"];
    if (type !== "object" && type !== "function" && type !== "undefined")
      return false;
    return true;
  }
  if (Object.keys(data).length === 0)
    return true;
  return false;
}
var init_utils = __esm({
  "worker/node_modules/drizzle-orm/utils.js"() {
    init_column();
    init_entity();
    init_sql2();
    init_subquery();
    init_table();
    init_view_common();
    __name(mapResultRow, "mapResultRow");
    __name(orderSelectedFields, "orderSelectedFields");
    __name(haveSameKeys, "haveSameKeys");
    __name(mapUpdateSet, "mapUpdateSet");
    __name(applyMixins, "applyMixins");
    __name(getTableColumns, "getTableColumns");
    __name(getTableLikeName, "getTableLikeName");
    __name(getColumnNameAndConfig, "getColumnNameAndConfig");
    __name(isConfig, "isConfig");
  }
});

// worker/node_modules/drizzle-orm/logger.js
var ConsoleLogWriter, DefaultLogger, NoopLogger;
var init_logger = __esm({
  "worker/node_modules/drizzle-orm/logger.js"() {
    init_entity();
    ConsoleLogWriter = class {
      static {
        __name(this, "ConsoleLogWriter");
      }
      static [entityKind] = "ConsoleLogWriter";
      write(message) {
        console.log(message);
      }
    };
    DefaultLogger = class {
      static {
        __name(this, "DefaultLogger");
      }
      static [entityKind] = "DefaultLogger";
      writer;
      constructor(config) {
        this.writer = config?.writer ?? new ConsoleLogWriter();
      }
      logQuery(query, params) {
        const stringifiedParams = params.map((p) => {
          try {
            return JSON.stringify(p);
          } catch {
            return String(p);
          }
        });
        const paramsStr = stringifiedParams.length ? ` -- params: [${stringifiedParams.join(", ")}]` : "";
        this.writer.write(`Query: ${query}${paramsStr}`);
      }
    };
    NoopLogger = class {
      static {
        __name(this, "NoopLogger");
      }
      static [entityKind] = "NoopLogger";
      logQuery() {
      }
    };
  }
});

// worker/node_modules/drizzle-orm/pg-core/table.js
var InlineForeignKeys, EnableRLS, PgTable;
var init_table2 = __esm({
  "worker/node_modules/drizzle-orm/pg-core/table.js"() {
    init_entity();
    init_table();
    InlineForeignKeys = Symbol.for("drizzle:PgInlineForeignKeys");
    EnableRLS = Symbol.for("drizzle:EnableRLS");
    PgTable = class extends Table {
      static {
        __name(this, "PgTable");
      }
      static [entityKind] = "PgTable";
      /** @internal */
      static Symbol = Object.assign({}, Table.Symbol, {
        InlineForeignKeys,
        EnableRLS
      });
      /**@internal */
      [InlineForeignKeys] = [];
      /** @internal */
      [EnableRLS] = false;
      /** @internal */
      [Table.Symbol.ExtraConfigBuilder] = void 0;
    };
  }
});

// worker/node_modules/drizzle-orm/pg-core/primary-keys.js
var PrimaryKeyBuilder, PrimaryKey;
var init_primary_keys = __esm({
  "worker/node_modules/drizzle-orm/pg-core/primary-keys.js"() {
    init_entity();
    init_table2();
    PrimaryKeyBuilder = class {
      static {
        __name(this, "PrimaryKeyBuilder");
      }
      static [entityKind] = "PgPrimaryKeyBuilder";
      /** @internal */
      columns;
      /** @internal */
      name;
      constructor(columns, name) {
        this.columns = columns;
        this.name = name;
      }
      /** @internal */
      build(table) {
        return new PrimaryKey(table, this.columns, this.name);
      }
    };
    PrimaryKey = class {
      static {
        __name(this, "PrimaryKey");
      }
      constructor(table, columns, name) {
        this.table = table;
        this.columns = columns;
        this.name = name;
      }
      static [entityKind] = "PgPrimaryKey";
      columns;
      name;
      getName() {
        return this.name ?? `${this.table[PgTable.Symbol.Name]}_${this.columns.map((column) => column.name).join("_")}_pk`;
      }
    };
  }
});

// worker/node_modules/drizzle-orm/sql/expressions/conditions.js
function bindIfParam(value, column) {
  if (isDriverValueEncoder(column) && !isSQLWrapper(value) && !is(value, Param) && !is(value, Placeholder) && !is(value, Column) && !is(value, Table) && !is(value, View)) {
    return new Param(value, column);
  }
  return value;
}
function and(...unfilteredConditions) {
  const conditions = unfilteredConditions.filter(
    (c) => c !== void 0
  );
  if (conditions.length === 0) {
    return void 0;
  }
  if (conditions.length === 1) {
    return new SQL(conditions);
  }
  return new SQL([
    new StringChunk("("),
    sql.join(conditions, new StringChunk(" and ")),
    new StringChunk(")")
  ]);
}
function or(...unfilteredConditions) {
  const conditions = unfilteredConditions.filter(
    (c) => c !== void 0
  );
  if (conditions.length === 0) {
    return void 0;
  }
  if (conditions.length === 1) {
    return new SQL(conditions);
  }
  return new SQL([
    new StringChunk("("),
    sql.join(conditions, new StringChunk(" or ")),
    new StringChunk(")")
  ]);
}
function not(condition) {
  return sql`not ${condition}`;
}
function inArray(column, values) {
  if (Array.isArray(values)) {
    if (values.length === 0) {
      return sql`false`;
    }
    return sql`${column} in ${values.map((v) => bindIfParam(v, column))}`;
  }
  return sql`${column} in ${bindIfParam(values, column)}`;
}
function notInArray(column, values) {
  if (Array.isArray(values)) {
    if (values.length === 0) {
      return sql`true`;
    }
    return sql`${column} not in ${values.map((v) => bindIfParam(v, column))}`;
  }
  return sql`${column} not in ${bindIfParam(values, column)}`;
}
function isNull(value) {
  return sql`${value} is null`;
}
function isNotNull(value) {
  return sql`${value} is not null`;
}
function exists(subquery) {
  return sql`exists ${subquery}`;
}
function notExists(subquery) {
  return sql`not exists ${subquery}`;
}
function between(column, min, max) {
  return sql`${column} between ${bindIfParam(min, column)} and ${bindIfParam(
    max,
    column
  )}`;
}
function notBetween(column, min, max) {
  return sql`${column} not between ${bindIfParam(
    min,
    column
  )} and ${bindIfParam(max, column)}`;
}
function like(column, value) {
  return sql`${column} like ${value}`;
}
function notLike(column, value) {
  return sql`${column} not like ${value}`;
}
function ilike(column, value) {
  return sql`${column} ilike ${value}`;
}
function notIlike(column, value) {
  return sql`${column} not ilike ${value}`;
}
var eq, ne, gt, gte, lt, lte;
var init_conditions = __esm({
  "worker/node_modules/drizzle-orm/sql/expressions/conditions.js"() {
    init_column();
    init_entity();
    init_table();
    init_sql2();
    __name(bindIfParam, "bindIfParam");
    eq = /* @__PURE__ */ __name((left, right) => {
      return sql`${left} = ${bindIfParam(right, left)}`;
    }, "eq");
    ne = /* @__PURE__ */ __name((left, right) => {
      return sql`${left} <> ${bindIfParam(right, left)}`;
    }, "ne");
    __name(and, "and");
    __name(or, "or");
    __name(not, "not");
    gt = /* @__PURE__ */ __name((left, right) => {
      return sql`${left} > ${bindIfParam(right, left)}`;
    }, "gt");
    gte = /* @__PURE__ */ __name((left, right) => {
      return sql`${left} >= ${bindIfParam(right, left)}`;
    }, "gte");
    lt = /* @__PURE__ */ __name((left, right) => {
      return sql`${left} < ${bindIfParam(right, left)}`;
    }, "lt");
    lte = /* @__PURE__ */ __name((left, right) => {
      return sql`${left} <= ${bindIfParam(right, left)}`;
    }, "lte");
    __name(inArray, "inArray");
    __name(notInArray, "notInArray");
    __name(isNull, "isNull");
    __name(isNotNull, "isNotNull");
    __name(exists, "exists");
    __name(notExists, "notExists");
    __name(between, "between");
    __name(notBetween, "notBetween");
    __name(like, "like");
    __name(notLike, "notLike");
    __name(ilike, "ilike");
    __name(notIlike, "notIlike");
  }
});

// worker/node_modules/drizzle-orm/sql/expressions/select.js
function asc(column) {
  return sql`${column} asc`;
}
function desc(column) {
  return sql`${column} desc`;
}
var init_select = __esm({
  "worker/node_modules/drizzle-orm/sql/expressions/select.js"() {
    init_sql2();
    __name(asc, "asc");
    __name(desc, "desc");
  }
});

// worker/node_modules/drizzle-orm/sql/expressions/index.js
var init_expressions = __esm({
  "worker/node_modules/drizzle-orm/sql/expressions/index.js"() {
    init_conditions();
    init_select();
  }
});

// worker/node_modules/drizzle-orm/relations.js
function getOperators() {
  return {
    and,
    between,
    eq,
    exists,
    gt,
    gte,
    ilike,
    inArray,
    isNull,
    isNotNull,
    like,
    lt,
    lte,
    ne,
    not,
    notBetween,
    notExists,
    notLike,
    notIlike,
    notInArray,
    or,
    sql
  };
}
function getOrderByOperators() {
  return {
    sql,
    asc,
    desc
  };
}
function extractTablesRelationalConfig(schema, configHelpers) {
  if (Object.keys(schema).length === 1 && "default" in schema && !is(schema["default"], Table)) {
    schema = schema["default"];
  }
  const tableNamesMap = {};
  const relationsBuffer = {};
  const tablesConfig = {};
  for (const [key, value] of Object.entries(schema)) {
    if (is(value, Table)) {
      const dbName = getTableUniqueName(value);
      const bufferedRelations = relationsBuffer[dbName];
      tableNamesMap[dbName] = key;
      tablesConfig[key] = {
        tsName: key,
        dbName: value[Table.Symbol.Name],
        schema: value[Table.Symbol.Schema],
        columns: value[Table.Symbol.Columns],
        relations: bufferedRelations?.relations ?? {},
        primaryKey: bufferedRelations?.primaryKey ?? []
      };
      for (const column of Object.values(
        value[Table.Symbol.Columns]
      )) {
        if (column.primary) {
          tablesConfig[key].primaryKey.push(column);
        }
      }
      const extraConfig = value[Table.Symbol.ExtraConfigBuilder]?.(value[Table.Symbol.ExtraConfigColumns]);
      if (extraConfig) {
        for (const configEntry of Object.values(extraConfig)) {
          if (is(configEntry, PrimaryKeyBuilder)) {
            tablesConfig[key].primaryKey.push(...configEntry.columns);
          }
        }
      }
    } else if (is(value, Relations)) {
      const dbName = getTableUniqueName(value.table);
      const tableName = tableNamesMap[dbName];
      const relations2 = value.config(
        configHelpers(value.table)
      );
      let primaryKey2;
      for (const [relationName, relation] of Object.entries(relations2)) {
        if (tableName) {
          const tableConfig = tablesConfig[tableName];
          tableConfig.relations[relationName] = relation;
          if (primaryKey2) {
            tableConfig.primaryKey.push(...primaryKey2);
          }
        } else {
          if (!(dbName in relationsBuffer)) {
            relationsBuffer[dbName] = {
              relations: {},
              primaryKey: primaryKey2
            };
          }
          relationsBuffer[dbName].relations[relationName] = relation;
        }
      }
    }
  }
  return { tables: tablesConfig, tableNamesMap };
}
function createOne(sourceTable) {
  return /* @__PURE__ */ __name(function one(table, config) {
    return new One(
      sourceTable,
      table,
      config,
      config?.fields.reduce((res, f) => res && f.notNull, true) ?? false
    );
  }, "one");
}
function createMany(sourceTable) {
  return /* @__PURE__ */ __name(function many(referencedTable, config) {
    return new Many(sourceTable, referencedTable, config);
  }, "many");
}
function normalizeRelation(schema, tableNamesMap, relation) {
  if (is(relation, One) && relation.config) {
    return {
      fields: relation.config.fields,
      references: relation.config.references
    };
  }
  const referencedTableTsName = tableNamesMap[getTableUniqueName(relation.referencedTable)];
  if (!referencedTableTsName) {
    throw new Error(
      `Table "${relation.referencedTable[Table.Symbol.Name]}" not found in schema`
    );
  }
  const referencedTableConfig = schema[referencedTableTsName];
  if (!referencedTableConfig) {
    throw new Error(`Table "${referencedTableTsName}" not found in schema`);
  }
  const sourceTable = relation.sourceTable;
  const sourceTableTsName = tableNamesMap[getTableUniqueName(sourceTable)];
  if (!sourceTableTsName) {
    throw new Error(
      `Table "${sourceTable[Table.Symbol.Name]}" not found in schema`
    );
  }
  const reverseRelations = [];
  for (const referencedTableRelation of Object.values(
    referencedTableConfig.relations
  )) {
    if (relation.relationName && relation !== referencedTableRelation && referencedTableRelation.relationName === relation.relationName || !relation.relationName && referencedTableRelation.referencedTable === relation.sourceTable) {
      reverseRelations.push(referencedTableRelation);
    }
  }
  if (reverseRelations.length > 1) {
    throw relation.relationName ? new Error(
      `There are multiple relations with name "${relation.relationName}" in table "${referencedTableTsName}"`
    ) : new Error(
      `There are multiple relations between "${referencedTableTsName}" and "${relation.sourceTable[Table.Symbol.Name]}". Please specify relation name`
    );
  }
  if (reverseRelations[0] && is(reverseRelations[0], One) && reverseRelations[0].config) {
    return {
      fields: reverseRelations[0].config.references,
      references: reverseRelations[0].config.fields
    };
  }
  throw new Error(
    `There is not enough information to infer relation "${sourceTableTsName}.${relation.fieldName}"`
  );
}
function createTableRelationsHelpers(sourceTable) {
  return {
    one: createOne(sourceTable),
    many: createMany(sourceTable)
  };
}
function mapRelationalRow(tablesConfig, tableConfig, row, buildQueryResultSelection, mapColumnValue = (value) => value) {
  const result = {};
  for (const [
    selectionItemIndex,
    selectionItem
  ] of buildQueryResultSelection.entries()) {
    if (selectionItem.isJson) {
      const relation = tableConfig.relations[selectionItem.tsKey];
      const rawSubRows = row[selectionItemIndex];
      const subRows = typeof rawSubRows === "string" ? JSON.parse(rawSubRows) : rawSubRows;
      result[selectionItem.tsKey] = is(relation, One) ? subRows && mapRelationalRow(
        tablesConfig,
        tablesConfig[selectionItem.relationTableTsKey],
        subRows,
        selectionItem.selection,
        mapColumnValue
      ) : subRows.map(
        (subRow) => mapRelationalRow(
          tablesConfig,
          tablesConfig[selectionItem.relationTableTsKey],
          subRow,
          selectionItem.selection,
          mapColumnValue
        )
      );
    } else {
      const value = mapColumnValue(row[selectionItemIndex]);
      const field = selectionItem.field;
      let decoder;
      if (is(field, Column)) {
        decoder = field;
      } else if (is(field, SQL)) {
        decoder = field.decoder;
      } else {
        decoder = field.sql.decoder;
      }
      result[selectionItem.tsKey] = value === null ? null : decoder.mapFromDriverValue(value);
    }
  }
  return result;
}
var Relation, Relations, One, Many;
var init_relations = __esm({
  "worker/node_modules/drizzle-orm/relations.js"() {
    init_table();
    init_column();
    init_entity();
    init_primary_keys();
    init_expressions();
    init_sql2();
    Relation = class {
      static {
        __name(this, "Relation");
      }
      constructor(sourceTable, referencedTable, relationName) {
        this.sourceTable = sourceTable;
        this.referencedTable = referencedTable;
        this.relationName = relationName;
        this.referencedTableName = referencedTable[Table.Symbol.Name];
      }
      static [entityKind] = "Relation";
      referencedTableName;
      fieldName;
    };
    Relations = class {
      static {
        __name(this, "Relations");
      }
      constructor(table, config) {
        this.table = table;
        this.config = config;
      }
      static [entityKind] = "Relations";
    };
    One = class _One extends Relation {
      static {
        __name(this, "One");
      }
      constructor(sourceTable, referencedTable, config, isNullable) {
        super(sourceTable, referencedTable, config?.relationName);
        this.config = config;
        this.isNullable = isNullable;
      }
      static [entityKind] = "One";
      withFieldName(fieldName) {
        const relation = new _One(
          this.sourceTable,
          this.referencedTable,
          this.config,
          this.isNullable
        );
        relation.fieldName = fieldName;
        return relation;
      }
    };
    Many = class _Many extends Relation {
      static {
        __name(this, "Many");
      }
      constructor(sourceTable, referencedTable, config) {
        super(sourceTable, referencedTable, config?.relationName);
        this.config = config;
      }
      static [entityKind] = "Many";
      withFieldName(fieldName) {
        const relation = new _Many(
          this.sourceTable,
          this.referencedTable,
          this.config
        );
        relation.fieldName = fieldName;
        return relation;
      }
    };
    __name(getOperators, "getOperators");
    __name(getOrderByOperators, "getOrderByOperators");
    __name(extractTablesRelationalConfig, "extractTablesRelationalConfig");
    __name(createOne, "createOne");
    __name(createMany, "createMany");
    __name(normalizeRelation, "normalizeRelation");
    __name(createTableRelationsHelpers, "createTableRelationsHelpers");
    __name(mapRelationalRow, "mapRelationalRow");
  }
});

// worker/node_modules/drizzle-orm/alias.js
function aliasedTable(table, tableAlias) {
  return new Proxy(table, new TableAliasProxyHandler(tableAlias, false));
}
function aliasedTableColumn(column, tableAlias) {
  return new Proxy(
    column,
    new ColumnAliasProxyHandler(new Proxy(column.table, new TableAliasProxyHandler(tableAlias, false)))
  );
}
function mapColumnsInAliasedSQLToAlias(query, alias) {
  return new SQL.Aliased(mapColumnsInSQLToAlias(query.sql, alias), query.fieldAlias);
}
function mapColumnsInSQLToAlias(query, alias) {
  return sql.join(query.queryChunks.map((c) => {
    if (is(c, Column)) {
      return aliasedTableColumn(c, alias);
    }
    if (is(c, SQL)) {
      return mapColumnsInSQLToAlias(c, alias);
    }
    if (is(c, SQL.Aliased)) {
      return mapColumnsInAliasedSQLToAlias(c, alias);
    }
    return c;
  }));
}
var ColumnAliasProxyHandler, TableAliasProxyHandler, RelationTableAliasProxyHandler;
var init_alias = __esm({
  "worker/node_modules/drizzle-orm/alias.js"() {
    init_column();
    init_entity();
    init_sql2();
    init_table();
    init_view_common();
    ColumnAliasProxyHandler = class {
      static {
        __name(this, "ColumnAliasProxyHandler");
      }
      constructor(table) {
        this.table = table;
      }
      static [entityKind] = "ColumnAliasProxyHandler";
      get(columnObj, prop) {
        if (prop === "table") {
          return this.table;
        }
        return columnObj[prop];
      }
    };
    TableAliasProxyHandler = class {
      static {
        __name(this, "TableAliasProxyHandler");
      }
      constructor(alias, replaceOriginalName) {
        this.alias = alias;
        this.replaceOriginalName = replaceOriginalName;
      }
      static [entityKind] = "TableAliasProxyHandler";
      get(target, prop) {
        if (prop === Table.Symbol.IsAlias) {
          return true;
        }
        if (prop === Table.Symbol.Name) {
          return this.alias;
        }
        if (this.replaceOriginalName && prop === Table.Symbol.OriginalName) {
          return this.alias;
        }
        if (prop === ViewBaseConfig) {
          return {
            ...target[ViewBaseConfig],
            name: this.alias,
            isAlias: true
          };
        }
        if (prop === Table.Symbol.Columns) {
          const columns = target[Table.Symbol.Columns];
          if (!columns) {
            return columns;
          }
          const proxiedColumns = {};
          Object.keys(columns).map((key) => {
            proxiedColumns[key] = new Proxy(
              columns[key],
              new ColumnAliasProxyHandler(new Proxy(target, this))
            );
          });
          return proxiedColumns;
        }
        const value = target[prop];
        if (is(value, Column)) {
          return new Proxy(value, new ColumnAliasProxyHandler(new Proxy(target, this)));
        }
        return value;
      }
    };
    RelationTableAliasProxyHandler = class {
      static {
        __name(this, "RelationTableAliasProxyHandler");
      }
      constructor(alias) {
        this.alias = alias;
      }
      static [entityKind] = "RelationTableAliasProxyHandler";
      get(target, prop) {
        if (prop === "sourceTable") {
          return aliasedTable(target.sourceTable, this.alias);
        }
        return target[prop];
      }
    };
    __name(aliasedTable, "aliasedTable");
    __name(aliasedTableColumn, "aliasedTableColumn");
    __name(mapColumnsInAliasedSQLToAlias, "mapColumnsInAliasedSQLToAlias");
    __name(mapColumnsInSQLToAlias, "mapColumnsInSQLToAlias");
  }
});

// worker/node_modules/drizzle-orm/selection-proxy.js
var SelectionProxyHandler;
var init_selection_proxy = __esm({
  "worker/node_modules/drizzle-orm/selection-proxy.js"() {
    init_alias();
    init_column();
    init_entity();
    init_sql2();
    init_subquery();
    init_view_common();
    SelectionProxyHandler = class _SelectionProxyHandler {
      static {
        __name(this, "SelectionProxyHandler");
      }
      static [entityKind] = "SelectionProxyHandler";
      config;
      constructor(config) {
        this.config = { ...config };
      }
      get(subquery, prop) {
        if (prop === "_") {
          return {
            ...subquery["_"],
            selectedFields: new Proxy(
              subquery._.selectedFields,
              this
            )
          };
        }
        if (prop === ViewBaseConfig) {
          return {
            ...subquery[ViewBaseConfig],
            selectedFields: new Proxy(
              subquery[ViewBaseConfig].selectedFields,
              this
            )
          };
        }
        if (typeof prop === "symbol") {
          return subquery[prop];
        }
        const columns = is(subquery, Subquery) ? subquery._.selectedFields : is(subquery, View) ? subquery[ViewBaseConfig].selectedFields : subquery;
        const value = columns[prop];
        if (is(value, SQL.Aliased)) {
          if (this.config.sqlAliasedBehavior === "sql" && !value.isSelectionField) {
            return value.sql;
          }
          const newValue = value.clone();
          newValue.isSelectionField = true;
          return newValue;
        }
        if (is(value, SQL)) {
          if (this.config.sqlBehavior === "sql") {
            return value;
          }
          throw new Error(
            `You tried to reference "${prop}" field from a subquery, which is a raw SQL field, but it doesn't have an alias declared. Please add an alias to the field using ".as('alias')" method.`
          );
        }
        if (is(value, Column)) {
          if (this.config.alias) {
            return new Proxy(
              value,
              new ColumnAliasProxyHandler(
                new Proxy(
                  value.table,
                  new TableAliasProxyHandler(this.config.alias, this.config.replaceOriginalName ?? false)
                )
              )
            );
          }
          return value;
        }
        if (typeof value !== "object" || value === null) {
          return value;
        }
        return new Proxy(value, new _SelectionProxyHandler(this.config));
      }
    };
  }
});

// worker/node_modules/drizzle-orm/query-promise.js
var QueryPromise;
var init_query_promise = __esm({
  "worker/node_modules/drizzle-orm/query-promise.js"() {
    init_entity();
    QueryPromise = class {
      static {
        __name(this, "QueryPromise");
      }
      static [entityKind] = "QueryPromise";
      [Symbol.toStringTag] = "QueryPromise";
      catch(onRejected) {
        return this.then(void 0, onRejected);
      }
      finally(onFinally) {
        return this.then(
          (value) => {
            onFinally?.();
            return value;
          },
          (reason) => {
            onFinally?.();
            throw reason;
          }
        );
      }
      then(onFulfilled, onRejected) {
        return this.execute().then(onFulfilled, onRejected);
      }
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/foreign-keys.js
var ForeignKeyBuilder2, ForeignKey2;
var init_foreign_keys2 = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/foreign-keys.js"() {
    init_entity();
    init_table_utils();
    ForeignKeyBuilder2 = class {
      static {
        __name(this, "ForeignKeyBuilder");
      }
      static [entityKind] = "SQLiteForeignKeyBuilder";
      /** @internal */
      reference;
      /** @internal */
      _onUpdate;
      /** @internal */
      _onDelete;
      constructor(config, actions) {
        this.reference = () => {
          const { name, columns, foreignColumns } = config();
          return { name, columns, foreignTable: foreignColumns[0].table, foreignColumns };
        };
        if (actions) {
          this._onUpdate = actions.onUpdate;
          this._onDelete = actions.onDelete;
        }
      }
      onUpdate(action) {
        this._onUpdate = action;
        return this;
      }
      onDelete(action) {
        this._onDelete = action;
        return this;
      }
      /** @internal */
      build(table) {
        return new ForeignKey2(table, this);
      }
    };
    ForeignKey2 = class {
      static {
        __name(this, "ForeignKey");
      }
      constructor(table, builder) {
        this.table = table;
        this.reference = builder.reference;
        this.onUpdate = builder._onUpdate;
        this.onDelete = builder._onDelete;
      }
      static [entityKind] = "SQLiteForeignKey";
      reference;
      onUpdate;
      onDelete;
      getName() {
        const { name, columns, foreignColumns } = this.reference();
        const columnNames = columns.map((column) => column.name);
        const foreignColumnNames = foreignColumns.map((column) => column.name);
        const chunks = [
          this.table[TableName],
          ...columnNames,
          foreignColumns[0].table[TableName],
          ...foreignColumnNames
        ];
        return name ?? `${chunks.join("_")}_fk`;
      }
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/unique-constraint.js
function uniqueKeyName2(table, columns) {
  return `${table[TableName]}_${columns.join("_")}_unique`;
}
var UniqueConstraintBuilder2, UniqueOnConstraintBuilder2, UniqueConstraint2;
var init_unique_constraint2 = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/unique-constraint.js"() {
    init_entity();
    init_table_utils();
    __name(uniqueKeyName2, "uniqueKeyName");
    UniqueConstraintBuilder2 = class {
      static {
        __name(this, "UniqueConstraintBuilder");
      }
      constructor(columns, name) {
        this.name = name;
        this.columns = columns;
      }
      static [entityKind] = "SQLiteUniqueConstraintBuilder";
      /** @internal */
      columns;
      /** @internal */
      build(table) {
        return new UniqueConstraint2(table, this.columns, this.name);
      }
    };
    UniqueOnConstraintBuilder2 = class {
      static {
        __name(this, "UniqueOnConstraintBuilder");
      }
      static [entityKind] = "SQLiteUniqueOnConstraintBuilder";
      /** @internal */
      name;
      constructor(name) {
        this.name = name;
      }
      on(...columns) {
        return new UniqueConstraintBuilder2(columns, this.name);
      }
    };
    UniqueConstraint2 = class {
      static {
        __name(this, "UniqueConstraint");
      }
      constructor(table, columns, name) {
        this.table = table;
        this.columns = columns;
        this.name = name ?? uniqueKeyName2(this.table, this.columns.map((column) => column.name));
      }
      static [entityKind] = "SQLiteUniqueConstraint";
      columns;
      name;
      getName() {
        return this.name;
      }
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/columns/common.js
var SQLiteColumnBuilder, SQLiteColumn;
var init_common2 = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/columns/common.js"() {
    init_column_builder();
    init_column();
    init_entity();
    init_foreign_keys2();
    init_unique_constraint2();
    SQLiteColumnBuilder = class extends ColumnBuilder {
      static {
        __name(this, "SQLiteColumnBuilder");
      }
      static [entityKind] = "SQLiteColumnBuilder";
      foreignKeyConfigs = [];
      references(ref, actions = {}) {
        this.foreignKeyConfigs.push({ ref, actions });
        return this;
      }
      unique(name) {
        this.config.isUnique = true;
        this.config.uniqueName = name;
        return this;
      }
      generatedAlwaysAs(as, config) {
        this.config.generated = {
          as,
          type: "always",
          mode: config?.mode ?? "virtual"
        };
        return this;
      }
      /** @internal */
      buildForeignKeys(column, table) {
        return this.foreignKeyConfigs.map(({ ref, actions }) => {
          return ((ref2, actions2) => {
            const builder = new ForeignKeyBuilder2(() => {
              const foreignColumn = ref2();
              return { columns: [column], foreignColumns: [foreignColumn] };
            });
            if (actions2.onUpdate) {
              builder.onUpdate(actions2.onUpdate);
            }
            if (actions2.onDelete) {
              builder.onDelete(actions2.onDelete);
            }
            return builder.build(table);
          })(ref, actions);
        });
      }
    };
    SQLiteColumn = class extends Column {
      static {
        __name(this, "SQLiteColumn");
      }
      constructor(table, config) {
        if (!config.uniqueName) {
          config.uniqueName = uniqueKeyName2(table, [config.name]);
        }
        super(table, config);
        this.table = table;
      }
      static [entityKind] = "SQLiteColumn";
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/columns/blob.js
function blob(a, b) {
  const { name, config } = getColumnNameAndConfig(a, b);
  if (config?.mode === "json") {
    return new SQLiteBlobJsonBuilder(name);
  }
  if (config?.mode === "bigint") {
    return new SQLiteBigIntBuilder(name);
  }
  return new SQLiteBlobBufferBuilder(name);
}
var SQLiteBigIntBuilder, SQLiteBigInt, SQLiteBlobJsonBuilder, SQLiteBlobJson, SQLiteBlobBufferBuilder, SQLiteBlobBuffer;
var init_blob = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/columns/blob.js"() {
    init_entity();
    init_utils();
    init_common2();
    SQLiteBigIntBuilder = class extends SQLiteColumnBuilder {
      static {
        __name(this, "SQLiteBigIntBuilder");
      }
      static [entityKind] = "SQLiteBigIntBuilder";
      constructor(name) {
        super(name, "bigint", "SQLiteBigInt");
      }
      /** @internal */
      build(table) {
        return new SQLiteBigInt(table, this.config);
      }
    };
    SQLiteBigInt = class extends SQLiteColumn {
      static {
        __name(this, "SQLiteBigInt");
      }
      static [entityKind] = "SQLiteBigInt";
      getSQLType() {
        return "blob";
      }
      mapFromDriverValue(value) {
        if (Buffer.isBuffer(value)) {
          return BigInt(value.toString());
        }
        if (value instanceof ArrayBuffer) {
          const decoder = new TextDecoder();
          return BigInt(decoder.decode(value));
        }
        return BigInt(String.fromCodePoint(...value));
      }
      mapToDriverValue(value) {
        return Buffer.from(value.toString());
      }
    };
    SQLiteBlobJsonBuilder = class extends SQLiteColumnBuilder {
      static {
        __name(this, "SQLiteBlobJsonBuilder");
      }
      static [entityKind] = "SQLiteBlobJsonBuilder";
      constructor(name) {
        super(name, "json", "SQLiteBlobJson");
      }
      /** @internal */
      build(table) {
        return new SQLiteBlobJson(
          table,
          this.config
        );
      }
    };
    SQLiteBlobJson = class extends SQLiteColumn {
      static {
        __name(this, "SQLiteBlobJson");
      }
      static [entityKind] = "SQLiteBlobJson";
      getSQLType() {
        return "blob";
      }
      mapFromDriverValue(value) {
        if (Buffer.isBuffer(value)) {
          return JSON.parse(value.toString());
        }
        if (value instanceof ArrayBuffer) {
          const decoder = new TextDecoder();
          return JSON.parse(decoder.decode(value));
        }
        return JSON.parse(String.fromCodePoint(...value));
      }
      mapToDriverValue(value) {
        return Buffer.from(JSON.stringify(value));
      }
    };
    SQLiteBlobBufferBuilder = class extends SQLiteColumnBuilder {
      static {
        __name(this, "SQLiteBlobBufferBuilder");
      }
      static [entityKind] = "SQLiteBlobBufferBuilder";
      constructor(name) {
        super(name, "buffer", "SQLiteBlobBuffer");
      }
      /** @internal */
      build(table) {
        return new SQLiteBlobBuffer(table, this.config);
      }
    };
    SQLiteBlobBuffer = class extends SQLiteColumn {
      static {
        __name(this, "SQLiteBlobBuffer");
      }
      static [entityKind] = "SQLiteBlobBuffer";
      getSQLType() {
        return "blob";
      }
    };
    __name(blob, "blob");
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/columns/custom.js
function customType(customTypeParams) {
  return (a, b) => {
    const { name, config } = getColumnNameAndConfig(a, b);
    return new SQLiteCustomColumnBuilder(
      name,
      config,
      customTypeParams
    );
  };
}
var SQLiteCustomColumnBuilder, SQLiteCustomColumn;
var init_custom = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/columns/custom.js"() {
    init_entity();
    init_utils();
    init_common2();
    SQLiteCustomColumnBuilder = class extends SQLiteColumnBuilder {
      static {
        __name(this, "SQLiteCustomColumnBuilder");
      }
      static [entityKind] = "SQLiteCustomColumnBuilder";
      constructor(name, fieldConfig, customTypeParams) {
        super(name, "custom", "SQLiteCustomColumn");
        this.config.fieldConfig = fieldConfig;
        this.config.customTypeParams = customTypeParams;
      }
      /** @internal */
      build(table) {
        return new SQLiteCustomColumn(
          table,
          this.config
        );
      }
    };
    SQLiteCustomColumn = class extends SQLiteColumn {
      static {
        __name(this, "SQLiteCustomColumn");
      }
      static [entityKind] = "SQLiteCustomColumn";
      sqlName;
      mapTo;
      mapFrom;
      constructor(table, config) {
        super(table, config);
        this.sqlName = config.customTypeParams.dataType(config.fieldConfig);
        this.mapTo = config.customTypeParams.toDriver;
        this.mapFrom = config.customTypeParams.fromDriver;
      }
      getSQLType() {
        return this.sqlName;
      }
      mapFromDriverValue(value) {
        return typeof this.mapFrom === "function" ? this.mapFrom(value) : value;
      }
      mapToDriverValue(value) {
        return typeof this.mapTo === "function" ? this.mapTo(value) : value;
      }
    };
    __name(customType, "customType");
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/columns/integer.js
function integer(a, b) {
  const { name, config } = getColumnNameAndConfig(a, b);
  if (config?.mode === "timestamp" || config?.mode === "timestamp_ms") {
    return new SQLiteTimestampBuilder(name, config.mode);
  }
  if (config?.mode === "boolean") {
    return new SQLiteBooleanBuilder(name, config.mode);
  }
  return new SQLiteIntegerBuilder(name);
}
var SQLiteBaseIntegerBuilder, SQLiteBaseInteger, SQLiteIntegerBuilder, SQLiteInteger, SQLiteTimestampBuilder, SQLiteTimestamp, SQLiteBooleanBuilder, SQLiteBoolean;
var init_integer = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/columns/integer.js"() {
    init_entity();
    init_sql2();
    init_utils();
    init_common2();
    SQLiteBaseIntegerBuilder = class extends SQLiteColumnBuilder {
      static {
        __name(this, "SQLiteBaseIntegerBuilder");
      }
      static [entityKind] = "SQLiteBaseIntegerBuilder";
      constructor(name, dataType, columnType) {
        super(name, dataType, columnType);
        this.config.autoIncrement = false;
      }
      primaryKey(config) {
        if (config?.autoIncrement) {
          this.config.autoIncrement = true;
        }
        this.config.hasDefault = true;
        return super.primaryKey();
      }
    };
    SQLiteBaseInteger = class extends SQLiteColumn {
      static {
        __name(this, "SQLiteBaseInteger");
      }
      static [entityKind] = "SQLiteBaseInteger";
      autoIncrement = this.config.autoIncrement;
      getSQLType() {
        return "integer";
      }
    };
    SQLiteIntegerBuilder = class extends SQLiteBaseIntegerBuilder {
      static {
        __name(this, "SQLiteIntegerBuilder");
      }
      static [entityKind] = "SQLiteIntegerBuilder";
      constructor(name) {
        super(name, "number", "SQLiteInteger");
      }
      build(table) {
        return new SQLiteInteger(
          table,
          this.config
        );
      }
    };
    SQLiteInteger = class extends SQLiteBaseInteger {
      static {
        __name(this, "SQLiteInteger");
      }
      static [entityKind] = "SQLiteInteger";
    };
    SQLiteTimestampBuilder = class extends SQLiteBaseIntegerBuilder {
      static {
        __name(this, "SQLiteTimestampBuilder");
      }
      static [entityKind] = "SQLiteTimestampBuilder";
      constructor(name, mode) {
        super(name, "date", "SQLiteTimestamp");
        this.config.mode = mode;
      }
      /**
       * @deprecated Use `default()` with your own expression instead.
       *
       * Adds `DEFAULT (cast((julianday('now') - 2440587.5)*86400000 as integer))` to the column, which is the current epoch timestamp in milliseconds.
       */
      defaultNow() {
        return this.default(sql`(cast((julianday('now') - 2440587.5)*86400000 as integer))`);
      }
      build(table) {
        return new SQLiteTimestamp(
          table,
          this.config
        );
      }
    };
    SQLiteTimestamp = class extends SQLiteBaseInteger {
      static {
        __name(this, "SQLiteTimestamp");
      }
      static [entityKind] = "SQLiteTimestamp";
      mode = this.config.mode;
      mapFromDriverValue(value) {
        if (this.config.mode === "timestamp") {
          return new Date(value * 1e3);
        }
        return new Date(value);
      }
      mapToDriverValue(value) {
        const unix = value.getTime();
        if (this.config.mode === "timestamp") {
          return Math.floor(unix / 1e3);
        }
        return unix;
      }
    };
    SQLiteBooleanBuilder = class extends SQLiteBaseIntegerBuilder {
      static {
        __name(this, "SQLiteBooleanBuilder");
      }
      static [entityKind] = "SQLiteBooleanBuilder";
      constructor(name, mode) {
        super(name, "boolean", "SQLiteBoolean");
        this.config.mode = mode;
      }
      build(table) {
        return new SQLiteBoolean(
          table,
          this.config
        );
      }
    };
    SQLiteBoolean = class extends SQLiteBaseInteger {
      static {
        __name(this, "SQLiteBoolean");
      }
      static [entityKind] = "SQLiteBoolean";
      mode = this.config.mode;
      mapFromDriverValue(value) {
        return Number(value) === 1;
      }
      mapToDriverValue(value) {
        return value ? 1 : 0;
      }
    };
    __name(integer, "integer");
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/columns/numeric.js
function numeric(name) {
  return new SQLiteNumericBuilder(name ?? "");
}
var SQLiteNumericBuilder, SQLiteNumeric;
var init_numeric = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/columns/numeric.js"() {
    init_entity();
    init_common2();
    SQLiteNumericBuilder = class extends SQLiteColumnBuilder {
      static {
        __name(this, "SQLiteNumericBuilder");
      }
      static [entityKind] = "SQLiteNumericBuilder";
      constructor(name) {
        super(name, "string", "SQLiteNumeric");
      }
      /** @internal */
      build(table) {
        return new SQLiteNumeric(
          table,
          this.config
        );
      }
    };
    SQLiteNumeric = class extends SQLiteColumn {
      static {
        __name(this, "SQLiteNumeric");
      }
      static [entityKind] = "SQLiteNumeric";
      getSQLType() {
        return "numeric";
      }
    };
    __name(numeric, "numeric");
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/columns/real.js
function real(name) {
  return new SQLiteRealBuilder(name ?? "");
}
var SQLiteRealBuilder, SQLiteReal;
var init_real = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/columns/real.js"() {
    init_entity();
    init_common2();
    SQLiteRealBuilder = class extends SQLiteColumnBuilder {
      static {
        __name(this, "SQLiteRealBuilder");
      }
      static [entityKind] = "SQLiteRealBuilder";
      constructor(name) {
        super(name, "number", "SQLiteReal");
      }
      /** @internal */
      build(table) {
        return new SQLiteReal(table, this.config);
      }
    };
    SQLiteReal = class extends SQLiteColumn {
      static {
        __name(this, "SQLiteReal");
      }
      static [entityKind] = "SQLiteReal";
      getSQLType() {
        return "real";
      }
    };
    __name(real, "real");
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/columns/text.js
function text(a, b = {}) {
  const { name, config } = getColumnNameAndConfig(a, b);
  if (config.mode === "json") {
    return new SQLiteTextJsonBuilder(name);
  }
  return new SQLiteTextBuilder(name, config);
}
var SQLiteTextBuilder, SQLiteText, SQLiteTextJsonBuilder, SQLiteTextJson;
var init_text = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/columns/text.js"() {
    init_entity();
    init_utils();
    init_common2();
    SQLiteTextBuilder = class extends SQLiteColumnBuilder {
      static {
        __name(this, "SQLiteTextBuilder");
      }
      static [entityKind] = "SQLiteTextBuilder";
      constructor(name, config) {
        super(name, "string", "SQLiteText");
        this.config.enumValues = config.enum;
        this.config.length = config.length;
      }
      /** @internal */
      build(table) {
        return new SQLiteText(
          table,
          this.config
        );
      }
    };
    SQLiteText = class extends SQLiteColumn {
      static {
        __name(this, "SQLiteText");
      }
      static [entityKind] = "SQLiteText";
      enumValues = this.config.enumValues;
      length = this.config.length;
      constructor(table, config) {
        super(table, config);
      }
      getSQLType() {
        return `text${this.config.length ? `(${this.config.length})` : ""}`;
      }
    };
    SQLiteTextJsonBuilder = class extends SQLiteColumnBuilder {
      static {
        __name(this, "SQLiteTextJsonBuilder");
      }
      static [entityKind] = "SQLiteTextJsonBuilder";
      constructor(name) {
        super(name, "json", "SQLiteTextJson");
      }
      /** @internal */
      build(table) {
        return new SQLiteTextJson(
          table,
          this.config
        );
      }
    };
    SQLiteTextJson = class extends SQLiteColumn {
      static {
        __name(this, "SQLiteTextJson");
      }
      static [entityKind] = "SQLiteTextJson";
      getSQLType() {
        return "text";
      }
      mapFromDriverValue(value) {
        return JSON.parse(value);
      }
      mapToDriverValue(value) {
        return JSON.stringify(value);
      }
    };
    __name(text, "text");
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/columns/all.js
function getSQLiteColumnBuilders() {
  return {
    blob,
    customType,
    integer,
    numeric,
    real,
    text
  };
}
var init_all = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/columns/all.js"() {
    init_blob();
    init_custom();
    init_integer();
    init_numeric();
    init_real();
    init_text();
    __name(getSQLiteColumnBuilders, "getSQLiteColumnBuilders");
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/table.js
function sqliteTableBase(name, columns, extraConfig, schema, baseName = name) {
  const rawTable = new SQLiteTable(name, schema, baseName);
  const parsedColumns = typeof columns === "function" ? columns(getSQLiteColumnBuilders()) : columns;
  const builtColumns = Object.fromEntries(
    Object.entries(parsedColumns).map(([name2, colBuilderBase]) => {
      const colBuilder = colBuilderBase;
      colBuilder.setName(name2);
      const column = colBuilder.build(rawTable);
      rawTable[InlineForeignKeys2].push(...colBuilder.buildForeignKeys(column, rawTable));
      return [name2, column];
    })
  );
  const table = Object.assign(rawTable, builtColumns);
  table[Table.Symbol.Columns] = builtColumns;
  table[Table.Symbol.ExtraConfigColumns] = builtColumns;
  if (extraConfig) {
    table[SQLiteTable.Symbol.ExtraConfigBuilder] = extraConfig;
  }
  return table;
}
var InlineForeignKeys2, SQLiteTable, sqliteTable;
var init_table3 = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/table.js"() {
    init_entity();
    init_table();
    init_all();
    InlineForeignKeys2 = Symbol.for("drizzle:SQLiteInlineForeignKeys");
    SQLiteTable = class extends Table {
      static {
        __name(this, "SQLiteTable");
      }
      static [entityKind] = "SQLiteTable";
      /** @internal */
      static Symbol = Object.assign({}, Table.Symbol, {
        InlineForeignKeys: InlineForeignKeys2
      });
      /** @internal */
      [Table.Symbol.Columns];
      /** @internal */
      [InlineForeignKeys2] = [];
      /** @internal */
      [Table.Symbol.ExtraConfigBuilder] = void 0;
    };
    __name(sqliteTableBase, "sqliteTableBase");
    sqliteTable = /* @__PURE__ */ __name((name, columns, extraConfig) => {
      return sqliteTableBase(name, columns, extraConfig);
    }, "sqliteTable");
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/query-builders/delete.js
var SQLiteDeleteBase;
var init_delete = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/query-builders/delete.js"() {
    init_entity();
    init_query_promise();
    init_selection_proxy();
    init_table3();
    init_table();
    init_utils();
    SQLiteDeleteBase = class extends QueryPromise {
      static {
        __name(this, "SQLiteDeleteBase");
      }
      constructor(table, session, dialect, withList) {
        super();
        this.table = table;
        this.session = session;
        this.dialect = dialect;
        this.config = { table, withList };
      }
      static [entityKind] = "SQLiteDelete";
      /** @internal */
      config;
      /**
       * Adds a `where` clause to the query.
       *
       * Calling this method will delete only those rows that fulfill a specified condition.
       *
       * See docs: {@link https://orm.drizzle.team/docs/delete}
       *
       * @param where the `where` clause.
       *
       * @example
       * You can use conditional operators and `sql function` to filter the rows to be deleted.
       *
       * ```ts
       * // Delete all cars with green color
       * db.delete(cars).where(eq(cars.color, 'green'));
       * // or
       * db.delete(cars).where(sql`${cars.color} = 'green'`)
       * ```
       *
       * You can logically combine conditional operators with `and()` and `or()` operators:
       *
       * ```ts
       * // Delete all BMW cars with a green color
       * db.delete(cars).where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
       *
       * // Delete all cars with the green or blue color
       * db.delete(cars).where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
       * ```
       */
      where(where) {
        this.config.where = where;
        return this;
      }
      orderBy(...columns) {
        if (typeof columns[0] === "function") {
          const orderBy = columns[0](
            new Proxy(
              this.config.table[Table.Symbol.Columns],
              new SelectionProxyHandler({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })
            )
          );
          const orderByArray = Array.isArray(orderBy) ? orderBy : [orderBy];
          this.config.orderBy = orderByArray;
        } else {
          const orderByArray = columns;
          this.config.orderBy = orderByArray;
        }
        return this;
      }
      limit(limit) {
        this.config.limit = limit;
        return this;
      }
      returning(fields = this.table[SQLiteTable.Symbol.Columns]) {
        this.config.returning = orderSelectedFields(fields);
        return this;
      }
      /** @internal */
      getSQL() {
        return this.dialect.buildDeleteQuery(this.config);
      }
      toSQL() {
        const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
        return rest;
      }
      /** @internal */
      _prepare(isOneTimeQuery = true) {
        return this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](
          this.dialect.sqlToQuery(this.getSQL()),
          this.config.returning,
          this.config.returning ? "all" : "run",
          true
        );
      }
      prepare() {
        return this._prepare(false);
      }
      run = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().run(placeholderValues);
      }, "run");
      all = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().all(placeholderValues);
      }, "all");
      get = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().get(placeholderValues);
      }, "get");
      values = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().values(placeholderValues);
      }, "values");
      async execute(placeholderValues) {
        return this._prepare().execute(placeholderValues);
      }
      $dynamic() {
        return this;
      }
    };
  }
});

// worker/node_modules/drizzle-orm/casing.js
function toSnakeCase(input) {
  const words = input.replace(/['\u2019]/g, "").match(/[\da-z]+|[A-Z]+(?![a-z])|[A-Z][\da-z]+/g) ?? [];
  return words.map((word) => word.toLowerCase()).join("_");
}
function toCamelCase(input) {
  const words = input.replace(/['\u2019]/g, "").match(/[\da-z]+|[A-Z]+(?![a-z])|[A-Z][\da-z]+/g) ?? [];
  return words.reduce((acc, word, i) => {
    const formattedWord = i === 0 ? word.toLowerCase() : `${word[0].toUpperCase()}${word.slice(1)}`;
    return acc + formattedWord;
  }, "");
}
function noopCase(input) {
  return input;
}
var CasingCache;
var init_casing = __esm({
  "worker/node_modules/drizzle-orm/casing.js"() {
    init_entity();
    init_table();
    __name(toSnakeCase, "toSnakeCase");
    __name(toCamelCase, "toCamelCase");
    __name(noopCase, "noopCase");
    CasingCache = class {
      static {
        __name(this, "CasingCache");
      }
      static [entityKind] = "CasingCache";
      /** @internal */
      cache = {};
      cachedTables = {};
      convert;
      constructor(casing) {
        this.convert = casing === "snake_case" ? toSnakeCase : casing === "camelCase" ? toCamelCase : noopCase;
      }
      getColumnCasing(column) {
        if (!column.keyAsName)
          return column.name;
        const schema = column.table[Table.Symbol.Schema] ?? "public";
        const tableName = column.table[Table.Symbol.OriginalName];
        const key = `${schema}.${tableName}.${column.name}`;
        if (!this.cache[key]) {
          this.cacheTable(column.table);
        }
        return this.cache[key];
      }
      cacheTable(table) {
        const schema = table[Table.Symbol.Schema] ?? "public";
        const tableName = table[Table.Symbol.OriginalName];
        const tableKey = `${schema}.${tableName}`;
        if (!this.cachedTables[tableKey]) {
          for (const column of Object.values(table[Table.Symbol.Columns])) {
            const columnKey = `${tableKey}.${column.name}`;
            this.cache[columnKey] = this.convert(column.name);
          }
          this.cachedTables[tableKey] = true;
        }
      }
      clearCache() {
        this.cache = {};
        this.cachedTables = {};
      }
    };
  }
});

// worker/node_modules/drizzle-orm/errors.js
var DrizzleError, TransactionRollbackError;
var init_errors2 = __esm({
  "worker/node_modules/drizzle-orm/errors.js"() {
    init_entity();
    DrizzleError = class extends Error {
      static {
        __name(this, "DrizzleError");
      }
      static [entityKind] = "DrizzleError";
      constructor({ message, cause }) {
        super(message);
        this.name = "DrizzleError";
        this.cause = cause;
      }
    };
    TransactionRollbackError = class extends DrizzleError {
      static {
        __name(this, "TransactionRollbackError");
      }
      static [entityKind] = "TransactionRollbackError";
      constructor() {
        super({ message: "Rollback" });
      }
    };
  }
});

// worker/node_modules/drizzle-orm/sql/functions/aggregate.js
var init_aggregate = __esm({
  "worker/node_modules/drizzle-orm/sql/functions/aggregate.js"() {
  }
});

// worker/node_modules/drizzle-orm/sql/functions/vector.js
var init_vector = __esm({
  "worker/node_modules/drizzle-orm/sql/functions/vector.js"() {
  }
});

// worker/node_modules/drizzle-orm/sql/functions/index.js
var init_functions = __esm({
  "worker/node_modules/drizzle-orm/sql/functions/index.js"() {
    init_aggregate();
    init_vector();
  }
});

// worker/node_modules/drizzle-orm/sql/index.js
var init_sql3 = __esm({
  "worker/node_modules/drizzle-orm/sql/index.js"() {
    init_expressions();
    init_functions();
    init_sql2();
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/columns/index.js
var init_columns = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/columns/index.js"() {
    init_blob();
    init_common2();
    init_custom();
    init_integer();
    init_numeric();
    init_real();
    init_text();
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/view-base.js
var SQLiteViewBase;
var init_view_base = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/view-base.js"() {
    init_entity();
    init_sql2();
    SQLiteViewBase = class extends View {
      static {
        __name(this, "SQLiteViewBase");
      }
      static [entityKind] = "SQLiteViewBase";
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/dialect.js
var SQLiteDialect, SQLiteSyncDialect, SQLiteAsyncDialect;
var init_dialect = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/dialect.js"() {
    init_alias();
    init_casing();
    init_column();
    init_entity();
    init_errors2();
    init_relations();
    init_sql3();
    init_sql2();
    init_columns();
    init_table3();
    init_subquery();
    init_table();
    init_utils();
    init_view_common();
    init_view_base();
    SQLiteDialect = class {
      static {
        __name(this, "SQLiteDialect");
      }
      static [entityKind] = "SQLiteDialect";
      /** @internal */
      casing;
      constructor(config) {
        this.casing = new CasingCache(config?.casing);
      }
      escapeName(name) {
        return `"${name}"`;
      }
      escapeParam(_num) {
        return "?";
      }
      escapeString(str) {
        return `'${str.replace(/'/g, "''")}'`;
      }
      buildWithCTE(queries) {
        if (!queries?.length)
          return void 0;
        const withSqlChunks = [sql`with `];
        for (const [i, w] of queries.entries()) {
          withSqlChunks.push(sql`${sql.identifier(w._.alias)} as (${w._.sql})`);
          if (i < queries.length - 1) {
            withSqlChunks.push(sql`, `);
          }
        }
        withSqlChunks.push(sql` `);
        return sql.join(withSqlChunks);
      }
      buildDeleteQuery({ table, where, returning, withList, limit, orderBy }) {
        const withSql = this.buildWithCTE(withList);
        const returningSql = returning ? sql` returning ${this.buildSelection(returning, { isSingleTable: true })}` : void 0;
        const whereSql = where ? sql` where ${where}` : void 0;
        const orderBySql = this.buildOrderBy(orderBy);
        const limitSql = this.buildLimit(limit);
        return sql`${withSql}delete from ${table}${whereSql}${returningSql}${orderBySql}${limitSql}`;
      }
      buildUpdateSet(table, set2) {
        const tableColumns = table[Table.Symbol.Columns];
        const columnNames = Object.keys(tableColumns).filter(
          (colName) => set2[colName] !== void 0 || tableColumns[colName]?.onUpdateFn !== void 0
        );
        const setSize = columnNames.length;
        return sql.join(columnNames.flatMap((colName, i) => {
          const col = tableColumns[colName];
          const value = set2[colName] ?? sql.param(col.onUpdateFn(), col);
          const res = sql`${sql.identifier(this.casing.getColumnCasing(col))} = ${value}`;
          if (i < setSize - 1) {
            return [res, sql.raw(", ")];
          }
          return [res];
        }));
      }
      buildUpdateQuery({ table, set: set2, where, returning, withList, joins, from, limit, orderBy }) {
        const withSql = this.buildWithCTE(withList);
        const setSql = this.buildUpdateSet(table, set2);
        const fromSql = from && sql.join([sql.raw(" from "), this.buildFromTable(from)]);
        const joinsSql = this.buildJoins(joins);
        const returningSql = returning ? sql` returning ${this.buildSelection(returning, { isSingleTable: true })}` : void 0;
        const whereSql = where ? sql` where ${where}` : void 0;
        const orderBySql = this.buildOrderBy(orderBy);
        const limitSql = this.buildLimit(limit);
        return sql`${withSql}update ${table} set ${setSql}${fromSql}${joinsSql}${whereSql}${returningSql}${orderBySql}${limitSql}`;
      }
      /**
       * Builds selection SQL with provided fields/expressions
       *
       * Examples:
       *
       * `select <selection> from`
       *
       * `insert ... returning <selection>`
       *
       * If `isSingleTable` is true, then columns won't be prefixed with table name
       */
      buildSelection(fields, { isSingleTable = false } = {}) {
        const columnsLen = fields.length;
        const chunks = fields.flatMap(({ field }, i) => {
          const chunk = [];
          if (is(field, SQL.Aliased) && field.isSelectionField) {
            chunk.push(sql.identifier(field.fieldAlias));
          } else if (is(field, SQL.Aliased) || is(field, SQL)) {
            const query = is(field, SQL.Aliased) ? field.sql : field;
            if (isSingleTable) {
              chunk.push(
                new SQL(
                  query.queryChunks.map((c) => {
                    if (is(c, Column)) {
                      return sql.identifier(this.casing.getColumnCasing(c));
                    }
                    return c;
                  })
                )
              );
            } else {
              chunk.push(query);
            }
            if (is(field, SQL.Aliased)) {
              chunk.push(sql` as ${sql.identifier(field.fieldAlias)}`);
            }
          } else if (is(field, Column)) {
            const tableName = field.table[Table.Symbol.Name];
            if (isSingleTable) {
              chunk.push(sql.identifier(this.casing.getColumnCasing(field)));
            } else {
              chunk.push(sql`${sql.identifier(tableName)}.${sql.identifier(this.casing.getColumnCasing(field))}`);
            }
          }
          if (i < columnsLen - 1) {
            chunk.push(sql`, `);
          }
          return chunk;
        });
        return sql.join(chunks);
      }
      buildJoins(joins) {
        if (!joins || joins.length === 0) {
          return void 0;
        }
        const joinsArray = [];
        if (joins) {
          for (const [index, joinMeta] of joins.entries()) {
            if (index === 0) {
              joinsArray.push(sql` `);
            }
            const table = joinMeta.table;
            if (is(table, SQLiteTable)) {
              const tableName = table[SQLiteTable.Symbol.Name];
              const tableSchema = table[SQLiteTable.Symbol.Schema];
              const origTableName = table[SQLiteTable.Symbol.OriginalName];
              const alias = tableName === origTableName ? void 0 : joinMeta.alias;
              joinsArray.push(
                sql`${sql.raw(joinMeta.joinType)} join ${tableSchema ? sql`${sql.identifier(tableSchema)}.` : void 0}${sql.identifier(origTableName)}${alias && sql` ${sql.identifier(alias)}`} on ${joinMeta.on}`
              );
            } else {
              joinsArray.push(
                sql`${sql.raw(joinMeta.joinType)} join ${table} on ${joinMeta.on}`
              );
            }
            if (index < joins.length - 1) {
              joinsArray.push(sql` `);
            }
          }
        }
        return sql.join(joinsArray);
      }
      buildLimit(limit) {
        return typeof limit === "object" || typeof limit === "number" && limit >= 0 ? sql` limit ${limit}` : void 0;
      }
      buildOrderBy(orderBy) {
        const orderByList = [];
        if (orderBy) {
          for (const [index, orderByValue] of orderBy.entries()) {
            orderByList.push(orderByValue);
            if (index < orderBy.length - 1) {
              orderByList.push(sql`, `);
            }
          }
        }
        return orderByList.length > 0 ? sql` order by ${sql.join(orderByList)}` : void 0;
      }
      buildFromTable(table) {
        if (is(table, Table) && table[Table.Symbol.OriginalName] !== table[Table.Symbol.Name]) {
          return sql`${sql.identifier(table[Table.Symbol.OriginalName])} ${sql.identifier(table[Table.Symbol.Name])}`;
        }
        return table;
      }
      buildSelectQuery({
        withList,
        fields,
        fieldsFlat,
        where,
        having,
        table,
        joins,
        orderBy,
        groupBy,
        limit,
        offset,
        distinct,
        setOperators
      }) {
        const fieldsList = fieldsFlat ?? orderSelectedFields(fields);
        for (const f of fieldsList) {
          if (is(f.field, Column) && getTableName(f.field.table) !== (is(table, Subquery) ? table._.alias : is(table, SQLiteViewBase) ? table[ViewBaseConfig].name : is(table, SQL) ? void 0 : getTableName(table)) && !((table2) => joins?.some(
            ({ alias }) => alias === (table2[Table.Symbol.IsAlias] ? getTableName(table2) : table2[Table.Symbol.BaseName])
          ))(f.field.table)) {
            const tableName = getTableName(f.field.table);
            throw new Error(
              `Your "${f.path.join("->")}" field references a column "${tableName}"."${f.field.name}", but the table "${tableName}" is not part of the query! Did you forget to join it?`
            );
          }
        }
        const isSingleTable = !joins || joins.length === 0;
        const withSql = this.buildWithCTE(withList);
        const distinctSql = distinct ? sql` distinct` : void 0;
        const selection = this.buildSelection(fieldsList, { isSingleTable });
        const tableSql = this.buildFromTable(table);
        const joinsSql = this.buildJoins(joins);
        const whereSql = where ? sql` where ${where}` : void 0;
        const havingSql = having ? sql` having ${having}` : void 0;
        const groupByList = [];
        if (groupBy) {
          for (const [index, groupByValue] of groupBy.entries()) {
            groupByList.push(groupByValue);
            if (index < groupBy.length - 1) {
              groupByList.push(sql`, `);
            }
          }
        }
        const groupBySql = groupByList.length > 0 ? sql` group by ${sql.join(groupByList)}` : void 0;
        const orderBySql = this.buildOrderBy(orderBy);
        const limitSql = this.buildLimit(limit);
        const offsetSql = offset ? sql` offset ${offset}` : void 0;
        const finalQuery = sql`${withSql}select${distinctSql} ${selection} from ${tableSql}${joinsSql}${whereSql}${groupBySql}${havingSql}${orderBySql}${limitSql}${offsetSql}`;
        if (setOperators.length > 0) {
          return this.buildSetOperations(finalQuery, setOperators);
        }
        return finalQuery;
      }
      buildSetOperations(leftSelect, setOperators) {
        const [setOperator, ...rest] = setOperators;
        if (!setOperator) {
          throw new Error("Cannot pass undefined values to any set operator");
        }
        if (rest.length === 0) {
          return this.buildSetOperationQuery({ leftSelect, setOperator });
        }
        return this.buildSetOperations(
          this.buildSetOperationQuery({ leftSelect, setOperator }),
          rest
        );
      }
      buildSetOperationQuery({
        leftSelect,
        setOperator: { type, isAll, rightSelect, limit, orderBy, offset }
      }) {
        const leftChunk = sql`${leftSelect.getSQL()} `;
        const rightChunk = sql`${rightSelect.getSQL()}`;
        let orderBySql;
        if (orderBy && orderBy.length > 0) {
          const orderByValues = [];
          for (const singleOrderBy of orderBy) {
            if (is(singleOrderBy, SQLiteColumn)) {
              orderByValues.push(sql.identifier(singleOrderBy.name));
            } else if (is(singleOrderBy, SQL)) {
              for (let i = 0; i < singleOrderBy.queryChunks.length; i++) {
                const chunk = singleOrderBy.queryChunks[i];
                if (is(chunk, SQLiteColumn)) {
                  singleOrderBy.queryChunks[i] = sql.identifier(this.casing.getColumnCasing(chunk));
                }
              }
              orderByValues.push(sql`${singleOrderBy}`);
            } else {
              orderByValues.push(sql`${singleOrderBy}`);
            }
          }
          orderBySql = sql` order by ${sql.join(orderByValues, sql`, `)}`;
        }
        const limitSql = typeof limit === "object" || typeof limit === "number" && limit >= 0 ? sql` limit ${limit}` : void 0;
        const operatorChunk = sql.raw(`${type} ${isAll ? "all " : ""}`);
        const offsetSql = offset ? sql` offset ${offset}` : void 0;
        return sql`${leftChunk}${operatorChunk}${rightChunk}${orderBySql}${limitSql}${offsetSql}`;
      }
      buildInsertQuery({ table, values: valuesOrSelect, onConflict, returning, withList, select }) {
        const valuesSqlList = [];
        const columns = table[Table.Symbol.Columns];
        const colEntries = Object.entries(columns).filter(
          ([_, col]) => !col.shouldDisableInsert()
        );
        const insertOrder = colEntries.map(([, column]) => sql.identifier(this.casing.getColumnCasing(column)));
        if (select) {
          const select2 = valuesOrSelect;
          if (is(select2, SQL)) {
            valuesSqlList.push(select2);
          } else {
            valuesSqlList.push(select2.getSQL());
          }
        } else {
          const values = valuesOrSelect;
          valuesSqlList.push(sql.raw("values "));
          for (const [valueIndex, value] of values.entries()) {
            const valueList = [];
            for (const [fieldName, col] of colEntries) {
              const colValue = value[fieldName];
              if (colValue === void 0 || is(colValue, Param) && colValue.value === void 0) {
                let defaultValue;
                if (col.default !== null && col.default !== void 0) {
                  defaultValue = is(col.default, SQL) ? col.default : sql.param(col.default, col);
                } else if (col.defaultFn !== void 0) {
                  const defaultFnResult = col.defaultFn();
                  defaultValue = is(defaultFnResult, SQL) ? defaultFnResult : sql.param(defaultFnResult, col);
                } else if (!col.default && col.onUpdateFn !== void 0) {
                  const onUpdateFnResult = col.onUpdateFn();
                  defaultValue = is(onUpdateFnResult, SQL) ? onUpdateFnResult : sql.param(onUpdateFnResult, col);
                } else {
                  defaultValue = sql`null`;
                }
                valueList.push(defaultValue);
              } else {
                valueList.push(colValue);
              }
            }
            valuesSqlList.push(valueList);
            if (valueIndex < values.length - 1) {
              valuesSqlList.push(sql`, `);
            }
          }
        }
        const withSql = this.buildWithCTE(withList);
        const valuesSql = sql.join(valuesSqlList);
        const returningSql = returning ? sql` returning ${this.buildSelection(returning, { isSingleTable: true })}` : void 0;
        const onConflictSql = onConflict?.length ? sql.join(onConflict) : void 0;
        return sql`${withSql}insert into ${table} ${insertOrder} ${valuesSql}${onConflictSql}${returningSql}`;
      }
      sqlToQuery(sql22, invokeSource) {
        return sql22.toQuery({
          casing: this.casing,
          escapeName: this.escapeName,
          escapeParam: this.escapeParam,
          escapeString: this.escapeString,
          invokeSource
        });
      }
      buildRelationalQuery({
        fullSchema,
        schema,
        tableNamesMap,
        table,
        tableConfig,
        queryConfig: config,
        tableAlias,
        nestedQueryRelation,
        joinOn
      }) {
        let selection = [];
        let limit, offset, orderBy = [], where;
        const joins = [];
        if (config === true) {
          const selectionEntries = Object.entries(tableConfig.columns);
          selection = selectionEntries.map(([key, value]) => ({
            dbKey: value.name,
            tsKey: key,
            field: aliasedTableColumn(value, tableAlias),
            relationTableTsKey: void 0,
            isJson: false,
            selection: []
          }));
        } else {
          const aliasedColumns = Object.fromEntries(
            Object.entries(tableConfig.columns).map(([key, value]) => [key, aliasedTableColumn(value, tableAlias)])
          );
          if (config.where) {
            const whereSql = typeof config.where === "function" ? config.where(aliasedColumns, getOperators()) : config.where;
            where = whereSql && mapColumnsInSQLToAlias(whereSql, tableAlias);
          }
          const fieldsSelection = [];
          let selectedColumns = [];
          if (config.columns) {
            let isIncludeMode = false;
            for (const [field, value] of Object.entries(config.columns)) {
              if (value === void 0) {
                continue;
              }
              if (field in tableConfig.columns) {
                if (!isIncludeMode && value === true) {
                  isIncludeMode = true;
                }
                selectedColumns.push(field);
              }
            }
            if (selectedColumns.length > 0) {
              selectedColumns = isIncludeMode ? selectedColumns.filter((c) => config.columns?.[c] === true) : Object.keys(tableConfig.columns).filter((key) => !selectedColumns.includes(key));
            }
          } else {
            selectedColumns = Object.keys(tableConfig.columns);
          }
          for (const field of selectedColumns) {
            const column = tableConfig.columns[field];
            fieldsSelection.push({ tsKey: field, value: column });
          }
          let selectedRelations = [];
          if (config.with) {
            selectedRelations = Object.entries(config.with).filter((entry) => !!entry[1]).map(([tsKey, queryConfig]) => ({ tsKey, queryConfig, relation: tableConfig.relations[tsKey] }));
          }
          let extras;
          if (config.extras) {
            extras = typeof config.extras === "function" ? config.extras(aliasedColumns, { sql }) : config.extras;
            for (const [tsKey, value] of Object.entries(extras)) {
              fieldsSelection.push({
                tsKey,
                value: mapColumnsInAliasedSQLToAlias(value, tableAlias)
              });
            }
          }
          for (const { tsKey, value } of fieldsSelection) {
            selection.push({
              dbKey: is(value, SQL.Aliased) ? value.fieldAlias : tableConfig.columns[tsKey].name,
              tsKey,
              field: is(value, Column) ? aliasedTableColumn(value, tableAlias) : value,
              relationTableTsKey: void 0,
              isJson: false,
              selection: []
            });
          }
          let orderByOrig = typeof config.orderBy === "function" ? config.orderBy(aliasedColumns, getOrderByOperators()) : config.orderBy ?? [];
          if (!Array.isArray(orderByOrig)) {
            orderByOrig = [orderByOrig];
          }
          orderBy = orderByOrig.map((orderByValue) => {
            if (is(orderByValue, Column)) {
              return aliasedTableColumn(orderByValue, tableAlias);
            }
            return mapColumnsInSQLToAlias(orderByValue, tableAlias);
          });
          limit = config.limit;
          offset = config.offset;
          for (const {
            tsKey: selectedRelationTsKey,
            queryConfig: selectedRelationConfigValue,
            relation
          } of selectedRelations) {
            const normalizedRelation = normalizeRelation(schema, tableNamesMap, relation);
            const relationTableName = getTableUniqueName(relation.referencedTable);
            const relationTableTsName = tableNamesMap[relationTableName];
            const relationTableAlias = `${tableAlias}_${selectedRelationTsKey}`;
            const joinOn2 = and(
              ...normalizedRelation.fields.map(
                (field2, i) => eq(
                  aliasedTableColumn(normalizedRelation.references[i], relationTableAlias),
                  aliasedTableColumn(field2, tableAlias)
                )
              )
            );
            const builtRelation = this.buildRelationalQuery({
              fullSchema,
              schema,
              tableNamesMap,
              table: fullSchema[relationTableTsName],
              tableConfig: schema[relationTableTsName],
              queryConfig: is(relation, One) ? selectedRelationConfigValue === true ? { limit: 1 } : { ...selectedRelationConfigValue, limit: 1 } : selectedRelationConfigValue,
              tableAlias: relationTableAlias,
              joinOn: joinOn2,
              nestedQueryRelation: relation
            });
            const field = sql`(${builtRelation.sql})`.as(selectedRelationTsKey);
            selection.push({
              dbKey: selectedRelationTsKey,
              tsKey: selectedRelationTsKey,
              field,
              relationTableTsKey: relationTableTsName,
              isJson: true,
              selection: builtRelation.selection
            });
          }
        }
        if (selection.length === 0) {
          throw new DrizzleError({
            message: `No fields selected for table "${tableConfig.tsName}" ("${tableAlias}"). You need to have at least one item in "columns", "with" or "extras". If you need to select all columns, omit the "columns" key or set it to undefined.`
          });
        }
        let result;
        where = and(joinOn, where);
        if (nestedQueryRelation) {
          let field = sql`json_array(${sql.join(
            selection.map(
              ({ field: field2 }) => is(field2, SQLiteColumn) ? sql.identifier(this.casing.getColumnCasing(field2)) : is(field2, SQL.Aliased) ? field2.sql : field2
            ),
            sql`, `
          )})`;
          if (is(nestedQueryRelation, Many)) {
            field = sql`coalesce(json_group_array(${field}), json_array())`;
          }
          const nestedSelection = [{
            dbKey: "data",
            tsKey: "data",
            field: field.as("data"),
            isJson: true,
            relationTableTsKey: tableConfig.tsName,
            selection
          }];
          const needsSubquery = limit !== void 0 || offset !== void 0 || orderBy.length > 0;
          if (needsSubquery) {
            result = this.buildSelectQuery({
              table: aliasedTable(table, tableAlias),
              fields: {},
              fieldsFlat: [
                {
                  path: [],
                  field: sql.raw("*")
                }
              ],
              where,
              limit,
              offset,
              orderBy,
              setOperators: []
            });
            where = void 0;
            limit = void 0;
            offset = void 0;
            orderBy = void 0;
          } else {
            result = aliasedTable(table, tableAlias);
          }
          result = this.buildSelectQuery({
            table: is(result, SQLiteTable) ? result : new Subquery(result, {}, tableAlias),
            fields: {},
            fieldsFlat: nestedSelection.map(({ field: field2 }) => ({
              path: [],
              field: is(field2, Column) ? aliasedTableColumn(field2, tableAlias) : field2
            })),
            joins,
            where,
            limit,
            offset,
            orderBy,
            setOperators: []
          });
        } else {
          result = this.buildSelectQuery({
            table: aliasedTable(table, tableAlias),
            fields: {},
            fieldsFlat: selection.map(({ field }) => ({
              path: [],
              field: is(field, Column) ? aliasedTableColumn(field, tableAlias) : field
            })),
            joins,
            where,
            limit,
            offset,
            orderBy,
            setOperators: []
          });
        }
        return {
          tableTsKey: tableConfig.tsName,
          sql: result,
          selection
        };
      }
    };
    SQLiteSyncDialect = class extends SQLiteDialect {
      static {
        __name(this, "SQLiteSyncDialect");
      }
      static [entityKind] = "SQLiteSyncDialect";
      migrate(migrations, session, config) {
        const migrationsTable = config === void 0 ? "__drizzle_migrations" : typeof config === "string" ? "__drizzle_migrations" : config.migrationsTable ?? "__drizzle_migrations";
        const migrationTableCreate = sql`
			CREATE TABLE IF NOT EXISTS ${sql.identifier(migrationsTable)} (
				id SERIAL PRIMARY KEY,
				hash text NOT NULL,
				created_at numeric
			)
		`;
        session.run(migrationTableCreate);
        const dbMigrations = session.values(
          sql`SELECT id, hash, created_at FROM ${sql.identifier(migrationsTable)} ORDER BY created_at DESC LIMIT 1`
        );
        const lastDbMigration = dbMigrations[0] ?? void 0;
        session.run(sql`BEGIN`);
        try {
          for (const migration of migrations) {
            if (!lastDbMigration || Number(lastDbMigration[2]) < migration.folderMillis) {
              for (const stmt of migration.sql) {
                session.run(sql.raw(stmt));
              }
              session.run(
                sql`INSERT INTO ${sql.identifier(migrationsTable)} ("hash", "created_at") VALUES(${migration.hash}, ${migration.folderMillis})`
              );
            }
          }
          session.run(sql`COMMIT`);
        } catch (e) {
          session.run(sql`ROLLBACK`);
          throw e;
        }
      }
    };
    SQLiteAsyncDialect = class extends SQLiteDialect {
      static {
        __name(this, "SQLiteAsyncDialect");
      }
      static [entityKind] = "SQLiteAsyncDialect";
      async migrate(migrations, session, config) {
        const migrationsTable = config === void 0 ? "__drizzle_migrations" : typeof config === "string" ? "__drizzle_migrations" : config.migrationsTable ?? "__drizzle_migrations";
        const migrationTableCreate = sql`
			CREATE TABLE IF NOT EXISTS ${sql.identifier(migrationsTable)} (
				id SERIAL PRIMARY KEY,
				hash text NOT NULL,
				created_at numeric
			)
		`;
        await session.run(migrationTableCreate);
        const dbMigrations = await session.values(
          sql`SELECT id, hash, created_at FROM ${sql.identifier(migrationsTable)} ORDER BY created_at DESC LIMIT 1`
        );
        const lastDbMigration = dbMigrations[0] ?? void 0;
        await session.transaction(async (tx) => {
          for (const migration of migrations) {
            if (!lastDbMigration || Number(lastDbMigration[2]) < migration.folderMillis) {
              for (const stmt of migration.sql) {
                await tx.run(sql.raw(stmt));
              }
              await tx.run(
                sql`INSERT INTO ${sql.identifier(migrationsTable)} ("hash", "created_at") VALUES(${migration.hash}, ${migration.folderMillis})`
              );
            }
          }
        });
      }
    };
  }
});

// worker/node_modules/drizzle-orm/query-builders/query-builder.js
var TypedQueryBuilder;
var init_query_builder = __esm({
  "worker/node_modules/drizzle-orm/query-builders/query-builder.js"() {
    init_entity();
    TypedQueryBuilder = class {
      static {
        __name(this, "TypedQueryBuilder");
      }
      static [entityKind] = "TypedQueryBuilder";
      /** @internal */
      getSelectedFields() {
        return this._.selectedFields;
      }
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/query-builders/select.js
function createSetOperator(type, isAll) {
  return (leftSelect, rightSelect, ...restSelects) => {
    const setOperators = [rightSelect, ...restSelects].map((select) => ({
      type,
      isAll,
      rightSelect: select
    }));
    for (const setOperator of setOperators) {
      if (!haveSameKeys(leftSelect.getSelectedFields(), setOperator.rightSelect.getSelectedFields())) {
        throw new Error(
          "Set operator error (union / intersect / except): selected fields are not the same or are in a different order"
        );
      }
    }
    return leftSelect.addSetOperators(setOperators);
  };
}
var SQLiteSelectBuilder, SQLiteSelectQueryBuilderBase, SQLiteSelectBase, getSQLiteSetOperators, union, unionAll, intersect, except;
var init_select2 = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/query-builders/select.js"() {
    init_entity();
    init_query_builder();
    init_query_promise();
    init_selection_proxy();
    init_sql2();
    init_subquery();
    init_table();
    init_utils();
    init_view_common();
    init_view_base();
    SQLiteSelectBuilder = class {
      static {
        __name(this, "SQLiteSelectBuilder");
      }
      static [entityKind] = "SQLiteSelectBuilder";
      fields;
      session;
      dialect;
      withList;
      distinct;
      constructor(config) {
        this.fields = config.fields;
        this.session = config.session;
        this.dialect = config.dialect;
        this.withList = config.withList;
        this.distinct = config.distinct;
      }
      from(source) {
        const isPartialSelect = !!this.fields;
        let fields;
        if (this.fields) {
          fields = this.fields;
        } else if (is(source, Subquery)) {
          fields = Object.fromEntries(
            Object.keys(source._.selectedFields).map((key) => [key, source[key]])
          );
        } else if (is(source, SQLiteViewBase)) {
          fields = source[ViewBaseConfig].selectedFields;
        } else if (is(source, SQL)) {
          fields = {};
        } else {
          fields = getTableColumns(source);
        }
        return new SQLiteSelectBase({
          table: source,
          fields,
          isPartialSelect,
          session: this.session,
          dialect: this.dialect,
          withList: this.withList,
          distinct: this.distinct
        });
      }
    };
    SQLiteSelectQueryBuilderBase = class extends TypedQueryBuilder {
      static {
        __name(this, "SQLiteSelectQueryBuilderBase");
      }
      static [entityKind] = "SQLiteSelectQueryBuilder";
      _;
      /** @internal */
      config;
      joinsNotNullableMap;
      tableName;
      isPartialSelect;
      session;
      dialect;
      constructor({ table, fields, isPartialSelect, session, dialect, withList, distinct }) {
        super();
        this.config = {
          withList,
          table,
          fields: { ...fields },
          distinct,
          setOperators: []
        };
        this.isPartialSelect = isPartialSelect;
        this.session = session;
        this.dialect = dialect;
        this._ = {
          selectedFields: fields
        };
        this.tableName = getTableLikeName(table);
        this.joinsNotNullableMap = typeof this.tableName === "string" ? { [this.tableName]: true } : {};
      }
      createJoin(joinType) {
        return (table, on) => {
          const baseTableName = this.tableName;
          const tableName = getTableLikeName(table);
          if (typeof tableName === "string" && this.config.joins?.some((join) => join.alias === tableName)) {
            throw new Error(`Alias "${tableName}" is already used in this query`);
          }
          if (!this.isPartialSelect) {
            if (Object.keys(this.joinsNotNullableMap).length === 1 && typeof baseTableName === "string") {
              this.config.fields = {
                [baseTableName]: this.config.fields
              };
            }
            if (typeof tableName === "string" && !is(table, SQL)) {
              const selection = is(table, Subquery) ? table._.selectedFields : is(table, View) ? table[ViewBaseConfig].selectedFields : table[Table.Symbol.Columns];
              this.config.fields[tableName] = selection;
            }
          }
          if (typeof on === "function") {
            on = on(
              new Proxy(
                this.config.fields,
                new SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
              )
            );
          }
          if (!this.config.joins) {
            this.config.joins = [];
          }
          this.config.joins.push({ on, table, joinType, alias: tableName });
          if (typeof tableName === "string") {
            switch (joinType) {
              case "left": {
                this.joinsNotNullableMap[tableName] = false;
                break;
              }
              case "right": {
                this.joinsNotNullableMap = Object.fromEntries(
                  Object.entries(this.joinsNotNullableMap).map(([key]) => [key, false])
                );
                this.joinsNotNullableMap[tableName] = true;
                break;
              }
              case "inner": {
                this.joinsNotNullableMap[tableName] = true;
                break;
              }
              case "full": {
                this.joinsNotNullableMap = Object.fromEntries(
                  Object.entries(this.joinsNotNullableMap).map(([key]) => [key, false])
                );
                this.joinsNotNullableMap[tableName] = false;
                break;
              }
            }
          }
          return this;
        };
      }
      /**
       * Executes a `left join` operation by adding another table to the current query.
       *
       * Calling this method associates each row of the table with the corresponding row from the joined table, if a match is found. If no matching row exists, it sets all columns of the joined table to null.
       *
       * See docs: {@link https://orm.drizzle.team/docs/joins#left-join}
       *
       * @param table the table to join.
       * @param on the `on` clause.
       *
       * @example
       *
       * ```ts
       * // Select all users and their pets
       * const usersWithPets: { user: User; pets: Pet | null }[] = await db.select()
       *   .from(users)
       *   .leftJoin(pets, eq(users.id, pets.ownerId))
       *
       * // Select userId and petId
       * const usersIdsAndPetIds: { userId: number; petId: number | null }[] = await db.select({
       *   userId: users.id,
       *   petId: pets.id,
       * })
       *   .from(users)
       *   .leftJoin(pets, eq(users.id, pets.ownerId))
       * ```
       */
      leftJoin = this.createJoin("left");
      /**
       * Executes a `right join` operation by adding another table to the current query.
       *
       * Calling this method associates each row of the joined table with the corresponding row from the main table, if a match is found. If no matching row exists, it sets all columns of the main table to null.
       *
       * See docs: {@link https://orm.drizzle.team/docs/joins#right-join}
       *
       * @param table the table to join.
       * @param on the `on` clause.
       *
       * @example
       *
       * ```ts
       * // Select all users and their pets
       * const usersWithPets: { user: User | null; pets: Pet }[] = await db.select()
       *   .from(users)
       *   .rightJoin(pets, eq(users.id, pets.ownerId))
       *
       * // Select userId and petId
       * const usersIdsAndPetIds: { userId: number | null; petId: number }[] = await db.select({
       *   userId: users.id,
       *   petId: pets.id,
       * })
       *   .from(users)
       *   .rightJoin(pets, eq(users.id, pets.ownerId))
       * ```
       */
      rightJoin = this.createJoin("right");
      /**
       * Executes an `inner join` operation, creating a new table by combining rows from two tables that have matching values.
       *
       * Calling this method retrieves rows that have corresponding entries in both joined tables. Rows without matching entries in either table are excluded, resulting in a table that includes only matching pairs.
       *
       * See docs: {@link https://orm.drizzle.team/docs/joins#inner-join}
       *
       * @param table the table to join.
       * @param on the `on` clause.
       *
       * @example
       *
       * ```ts
       * // Select all users and their pets
       * const usersWithPets: { user: User; pets: Pet }[] = await db.select()
       *   .from(users)
       *   .innerJoin(pets, eq(users.id, pets.ownerId))
       *
       * // Select userId and petId
       * const usersIdsAndPetIds: { userId: number; petId: number }[] = await db.select({
       *   userId: users.id,
       *   petId: pets.id,
       * })
       *   .from(users)
       *   .innerJoin(pets, eq(users.id, pets.ownerId))
       * ```
       */
      innerJoin = this.createJoin("inner");
      /**
       * Executes a `full join` operation by combining rows from two tables into a new table.
       *
       * Calling this method retrieves all rows from both main and joined tables, merging rows with matching values and filling in `null` for non-matching columns.
       *
       * See docs: {@link https://orm.drizzle.team/docs/joins#full-join}
       *
       * @param table the table to join.
       * @param on the `on` clause.
       *
       * @example
       *
       * ```ts
       * // Select all users and their pets
       * const usersWithPets: { user: User | null; pets: Pet | null }[] = await db.select()
       *   .from(users)
       *   .fullJoin(pets, eq(users.id, pets.ownerId))
       *
       * // Select userId and petId
       * const usersIdsAndPetIds: { userId: number | null; petId: number | null }[] = await db.select({
       *   userId: users.id,
       *   petId: pets.id,
       * })
       *   .from(users)
       *   .fullJoin(pets, eq(users.id, pets.ownerId))
       * ```
       */
      fullJoin = this.createJoin("full");
      createSetOperator(type, isAll) {
        return (rightSelection) => {
          const rightSelect = typeof rightSelection === "function" ? rightSelection(getSQLiteSetOperators()) : rightSelection;
          if (!haveSameKeys(this.getSelectedFields(), rightSelect.getSelectedFields())) {
            throw new Error(
              "Set operator error (union / intersect / except): selected fields are not the same or are in a different order"
            );
          }
          this.config.setOperators.push({ type, isAll, rightSelect });
          return this;
        };
      }
      /**
       * Adds `union` set operator to the query.
       *
       * Calling this method will combine the result sets of the `select` statements and remove any duplicate rows that appear across them.
       *
       * See docs: {@link https://orm.drizzle.team/docs/set-operations#union}
       *
       * @example
       *
       * ```ts
       * // Select all unique names from customers and users tables
       * await db.select({ name: users.name })
       *   .from(users)
       *   .union(
       *     db.select({ name: customers.name }).from(customers)
       *   );
       * // or
       * import { union } from 'drizzle-orm/sqlite-core'
       *
       * await union(
       *   db.select({ name: users.name }).from(users),
       *   db.select({ name: customers.name }).from(customers)
       * );
       * ```
       */
      union = this.createSetOperator("union", false);
      /**
       * Adds `union all` set operator to the query.
       *
       * Calling this method will combine the result-set of the `select` statements and keep all duplicate rows that appear across them.
       *
       * See docs: {@link https://orm.drizzle.team/docs/set-operations#union-all}
       *
       * @example
       *
       * ```ts
       * // Select all transaction ids from both online and in-store sales
       * await db.select({ transaction: onlineSales.transactionId })
       *   .from(onlineSales)
       *   .unionAll(
       *     db.select({ transaction: inStoreSales.transactionId }).from(inStoreSales)
       *   );
       * // or
       * import { unionAll } from 'drizzle-orm/sqlite-core'
       *
       * await unionAll(
       *   db.select({ transaction: onlineSales.transactionId }).from(onlineSales),
       *   db.select({ transaction: inStoreSales.transactionId }).from(inStoreSales)
       * );
       * ```
       */
      unionAll = this.createSetOperator("union", true);
      /**
       * Adds `intersect` set operator to the query.
       *
       * Calling this method will retain only the rows that are present in both result sets and eliminate duplicates.
       *
       * See docs: {@link https://orm.drizzle.team/docs/set-operations#intersect}
       *
       * @example
       *
       * ```ts
       * // Select course names that are offered in both departments A and B
       * await db.select({ courseName: depA.courseName })
       *   .from(depA)
       *   .intersect(
       *     db.select({ courseName: depB.courseName }).from(depB)
       *   );
       * // or
       * import { intersect } from 'drizzle-orm/sqlite-core'
       *
       * await intersect(
       *   db.select({ courseName: depA.courseName }).from(depA),
       *   db.select({ courseName: depB.courseName }).from(depB)
       * );
       * ```
       */
      intersect = this.createSetOperator("intersect", false);
      /**
       * Adds `except` set operator to the query.
       *
       * Calling this method will retrieve all unique rows from the left query, except for the rows that are present in the result set of the right query.
       *
       * See docs: {@link https://orm.drizzle.team/docs/set-operations#except}
       *
       * @example
       *
       * ```ts
       * // Select all courses offered in department A but not in department B
       * await db.select({ courseName: depA.courseName })
       *   .from(depA)
       *   .except(
       *     db.select({ courseName: depB.courseName }).from(depB)
       *   );
       * // or
       * import { except } from 'drizzle-orm/sqlite-core'
       *
       * await except(
       *   db.select({ courseName: depA.courseName }).from(depA),
       *   db.select({ courseName: depB.courseName }).from(depB)
       * );
       * ```
       */
      except = this.createSetOperator("except", false);
      /** @internal */
      addSetOperators(setOperators) {
        this.config.setOperators.push(...setOperators);
        return this;
      }
      /**
       * Adds a `where` clause to the query.
       *
       * Calling this method will select only those rows that fulfill a specified condition.
       *
       * See docs: {@link https://orm.drizzle.team/docs/select#filtering}
       *
       * @param where the `where` clause.
       *
       * @example
       * You can use conditional operators and `sql function` to filter the rows to be selected.
       *
       * ```ts
       * // Select all cars with green color
       * await db.select().from(cars).where(eq(cars.color, 'green'));
       * // or
       * await db.select().from(cars).where(sql`${cars.color} = 'green'`)
       * ```
       *
       * You can logically combine conditional operators with `and()` and `or()` operators:
       *
       * ```ts
       * // Select all BMW cars with a green color
       * await db.select().from(cars).where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
       *
       * // Select all cars with the green or blue color
       * await db.select().from(cars).where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
       * ```
       */
      where(where) {
        if (typeof where === "function") {
          where = where(
            new Proxy(
              this.config.fields,
              new SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
            )
          );
        }
        this.config.where = where;
        return this;
      }
      /**
       * Adds a `having` clause to the query.
       *
       * Calling this method will select only those rows that fulfill a specified condition. It is typically used with aggregate functions to filter the aggregated data based on a specified condition.
       *
       * See docs: {@link https://orm.drizzle.team/docs/select#aggregations}
       *
       * @param having the `having` clause.
       *
       * @example
       *
       * ```ts
       * // Select all brands with more than one car
       * await db.select({
       * 	brand: cars.brand,
       * 	count: sql<number>`cast(count(${cars.id}) as int)`,
       * })
       *   .from(cars)
       *   .groupBy(cars.brand)
       *   .having(({ count }) => gt(count, 1));
       * ```
       */
      having(having) {
        if (typeof having === "function") {
          having = having(
            new Proxy(
              this.config.fields,
              new SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
            )
          );
        }
        this.config.having = having;
        return this;
      }
      groupBy(...columns) {
        if (typeof columns[0] === "function") {
          const groupBy = columns[0](
            new Proxy(
              this.config.fields,
              new SelectionProxyHandler({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })
            )
          );
          this.config.groupBy = Array.isArray(groupBy) ? groupBy : [groupBy];
        } else {
          this.config.groupBy = columns;
        }
        return this;
      }
      orderBy(...columns) {
        if (typeof columns[0] === "function") {
          const orderBy = columns[0](
            new Proxy(
              this.config.fields,
              new SelectionProxyHandler({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })
            )
          );
          const orderByArray = Array.isArray(orderBy) ? orderBy : [orderBy];
          if (this.config.setOperators.length > 0) {
            this.config.setOperators.at(-1).orderBy = orderByArray;
          } else {
            this.config.orderBy = orderByArray;
          }
        } else {
          const orderByArray = columns;
          if (this.config.setOperators.length > 0) {
            this.config.setOperators.at(-1).orderBy = orderByArray;
          } else {
            this.config.orderBy = orderByArray;
          }
        }
        return this;
      }
      /**
       * Adds a `limit` clause to the query.
       *
       * Calling this method will set the maximum number of rows that will be returned by this query.
       *
       * See docs: {@link https://orm.drizzle.team/docs/select#limit--offset}
       *
       * @param limit the `limit` clause.
       *
       * @example
       *
       * ```ts
       * // Get the first 10 people from this query.
       * await db.select().from(people).limit(10);
       * ```
       */
      limit(limit) {
        if (this.config.setOperators.length > 0) {
          this.config.setOperators.at(-1).limit = limit;
        } else {
          this.config.limit = limit;
        }
        return this;
      }
      /**
       * Adds an `offset` clause to the query.
       *
       * Calling this method will skip a number of rows when returning results from this query.
       *
       * See docs: {@link https://orm.drizzle.team/docs/select#limit--offset}
       *
       * @param offset the `offset` clause.
       *
       * @example
       *
       * ```ts
       * // Get the 10th-20th people from this query.
       * await db.select().from(people).offset(10).limit(10);
       * ```
       */
      offset(offset) {
        if (this.config.setOperators.length > 0) {
          this.config.setOperators.at(-1).offset = offset;
        } else {
          this.config.offset = offset;
        }
        return this;
      }
      /** @internal */
      getSQL() {
        return this.dialect.buildSelectQuery(this.config);
      }
      toSQL() {
        const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
        return rest;
      }
      as(alias) {
        return new Proxy(
          new Subquery(this.getSQL(), this.config.fields, alias),
          new SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
        );
      }
      /** @internal */
      getSelectedFields() {
        return new Proxy(
          this.config.fields,
          new SelectionProxyHandler({ alias: this.tableName, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
        );
      }
      $dynamic() {
        return this;
      }
    };
    SQLiteSelectBase = class extends SQLiteSelectQueryBuilderBase {
      static {
        __name(this, "SQLiteSelectBase");
      }
      static [entityKind] = "SQLiteSelect";
      /** @internal */
      _prepare(isOneTimeQuery = true) {
        if (!this.session) {
          throw new Error("Cannot execute a query on a query builder. Please use a database instance instead.");
        }
        const fieldsList = orderSelectedFields(this.config.fields);
        const query = this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](
          this.dialect.sqlToQuery(this.getSQL()),
          fieldsList,
          "all",
          true
        );
        query.joinsNotNullableMap = this.joinsNotNullableMap;
        return query;
      }
      prepare() {
        return this._prepare(false);
      }
      run = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().run(placeholderValues);
      }, "run");
      all = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().all(placeholderValues);
      }, "all");
      get = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().get(placeholderValues);
      }, "get");
      values = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().values(placeholderValues);
      }, "values");
      async execute() {
        return this.all();
      }
    };
    applyMixins(SQLiteSelectBase, [QueryPromise]);
    __name(createSetOperator, "createSetOperator");
    getSQLiteSetOperators = /* @__PURE__ */ __name(() => ({
      union,
      unionAll,
      intersect,
      except
    }), "getSQLiteSetOperators");
    union = createSetOperator("union", false);
    unionAll = createSetOperator("union", true);
    intersect = createSetOperator("intersect", false);
    except = createSetOperator("except", false);
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/query-builders/query-builder.js
var QueryBuilder;
var init_query_builder2 = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/query-builders/query-builder.js"() {
    init_entity();
    init_selection_proxy();
    init_dialect();
    init_subquery();
    init_select2();
    QueryBuilder = class {
      static {
        __name(this, "QueryBuilder");
      }
      static [entityKind] = "SQLiteQueryBuilder";
      dialect;
      dialectConfig;
      constructor(dialect) {
        this.dialect = is(dialect, SQLiteDialect) ? dialect : void 0;
        this.dialectConfig = is(dialect, SQLiteDialect) ? void 0 : dialect;
      }
      $with = /* @__PURE__ */ __name((alias, selection) => {
        const queryBuilder = this;
        const as = /* @__PURE__ */ __name((qb) => {
          if (typeof qb === "function") {
            qb = qb(queryBuilder);
          }
          return new Proxy(
            new WithSubquery(
              qb.getSQL(),
              selection ?? ("getSelectedFields" in qb ? qb.getSelectedFields() ?? {} : {}),
              alias,
              true
            ),
            new SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
          );
        }, "as");
        return { as };
      }, "$with");
      with(...queries) {
        const self2 = this;
        function select(fields) {
          return new SQLiteSelectBuilder({
            fields: fields ?? void 0,
            session: void 0,
            dialect: self2.getDialect(),
            withList: queries
          });
        }
        __name(select, "select");
        function selectDistinct(fields) {
          return new SQLiteSelectBuilder({
            fields: fields ?? void 0,
            session: void 0,
            dialect: self2.getDialect(),
            withList: queries,
            distinct: true
          });
        }
        __name(selectDistinct, "selectDistinct");
        return { select, selectDistinct };
      }
      select(fields) {
        return new SQLiteSelectBuilder({ fields: fields ?? void 0, session: void 0, dialect: this.getDialect() });
      }
      selectDistinct(fields) {
        return new SQLiteSelectBuilder({
          fields: fields ?? void 0,
          session: void 0,
          dialect: this.getDialect(),
          distinct: true
        });
      }
      // Lazy load dialect to avoid circular dependency
      getDialect() {
        if (!this.dialect) {
          this.dialect = new SQLiteSyncDialect(this.dialectConfig);
        }
        return this.dialect;
      }
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/query-builders/insert.js
var SQLiteInsertBuilder, SQLiteInsertBase;
var init_insert = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/query-builders/insert.js"() {
    init_entity();
    init_query_promise();
    init_sql2();
    init_table3();
    init_table();
    init_utils();
    init_query_builder2();
    SQLiteInsertBuilder = class {
      static {
        __name(this, "SQLiteInsertBuilder");
      }
      constructor(table, session, dialect, withList) {
        this.table = table;
        this.session = session;
        this.dialect = dialect;
        this.withList = withList;
      }
      static [entityKind] = "SQLiteInsertBuilder";
      values(values) {
        values = Array.isArray(values) ? values : [values];
        if (values.length === 0) {
          throw new Error("values() must be called with at least one value");
        }
        const mappedValues = values.map((entry) => {
          const result = {};
          const cols = this.table[Table.Symbol.Columns];
          for (const colKey of Object.keys(entry)) {
            const colValue = entry[colKey];
            result[colKey] = is(colValue, SQL) ? colValue : new Param(colValue, cols[colKey]);
          }
          return result;
        });
        return new SQLiteInsertBase(this.table, mappedValues, this.session, this.dialect, this.withList);
      }
      select(selectQuery) {
        const select = typeof selectQuery === "function" ? selectQuery(new QueryBuilder()) : selectQuery;
        if (!is(select, SQL) && !haveSameKeys(this.table[Columns], select._.selectedFields)) {
          throw new Error(
            "Insert select error: selected fields are not the same or are in a different order compared to the table definition"
          );
        }
        return new SQLiteInsertBase(this.table, select, this.session, this.dialect, this.withList, true);
      }
    };
    SQLiteInsertBase = class extends QueryPromise {
      static {
        __name(this, "SQLiteInsertBase");
      }
      constructor(table, values, session, dialect, withList, select) {
        super();
        this.session = session;
        this.dialect = dialect;
        this.config = { table, values, withList, select };
      }
      static [entityKind] = "SQLiteInsert";
      /** @internal */
      config;
      returning(fields = this.config.table[SQLiteTable.Symbol.Columns]) {
        this.config.returning = orderSelectedFields(fields);
        return this;
      }
      /**
       * Adds an `on conflict do nothing` clause to the query.
       *
       * Calling this method simply avoids inserting a row as its alternative action.
       *
       * See docs: {@link https://orm.drizzle.team/docs/insert#on-conflict-do-nothing}
       *
       * @param config The `target` and `where` clauses.
       *
       * @example
       * ```ts
       * // Insert one row and cancel the insert if there's a conflict
       * await db.insert(cars)
       *   .values({ id: 1, brand: 'BMW' })
       *   .onConflictDoNothing();
       *
       * // Explicitly specify conflict target
       * await db.insert(cars)
       *   .values({ id: 1, brand: 'BMW' })
       *   .onConflictDoNothing({ target: cars.id });
       * ```
       */
      onConflictDoNothing(config = {}) {
        if (!this.config.onConflict)
          this.config.onConflict = [];
        if (config.target === void 0) {
          this.config.onConflict.push(sql` on conflict do nothing`);
        } else {
          const targetSql = Array.isArray(config.target) ? sql`${config.target}` : sql`${[config.target]}`;
          const whereSql = config.where ? sql` where ${config.where}` : sql``;
          this.config.onConflict.push(sql` on conflict ${targetSql} do nothing${whereSql}`);
        }
        return this;
      }
      /**
       * Adds an `on conflict do update` clause to the query.
       *
       * Calling this method will update the existing row that conflicts with the row proposed for insertion as its alternative action.
       *
       * See docs: {@link https://orm.drizzle.team/docs/insert#upserts-and-conflicts}
       *
       * @param config The `target`, `set` and `where` clauses.
       *
       * @example
       * ```ts
       * // Update the row if there's a conflict
       * await db.insert(cars)
       *   .values({ id: 1, brand: 'BMW' })
       *   .onConflictDoUpdate({
       *     target: cars.id,
       *     set: { brand: 'Porsche' }
       *   });
       *
       * // Upsert with 'where' clause
       * await db.insert(cars)
       *   .values({ id: 1, brand: 'BMW' })
       *   .onConflictDoUpdate({
       *     target: cars.id,
       *     set: { brand: 'newBMW' },
       *     where: sql`${cars.createdAt} > '2023-01-01'::date`,
       *   });
       * ```
       */
      onConflictDoUpdate(config) {
        if (config.where && (config.targetWhere || config.setWhere)) {
          throw new Error(
            'You cannot use both "where" and "targetWhere"/"setWhere" at the same time - "where" is deprecated, use "targetWhere" or "setWhere" instead.'
          );
        }
        if (!this.config.onConflict)
          this.config.onConflict = [];
        const whereSql = config.where ? sql` where ${config.where}` : void 0;
        const targetWhereSql = config.targetWhere ? sql` where ${config.targetWhere}` : void 0;
        const setWhereSql = config.setWhere ? sql` where ${config.setWhere}` : void 0;
        const targetSql = Array.isArray(config.target) ? sql`${config.target}` : sql`${[config.target]}`;
        const setSql = this.dialect.buildUpdateSet(this.config.table, mapUpdateSet(this.config.table, config.set));
        this.config.onConflict.push(
          sql` on conflict ${targetSql}${targetWhereSql} do update set ${setSql}${whereSql}${setWhereSql}`
        );
        return this;
      }
      /** @internal */
      getSQL() {
        return this.dialect.buildInsertQuery(this.config);
      }
      toSQL() {
        const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
        return rest;
      }
      /** @internal */
      _prepare(isOneTimeQuery = true) {
        return this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](
          this.dialect.sqlToQuery(this.getSQL()),
          this.config.returning,
          this.config.returning ? "all" : "run",
          true
        );
      }
      prepare() {
        return this._prepare(false);
      }
      run = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().run(placeholderValues);
      }, "run");
      all = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().all(placeholderValues);
      }, "all");
      get = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().get(placeholderValues);
      }, "get");
      values = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().values(placeholderValues);
      }, "values");
      async execute() {
        return this.config.returning ? this.all() : this.run();
      }
      $dynamic() {
        return this;
      }
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/query-builders/select.types.js
var init_select_types = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/query-builders/select.types.js"() {
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/query-builders/update.js
var SQLiteUpdateBuilder, SQLiteUpdateBase;
var init_update = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/query-builders/update.js"() {
    init_entity();
    init_query_promise();
    init_selection_proxy();
    init_table3();
    init_subquery();
    init_table();
    init_utils();
    init_view_common();
    init_view_base();
    SQLiteUpdateBuilder = class {
      static {
        __name(this, "SQLiteUpdateBuilder");
      }
      constructor(table, session, dialect, withList) {
        this.table = table;
        this.session = session;
        this.dialect = dialect;
        this.withList = withList;
      }
      static [entityKind] = "SQLiteUpdateBuilder";
      set(values) {
        return new SQLiteUpdateBase(
          this.table,
          mapUpdateSet(this.table, values),
          this.session,
          this.dialect,
          this.withList
        );
      }
    };
    SQLiteUpdateBase = class extends QueryPromise {
      static {
        __name(this, "SQLiteUpdateBase");
      }
      constructor(table, set2, session, dialect, withList) {
        super();
        this.session = session;
        this.dialect = dialect;
        this.config = { set: set2, table, withList, joins: [] };
      }
      static [entityKind] = "SQLiteUpdate";
      /** @internal */
      config;
      from(source) {
        this.config.from = source;
        return this;
      }
      createJoin(joinType) {
        return (table, on) => {
          const tableName = getTableLikeName(table);
          if (typeof tableName === "string" && this.config.joins.some((join) => join.alias === tableName)) {
            throw new Error(`Alias "${tableName}" is already used in this query`);
          }
          if (typeof on === "function") {
            const from = this.config.from ? is(table, SQLiteTable) ? table[Table.Symbol.Columns] : is(table, Subquery) ? table._.selectedFields : is(table, SQLiteViewBase) ? table[ViewBaseConfig].selectedFields : void 0 : void 0;
            on = on(
              new Proxy(
                this.config.table[Table.Symbol.Columns],
                new SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
              ),
              from && new Proxy(
                from,
                new SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
              )
            );
          }
          this.config.joins.push({ on, table, joinType, alias: tableName });
          return this;
        };
      }
      leftJoin = this.createJoin("left");
      rightJoin = this.createJoin("right");
      innerJoin = this.createJoin("inner");
      fullJoin = this.createJoin("full");
      /**
       * Adds a 'where' clause to the query.
       *
       * Calling this method will update only those rows that fulfill a specified condition.
       *
       * See docs: {@link https://orm.drizzle.team/docs/update}
       *
       * @param where the 'where' clause.
       *
       * @example
       * You can use conditional operators and `sql function` to filter the rows to be updated.
       *
       * ```ts
       * // Update all cars with green color
       * db.update(cars).set({ color: 'red' })
       *   .where(eq(cars.color, 'green'));
       * // or
       * db.update(cars).set({ color: 'red' })
       *   .where(sql`${cars.color} = 'green'`)
       * ```
       *
       * You can logically combine conditional operators with `and()` and `or()` operators:
       *
       * ```ts
       * // Update all BMW cars with a green color
       * db.update(cars).set({ color: 'red' })
       *   .where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
       *
       * // Update all cars with the green or blue color
       * db.update(cars).set({ color: 'red' })
       *   .where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
       * ```
       */
      where(where) {
        this.config.where = where;
        return this;
      }
      orderBy(...columns) {
        if (typeof columns[0] === "function") {
          const orderBy = columns[0](
            new Proxy(
              this.config.table[Table.Symbol.Columns],
              new SelectionProxyHandler({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })
            )
          );
          const orderByArray = Array.isArray(orderBy) ? orderBy : [orderBy];
          this.config.orderBy = orderByArray;
        } else {
          const orderByArray = columns;
          this.config.orderBy = orderByArray;
        }
        return this;
      }
      limit(limit) {
        this.config.limit = limit;
        return this;
      }
      returning(fields = this.config.table[SQLiteTable.Symbol.Columns]) {
        this.config.returning = orderSelectedFields(fields);
        return this;
      }
      /** @internal */
      getSQL() {
        return this.dialect.buildUpdateQuery(this.config);
      }
      toSQL() {
        const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
        return rest;
      }
      /** @internal */
      _prepare(isOneTimeQuery = true) {
        return this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](
          this.dialect.sqlToQuery(this.getSQL()),
          this.config.returning,
          this.config.returning ? "all" : "run",
          true
        );
      }
      prepare() {
        return this._prepare(false);
      }
      run = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().run(placeholderValues);
      }, "run");
      all = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().all(placeholderValues);
      }, "all");
      get = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().get(placeholderValues);
      }, "get");
      values = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().values(placeholderValues);
      }, "values");
      async execute() {
        return this.config.returning ? this.all() : this.run();
      }
      $dynamic() {
        return this;
      }
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/query-builders/index.js
var init_query_builders = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/query-builders/index.js"() {
    init_delete();
    init_insert();
    init_query_builder2();
    init_select2();
    init_select_types();
    init_update();
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/query-builders/count.js
var SQLiteCountBuilder;
var init_count = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/query-builders/count.js"() {
    init_entity();
    init_sql2();
    SQLiteCountBuilder = class _SQLiteCountBuilder extends SQL {
      static {
        __name(this, "SQLiteCountBuilder");
      }
      constructor(params) {
        super(_SQLiteCountBuilder.buildEmbeddedCount(params.source, params.filters).queryChunks);
        this.params = params;
        this.session = params.session;
        this.sql = _SQLiteCountBuilder.buildCount(
          params.source,
          params.filters
        );
      }
      sql;
      static [entityKind] = "SQLiteCountBuilderAsync";
      [Symbol.toStringTag] = "SQLiteCountBuilderAsync";
      session;
      static buildEmbeddedCount(source, filters) {
        return sql`(select count(*) from ${source}${sql.raw(" where ").if(filters)}${filters})`;
      }
      static buildCount(source, filters) {
        return sql`select count(*) from ${source}${sql.raw(" where ").if(filters)}${filters}`;
      }
      then(onfulfilled, onrejected) {
        return Promise.resolve(this.session.count(this.sql)).then(
          onfulfilled,
          onrejected
        );
      }
      catch(onRejected) {
        return this.then(void 0, onRejected);
      }
      finally(onFinally) {
        return this.then(
          (value) => {
            onFinally?.();
            return value;
          },
          (reason) => {
            onFinally?.();
            throw reason;
          }
        );
      }
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/query-builders/query.js
var RelationalQueryBuilder, SQLiteRelationalQuery, SQLiteSyncRelationalQuery;
var init_query = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/query-builders/query.js"() {
    init_entity();
    init_query_promise();
    init_relations();
    RelationalQueryBuilder = class {
      static {
        __name(this, "RelationalQueryBuilder");
      }
      constructor(mode, fullSchema, schema, tableNamesMap, table, tableConfig, dialect, session) {
        this.mode = mode;
        this.fullSchema = fullSchema;
        this.schema = schema;
        this.tableNamesMap = tableNamesMap;
        this.table = table;
        this.tableConfig = tableConfig;
        this.dialect = dialect;
        this.session = session;
      }
      static [entityKind] = "SQLiteAsyncRelationalQueryBuilder";
      findMany(config) {
        return this.mode === "sync" ? new SQLiteSyncRelationalQuery(
          this.fullSchema,
          this.schema,
          this.tableNamesMap,
          this.table,
          this.tableConfig,
          this.dialect,
          this.session,
          config ? config : {},
          "many"
        ) : new SQLiteRelationalQuery(
          this.fullSchema,
          this.schema,
          this.tableNamesMap,
          this.table,
          this.tableConfig,
          this.dialect,
          this.session,
          config ? config : {},
          "many"
        );
      }
      findFirst(config) {
        return this.mode === "sync" ? new SQLiteSyncRelationalQuery(
          this.fullSchema,
          this.schema,
          this.tableNamesMap,
          this.table,
          this.tableConfig,
          this.dialect,
          this.session,
          config ? { ...config, limit: 1 } : { limit: 1 },
          "first"
        ) : new SQLiteRelationalQuery(
          this.fullSchema,
          this.schema,
          this.tableNamesMap,
          this.table,
          this.tableConfig,
          this.dialect,
          this.session,
          config ? { ...config, limit: 1 } : { limit: 1 },
          "first"
        );
      }
    };
    SQLiteRelationalQuery = class extends QueryPromise {
      static {
        __name(this, "SQLiteRelationalQuery");
      }
      constructor(fullSchema, schema, tableNamesMap, table, tableConfig, dialect, session, config, mode) {
        super();
        this.fullSchema = fullSchema;
        this.schema = schema;
        this.tableNamesMap = tableNamesMap;
        this.table = table;
        this.tableConfig = tableConfig;
        this.dialect = dialect;
        this.session = session;
        this.config = config;
        this.mode = mode;
      }
      static [entityKind] = "SQLiteAsyncRelationalQuery";
      /** @internal */
      mode;
      /** @internal */
      getSQL() {
        return this.dialect.buildRelationalQuery({
          fullSchema: this.fullSchema,
          schema: this.schema,
          tableNamesMap: this.tableNamesMap,
          table: this.table,
          tableConfig: this.tableConfig,
          queryConfig: this.config,
          tableAlias: this.tableConfig.tsName
        }).sql;
      }
      /** @internal */
      _prepare(isOneTimeQuery = false) {
        const { query, builtQuery } = this._toSQL();
        return this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](
          builtQuery,
          void 0,
          this.mode === "first" ? "get" : "all",
          true,
          (rawRows, mapColumnValue) => {
            const rows = rawRows.map(
              (row) => mapRelationalRow(this.schema, this.tableConfig, row, query.selection, mapColumnValue)
            );
            if (this.mode === "first") {
              return rows[0];
            }
            return rows;
          }
        );
      }
      prepare() {
        return this._prepare(false);
      }
      _toSQL() {
        const query = this.dialect.buildRelationalQuery({
          fullSchema: this.fullSchema,
          schema: this.schema,
          tableNamesMap: this.tableNamesMap,
          table: this.table,
          tableConfig: this.tableConfig,
          queryConfig: this.config,
          tableAlias: this.tableConfig.tsName
        });
        const builtQuery = this.dialect.sqlToQuery(query.sql);
        return { query, builtQuery };
      }
      toSQL() {
        return this._toSQL().builtQuery;
      }
      /** @internal */
      executeRaw() {
        if (this.mode === "first") {
          return this._prepare(false).get();
        }
        return this._prepare(false).all();
      }
      async execute() {
        return this.executeRaw();
      }
    };
    SQLiteSyncRelationalQuery = class extends SQLiteRelationalQuery {
      static {
        __name(this, "SQLiteSyncRelationalQuery");
      }
      static [entityKind] = "SQLiteSyncRelationalQuery";
      sync() {
        return this.executeRaw();
      }
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/query-builders/raw.js
var SQLiteRaw;
var init_raw = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/query-builders/raw.js"() {
    init_entity();
    init_query_promise();
    SQLiteRaw = class extends QueryPromise {
      static {
        __name(this, "SQLiteRaw");
      }
      constructor(execute, getSQL, action, dialect, mapBatchResult) {
        super();
        this.execute = execute;
        this.getSQL = getSQL;
        this.dialect = dialect;
        this.mapBatchResult = mapBatchResult;
        this.config = { action };
      }
      static [entityKind] = "SQLiteRaw";
      /** @internal */
      config;
      getQuery() {
        return { ...this.dialect.sqlToQuery(this.getSQL()), method: this.config.action };
      }
      mapResult(result, isFromBatch) {
        return isFromBatch ? this.mapBatchResult(result) : result;
      }
      _prepare() {
        return this;
      }
      /** @internal */
      isResponseInArrayMode() {
        return false;
      }
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/db.js
var BaseSQLiteDatabase;
var init_db = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/db.js"() {
    init_entity();
    init_selection_proxy();
    init_sql2();
    init_query_builders();
    init_subquery();
    init_count();
    init_query();
    init_raw();
    BaseSQLiteDatabase = class {
      static {
        __name(this, "BaseSQLiteDatabase");
      }
      constructor(resultKind, dialect, session, schema) {
        this.resultKind = resultKind;
        this.dialect = dialect;
        this.session = session;
        this._ = schema ? {
          schema: schema.schema,
          fullSchema: schema.fullSchema,
          tableNamesMap: schema.tableNamesMap
        } : {
          schema: void 0,
          fullSchema: {},
          tableNamesMap: {}
        };
        this.query = {};
        const query = this.query;
        if (this._.schema) {
          for (const [tableName, columns] of Object.entries(this._.schema)) {
            query[tableName] = new RelationalQueryBuilder(
              resultKind,
              schema.fullSchema,
              this._.schema,
              this._.tableNamesMap,
              schema.fullSchema[tableName],
              columns,
              dialect,
              session
            );
          }
        }
      }
      static [entityKind] = "BaseSQLiteDatabase";
      query;
      /**
       * Creates a subquery that defines a temporary named result set as a CTE.
       *
       * It is useful for breaking down complex queries into simpler parts and for reusing the result set in subsequent parts of the query.
       *
       * See docs: {@link https://orm.drizzle.team/docs/select#with-clause}
       *
       * @param alias The alias for the subquery.
       *
       * Failure to provide an alias will result in a DrizzleTypeError, preventing the subquery from being referenced in other queries.
       *
       * @example
       *
       * ```ts
       * // Create a subquery with alias 'sq' and use it in the select query
       * const sq = db.$with('sq').as(db.select().from(users).where(eq(users.id, 42)));
       *
       * const result = await db.with(sq).select().from(sq);
       * ```
       *
       * To select arbitrary SQL values as fields in a CTE and reference them in other CTEs or in the main query, you need to add aliases to them:
       *
       * ```ts
       * // Select an arbitrary SQL value as a field in a CTE and reference it in the main query
       * const sq = db.$with('sq').as(db.select({
       *   name: sql<string>`upper(${users.name})`.as('name'),
       * })
       * .from(users));
       *
       * const result = await db.with(sq).select({ name: sq.name }).from(sq);
       * ```
       */
      $with = /* @__PURE__ */ __name((alias, selection) => {
        const self2 = this;
        const as = /* @__PURE__ */ __name((qb) => {
          if (typeof qb === "function") {
            qb = qb(new QueryBuilder(self2.dialect));
          }
          return new Proxy(
            new WithSubquery(
              qb.getSQL(),
              selection ?? ("getSelectedFields" in qb ? qb.getSelectedFields() ?? {} : {}),
              alias,
              true
            ),
            new SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
          );
        }, "as");
        return { as };
      }, "$with");
      $count(source, filters) {
        return new SQLiteCountBuilder({ source, filters, session: this.session });
      }
      /**
       * Incorporates a previously defined CTE (using `$with`) into the main query.
       *
       * This method allows the main query to reference a temporary named result set.
       *
       * See docs: {@link https://orm.drizzle.team/docs/select#with-clause}
       *
       * @param queries The CTEs to incorporate into the main query.
       *
       * @example
       *
       * ```ts
       * // Define a subquery 'sq' as a CTE using $with
       * const sq = db.$with('sq').as(db.select().from(users).where(eq(users.id, 42)));
       *
       * // Incorporate the CTE 'sq' into the main query and select from it
       * const result = await db.with(sq).select().from(sq);
       * ```
       */
      with(...queries) {
        const self2 = this;
        function select(fields) {
          return new SQLiteSelectBuilder({
            fields: fields ?? void 0,
            session: self2.session,
            dialect: self2.dialect,
            withList: queries
          });
        }
        __name(select, "select");
        function selectDistinct(fields) {
          return new SQLiteSelectBuilder({
            fields: fields ?? void 0,
            session: self2.session,
            dialect: self2.dialect,
            withList: queries,
            distinct: true
          });
        }
        __name(selectDistinct, "selectDistinct");
        function update(table) {
          return new SQLiteUpdateBuilder(table, self2.session, self2.dialect, queries);
        }
        __name(update, "update");
        function insert(into) {
          return new SQLiteInsertBuilder(into, self2.session, self2.dialect, queries);
        }
        __name(insert, "insert");
        function delete_(from) {
          return new SQLiteDeleteBase(from, self2.session, self2.dialect, queries);
        }
        __name(delete_, "delete_");
        return { select, selectDistinct, update, insert, delete: delete_ };
      }
      select(fields) {
        return new SQLiteSelectBuilder({ fields: fields ?? void 0, session: this.session, dialect: this.dialect });
      }
      selectDistinct(fields) {
        return new SQLiteSelectBuilder({
          fields: fields ?? void 0,
          session: this.session,
          dialect: this.dialect,
          distinct: true
        });
      }
      /**
       * Creates an update query.
       *
       * Calling this method without `.where()` clause will update all rows in a table. The `.where()` clause specifies which rows should be updated.
       *
       * Use `.set()` method to specify which values to update.
       *
       * See docs: {@link https://orm.drizzle.team/docs/update}
       *
       * @param table The table to update.
       *
       * @example
       *
       * ```ts
       * // Update all rows in the 'cars' table
       * await db.update(cars).set({ color: 'red' });
       *
       * // Update rows with filters and conditions
       * await db.update(cars).set({ color: 'red' }).where(eq(cars.brand, 'BMW'));
       *
       * // Update with returning clause
       * const updatedCar: Car[] = await db.update(cars)
       *   .set({ color: 'red' })
       *   .where(eq(cars.id, 1))
       *   .returning();
       * ```
       */
      update(table) {
        return new SQLiteUpdateBuilder(table, this.session, this.dialect);
      }
      /**
       * Creates an insert query.
       *
       * Calling this method will create new rows in a table. Use `.values()` method to specify which values to insert.
       *
       * See docs: {@link https://orm.drizzle.team/docs/insert}
       *
       * @param table The table to insert into.
       *
       * @example
       *
       * ```ts
       * // Insert one row
       * await db.insert(cars).values({ brand: 'BMW' });
       *
       * // Insert multiple rows
       * await db.insert(cars).values([{ brand: 'BMW' }, { brand: 'Porsche' }]);
       *
       * // Insert with returning clause
       * const insertedCar: Car[] = await db.insert(cars)
       *   .values({ brand: 'BMW' })
       *   .returning();
       * ```
       */
      insert(into) {
        return new SQLiteInsertBuilder(into, this.session, this.dialect);
      }
      /**
       * Creates a delete query.
       *
       * Calling this method without `.where()` clause will delete all rows in a table. The `.where()` clause specifies which rows should be deleted.
       *
       * See docs: {@link https://orm.drizzle.team/docs/delete}
       *
       * @param table The table to delete from.
       *
       * @example
       *
       * ```ts
       * // Delete all rows in the 'cars' table
       * await db.delete(cars);
       *
       * // Delete rows with filters and conditions
       * await db.delete(cars).where(eq(cars.color, 'green'));
       *
       * // Delete with returning clause
       * const deletedCar: Car[] = await db.delete(cars)
       *   .where(eq(cars.id, 1))
       *   .returning();
       * ```
       */
      delete(from) {
        return new SQLiteDeleteBase(from, this.session, this.dialect);
      }
      run(query) {
        const sequel = typeof query === "string" ? sql.raw(query) : query.getSQL();
        if (this.resultKind === "async") {
          return new SQLiteRaw(
            async () => this.session.run(sequel),
            () => sequel,
            "run",
            this.dialect,
            this.session.extractRawRunValueFromBatchResult.bind(this.session)
          );
        }
        return this.session.run(sequel);
      }
      all(query) {
        const sequel = typeof query === "string" ? sql.raw(query) : query.getSQL();
        if (this.resultKind === "async") {
          return new SQLiteRaw(
            async () => this.session.all(sequel),
            () => sequel,
            "all",
            this.dialect,
            this.session.extractRawAllValueFromBatchResult.bind(this.session)
          );
        }
        return this.session.all(sequel);
      }
      get(query) {
        const sequel = typeof query === "string" ? sql.raw(query) : query.getSQL();
        if (this.resultKind === "async") {
          return new SQLiteRaw(
            async () => this.session.get(sequel),
            () => sequel,
            "get",
            this.dialect,
            this.session.extractRawGetValueFromBatchResult.bind(this.session)
          );
        }
        return this.session.get(sequel);
      }
      values(query) {
        const sequel = typeof query === "string" ? sql.raw(query) : query.getSQL();
        if (this.resultKind === "async") {
          return new SQLiteRaw(
            async () => this.session.values(sequel),
            () => sequel,
            "values",
            this.dialect,
            this.session.extractRawValuesValueFromBatchResult.bind(this.session)
          );
        }
        return this.session.values(sequel);
      }
      transaction(transaction, config) {
        return this.session.transaction(transaction, config);
      }
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/alias.js
var init_alias2 = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/alias.js"() {
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/checks.js
var CheckBuilder, Check;
var init_checks = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/checks.js"() {
    init_entity();
    CheckBuilder = class {
      static {
        __name(this, "CheckBuilder");
      }
      constructor(name, value) {
        this.name = name;
        this.value = value;
      }
      static [entityKind] = "SQLiteCheckBuilder";
      brand;
      build(table) {
        return new Check(table, this);
      }
    };
    Check = class {
      static {
        __name(this, "Check");
      }
      constructor(table, builder) {
        this.table = table;
        this.name = builder.name;
        this.value = builder.value;
      }
      static [entityKind] = "SQLiteCheck";
      name;
      value;
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/indexes.js
var IndexBuilderOn, IndexBuilder, Index;
var init_indexes = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/indexes.js"() {
    init_entity();
    IndexBuilderOn = class {
      static {
        __name(this, "IndexBuilderOn");
      }
      constructor(name, unique) {
        this.name = name;
        this.unique = unique;
      }
      static [entityKind] = "SQLiteIndexBuilderOn";
      on(...columns) {
        return new IndexBuilder(this.name, columns, this.unique);
      }
    };
    IndexBuilder = class {
      static {
        __name(this, "IndexBuilder");
      }
      static [entityKind] = "SQLiteIndexBuilder";
      /** @internal */
      config;
      constructor(name, columns, unique) {
        this.config = {
          name,
          columns,
          unique,
          where: void 0
        };
      }
      /**
       * Condition for partial index.
       */
      where(condition) {
        this.config.where = condition;
        return this;
      }
      /** @internal */
      build(table) {
        return new Index(this.config, table);
      }
    };
    Index = class {
      static {
        __name(this, "Index");
      }
      static [entityKind] = "SQLiteIndex";
      config;
      constructor(config, table) {
        this.config = { ...config, table };
      }
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/primary-keys.js
var PrimaryKeyBuilder2, PrimaryKey2;
var init_primary_keys2 = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/primary-keys.js"() {
    init_entity();
    init_table3();
    PrimaryKeyBuilder2 = class {
      static {
        __name(this, "PrimaryKeyBuilder");
      }
      static [entityKind] = "SQLitePrimaryKeyBuilder";
      /** @internal */
      columns;
      /** @internal */
      name;
      constructor(columns, name) {
        this.columns = columns;
        this.name = name;
      }
      /** @internal */
      build(table) {
        return new PrimaryKey2(table, this.columns, this.name);
      }
    };
    PrimaryKey2 = class {
      static {
        __name(this, "PrimaryKey");
      }
      constructor(table, columns, name) {
        this.table = table;
        this.columns = columns;
        this.name = name;
      }
      static [entityKind] = "SQLitePrimaryKey";
      columns;
      name;
      getName() {
        return this.name ?? `${this.table[SQLiteTable.Symbol.Name]}_${this.columns.map((column) => column.name).join("_")}_pk`;
      }
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/session.js
var ExecuteResultSync, SQLitePreparedQuery, SQLiteSession, SQLiteTransaction;
var init_session = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/session.js"() {
    init_entity();
    init_errors2();
    init_query_promise();
    init_db();
    ExecuteResultSync = class extends QueryPromise {
      static {
        __name(this, "ExecuteResultSync");
      }
      constructor(resultCb) {
        super();
        this.resultCb = resultCb;
      }
      static [entityKind] = "ExecuteResultSync";
      async execute() {
        return this.resultCb();
      }
      sync() {
        return this.resultCb();
      }
    };
    SQLitePreparedQuery = class {
      static {
        __name(this, "SQLitePreparedQuery");
      }
      constructor(mode, executeMethod, query) {
        this.mode = mode;
        this.executeMethod = executeMethod;
        this.query = query;
      }
      static [entityKind] = "PreparedQuery";
      /** @internal */
      joinsNotNullableMap;
      getQuery() {
        return this.query;
      }
      mapRunResult(result, _isFromBatch) {
        return result;
      }
      mapAllResult(_result, _isFromBatch) {
        throw new Error("Not implemented");
      }
      mapGetResult(_result, _isFromBatch) {
        throw new Error("Not implemented");
      }
      execute(placeholderValues) {
        if (this.mode === "async") {
          return this[this.executeMethod](placeholderValues);
        }
        return new ExecuteResultSync(() => this[this.executeMethod](placeholderValues));
      }
      mapResult(response, isFromBatch) {
        switch (this.executeMethod) {
          case "run": {
            return this.mapRunResult(response, isFromBatch);
          }
          case "all": {
            return this.mapAllResult(response, isFromBatch);
          }
          case "get": {
            return this.mapGetResult(response, isFromBatch);
          }
        }
      }
    };
    SQLiteSession = class {
      static {
        __name(this, "SQLiteSession");
      }
      constructor(dialect) {
        this.dialect = dialect;
      }
      static [entityKind] = "SQLiteSession";
      prepareOneTimeQuery(query, fields, executeMethod, isResponseInArrayMode) {
        return this.prepareQuery(query, fields, executeMethod, isResponseInArrayMode);
      }
      run(query) {
        const staticQuery = this.dialect.sqlToQuery(query);
        try {
          return this.prepareOneTimeQuery(staticQuery, void 0, "run", false).run();
        } catch (err) {
          throw new DrizzleError({ cause: err, message: `Failed to run the query '${staticQuery.sql}'` });
        }
      }
      /** @internal */
      extractRawRunValueFromBatchResult(result) {
        return result;
      }
      all(query) {
        return this.prepareOneTimeQuery(this.dialect.sqlToQuery(query), void 0, "run", false).all();
      }
      /** @internal */
      extractRawAllValueFromBatchResult(_result) {
        throw new Error("Not implemented");
      }
      get(query) {
        return this.prepareOneTimeQuery(this.dialect.sqlToQuery(query), void 0, "run", false).get();
      }
      /** @internal */
      extractRawGetValueFromBatchResult(_result) {
        throw new Error("Not implemented");
      }
      values(query) {
        return this.prepareOneTimeQuery(this.dialect.sqlToQuery(query), void 0, "run", false).values();
      }
      async count(sql3) {
        const result = await this.values(sql3);
        return result[0][0];
      }
      /** @internal */
      extractRawValuesValueFromBatchResult(_result) {
        throw new Error("Not implemented");
      }
    };
    SQLiteTransaction = class extends BaseSQLiteDatabase {
      static {
        __name(this, "SQLiteTransaction");
      }
      constructor(resultType, dialect, session, schema, nestedIndex = 0) {
        super(resultType, dialect, session, schema);
        this.schema = schema;
        this.nestedIndex = nestedIndex;
      }
      static [entityKind] = "SQLiteTransaction";
      rollback() {
        throw new TransactionRollbackError();
      }
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/subquery.js
var init_subquery2 = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/subquery.js"() {
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/utils.js
var init_utils2 = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/utils.js"() {
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/view.js
var ViewBuilderCore, ViewBuilder, ManualViewBuilder, SQLiteView;
var init_view = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/view.js"() {
    init_entity();
    init_selection_proxy();
    init_utils();
    init_query_builder2();
    init_table3();
    init_view_base();
    ViewBuilderCore = class {
      static {
        __name(this, "ViewBuilderCore");
      }
      constructor(name) {
        this.name = name;
      }
      static [entityKind] = "SQLiteViewBuilderCore";
      config = {};
    };
    ViewBuilder = class extends ViewBuilderCore {
      static {
        __name(this, "ViewBuilder");
      }
      static [entityKind] = "SQLiteViewBuilder";
      as(qb) {
        if (typeof qb === "function") {
          qb = qb(new QueryBuilder());
        }
        const selectionProxy = new SelectionProxyHandler({
          alias: this.name,
          sqlBehavior: "error",
          sqlAliasedBehavior: "alias",
          replaceOriginalName: true
        });
        const aliasedSelectedFields = qb.getSelectedFields();
        return new Proxy(
          new SQLiteView({
            // sqliteConfig: this.config,
            config: {
              name: this.name,
              schema: void 0,
              selectedFields: aliasedSelectedFields,
              query: qb.getSQL().inlineParams()
            }
          }),
          selectionProxy
        );
      }
    };
    ManualViewBuilder = class extends ViewBuilderCore {
      static {
        __name(this, "ManualViewBuilder");
      }
      static [entityKind] = "SQLiteManualViewBuilder";
      columns;
      constructor(name, columns) {
        super(name);
        this.columns = getTableColumns(sqliteTable(name, columns));
      }
      existing() {
        return new Proxy(
          new SQLiteView({
            config: {
              name: this.name,
              schema: void 0,
              selectedFields: this.columns,
              query: void 0
            }
          }),
          new SelectionProxyHandler({
            alias: this.name,
            sqlBehavior: "error",
            sqlAliasedBehavior: "alias",
            replaceOriginalName: true
          })
        );
      }
      as(query) {
        return new Proxy(
          new SQLiteView({
            config: {
              name: this.name,
              schema: void 0,
              selectedFields: this.columns,
              query: query.inlineParams()
            }
          }),
          new SelectionProxyHandler({
            alias: this.name,
            sqlBehavior: "error",
            sqlAliasedBehavior: "alias",
            replaceOriginalName: true
          })
        );
      }
    };
    SQLiteView = class extends SQLiteViewBase {
      static {
        __name(this, "SQLiteView");
      }
      static [entityKind] = "SQLiteView";
      constructor({ config }) {
        super(config);
      }
    };
  }
});

// worker/node_modules/drizzle-orm/sqlite-core/index.js
var init_sqlite_core = __esm({
  "worker/node_modules/drizzle-orm/sqlite-core/index.js"() {
    init_alias2();
    init_checks();
    init_columns();
    init_db();
    init_dialect();
    init_foreign_keys2();
    init_indexes();
    init_primary_keys2();
    init_query_builders();
    init_session();
    init_subquery2();
    init_table3();
    init_unique_constraint2();
    init_utils2();
    init_view();
  }
});

// worker/node_modules/drizzle-orm/libsql/session.js
function normalizeRow(obj) {
  return Object.keys(obj).reduce((acc, key) => {
    if (Object.prototype.propertyIsEnumerable.call(obj, key)) {
      acc[key] = obj[key];
    }
    return acc;
  }, {});
}
function normalizeFieldValue(value) {
  if (typeof ArrayBuffer !== "undefined" && value instanceof ArrayBuffer) {
    if (typeof Buffer !== "undefined") {
      if (!(value instanceof Buffer)) {
        return Buffer.from(value);
      }
      return value;
    }
    if (typeof TextDecoder !== "undefined") {
      return new TextDecoder().decode(value);
    }
    throw new Error("TextDecoder is not available. Please provide either Buffer or TextDecoder polyfill.");
  }
  return value;
}
var LibSQLSession, LibSQLTransaction, LibSQLPreparedQuery;
var init_session2 = __esm({
  "worker/node_modules/drizzle-orm/libsql/session.js"() {
    init_entity();
    init_logger();
    init_sql2();
    init_sqlite_core();
    init_session();
    init_utils();
    LibSQLSession = class _LibSQLSession extends SQLiteSession {
      static {
        __name(this, "LibSQLSession");
      }
      constructor(client, dialect, schema, options, tx) {
        super(dialect);
        this.client = client;
        this.schema = schema;
        this.options = options;
        this.tx = tx;
        this.logger = options.logger ?? new NoopLogger();
      }
      static [entityKind] = "LibSQLSession";
      logger;
      prepareQuery(query, fields, executeMethod, isResponseInArrayMode, customResultMapper) {
        return new LibSQLPreparedQuery(
          this.client,
          query,
          this.logger,
          fields,
          this.tx,
          executeMethod,
          isResponseInArrayMode,
          customResultMapper
        );
      }
      async batch(queries) {
        const preparedQueries = [];
        const builtQueries = [];
        for (const query of queries) {
          const preparedQuery = query._prepare();
          const builtQuery = preparedQuery.getQuery();
          preparedQueries.push(preparedQuery);
          builtQueries.push({ sql: builtQuery.sql, args: builtQuery.params });
        }
        const batchResults = await this.client.batch(builtQueries);
        return batchResults.map((result, i) => preparedQueries[i].mapResult(result, true));
      }
      async migrate(queries) {
        const preparedQueries = [];
        const builtQueries = [];
        for (const query of queries) {
          const preparedQuery = query._prepare();
          const builtQuery = preparedQuery.getQuery();
          preparedQueries.push(preparedQuery);
          builtQueries.push({ sql: builtQuery.sql, args: builtQuery.params });
        }
        const batchResults = await this.client.migrate(builtQueries);
        return batchResults.map((result, i) => preparedQueries[i].mapResult(result, true));
      }
      async transaction(transaction, _config) {
        const libsqlTx = await this.client.transaction();
        const session = new _LibSQLSession(
          this.client,
          this.dialect,
          this.schema,
          this.options,
          libsqlTx
        );
        const tx = new LibSQLTransaction("async", this.dialect, session, this.schema);
        try {
          const result = await transaction(tx);
          await libsqlTx.commit();
          return result;
        } catch (err) {
          await libsqlTx.rollback();
          throw err;
        }
      }
      extractRawAllValueFromBatchResult(result) {
        return result.rows;
      }
      extractRawGetValueFromBatchResult(result) {
        return result.rows[0];
      }
      extractRawValuesValueFromBatchResult(result) {
        return result.rows;
      }
    };
    LibSQLTransaction = class _LibSQLTransaction extends SQLiteTransaction {
      static {
        __name(this, "LibSQLTransaction");
      }
      static [entityKind] = "LibSQLTransaction";
      async transaction(transaction) {
        const savepointName = `sp${this.nestedIndex}`;
        const tx = new _LibSQLTransaction("async", this.dialect, this.session, this.schema, this.nestedIndex + 1);
        await this.session.run(sql.raw(`savepoint ${savepointName}`));
        try {
          const result = await transaction(tx);
          await this.session.run(sql.raw(`release savepoint ${savepointName}`));
          return result;
        } catch (err) {
          await this.session.run(sql.raw(`rollback to savepoint ${savepointName}`));
          throw err;
        }
      }
    };
    LibSQLPreparedQuery = class extends SQLitePreparedQuery {
      static {
        __name(this, "LibSQLPreparedQuery");
      }
      constructor(client, query, logger2, fields, tx, executeMethod, _isResponseInArrayMode, customResultMapper) {
        super("async", executeMethod, query);
        this.client = client;
        this.logger = logger2;
        this.fields = fields;
        this.tx = tx;
        this._isResponseInArrayMode = _isResponseInArrayMode;
        this.customResultMapper = customResultMapper;
        this.customResultMapper = customResultMapper;
        this.fields = fields;
      }
      static [entityKind] = "LibSQLPreparedQuery";
      run(placeholderValues) {
        const params = fillPlaceholders(this.query.params, placeholderValues ?? {});
        this.logger.logQuery(this.query.sql, params);
        const stmt = { sql: this.query.sql, args: params };
        return this.tx ? this.tx.execute(stmt) : this.client.execute(stmt);
      }
      async all(placeholderValues) {
        const { fields, logger: logger2, query, tx, client, customResultMapper } = this;
        if (!fields && !customResultMapper) {
          const params = fillPlaceholders(query.params, placeholderValues ?? {});
          logger2.logQuery(query.sql, params);
          const stmt = { sql: query.sql, args: params };
          return (tx ? tx.execute(stmt) : client.execute(stmt)).then(({ rows: rows2 }) => this.mapAllResult(rows2));
        }
        const rows = await this.values(placeholderValues);
        return this.mapAllResult(rows);
      }
      mapAllResult(rows, isFromBatch) {
        if (isFromBatch) {
          rows = rows.rows;
        }
        if (!this.fields && !this.customResultMapper) {
          return rows.map((row) => normalizeRow(row));
        }
        if (this.customResultMapper) {
          return this.customResultMapper(rows, normalizeFieldValue);
        }
        return rows.map((row) => {
          return mapResultRow(
            this.fields,
            Array.prototype.slice.call(row).map((v) => normalizeFieldValue(v)),
            this.joinsNotNullableMap
          );
        });
      }
      async get(placeholderValues) {
        const { fields, logger: logger2, query, tx, client, customResultMapper } = this;
        if (!fields && !customResultMapper) {
          const params = fillPlaceholders(query.params, placeholderValues ?? {});
          logger2.logQuery(query.sql, params);
          const stmt = { sql: query.sql, args: params };
          return (tx ? tx.execute(stmt) : client.execute(stmt)).then(({ rows: rows2 }) => this.mapGetResult(rows2));
        }
        const rows = await this.values(placeholderValues);
        return this.mapGetResult(rows);
      }
      mapGetResult(rows, isFromBatch) {
        if (isFromBatch) {
          rows = rows.rows;
        }
        const row = rows[0];
        if (!this.fields && !this.customResultMapper) {
          return normalizeRow(row);
        }
        if (!row) {
          return void 0;
        }
        if (this.customResultMapper) {
          return this.customResultMapper(rows, normalizeFieldValue);
        }
        return mapResultRow(
          this.fields,
          Array.prototype.slice.call(row).map((v) => normalizeFieldValue(v)),
          this.joinsNotNullableMap
        );
      }
      values(placeholderValues) {
        const params = fillPlaceholders(this.query.params, placeholderValues ?? {});
        this.logger.logQuery(this.query.sql, params);
        const stmt = { sql: this.query.sql, args: params };
        return (this.tx ? this.tx.execute(stmt) : this.client.execute(stmt)).then(({ rows }) => rows);
      }
      /** @internal */
      isResponseInArrayMode() {
        return this._isResponseInArrayMode;
      }
    };
    __name(normalizeRow, "normalizeRow");
    __name(normalizeFieldValue, "normalizeFieldValue");
  }
});

// worker/node_modules/drizzle-orm/libsql/driver-core.js
function construct(client, config = {}) {
  const dialect = new SQLiteAsyncDialect({ casing: config.casing });
  let logger2;
  if (config.logger === true) {
    logger2 = new DefaultLogger();
  } else if (config.logger !== false) {
    logger2 = config.logger;
  }
  let schema;
  if (config.schema) {
    const tablesConfig = extractTablesRelationalConfig(
      config.schema,
      createTableRelationsHelpers
    );
    schema = {
      fullSchema: config.schema,
      schema: tablesConfig.tables,
      tableNamesMap: tablesConfig.tableNamesMap
    };
  }
  const session = new LibSQLSession(client, dialect, schema, { logger: logger2 }, void 0);
  const db = new LibSQLDatabase("async", dialect, session, schema);
  db.$client = client;
  return db;
}
var LibSQLDatabase;
var init_driver_core = __esm({
  "worker/node_modules/drizzle-orm/libsql/driver-core.js"() {
    init_entity();
    init_logger();
    init_relations();
    init_db();
    init_dialect();
    init_session2();
    LibSQLDatabase = class extends BaseSQLiteDatabase {
      static {
        __name(this, "LibSQLDatabase");
      }
      static [entityKind] = "LibSQLDatabase";
      async batch(batch) {
        return this.session.batch(batch);
      }
    };
    __name(construct, "construct");
  }
});

// worker/node_modules/drizzle-orm/libsql/driver.js
function drizzle(...params) {
  if (typeof params[0] === "string") {
    const instance = createClient({
      url: params[0]
    });
    return construct(instance, params[1]);
  }
  if (isConfig(params[0])) {
    const { connection, client, ...drizzleConfig } = params[0];
    if (client)
      return construct(client, drizzleConfig);
    const instance = typeof connection === "string" ? createClient({ url: connection }) : createClient(connection);
    return construct(instance, drizzleConfig);
  }
  return construct(params[0], params[1]);
}
var init_driver = __esm({
  "worker/node_modules/drizzle-orm/libsql/driver.js"() {
    init_web2();
    init_utils();
    init_driver_core();
    __name(drizzle, "drizzle");
    ((drizzle2) => {
      function mock(config) {
        return construct({}, config);
      }
      __name(mock, "mock");
      drizzle2.mock = mock;
    })(drizzle || (drizzle = {}));
  }
});

// worker/node_modules/drizzle-orm/libsql/index.js
var init_libsql = __esm({
  "worker/node_modules/drizzle-orm/libsql/index.js"() {
    init_driver();
    init_session2();
  }
});

// node_modules/drizzle-orm/entity.js
function is2(value, type) {
  if (!value || typeof value !== "object") {
    return false;
  }
  if (value instanceof type) {
    return true;
  }
  if (!Object.prototype.hasOwnProperty.call(type, entityKind2)) {
    throw new Error(
      `Class "${type.name ?? "<unknown>"}" doesn't look like a Drizzle entity. If this is incorrect and the class is provided by Drizzle, please report this as a bug.`
    );
  }
  let cls = Object.getPrototypeOf(value).constructor;
  if (cls) {
    while (cls) {
      if (entityKind2 in cls && cls[entityKind2] === type[entityKind2]) {
        return true;
      }
      cls = Object.getPrototypeOf(cls);
    }
  }
  return false;
}
var entityKind2, hasOwnEntityKind2;
var init_entity2 = __esm({
  "node_modules/drizzle-orm/entity.js"() {
    entityKind2 = Symbol.for("drizzle:entityKind");
    hasOwnEntityKind2 = Symbol.for("drizzle:hasOwnEntityKind");
    __name(is2, "is");
  }
});

// node_modules/drizzle-orm/column.js
var Column2;
var init_column2 = __esm({
  "node_modules/drizzle-orm/column.js"() {
    init_entity2();
    Column2 = class {
      static {
        __name(this, "Column");
      }
      constructor(table, config) {
        this.table = table;
        this.config = config;
        this.name = config.name;
        this.keyAsName = config.keyAsName;
        this.notNull = config.notNull;
        this.default = config.default;
        this.defaultFn = config.defaultFn;
        this.onUpdateFn = config.onUpdateFn;
        this.hasDefault = config.hasDefault;
        this.primary = config.primaryKey;
        this.isUnique = config.isUnique;
        this.uniqueName = config.uniqueName;
        this.uniqueType = config.uniqueType;
        this.dataType = config.dataType;
        this.columnType = config.columnType;
        this.generated = config.generated;
        this.generatedIdentity = config.generatedIdentity;
      }
      static [entityKind2] = "Column";
      name;
      keyAsName;
      primary;
      notNull;
      default;
      defaultFn;
      onUpdateFn;
      hasDefault;
      isUnique;
      uniqueName;
      uniqueType;
      dataType;
      columnType;
      enumValues = void 0;
      generated = void 0;
      generatedIdentity = void 0;
      config;
      mapFromDriverValue(value) {
        return value;
      }
      mapToDriverValue(value) {
        return value;
      }
      // ** @internal */
      shouldDisableInsert() {
        return this.config.generated !== void 0 && this.config.generated.type !== "byDefault";
      }
    };
  }
});

// node_modules/drizzle-orm/column-builder.js
var ColumnBuilder2;
var init_column_builder2 = __esm({
  "node_modules/drizzle-orm/column-builder.js"() {
    init_entity2();
    ColumnBuilder2 = class {
      static {
        __name(this, "ColumnBuilder");
      }
      static [entityKind2] = "ColumnBuilder";
      config;
      constructor(name, dataType, columnType) {
        this.config = {
          name,
          keyAsName: name === "",
          notNull: false,
          default: void 0,
          hasDefault: false,
          primaryKey: false,
          isUnique: false,
          uniqueName: void 0,
          uniqueType: void 0,
          dataType,
          columnType,
          generated: void 0
        };
      }
      /**
       * Changes the data type of the column. Commonly used with `json` columns. Also, useful for branded types.
       *
       * @example
       * ```ts
       * const users = pgTable('users', {
       * 	id: integer('id').$type<UserId>().primaryKey(),
       * 	details: json('details').$type<UserDetails>().notNull(),
       * });
       * ```
       */
      $type() {
        return this;
      }
      /**
       * Adds a `not null` clause to the column definition.
       *
       * Affects the `select` model of the table - columns *without* `not null` will be nullable on select.
       */
      notNull() {
        this.config.notNull = true;
        return this;
      }
      /**
       * Adds a `default <value>` clause to the column definition.
       *
       * Affects the `insert` model of the table - columns *with* `default` are optional on insert.
       *
       * If you need to set a dynamic default value, use {@link $defaultFn} instead.
       */
      default(value) {
        this.config.default = value;
        this.config.hasDefault = true;
        return this;
      }
      /**
       * Adds a dynamic default value to the column.
       * The function will be called when the row is inserted, and the returned value will be used as the column value.
       *
       * **Note:** This value does not affect the `drizzle-kit` behavior, it is only used at runtime in `drizzle-orm`.
       */
      $defaultFn(fn) {
        this.config.defaultFn = fn;
        this.config.hasDefault = true;
        return this;
      }
      /**
       * Alias for {@link $defaultFn}.
       */
      $default = this.$defaultFn;
      /**
       * Adds a dynamic update value to the column.
       * The function will be called when the row is updated, and the returned value will be used as the column value if none is provided.
       * If no `default` (or `$defaultFn`) value is provided, the function will be called when the row is inserted as well, and the returned value will be used as the column value.
       *
       * **Note:** This value does not affect the `drizzle-kit` behavior, it is only used at runtime in `drizzle-orm`.
       */
      $onUpdateFn(fn) {
        this.config.onUpdateFn = fn;
        this.config.hasDefault = true;
        return this;
      }
      /**
       * Alias for {@link $onUpdateFn}.
       */
      $onUpdate = this.$onUpdateFn;
      /**
       * Adds a `primary key` clause to the column definition. This implicitly makes the column `not null`.
       *
       * In SQLite, `integer primary key` implicitly makes the column auto-incrementing.
       */
      primaryKey() {
        this.config.primaryKey = true;
        this.config.notNull = true;
        return this;
      }
      /** @internal Sets the name of the column to the key within the table definition if a name was not given. */
      setName(name) {
        if (this.config.name !== "")
          return;
        this.config.name = name;
      }
    };
  }
});

// node_modules/drizzle-orm/table.utils.js
var TableName2;
var init_table_utils2 = __esm({
  "node_modules/drizzle-orm/table.utils.js"() {
    TableName2 = Symbol.for("drizzle:Name");
  }
});

// node_modules/drizzle-orm/pg-core/foreign-keys.js
var ForeignKeyBuilder3, ForeignKey3;
var init_foreign_keys3 = __esm({
  "node_modules/drizzle-orm/pg-core/foreign-keys.js"() {
    init_entity2();
    init_table_utils2();
    ForeignKeyBuilder3 = class {
      static {
        __name(this, "ForeignKeyBuilder");
      }
      static [entityKind2] = "PgForeignKeyBuilder";
      /** @internal */
      reference;
      /** @internal */
      _onUpdate = "no action";
      /** @internal */
      _onDelete = "no action";
      constructor(config, actions) {
        this.reference = () => {
          const { name, columns, foreignColumns } = config();
          return { name, columns, foreignTable: foreignColumns[0].table, foreignColumns };
        };
        if (actions) {
          this._onUpdate = actions.onUpdate;
          this._onDelete = actions.onDelete;
        }
      }
      onUpdate(action) {
        this._onUpdate = action === void 0 ? "no action" : action;
        return this;
      }
      onDelete(action) {
        this._onDelete = action === void 0 ? "no action" : action;
        return this;
      }
      /** @internal */
      build(table) {
        return new ForeignKey3(table, this);
      }
    };
    ForeignKey3 = class {
      static {
        __name(this, "ForeignKey");
      }
      constructor(table, builder) {
        this.table = table;
        this.reference = builder.reference;
        this.onUpdate = builder._onUpdate;
        this.onDelete = builder._onDelete;
      }
      static [entityKind2] = "PgForeignKey";
      reference;
      onUpdate;
      onDelete;
      getName() {
        const { name, columns, foreignColumns } = this.reference();
        const columnNames = columns.map((column) => column.name);
        const foreignColumnNames = foreignColumns.map((column) => column.name);
        const chunks = [
          this.table[TableName2],
          ...columnNames,
          foreignColumns[0].table[TableName2],
          ...foreignColumnNames
        ];
        return name ?? `${chunks.join("_")}_fk`;
      }
    };
  }
});

// node_modules/drizzle-orm/tracing-utils.js
function iife2(fn, ...args) {
  return fn(...args);
}
var init_tracing_utils2 = __esm({
  "node_modules/drizzle-orm/tracing-utils.js"() {
    __name(iife2, "iife");
  }
});

// node_modules/drizzle-orm/pg-core/unique-constraint.js
function uniqueKeyName3(table, columns) {
  return `${table[TableName2]}_${columns.join("_")}_unique`;
}
var UniqueConstraintBuilder3, UniqueOnConstraintBuilder3, UniqueConstraint3;
var init_unique_constraint3 = __esm({
  "node_modules/drizzle-orm/pg-core/unique-constraint.js"() {
    init_entity2();
    init_table_utils2();
    __name(uniqueKeyName3, "uniqueKeyName");
    UniqueConstraintBuilder3 = class {
      static {
        __name(this, "UniqueConstraintBuilder");
      }
      constructor(columns, name) {
        this.name = name;
        this.columns = columns;
      }
      static [entityKind2] = "PgUniqueConstraintBuilder";
      /** @internal */
      columns;
      /** @internal */
      nullsNotDistinctConfig = false;
      nullsNotDistinct() {
        this.nullsNotDistinctConfig = true;
        return this;
      }
      /** @internal */
      build(table) {
        return new UniqueConstraint3(table, this.columns, this.nullsNotDistinctConfig, this.name);
      }
    };
    UniqueOnConstraintBuilder3 = class {
      static {
        __name(this, "UniqueOnConstraintBuilder");
      }
      static [entityKind2] = "PgUniqueOnConstraintBuilder";
      /** @internal */
      name;
      constructor(name) {
        this.name = name;
      }
      on(...columns) {
        return new UniqueConstraintBuilder3(columns, this.name);
      }
    };
    UniqueConstraint3 = class {
      static {
        __name(this, "UniqueConstraint");
      }
      constructor(table, columns, nullsNotDistinct, name) {
        this.table = table;
        this.columns = columns;
        this.name = name ?? uniqueKeyName3(this.table, this.columns.map((column) => column.name));
        this.nullsNotDistinct = nullsNotDistinct;
      }
      static [entityKind2] = "PgUniqueConstraint";
      columns;
      name;
      nullsNotDistinct = false;
      getName() {
        return this.name;
      }
    };
  }
});

// node_modules/drizzle-orm/pg-core/utils/array.js
function parsePgArrayValue2(arrayString, startFrom, inQuotes) {
  for (let i = startFrom; i < arrayString.length; i++) {
    const char = arrayString[i];
    if (char === "\\") {
      i++;
      continue;
    }
    if (char === '"') {
      return [arrayString.slice(startFrom, i).replace(/\\/g, ""), i + 1];
    }
    if (inQuotes) {
      continue;
    }
    if (char === "," || char === "}") {
      return [arrayString.slice(startFrom, i).replace(/\\/g, ""), i];
    }
  }
  return [arrayString.slice(startFrom).replace(/\\/g, ""), arrayString.length];
}
function parsePgNestedArray2(arrayString, startFrom = 0) {
  const result = [];
  let i = startFrom;
  let lastCharIsComma = false;
  while (i < arrayString.length) {
    const char = arrayString[i];
    if (char === ",") {
      if (lastCharIsComma || i === startFrom) {
        result.push("");
      }
      lastCharIsComma = true;
      i++;
      continue;
    }
    lastCharIsComma = false;
    if (char === "\\") {
      i += 2;
      continue;
    }
    if (char === '"') {
      const [value2, startFrom2] = parsePgArrayValue2(arrayString, i + 1, true);
      result.push(value2);
      i = startFrom2;
      continue;
    }
    if (char === "}") {
      return [result, i + 1];
    }
    if (char === "{") {
      const [value2, startFrom2] = parsePgNestedArray2(arrayString, i + 1);
      result.push(value2);
      i = startFrom2;
      continue;
    }
    const [value, newStartFrom] = parsePgArrayValue2(arrayString, i, false);
    result.push(value);
    i = newStartFrom;
  }
  return [result, i];
}
function parsePgArray2(arrayString) {
  const [result] = parsePgNestedArray2(arrayString, 1);
  return result;
}
function makePgArray2(array3) {
  return `{${array3.map((item) => {
    if (Array.isArray(item)) {
      return makePgArray2(item);
    }
    if (typeof item === "string") {
      return `"${item.replace(/\\/g, "\\\\").replace(/"/g, '\\"')}"`;
    }
    return `${item}`;
  }).join(",")}}`;
}
var init_array2 = __esm({
  "node_modules/drizzle-orm/pg-core/utils/array.js"() {
    __name(parsePgArrayValue2, "parsePgArrayValue");
    __name(parsePgNestedArray2, "parsePgNestedArray");
    __name(parsePgArray2, "parsePgArray");
    __name(makePgArray2, "makePgArray");
  }
});

// node_modules/drizzle-orm/pg-core/columns/common.js
var PgColumnBuilder2, PgColumn2, ExtraConfigColumn2, IndexedColumn2, PgArrayBuilder2, PgArray2;
var init_common3 = __esm({
  "node_modules/drizzle-orm/pg-core/columns/common.js"() {
    init_column_builder2();
    init_column2();
    init_entity2();
    init_foreign_keys3();
    init_tracing_utils2();
    init_unique_constraint3();
    init_array2();
    PgColumnBuilder2 = class extends ColumnBuilder2 {
      static {
        __name(this, "PgColumnBuilder");
      }
      foreignKeyConfigs = [];
      static [entityKind2] = "PgColumnBuilder";
      array(size) {
        return new PgArrayBuilder2(this.config.name, this, size);
      }
      references(ref, actions = {}) {
        this.foreignKeyConfigs.push({ ref, actions });
        return this;
      }
      unique(name, config) {
        this.config.isUnique = true;
        this.config.uniqueName = name;
        this.config.uniqueType = config?.nulls;
        return this;
      }
      generatedAlwaysAs(as) {
        this.config.generated = {
          as,
          type: "always",
          mode: "stored"
        };
        return this;
      }
      /** @internal */
      buildForeignKeys(column, table) {
        return this.foreignKeyConfigs.map(({ ref, actions }) => {
          return iife2(
            (ref2, actions2) => {
              const builder = new ForeignKeyBuilder3(() => {
                const foreignColumn = ref2();
                return { columns: [column], foreignColumns: [foreignColumn] };
              });
              if (actions2.onUpdate) {
                builder.onUpdate(actions2.onUpdate);
              }
              if (actions2.onDelete) {
                builder.onDelete(actions2.onDelete);
              }
              return builder.build(table);
            },
            ref,
            actions
          );
        });
      }
      /** @internal */
      buildExtraConfigColumn(table) {
        return new ExtraConfigColumn2(table, this.config);
      }
    };
    PgColumn2 = class extends Column2 {
      static {
        __name(this, "PgColumn");
      }
      constructor(table, config) {
        if (!config.uniqueName) {
          config.uniqueName = uniqueKeyName3(table, [config.name]);
        }
        super(table, config);
        this.table = table;
      }
      static [entityKind2] = "PgColumn";
    };
    ExtraConfigColumn2 = class extends PgColumn2 {
      static {
        __name(this, "ExtraConfigColumn");
      }
      static [entityKind2] = "ExtraConfigColumn";
      getSQLType() {
        return this.getSQLType();
      }
      indexConfig = {
        order: this.config.order ?? "asc",
        nulls: this.config.nulls ?? "last",
        opClass: this.config.opClass
      };
      defaultConfig = {
        order: "asc",
        nulls: "last",
        opClass: void 0
      };
      asc() {
        this.indexConfig.order = "asc";
        return this;
      }
      desc() {
        this.indexConfig.order = "desc";
        return this;
      }
      nullsFirst() {
        this.indexConfig.nulls = "first";
        return this;
      }
      nullsLast() {
        this.indexConfig.nulls = "last";
        return this;
      }
      /**
       * ### PostgreSQL documentation quote
       *
       * > An operator class with optional parameters can be specified for each column of an index.
       * The operator class identifies the operators to be used by the index for that column.
       * For example, a B-tree index on four-byte integers would use the int4_ops class;
       * this operator class includes comparison functions for four-byte integers.
       * In practice the default operator class for the column's data type is usually sufficient.
       * The main point of having operator classes is that for some data types, there could be more than one meaningful ordering.
       * For example, we might want to sort a complex-number data type either by absolute value or by real part.
       * We could do this by defining two operator classes for the data type and then selecting the proper class when creating an index.
       * More information about operator classes check:
       *
       * ### Useful links
       * https://www.postgresql.org/docs/current/sql-createindex.html
       *
       * https://www.postgresql.org/docs/current/indexes-opclass.html
       *
       * https://www.postgresql.org/docs/current/xindex.html
       *
       * ### Additional types
       * If you have the `pg_vector` extension installed in your database, you can use the
       * `vector_l2_ops`, `vector_ip_ops`, `vector_cosine_ops`, `vector_l1_ops`, `bit_hamming_ops`, `bit_jaccard_ops`, `halfvec_l2_ops`, `sparsevec_l2_ops` options, which are predefined types.
       *
       * **You can always specify any string you want in the operator class, in case Drizzle doesn't have it natively in its types**
       *
       * @param opClass
       * @returns
       */
      op(opClass) {
        this.indexConfig.opClass = opClass;
        return this;
      }
    };
    IndexedColumn2 = class {
      static {
        __name(this, "IndexedColumn");
      }
      static [entityKind2] = "IndexedColumn";
      constructor(name, keyAsName, type, indexConfig) {
        this.name = name;
        this.keyAsName = keyAsName;
        this.type = type;
        this.indexConfig = indexConfig;
      }
      name;
      keyAsName;
      type;
      indexConfig;
    };
    PgArrayBuilder2 = class extends PgColumnBuilder2 {
      static {
        __name(this, "PgArrayBuilder");
      }
      static [entityKind2] = "PgArrayBuilder";
      constructor(name, baseBuilder, size) {
        super(name, "array", "PgArray");
        this.config.baseBuilder = baseBuilder;
        this.config.size = size;
      }
      /** @internal */
      build(table) {
        const baseColumn = this.config.baseBuilder.build(table);
        return new PgArray2(
          table,
          this.config,
          baseColumn
        );
      }
    };
    PgArray2 = class _PgArray extends PgColumn2 {
      static {
        __name(this, "PgArray");
      }
      constructor(table, config, baseColumn, range) {
        super(table, config);
        this.baseColumn = baseColumn;
        this.range = range;
        this.size = config.size;
      }
      size;
      static [entityKind2] = "PgArray";
      getSQLType() {
        return `${this.baseColumn.getSQLType()}[${typeof this.size === "number" ? this.size : ""}]`;
      }
      mapFromDriverValue(value) {
        if (typeof value === "string") {
          value = parsePgArray2(value);
        }
        return value.map((v) => this.baseColumn.mapFromDriverValue(v));
      }
      mapToDriverValue(value, isNestedArray = false) {
        const a = value.map(
          (v) => v === null ? null : is2(this.baseColumn, _PgArray) ? this.baseColumn.mapToDriverValue(v, true) : this.baseColumn.mapToDriverValue(v)
        );
        if (isNestedArray)
          return a;
        return makePgArray2(a);
      }
    };
  }
});

// node_modules/drizzle-orm/pg-core/columns/enum.js
function isPgEnum2(obj) {
  return !!obj && typeof obj === "function" && isPgEnumSym2 in obj && obj[isPgEnumSym2] === true;
}
var isPgEnumSym2, PgEnumColumnBuilder2, PgEnumColumn2;
var init_enum2 = __esm({
  "node_modules/drizzle-orm/pg-core/columns/enum.js"() {
    init_entity2();
    init_common3();
    isPgEnumSym2 = Symbol.for("drizzle:isPgEnum");
    __name(isPgEnum2, "isPgEnum");
    PgEnumColumnBuilder2 = class extends PgColumnBuilder2 {
      static {
        __name(this, "PgEnumColumnBuilder");
      }
      static [entityKind2] = "PgEnumColumnBuilder";
      constructor(name, enumInstance) {
        super(name, "string", "PgEnumColumn");
        this.config.enum = enumInstance;
      }
      /** @internal */
      build(table) {
        return new PgEnumColumn2(
          table,
          this.config
        );
      }
    };
    PgEnumColumn2 = class extends PgColumn2 {
      static {
        __name(this, "PgEnumColumn");
      }
      static [entityKind2] = "PgEnumColumn";
      enum = this.config.enum;
      enumValues = this.config.enum.enumValues;
      constructor(table, config) {
        super(table, config);
        this.enum = config.enum;
      }
      getSQLType() {
        return this.enum.enumName;
      }
    };
  }
});

// node_modules/drizzle-orm/subquery.js
var Subquery2, WithSubquery2;
var init_subquery3 = __esm({
  "node_modules/drizzle-orm/subquery.js"() {
    init_entity2();
    Subquery2 = class {
      static {
        __name(this, "Subquery");
      }
      static [entityKind2] = "Subquery";
      constructor(sql3, selection, alias, isWith = false) {
        this._ = {
          brand: "Subquery",
          sql: sql3,
          selectedFields: selection,
          alias,
          isWith
        };
      }
      // getSQL(): SQL<unknown> {
      // 	return new SQL([this]);
      // }
    };
    WithSubquery2 = class extends Subquery2 {
      static {
        __name(this, "WithSubquery");
      }
      static [entityKind2] = "WithSubquery";
    };
  }
});

// node_modules/drizzle-orm/version.js
var version3;
var init_version2 = __esm({
  "node_modules/drizzle-orm/version.js"() {
    version3 = "0.39.1";
  }
});

// node_modules/drizzle-orm/tracing.js
var otel2, rawTracer2, tracer2;
var init_tracing2 = __esm({
  "node_modules/drizzle-orm/tracing.js"() {
    init_tracing_utils2();
    init_version2();
    tracer2 = {
      startActiveSpan(name, fn) {
        if (!otel2) {
          return fn();
        }
        if (!rawTracer2) {
          rawTracer2 = otel2.trace.getTracer("drizzle-orm", version3);
        }
        return iife2(
          (otel22, rawTracer22) => rawTracer22.startActiveSpan(
            name,
            (span) => {
              try {
                return fn(span);
              } catch (e) {
                span.setStatus({
                  code: otel22.SpanStatusCode.ERROR,
                  message: e instanceof Error ? e.message : "Unknown error"
                  // eslint-disable-line no-instanceof/no-instanceof
                });
                throw e;
              } finally {
                span.end();
              }
            }
          ),
          otel2,
          rawTracer2
        );
      }
    };
  }
});

// node_modules/drizzle-orm/view-common.js
var ViewBaseConfig2;
var init_view_common2 = __esm({
  "node_modules/drizzle-orm/view-common.js"() {
    ViewBaseConfig2 = Symbol.for("drizzle:ViewBaseConfig");
  }
});

// node_modules/drizzle-orm/table.js
function isTable(table) {
  return typeof table === "object" && table !== null && IsDrizzleTable2 in table;
}
function getTableName2(table) {
  return table[TableName2];
}
function getTableUniqueName2(table) {
  return `${table[Schema2] ?? "public"}.${table[TableName2]}`;
}
var Schema2, Columns2, ExtraConfigColumns2, OriginalName2, BaseName2, IsAlias2, ExtraConfigBuilder2, IsDrizzleTable2, Table2;
var init_table4 = __esm({
  "node_modules/drizzle-orm/table.js"() {
    init_entity2();
    init_table_utils2();
    Schema2 = Symbol.for("drizzle:Schema");
    Columns2 = Symbol.for("drizzle:Columns");
    ExtraConfigColumns2 = Symbol.for("drizzle:ExtraConfigColumns");
    OriginalName2 = Symbol.for("drizzle:OriginalName");
    BaseName2 = Symbol.for("drizzle:BaseName");
    IsAlias2 = Symbol.for("drizzle:IsAlias");
    ExtraConfigBuilder2 = Symbol.for("drizzle:ExtraConfigBuilder");
    IsDrizzleTable2 = Symbol.for("drizzle:IsDrizzleTable");
    Table2 = class {
      static {
        __name(this, "Table");
      }
      static [entityKind2] = "Table";
      /** @internal */
      static Symbol = {
        Name: TableName2,
        Schema: Schema2,
        OriginalName: OriginalName2,
        Columns: Columns2,
        ExtraConfigColumns: ExtraConfigColumns2,
        BaseName: BaseName2,
        IsAlias: IsAlias2,
        ExtraConfigBuilder: ExtraConfigBuilder2
      };
      /**
       * @internal
       * Can be changed if the table is aliased.
       */
      [TableName2];
      /**
       * @internal
       * Used to store the original name of the table, before any aliasing.
       */
      [OriginalName2];
      /** @internal */
      [Schema2];
      /** @internal */
      [Columns2];
      /** @internal */
      [ExtraConfigColumns2];
      /**
       *  @internal
       * Used to store the table name before the transformation via the `tableCreator` functions.
       */
      [BaseName2];
      /** @internal */
      [IsAlias2] = false;
      /** @internal */
      [IsDrizzleTable2] = true;
      /** @internal */
      [ExtraConfigBuilder2] = void 0;
      constructor(name, schema, baseName) {
        this[TableName2] = this[OriginalName2] = name;
        this[Schema2] = schema;
        this[BaseName2] = baseName;
      }
    };
    __name(isTable, "isTable");
    __name(getTableName2, "getTableName");
    __name(getTableUniqueName2, "getTableUniqueName");
  }
});

// node_modules/drizzle-orm/sql/sql.js
function isSQLWrapper2(value) {
  return value !== null && value !== void 0 && typeof value.getSQL === "function";
}
function mergeQueries2(queries) {
  const result = { sql: "", params: [] };
  for (const query of queries) {
    result.sql += query.sql;
    result.params.push(...query.params);
    if (query.typings?.length) {
      if (!result.typings) {
        result.typings = [];
      }
      result.typings.push(...query.typings);
    }
  }
  return result;
}
function isDriverValueEncoder2(value) {
  return typeof value === "object" && value !== null && "mapToDriverValue" in value && typeof value.mapToDriverValue === "function";
}
function sql2(strings, ...params) {
  const queryChunks = [];
  if (params.length > 0 || strings.length > 0 && strings[0] !== "") {
    queryChunks.push(new StringChunk2(strings[0]));
  }
  for (const [paramIndex, param2] of params.entries()) {
    queryChunks.push(param2, new StringChunk2(strings[paramIndex + 1]));
  }
  return new SQL2(queryChunks);
}
function isView(view) {
  return typeof view === "object" && view !== null && IsDrizzleView2 in view;
}
var FakePrimitiveParam2, StringChunk2, SQL2, Name2, noopDecoder2, noopEncoder2, noopMapper2, Param2, Placeholder2, IsDrizzleView2, View2;
var init_sql4 = __esm({
  "node_modules/drizzle-orm/sql/sql.js"() {
    init_entity2();
    init_enum2();
    init_subquery3();
    init_tracing2();
    init_view_common2();
    init_column2();
    init_table4();
    FakePrimitiveParam2 = class {
      static {
        __name(this, "FakePrimitiveParam");
      }
      static [entityKind2] = "FakePrimitiveParam";
    };
    __name(isSQLWrapper2, "isSQLWrapper");
    __name(mergeQueries2, "mergeQueries");
    StringChunk2 = class {
      static {
        __name(this, "StringChunk");
      }
      static [entityKind2] = "StringChunk";
      value;
      constructor(value) {
        this.value = Array.isArray(value) ? value : [value];
      }
      getSQL() {
        return new SQL2([this]);
      }
    };
    SQL2 = class _SQL {
      static {
        __name(this, "SQL");
      }
      constructor(queryChunks) {
        this.queryChunks = queryChunks;
      }
      static [entityKind2] = "SQL";
      /** @internal */
      decoder = noopDecoder2;
      shouldInlineParams = false;
      append(query) {
        this.queryChunks.push(...query.queryChunks);
        return this;
      }
      toQuery(config) {
        return tracer2.startActiveSpan("drizzle.buildSQL", (span) => {
          const query = this.buildQueryFromSourceParams(this.queryChunks, config);
          span?.setAttributes({
            "drizzle.query.text": query.sql,
            "drizzle.query.params": JSON.stringify(query.params)
          });
          return query;
        });
      }
      buildQueryFromSourceParams(chunks, _config) {
        const config = Object.assign({}, _config, {
          inlineParams: _config.inlineParams || this.shouldInlineParams,
          paramStartIndex: _config.paramStartIndex || { value: 0 }
        });
        const {
          casing,
          escapeName,
          escapeParam,
          prepareTyping,
          inlineParams,
          paramStartIndex
        } = config;
        return mergeQueries2(chunks.map((chunk) => {
          if (is2(chunk, StringChunk2)) {
            return { sql: chunk.value.join(""), params: [] };
          }
          if (is2(chunk, Name2)) {
            return { sql: escapeName(chunk.value), params: [] };
          }
          if (chunk === void 0) {
            return { sql: "", params: [] };
          }
          if (Array.isArray(chunk)) {
            const result = [new StringChunk2("(")];
            for (const [i, p] of chunk.entries()) {
              result.push(p);
              if (i < chunk.length - 1) {
                result.push(new StringChunk2(", "));
              }
            }
            result.push(new StringChunk2(")"));
            return this.buildQueryFromSourceParams(result, config);
          }
          if (is2(chunk, _SQL)) {
            return this.buildQueryFromSourceParams(chunk.queryChunks, {
              ...config,
              inlineParams: inlineParams || chunk.shouldInlineParams
            });
          }
          if (is2(chunk, Table2)) {
            const schemaName = chunk[Table2.Symbol.Schema];
            const tableName = chunk[Table2.Symbol.Name];
            return {
              sql: schemaName === void 0 || chunk[IsAlias2] ? escapeName(tableName) : escapeName(schemaName) + "." + escapeName(tableName),
              params: []
            };
          }
          if (is2(chunk, Column2)) {
            const columnName = casing.getColumnCasing(chunk);
            if (_config.invokeSource === "indexes") {
              return { sql: escapeName(columnName), params: [] };
            }
            const schemaName = chunk.table[Table2.Symbol.Schema];
            return {
              sql: chunk.table[IsAlias2] || schemaName === void 0 ? escapeName(chunk.table[Table2.Symbol.Name]) + "." + escapeName(columnName) : escapeName(schemaName) + "." + escapeName(chunk.table[Table2.Symbol.Name]) + "." + escapeName(columnName),
              params: []
            };
          }
          if (is2(chunk, View2)) {
            const schemaName = chunk[ViewBaseConfig2].schema;
            const viewName = chunk[ViewBaseConfig2].name;
            return {
              sql: schemaName === void 0 || chunk[ViewBaseConfig2].isAlias ? escapeName(viewName) : escapeName(schemaName) + "." + escapeName(viewName),
              params: []
            };
          }
          if (is2(chunk, Param2)) {
            if (is2(chunk.value, Placeholder2)) {
              return { sql: escapeParam(paramStartIndex.value++, chunk), params: [chunk], typings: ["none"] };
            }
            const mappedValue = chunk.value === null ? null : chunk.encoder.mapToDriverValue(chunk.value);
            if (is2(mappedValue, _SQL)) {
              return this.buildQueryFromSourceParams([mappedValue], config);
            }
            if (inlineParams) {
              return { sql: this.mapInlineParam(mappedValue, config), params: [] };
            }
            let typings = ["none"];
            if (prepareTyping) {
              typings = [prepareTyping(chunk.encoder)];
            }
            return { sql: escapeParam(paramStartIndex.value++, mappedValue), params: [mappedValue], typings };
          }
          if (is2(chunk, Placeholder2)) {
            return { sql: escapeParam(paramStartIndex.value++, chunk), params: [chunk], typings: ["none"] };
          }
          if (is2(chunk, _SQL.Aliased) && chunk.fieldAlias !== void 0) {
            return { sql: escapeName(chunk.fieldAlias), params: [] };
          }
          if (is2(chunk, Subquery2)) {
            if (chunk._.isWith) {
              return { sql: escapeName(chunk._.alias), params: [] };
            }
            return this.buildQueryFromSourceParams([
              new StringChunk2("("),
              chunk._.sql,
              new StringChunk2(") "),
              new Name2(chunk._.alias)
            ], config);
          }
          if (isPgEnum2(chunk)) {
            if (chunk.schema) {
              return { sql: escapeName(chunk.schema) + "." + escapeName(chunk.enumName), params: [] };
            }
            return { sql: escapeName(chunk.enumName), params: [] };
          }
          if (isSQLWrapper2(chunk)) {
            if (chunk.shouldOmitSQLParens?.()) {
              return this.buildQueryFromSourceParams([chunk.getSQL()], config);
            }
            return this.buildQueryFromSourceParams([
              new StringChunk2("("),
              chunk.getSQL(),
              new StringChunk2(")")
            ], config);
          }
          if (inlineParams) {
            return { sql: this.mapInlineParam(chunk, config), params: [] };
          }
          return { sql: escapeParam(paramStartIndex.value++, chunk), params: [chunk], typings: ["none"] };
        }));
      }
      mapInlineParam(chunk, { escapeString }) {
        if (chunk === null) {
          return "null";
        }
        if (typeof chunk === "number" || typeof chunk === "boolean") {
          return chunk.toString();
        }
        if (typeof chunk === "string") {
          return escapeString(chunk);
        }
        if (typeof chunk === "object") {
          const mappedValueAsString = chunk.toString();
          if (mappedValueAsString === "[object Object]") {
            return escapeString(JSON.stringify(chunk));
          }
          return escapeString(mappedValueAsString);
        }
        throw new Error("Unexpected param value: " + chunk);
      }
      getSQL() {
        return this;
      }
      as(alias) {
        if (alias === void 0) {
          return this;
        }
        return new _SQL.Aliased(this, alias);
      }
      mapWith(decoder) {
        this.decoder = typeof decoder === "function" ? { mapFromDriverValue: decoder } : decoder;
        return this;
      }
      inlineParams() {
        this.shouldInlineParams = true;
        return this;
      }
      /**
       * This method is used to conditionally include a part of the query.
       *
       * @param condition - Condition to check
       * @returns itself if the condition is `true`, otherwise `undefined`
       */
      if(condition) {
        return condition ? this : void 0;
      }
    };
    Name2 = class {
      static {
        __name(this, "Name");
      }
      constructor(value) {
        this.value = value;
      }
      static [entityKind2] = "Name";
      brand;
      getSQL() {
        return new SQL2([this]);
      }
    };
    __name(isDriverValueEncoder2, "isDriverValueEncoder");
    noopDecoder2 = {
      mapFromDriverValue: /* @__PURE__ */ __name((value) => value, "mapFromDriverValue")
    };
    noopEncoder2 = {
      mapToDriverValue: /* @__PURE__ */ __name((value) => value, "mapToDriverValue")
    };
    noopMapper2 = {
      ...noopDecoder2,
      ...noopEncoder2
    };
    Param2 = class {
      static {
        __name(this, "Param");
      }
      /**
       * @param value - Parameter value
       * @param encoder - Encoder to convert the value to a driver parameter
       */
      constructor(value, encoder = noopEncoder2) {
        this.value = value;
        this.encoder = encoder;
      }
      static [entityKind2] = "Param";
      brand;
      getSQL() {
        return new SQL2([this]);
      }
    };
    __name(sql2, "sql");
    ((sql22) => {
      function empty() {
        return new SQL2([]);
      }
      __name(empty, "empty");
      sql22.empty = empty;
      function fromList(list) {
        return new SQL2(list);
      }
      __name(fromList, "fromList");
      sql22.fromList = fromList;
      function raw2(str) {
        return new SQL2([new StringChunk2(str)]);
      }
      __name(raw2, "raw");
      sql22.raw = raw2;
      function join(chunks, separator) {
        const result = [];
        for (const [i, chunk] of chunks.entries()) {
          if (i > 0 && separator !== void 0) {
            result.push(separator);
          }
          result.push(chunk);
        }
        return new SQL2(result);
      }
      __name(join, "join");
      sql22.join = join;
      function identifier(value) {
        return new Name2(value);
      }
      __name(identifier, "identifier");
      sql22.identifier = identifier;
      function placeholder2(name2) {
        return new Placeholder2(name2);
      }
      __name(placeholder2, "placeholder2");
      sql22.placeholder = placeholder2;
      function param2(value, encoder) {
        return new Param2(value, encoder);
      }
      __name(param2, "param2");
      sql22.param = param2;
    })(sql2 || (sql2 = {}));
    ((SQL22) => {
      class Aliased {
        static {
          __name(this, "Aliased");
        }
        constructor(sql22, fieldAlias) {
          this.sql = sql22;
          this.fieldAlias = fieldAlias;
        }
        static [entityKind2] = "SQL.Aliased";
        /** @internal */
        isSelectionField = false;
        getSQL() {
          return this.sql;
        }
        /** @internal */
        clone() {
          return new Aliased(this.sql, this.fieldAlias);
        }
      }
      SQL22.Aliased = Aliased;
    })(SQL2 || (SQL2 = {}));
    Placeholder2 = class {
      static {
        __name(this, "Placeholder");
      }
      constructor(name2) {
        this.name = name2;
      }
      static [entityKind2] = "Placeholder";
      getSQL() {
        return new SQL2([this]);
      }
    };
    IsDrizzleView2 = Symbol.for("drizzle:IsDrizzleView");
    View2 = class {
      static {
        __name(this, "View");
      }
      static [entityKind2] = "View";
      /** @internal */
      [ViewBaseConfig2];
      /** @internal */
      [IsDrizzleView2] = true;
      constructor({ name: name2, schema, selectedFields, query }) {
        this[ViewBaseConfig2] = {
          name: name2,
          originalName: name2,
          schema,
          selectedFields,
          query,
          isExisting: !query,
          isAlias: false
        };
      }
      getSQL() {
        return new SQL2([this]);
      }
    };
    __name(isView, "isView");
    Column2.prototype.getSQL = function() {
      return new SQL2([this]);
    };
    Table2.prototype.getSQL = function() {
      return new SQL2([this]);
    };
    Subquery2.prototype.getSQL = function() {
      return new SQL2([this]);
    };
  }
});

// node_modules/drizzle-orm/alias.js
function aliasedTable2(table, tableAlias) {
  return new Proxy(table, new TableAliasProxyHandler2(tableAlias, false));
}
function aliasedTableColumn2(column, tableAlias) {
  return new Proxy(
    column,
    new ColumnAliasProxyHandler2(new Proxy(column.table, new TableAliasProxyHandler2(tableAlias, false)))
  );
}
function mapColumnsInAliasedSQLToAlias2(query, alias) {
  return new SQL2.Aliased(mapColumnsInSQLToAlias2(query.sql, alias), query.fieldAlias);
}
function mapColumnsInSQLToAlias2(query, alias) {
  return sql2.join(query.queryChunks.map((c) => {
    if (is2(c, Column2)) {
      return aliasedTableColumn2(c, alias);
    }
    if (is2(c, SQL2)) {
      return mapColumnsInSQLToAlias2(c, alias);
    }
    if (is2(c, SQL2.Aliased)) {
      return mapColumnsInAliasedSQLToAlias2(c, alias);
    }
    return c;
  }));
}
var ColumnAliasProxyHandler2, TableAliasProxyHandler2, RelationTableAliasProxyHandler2;
var init_alias3 = __esm({
  "node_modules/drizzle-orm/alias.js"() {
    init_column2();
    init_entity2();
    init_sql4();
    init_table4();
    init_view_common2();
    ColumnAliasProxyHandler2 = class {
      static {
        __name(this, "ColumnAliasProxyHandler");
      }
      constructor(table) {
        this.table = table;
      }
      static [entityKind2] = "ColumnAliasProxyHandler";
      get(columnObj, prop) {
        if (prop === "table") {
          return this.table;
        }
        return columnObj[prop];
      }
    };
    TableAliasProxyHandler2 = class {
      static {
        __name(this, "TableAliasProxyHandler");
      }
      constructor(alias, replaceOriginalName) {
        this.alias = alias;
        this.replaceOriginalName = replaceOriginalName;
      }
      static [entityKind2] = "TableAliasProxyHandler";
      get(target, prop) {
        if (prop === Table2.Symbol.IsAlias) {
          return true;
        }
        if (prop === Table2.Symbol.Name) {
          return this.alias;
        }
        if (this.replaceOriginalName && prop === Table2.Symbol.OriginalName) {
          return this.alias;
        }
        if (prop === ViewBaseConfig2) {
          return {
            ...target[ViewBaseConfig2],
            name: this.alias,
            isAlias: true
          };
        }
        if (prop === Table2.Symbol.Columns) {
          const columns = target[Table2.Symbol.Columns];
          if (!columns) {
            return columns;
          }
          const proxiedColumns = {};
          Object.keys(columns).map((key) => {
            proxiedColumns[key] = new Proxy(
              columns[key],
              new ColumnAliasProxyHandler2(new Proxy(target, this))
            );
          });
          return proxiedColumns;
        }
        const value = target[prop];
        if (is2(value, Column2)) {
          return new Proxy(value, new ColumnAliasProxyHandler2(new Proxy(target, this)));
        }
        return value;
      }
    };
    RelationTableAliasProxyHandler2 = class {
      static {
        __name(this, "RelationTableAliasProxyHandler");
      }
      constructor(alias) {
        this.alias = alias;
      }
      static [entityKind2] = "RelationTableAliasProxyHandler";
      get(target, prop) {
        if (prop === "sourceTable") {
          return aliasedTable2(target.sourceTable, this.alias);
        }
        return target[prop];
      }
    };
    __name(aliasedTable2, "aliasedTable");
    __name(aliasedTableColumn2, "aliasedTableColumn");
    __name(mapColumnsInAliasedSQLToAlias2, "mapColumnsInAliasedSQLToAlias");
    __name(mapColumnsInSQLToAlias2, "mapColumnsInSQLToAlias");
  }
});

// node_modules/drizzle-orm/errors.js
var DrizzleError2, TransactionRollbackError2;
var init_errors3 = __esm({
  "node_modules/drizzle-orm/errors.js"() {
    init_entity2();
    DrizzleError2 = class extends Error {
      static {
        __name(this, "DrizzleError");
      }
      static [entityKind2] = "DrizzleError";
      constructor({ message, cause }) {
        super(message);
        this.name = "DrizzleError";
        this.cause = cause;
      }
    };
    TransactionRollbackError2 = class extends DrizzleError2 {
      static {
        __name(this, "TransactionRollbackError");
      }
      static [entityKind2] = "TransactionRollbackError";
      constructor() {
        super({ message: "Rollback" });
      }
    };
  }
});

// node_modules/drizzle-orm/sql/expressions/conditions.js
function bindIfParam2(value, column) {
  if (isDriverValueEncoder2(column) && !isSQLWrapper2(value) && !is2(value, Param2) && !is2(value, Placeholder2) && !is2(value, Column2) && !is2(value, Table2) && !is2(value, View2)) {
    return new Param2(value, column);
  }
  return value;
}
function and2(...unfilteredConditions) {
  const conditions = unfilteredConditions.filter(
    (c) => c !== void 0
  );
  if (conditions.length === 0) {
    return void 0;
  }
  if (conditions.length === 1) {
    return new SQL2(conditions);
  }
  return new SQL2([
    new StringChunk2("("),
    sql2.join(conditions, new StringChunk2(" and ")),
    new StringChunk2(")")
  ]);
}
function or2(...unfilteredConditions) {
  const conditions = unfilteredConditions.filter(
    (c) => c !== void 0
  );
  if (conditions.length === 0) {
    return void 0;
  }
  if (conditions.length === 1) {
    return new SQL2(conditions);
  }
  return new SQL2([
    new StringChunk2("("),
    sql2.join(conditions, new StringChunk2(" or ")),
    new StringChunk2(")")
  ]);
}
function not2(condition) {
  return sql2`not ${condition}`;
}
function inArray2(column, values) {
  if (Array.isArray(values)) {
    if (values.length === 0) {
      return sql2`false`;
    }
    return sql2`${column} in ${values.map((v) => bindIfParam2(v, column))}`;
  }
  return sql2`${column} in ${bindIfParam2(values, column)}`;
}
function notInArray2(column, values) {
  if (Array.isArray(values)) {
    if (values.length === 0) {
      return sql2`true`;
    }
    return sql2`${column} not in ${values.map((v) => bindIfParam2(v, column))}`;
  }
  return sql2`${column} not in ${bindIfParam2(values, column)}`;
}
function isNull2(value) {
  return sql2`${value} is null`;
}
function isNotNull2(value) {
  return sql2`${value} is not null`;
}
function exists2(subquery) {
  return sql2`exists ${subquery}`;
}
function notExists2(subquery) {
  return sql2`not exists ${subquery}`;
}
function between2(column, min, max) {
  return sql2`${column} between ${bindIfParam2(min, column)} and ${bindIfParam2(
    max,
    column
  )}`;
}
function notBetween2(column, min, max) {
  return sql2`${column} not between ${bindIfParam2(
    min,
    column
  )} and ${bindIfParam2(max, column)}`;
}
function like2(column, value) {
  return sql2`${column} like ${value}`;
}
function notLike2(column, value) {
  return sql2`${column} not like ${value}`;
}
function ilike2(column, value) {
  return sql2`${column} ilike ${value}`;
}
function notIlike2(column, value) {
  return sql2`${column} not ilike ${value}`;
}
var eq2, ne2, gt2, gte2, lt2, lte2;
var init_conditions2 = __esm({
  "node_modules/drizzle-orm/sql/expressions/conditions.js"() {
    init_column2();
    init_entity2();
    init_table4();
    init_sql4();
    __name(bindIfParam2, "bindIfParam");
    eq2 = /* @__PURE__ */ __name((left, right) => {
      return sql2`${left} = ${bindIfParam2(right, left)}`;
    }, "eq");
    ne2 = /* @__PURE__ */ __name((left, right) => {
      return sql2`${left} <> ${bindIfParam2(right, left)}`;
    }, "ne");
    __name(and2, "and");
    __name(or2, "or");
    __name(not2, "not");
    gt2 = /* @__PURE__ */ __name((left, right) => {
      return sql2`${left} > ${bindIfParam2(right, left)}`;
    }, "gt");
    gte2 = /* @__PURE__ */ __name((left, right) => {
      return sql2`${left} >= ${bindIfParam2(right, left)}`;
    }, "gte");
    lt2 = /* @__PURE__ */ __name((left, right) => {
      return sql2`${left} < ${bindIfParam2(right, left)}`;
    }, "lt");
    lte2 = /* @__PURE__ */ __name((left, right) => {
      return sql2`${left} <= ${bindIfParam2(right, left)}`;
    }, "lte");
    __name(inArray2, "inArray");
    __name(notInArray2, "notInArray");
    __name(isNull2, "isNull");
    __name(isNotNull2, "isNotNull");
    __name(exists2, "exists");
    __name(notExists2, "notExists");
    __name(between2, "between");
    __name(notBetween2, "notBetween");
    __name(like2, "like");
    __name(notLike2, "notLike");
    __name(ilike2, "ilike");
    __name(notIlike2, "notIlike");
  }
});

// node_modules/drizzle-orm/sql/expressions/select.js
function asc2(column) {
  return sql2`${column} asc`;
}
function desc2(column) {
  return sql2`${column} desc`;
}
var init_select3 = __esm({
  "node_modules/drizzle-orm/sql/expressions/select.js"() {
    init_sql4();
    __name(asc2, "asc");
    __name(desc2, "desc");
  }
});

// node_modules/drizzle-orm/sql/expressions/index.js
var init_expressions2 = __esm({
  "node_modules/drizzle-orm/sql/expressions/index.js"() {
    init_conditions2();
    init_select3();
  }
});

// node_modules/drizzle-orm/expressions.js
var init_expressions3 = __esm({
  "node_modules/drizzle-orm/expressions.js"() {
    init_expressions2();
  }
});

// node_modules/drizzle-orm/logger.js
var ConsoleLogWriter2, DefaultLogger2, NoopLogger2;
var init_logger2 = __esm({
  "node_modules/drizzle-orm/logger.js"() {
    init_entity2();
    ConsoleLogWriter2 = class {
      static {
        __name(this, "ConsoleLogWriter");
      }
      static [entityKind2] = "ConsoleLogWriter";
      write(message) {
        console.log(message);
      }
    };
    DefaultLogger2 = class {
      static {
        __name(this, "DefaultLogger");
      }
      static [entityKind2] = "DefaultLogger";
      writer;
      constructor(config) {
        this.writer = config?.writer ?? new ConsoleLogWriter2();
      }
      logQuery(query, params) {
        const stringifiedParams = params.map((p) => {
          try {
            return JSON.stringify(p);
          } catch {
            return String(p);
          }
        });
        const paramsStr = stringifiedParams.length ? ` -- params: [${stringifiedParams.join(", ")}]` : "";
        this.writer.write(`Query: ${query}${paramsStr}`);
      }
    };
    NoopLogger2 = class {
      static {
        __name(this, "NoopLogger");
      }
      static [entityKind2] = "NoopLogger";
      logQuery() {
      }
    };
  }
});

// node_modules/drizzle-orm/operations.js
var init_operations = __esm({
  "node_modules/drizzle-orm/operations.js"() {
  }
});

// node_modules/drizzle-orm/query-promise.js
var QueryPromise2;
var init_query_promise2 = __esm({
  "node_modules/drizzle-orm/query-promise.js"() {
    init_entity2();
    QueryPromise2 = class {
      static {
        __name(this, "QueryPromise");
      }
      static [entityKind2] = "QueryPromise";
      [Symbol.toStringTag] = "QueryPromise";
      catch(onRejected) {
        return this.then(void 0, onRejected);
      }
      finally(onFinally) {
        return this.then(
          (value) => {
            onFinally?.();
            return value;
          },
          (reason) => {
            onFinally?.();
            throw reason;
          }
        );
      }
      then(onFulfilled, onRejected) {
        return this.execute().then(onFulfilled, onRejected);
      }
    };
  }
});

// node_modules/drizzle-orm/utils.js
function orderSelectedFields2(fields, pathPrefix) {
  return Object.entries(fields).reduce((result, [name, field]) => {
    if (typeof name !== "string") {
      return result;
    }
    const newPath = pathPrefix ? [...pathPrefix, name] : [name];
    if (is2(field, Column2) || is2(field, SQL2) || is2(field, SQL2.Aliased)) {
      result.push({ path: newPath, field });
    } else if (is2(field, Table2)) {
      result.push(...orderSelectedFields2(field[Table2.Symbol.Columns], newPath));
    } else {
      result.push(...orderSelectedFields2(field, newPath));
    }
    return result;
  }, []);
}
function haveSameKeys2(left, right) {
  const leftKeys = Object.keys(left);
  const rightKeys = Object.keys(right);
  if (leftKeys.length !== rightKeys.length) {
    return false;
  }
  for (const [index, key] of leftKeys.entries()) {
    if (key !== rightKeys[index]) {
      return false;
    }
  }
  return true;
}
function mapUpdateSet2(table, values) {
  const entries = Object.entries(values).filter(([, value]) => value !== void 0).map(([key, value]) => {
    if (is2(value, SQL2) || is2(value, Column2)) {
      return [key, value];
    } else {
      return [key, new Param2(value, table[Table2.Symbol.Columns][key])];
    }
  });
  if (entries.length === 0) {
    throw new Error("No values to set");
  }
  return Object.fromEntries(entries);
}
function applyMixins2(baseClass, extendedClasses) {
  for (const extendedClass of extendedClasses) {
    for (const name of Object.getOwnPropertyNames(extendedClass.prototype)) {
      if (name === "constructor")
        continue;
      Object.defineProperty(
        baseClass.prototype,
        name,
        Object.getOwnPropertyDescriptor(extendedClass.prototype, name) || /* @__PURE__ */ Object.create(null)
      );
    }
  }
}
function getTableColumns2(table) {
  return table[Table2.Symbol.Columns];
}
function getViewSelectedFields(view) {
  return view[ViewBaseConfig2].selectedFields;
}
function getTableLikeName2(table) {
  return is2(table, Subquery2) ? table._.alias : is2(table, View2) ? table[ViewBaseConfig2].name : is2(table, SQL2) ? void 0 : table[Table2.Symbol.IsAlias] ? table[Table2.Symbol.Name] : table[Table2.Symbol.BaseName];
}
function getColumnNameAndConfig2(a, b) {
  return {
    name: typeof a === "string" && a.length > 0 ? a : "",
    config: typeof a === "object" ? a : b
  };
}
var init_utils3 = __esm({
  "node_modules/drizzle-orm/utils.js"() {
    init_column2();
    init_entity2();
    init_sql4();
    init_subquery3();
    init_table4();
    init_view_common2();
    __name(orderSelectedFields2, "orderSelectedFields");
    __name(haveSameKeys2, "haveSameKeys");
    __name(mapUpdateSet2, "mapUpdateSet");
    __name(applyMixins2, "applyMixins");
    __name(getTableColumns2, "getTableColumns");
    __name(getViewSelectedFields, "getViewSelectedFields");
    __name(getTableLikeName2, "getTableLikeName");
    __name(getColumnNameAndConfig2, "getColumnNameAndConfig");
  }
});

// node_modules/drizzle-orm/relations.js
function getOperators2() {
  return {
    and: and2,
    between: between2,
    eq: eq2,
    exists: exists2,
    gt: gt2,
    gte: gte2,
    ilike: ilike2,
    inArray: inArray2,
    isNull: isNull2,
    isNotNull: isNotNull2,
    like: like2,
    lt: lt2,
    lte: lte2,
    ne: ne2,
    not: not2,
    notBetween: notBetween2,
    notExists: notExists2,
    notLike: notLike2,
    notIlike: notIlike2,
    notInArray: notInArray2,
    or: or2,
    sql: sql2
  };
}
function getOrderByOperators2() {
  return {
    sql: sql2,
    asc: asc2,
    desc: desc2
  };
}
function relations(table, relations2) {
  return new Relations2(
    table,
    (helpers) => Object.fromEntries(
      Object.entries(relations2(helpers)).map(([key, value]) => [
        key,
        value.withFieldName(key)
      ])
    )
  );
}
function normalizeRelation2(schema, tableNamesMap, relation) {
  if (is2(relation, One2) && relation.config) {
    return {
      fields: relation.config.fields,
      references: relation.config.references
    };
  }
  const referencedTableTsName = tableNamesMap[getTableUniqueName2(relation.referencedTable)];
  if (!referencedTableTsName) {
    throw new Error(
      `Table "${relation.referencedTable[Table2.Symbol.Name]}" not found in schema`
    );
  }
  const referencedTableConfig = schema[referencedTableTsName];
  if (!referencedTableConfig) {
    throw new Error(`Table "${referencedTableTsName}" not found in schema`);
  }
  const sourceTable = relation.sourceTable;
  const sourceTableTsName = tableNamesMap[getTableUniqueName2(sourceTable)];
  if (!sourceTableTsName) {
    throw new Error(
      `Table "${sourceTable[Table2.Symbol.Name]}" not found in schema`
    );
  }
  const reverseRelations = [];
  for (const referencedTableRelation of Object.values(
    referencedTableConfig.relations
  )) {
    if (relation.relationName && relation !== referencedTableRelation && referencedTableRelation.relationName === relation.relationName || !relation.relationName && referencedTableRelation.referencedTable === relation.sourceTable) {
      reverseRelations.push(referencedTableRelation);
    }
  }
  if (reverseRelations.length > 1) {
    throw relation.relationName ? new Error(
      `There are multiple relations with name "${relation.relationName}" in table "${referencedTableTsName}"`
    ) : new Error(
      `There are multiple relations between "${referencedTableTsName}" and "${relation.sourceTable[Table2.Symbol.Name]}". Please specify relation name`
    );
  }
  if (reverseRelations[0] && is2(reverseRelations[0], One2) && reverseRelations[0].config) {
    return {
      fields: reverseRelations[0].config.references,
      references: reverseRelations[0].config.fields
    };
  }
  throw new Error(
    `There is not enough information to infer relation "${sourceTableTsName}.${relation.fieldName}"`
  );
}
function mapRelationalRow2(tablesConfig, tableConfig, row, buildQueryResultSelection, mapColumnValue = (value) => value) {
  const result = {};
  for (const [
    selectionItemIndex,
    selectionItem
  ] of buildQueryResultSelection.entries()) {
    if (selectionItem.isJson) {
      const relation = tableConfig.relations[selectionItem.tsKey];
      const rawSubRows = row[selectionItemIndex];
      const subRows = typeof rawSubRows === "string" ? JSON.parse(rawSubRows) : rawSubRows;
      result[selectionItem.tsKey] = is2(relation, One2) ? subRows && mapRelationalRow2(
        tablesConfig,
        tablesConfig[selectionItem.relationTableTsKey],
        subRows,
        selectionItem.selection,
        mapColumnValue
      ) : subRows.map(
        (subRow) => mapRelationalRow2(
          tablesConfig,
          tablesConfig[selectionItem.relationTableTsKey],
          subRow,
          selectionItem.selection,
          mapColumnValue
        )
      );
    } else {
      const value = mapColumnValue(row[selectionItemIndex]);
      const field = selectionItem.field;
      let decoder;
      if (is2(field, Column2)) {
        decoder = field;
      } else if (is2(field, SQL2)) {
        decoder = field.decoder;
      } else {
        decoder = field.sql.decoder;
      }
      result[selectionItem.tsKey] = value === null ? null : decoder.mapFromDriverValue(value);
    }
  }
  return result;
}
var Relation2, Relations2, One2, Many2;
var init_relations2 = __esm({
  "node_modules/drizzle-orm/relations.js"() {
    init_table4();
    init_column2();
    init_entity2();
    init_expressions2();
    init_sql4();
    Relation2 = class {
      static {
        __name(this, "Relation");
      }
      constructor(sourceTable, referencedTable, relationName) {
        this.sourceTable = sourceTable;
        this.referencedTable = referencedTable;
        this.relationName = relationName;
        this.referencedTableName = referencedTable[Table2.Symbol.Name];
      }
      static [entityKind2] = "Relation";
      referencedTableName;
      fieldName;
    };
    Relations2 = class {
      static {
        __name(this, "Relations");
      }
      constructor(table, config) {
        this.table = table;
        this.config = config;
      }
      static [entityKind2] = "Relations";
    };
    One2 = class _One extends Relation2 {
      static {
        __name(this, "One");
      }
      constructor(sourceTable, referencedTable, config, isNullable) {
        super(sourceTable, referencedTable, config?.relationName);
        this.config = config;
        this.isNullable = isNullable;
      }
      static [entityKind2] = "One";
      withFieldName(fieldName) {
        const relation = new _One(
          this.sourceTable,
          this.referencedTable,
          this.config,
          this.isNullable
        );
        relation.fieldName = fieldName;
        return relation;
      }
    };
    Many2 = class _Many extends Relation2 {
      static {
        __name(this, "Many");
      }
      constructor(sourceTable, referencedTable, config) {
        super(sourceTable, referencedTable, config?.relationName);
        this.config = config;
      }
      static [entityKind2] = "Many";
      withFieldName(fieldName) {
        const relation = new _Many(
          this.sourceTable,
          this.referencedTable,
          this.config
        );
        relation.fieldName = fieldName;
        return relation;
      }
    };
    __name(getOperators2, "getOperators");
    __name(getOrderByOperators2, "getOrderByOperators");
    __name(relations, "relations");
    __name(normalizeRelation2, "normalizeRelation");
    __name(mapRelationalRow2, "mapRelationalRow");
  }
});

// node_modules/drizzle-orm/sql/functions/aggregate.js
var init_aggregate2 = __esm({
  "node_modules/drizzle-orm/sql/functions/aggregate.js"() {
  }
});

// node_modules/drizzle-orm/sql/functions/vector.js
var init_vector2 = __esm({
  "node_modules/drizzle-orm/sql/functions/vector.js"() {
  }
});

// node_modules/drizzle-orm/sql/functions/index.js
var init_functions2 = __esm({
  "node_modules/drizzle-orm/sql/functions/index.js"() {
    init_aggregate2();
    init_vector2();
  }
});

// node_modules/drizzle-orm/sql/index.js
var init_sql5 = __esm({
  "node_modules/drizzle-orm/sql/index.js"() {
    init_expressions2();
    init_functions2();
    init_sql4();
  }
});

// node_modules/drizzle-orm/index.js
var init_drizzle_orm = __esm({
  "node_modules/drizzle-orm/index.js"() {
    init_alias3();
    init_column_builder2();
    init_column2();
    init_entity2();
    init_errors3();
    init_expressions3();
    init_logger2();
    init_operations();
    init_query_promise2();
    init_relations2();
    init_sql5();
    init_subquery3();
    init_table4();
    init_utils3();
    init_view_common2();
  }
});

// node_modules/drizzle-orm/sqlite-core/alias.js
var init_alias4 = __esm({
  "node_modules/drizzle-orm/sqlite-core/alias.js"() {
  }
});

// node_modules/drizzle-orm/sqlite-core/checks.js
var CheckBuilder2, Check2;
var init_checks2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/checks.js"() {
    init_entity2();
    CheckBuilder2 = class {
      static {
        __name(this, "CheckBuilder");
      }
      constructor(name, value) {
        this.name = name;
        this.value = value;
      }
      static [entityKind2] = "SQLiteCheckBuilder";
      brand;
      build(table) {
        return new Check2(table, this);
      }
    };
    Check2 = class {
      static {
        __name(this, "Check");
      }
      constructor(table, builder) {
        this.table = table;
        this.name = builder.name;
        this.value = builder.value;
      }
      static [entityKind2] = "SQLiteCheck";
      name;
      value;
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/foreign-keys.js
var ForeignKeyBuilder4, ForeignKey4;
var init_foreign_keys4 = __esm({
  "node_modules/drizzle-orm/sqlite-core/foreign-keys.js"() {
    init_entity2();
    init_table_utils2();
    ForeignKeyBuilder4 = class {
      static {
        __name(this, "ForeignKeyBuilder");
      }
      static [entityKind2] = "SQLiteForeignKeyBuilder";
      /** @internal */
      reference;
      /** @internal */
      _onUpdate;
      /** @internal */
      _onDelete;
      constructor(config, actions) {
        this.reference = () => {
          const { name, columns, foreignColumns } = config();
          return { name, columns, foreignTable: foreignColumns[0].table, foreignColumns };
        };
        if (actions) {
          this._onUpdate = actions.onUpdate;
          this._onDelete = actions.onDelete;
        }
      }
      onUpdate(action) {
        this._onUpdate = action;
        return this;
      }
      onDelete(action) {
        this._onDelete = action;
        return this;
      }
      /** @internal */
      build(table) {
        return new ForeignKey4(table, this);
      }
    };
    ForeignKey4 = class {
      static {
        __name(this, "ForeignKey");
      }
      constructor(table, builder) {
        this.table = table;
        this.reference = builder.reference;
        this.onUpdate = builder._onUpdate;
        this.onDelete = builder._onDelete;
      }
      static [entityKind2] = "SQLiteForeignKey";
      reference;
      onUpdate;
      onDelete;
      getName() {
        const { name, columns, foreignColumns } = this.reference();
        const columnNames = columns.map((column) => column.name);
        const foreignColumnNames = foreignColumns.map((column) => column.name);
        const chunks = [
          this.table[TableName2],
          ...columnNames,
          foreignColumns[0].table[TableName2],
          ...foreignColumnNames
        ];
        return name ?? `${chunks.join("_")}_fk`;
      }
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/unique-constraint.js
function uniqueKeyName4(table, columns) {
  return `${table[TableName2]}_${columns.join("_")}_unique`;
}
var UniqueConstraintBuilder4, UniqueOnConstraintBuilder4, UniqueConstraint4;
var init_unique_constraint4 = __esm({
  "node_modules/drizzle-orm/sqlite-core/unique-constraint.js"() {
    init_entity2();
    init_table_utils2();
    __name(uniqueKeyName4, "uniqueKeyName");
    UniqueConstraintBuilder4 = class {
      static {
        __name(this, "UniqueConstraintBuilder");
      }
      constructor(columns, name) {
        this.name = name;
        this.columns = columns;
      }
      static [entityKind2] = "SQLiteUniqueConstraintBuilder";
      /** @internal */
      columns;
      /** @internal */
      build(table) {
        return new UniqueConstraint4(table, this.columns, this.name);
      }
    };
    UniqueOnConstraintBuilder4 = class {
      static {
        __name(this, "UniqueOnConstraintBuilder");
      }
      static [entityKind2] = "SQLiteUniqueOnConstraintBuilder";
      /** @internal */
      name;
      constructor(name) {
        this.name = name;
      }
      on(...columns) {
        return new UniqueConstraintBuilder4(columns, this.name);
      }
    };
    UniqueConstraint4 = class {
      static {
        __name(this, "UniqueConstraint");
      }
      constructor(table, columns, name) {
        this.table = table;
        this.columns = columns;
        this.name = name ?? uniqueKeyName4(this.table, this.columns.map((column) => column.name));
      }
      static [entityKind2] = "SQLiteUniqueConstraint";
      columns;
      name;
      getName() {
        return this.name;
      }
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/columns/common.js
var SQLiteColumnBuilder2, SQLiteColumn2;
var init_common4 = __esm({
  "node_modules/drizzle-orm/sqlite-core/columns/common.js"() {
    init_column_builder2();
    init_column2();
    init_entity2();
    init_foreign_keys4();
    init_unique_constraint4();
    SQLiteColumnBuilder2 = class extends ColumnBuilder2 {
      static {
        __name(this, "SQLiteColumnBuilder");
      }
      static [entityKind2] = "SQLiteColumnBuilder";
      foreignKeyConfigs = [];
      references(ref, actions = {}) {
        this.foreignKeyConfigs.push({ ref, actions });
        return this;
      }
      unique(name) {
        this.config.isUnique = true;
        this.config.uniqueName = name;
        return this;
      }
      generatedAlwaysAs(as, config) {
        this.config.generated = {
          as,
          type: "always",
          mode: config?.mode ?? "virtual"
        };
        return this;
      }
      /** @internal */
      buildForeignKeys(column, table) {
        return this.foreignKeyConfigs.map(({ ref, actions }) => {
          return ((ref2, actions2) => {
            const builder = new ForeignKeyBuilder4(() => {
              const foreignColumn = ref2();
              return { columns: [column], foreignColumns: [foreignColumn] };
            });
            if (actions2.onUpdate) {
              builder.onUpdate(actions2.onUpdate);
            }
            if (actions2.onDelete) {
              builder.onDelete(actions2.onDelete);
            }
            return builder.build(table);
          })(ref, actions);
        });
      }
    };
    SQLiteColumn2 = class extends Column2 {
      static {
        __name(this, "SQLiteColumn");
      }
      constructor(table, config) {
        if (!config.uniqueName) {
          config.uniqueName = uniqueKeyName4(table, [config.name]);
        }
        super(table, config);
        this.table = table;
      }
      static [entityKind2] = "SQLiteColumn";
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/columns/blob.js
function blob2(a, b) {
  const { name, config } = getColumnNameAndConfig2(a, b);
  if (config?.mode === "json") {
    return new SQLiteBlobJsonBuilder2(name);
  }
  if (config?.mode === "bigint") {
    return new SQLiteBigIntBuilder2(name);
  }
  return new SQLiteBlobBufferBuilder2(name);
}
var SQLiteBigIntBuilder2, SQLiteBigInt2, SQLiteBlobJsonBuilder2, SQLiteBlobJson2, SQLiteBlobBufferBuilder2, SQLiteBlobBuffer2;
var init_blob2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/columns/blob.js"() {
    init_entity2();
    init_utils3();
    init_common4();
    SQLiteBigIntBuilder2 = class extends SQLiteColumnBuilder2 {
      static {
        __name(this, "SQLiteBigIntBuilder");
      }
      static [entityKind2] = "SQLiteBigIntBuilder";
      constructor(name) {
        super(name, "bigint", "SQLiteBigInt");
      }
      /** @internal */
      build(table) {
        return new SQLiteBigInt2(table, this.config);
      }
    };
    SQLiteBigInt2 = class extends SQLiteColumn2 {
      static {
        __name(this, "SQLiteBigInt");
      }
      static [entityKind2] = "SQLiteBigInt";
      getSQLType() {
        return "blob";
      }
      mapFromDriverValue(value) {
        if (Buffer.isBuffer(value)) {
          return BigInt(value.toString());
        }
        if (value instanceof ArrayBuffer) {
          const decoder = new TextDecoder();
          return BigInt(decoder.decode(value));
        }
        return BigInt(String.fromCodePoint(...value));
      }
      mapToDriverValue(value) {
        return Buffer.from(value.toString());
      }
    };
    SQLiteBlobJsonBuilder2 = class extends SQLiteColumnBuilder2 {
      static {
        __name(this, "SQLiteBlobJsonBuilder");
      }
      static [entityKind2] = "SQLiteBlobJsonBuilder";
      constructor(name) {
        super(name, "json", "SQLiteBlobJson");
      }
      /** @internal */
      build(table) {
        return new SQLiteBlobJson2(
          table,
          this.config
        );
      }
    };
    SQLiteBlobJson2 = class extends SQLiteColumn2 {
      static {
        __name(this, "SQLiteBlobJson");
      }
      static [entityKind2] = "SQLiteBlobJson";
      getSQLType() {
        return "blob";
      }
      mapFromDriverValue(value) {
        if (Buffer.isBuffer(value)) {
          return JSON.parse(value.toString());
        }
        if (value instanceof ArrayBuffer) {
          const decoder = new TextDecoder();
          return JSON.parse(decoder.decode(value));
        }
        return JSON.parse(String.fromCodePoint(...value));
      }
      mapToDriverValue(value) {
        return Buffer.from(JSON.stringify(value));
      }
    };
    SQLiteBlobBufferBuilder2 = class extends SQLiteColumnBuilder2 {
      static {
        __name(this, "SQLiteBlobBufferBuilder");
      }
      static [entityKind2] = "SQLiteBlobBufferBuilder";
      constructor(name) {
        super(name, "buffer", "SQLiteBlobBuffer");
      }
      /** @internal */
      build(table) {
        return new SQLiteBlobBuffer2(table, this.config);
      }
    };
    SQLiteBlobBuffer2 = class extends SQLiteColumn2 {
      static {
        __name(this, "SQLiteBlobBuffer");
      }
      static [entityKind2] = "SQLiteBlobBuffer";
      getSQLType() {
        return "blob";
      }
    };
    __name(blob2, "blob");
  }
});

// node_modules/drizzle-orm/sqlite-core/columns/custom.js
function customType2(customTypeParams) {
  return (a, b) => {
    const { name, config } = getColumnNameAndConfig2(a, b);
    return new SQLiteCustomColumnBuilder2(
      name,
      config,
      customTypeParams
    );
  };
}
var SQLiteCustomColumnBuilder2, SQLiteCustomColumn2;
var init_custom2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/columns/custom.js"() {
    init_entity2();
    init_utils3();
    init_common4();
    SQLiteCustomColumnBuilder2 = class extends SQLiteColumnBuilder2 {
      static {
        __name(this, "SQLiteCustomColumnBuilder");
      }
      static [entityKind2] = "SQLiteCustomColumnBuilder";
      constructor(name, fieldConfig, customTypeParams) {
        super(name, "custom", "SQLiteCustomColumn");
        this.config.fieldConfig = fieldConfig;
        this.config.customTypeParams = customTypeParams;
      }
      /** @internal */
      build(table) {
        return new SQLiteCustomColumn2(
          table,
          this.config
        );
      }
    };
    SQLiteCustomColumn2 = class extends SQLiteColumn2 {
      static {
        __name(this, "SQLiteCustomColumn");
      }
      static [entityKind2] = "SQLiteCustomColumn";
      sqlName;
      mapTo;
      mapFrom;
      constructor(table, config) {
        super(table, config);
        this.sqlName = config.customTypeParams.dataType(config.fieldConfig);
        this.mapTo = config.customTypeParams.toDriver;
        this.mapFrom = config.customTypeParams.fromDriver;
      }
      getSQLType() {
        return this.sqlName;
      }
      mapFromDriverValue(value) {
        return typeof this.mapFrom === "function" ? this.mapFrom(value) : value;
      }
      mapToDriverValue(value) {
        return typeof this.mapTo === "function" ? this.mapTo(value) : value;
      }
    };
    __name(customType2, "customType");
  }
});

// node_modules/drizzle-orm/sqlite-core/columns/integer.js
function integer2(a, b) {
  const { name, config } = getColumnNameAndConfig2(a, b);
  if (config?.mode === "timestamp" || config?.mode === "timestamp_ms") {
    return new SQLiteTimestampBuilder2(name, config.mode);
  }
  if (config?.mode === "boolean") {
    return new SQLiteBooleanBuilder2(name, config.mode);
  }
  return new SQLiteIntegerBuilder2(name);
}
var SQLiteBaseIntegerBuilder2, SQLiteBaseInteger2, SQLiteIntegerBuilder2, SQLiteInteger2, SQLiteTimestampBuilder2, SQLiteTimestamp2, SQLiteBooleanBuilder2, SQLiteBoolean2;
var init_integer2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/columns/integer.js"() {
    init_entity2();
    init_sql4();
    init_utils3();
    init_common4();
    SQLiteBaseIntegerBuilder2 = class extends SQLiteColumnBuilder2 {
      static {
        __name(this, "SQLiteBaseIntegerBuilder");
      }
      static [entityKind2] = "SQLiteBaseIntegerBuilder";
      constructor(name, dataType, columnType) {
        super(name, dataType, columnType);
        this.config.autoIncrement = false;
      }
      primaryKey(config) {
        if (config?.autoIncrement) {
          this.config.autoIncrement = true;
        }
        this.config.hasDefault = true;
        return super.primaryKey();
      }
    };
    SQLiteBaseInteger2 = class extends SQLiteColumn2 {
      static {
        __name(this, "SQLiteBaseInteger");
      }
      static [entityKind2] = "SQLiteBaseInteger";
      autoIncrement = this.config.autoIncrement;
      getSQLType() {
        return "integer";
      }
    };
    SQLiteIntegerBuilder2 = class extends SQLiteBaseIntegerBuilder2 {
      static {
        __name(this, "SQLiteIntegerBuilder");
      }
      static [entityKind2] = "SQLiteIntegerBuilder";
      constructor(name) {
        super(name, "number", "SQLiteInteger");
      }
      build(table) {
        return new SQLiteInteger2(
          table,
          this.config
        );
      }
    };
    SQLiteInteger2 = class extends SQLiteBaseInteger2 {
      static {
        __name(this, "SQLiteInteger");
      }
      static [entityKind2] = "SQLiteInteger";
    };
    SQLiteTimestampBuilder2 = class extends SQLiteBaseIntegerBuilder2 {
      static {
        __name(this, "SQLiteTimestampBuilder");
      }
      static [entityKind2] = "SQLiteTimestampBuilder";
      constructor(name, mode) {
        super(name, "date", "SQLiteTimestamp");
        this.config.mode = mode;
      }
      /**
       * @deprecated Use `default()` with your own expression instead.
       *
       * Adds `DEFAULT (cast((julianday('now') - 2440587.5)*86400000 as integer))` to the column, which is the current epoch timestamp in milliseconds.
       */
      defaultNow() {
        return this.default(sql2`(cast((julianday('now') - 2440587.5)*86400000 as integer))`);
      }
      build(table) {
        return new SQLiteTimestamp2(
          table,
          this.config
        );
      }
    };
    SQLiteTimestamp2 = class extends SQLiteBaseInteger2 {
      static {
        __name(this, "SQLiteTimestamp");
      }
      static [entityKind2] = "SQLiteTimestamp";
      mode = this.config.mode;
      mapFromDriverValue(value) {
        if (this.config.mode === "timestamp") {
          return new Date(value * 1e3);
        }
        return new Date(value);
      }
      mapToDriverValue(value) {
        const unix = value.getTime();
        if (this.config.mode === "timestamp") {
          return Math.floor(unix / 1e3);
        }
        return unix;
      }
    };
    SQLiteBooleanBuilder2 = class extends SQLiteBaseIntegerBuilder2 {
      static {
        __name(this, "SQLiteBooleanBuilder");
      }
      static [entityKind2] = "SQLiteBooleanBuilder";
      constructor(name, mode) {
        super(name, "boolean", "SQLiteBoolean");
        this.config.mode = mode;
      }
      build(table) {
        return new SQLiteBoolean2(
          table,
          this.config
        );
      }
    };
    SQLiteBoolean2 = class extends SQLiteBaseInteger2 {
      static {
        __name(this, "SQLiteBoolean");
      }
      static [entityKind2] = "SQLiteBoolean";
      mode = this.config.mode;
      mapFromDriverValue(value) {
        return Number(value) === 1;
      }
      mapToDriverValue(value) {
        return value ? 1 : 0;
      }
    };
    __name(integer2, "integer");
  }
});

// node_modules/drizzle-orm/sqlite-core/columns/numeric.js
function numeric2(name) {
  return new SQLiteNumericBuilder2(name ?? "");
}
var SQLiteNumericBuilder2, SQLiteNumeric2;
var init_numeric2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/columns/numeric.js"() {
    init_entity2();
    init_common4();
    SQLiteNumericBuilder2 = class extends SQLiteColumnBuilder2 {
      static {
        __name(this, "SQLiteNumericBuilder");
      }
      static [entityKind2] = "SQLiteNumericBuilder";
      constructor(name) {
        super(name, "string", "SQLiteNumeric");
      }
      /** @internal */
      build(table) {
        return new SQLiteNumeric2(
          table,
          this.config
        );
      }
    };
    SQLiteNumeric2 = class extends SQLiteColumn2 {
      static {
        __name(this, "SQLiteNumeric");
      }
      static [entityKind2] = "SQLiteNumeric";
      getSQLType() {
        return "numeric";
      }
    };
    __name(numeric2, "numeric");
  }
});

// node_modules/drizzle-orm/sqlite-core/columns/real.js
function real2(name) {
  return new SQLiteRealBuilder2(name ?? "");
}
var SQLiteRealBuilder2, SQLiteReal2;
var init_real2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/columns/real.js"() {
    init_entity2();
    init_common4();
    SQLiteRealBuilder2 = class extends SQLiteColumnBuilder2 {
      static {
        __name(this, "SQLiteRealBuilder");
      }
      static [entityKind2] = "SQLiteRealBuilder";
      constructor(name) {
        super(name, "number", "SQLiteReal");
      }
      /** @internal */
      build(table) {
        return new SQLiteReal2(table, this.config);
      }
    };
    SQLiteReal2 = class extends SQLiteColumn2 {
      static {
        __name(this, "SQLiteReal");
      }
      static [entityKind2] = "SQLiteReal";
      getSQLType() {
        return "real";
      }
    };
    __name(real2, "real");
  }
});

// node_modules/drizzle-orm/sqlite-core/columns/text.js
function text2(a, b = {}) {
  const { name, config } = getColumnNameAndConfig2(a, b);
  if (config.mode === "json") {
    return new SQLiteTextJsonBuilder2(name);
  }
  return new SQLiteTextBuilder2(name, config);
}
var SQLiteTextBuilder2, SQLiteText2, SQLiteTextJsonBuilder2, SQLiteTextJson2;
var init_text2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/columns/text.js"() {
    init_entity2();
    init_utils3();
    init_common4();
    SQLiteTextBuilder2 = class extends SQLiteColumnBuilder2 {
      static {
        __name(this, "SQLiteTextBuilder");
      }
      static [entityKind2] = "SQLiteTextBuilder";
      constructor(name, config) {
        super(name, "string", "SQLiteText");
        this.config.enumValues = config.enum;
        this.config.length = config.length;
      }
      /** @internal */
      build(table) {
        return new SQLiteText2(
          table,
          this.config
        );
      }
    };
    SQLiteText2 = class extends SQLiteColumn2 {
      static {
        __name(this, "SQLiteText");
      }
      static [entityKind2] = "SQLiteText";
      enumValues = this.config.enumValues;
      length = this.config.length;
      constructor(table, config) {
        super(table, config);
      }
      getSQLType() {
        return `text${this.config.length ? `(${this.config.length})` : ""}`;
      }
    };
    SQLiteTextJsonBuilder2 = class extends SQLiteColumnBuilder2 {
      static {
        __name(this, "SQLiteTextJsonBuilder");
      }
      static [entityKind2] = "SQLiteTextJsonBuilder";
      constructor(name) {
        super(name, "json", "SQLiteTextJson");
      }
      /** @internal */
      build(table) {
        return new SQLiteTextJson2(
          table,
          this.config
        );
      }
    };
    SQLiteTextJson2 = class extends SQLiteColumn2 {
      static {
        __name(this, "SQLiteTextJson");
      }
      static [entityKind2] = "SQLiteTextJson";
      getSQLType() {
        return "text";
      }
      mapFromDriverValue(value) {
        return JSON.parse(value);
      }
      mapToDriverValue(value) {
        return JSON.stringify(value);
      }
    };
    __name(text2, "text");
  }
});

// node_modules/drizzle-orm/sqlite-core/columns/index.js
var init_columns2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/columns/index.js"() {
    init_blob2();
    init_common4();
    init_custom2();
    init_integer2();
    init_numeric2();
    init_real2();
    init_text2();
  }
});

// node_modules/drizzle-orm/selection-proxy.js
var SelectionProxyHandler2;
var init_selection_proxy2 = __esm({
  "node_modules/drizzle-orm/selection-proxy.js"() {
    init_alias3();
    init_column2();
    init_entity2();
    init_sql4();
    init_subquery3();
    init_view_common2();
    SelectionProxyHandler2 = class _SelectionProxyHandler {
      static {
        __name(this, "SelectionProxyHandler");
      }
      static [entityKind2] = "SelectionProxyHandler";
      config;
      constructor(config) {
        this.config = { ...config };
      }
      get(subquery, prop) {
        if (prop === "_") {
          return {
            ...subquery["_"],
            selectedFields: new Proxy(
              subquery._.selectedFields,
              this
            )
          };
        }
        if (prop === ViewBaseConfig2) {
          return {
            ...subquery[ViewBaseConfig2],
            selectedFields: new Proxy(
              subquery[ViewBaseConfig2].selectedFields,
              this
            )
          };
        }
        if (typeof prop === "symbol") {
          return subquery[prop];
        }
        const columns = is2(subquery, Subquery2) ? subquery._.selectedFields : is2(subquery, View2) ? subquery[ViewBaseConfig2].selectedFields : subquery;
        const value = columns[prop];
        if (is2(value, SQL2.Aliased)) {
          if (this.config.sqlAliasedBehavior === "sql" && !value.isSelectionField) {
            return value.sql;
          }
          const newValue = value.clone();
          newValue.isSelectionField = true;
          return newValue;
        }
        if (is2(value, SQL2)) {
          if (this.config.sqlBehavior === "sql") {
            return value;
          }
          throw new Error(
            `You tried to reference "${prop}" field from a subquery, which is a raw SQL field, but it doesn't have an alias declared. Please add an alias to the field using ".as('alias')" method.`
          );
        }
        if (is2(value, Column2)) {
          if (this.config.alias) {
            return new Proxy(
              value,
              new ColumnAliasProxyHandler2(
                new Proxy(
                  value.table,
                  new TableAliasProxyHandler2(this.config.alias, this.config.replaceOriginalName ?? false)
                )
              )
            );
          }
          return value;
        }
        if (typeof value !== "object" || value === null) {
          return value;
        }
        return new Proxy(value, new _SelectionProxyHandler(this.config));
      }
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/columns/all.js
function getSQLiteColumnBuilders2() {
  return {
    blob: blob2,
    customType: customType2,
    integer: integer2,
    numeric: numeric2,
    real: real2,
    text: text2
  };
}
var init_all2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/columns/all.js"() {
    init_blob2();
    init_custom2();
    init_integer2();
    init_numeric2();
    init_real2();
    init_text2();
    __name(getSQLiteColumnBuilders2, "getSQLiteColumnBuilders");
  }
});

// node_modules/drizzle-orm/sqlite-core/table.js
function sqliteTableBase2(name, columns, extraConfig, schema, baseName = name) {
  const rawTable = new SQLiteTable2(name, schema, baseName);
  const parsedColumns = typeof columns === "function" ? columns(getSQLiteColumnBuilders2()) : columns;
  const builtColumns = Object.fromEntries(
    Object.entries(parsedColumns).map(([name2, colBuilderBase]) => {
      const colBuilder = colBuilderBase;
      colBuilder.setName(name2);
      const column = colBuilder.build(rawTable);
      rawTable[InlineForeignKeys3].push(...colBuilder.buildForeignKeys(column, rawTable));
      return [name2, column];
    })
  );
  const table = Object.assign(rawTable, builtColumns);
  table[Table2.Symbol.Columns] = builtColumns;
  table[Table2.Symbol.ExtraConfigColumns] = builtColumns;
  if (extraConfig) {
    table[SQLiteTable2.Symbol.ExtraConfigBuilder] = extraConfig;
  }
  return table;
}
var InlineForeignKeys3, SQLiteTable2, sqliteTable2;
var init_table5 = __esm({
  "node_modules/drizzle-orm/sqlite-core/table.js"() {
    init_entity2();
    init_table4();
    init_all2();
    InlineForeignKeys3 = Symbol.for("drizzle:SQLiteInlineForeignKeys");
    SQLiteTable2 = class extends Table2 {
      static {
        __name(this, "SQLiteTable");
      }
      static [entityKind2] = "SQLiteTable";
      /** @internal */
      static Symbol = Object.assign({}, Table2.Symbol, {
        InlineForeignKeys: InlineForeignKeys3
      });
      /** @internal */
      [Table2.Symbol.Columns];
      /** @internal */
      [InlineForeignKeys3] = [];
      /** @internal */
      [Table2.Symbol.ExtraConfigBuilder] = void 0;
    };
    __name(sqliteTableBase2, "sqliteTableBase");
    sqliteTable2 = /* @__PURE__ */ __name((name, columns, extraConfig) => {
      return sqliteTableBase2(name, columns, extraConfig);
    }, "sqliteTable");
  }
});

// node_modules/drizzle-orm/sqlite-core/query-builders/delete.js
var SQLiteDeleteBase2;
var init_delete2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/query-builders/delete.js"() {
    init_entity2();
    init_query_promise2();
    init_selection_proxy2();
    init_table5();
    init_table4();
    init_utils3();
    SQLiteDeleteBase2 = class extends QueryPromise2 {
      static {
        __name(this, "SQLiteDeleteBase");
      }
      constructor(table, session, dialect, withList) {
        super();
        this.table = table;
        this.session = session;
        this.dialect = dialect;
        this.config = { table, withList };
      }
      static [entityKind2] = "SQLiteDelete";
      /** @internal */
      config;
      /**
       * Adds a `where` clause to the query.
       *
       * Calling this method will delete only those rows that fulfill a specified condition.
       *
       * See docs: {@link https://orm.drizzle.team/docs/delete}
       *
       * @param where the `where` clause.
       *
       * @example
       * You can use conditional operators and `sql function` to filter the rows to be deleted.
       *
       * ```ts
       * // Delete all cars with green color
       * db.delete(cars).where(eq(cars.color, 'green'));
       * // or
       * db.delete(cars).where(sql`${cars.color} = 'green'`)
       * ```
       *
       * You can logically combine conditional operators with `and()` and `or()` operators:
       *
       * ```ts
       * // Delete all BMW cars with a green color
       * db.delete(cars).where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
       *
       * // Delete all cars with the green or blue color
       * db.delete(cars).where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
       * ```
       */
      where(where) {
        this.config.where = where;
        return this;
      }
      orderBy(...columns) {
        if (typeof columns[0] === "function") {
          const orderBy = columns[0](
            new Proxy(
              this.config.table[Table2.Symbol.Columns],
              new SelectionProxyHandler2({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })
            )
          );
          const orderByArray = Array.isArray(orderBy) ? orderBy : [orderBy];
          this.config.orderBy = orderByArray;
        } else {
          const orderByArray = columns;
          this.config.orderBy = orderByArray;
        }
        return this;
      }
      limit(limit) {
        this.config.limit = limit;
        return this;
      }
      returning(fields = this.table[SQLiteTable2.Symbol.Columns]) {
        this.config.returning = orderSelectedFields2(fields);
        return this;
      }
      /** @internal */
      getSQL() {
        return this.dialect.buildDeleteQuery(this.config);
      }
      toSQL() {
        const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
        return rest;
      }
      /** @internal */
      _prepare(isOneTimeQuery = true) {
        return this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](
          this.dialect.sqlToQuery(this.getSQL()),
          this.config.returning,
          this.config.returning ? "all" : "run",
          true
        );
      }
      prepare() {
        return this._prepare(false);
      }
      run = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().run(placeholderValues);
      }, "run");
      all = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().all(placeholderValues);
      }, "all");
      get = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().get(placeholderValues);
      }, "get");
      values = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().values(placeholderValues);
      }, "values");
      async execute(placeholderValues) {
        return this._prepare().execute(placeholderValues);
      }
      $dynamic() {
        return this;
      }
    };
  }
});

// node_modules/drizzle-orm/casing.js
function toSnakeCase2(input) {
  const words = input.replace(/['\u2019]/g, "").match(/[\da-z]+|[A-Z]+(?![a-z])|[A-Z][\da-z]+/g) ?? [];
  return words.map((word) => word.toLowerCase()).join("_");
}
function toCamelCase2(input) {
  const words = input.replace(/['\u2019]/g, "").match(/[\da-z]+|[A-Z]+(?![a-z])|[A-Z][\da-z]+/g) ?? [];
  return words.reduce((acc, word, i) => {
    const formattedWord = i === 0 ? word.toLowerCase() : `${word[0].toUpperCase()}${word.slice(1)}`;
    return acc + formattedWord;
  }, "");
}
function noopCase2(input) {
  return input;
}
var CasingCache2;
var init_casing2 = __esm({
  "node_modules/drizzle-orm/casing.js"() {
    init_entity2();
    init_table4();
    __name(toSnakeCase2, "toSnakeCase");
    __name(toCamelCase2, "toCamelCase");
    __name(noopCase2, "noopCase");
    CasingCache2 = class {
      static {
        __name(this, "CasingCache");
      }
      static [entityKind2] = "CasingCache";
      /** @internal */
      cache = {};
      cachedTables = {};
      convert;
      constructor(casing) {
        this.convert = casing === "snake_case" ? toSnakeCase2 : casing === "camelCase" ? toCamelCase2 : noopCase2;
      }
      getColumnCasing(column) {
        if (!column.keyAsName)
          return column.name;
        const schema = column.table[Table2.Symbol.Schema] ?? "public";
        const tableName = column.table[Table2.Symbol.OriginalName];
        const key = `${schema}.${tableName}.${column.name}`;
        if (!this.cache[key]) {
          this.cacheTable(column.table);
        }
        return this.cache[key];
      }
      cacheTable(table) {
        const schema = table[Table2.Symbol.Schema] ?? "public";
        const tableName = table[Table2.Symbol.OriginalName];
        const tableKey = `${schema}.${tableName}`;
        if (!this.cachedTables[tableKey]) {
          for (const column of Object.values(table[Table2.Symbol.Columns])) {
            const columnKey = `${tableKey}.${column.name}`;
            this.cache[columnKey] = this.convert(column.name);
          }
          this.cachedTables[tableKey] = true;
        }
      }
      clearCache() {
        this.cache = {};
        this.cachedTables = {};
      }
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/view-base.js
var SQLiteViewBase2;
var init_view_base2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/view-base.js"() {
    init_entity2();
    init_sql4();
    SQLiteViewBase2 = class extends View2 {
      static {
        __name(this, "SQLiteViewBase");
      }
      static [entityKind2] = "SQLiteViewBase";
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/dialect.js
var SQLiteDialect2, SQLiteSyncDialect2, SQLiteAsyncDialect2;
var init_dialect2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/dialect.js"() {
    init_alias3();
    init_casing2();
    init_column2();
    init_entity2();
    init_errors3();
    init_relations2();
    init_sql5();
    init_sql4();
    init_columns2();
    init_table5();
    init_subquery3();
    init_table4();
    init_utils3();
    init_view_common2();
    init_view_base2();
    SQLiteDialect2 = class {
      static {
        __name(this, "SQLiteDialect");
      }
      static [entityKind2] = "SQLiteDialect";
      /** @internal */
      casing;
      constructor(config) {
        this.casing = new CasingCache2(config?.casing);
      }
      escapeName(name) {
        return `"${name}"`;
      }
      escapeParam(_num) {
        return "?";
      }
      escapeString(str) {
        return `'${str.replace(/'/g, "''")}'`;
      }
      buildWithCTE(queries) {
        if (!queries?.length)
          return void 0;
        const withSqlChunks = [sql2`with `];
        for (const [i, w] of queries.entries()) {
          withSqlChunks.push(sql2`${sql2.identifier(w._.alias)} as (${w._.sql})`);
          if (i < queries.length - 1) {
            withSqlChunks.push(sql2`, `);
          }
        }
        withSqlChunks.push(sql2` `);
        return sql2.join(withSqlChunks);
      }
      buildDeleteQuery({ table, where, returning, withList, limit, orderBy }) {
        const withSql = this.buildWithCTE(withList);
        const returningSql = returning ? sql2` returning ${this.buildSelection(returning, { isSingleTable: true })}` : void 0;
        const whereSql = where ? sql2` where ${where}` : void 0;
        const orderBySql = this.buildOrderBy(orderBy);
        const limitSql = this.buildLimit(limit);
        return sql2`${withSql}delete from ${table}${whereSql}${returningSql}${orderBySql}${limitSql}`;
      }
      buildUpdateSet(table, set2) {
        const tableColumns = table[Table2.Symbol.Columns];
        const columnNames = Object.keys(tableColumns).filter(
          (colName) => set2[colName] !== void 0 || tableColumns[colName]?.onUpdateFn !== void 0
        );
        const setSize = columnNames.length;
        return sql2.join(columnNames.flatMap((colName, i) => {
          const col = tableColumns[colName];
          const value = set2[colName] ?? sql2.param(col.onUpdateFn(), col);
          const res = sql2`${sql2.identifier(this.casing.getColumnCasing(col))} = ${value}`;
          if (i < setSize - 1) {
            return [res, sql2.raw(", ")];
          }
          return [res];
        }));
      }
      buildUpdateQuery({ table, set: set2, where, returning, withList, joins, from, limit, orderBy }) {
        const withSql = this.buildWithCTE(withList);
        const setSql = this.buildUpdateSet(table, set2);
        const fromSql = from && sql2.join([sql2.raw(" from "), this.buildFromTable(from)]);
        const joinsSql = this.buildJoins(joins);
        const returningSql = returning ? sql2` returning ${this.buildSelection(returning, { isSingleTable: true })}` : void 0;
        const whereSql = where ? sql2` where ${where}` : void 0;
        const orderBySql = this.buildOrderBy(orderBy);
        const limitSql = this.buildLimit(limit);
        return sql2`${withSql}update ${table} set ${setSql}${fromSql}${joinsSql}${whereSql}${returningSql}${orderBySql}${limitSql}`;
      }
      /**
       * Builds selection SQL with provided fields/expressions
       *
       * Examples:
       *
       * `select <selection> from`
       *
       * `insert ... returning <selection>`
       *
       * If `isSingleTable` is true, then columns won't be prefixed with table name
       */
      buildSelection(fields, { isSingleTable = false } = {}) {
        const columnsLen = fields.length;
        const chunks = fields.flatMap(({ field }, i) => {
          const chunk = [];
          if (is2(field, SQL2.Aliased) && field.isSelectionField) {
            chunk.push(sql2.identifier(field.fieldAlias));
          } else if (is2(field, SQL2.Aliased) || is2(field, SQL2)) {
            const query = is2(field, SQL2.Aliased) ? field.sql : field;
            if (isSingleTable) {
              chunk.push(
                new SQL2(
                  query.queryChunks.map((c) => {
                    if (is2(c, Column2)) {
                      return sql2.identifier(this.casing.getColumnCasing(c));
                    }
                    return c;
                  })
                )
              );
            } else {
              chunk.push(query);
            }
            if (is2(field, SQL2.Aliased)) {
              chunk.push(sql2` as ${sql2.identifier(field.fieldAlias)}`);
            }
          } else if (is2(field, Column2)) {
            const tableName = field.table[Table2.Symbol.Name];
            if (isSingleTable) {
              chunk.push(sql2.identifier(this.casing.getColumnCasing(field)));
            } else {
              chunk.push(sql2`${sql2.identifier(tableName)}.${sql2.identifier(this.casing.getColumnCasing(field))}`);
            }
          }
          if (i < columnsLen - 1) {
            chunk.push(sql2`, `);
          }
          return chunk;
        });
        return sql2.join(chunks);
      }
      buildJoins(joins) {
        if (!joins || joins.length === 0) {
          return void 0;
        }
        const joinsArray = [];
        if (joins) {
          for (const [index, joinMeta] of joins.entries()) {
            if (index === 0) {
              joinsArray.push(sql2` `);
            }
            const table = joinMeta.table;
            if (is2(table, SQLiteTable2)) {
              const tableName = table[SQLiteTable2.Symbol.Name];
              const tableSchema = table[SQLiteTable2.Symbol.Schema];
              const origTableName = table[SQLiteTable2.Symbol.OriginalName];
              const alias = tableName === origTableName ? void 0 : joinMeta.alias;
              joinsArray.push(
                sql2`${sql2.raw(joinMeta.joinType)} join ${tableSchema ? sql2`${sql2.identifier(tableSchema)}.` : void 0}${sql2.identifier(origTableName)}${alias && sql2` ${sql2.identifier(alias)}`} on ${joinMeta.on}`
              );
            } else {
              joinsArray.push(
                sql2`${sql2.raw(joinMeta.joinType)} join ${table} on ${joinMeta.on}`
              );
            }
            if (index < joins.length - 1) {
              joinsArray.push(sql2` `);
            }
          }
        }
        return sql2.join(joinsArray);
      }
      buildLimit(limit) {
        return typeof limit === "object" || typeof limit === "number" && limit >= 0 ? sql2` limit ${limit}` : void 0;
      }
      buildOrderBy(orderBy) {
        const orderByList = [];
        if (orderBy) {
          for (const [index, orderByValue] of orderBy.entries()) {
            orderByList.push(orderByValue);
            if (index < orderBy.length - 1) {
              orderByList.push(sql2`, `);
            }
          }
        }
        return orderByList.length > 0 ? sql2` order by ${sql2.join(orderByList)}` : void 0;
      }
      buildFromTable(table) {
        if (is2(table, Table2) && table[Table2.Symbol.OriginalName] !== table[Table2.Symbol.Name]) {
          return sql2`${sql2.identifier(table[Table2.Symbol.OriginalName])} ${sql2.identifier(table[Table2.Symbol.Name])}`;
        }
        return table;
      }
      buildSelectQuery({
        withList,
        fields,
        fieldsFlat,
        where,
        having,
        table,
        joins,
        orderBy,
        groupBy,
        limit,
        offset,
        distinct,
        setOperators
      }) {
        const fieldsList = fieldsFlat ?? orderSelectedFields2(fields);
        for (const f of fieldsList) {
          if (is2(f.field, Column2) && getTableName2(f.field.table) !== (is2(table, Subquery2) ? table._.alias : is2(table, SQLiteViewBase2) ? table[ViewBaseConfig2].name : is2(table, SQL2) ? void 0 : getTableName2(table)) && !((table2) => joins?.some(
            ({ alias }) => alias === (table2[Table2.Symbol.IsAlias] ? getTableName2(table2) : table2[Table2.Symbol.BaseName])
          ))(f.field.table)) {
            const tableName = getTableName2(f.field.table);
            throw new Error(
              `Your "${f.path.join("->")}" field references a column "${tableName}"."${f.field.name}", but the table "${tableName}" is not part of the query! Did you forget to join it?`
            );
          }
        }
        const isSingleTable = !joins || joins.length === 0;
        const withSql = this.buildWithCTE(withList);
        const distinctSql = distinct ? sql2` distinct` : void 0;
        const selection = this.buildSelection(fieldsList, { isSingleTable });
        const tableSql = this.buildFromTable(table);
        const joinsSql = this.buildJoins(joins);
        const whereSql = where ? sql2` where ${where}` : void 0;
        const havingSql = having ? sql2` having ${having}` : void 0;
        const groupByList = [];
        if (groupBy) {
          for (const [index, groupByValue] of groupBy.entries()) {
            groupByList.push(groupByValue);
            if (index < groupBy.length - 1) {
              groupByList.push(sql2`, `);
            }
          }
        }
        const groupBySql = groupByList.length > 0 ? sql2` group by ${sql2.join(groupByList)}` : void 0;
        const orderBySql = this.buildOrderBy(orderBy);
        const limitSql = this.buildLimit(limit);
        const offsetSql = offset ? sql2` offset ${offset}` : void 0;
        const finalQuery = sql2`${withSql}select${distinctSql} ${selection} from ${tableSql}${joinsSql}${whereSql}${groupBySql}${havingSql}${orderBySql}${limitSql}${offsetSql}`;
        if (setOperators.length > 0) {
          return this.buildSetOperations(finalQuery, setOperators);
        }
        return finalQuery;
      }
      buildSetOperations(leftSelect, setOperators) {
        const [setOperator, ...rest] = setOperators;
        if (!setOperator) {
          throw new Error("Cannot pass undefined values to any set operator");
        }
        if (rest.length === 0) {
          return this.buildSetOperationQuery({ leftSelect, setOperator });
        }
        return this.buildSetOperations(
          this.buildSetOperationQuery({ leftSelect, setOperator }),
          rest
        );
      }
      buildSetOperationQuery({
        leftSelect,
        setOperator: { type, isAll, rightSelect, limit, orderBy, offset }
      }) {
        const leftChunk = sql2`${leftSelect.getSQL()} `;
        const rightChunk = sql2`${rightSelect.getSQL()}`;
        let orderBySql;
        if (orderBy && orderBy.length > 0) {
          const orderByValues = [];
          for (const singleOrderBy of orderBy) {
            if (is2(singleOrderBy, SQLiteColumn2)) {
              orderByValues.push(sql2.identifier(singleOrderBy.name));
            } else if (is2(singleOrderBy, SQL2)) {
              for (let i = 0; i < singleOrderBy.queryChunks.length; i++) {
                const chunk = singleOrderBy.queryChunks[i];
                if (is2(chunk, SQLiteColumn2)) {
                  singleOrderBy.queryChunks[i] = sql2.identifier(this.casing.getColumnCasing(chunk));
                }
              }
              orderByValues.push(sql2`${singleOrderBy}`);
            } else {
              orderByValues.push(sql2`${singleOrderBy}`);
            }
          }
          orderBySql = sql2` order by ${sql2.join(orderByValues, sql2`, `)}`;
        }
        const limitSql = typeof limit === "object" || typeof limit === "number" && limit >= 0 ? sql2` limit ${limit}` : void 0;
        const operatorChunk = sql2.raw(`${type} ${isAll ? "all " : ""}`);
        const offsetSql = offset ? sql2` offset ${offset}` : void 0;
        return sql2`${leftChunk}${operatorChunk}${rightChunk}${orderBySql}${limitSql}${offsetSql}`;
      }
      buildInsertQuery({ table, values: valuesOrSelect, onConflict, returning, withList, select }) {
        const valuesSqlList = [];
        const columns = table[Table2.Symbol.Columns];
        const colEntries = Object.entries(columns).filter(
          ([_, col]) => !col.shouldDisableInsert()
        );
        const insertOrder = colEntries.map(([, column]) => sql2.identifier(this.casing.getColumnCasing(column)));
        if (select) {
          const select2 = valuesOrSelect;
          if (is2(select2, SQL2)) {
            valuesSqlList.push(select2);
          } else {
            valuesSqlList.push(select2.getSQL());
          }
        } else {
          const values = valuesOrSelect;
          valuesSqlList.push(sql2.raw("values "));
          for (const [valueIndex, value] of values.entries()) {
            const valueList = [];
            for (const [fieldName, col] of colEntries) {
              const colValue = value[fieldName];
              if (colValue === void 0 || is2(colValue, Param2) && colValue.value === void 0) {
                let defaultValue;
                if (col.default !== null && col.default !== void 0) {
                  defaultValue = is2(col.default, SQL2) ? col.default : sql2.param(col.default, col);
                } else if (col.defaultFn !== void 0) {
                  const defaultFnResult = col.defaultFn();
                  defaultValue = is2(defaultFnResult, SQL2) ? defaultFnResult : sql2.param(defaultFnResult, col);
                } else if (!col.default && col.onUpdateFn !== void 0) {
                  const onUpdateFnResult = col.onUpdateFn();
                  defaultValue = is2(onUpdateFnResult, SQL2) ? onUpdateFnResult : sql2.param(onUpdateFnResult, col);
                } else {
                  defaultValue = sql2`null`;
                }
                valueList.push(defaultValue);
              } else {
                valueList.push(colValue);
              }
            }
            valuesSqlList.push(valueList);
            if (valueIndex < values.length - 1) {
              valuesSqlList.push(sql2`, `);
            }
          }
        }
        const withSql = this.buildWithCTE(withList);
        const valuesSql = sql2.join(valuesSqlList);
        const returningSql = returning ? sql2` returning ${this.buildSelection(returning, { isSingleTable: true })}` : void 0;
        const onConflictSql = onConflict?.length ? sql2.join(onConflict) : void 0;
        return sql2`${withSql}insert into ${table} ${insertOrder} ${valuesSql}${onConflictSql}${returningSql}`;
      }
      sqlToQuery(sql22, invokeSource) {
        return sql22.toQuery({
          casing: this.casing,
          escapeName: this.escapeName,
          escapeParam: this.escapeParam,
          escapeString: this.escapeString,
          invokeSource
        });
      }
      buildRelationalQuery({
        fullSchema,
        schema,
        tableNamesMap,
        table,
        tableConfig,
        queryConfig: config,
        tableAlias,
        nestedQueryRelation,
        joinOn
      }) {
        let selection = [];
        let limit, offset, orderBy = [], where;
        const joins = [];
        if (config === true) {
          const selectionEntries = Object.entries(tableConfig.columns);
          selection = selectionEntries.map(([key, value]) => ({
            dbKey: value.name,
            tsKey: key,
            field: aliasedTableColumn2(value, tableAlias),
            relationTableTsKey: void 0,
            isJson: false,
            selection: []
          }));
        } else {
          const aliasedColumns = Object.fromEntries(
            Object.entries(tableConfig.columns).map(([key, value]) => [key, aliasedTableColumn2(value, tableAlias)])
          );
          if (config.where) {
            const whereSql = typeof config.where === "function" ? config.where(aliasedColumns, getOperators2()) : config.where;
            where = whereSql && mapColumnsInSQLToAlias2(whereSql, tableAlias);
          }
          const fieldsSelection = [];
          let selectedColumns = [];
          if (config.columns) {
            let isIncludeMode = false;
            for (const [field, value] of Object.entries(config.columns)) {
              if (value === void 0) {
                continue;
              }
              if (field in tableConfig.columns) {
                if (!isIncludeMode && value === true) {
                  isIncludeMode = true;
                }
                selectedColumns.push(field);
              }
            }
            if (selectedColumns.length > 0) {
              selectedColumns = isIncludeMode ? selectedColumns.filter((c) => config.columns?.[c] === true) : Object.keys(tableConfig.columns).filter((key) => !selectedColumns.includes(key));
            }
          } else {
            selectedColumns = Object.keys(tableConfig.columns);
          }
          for (const field of selectedColumns) {
            const column = tableConfig.columns[field];
            fieldsSelection.push({ tsKey: field, value: column });
          }
          let selectedRelations = [];
          if (config.with) {
            selectedRelations = Object.entries(config.with).filter((entry) => !!entry[1]).map(([tsKey, queryConfig]) => ({ tsKey, queryConfig, relation: tableConfig.relations[tsKey] }));
          }
          let extras;
          if (config.extras) {
            extras = typeof config.extras === "function" ? config.extras(aliasedColumns, { sql: sql2 }) : config.extras;
            for (const [tsKey, value] of Object.entries(extras)) {
              fieldsSelection.push({
                tsKey,
                value: mapColumnsInAliasedSQLToAlias2(value, tableAlias)
              });
            }
          }
          for (const { tsKey, value } of fieldsSelection) {
            selection.push({
              dbKey: is2(value, SQL2.Aliased) ? value.fieldAlias : tableConfig.columns[tsKey].name,
              tsKey,
              field: is2(value, Column2) ? aliasedTableColumn2(value, tableAlias) : value,
              relationTableTsKey: void 0,
              isJson: false,
              selection: []
            });
          }
          let orderByOrig = typeof config.orderBy === "function" ? config.orderBy(aliasedColumns, getOrderByOperators2()) : config.orderBy ?? [];
          if (!Array.isArray(orderByOrig)) {
            orderByOrig = [orderByOrig];
          }
          orderBy = orderByOrig.map((orderByValue) => {
            if (is2(orderByValue, Column2)) {
              return aliasedTableColumn2(orderByValue, tableAlias);
            }
            return mapColumnsInSQLToAlias2(orderByValue, tableAlias);
          });
          limit = config.limit;
          offset = config.offset;
          for (const {
            tsKey: selectedRelationTsKey,
            queryConfig: selectedRelationConfigValue,
            relation
          } of selectedRelations) {
            const normalizedRelation = normalizeRelation2(schema, tableNamesMap, relation);
            const relationTableName = getTableUniqueName2(relation.referencedTable);
            const relationTableTsName = tableNamesMap[relationTableName];
            const relationTableAlias = `${tableAlias}_${selectedRelationTsKey}`;
            const joinOn2 = and2(
              ...normalizedRelation.fields.map(
                (field2, i) => eq2(
                  aliasedTableColumn2(normalizedRelation.references[i], relationTableAlias),
                  aliasedTableColumn2(field2, tableAlias)
                )
              )
            );
            const builtRelation = this.buildRelationalQuery({
              fullSchema,
              schema,
              tableNamesMap,
              table: fullSchema[relationTableTsName],
              tableConfig: schema[relationTableTsName],
              queryConfig: is2(relation, One2) ? selectedRelationConfigValue === true ? { limit: 1 } : { ...selectedRelationConfigValue, limit: 1 } : selectedRelationConfigValue,
              tableAlias: relationTableAlias,
              joinOn: joinOn2,
              nestedQueryRelation: relation
            });
            const field = sql2`(${builtRelation.sql})`.as(selectedRelationTsKey);
            selection.push({
              dbKey: selectedRelationTsKey,
              tsKey: selectedRelationTsKey,
              field,
              relationTableTsKey: relationTableTsName,
              isJson: true,
              selection: builtRelation.selection
            });
          }
        }
        if (selection.length === 0) {
          throw new DrizzleError2({
            message: `No fields selected for table "${tableConfig.tsName}" ("${tableAlias}"). You need to have at least one item in "columns", "with" or "extras". If you need to select all columns, omit the "columns" key or set it to undefined.`
          });
        }
        let result;
        where = and2(joinOn, where);
        if (nestedQueryRelation) {
          let field = sql2`json_array(${sql2.join(
            selection.map(
              ({ field: field2 }) => is2(field2, SQLiteColumn2) ? sql2.identifier(this.casing.getColumnCasing(field2)) : is2(field2, SQL2.Aliased) ? field2.sql : field2
            ),
            sql2`, `
          )})`;
          if (is2(nestedQueryRelation, Many2)) {
            field = sql2`coalesce(json_group_array(${field}), json_array())`;
          }
          const nestedSelection = [{
            dbKey: "data",
            tsKey: "data",
            field: field.as("data"),
            isJson: true,
            relationTableTsKey: tableConfig.tsName,
            selection
          }];
          const needsSubquery = limit !== void 0 || offset !== void 0 || orderBy.length > 0;
          if (needsSubquery) {
            result = this.buildSelectQuery({
              table: aliasedTable2(table, tableAlias),
              fields: {},
              fieldsFlat: [
                {
                  path: [],
                  field: sql2.raw("*")
                }
              ],
              where,
              limit,
              offset,
              orderBy,
              setOperators: []
            });
            where = void 0;
            limit = void 0;
            offset = void 0;
            orderBy = void 0;
          } else {
            result = aliasedTable2(table, tableAlias);
          }
          result = this.buildSelectQuery({
            table: is2(result, SQLiteTable2) ? result : new Subquery2(result, {}, tableAlias),
            fields: {},
            fieldsFlat: nestedSelection.map(({ field: field2 }) => ({
              path: [],
              field: is2(field2, Column2) ? aliasedTableColumn2(field2, tableAlias) : field2
            })),
            joins,
            where,
            limit,
            offset,
            orderBy,
            setOperators: []
          });
        } else {
          result = this.buildSelectQuery({
            table: aliasedTable2(table, tableAlias),
            fields: {},
            fieldsFlat: selection.map(({ field }) => ({
              path: [],
              field: is2(field, Column2) ? aliasedTableColumn2(field, tableAlias) : field
            })),
            joins,
            where,
            limit,
            offset,
            orderBy,
            setOperators: []
          });
        }
        return {
          tableTsKey: tableConfig.tsName,
          sql: result,
          selection
        };
      }
    };
    SQLiteSyncDialect2 = class extends SQLiteDialect2 {
      static {
        __name(this, "SQLiteSyncDialect");
      }
      static [entityKind2] = "SQLiteSyncDialect";
      migrate(migrations, session, config) {
        const migrationsTable = config === void 0 ? "__drizzle_migrations" : typeof config === "string" ? "__drizzle_migrations" : config.migrationsTable ?? "__drizzle_migrations";
        const migrationTableCreate = sql2`
			CREATE TABLE IF NOT EXISTS ${sql2.identifier(migrationsTable)} (
				id SERIAL PRIMARY KEY,
				hash text NOT NULL,
				created_at numeric
			)
		`;
        session.run(migrationTableCreate);
        const dbMigrations = session.values(
          sql2`SELECT id, hash, created_at FROM ${sql2.identifier(migrationsTable)} ORDER BY created_at DESC LIMIT 1`
        );
        const lastDbMigration = dbMigrations[0] ?? void 0;
        session.run(sql2`BEGIN`);
        try {
          for (const migration of migrations) {
            if (!lastDbMigration || Number(lastDbMigration[2]) < migration.folderMillis) {
              for (const stmt of migration.sql) {
                session.run(sql2.raw(stmt));
              }
              session.run(
                sql2`INSERT INTO ${sql2.identifier(migrationsTable)} ("hash", "created_at") VALUES(${migration.hash}, ${migration.folderMillis})`
              );
            }
          }
          session.run(sql2`COMMIT`);
        } catch (e) {
          session.run(sql2`ROLLBACK`);
          throw e;
        }
      }
    };
    SQLiteAsyncDialect2 = class extends SQLiteDialect2 {
      static {
        __name(this, "SQLiteAsyncDialect");
      }
      static [entityKind2] = "SQLiteAsyncDialect";
      async migrate(migrations, session, config) {
        const migrationsTable = config === void 0 ? "__drizzle_migrations" : typeof config === "string" ? "__drizzle_migrations" : config.migrationsTable ?? "__drizzle_migrations";
        const migrationTableCreate = sql2`
			CREATE TABLE IF NOT EXISTS ${sql2.identifier(migrationsTable)} (
				id SERIAL PRIMARY KEY,
				hash text NOT NULL,
				created_at numeric
			)
		`;
        await session.run(migrationTableCreate);
        const dbMigrations = await session.values(
          sql2`SELECT id, hash, created_at FROM ${sql2.identifier(migrationsTable)} ORDER BY created_at DESC LIMIT 1`
        );
        const lastDbMigration = dbMigrations[0] ?? void 0;
        await session.transaction(async (tx) => {
          for (const migration of migrations) {
            if (!lastDbMigration || Number(lastDbMigration[2]) < migration.folderMillis) {
              for (const stmt of migration.sql) {
                await tx.run(sql2.raw(stmt));
              }
              await tx.run(
                sql2`INSERT INTO ${sql2.identifier(migrationsTable)} ("hash", "created_at") VALUES(${migration.hash}, ${migration.folderMillis})`
              );
            }
          }
        });
      }
    };
  }
});

// node_modules/drizzle-orm/query-builders/query-builder.js
var TypedQueryBuilder2;
var init_query_builder3 = __esm({
  "node_modules/drizzle-orm/query-builders/query-builder.js"() {
    init_entity2();
    TypedQueryBuilder2 = class {
      static {
        __name(this, "TypedQueryBuilder");
      }
      static [entityKind2] = "TypedQueryBuilder";
      /** @internal */
      getSelectedFields() {
        return this._.selectedFields;
      }
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/query-builders/select.js
function createSetOperator2(type, isAll) {
  return (leftSelect, rightSelect, ...restSelects) => {
    const setOperators = [rightSelect, ...restSelects].map((select) => ({
      type,
      isAll,
      rightSelect: select
    }));
    for (const setOperator of setOperators) {
      if (!haveSameKeys2(leftSelect.getSelectedFields(), setOperator.rightSelect.getSelectedFields())) {
        throw new Error(
          "Set operator error (union / intersect / except): selected fields are not the same or are in a different order"
        );
      }
    }
    return leftSelect.addSetOperators(setOperators);
  };
}
var SQLiteSelectBuilder2, SQLiteSelectQueryBuilderBase2, SQLiteSelectBase2, getSQLiteSetOperators2, union2, unionAll2, intersect2, except2;
var init_select4 = __esm({
  "node_modules/drizzle-orm/sqlite-core/query-builders/select.js"() {
    init_entity2();
    init_query_builder3();
    init_query_promise2();
    init_selection_proxy2();
    init_sql4();
    init_subquery3();
    init_table4();
    init_utils3();
    init_view_common2();
    init_view_base2();
    SQLiteSelectBuilder2 = class {
      static {
        __name(this, "SQLiteSelectBuilder");
      }
      static [entityKind2] = "SQLiteSelectBuilder";
      fields;
      session;
      dialect;
      withList;
      distinct;
      constructor(config) {
        this.fields = config.fields;
        this.session = config.session;
        this.dialect = config.dialect;
        this.withList = config.withList;
        this.distinct = config.distinct;
      }
      from(source) {
        const isPartialSelect = !!this.fields;
        let fields;
        if (this.fields) {
          fields = this.fields;
        } else if (is2(source, Subquery2)) {
          fields = Object.fromEntries(
            Object.keys(source._.selectedFields).map((key) => [key, source[key]])
          );
        } else if (is2(source, SQLiteViewBase2)) {
          fields = source[ViewBaseConfig2].selectedFields;
        } else if (is2(source, SQL2)) {
          fields = {};
        } else {
          fields = getTableColumns2(source);
        }
        return new SQLiteSelectBase2({
          table: source,
          fields,
          isPartialSelect,
          session: this.session,
          dialect: this.dialect,
          withList: this.withList,
          distinct: this.distinct
        });
      }
    };
    SQLiteSelectQueryBuilderBase2 = class extends TypedQueryBuilder2 {
      static {
        __name(this, "SQLiteSelectQueryBuilderBase");
      }
      static [entityKind2] = "SQLiteSelectQueryBuilder";
      _;
      /** @internal */
      config;
      joinsNotNullableMap;
      tableName;
      isPartialSelect;
      session;
      dialect;
      constructor({ table, fields, isPartialSelect, session, dialect, withList, distinct }) {
        super();
        this.config = {
          withList,
          table,
          fields: { ...fields },
          distinct,
          setOperators: []
        };
        this.isPartialSelect = isPartialSelect;
        this.session = session;
        this.dialect = dialect;
        this._ = {
          selectedFields: fields
        };
        this.tableName = getTableLikeName2(table);
        this.joinsNotNullableMap = typeof this.tableName === "string" ? { [this.tableName]: true } : {};
      }
      createJoin(joinType) {
        return (table, on) => {
          const baseTableName = this.tableName;
          const tableName = getTableLikeName2(table);
          if (typeof tableName === "string" && this.config.joins?.some((join) => join.alias === tableName)) {
            throw new Error(`Alias "${tableName}" is already used in this query`);
          }
          if (!this.isPartialSelect) {
            if (Object.keys(this.joinsNotNullableMap).length === 1 && typeof baseTableName === "string") {
              this.config.fields = {
                [baseTableName]: this.config.fields
              };
            }
            if (typeof tableName === "string" && !is2(table, SQL2)) {
              const selection = is2(table, Subquery2) ? table._.selectedFields : is2(table, View2) ? table[ViewBaseConfig2].selectedFields : table[Table2.Symbol.Columns];
              this.config.fields[tableName] = selection;
            }
          }
          if (typeof on === "function") {
            on = on(
              new Proxy(
                this.config.fields,
                new SelectionProxyHandler2({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
              )
            );
          }
          if (!this.config.joins) {
            this.config.joins = [];
          }
          this.config.joins.push({ on, table, joinType, alias: tableName });
          if (typeof tableName === "string") {
            switch (joinType) {
              case "left": {
                this.joinsNotNullableMap[tableName] = false;
                break;
              }
              case "right": {
                this.joinsNotNullableMap = Object.fromEntries(
                  Object.entries(this.joinsNotNullableMap).map(([key]) => [key, false])
                );
                this.joinsNotNullableMap[tableName] = true;
                break;
              }
              case "inner": {
                this.joinsNotNullableMap[tableName] = true;
                break;
              }
              case "full": {
                this.joinsNotNullableMap = Object.fromEntries(
                  Object.entries(this.joinsNotNullableMap).map(([key]) => [key, false])
                );
                this.joinsNotNullableMap[tableName] = false;
                break;
              }
            }
          }
          return this;
        };
      }
      /**
       * Executes a `left join` operation by adding another table to the current query.
       *
       * Calling this method associates each row of the table with the corresponding row from the joined table, if a match is found. If no matching row exists, it sets all columns of the joined table to null.
       *
       * See docs: {@link https://orm.drizzle.team/docs/joins#left-join}
       *
       * @param table the table to join.
       * @param on the `on` clause.
       *
       * @example
       *
       * ```ts
       * // Select all users and their pets
       * const usersWithPets: { user: User; pets: Pet | null }[] = await db.select()
       *   .from(users)
       *   .leftJoin(pets, eq(users.id, pets.ownerId))
       *
       * // Select userId and petId
       * const usersIdsAndPetIds: { userId: number; petId: number | null }[] = await db.select({
       *   userId: users.id,
       *   petId: pets.id,
       * })
       *   .from(users)
       *   .leftJoin(pets, eq(users.id, pets.ownerId))
       * ```
       */
      leftJoin = this.createJoin("left");
      /**
       * Executes a `right join` operation by adding another table to the current query.
       *
       * Calling this method associates each row of the joined table with the corresponding row from the main table, if a match is found. If no matching row exists, it sets all columns of the main table to null.
       *
       * See docs: {@link https://orm.drizzle.team/docs/joins#right-join}
       *
       * @param table the table to join.
       * @param on the `on` clause.
       *
       * @example
       *
       * ```ts
       * // Select all users and their pets
       * const usersWithPets: { user: User | null; pets: Pet }[] = await db.select()
       *   .from(users)
       *   .rightJoin(pets, eq(users.id, pets.ownerId))
       *
       * // Select userId and petId
       * const usersIdsAndPetIds: { userId: number | null; petId: number }[] = await db.select({
       *   userId: users.id,
       *   petId: pets.id,
       * })
       *   .from(users)
       *   .rightJoin(pets, eq(users.id, pets.ownerId))
       * ```
       */
      rightJoin = this.createJoin("right");
      /**
       * Executes an `inner join` operation, creating a new table by combining rows from two tables that have matching values.
       *
       * Calling this method retrieves rows that have corresponding entries in both joined tables. Rows without matching entries in either table are excluded, resulting in a table that includes only matching pairs.
       *
       * See docs: {@link https://orm.drizzle.team/docs/joins#inner-join}
       *
       * @param table the table to join.
       * @param on the `on` clause.
       *
       * @example
       *
       * ```ts
       * // Select all users and their pets
       * const usersWithPets: { user: User; pets: Pet }[] = await db.select()
       *   .from(users)
       *   .innerJoin(pets, eq(users.id, pets.ownerId))
       *
       * // Select userId and petId
       * const usersIdsAndPetIds: { userId: number; petId: number }[] = await db.select({
       *   userId: users.id,
       *   petId: pets.id,
       * })
       *   .from(users)
       *   .innerJoin(pets, eq(users.id, pets.ownerId))
       * ```
       */
      innerJoin = this.createJoin("inner");
      /**
       * Executes a `full join` operation by combining rows from two tables into a new table.
       *
       * Calling this method retrieves all rows from both main and joined tables, merging rows with matching values and filling in `null` for non-matching columns.
       *
       * See docs: {@link https://orm.drizzle.team/docs/joins#full-join}
       *
       * @param table the table to join.
       * @param on the `on` clause.
       *
       * @example
       *
       * ```ts
       * // Select all users and their pets
       * const usersWithPets: { user: User | null; pets: Pet | null }[] = await db.select()
       *   .from(users)
       *   .fullJoin(pets, eq(users.id, pets.ownerId))
       *
       * // Select userId and petId
       * const usersIdsAndPetIds: { userId: number | null; petId: number | null }[] = await db.select({
       *   userId: users.id,
       *   petId: pets.id,
       * })
       *   .from(users)
       *   .fullJoin(pets, eq(users.id, pets.ownerId))
       * ```
       */
      fullJoin = this.createJoin("full");
      createSetOperator(type, isAll) {
        return (rightSelection) => {
          const rightSelect = typeof rightSelection === "function" ? rightSelection(getSQLiteSetOperators2()) : rightSelection;
          if (!haveSameKeys2(this.getSelectedFields(), rightSelect.getSelectedFields())) {
            throw new Error(
              "Set operator error (union / intersect / except): selected fields are not the same or are in a different order"
            );
          }
          this.config.setOperators.push({ type, isAll, rightSelect });
          return this;
        };
      }
      /**
       * Adds `union` set operator to the query.
       *
       * Calling this method will combine the result sets of the `select` statements and remove any duplicate rows that appear across them.
       *
       * See docs: {@link https://orm.drizzle.team/docs/set-operations#union}
       *
       * @example
       *
       * ```ts
       * // Select all unique names from customers and users tables
       * await db.select({ name: users.name })
       *   .from(users)
       *   .union(
       *     db.select({ name: customers.name }).from(customers)
       *   );
       * // or
       * import { union } from 'drizzle-orm/sqlite-core'
       *
       * await union(
       *   db.select({ name: users.name }).from(users),
       *   db.select({ name: customers.name }).from(customers)
       * );
       * ```
       */
      union = this.createSetOperator("union", false);
      /**
       * Adds `union all` set operator to the query.
       *
       * Calling this method will combine the result-set of the `select` statements and keep all duplicate rows that appear across them.
       *
       * See docs: {@link https://orm.drizzle.team/docs/set-operations#union-all}
       *
       * @example
       *
       * ```ts
       * // Select all transaction ids from both online and in-store sales
       * await db.select({ transaction: onlineSales.transactionId })
       *   .from(onlineSales)
       *   .unionAll(
       *     db.select({ transaction: inStoreSales.transactionId }).from(inStoreSales)
       *   );
       * // or
       * import { unionAll } from 'drizzle-orm/sqlite-core'
       *
       * await unionAll(
       *   db.select({ transaction: onlineSales.transactionId }).from(onlineSales),
       *   db.select({ transaction: inStoreSales.transactionId }).from(inStoreSales)
       * );
       * ```
       */
      unionAll = this.createSetOperator("union", true);
      /**
       * Adds `intersect` set operator to the query.
       *
       * Calling this method will retain only the rows that are present in both result sets and eliminate duplicates.
       *
       * See docs: {@link https://orm.drizzle.team/docs/set-operations#intersect}
       *
       * @example
       *
       * ```ts
       * // Select course names that are offered in both departments A and B
       * await db.select({ courseName: depA.courseName })
       *   .from(depA)
       *   .intersect(
       *     db.select({ courseName: depB.courseName }).from(depB)
       *   );
       * // or
       * import { intersect } from 'drizzle-orm/sqlite-core'
       *
       * await intersect(
       *   db.select({ courseName: depA.courseName }).from(depA),
       *   db.select({ courseName: depB.courseName }).from(depB)
       * );
       * ```
       */
      intersect = this.createSetOperator("intersect", false);
      /**
       * Adds `except` set operator to the query.
       *
       * Calling this method will retrieve all unique rows from the left query, except for the rows that are present in the result set of the right query.
       *
       * See docs: {@link https://orm.drizzle.team/docs/set-operations#except}
       *
       * @example
       *
       * ```ts
       * // Select all courses offered in department A but not in department B
       * await db.select({ courseName: depA.courseName })
       *   .from(depA)
       *   .except(
       *     db.select({ courseName: depB.courseName }).from(depB)
       *   );
       * // or
       * import { except } from 'drizzle-orm/sqlite-core'
       *
       * await except(
       *   db.select({ courseName: depA.courseName }).from(depA),
       *   db.select({ courseName: depB.courseName }).from(depB)
       * );
       * ```
       */
      except = this.createSetOperator("except", false);
      /** @internal */
      addSetOperators(setOperators) {
        this.config.setOperators.push(...setOperators);
        return this;
      }
      /**
       * Adds a `where` clause to the query.
       *
       * Calling this method will select only those rows that fulfill a specified condition.
       *
       * See docs: {@link https://orm.drizzle.team/docs/select#filtering}
       *
       * @param where the `where` clause.
       *
       * @example
       * You can use conditional operators and `sql function` to filter the rows to be selected.
       *
       * ```ts
       * // Select all cars with green color
       * await db.select().from(cars).where(eq(cars.color, 'green'));
       * // or
       * await db.select().from(cars).where(sql`${cars.color} = 'green'`)
       * ```
       *
       * You can logically combine conditional operators with `and()` and `or()` operators:
       *
       * ```ts
       * // Select all BMW cars with a green color
       * await db.select().from(cars).where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
       *
       * // Select all cars with the green or blue color
       * await db.select().from(cars).where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
       * ```
       */
      where(where) {
        if (typeof where === "function") {
          where = where(
            new Proxy(
              this.config.fields,
              new SelectionProxyHandler2({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
            )
          );
        }
        this.config.where = where;
        return this;
      }
      /**
       * Adds a `having` clause to the query.
       *
       * Calling this method will select only those rows that fulfill a specified condition. It is typically used with aggregate functions to filter the aggregated data based on a specified condition.
       *
       * See docs: {@link https://orm.drizzle.team/docs/select#aggregations}
       *
       * @param having the `having` clause.
       *
       * @example
       *
       * ```ts
       * // Select all brands with more than one car
       * await db.select({
       * 	brand: cars.brand,
       * 	count: sql<number>`cast(count(${cars.id}) as int)`,
       * })
       *   .from(cars)
       *   .groupBy(cars.brand)
       *   .having(({ count }) => gt(count, 1));
       * ```
       */
      having(having) {
        if (typeof having === "function") {
          having = having(
            new Proxy(
              this.config.fields,
              new SelectionProxyHandler2({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
            )
          );
        }
        this.config.having = having;
        return this;
      }
      groupBy(...columns) {
        if (typeof columns[0] === "function") {
          const groupBy = columns[0](
            new Proxy(
              this.config.fields,
              new SelectionProxyHandler2({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })
            )
          );
          this.config.groupBy = Array.isArray(groupBy) ? groupBy : [groupBy];
        } else {
          this.config.groupBy = columns;
        }
        return this;
      }
      orderBy(...columns) {
        if (typeof columns[0] === "function") {
          const orderBy = columns[0](
            new Proxy(
              this.config.fields,
              new SelectionProxyHandler2({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })
            )
          );
          const orderByArray = Array.isArray(orderBy) ? orderBy : [orderBy];
          if (this.config.setOperators.length > 0) {
            this.config.setOperators.at(-1).orderBy = orderByArray;
          } else {
            this.config.orderBy = orderByArray;
          }
        } else {
          const orderByArray = columns;
          if (this.config.setOperators.length > 0) {
            this.config.setOperators.at(-1).orderBy = orderByArray;
          } else {
            this.config.orderBy = orderByArray;
          }
        }
        return this;
      }
      /**
       * Adds a `limit` clause to the query.
       *
       * Calling this method will set the maximum number of rows that will be returned by this query.
       *
       * See docs: {@link https://orm.drizzle.team/docs/select#limit--offset}
       *
       * @param limit the `limit` clause.
       *
       * @example
       *
       * ```ts
       * // Get the first 10 people from this query.
       * await db.select().from(people).limit(10);
       * ```
       */
      limit(limit) {
        if (this.config.setOperators.length > 0) {
          this.config.setOperators.at(-1).limit = limit;
        } else {
          this.config.limit = limit;
        }
        return this;
      }
      /**
       * Adds an `offset` clause to the query.
       *
       * Calling this method will skip a number of rows when returning results from this query.
       *
       * See docs: {@link https://orm.drizzle.team/docs/select#limit--offset}
       *
       * @param offset the `offset` clause.
       *
       * @example
       *
       * ```ts
       * // Get the 10th-20th people from this query.
       * await db.select().from(people).offset(10).limit(10);
       * ```
       */
      offset(offset) {
        if (this.config.setOperators.length > 0) {
          this.config.setOperators.at(-1).offset = offset;
        } else {
          this.config.offset = offset;
        }
        return this;
      }
      /** @internal */
      getSQL() {
        return this.dialect.buildSelectQuery(this.config);
      }
      toSQL() {
        const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
        return rest;
      }
      as(alias) {
        return new Proxy(
          new Subquery2(this.getSQL(), this.config.fields, alias),
          new SelectionProxyHandler2({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
        );
      }
      /** @internal */
      getSelectedFields() {
        return new Proxy(
          this.config.fields,
          new SelectionProxyHandler2({ alias: this.tableName, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
        );
      }
      $dynamic() {
        return this;
      }
    };
    SQLiteSelectBase2 = class extends SQLiteSelectQueryBuilderBase2 {
      static {
        __name(this, "SQLiteSelectBase");
      }
      static [entityKind2] = "SQLiteSelect";
      /** @internal */
      _prepare(isOneTimeQuery = true) {
        if (!this.session) {
          throw new Error("Cannot execute a query on a query builder. Please use a database instance instead.");
        }
        const fieldsList = orderSelectedFields2(this.config.fields);
        const query = this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](
          this.dialect.sqlToQuery(this.getSQL()),
          fieldsList,
          "all",
          true
        );
        query.joinsNotNullableMap = this.joinsNotNullableMap;
        return query;
      }
      prepare() {
        return this._prepare(false);
      }
      run = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().run(placeholderValues);
      }, "run");
      all = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().all(placeholderValues);
      }, "all");
      get = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().get(placeholderValues);
      }, "get");
      values = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().values(placeholderValues);
      }, "values");
      async execute() {
        return this.all();
      }
    };
    applyMixins2(SQLiteSelectBase2, [QueryPromise2]);
    __name(createSetOperator2, "createSetOperator");
    getSQLiteSetOperators2 = /* @__PURE__ */ __name(() => ({
      union: union2,
      unionAll: unionAll2,
      intersect: intersect2,
      except: except2
    }), "getSQLiteSetOperators");
    union2 = createSetOperator2("union", false);
    unionAll2 = createSetOperator2("union", true);
    intersect2 = createSetOperator2("intersect", false);
    except2 = createSetOperator2("except", false);
  }
});

// node_modules/drizzle-orm/sqlite-core/query-builders/query-builder.js
var QueryBuilder2;
var init_query_builder4 = __esm({
  "node_modules/drizzle-orm/sqlite-core/query-builders/query-builder.js"() {
    init_entity2();
    init_selection_proxy2();
    init_dialect2();
    init_subquery3();
    init_select4();
    QueryBuilder2 = class {
      static {
        __name(this, "QueryBuilder");
      }
      static [entityKind2] = "SQLiteQueryBuilder";
      dialect;
      dialectConfig;
      constructor(dialect) {
        this.dialect = is2(dialect, SQLiteDialect2) ? dialect : void 0;
        this.dialectConfig = is2(dialect, SQLiteDialect2) ? void 0 : dialect;
      }
      $with = /* @__PURE__ */ __name((alias, selection) => {
        const queryBuilder = this;
        const as = /* @__PURE__ */ __name((qb) => {
          if (typeof qb === "function") {
            qb = qb(queryBuilder);
          }
          return new Proxy(
            new WithSubquery2(
              qb.getSQL(),
              selection ?? ("getSelectedFields" in qb ? qb.getSelectedFields() ?? {} : {}),
              alias,
              true
            ),
            new SelectionProxyHandler2({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
          );
        }, "as");
        return { as };
      }, "$with");
      with(...queries) {
        const self2 = this;
        function select(fields) {
          return new SQLiteSelectBuilder2({
            fields: fields ?? void 0,
            session: void 0,
            dialect: self2.getDialect(),
            withList: queries
          });
        }
        __name(select, "select");
        function selectDistinct(fields) {
          return new SQLiteSelectBuilder2({
            fields: fields ?? void 0,
            session: void 0,
            dialect: self2.getDialect(),
            withList: queries,
            distinct: true
          });
        }
        __name(selectDistinct, "selectDistinct");
        return { select, selectDistinct };
      }
      select(fields) {
        return new SQLiteSelectBuilder2({ fields: fields ?? void 0, session: void 0, dialect: this.getDialect() });
      }
      selectDistinct(fields) {
        return new SQLiteSelectBuilder2({
          fields: fields ?? void 0,
          session: void 0,
          dialect: this.getDialect(),
          distinct: true
        });
      }
      // Lazy load dialect to avoid circular dependency
      getDialect() {
        if (!this.dialect) {
          this.dialect = new SQLiteSyncDialect2(this.dialectConfig);
        }
        return this.dialect;
      }
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/query-builders/insert.js
var SQLiteInsertBuilder2, SQLiteInsertBase2;
var init_insert2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/query-builders/insert.js"() {
    init_entity2();
    init_query_promise2();
    init_sql4();
    init_table5();
    init_table4();
    init_utils3();
    init_query_builder4();
    SQLiteInsertBuilder2 = class {
      static {
        __name(this, "SQLiteInsertBuilder");
      }
      constructor(table, session, dialect, withList) {
        this.table = table;
        this.session = session;
        this.dialect = dialect;
        this.withList = withList;
      }
      static [entityKind2] = "SQLiteInsertBuilder";
      values(values) {
        values = Array.isArray(values) ? values : [values];
        if (values.length === 0) {
          throw new Error("values() must be called with at least one value");
        }
        const mappedValues = values.map((entry) => {
          const result = {};
          const cols = this.table[Table2.Symbol.Columns];
          for (const colKey of Object.keys(entry)) {
            const colValue = entry[colKey];
            result[colKey] = is2(colValue, SQL2) ? colValue : new Param2(colValue, cols[colKey]);
          }
          return result;
        });
        return new SQLiteInsertBase2(this.table, mappedValues, this.session, this.dialect, this.withList);
      }
      select(selectQuery) {
        const select = typeof selectQuery === "function" ? selectQuery(new QueryBuilder2()) : selectQuery;
        if (!is2(select, SQL2) && !haveSameKeys2(this.table[Columns2], select._.selectedFields)) {
          throw new Error(
            "Insert select error: selected fields are not the same or are in a different order compared to the table definition"
          );
        }
        return new SQLiteInsertBase2(this.table, select, this.session, this.dialect, this.withList, true);
      }
    };
    SQLiteInsertBase2 = class extends QueryPromise2 {
      static {
        __name(this, "SQLiteInsertBase");
      }
      constructor(table, values, session, dialect, withList, select) {
        super();
        this.session = session;
        this.dialect = dialect;
        this.config = { table, values, withList, select };
      }
      static [entityKind2] = "SQLiteInsert";
      /** @internal */
      config;
      returning(fields = this.config.table[SQLiteTable2.Symbol.Columns]) {
        this.config.returning = orderSelectedFields2(fields);
        return this;
      }
      /**
       * Adds an `on conflict do nothing` clause to the query.
       *
       * Calling this method simply avoids inserting a row as its alternative action.
       *
       * See docs: {@link https://orm.drizzle.team/docs/insert#on-conflict-do-nothing}
       *
       * @param config The `target` and `where` clauses.
       *
       * @example
       * ```ts
       * // Insert one row and cancel the insert if there's a conflict
       * await db.insert(cars)
       *   .values({ id: 1, brand: 'BMW' })
       *   .onConflictDoNothing();
       *
       * // Explicitly specify conflict target
       * await db.insert(cars)
       *   .values({ id: 1, brand: 'BMW' })
       *   .onConflictDoNothing({ target: cars.id });
       * ```
       */
      onConflictDoNothing(config = {}) {
        if (!this.config.onConflict)
          this.config.onConflict = [];
        if (config.target === void 0) {
          this.config.onConflict.push(sql2` on conflict do nothing`);
        } else {
          const targetSql = Array.isArray(config.target) ? sql2`${config.target}` : sql2`${[config.target]}`;
          const whereSql = config.where ? sql2` where ${config.where}` : sql2``;
          this.config.onConflict.push(sql2` on conflict ${targetSql} do nothing${whereSql}`);
        }
        return this;
      }
      /**
       * Adds an `on conflict do update` clause to the query.
       *
       * Calling this method will update the existing row that conflicts with the row proposed for insertion as its alternative action.
       *
       * See docs: {@link https://orm.drizzle.team/docs/insert#upserts-and-conflicts}
       *
       * @param config The `target`, `set` and `where` clauses.
       *
       * @example
       * ```ts
       * // Update the row if there's a conflict
       * await db.insert(cars)
       *   .values({ id: 1, brand: 'BMW' })
       *   .onConflictDoUpdate({
       *     target: cars.id,
       *     set: { brand: 'Porsche' }
       *   });
       *
       * // Upsert with 'where' clause
       * await db.insert(cars)
       *   .values({ id: 1, brand: 'BMW' })
       *   .onConflictDoUpdate({
       *     target: cars.id,
       *     set: { brand: 'newBMW' },
       *     where: sql`${cars.createdAt} > '2023-01-01'::date`,
       *   });
       * ```
       */
      onConflictDoUpdate(config) {
        if (config.where && (config.targetWhere || config.setWhere)) {
          throw new Error(
            'You cannot use both "where" and "targetWhere"/"setWhere" at the same time - "where" is deprecated, use "targetWhere" or "setWhere" instead.'
          );
        }
        if (!this.config.onConflict)
          this.config.onConflict = [];
        const whereSql = config.where ? sql2` where ${config.where}` : void 0;
        const targetWhereSql = config.targetWhere ? sql2` where ${config.targetWhere}` : void 0;
        const setWhereSql = config.setWhere ? sql2` where ${config.setWhere}` : void 0;
        const targetSql = Array.isArray(config.target) ? sql2`${config.target}` : sql2`${[config.target]}`;
        const setSql = this.dialect.buildUpdateSet(this.config.table, mapUpdateSet2(this.config.table, config.set));
        this.config.onConflict.push(
          sql2` on conflict ${targetSql}${targetWhereSql} do update set ${setSql}${whereSql}${setWhereSql}`
        );
        return this;
      }
      /** @internal */
      getSQL() {
        return this.dialect.buildInsertQuery(this.config);
      }
      toSQL() {
        const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
        return rest;
      }
      /** @internal */
      _prepare(isOneTimeQuery = true) {
        return this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](
          this.dialect.sqlToQuery(this.getSQL()),
          this.config.returning,
          this.config.returning ? "all" : "run",
          true
        );
      }
      prepare() {
        return this._prepare(false);
      }
      run = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().run(placeholderValues);
      }, "run");
      all = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().all(placeholderValues);
      }, "all");
      get = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().get(placeholderValues);
      }, "get");
      values = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().values(placeholderValues);
      }, "values");
      async execute() {
        return this.config.returning ? this.all() : this.run();
      }
      $dynamic() {
        return this;
      }
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/query-builders/select.types.js
var init_select_types2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/query-builders/select.types.js"() {
  }
});

// node_modules/drizzle-orm/sqlite-core/query-builders/update.js
var SQLiteUpdateBuilder2, SQLiteUpdateBase2;
var init_update2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/query-builders/update.js"() {
    init_entity2();
    init_query_promise2();
    init_selection_proxy2();
    init_table5();
    init_subquery3();
    init_table4();
    init_utils3();
    init_view_common2();
    init_view_base2();
    SQLiteUpdateBuilder2 = class {
      static {
        __name(this, "SQLiteUpdateBuilder");
      }
      constructor(table, session, dialect, withList) {
        this.table = table;
        this.session = session;
        this.dialect = dialect;
        this.withList = withList;
      }
      static [entityKind2] = "SQLiteUpdateBuilder";
      set(values) {
        return new SQLiteUpdateBase2(
          this.table,
          mapUpdateSet2(this.table, values),
          this.session,
          this.dialect,
          this.withList
        );
      }
    };
    SQLiteUpdateBase2 = class extends QueryPromise2 {
      static {
        __name(this, "SQLiteUpdateBase");
      }
      constructor(table, set2, session, dialect, withList) {
        super();
        this.session = session;
        this.dialect = dialect;
        this.config = { set: set2, table, withList, joins: [] };
      }
      static [entityKind2] = "SQLiteUpdate";
      /** @internal */
      config;
      from(source) {
        this.config.from = source;
        return this;
      }
      createJoin(joinType) {
        return (table, on) => {
          const tableName = getTableLikeName2(table);
          if (typeof tableName === "string" && this.config.joins.some((join) => join.alias === tableName)) {
            throw new Error(`Alias "${tableName}" is already used in this query`);
          }
          if (typeof on === "function") {
            const from = this.config.from ? is2(table, SQLiteTable2) ? table[Table2.Symbol.Columns] : is2(table, Subquery2) ? table._.selectedFields : is2(table, SQLiteViewBase2) ? table[ViewBaseConfig2].selectedFields : void 0 : void 0;
            on = on(
              new Proxy(
                this.config.table[Table2.Symbol.Columns],
                new SelectionProxyHandler2({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
              ),
              from && new Proxy(
                from,
                new SelectionProxyHandler2({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
              )
            );
          }
          this.config.joins.push({ on, table, joinType, alias: tableName });
          return this;
        };
      }
      leftJoin = this.createJoin("left");
      rightJoin = this.createJoin("right");
      innerJoin = this.createJoin("inner");
      fullJoin = this.createJoin("full");
      /**
       * Adds a 'where' clause to the query.
       *
       * Calling this method will update only those rows that fulfill a specified condition.
       *
       * See docs: {@link https://orm.drizzle.team/docs/update}
       *
       * @param where the 'where' clause.
       *
       * @example
       * You can use conditional operators and `sql function` to filter the rows to be updated.
       *
       * ```ts
       * // Update all cars with green color
       * db.update(cars).set({ color: 'red' })
       *   .where(eq(cars.color, 'green'));
       * // or
       * db.update(cars).set({ color: 'red' })
       *   .where(sql`${cars.color} = 'green'`)
       * ```
       *
       * You can logically combine conditional operators with `and()` and `or()` operators:
       *
       * ```ts
       * // Update all BMW cars with a green color
       * db.update(cars).set({ color: 'red' })
       *   .where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
       *
       * // Update all cars with the green or blue color
       * db.update(cars).set({ color: 'red' })
       *   .where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
       * ```
       */
      where(where) {
        this.config.where = where;
        return this;
      }
      orderBy(...columns) {
        if (typeof columns[0] === "function") {
          const orderBy = columns[0](
            new Proxy(
              this.config.table[Table2.Symbol.Columns],
              new SelectionProxyHandler2({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })
            )
          );
          const orderByArray = Array.isArray(orderBy) ? orderBy : [orderBy];
          this.config.orderBy = orderByArray;
        } else {
          const orderByArray = columns;
          this.config.orderBy = orderByArray;
        }
        return this;
      }
      limit(limit) {
        this.config.limit = limit;
        return this;
      }
      returning(fields = this.config.table[SQLiteTable2.Symbol.Columns]) {
        this.config.returning = orderSelectedFields2(fields);
        return this;
      }
      /** @internal */
      getSQL() {
        return this.dialect.buildUpdateQuery(this.config);
      }
      toSQL() {
        const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
        return rest;
      }
      /** @internal */
      _prepare(isOneTimeQuery = true) {
        return this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](
          this.dialect.sqlToQuery(this.getSQL()),
          this.config.returning,
          this.config.returning ? "all" : "run",
          true
        );
      }
      prepare() {
        return this._prepare(false);
      }
      run = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().run(placeholderValues);
      }, "run");
      all = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().all(placeholderValues);
      }, "all");
      get = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().get(placeholderValues);
      }, "get");
      values = /* @__PURE__ */ __name((placeholderValues) => {
        return this._prepare().values(placeholderValues);
      }, "values");
      async execute() {
        return this.config.returning ? this.all() : this.run();
      }
      $dynamic() {
        return this;
      }
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/query-builders/index.js
var init_query_builders2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/query-builders/index.js"() {
    init_delete2();
    init_insert2();
    init_query_builder4();
    init_select4();
    init_select_types2();
    init_update2();
  }
});

// node_modules/drizzle-orm/sqlite-core/query-builders/count.js
var SQLiteCountBuilder2;
var init_count2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/query-builders/count.js"() {
    init_entity2();
    init_sql4();
    SQLiteCountBuilder2 = class _SQLiteCountBuilder extends SQL2 {
      static {
        __name(this, "SQLiteCountBuilder");
      }
      constructor(params) {
        super(_SQLiteCountBuilder.buildEmbeddedCount(params.source, params.filters).queryChunks);
        this.params = params;
        this.session = params.session;
        this.sql = _SQLiteCountBuilder.buildCount(
          params.source,
          params.filters
        );
      }
      sql;
      static [entityKind2] = "SQLiteCountBuilderAsync";
      [Symbol.toStringTag] = "SQLiteCountBuilderAsync";
      session;
      static buildEmbeddedCount(source, filters) {
        return sql2`(select count(*) from ${source}${sql2.raw(" where ").if(filters)}${filters})`;
      }
      static buildCount(source, filters) {
        return sql2`select count(*) from ${source}${sql2.raw(" where ").if(filters)}${filters}`;
      }
      then(onfulfilled, onrejected) {
        return Promise.resolve(this.session.count(this.sql)).then(
          onfulfilled,
          onrejected
        );
      }
      catch(onRejected) {
        return this.then(void 0, onRejected);
      }
      finally(onFinally) {
        return this.then(
          (value) => {
            onFinally?.();
            return value;
          },
          (reason) => {
            onFinally?.();
            throw reason;
          }
        );
      }
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/query-builders/query.js
var RelationalQueryBuilder2, SQLiteRelationalQuery2, SQLiteSyncRelationalQuery2;
var init_query2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/query-builders/query.js"() {
    init_entity2();
    init_query_promise2();
    init_relations2();
    RelationalQueryBuilder2 = class {
      static {
        __name(this, "RelationalQueryBuilder");
      }
      constructor(mode, fullSchema, schema, tableNamesMap, table, tableConfig, dialect, session) {
        this.mode = mode;
        this.fullSchema = fullSchema;
        this.schema = schema;
        this.tableNamesMap = tableNamesMap;
        this.table = table;
        this.tableConfig = tableConfig;
        this.dialect = dialect;
        this.session = session;
      }
      static [entityKind2] = "SQLiteAsyncRelationalQueryBuilder";
      findMany(config) {
        return this.mode === "sync" ? new SQLiteSyncRelationalQuery2(
          this.fullSchema,
          this.schema,
          this.tableNamesMap,
          this.table,
          this.tableConfig,
          this.dialect,
          this.session,
          config ? config : {},
          "many"
        ) : new SQLiteRelationalQuery2(
          this.fullSchema,
          this.schema,
          this.tableNamesMap,
          this.table,
          this.tableConfig,
          this.dialect,
          this.session,
          config ? config : {},
          "many"
        );
      }
      findFirst(config) {
        return this.mode === "sync" ? new SQLiteSyncRelationalQuery2(
          this.fullSchema,
          this.schema,
          this.tableNamesMap,
          this.table,
          this.tableConfig,
          this.dialect,
          this.session,
          config ? { ...config, limit: 1 } : { limit: 1 },
          "first"
        ) : new SQLiteRelationalQuery2(
          this.fullSchema,
          this.schema,
          this.tableNamesMap,
          this.table,
          this.tableConfig,
          this.dialect,
          this.session,
          config ? { ...config, limit: 1 } : { limit: 1 },
          "first"
        );
      }
    };
    SQLiteRelationalQuery2 = class extends QueryPromise2 {
      static {
        __name(this, "SQLiteRelationalQuery");
      }
      constructor(fullSchema, schema, tableNamesMap, table, tableConfig, dialect, session, config, mode) {
        super();
        this.fullSchema = fullSchema;
        this.schema = schema;
        this.tableNamesMap = tableNamesMap;
        this.table = table;
        this.tableConfig = tableConfig;
        this.dialect = dialect;
        this.session = session;
        this.config = config;
        this.mode = mode;
      }
      static [entityKind2] = "SQLiteAsyncRelationalQuery";
      /** @internal */
      mode;
      /** @internal */
      getSQL() {
        return this.dialect.buildRelationalQuery({
          fullSchema: this.fullSchema,
          schema: this.schema,
          tableNamesMap: this.tableNamesMap,
          table: this.table,
          tableConfig: this.tableConfig,
          queryConfig: this.config,
          tableAlias: this.tableConfig.tsName
        }).sql;
      }
      /** @internal */
      _prepare(isOneTimeQuery = false) {
        const { query, builtQuery } = this._toSQL();
        return this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](
          builtQuery,
          void 0,
          this.mode === "first" ? "get" : "all",
          true,
          (rawRows, mapColumnValue) => {
            const rows = rawRows.map(
              (row) => mapRelationalRow2(this.schema, this.tableConfig, row, query.selection, mapColumnValue)
            );
            if (this.mode === "first") {
              return rows[0];
            }
            return rows;
          }
        );
      }
      prepare() {
        return this._prepare(false);
      }
      _toSQL() {
        const query = this.dialect.buildRelationalQuery({
          fullSchema: this.fullSchema,
          schema: this.schema,
          tableNamesMap: this.tableNamesMap,
          table: this.table,
          tableConfig: this.tableConfig,
          queryConfig: this.config,
          tableAlias: this.tableConfig.tsName
        });
        const builtQuery = this.dialect.sqlToQuery(query.sql);
        return { query, builtQuery };
      }
      toSQL() {
        return this._toSQL().builtQuery;
      }
      /** @internal */
      executeRaw() {
        if (this.mode === "first") {
          return this._prepare(false).get();
        }
        return this._prepare(false).all();
      }
      async execute() {
        return this.executeRaw();
      }
    };
    SQLiteSyncRelationalQuery2 = class extends SQLiteRelationalQuery2 {
      static {
        __name(this, "SQLiteSyncRelationalQuery");
      }
      static [entityKind2] = "SQLiteSyncRelationalQuery";
      sync() {
        return this.executeRaw();
      }
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/query-builders/raw.js
var SQLiteRaw2;
var init_raw2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/query-builders/raw.js"() {
    init_entity2();
    init_query_promise2();
    SQLiteRaw2 = class extends QueryPromise2 {
      static {
        __name(this, "SQLiteRaw");
      }
      constructor(execute, getSQL, action, dialect, mapBatchResult) {
        super();
        this.execute = execute;
        this.getSQL = getSQL;
        this.dialect = dialect;
        this.mapBatchResult = mapBatchResult;
        this.config = { action };
      }
      static [entityKind2] = "SQLiteRaw";
      /** @internal */
      config;
      getQuery() {
        return { ...this.dialect.sqlToQuery(this.getSQL()), method: this.config.action };
      }
      mapResult(result, isFromBatch) {
        return isFromBatch ? this.mapBatchResult(result) : result;
      }
      _prepare() {
        return this;
      }
      /** @internal */
      isResponseInArrayMode() {
        return false;
      }
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/db.js
var BaseSQLiteDatabase2;
var init_db2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/db.js"() {
    init_entity2();
    init_selection_proxy2();
    init_sql4();
    init_query_builders2();
    init_subquery3();
    init_count2();
    init_query2();
    init_raw2();
    BaseSQLiteDatabase2 = class {
      static {
        __name(this, "BaseSQLiteDatabase");
      }
      constructor(resultKind, dialect, session, schema) {
        this.resultKind = resultKind;
        this.dialect = dialect;
        this.session = session;
        this._ = schema ? {
          schema: schema.schema,
          fullSchema: schema.fullSchema,
          tableNamesMap: schema.tableNamesMap
        } : {
          schema: void 0,
          fullSchema: {},
          tableNamesMap: {}
        };
        this.query = {};
        const query = this.query;
        if (this._.schema) {
          for (const [tableName, columns] of Object.entries(this._.schema)) {
            query[tableName] = new RelationalQueryBuilder2(
              resultKind,
              schema.fullSchema,
              this._.schema,
              this._.tableNamesMap,
              schema.fullSchema[tableName],
              columns,
              dialect,
              session
            );
          }
        }
      }
      static [entityKind2] = "BaseSQLiteDatabase";
      query;
      /**
       * Creates a subquery that defines a temporary named result set as a CTE.
       *
       * It is useful for breaking down complex queries into simpler parts and for reusing the result set in subsequent parts of the query.
       *
       * See docs: {@link https://orm.drizzle.team/docs/select#with-clause}
       *
       * @param alias The alias for the subquery.
       *
       * Failure to provide an alias will result in a DrizzleTypeError, preventing the subquery from being referenced in other queries.
       *
       * @example
       *
       * ```ts
       * // Create a subquery with alias 'sq' and use it in the select query
       * const sq = db.$with('sq').as(db.select().from(users).where(eq(users.id, 42)));
       *
       * const result = await db.with(sq).select().from(sq);
       * ```
       *
       * To select arbitrary SQL values as fields in a CTE and reference them in other CTEs or in the main query, you need to add aliases to them:
       *
       * ```ts
       * // Select an arbitrary SQL value as a field in a CTE and reference it in the main query
       * const sq = db.$with('sq').as(db.select({
       *   name: sql<string>`upper(${users.name})`.as('name'),
       * })
       * .from(users));
       *
       * const result = await db.with(sq).select({ name: sq.name }).from(sq);
       * ```
       */
      $with = /* @__PURE__ */ __name((alias, selection) => {
        const self2 = this;
        const as = /* @__PURE__ */ __name((qb) => {
          if (typeof qb === "function") {
            qb = qb(new QueryBuilder2(self2.dialect));
          }
          return new Proxy(
            new WithSubquery2(
              qb.getSQL(),
              selection ?? ("getSelectedFields" in qb ? qb.getSelectedFields() ?? {} : {}),
              alias,
              true
            ),
            new SelectionProxyHandler2({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
          );
        }, "as");
        return { as };
      }, "$with");
      $count(source, filters) {
        return new SQLiteCountBuilder2({ source, filters, session: this.session });
      }
      /**
       * Incorporates a previously defined CTE (using `$with`) into the main query.
       *
       * This method allows the main query to reference a temporary named result set.
       *
       * See docs: {@link https://orm.drizzle.team/docs/select#with-clause}
       *
       * @param queries The CTEs to incorporate into the main query.
       *
       * @example
       *
       * ```ts
       * // Define a subquery 'sq' as a CTE using $with
       * const sq = db.$with('sq').as(db.select().from(users).where(eq(users.id, 42)));
       *
       * // Incorporate the CTE 'sq' into the main query and select from it
       * const result = await db.with(sq).select().from(sq);
       * ```
       */
      with(...queries) {
        const self2 = this;
        function select(fields) {
          return new SQLiteSelectBuilder2({
            fields: fields ?? void 0,
            session: self2.session,
            dialect: self2.dialect,
            withList: queries
          });
        }
        __name(select, "select");
        function selectDistinct(fields) {
          return new SQLiteSelectBuilder2({
            fields: fields ?? void 0,
            session: self2.session,
            dialect: self2.dialect,
            withList: queries,
            distinct: true
          });
        }
        __name(selectDistinct, "selectDistinct");
        function update(table) {
          return new SQLiteUpdateBuilder2(table, self2.session, self2.dialect, queries);
        }
        __name(update, "update");
        function insert(into) {
          return new SQLiteInsertBuilder2(into, self2.session, self2.dialect, queries);
        }
        __name(insert, "insert");
        function delete_(from) {
          return new SQLiteDeleteBase2(from, self2.session, self2.dialect, queries);
        }
        __name(delete_, "delete_");
        return { select, selectDistinct, update, insert, delete: delete_ };
      }
      select(fields) {
        return new SQLiteSelectBuilder2({ fields: fields ?? void 0, session: this.session, dialect: this.dialect });
      }
      selectDistinct(fields) {
        return new SQLiteSelectBuilder2({
          fields: fields ?? void 0,
          session: this.session,
          dialect: this.dialect,
          distinct: true
        });
      }
      /**
       * Creates an update query.
       *
       * Calling this method without `.where()` clause will update all rows in a table. The `.where()` clause specifies which rows should be updated.
       *
       * Use `.set()` method to specify which values to update.
       *
       * See docs: {@link https://orm.drizzle.team/docs/update}
       *
       * @param table The table to update.
       *
       * @example
       *
       * ```ts
       * // Update all rows in the 'cars' table
       * await db.update(cars).set({ color: 'red' });
       *
       * // Update rows with filters and conditions
       * await db.update(cars).set({ color: 'red' }).where(eq(cars.brand, 'BMW'));
       *
       * // Update with returning clause
       * const updatedCar: Car[] = await db.update(cars)
       *   .set({ color: 'red' })
       *   .where(eq(cars.id, 1))
       *   .returning();
       * ```
       */
      update(table) {
        return new SQLiteUpdateBuilder2(table, this.session, this.dialect);
      }
      /**
       * Creates an insert query.
       *
       * Calling this method will create new rows in a table. Use `.values()` method to specify which values to insert.
       *
       * See docs: {@link https://orm.drizzle.team/docs/insert}
       *
       * @param table The table to insert into.
       *
       * @example
       *
       * ```ts
       * // Insert one row
       * await db.insert(cars).values({ brand: 'BMW' });
       *
       * // Insert multiple rows
       * await db.insert(cars).values([{ brand: 'BMW' }, { brand: 'Porsche' }]);
       *
       * // Insert with returning clause
       * const insertedCar: Car[] = await db.insert(cars)
       *   .values({ brand: 'BMW' })
       *   .returning();
       * ```
       */
      insert(into) {
        return new SQLiteInsertBuilder2(into, this.session, this.dialect);
      }
      /**
       * Creates a delete query.
       *
       * Calling this method without `.where()` clause will delete all rows in a table. The `.where()` clause specifies which rows should be deleted.
       *
       * See docs: {@link https://orm.drizzle.team/docs/delete}
       *
       * @param table The table to delete from.
       *
       * @example
       *
       * ```ts
       * // Delete all rows in the 'cars' table
       * await db.delete(cars);
       *
       * // Delete rows with filters and conditions
       * await db.delete(cars).where(eq(cars.color, 'green'));
       *
       * // Delete with returning clause
       * const deletedCar: Car[] = await db.delete(cars)
       *   .where(eq(cars.id, 1))
       *   .returning();
       * ```
       */
      delete(from) {
        return new SQLiteDeleteBase2(from, this.session, this.dialect);
      }
      run(query) {
        const sequel = typeof query === "string" ? sql2.raw(query) : query.getSQL();
        if (this.resultKind === "async") {
          return new SQLiteRaw2(
            async () => this.session.run(sequel),
            () => sequel,
            "run",
            this.dialect,
            this.session.extractRawRunValueFromBatchResult.bind(this.session)
          );
        }
        return this.session.run(sequel);
      }
      all(query) {
        const sequel = typeof query === "string" ? sql2.raw(query) : query.getSQL();
        if (this.resultKind === "async") {
          return new SQLiteRaw2(
            async () => this.session.all(sequel),
            () => sequel,
            "all",
            this.dialect,
            this.session.extractRawAllValueFromBatchResult.bind(this.session)
          );
        }
        return this.session.all(sequel);
      }
      get(query) {
        const sequel = typeof query === "string" ? sql2.raw(query) : query.getSQL();
        if (this.resultKind === "async") {
          return new SQLiteRaw2(
            async () => this.session.get(sequel),
            () => sequel,
            "get",
            this.dialect,
            this.session.extractRawGetValueFromBatchResult.bind(this.session)
          );
        }
        return this.session.get(sequel);
      }
      values(query) {
        const sequel = typeof query === "string" ? sql2.raw(query) : query.getSQL();
        if (this.resultKind === "async") {
          return new SQLiteRaw2(
            async () => this.session.values(sequel),
            () => sequel,
            "values",
            this.dialect,
            this.session.extractRawValuesValueFromBatchResult.bind(this.session)
          );
        }
        return this.session.values(sequel);
      }
      transaction(transaction, config) {
        return this.session.transaction(transaction, config);
      }
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/indexes.js
var IndexBuilderOn2, IndexBuilder2, Index2;
var init_indexes2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/indexes.js"() {
    init_entity2();
    IndexBuilderOn2 = class {
      static {
        __name(this, "IndexBuilderOn");
      }
      constructor(name, unique) {
        this.name = name;
        this.unique = unique;
      }
      static [entityKind2] = "SQLiteIndexBuilderOn";
      on(...columns) {
        return new IndexBuilder2(this.name, columns, this.unique);
      }
    };
    IndexBuilder2 = class {
      static {
        __name(this, "IndexBuilder");
      }
      static [entityKind2] = "SQLiteIndexBuilder";
      /** @internal */
      config;
      constructor(name, columns, unique) {
        this.config = {
          name,
          columns,
          unique,
          where: void 0
        };
      }
      /**
       * Condition for partial index.
       */
      where(condition) {
        this.config.where = condition;
        return this;
      }
      /** @internal */
      build(table) {
        return new Index2(this.config, table);
      }
    };
    Index2 = class {
      static {
        __name(this, "Index");
      }
      static [entityKind2] = "SQLiteIndex";
      config;
      constructor(config, table) {
        this.config = { ...config, table };
      }
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/primary-keys.js
var PrimaryKeyBuilder3, PrimaryKey3;
var init_primary_keys3 = __esm({
  "node_modules/drizzle-orm/sqlite-core/primary-keys.js"() {
    init_entity2();
    init_table5();
    PrimaryKeyBuilder3 = class {
      static {
        __name(this, "PrimaryKeyBuilder");
      }
      static [entityKind2] = "SQLitePrimaryKeyBuilder";
      /** @internal */
      columns;
      /** @internal */
      name;
      constructor(columns, name) {
        this.columns = columns;
        this.name = name;
      }
      /** @internal */
      build(table) {
        return new PrimaryKey3(table, this.columns, this.name);
      }
    };
    PrimaryKey3 = class {
      static {
        __name(this, "PrimaryKey");
      }
      constructor(table, columns, name) {
        this.table = table;
        this.columns = columns;
        this.name = name;
      }
      static [entityKind2] = "SQLitePrimaryKey";
      columns;
      name;
      getName() {
        return this.name ?? `${this.table[SQLiteTable2.Symbol.Name]}_${this.columns.map((column) => column.name).join("_")}_pk`;
      }
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/session.js
var ExecuteResultSync2, SQLitePreparedQuery2, SQLiteSession2, SQLiteTransaction2;
var init_session3 = __esm({
  "node_modules/drizzle-orm/sqlite-core/session.js"() {
    init_entity2();
    init_errors3();
    init_query_promise2();
    init_db2();
    ExecuteResultSync2 = class extends QueryPromise2 {
      static {
        __name(this, "ExecuteResultSync");
      }
      constructor(resultCb) {
        super();
        this.resultCb = resultCb;
      }
      static [entityKind2] = "ExecuteResultSync";
      async execute() {
        return this.resultCb();
      }
      sync() {
        return this.resultCb();
      }
    };
    SQLitePreparedQuery2 = class {
      static {
        __name(this, "SQLitePreparedQuery");
      }
      constructor(mode, executeMethod, query) {
        this.mode = mode;
        this.executeMethod = executeMethod;
        this.query = query;
      }
      static [entityKind2] = "PreparedQuery";
      /** @internal */
      joinsNotNullableMap;
      getQuery() {
        return this.query;
      }
      mapRunResult(result, _isFromBatch) {
        return result;
      }
      mapAllResult(_result, _isFromBatch) {
        throw new Error("Not implemented");
      }
      mapGetResult(_result, _isFromBatch) {
        throw new Error("Not implemented");
      }
      execute(placeholderValues) {
        if (this.mode === "async") {
          return this[this.executeMethod](placeholderValues);
        }
        return new ExecuteResultSync2(() => this[this.executeMethod](placeholderValues));
      }
      mapResult(response, isFromBatch) {
        switch (this.executeMethod) {
          case "run": {
            return this.mapRunResult(response, isFromBatch);
          }
          case "all": {
            return this.mapAllResult(response, isFromBatch);
          }
          case "get": {
            return this.mapGetResult(response, isFromBatch);
          }
        }
      }
    };
    SQLiteSession2 = class {
      static {
        __name(this, "SQLiteSession");
      }
      constructor(dialect) {
        this.dialect = dialect;
      }
      static [entityKind2] = "SQLiteSession";
      prepareOneTimeQuery(query, fields, executeMethod, isResponseInArrayMode) {
        return this.prepareQuery(query, fields, executeMethod, isResponseInArrayMode);
      }
      run(query) {
        const staticQuery = this.dialect.sqlToQuery(query);
        try {
          return this.prepareOneTimeQuery(staticQuery, void 0, "run", false).run();
        } catch (err) {
          throw new DrizzleError2({ cause: err, message: `Failed to run the query '${staticQuery.sql}'` });
        }
      }
      /** @internal */
      extractRawRunValueFromBatchResult(result) {
        return result;
      }
      all(query) {
        return this.prepareOneTimeQuery(this.dialect.sqlToQuery(query), void 0, "run", false).all();
      }
      /** @internal */
      extractRawAllValueFromBatchResult(_result) {
        throw new Error("Not implemented");
      }
      get(query) {
        return this.prepareOneTimeQuery(this.dialect.sqlToQuery(query), void 0, "run", false).get();
      }
      /** @internal */
      extractRawGetValueFromBatchResult(_result) {
        throw new Error("Not implemented");
      }
      values(query) {
        return this.prepareOneTimeQuery(this.dialect.sqlToQuery(query), void 0, "run", false).values();
      }
      async count(sql3) {
        const result = await this.values(sql3);
        return result[0][0];
      }
      /** @internal */
      extractRawValuesValueFromBatchResult(_result) {
        throw new Error("Not implemented");
      }
    };
    SQLiteTransaction2 = class extends BaseSQLiteDatabase2 {
      static {
        __name(this, "SQLiteTransaction");
      }
      constructor(resultType, dialect, session, schema, nestedIndex = 0) {
        super(resultType, dialect, session, schema);
        this.schema = schema;
        this.nestedIndex = nestedIndex;
      }
      static [entityKind2] = "SQLiteTransaction";
      rollback() {
        throw new TransactionRollbackError2();
      }
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/subquery.js
var init_subquery4 = __esm({
  "node_modules/drizzle-orm/sqlite-core/subquery.js"() {
  }
});

// node_modules/drizzle-orm/sqlite-core/utils.js
var init_utils4 = __esm({
  "node_modules/drizzle-orm/sqlite-core/utils.js"() {
  }
});

// node_modules/drizzle-orm/sqlite-core/view.js
var ViewBuilderCore2, ViewBuilder2, ManualViewBuilder2, SQLiteView2;
var init_view2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/view.js"() {
    init_entity2();
    init_selection_proxy2();
    init_utils3();
    init_query_builder4();
    init_table5();
    init_view_base2();
    ViewBuilderCore2 = class {
      static {
        __name(this, "ViewBuilderCore");
      }
      constructor(name) {
        this.name = name;
      }
      static [entityKind2] = "SQLiteViewBuilderCore";
      config = {};
    };
    ViewBuilder2 = class extends ViewBuilderCore2 {
      static {
        __name(this, "ViewBuilder");
      }
      static [entityKind2] = "SQLiteViewBuilder";
      as(qb) {
        if (typeof qb === "function") {
          qb = qb(new QueryBuilder2());
        }
        const selectionProxy = new SelectionProxyHandler2({
          alias: this.name,
          sqlBehavior: "error",
          sqlAliasedBehavior: "alias",
          replaceOriginalName: true
        });
        const aliasedSelectedFields = qb.getSelectedFields();
        return new Proxy(
          new SQLiteView2({
            // sqliteConfig: this.config,
            config: {
              name: this.name,
              schema: void 0,
              selectedFields: aliasedSelectedFields,
              query: qb.getSQL().inlineParams()
            }
          }),
          selectionProxy
        );
      }
    };
    ManualViewBuilder2 = class extends ViewBuilderCore2 {
      static {
        __name(this, "ManualViewBuilder");
      }
      static [entityKind2] = "SQLiteManualViewBuilder";
      columns;
      constructor(name, columns) {
        super(name);
        this.columns = getTableColumns2(sqliteTable2(name, columns));
      }
      existing() {
        return new Proxy(
          new SQLiteView2({
            config: {
              name: this.name,
              schema: void 0,
              selectedFields: this.columns,
              query: void 0
            }
          }),
          new SelectionProxyHandler2({
            alias: this.name,
            sqlBehavior: "error",
            sqlAliasedBehavior: "alias",
            replaceOriginalName: true
          })
        );
      }
      as(query) {
        return new Proxy(
          new SQLiteView2({
            config: {
              name: this.name,
              schema: void 0,
              selectedFields: this.columns,
              query: query.inlineParams()
            }
          }),
          new SelectionProxyHandler2({
            alias: this.name,
            sqlBehavior: "error",
            sqlAliasedBehavior: "alias",
            replaceOriginalName: true
          })
        );
      }
    };
    SQLiteView2 = class extends SQLiteViewBase2 {
      static {
        __name(this, "SQLiteView");
      }
      static [entityKind2] = "SQLiteView";
      constructor({ config }) {
        super(config);
      }
    };
  }
});

// node_modules/drizzle-orm/sqlite-core/index.js
var init_sqlite_core2 = __esm({
  "node_modules/drizzle-orm/sqlite-core/index.js"() {
    init_alias4();
    init_checks2();
    init_columns2();
    init_db2();
    init_dialect2();
    init_foreign_keys4();
    init_indexes2();
    init_primary_keys3();
    init_query_builders2();
    init_session3();
    init_subquery4();
    init_table5();
    init_unique_constraint4();
    init_utils4();
    init_view2();
  }
});

// node_modules/zod/lib/index.mjs
function setErrorMap(map2) {
  overrideErrorMap = map2;
}
function getErrorMap() {
  return overrideErrorMap;
}
function addIssueToContext(ctx, issueData) {
  const overrideMap = getErrorMap();
  const issue = makeIssue({
    issueData,
    data: ctx.data,
    path: ctx.path,
    errorMaps: [
      ctx.common.contextualErrorMap,
      // contextual error map is first priority
      ctx.schemaErrorMap,
      // then schema-bound map if available
      overrideMap,
      // then global override map
      overrideMap === errorMap ? void 0 : errorMap
      // then global default map
    ].filter((x) => !!x)
  });
  ctx.common.issues.push(issue);
}
function __classPrivateFieldGet(receiver, state, kind, f) {
  if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
  if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
  return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
}
function __classPrivateFieldSet(receiver, state, value, kind, f) {
  if (kind === "m") throw new TypeError("Private method is not writable");
  if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
  if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
  return kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value), value;
}
function processCreateParams(params) {
  if (!params)
    return {};
  const { errorMap: errorMap2, invalid_type_error, required_error, description } = params;
  if (errorMap2 && (invalid_type_error || required_error)) {
    throw new Error(`Can't use "invalid_type_error" or "required_error" in conjunction with custom error map.`);
  }
  if (errorMap2)
    return { errorMap: errorMap2, description };
  const customMap = /* @__PURE__ */ __name((iss, ctx) => {
    var _a, _b;
    const { message } = params;
    if (iss.code === "invalid_enum_value") {
      return { message: message !== null && message !== void 0 ? message : ctx.defaultError };
    }
    if (typeof ctx.data === "undefined") {
      return { message: (_a = message !== null && message !== void 0 ? message : required_error) !== null && _a !== void 0 ? _a : ctx.defaultError };
    }
    if (iss.code !== "invalid_type")
      return { message: ctx.defaultError };
    return { message: (_b = message !== null && message !== void 0 ? message : invalid_type_error) !== null && _b !== void 0 ? _b : ctx.defaultError };
  }, "customMap");
  return { errorMap: customMap, description };
}
function timeRegexSource(args) {
  let regex = `([01]\\d|2[0-3]):[0-5]\\d:[0-5]\\d`;
  if (args.precision) {
    regex = `${regex}\\.\\d{${args.precision}}`;
  } else if (args.precision == null) {
    regex = `${regex}(\\.\\d+)?`;
  }
  return regex;
}
function timeRegex(args) {
  return new RegExp(`^${timeRegexSource(args)}$`);
}
function datetimeRegex(args) {
  let regex = `${dateRegexSource}T${timeRegexSource(args)}`;
  const opts = [];
  opts.push(args.local ? `Z?` : `Z`);
  if (args.offset)
    opts.push(`([+-]\\d{2}:?\\d{2})`);
  regex = `${regex}(${opts.join("|")})`;
  return new RegExp(`^${regex}$`);
}
function isValidIP(ip, version5) {
  if ((version5 === "v4" || !version5) && ipv4Regex.test(ip)) {
    return true;
  }
  if ((version5 === "v6" || !version5) && ipv6Regex.test(ip)) {
    return true;
  }
  return false;
}
function isValidJWT(jwt, alg) {
  if (!jwtRegex.test(jwt))
    return false;
  try {
    const [header] = jwt.split(".");
    const base64 = header.replace(/-/g, "+").replace(/_/g, "/").padEnd(header.length + (4 - header.length % 4) % 4, "=");
    const decoded = JSON.parse(atob(base64));
    if (typeof decoded !== "object" || decoded === null)
      return false;
    if (!decoded.typ || !decoded.alg)
      return false;
    if (alg && decoded.alg !== alg)
      return false;
    return true;
  } catch (_a) {
    return false;
  }
}
function isValidCidr(ip, version5) {
  if ((version5 === "v4" || !version5) && ipv4CidrRegex.test(ip)) {
    return true;
  }
  if ((version5 === "v6" || !version5) && ipv6CidrRegex.test(ip)) {
    return true;
  }
  return false;
}
function floatSafeRemainder(val, step) {
  const valDecCount = (val.toString().split(".")[1] || "").length;
  const stepDecCount = (step.toString().split(".")[1] || "").length;
  const decCount = valDecCount > stepDecCount ? valDecCount : stepDecCount;
  const valInt = parseInt(val.toFixed(decCount).replace(".", ""));
  const stepInt = parseInt(step.toFixed(decCount).replace(".", ""));
  return valInt % stepInt / Math.pow(10, decCount);
}
function deepPartialify(schema) {
  if (schema instanceof ZodObject) {
    const newShape = {};
    for (const key in schema.shape) {
      const fieldSchema = schema.shape[key];
      newShape[key] = ZodOptional.create(deepPartialify(fieldSchema));
    }
    return new ZodObject({
      ...schema._def,
      shape: /* @__PURE__ */ __name(() => newShape, "shape")
    });
  } else if (schema instanceof ZodArray) {
    return new ZodArray({
      ...schema._def,
      type: deepPartialify(schema.element)
    });
  } else if (schema instanceof ZodOptional) {
    return ZodOptional.create(deepPartialify(schema.unwrap()));
  } else if (schema instanceof ZodNullable) {
    return ZodNullable.create(deepPartialify(schema.unwrap()));
  } else if (schema instanceof ZodTuple) {
    return ZodTuple.create(schema.items.map((item) => deepPartialify(item)));
  } else {
    return schema;
  }
}
function mergeValues(a, b) {
  const aType = getParsedType(a);
  const bType = getParsedType(b);
  if (a === b) {
    return { valid: true, data: a };
  } else if (aType === ZodParsedType.object && bType === ZodParsedType.object) {
    const bKeys = util.objectKeys(b);
    const sharedKeys = util.objectKeys(a).filter((key) => bKeys.indexOf(key) !== -1);
    const newObj = { ...a, ...b };
    for (const key of sharedKeys) {
      const sharedValue = mergeValues(a[key], b[key]);
      if (!sharedValue.valid) {
        return { valid: false };
      }
      newObj[key] = sharedValue.data;
    }
    return { valid: true, data: newObj };
  } else if (aType === ZodParsedType.array && bType === ZodParsedType.array) {
    if (a.length !== b.length) {
      return { valid: false };
    }
    const newArray = [];
    for (let index = 0; index < a.length; index++) {
      const itemA = a[index];
      const itemB = b[index];
      const sharedValue = mergeValues(itemA, itemB);
      if (!sharedValue.valid) {
        return { valid: false };
      }
      newArray.push(sharedValue.data);
    }
    return { valid: true, data: newArray };
  } else if (aType === ZodParsedType.date && bType === ZodParsedType.date && +a === +b) {
    return { valid: true, data: a };
  } else {
    return { valid: false };
  }
}
function createZodEnum(values, params) {
  return new ZodEnum({
    values,
    typeName: ZodFirstPartyTypeKind.ZodEnum,
    ...processCreateParams(params)
  });
}
function cleanParams(params, data) {
  const p = typeof params === "function" ? params(data) : typeof params === "string" ? { message: params } : params;
  const p2 = typeof p === "string" ? { message: p } : p;
  return p2;
}
function custom(check, _params = {}, fatal) {
  if (check)
    return ZodAny.create().superRefine((data, ctx) => {
      var _a, _b;
      const r = check(data);
      if (r instanceof Promise) {
        return r.then((r2) => {
          var _a2, _b2;
          if (!r2) {
            const params = cleanParams(_params, data);
            const _fatal = (_b2 = (_a2 = params.fatal) !== null && _a2 !== void 0 ? _a2 : fatal) !== null && _b2 !== void 0 ? _b2 : true;
            ctx.addIssue({ code: "custom", ...params, fatal: _fatal });
          }
        });
      }
      if (!r) {
        const params = cleanParams(_params, data);
        const _fatal = (_b = (_a = params.fatal) !== null && _a !== void 0 ? _a : fatal) !== null && _b !== void 0 ? _b : true;
        ctx.addIssue({ code: "custom", ...params, fatal: _fatal });
      }
      return;
    });
  return ZodAny.create();
}
var util, objectUtil, ZodParsedType, getParsedType, ZodIssueCode, quotelessJson, ZodError, errorMap, overrideErrorMap, makeIssue, EMPTY_PATH, ParseStatus, INVALID, DIRTY, OK, isAborted, isDirty, isValid2, isAsync, errorUtil, _ZodEnum_cache, _ZodNativeEnum_cache, ParseInputLazyPath, handleResult, ZodType, cuidRegex, cuid2Regex, ulidRegex, uuidRegex, nanoidRegex, jwtRegex, durationRegex, emailRegex, _emojiRegex, emojiRegex, ipv4Regex, ipv4CidrRegex, ipv6Regex, ipv6CidrRegex, base64Regex, base64urlRegex, dateRegexSource, dateRegex, ZodString, ZodNumber, ZodBigInt, ZodBoolean, ZodDate, ZodSymbol, ZodUndefined, ZodNull, ZodAny, ZodUnknown, ZodNever, ZodVoid, ZodArray, ZodObject, ZodUnion, getDiscriminator, ZodDiscriminatedUnion, ZodIntersection, ZodTuple, ZodRecord, ZodMap, ZodSet, ZodFunction, ZodLazy, ZodLiteral, ZodEnum, ZodNativeEnum, ZodPromise, ZodEffects, ZodOptional, ZodNullable, ZodDefault, ZodCatch, ZodNaN, BRAND, ZodBranded, ZodPipeline, ZodReadonly, late, ZodFirstPartyTypeKind, instanceOfType, stringType, numberType, nanType, bigIntType, booleanType, dateType, symbolType, undefinedType, nullType, anyType, unknownType, neverType, voidType, arrayType, objectType, strictObjectType, unionType, discriminatedUnionType, intersectionType, tupleType, recordType, mapType, setType, functionType, lazyType, literalType, enumType, nativeEnumType, promiseType, effectsType, optionalType, nullableType, preprocessType, pipelineType, ostring, onumber, oboolean, coerce, NEVER, z;
var init_lib = __esm({
  "node_modules/zod/lib/index.mjs"() {
    (function(util2) {
      util2.assertEqual = (val) => val;
      function assertIs(_arg) {
      }
      __name(assertIs, "assertIs");
      util2.assertIs = assertIs;
      function assertNever(_x) {
        throw new Error();
      }
      __name(assertNever, "assertNever");
      util2.assertNever = assertNever;
      util2.arrayToEnum = (items) => {
        const obj = {};
        for (const item of items) {
          obj[item] = item;
        }
        return obj;
      };
      util2.getValidEnumValues = (obj) => {
        const validKeys = util2.objectKeys(obj).filter((k) => typeof obj[obj[k]] !== "number");
        const filtered = {};
        for (const k of validKeys) {
          filtered[k] = obj[k];
        }
        return util2.objectValues(filtered);
      };
      util2.objectValues = (obj) => {
        return util2.objectKeys(obj).map(function(e) {
          return obj[e];
        });
      };
      util2.objectKeys = typeof Object.keys === "function" ? (obj) => Object.keys(obj) : (object2) => {
        const keys = [];
        for (const key in object2) {
          if (Object.prototype.hasOwnProperty.call(object2, key)) {
            keys.push(key);
          }
        }
        return keys;
      };
      util2.find = (arr, checker) => {
        for (const item of arr) {
          if (checker(item))
            return item;
        }
        return void 0;
      };
      util2.isInteger = typeof Number.isInteger === "function" ? (val) => Number.isInteger(val) : (val) => typeof val === "number" && isFinite(val) && Math.floor(val) === val;
      function joinValues(array3, separator = " | ") {
        return array3.map((val) => typeof val === "string" ? `'${val}'` : val).join(separator);
      }
      __name(joinValues, "joinValues");
      util2.joinValues = joinValues;
      util2.jsonStringifyReplacer = (_, value) => {
        if (typeof value === "bigint") {
          return value.toString();
        }
        return value;
      };
    })(util || (util = {}));
    (function(objectUtil2) {
      objectUtil2.mergeShapes = (first, second) => {
        return {
          ...first,
          ...second
          // second overwrites first
        };
      };
    })(objectUtil || (objectUtil = {}));
    ZodParsedType = util.arrayToEnum([
      "string",
      "nan",
      "number",
      "integer",
      "float",
      "boolean",
      "date",
      "bigint",
      "symbol",
      "function",
      "undefined",
      "null",
      "array",
      "object",
      "unknown",
      "promise",
      "void",
      "never",
      "map",
      "set"
    ]);
    getParsedType = /* @__PURE__ */ __name((data) => {
      const t = typeof data;
      switch (t) {
        case "undefined":
          return ZodParsedType.undefined;
        case "string":
          return ZodParsedType.string;
        case "number":
          return isNaN(data) ? ZodParsedType.nan : ZodParsedType.number;
        case "boolean":
          return ZodParsedType.boolean;
        case "function":
          return ZodParsedType.function;
        case "bigint":
          return ZodParsedType.bigint;
        case "symbol":
          return ZodParsedType.symbol;
        case "object":
          if (Array.isArray(data)) {
            return ZodParsedType.array;
          }
          if (data === null) {
            return ZodParsedType.null;
          }
          if (data.then && typeof data.then === "function" && data.catch && typeof data.catch === "function") {
            return ZodParsedType.promise;
          }
          if (typeof Map !== "undefined" && data instanceof Map) {
            return ZodParsedType.map;
          }
          if (typeof Set !== "undefined" && data instanceof Set) {
            return ZodParsedType.set;
          }
          if (typeof Date !== "undefined" && data instanceof Date) {
            return ZodParsedType.date;
          }
          return ZodParsedType.object;
        default:
          return ZodParsedType.unknown;
      }
    }, "getParsedType");
    ZodIssueCode = util.arrayToEnum([
      "invalid_type",
      "invalid_literal",
      "custom",
      "invalid_union",
      "invalid_union_discriminator",
      "invalid_enum_value",
      "unrecognized_keys",
      "invalid_arguments",
      "invalid_return_type",
      "invalid_date",
      "invalid_string",
      "too_small",
      "too_big",
      "invalid_intersection_types",
      "not_multiple_of",
      "not_finite"
    ]);
    quotelessJson = /* @__PURE__ */ __name((obj) => {
      const json = JSON.stringify(obj, null, 2);
      return json.replace(/"([^"]+)":/g, "$1:");
    }, "quotelessJson");
    ZodError = class _ZodError extends Error {
      static {
        __name(this, "ZodError");
      }
      get errors() {
        return this.issues;
      }
      constructor(issues) {
        super();
        this.issues = [];
        this.addIssue = (sub) => {
          this.issues = [...this.issues, sub];
        };
        this.addIssues = (subs = []) => {
          this.issues = [...this.issues, ...subs];
        };
        const actualProto = new.target.prototype;
        if (Object.setPrototypeOf) {
          Object.setPrototypeOf(this, actualProto);
        } else {
          this.__proto__ = actualProto;
        }
        this.name = "ZodError";
        this.issues = issues;
      }
      format(_mapper) {
        const mapper = _mapper || function(issue) {
          return issue.message;
        };
        const fieldErrors = { _errors: [] };
        const processError = /* @__PURE__ */ __name((error) => {
          for (const issue of error.issues) {
            if (issue.code === "invalid_union") {
              issue.unionErrors.map(processError);
            } else if (issue.code === "invalid_return_type") {
              processError(issue.returnTypeError);
            } else if (issue.code === "invalid_arguments") {
              processError(issue.argumentsError);
            } else if (issue.path.length === 0) {
              fieldErrors._errors.push(mapper(issue));
            } else {
              let curr = fieldErrors;
              let i = 0;
              while (i < issue.path.length) {
                const el = issue.path[i];
                const terminal = i === issue.path.length - 1;
                if (!terminal) {
                  curr[el] = curr[el] || { _errors: [] };
                } else {
                  curr[el] = curr[el] || { _errors: [] };
                  curr[el]._errors.push(mapper(issue));
                }
                curr = curr[el];
                i++;
              }
            }
          }
        }, "processError");
        processError(this);
        return fieldErrors;
      }
      static assert(value) {
        if (!(value instanceof _ZodError)) {
          throw new Error(`Not a ZodError: ${value}`);
        }
      }
      toString() {
        return this.message;
      }
      get message() {
        return JSON.stringify(this.issues, util.jsonStringifyReplacer, 2);
      }
      get isEmpty() {
        return this.issues.length === 0;
      }
      flatten(mapper = (issue) => issue.message) {
        const fieldErrors = {};
        const formErrors = [];
        for (const sub of this.issues) {
          if (sub.path.length > 0) {
            fieldErrors[sub.path[0]] = fieldErrors[sub.path[0]] || [];
            fieldErrors[sub.path[0]].push(mapper(sub));
          } else {
            formErrors.push(mapper(sub));
          }
        }
        return { formErrors, fieldErrors };
      }
      get formErrors() {
        return this.flatten();
      }
    };
    ZodError.create = (issues) => {
      const error = new ZodError(issues);
      return error;
    };
    errorMap = /* @__PURE__ */ __name((issue, _ctx) => {
      let message;
      switch (issue.code) {
        case ZodIssueCode.invalid_type:
          if (issue.received === ZodParsedType.undefined) {
            message = "Required";
          } else {
            message = `Expected ${issue.expected}, received ${issue.received}`;
          }
          break;
        case ZodIssueCode.invalid_literal:
          message = `Invalid literal value, expected ${JSON.stringify(issue.expected, util.jsonStringifyReplacer)}`;
          break;
        case ZodIssueCode.unrecognized_keys:
          message = `Unrecognized key(s) in object: ${util.joinValues(issue.keys, ", ")}`;
          break;
        case ZodIssueCode.invalid_union:
          message = `Invalid input`;
          break;
        case ZodIssueCode.invalid_union_discriminator:
          message = `Invalid discriminator value. Expected ${util.joinValues(issue.options)}`;
          break;
        case ZodIssueCode.invalid_enum_value:
          message = `Invalid enum value. Expected ${util.joinValues(issue.options)}, received '${issue.received}'`;
          break;
        case ZodIssueCode.invalid_arguments:
          message = `Invalid function arguments`;
          break;
        case ZodIssueCode.invalid_return_type:
          message = `Invalid function return type`;
          break;
        case ZodIssueCode.invalid_date:
          message = `Invalid date`;
          break;
        case ZodIssueCode.invalid_string:
          if (typeof issue.validation === "object") {
            if ("includes" in issue.validation) {
              message = `Invalid input: must include "${issue.validation.includes}"`;
              if (typeof issue.validation.position === "number") {
                message = `${message} at one or more positions greater than or equal to ${issue.validation.position}`;
              }
            } else if ("startsWith" in issue.validation) {
              message = `Invalid input: must start with "${issue.validation.startsWith}"`;
            } else if ("endsWith" in issue.validation) {
              message = `Invalid input: must end with "${issue.validation.endsWith}"`;
            } else {
              util.assertNever(issue.validation);
            }
          } else if (issue.validation !== "regex") {
            message = `Invalid ${issue.validation}`;
          } else {
            message = "Invalid";
          }
          break;
        case ZodIssueCode.too_small:
          if (issue.type === "array")
            message = `Array must contain ${issue.exact ? "exactly" : issue.inclusive ? `at least` : `more than`} ${issue.minimum} element(s)`;
          else if (issue.type === "string")
            message = `String must contain ${issue.exact ? "exactly" : issue.inclusive ? `at least` : `over`} ${issue.minimum} character(s)`;
          else if (issue.type === "number")
            message = `Number must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${issue.minimum}`;
          else if (issue.type === "date")
            message = `Date must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${new Date(Number(issue.minimum))}`;
          else
            message = "Invalid input";
          break;
        case ZodIssueCode.too_big:
          if (issue.type === "array")
            message = `Array must contain ${issue.exact ? `exactly` : issue.inclusive ? `at most` : `less than`} ${issue.maximum} element(s)`;
          else if (issue.type === "string")
            message = `String must contain ${issue.exact ? `exactly` : issue.inclusive ? `at most` : `under`} ${issue.maximum} character(s)`;
          else if (issue.type === "number")
            message = `Number must be ${issue.exact ? `exactly` : issue.inclusive ? `less than or equal to` : `less than`} ${issue.maximum}`;
          else if (issue.type === "bigint")
            message = `BigInt must be ${issue.exact ? `exactly` : issue.inclusive ? `less than or equal to` : `less than`} ${issue.maximum}`;
          else if (issue.type === "date")
            message = `Date must be ${issue.exact ? `exactly` : issue.inclusive ? `smaller than or equal to` : `smaller than`} ${new Date(Number(issue.maximum))}`;
          else
            message = "Invalid input";
          break;
        case ZodIssueCode.custom:
          message = `Invalid input`;
          break;
        case ZodIssueCode.invalid_intersection_types:
          message = `Intersection results could not be merged`;
          break;
        case ZodIssueCode.not_multiple_of:
          message = `Number must be a multiple of ${issue.multipleOf}`;
          break;
        case ZodIssueCode.not_finite:
          message = "Number must be finite";
          break;
        default:
          message = _ctx.defaultError;
          util.assertNever(issue);
      }
      return { message };
    }, "errorMap");
    overrideErrorMap = errorMap;
    __name(setErrorMap, "setErrorMap");
    __name(getErrorMap, "getErrorMap");
    makeIssue = /* @__PURE__ */ __name((params) => {
      const { data, path, errorMaps, issueData } = params;
      const fullPath = [...path, ...issueData.path || []];
      const fullIssue = {
        ...issueData,
        path: fullPath
      };
      if (issueData.message !== void 0) {
        return {
          ...issueData,
          path: fullPath,
          message: issueData.message
        };
      }
      let errorMessage = "";
      const maps = errorMaps.filter((m) => !!m).slice().reverse();
      for (const map2 of maps) {
        errorMessage = map2(fullIssue, { data, defaultError: errorMessage }).message;
      }
      return {
        ...issueData,
        path: fullPath,
        message: errorMessage
      };
    }, "makeIssue");
    EMPTY_PATH = [];
    __name(addIssueToContext, "addIssueToContext");
    ParseStatus = class _ParseStatus {
      static {
        __name(this, "ParseStatus");
      }
      constructor() {
        this.value = "valid";
      }
      dirty() {
        if (this.value === "valid")
          this.value = "dirty";
      }
      abort() {
        if (this.value !== "aborted")
          this.value = "aborted";
      }
      static mergeArray(status, results) {
        const arrayValue = [];
        for (const s of results) {
          if (s.status === "aborted")
            return INVALID;
          if (s.status === "dirty")
            status.dirty();
          arrayValue.push(s.value);
        }
        return { status: status.value, value: arrayValue };
      }
      static async mergeObjectAsync(status, pairs) {
        const syncPairs = [];
        for (const pair2 of pairs) {
          const key = await pair2.key;
          const value = await pair2.value;
          syncPairs.push({
            key,
            value
          });
        }
        return _ParseStatus.mergeObjectSync(status, syncPairs);
      }
      static mergeObjectSync(status, pairs) {
        const finalObject = {};
        for (const pair2 of pairs) {
          const { key, value } = pair2;
          if (key.status === "aborted")
            return INVALID;
          if (value.status === "aborted")
            return INVALID;
          if (key.status === "dirty")
            status.dirty();
          if (value.status === "dirty")
            status.dirty();
          if (key.value !== "__proto__" && (typeof value.value !== "undefined" || pair2.alwaysSet)) {
            finalObject[key.value] = value.value;
          }
        }
        return { status: status.value, value: finalObject };
      }
    };
    INVALID = Object.freeze({
      status: "aborted"
    });
    DIRTY = /* @__PURE__ */ __name((value) => ({ status: "dirty", value }), "DIRTY");
    OK = /* @__PURE__ */ __name((value) => ({ status: "valid", value }), "OK");
    isAborted = /* @__PURE__ */ __name((x) => x.status === "aborted", "isAborted");
    isDirty = /* @__PURE__ */ __name((x) => x.status === "dirty", "isDirty");
    isValid2 = /* @__PURE__ */ __name((x) => x.status === "valid", "isValid");
    isAsync = /* @__PURE__ */ __name((x) => typeof Promise !== "undefined" && x instanceof Promise, "isAsync");
    __name(__classPrivateFieldGet, "__classPrivateFieldGet");
    __name(__classPrivateFieldSet, "__classPrivateFieldSet");
    (function(errorUtil2) {
      errorUtil2.errToObj = (message) => typeof message === "string" ? { message } : message || {};
      errorUtil2.toString = (message) => typeof message === "string" ? message : message === null || message === void 0 ? void 0 : message.message;
    })(errorUtil || (errorUtil = {}));
    ParseInputLazyPath = class {
      static {
        __name(this, "ParseInputLazyPath");
      }
      constructor(parent, value, path, key) {
        this._cachedPath = [];
        this.parent = parent;
        this.data = value;
        this._path = path;
        this._key = key;
      }
      get path() {
        if (!this._cachedPath.length) {
          if (this._key instanceof Array) {
            this._cachedPath.push(...this._path, ...this._key);
          } else {
            this._cachedPath.push(...this._path, this._key);
          }
        }
        return this._cachedPath;
      }
    };
    handleResult = /* @__PURE__ */ __name((ctx, result) => {
      if (isValid2(result)) {
        return { success: true, data: result.value };
      } else {
        if (!ctx.common.issues.length) {
          throw new Error("Validation failed but no issues detected.");
        }
        return {
          success: false,
          get error() {
            if (this._error)
              return this._error;
            const error = new ZodError(ctx.common.issues);
            this._error = error;
            return this._error;
          }
        };
      }
    }, "handleResult");
    __name(processCreateParams, "processCreateParams");
    ZodType = class {
      static {
        __name(this, "ZodType");
      }
      get description() {
        return this._def.description;
      }
      _getType(input) {
        return getParsedType(input.data);
      }
      _getOrReturnCtx(input, ctx) {
        return ctx || {
          common: input.parent.common,
          data: input.data,
          parsedType: getParsedType(input.data),
          schemaErrorMap: this._def.errorMap,
          path: input.path,
          parent: input.parent
        };
      }
      _processInputParams(input) {
        return {
          status: new ParseStatus(),
          ctx: {
            common: input.parent.common,
            data: input.data,
            parsedType: getParsedType(input.data),
            schemaErrorMap: this._def.errorMap,
            path: input.path,
            parent: input.parent
          }
        };
      }
      _parseSync(input) {
        const result = this._parse(input);
        if (isAsync(result)) {
          throw new Error("Synchronous parse encountered promise.");
        }
        return result;
      }
      _parseAsync(input) {
        const result = this._parse(input);
        return Promise.resolve(result);
      }
      parse(data, params) {
        const result = this.safeParse(data, params);
        if (result.success)
          return result.data;
        throw result.error;
      }
      safeParse(data, params) {
        var _a;
        const ctx = {
          common: {
            issues: [],
            async: (_a = params === null || params === void 0 ? void 0 : params.async) !== null && _a !== void 0 ? _a : false,
            contextualErrorMap: params === null || params === void 0 ? void 0 : params.errorMap
          },
          path: (params === null || params === void 0 ? void 0 : params.path) || [],
          schemaErrorMap: this._def.errorMap,
          parent: null,
          data,
          parsedType: getParsedType(data)
        };
        const result = this._parseSync({ data, path: ctx.path, parent: ctx });
        return handleResult(ctx, result);
      }
      "~validate"(data) {
        var _a, _b;
        const ctx = {
          common: {
            issues: [],
            async: !!this["~standard"].async
          },
          path: [],
          schemaErrorMap: this._def.errorMap,
          parent: null,
          data,
          parsedType: getParsedType(data)
        };
        if (!this["~standard"].async) {
          try {
            const result = this._parseSync({ data, path: [], parent: ctx });
            return isValid2(result) ? {
              value: result.value
            } : {
              issues: ctx.common.issues
            };
          } catch (err) {
            if ((_b = (_a = err === null || err === void 0 ? void 0 : err.message) === null || _a === void 0 ? void 0 : _a.toLowerCase()) === null || _b === void 0 ? void 0 : _b.includes("encountered")) {
              this["~standard"].async = true;
            }
            ctx.common = {
              issues: [],
              async: true
            };
          }
        }
        return this._parseAsync({ data, path: [], parent: ctx }).then((result) => isValid2(result) ? {
          value: result.value
        } : {
          issues: ctx.common.issues
        });
      }
      async parseAsync(data, params) {
        const result = await this.safeParseAsync(data, params);
        if (result.success)
          return result.data;
        throw result.error;
      }
      async safeParseAsync(data, params) {
        const ctx = {
          common: {
            issues: [],
            contextualErrorMap: params === null || params === void 0 ? void 0 : params.errorMap,
            async: true
          },
          path: (params === null || params === void 0 ? void 0 : params.path) || [],
          schemaErrorMap: this._def.errorMap,
          parent: null,
          data,
          parsedType: getParsedType(data)
        };
        const maybeAsyncResult = this._parse({ data, path: ctx.path, parent: ctx });
        const result = await (isAsync(maybeAsyncResult) ? maybeAsyncResult : Promise.resolve(maybeAsyncResult));
        return handleResult(ctx, result);
      }
      refine(check, message) {
        const getIssueProperties = /* @__PURE__ */ __name((val) => {
          if (typeof message === "string" || typeof message === "undefined") {
            return { message };
          } else if (typeof message === "function") {
            return message(val);
          } else {
            return message;
          }
        }, "getIssueProperties");
        return this._refinement((val, ctx) => {
          const result = check(val);
          const setError = /* @__PURE__ */ __name(() => ctx.addIssue({
            code: ZodIssueCode.custom,
            ...getIssueProperties(val)
          }), "setError");
          if (typeof Promise !== "undefined" && result instanceof Promise) {
            return result.then((data) => {
              if (!data) {
                setError();
                return false;
              } else {
                return true;
              }
            });
          }
          if (!result) {
            setError();
            return false;
          } else {
            return true;
          }
        });
      }
      refinement(check, refinementData) {
        return this._refinement((val, ctx) => {
          if (!check(val)) {
            ctx.addIssue(typeof refinementData === "function" ? refinementData(val, ctx) : refinementData);
            return false;
          } else {
            return true;
          }
        });
      }
      _refinement(refinement) {
        return new ZodEffects({
          schema: this,
          typeName: ZodFirstPartyTypeKind.ZodEffects,
          effect: { type: "refinement", refinement }
        });
      }
      superRefine(refinement) {
        return this._refinement(refinement);
      }
      constructor(def) {
        this.spa = this.safeParseAsync;
        this._def = def;
        this.parse = this.parse.bind(this);
        this.safeParse = this.safeParse.bind(this);
        this.parseAsync = this.parseAsync.bind(this);
        this.safeParseAsync = this.safeParseAsync.bind(this);
        this.spa = this.spa.bind(this);
        this.refine = this.refine.bind(this);
        this.refinement = this.refinement.bind(this);
        this.superRefine = this.superRefine.bind(this);
        this.optional = this.optional.bind(this);
        this.nullable = this.nullable.bind(this);
        this.nullish = this.nullish.bind(this);
        this.array = this.array.bind(this);
        this.promise = this.promise.bind(this);
        this.or = this.or.bind(this);
        this.and = this.and.bind(this);
        this.transform = this.transform.bind(this);
        this.brand = this.brand.bind(this);
        this.default = this.default.bind(this);
        this.catch = this.catch.bind(this);
        this.describe = this.describe.bind(this);
        this.pipe = this.pipe.bind(this);
        this.readonly = this.readonly.bind(this);
        this.isNullable = this.isNullable.bind(this);
        this.isOptional = this.isOptional.bind(this);
        this["~standard"] = {
          version: 1,
          vendor: "zod",
          validate: /* @__PURE__ */ __name((data) => this["~validate"](data), "validate")
        };
      }
      optional() {
        return ZodOptional.create(this, this._def);
      }
      nullable() {
        return ZodNullable.create(this, this._def);
      }
      nullish() {
        return this.nullable().optional();
      }
      array() {
        return ZodArray.create(this);
      }
      promise() {
        return ZodPromise.create(this, this._def);
      }
      or(option) {
        return ZodUnion.create([this, option], this._def);
      }
      and(incoming) {
        return ZodIntersection.create(this, incoming, this._def);
      }
      transform(transform) {
        return new ZodEffects({
          ...processCreateParams(this._def),
          schema: this,
          typeName: ZodFirstPartyTypeKind.ZodEffects,
          effect: { type: "transform", transform }
        });
      }
      default(def) {
        const defaultValueFunc = typeof def === "function" ? def : () => def;
        return new ZodDefault({
          ...processCreateParams(this._def),
          innerType: this,
          defaultValue: defaultValueFunc,
          typeName: ZodFirstPartyTypeKind.ZodDefault
        });
      }
      brand() {
        return new ZodBranded({
          typeName: ZodFirstPartyTypeKind.ZodBranded,
          type: this,
          ...processCreateParams(this._def)
        });
      }
      catch(def) {
        const catchValueFunc = typeof def === "function" ? def : () => def;
        return new ZodCatch({
          ...processCreateParams(this._def),
          innerType: this,
          catchValue: catchValueFunc,
          typeName: ZodFirstPartyTypeKind.ZodCatch
        });
      }
      describe(description) {
        const This = this.constructor;
        return new This({
          ...this._def,
          description
        });
      }
      pipe(target) {
        return ZodPipeline.create(this, target);
      }
      readonly() {
        return ZodReadonly.create(this);
      }
      isOptional() {
        return this.safeParse(void 0).success;
      }
      isNullable() {
        return this.safeParse(null).success;
      }
    };
    cuidRegex = /^c[^\s-]{8,}$/i;
    cuid2Regex = /^[0-9a-z]+$/;
    ulidRegex = /^[0-9A-HJKMNP-TV-Z]{26}$/i;
    uuidRegex = /^[0-9a-fA-F]{8}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{12}$/i;
    nanoidRegex = /^[a-z0-9_-]{21}$/i;
    jwtRegex = /^[A-Za-z0-9-_]+\.[A-Za-z0-9-_]+\.[A-Za-z0-9-_]*$/;
    durationRegex = /^[-+]?P(?!$)(?:(?:[-+]?\d+Y)|(?:[-+]?\d+[.,]\d+Y$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:(?:[-+]?\d+W)|(?:[-+]?\d+[.,]\d+W$))?(?:(?:[-+]?\d+D)|(?:[-+]?\d+[.,]\d+D$))?(?:T(?=[\d+-])(?:(?:[-+]?\d+H)|(?:[-+]?\d+[.,]\d+H$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:[-+]?\d+(?:[.,]\d+)?S)?)??$/;
    emailRegex = /^(?!\.)(?!.*\.\.)([A-Z0-9_'+\-\.]*)[A-Z0-9_+-]@([A-Z0-9][A-Z0-9\-]*\.)+[A-Z]{2,}$/i;
    _emojiRegex = `^(\\p{Extended_Pictographic}|\\p{Emoji_Component})+$`;
    ipv4Regex = /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])$/;
    ipv4CidrRegex = /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\/(3[0-2]|[12]?[0-9])$/;
    ipv6Regex = /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))$/;
    ipv6CidrRegex = /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))\/(12[0-8]|1[01][0-9]|[1-9]?[0-9])$/;
    base64Regex = /^([0-9a-zA-Z+/]{4})*(([0-9a-zA-Z+/]{2}==)|([0-9a-zA-Z+/]{3}=))?$/;
    base64urlRegex = /^([0-9a-zA-Z-_]{4})*(([0-9a-zA-Z-_]{2}(==)?)|([0-9a-zA-Z-_]{3}(=)?))?$/;
    dateRegexSource = `((\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-((0[13578]|1[02])-(0[1-9]|[12]\\d|3[01])|(0[469]|11)-(0[1-9]|[12]\\d|30)|(02)-(0[1-9]|1\\d|2[0-8])))`;
    dateRegex = new RegExp(`^${dateRegexSource}$`);
    __name(timeRegexSource, "timeRegexSource");
    __name(timeRegex, "timeRegex");
    __name(datetimeRegex, "datetimeRegex");
    __name(isValidIP, "isValidIP");
    __name(isValidJWT, "isValidJWT");
    __name(isValidCidr, "isValidCidr");
    ZodString = class _ZodString extends ZodType {
      static {
        __name(this, "ZodString");
      }
      _parse(input) {
        if (this._def.coerce) {
          input.data = String(input.data);
        }
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.string) {
          const ctx2 = this._getOrReturnCtx(input);
          addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.string,
            received: ctx2.parsedType
          });
          return INVALID;
        }
        const status = new ParseStatus();
        let ctx = void 0;
        for (const check of this._def.checks) {
          if (check.kind === "min") {
            if (input.data.length < check.value) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_small,
                minimum: check.value,
                type: "string",
                inclusive: true,
                exact: false,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "max") {
            if (input.data.length > check.value) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_big,
                maximum: check.value,
                type: "string",
                inclusive: true,
                exact: false,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "length") {
            const tooBig = input.data.length > check.value;
            const tooSmall = input.data.length < check.value;
            if (tooBig || tooSmall) {
              ctx = this._getOrReturnCtx(input, ctx);
              if (tooBig) {
                addIssueToContext(ctx, {
                  code: ZodIssueCode.too_big,
                  maximum: check.value,
                  type: "string",
                  inclusive: true,
                  exact: true,
                  message: check.message
                });
              } else if (tooSmall) {
                addIssueToContext(ctx, {
                  code: ZodIssueCode.too_small,
                  minimum: check.value,
                  type: "string",
                  inclusive: true,
                  exact: true,
                  message: check.message
                });
              }
              status.dirty();
            }
          } else if (check.kind === "email") {
            if (!emailRegex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "email",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "emoji") {
            if (!emojiRegex) {
              emojiRegex = new RegExp(_emojiRegex, "u");
            }
            if (!emojiRegex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "emoji",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "uuid") {
            if (!uuidRegex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "uuid",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "nanoid") {
            if (!nanoidRegex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "nanoid",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "cuid") {
            if (!cuidRegex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "cuid",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "cuid2") {
            if (!cuid2Regex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "cuid2",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "ulid") {
            if (!ulidRegex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "ulid",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "url") {
            try {
              new URL(input.data);
            } catch (_a) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "url",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "regex") {
            check.regex.lastIndex = 0;
            const testResult = check.regex.test(input.data);
            if (!testResult) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "regex",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "trim") {
            input.data = input.data.trim();
          } else if (check.kind === "includes") {
            if (!input.data.includes(check.value, check.position)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: { includes: check.value, position: check.position },
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "toLowerCase") {
            input.data = input.data.toLowerCase();
          } else if (check.kind === "toUpperCase") {
            input.data = input.data.toUpperCase();
          } else if (check.kind === "startsWith") {
            if (!input.data.startsWith(check.value)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: { startsWith: check.value },
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "endsWith") {
            if (!input.data.endsWith(check.value)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: { endsWith: check.value },
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "datetime") {
            const regex = datetimeRegex(check);
            if (!regex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: "datetime",
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "date") {
            const regex = dateRegex;
            if (!regex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: "date",
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "time") {
            const regex = timeRegex(check);
            if (!regex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: "time",
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "duration") {
            if (!durationRegex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "duration",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "ip") {
            if (!isValidIP(input.data, check.version)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "ip",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "jwt") {
            if (!isValidJWT(input.data, check.alg)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "jwt",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "cidr") {
            if (!isValidCidr(input.data, check.version)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "cidr",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "base64") {
            if (!base64Regex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "base64",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "base64url") {
            if (!base64urlRegex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "base64url",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else {
            util.assertNever(check);
          }
        }
        return { status: status.value, value: input.data };
      }
      _regex(regex, validation, message) {
        return this.refinement((data) => regex.test(data), {
          validation,
          code: ZodIssueCode.invalid_string,
          ...errorUtil.errToObj(message)
        });
      }
      _addCheck(check) {
        return new _ZodString({
          ...this._def,
          checks: [...this._def.checks, check]
        });
      }
      email(message) {
        return this._addCheck({ kind: "email", ...errorUtil.errToObj(message) });
      }
      url(message) {
        return this._addCheck({ kind: "url", ...errorUtil.errToObj(message) });
      }
      emoji(message) {
        return this._addCheck({ kind: "emoji", ...errorUtil.errToObj(message) });
      }
      uuid(message) {
        return this._addCheck({ kind: "uuid", ...errorUtil.errToObj(message) });
      }
      nanoid(message) {
        return this._addCheck({ kind: "nanoid", ...errorUtil.errToObj(message) });
      }
      cuid(message) {
        return this._addCheck({ kind: "cuid", ...errorUtil.errToObj(message) });
      }
      cuid2(message) {
        return this._addCheck({ kind: "cuid2", ...errorUtil.errToObj(message) });
      }
      ulid(message) {
        return this._addCheck({ kind: "ulid", ...errorUtil.errToObj(message) });
      }
      base64(message) {
        return this._addCheck({ kind: "base64", ...errorUtil.errToObj(message) });
      }
      base64url(message) {
        return this._addCheck({
          kind: "base64url",
          ...errorUtil.errToObj(message)
        });
      }
      jwt(options) {
        return this._addCheck({ kind: "jwt", ...errorUtil.errToObj(options) });
      }
      ip(options) {
        return this._addCheck({ kind: "ip", ...errorUtil.errToObj(options) });
      }
      cidr(options) {
        return this._addCheck({ kind: "cidr", ...errorUtil.errToObj(options) });
      }
      datetime(options) {
        var _a, _b;
        if (typeof options === "string") {
          return this._addCheck({
            kind: "datetime",
            precision: null,
            offset: false,
            local: false,
            message: options
          });
        }
        return this._addCheck({
          kind: "datetime",
          precision: typeof (options === null || options === void 0 ? void 0 : options.precision) === "undefined" ? null : options === null || options === void 0 ? void 0 : options.precision,
          offset: (_a = options === null || options === void 0 ? void 0 : options.offset) !== null && _a !== void 0 ? _a : false,
          local: (_b = options === null || options === void 0 ? void 0 : options.local) !== null && _b !== void 0 ? _b : false,
          ...errorUtil.errToObj(options === null || options === void 0 ? void 0 : options.message)
        });
      }
      date(message) {
        return this._addCheck({ kind: "date", message });
      }
      time(options) {
        if (typeof options === "string") {
          return this._addCheck({
            kind: "time",
            precision: null,
            message: options
          });
        }
        return this._addCheck({
          kind: "time",
          precision: typeof (options === null || options === void 0 ? void 0 : options.precision) === "undefined" ? null : options === null || options === void 0 ? void 0 : options.precision,
          ...errorUtil.errToObj(options === null || options === void 0 ? void 0 : options.message)
        });
      }
      duration(message) {
        return this._addCheck({ kind: "duration", ...errorUtil.errToObj(message) });
      }
      regex(regex, message) {
        return this._addCheck({
          kind: "regex",
          regex,
          ...errorUtil.errToObj(message)
        });
      }
      includes(value, options) {
        return this._addCheck({
          kind: "includes",
          value,
          position: options === null || options === void 0 ? void 0 : options.position,
          ...errorUtil.errToObj(options === null || options === void 0 ? void 0 : options.message)
        });
      }
      startsWith(value, message) {
        return this._addCheck({
          kind: "startsWith",
          value,
          ...errorUtil.errToObj(message)
        });
      }
      endsWith(value, message) {
        return this._addCheck({
          kind: "endsWith",
          value,
          ...errorUtil.errToObj(message)
        });
      }
      min(minLength, message) {
        return this._addCheck({
          kind: "min",
          value: minLength,
          ...errorUtil.errToObj(message)
        });
      }
      max(maxLength, message) {
        return this._addCheck({
          kind: "max",
          value: maxLength,
          ...errorUtil.errToObj(message)
        });
      }
      length(len, message) {
        return this._addCheck({
          kind: "length",
          value: len,
          ...errorUtil.errToObj(message)
        });
      }
      /**
       * Equivalent to `.min(1)`
       */
      nonempty(message) {
        return this.min(1, errorUtil.errToObj(message));
      }
      trim() {
        return new _ZodString({
          ...this._def,
          checks: [...this._def.checks, { kind: "trim" }]
        });
      }
      toLowerCase() {
        return new _ZodString({
          ...this._def,
          checks: [...this._def.checks, { kind: "toLowerCase" }]
        });
      }
      toUpperCase() {
        return new _ZodString({
          ...this._def,
          checks: [...this._def.checks, { kind: "toUpperCase" }]
        });
      }
      get isDatetime() {
        return !!this._def.checks.find((ch) => ch.kind === "datetime");
      }
      get isDate() {
        return !!this._def.checks.find((ch) => ch.kind === "date");
      }
      get isTime() {
        return !!this._def.checks.find((ch) => ch.kind === "time");
      }
      get isDuration() {
        return !!this._def.checks.find((ch) => ch.kind === "duration");
      }
      get isEmail() {
        return !!this._def.checks.find((ch) => ch.kind === "email");
      }
      get isURL() {
        return !!this._def.checks.find((ch) => ch.kind === "url");
      }
      get isEmoji() {
        return !!this._def.checks.find((ch) => ch.kind === "emoji");
      }
      get isUUID() {
        return !!this._def.checks.find((ch) => ch.kind === "uuid");
      }
      get isNANOID() {
        return !!this._def.checks.find((ch) => ch.kind === "nanoid");
      }
      get isCUID() {
        return !!this._def.checks.find((ch) => ch.kind === "cuid");
      }
      get isCUID2() {
        return !!this._def.checks.find((ch) => ch.kind === "cuid2");
      }
      get isULID() {
        return !!this._def.checks.find((ch) => ch.kind === "ulid");
      }
      get isIP() {
        return !!this._def.checks.find((ch) => ch.kind === "ip");
      }
      get isCIDR() {
        return !!this._def.checks.find((ch) => ch.kind === "cidr");
      }
      get isBase64() {
        return !!this._def.checks.find((ch) => ch.kind === "base64");
      }
      get isBase64url() {
        return !!this._def.checks.find((ch) => ch.kind === "base64url");
      }
      get minLength() {
        let min = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "min") {
            if (min === null || ch.value > min)
              min = ch.value;
          }
        }
        return min;
      }
      get maxLength() {
        let max = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "max") {
            if (max === null || ch.value < max)
              max = ch.value;
          }
        }
        return max;
      }
    };
    ZodString.create = (params) => {
      var _a;
      return new ZodString({
        checks: [],
        typeName: ZodFirstPartyTypeKind.ZodString,
        coerce: (_a = params === null || params === void 0 ? void 0 : params.coerce) !== null && _a !== void 0 ? _a : false,
        ...processCreateParams(params)
      });
    };
    __name(floatSafeRemainder, "floatSafeRemainder");
    ZodNumber = class _ZodNumber extends ZodType {
      static {
        __name(this, "ZodNumber");
      }
      constructor() {
        super(...arguments);
        this.min = this.gte;
        this.max = this.lte;
        this.step = this.multipleOf;
      }
      _parse(input) {
        if (this._def.coerce) {
          input.data = Number(input.data);
        }
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.number) {
          const ctx2 = this._getOrReturnCtx(input);
          addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.number,
            received: ctx2.parsedType
          });
          return INVALID;
        }
        let ctx = void 0;
        const status = new ParseStatus();
        for (const check of this._def.checks) {
          if (check.kind === "int") {
            if (!util.isInteger(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_type,
                expected: "integer",
                received: "float",
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "min") {
            const tooSmall = check.inclusive ? input.data < check.value : input.data <= check.value;
            if (tooSmall) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_small,
                minimum: check.value,
                type: "number",
                inclusive: check.inclusive,
                exact: false,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "max") {
            const tooBig = check.inclusive ? input.data > check.value : input.data >= check.value;
            if (tooBig) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_big,
                maximum: check.value,
                type: "number",
                inclusive: check.inclusive,
                exact: false,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "multipleOf") {
            if (floatSafeRemainder(input.data, check.value) !== 0) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.not_multiple_of,
                multipleOf: check.value,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "finite") {
            if (!Number.isFinite(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.not_finite,
                message: check.message
              });
              status.dirty();
            }
          } else {
            util.assertNever(check);
          }
        }
        return { status: status.value, value: input.data };
      }
      gte(value, message) {
        return this.setLimit("min", value, true, errorUtil.toString(message));
      }
      gt(value, message) {
        return this.setLimit("min", value, false, errorUtil.toString(message));
      }
      lte(value, message) {
        return this.setLimit("max", value, true, errorUtil.toString(message));
      }
      lt(value, message) {
        return this.setLimit("max", value, false, errorUtil.toString(message));
      }
      setLimit(kind, value, inclusive, message) {
        return new _ZodNumber({
          ...this._def,
          checks: [
            ...this._def.checks,
            {
              kind,
              value,
              inclusive,
              message: errorUtil.toString(message)
            }
          ]
        });
      }
      _addCheck(check) {
        return new _ZodNumber({
          ...this._def,
          checks: [...this._def.checks, check]
        });
      }
      int(message) {
        return this._addCheck({
          kind: "int",
          message: errorUtil.toString(message)
        });
      }
      positive(message) {
        return this._addCheck({
          kind: "min",
          value: 0,
          inclusive: false,
          message: errorUtil.toString(message)
        });
      }
      negative(message) {
        return this._addCheck({
          kind: "max",
          value: 0,
          inclusive: false,
          message: errorUtil.toString(message)
        });
      }
      nonpositive(message) {
        return this._addCheck({
          kind: "max",
          value: 0,
          inclusive: true,
          message: errorUtil.toString(message)
        });
      }
      nonnegative(message) {
        return this._addCheck({
          kind: "min",
          value: 0,
          inclusive: true,
          message: errorUtil.toString(message)
        });
      }
      multipleOf(value, message) {
        return this._addCheck({
          kind: "multipleOf",
          value,
          message: errorUtil.toString(message)
        });
      }
      finite(message) {
        return this._addCheck({
          kind: "finite",
          message: errorUtil.toString(message)
        });
      }
      safe(message) {
        return this._addCheck({
          kind: "min",
          inclusive: true,
          value: Number.MIN_SAFE_INTEGER,
          message: errorUtil.toString(message)
        })._addCheck({
          kind: "max",
          inclusive: true,
          value: Number.MAX_SAFE_INTEGER,
          message: errorUtil.toString(message)
        });
      }
      get minValue() {
        let min = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "min") {
            if (min === null || ch.value > min)
              min = ch.value;
          }
        }
        return min;
      }
      get maxValue() {
        let max = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "max") {
            if (max === null || ch.value < max)
              max = ch.value;
          }
        }
        return max;
      }
      get isInt() {
        return !!this._def.checks.find((ch) => ch.kind === "int" || ch.kind === "multipleOf" && util.isInteger(ch.value));
      }
      get isFinite() {
        let max = null, min = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "finite" || ch.kind === "int" || ch.kind === "multipleOf") {
            return true;
          } else if (ch.kind === "min") {
            if (min === null || ch.value > min)
              min = ch.value;
          } else if (ch.kind === "max") {
            if (max === null || ch.value < max)
              max = ch.value;
          }
        }
        return Number.isFinite(min) && Number.isFinite(max);
      }
    };
    ZodNumber.create = (params) => {
      return new ZodNumber({
        checks: [],
        typeName: ZodFirstPartyTypeKind.ZodNumber,
        coerce: (params === null || params === void 0 ? void 0 : params.coerce) || false,
        ...processCreateParams(params)
      });
    };
    ZodBigInt = class _ZodBigInt extends ZodType {
      static {
        __name(this, "ZodBigInt");
      }
      constructor() {
        super(...arguments);
        this.min = this.gte;
        this.max = this.lte;
      }
      _parse(input) {
        if (this._def.coerce) {
          try {
            input.data = BigInt(input.data);
          } catch (_a) {
            return this._getInvalidInput(input);
          }
        }
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.bigint) {
          return this._getInvalidInput(input);
        }
        let ctx = void 0;
        const status = new ParseStatus();
        for (const check of this._def.checks) {
          if (check.kind === "min") {
            const tooSmall = check.inclusive ? input.data < check.value : input.data <= check.value;
            if (tooSmall) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_small,
                type: "bigint",
                minimum: check.value,
                inclusive: check.inclusive,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "max") {
            const tooBig = check.inclusive ? input.data > check.value : input.data >= check.value;
            if (tooBig) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_big,
                type: "bigint",
                maximum: check.value,
                inclusive: check.inclusive,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "multipleOf") {
            if (input.data % check.value !== BigInt(0)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.not_multiple_of,
                multipleOf: check.value,
                message: check.message
              });
              status.dirty();
            }
          } else {
            util.assertNever(check);
          }
        }
        return { status: status.value, value: input.data };
      }
      _getInvalidInput(input) {
        const ctx = this._getOrReturnCtx(input);
        addIssueToContext(ctx, {
          code: ZodIssueCode.invalid_type,
          expected: ZodParsedType.bigint,
          received: ctx.parsedType
        });
        return INVALID;
      }
      gte(value, message) {
        return this.setLimit("min", value, true, errorUtil.toString(message));
      }
      gt(value, message) {
        return this.setLimit("min", value, false, errorUtil.toString(message));
      }
      lte(value, message) {
        return this.setLimit("max", value, true, errorUtil.toString(message));
      }
      lt(value, message) {
        return this.setLimit("max", value, false, errorUtil.toString(message));
      }
      setLimit(kind, value, inclusive, message) {
        return new _ZodBigInt({
          ...this._def,
          checks: [
            ...this._def.checks,
            {
              kind,
              value,
              inclusive,
              message: errorUtil.toString(message)
            }
          ]
        });
      }
      _addCheck(check) {
        return new _ZodBigInt({
          ...this._def,
          checks: [...this._def.checks, check]
        });
      }
      positive(message) {
        return this._addCheck({
          kind: "min",
          value: BigInt(0),
          inclusive: false,
          message: errorUtil.toString(message)
        });
      }
      negative(message) {
        return this._addCheck({
          kind: "max",
          value: BigInt(0),
          inclusive: false,
          message: errorUtil.toString(message)
        });
      }
      nonpositive(message) {
        return this._addCheck({
          kind: "max",
          value: BigInt(0),
          inclusive: true,
          message: errorUtil.toString(message)
        });
      }
      nonnegative(message) {
        return this._addCheck({
          kind: "min",
          value: BigInt(0),
          inclusive: true,
          message: errorUtil.toString(message)
        });
      }
      multipleOf(value, message) {
        return this._addCheck({
          kind: "multipleOf",
          value,
          message: errorUtil.toString(message)
        });
      }
      get minValue() {
        let min = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "min") {
            if (min === null || ch.value > min)
              min = ch.value;
          }
        }
        return min;
      }
      get maxValue() {
        let max = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "max") {
            if (max === null || ch.value < max)
              max = ch.value;
          }
        }
        return max;
      }
    };
    ZodBigInt.create = (params) => {
      var _a;
      return new ZodBigInt({
        checks: [],
        typeName: ZodFirstPartyTypeKind.ZodBigInt,
        coerce: (_a = params === null || params === void 0 ? void 0 : params.coerce) !== null && _a !== void 0 ? _a : false,
        ...processCreateParams(params)
      });
    };
    ZodBoolean = class extends ZodType {
      static {
        __name(this, "ZodBoolean");
      }
      _parse(input) {
        if (this._def.coerce) {
          input.data = Boolean(input.data);
        }
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.boolean) {
          const ctx = this._getOrReturnCtx(input);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.boolean,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return OK(input.data);
      }
    };
    ZodBoolean.create = (params) => {
      return new ZodBoolean({
        typeName: ZodFirstPartyTypeKind.ZodBoolean,
        coerce: (params === null || params === void 0 ? void 0 : params.coerce) || false,
        ...processCreateParams(params)
      });
    };
    ZodDate = class _ZodDate extends ZodType {
      static {
        __name(this, "ZodDate");
      }
      _parse(input) {
        if (this._def.coerce) {
          input.data = new Date(input.data);
        }
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.date) {
          const ctx2 = this._getOrReturnCtx(input);
          addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.date,
            received: ctx2.parsedType
          });
          return INVALID;
        }
        if (isNaN(input.data.getTime())) {
          const ctx2 = this._getOrReturnCtx(input);
          addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_date
          });
          return INVALID;
        }
        const status = new ParseStatus();
        let ctx = void 0;
        for (const check of this._def.checks) {
          if (check.kind === "min") {
            if (input.data.getTime() < check.value) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_small,
                message: check.message,
                inclusive: true,
                exact: false,
                minimum: check.value,
                type: "date"
              });
              status.dirty();
            }
          } else if (check.kind === "max") {
            if (input.data.getTime() > check.value) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_big,
                message: check.message,
                inclusive: true,
                exact: false,
                maximum: check.value,
                type: "date"
              });
              status.dirty();
            }
          } else {
            util.assertNever(check);
          }
        }
        return {
          status: status.value,
          value: new Date(input.data.getTime())
        };
      }
      _addCheck(check) {
        return new _ZodDate({
          ...this._def,
          checks: [...this._def.checks, check]
        });
      }
      min(minDate, message) {
        return this._addCheck({
          kind: "min",
          value: minDate.getTime(),
          message: errorUtil.toString(message)
        });
      }
      max(maxDate, message) {
        return this._addCheck({
          kind: "max",
          value: maxDate.getTime(),
          message: errorUtil.toString(message)
        });
      }
      get minDate() {
        let min = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "min") {
            if (min === null || ch.value > min)
              min = ch.value;
          }
        }
        return min != null ? new Date(min) : null;
      }
      get maxDate() {
        let max = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "max") {
            if (max === null || ch.value < max)
              max = ch.value;
          }
        }
        return max != null ? new Date(max) : null;
      }
    };
    ZodDate.create = (params) => {
      return new ZodDate({
        checks: [],
        coerce: (params === null || params === void 0 ? void 0 : params.coerce) || false,
        typeName: ZodFirstPartyTypeKind.ZodDate,
        ...processCreateParams(params)
      });
    };
    ZodSymbol = class extends ZodType {
      static {
        __name(this, "ZodSymbol");
      }
      _parse(input) {
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.symbol) {
          const ctx = this._getOrReturnCtx(input);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.symbol,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return OK(input.data);
      }
    };
    ZodSymbol.create = (params) => {
      return new ZodSymbol({
        typeName: ZodFirstPartyTypeKind.ZodSymbol,
        ...processCreateParams(params)
      });
    };
    ZodUndefined = class extends ZodType {
      static {
        __name(this, "ZodUndefined");
      }
      _parse(input) {
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.undefined) {
          const ctx = this._getOrReturnCtx(input);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.undefined,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return OK(input.data);
      }
    };
    ZodUndefined.create = (params) => {
      return new ZodUndefined({
        typeName: ZodFirstPartyTypeKind.ZodUndefined,
        ...processCreateParams(params)
      });
    };
    ZodNull = class extends ZodType {
      static {
        __name(this, "ZodNull");
      }
      _parse(input) {
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.null) {
          const ctx = this._getOrReturnCtx(input);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.null,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return OK(input.data);
      }
    };
    ZodNull.create = (params) => {
      return new ZodNull({
        typeName: ZodFirstPartyTypeKind.ZodNull,
        ...processCreateParams(params)
      });
    };
    ZodAny = class extends ZodType {
      static {
        __name(this, "ZodAny");
      }
      constructor() {
        super(...arguments);
        this._any = true;
      }
      _parse(input) {
        return OK(input.data);
      }
    };
    ZodAny.create = (params) => {
      return new ZodAny({
        typeName: ZodFirstPartyTypeKind.ZodAny,
        ...processCreateParams(params)
      });
    };
    ZodUnknown = class extends ZodType {
      static {
        __name(this, "ZodUnknown");
      }
      constructor() {
        super(...arguments);
        this._unknown = true;
      }
      _parse(input) {
        return OK(input.data);
      }
    };
    ZodUnknown.create = (params) => {
      return new ZodUnknown({
        typeName: ZodFirstPartyTypeKind.ZodUnknown,
        ...processCreateParams(params)
      });
    };
    ZodNever = class extends ZodType {
      static {
        __name(this, "ZodNever");
      }
      _parse(input) {
        const ctx = this._getOrReturnCtx(input);
        addIssueToContext(ctx, {
          code: ZodIssueCode.invalid_type,
          expected: ZodParsedType.never,
          received: ctx.parsedType
        });
        return INVALID;
      }
    };
    ZodNever.create = (params) => {
      return new ZodNever({
        typeName: ZodFirstPartyTypeKind.ZodNever,
        ...processCreateParams(params)
      });
    };
    ZodVoid = class extends ZodType {
      static {
        __name(this, "ZodVoid");
      }
      _parse(input) {
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.undefined) {
          const ctx = this._getOrReturnCtx(input);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.void,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return OK(input.data);
      }
    };
    ZodVoid.create = (params) => {
      return new ZodVoid({
        typeName: ZodFirstPartyTypeKind.ZodVoid,
        ...processCreateParams(params)
      });
    };
    ZodArray = class _ZodArray extends ZodType {
      static {
        __name(this, "ZodArray");
      }
      _parse(input) {
        const { ctx, status } = this._processInputParams(input);
        const def = this._def;
        if (ctx.parsedType !== ZodParsedType.array) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.array,
            received: ctx.parsedType
          });
          return INVALID;
        }
        if (def.exactLength !== null) {
          const tooBig = ctx.data.length > def.exactLength.value;
          const tooSmall = ctx.data.length < def.exactLength.value;
          if (tooBig || tooSmall) {
            addIssueToContext(ctx, {
              code: tooBig ? ZodIssueCode.too_big : ZodIssueCode.too_small,
              minimum: tooSmall ? def.exactLength.value : void 0,
              maximum: tooBig ? def.exactLength.value : void 0,
              type: "array",
              inclusive: true,
              exact: true,
              message: def.exactLength.message
            });
            status.dirty();
          }
        }
        if (def.minLength !== null) {
          if (ctx.data.length < def.minLength.value) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_small,
              minimum: def.minLength.value,
              type: "array",
              inclusive: true,
              exact: false,
              message: def.minLength.message
            });
            status.dirty();
          }
        }
        if (def.maxLength !== null) {
          if (ctx.data.length > def.maxLength.value) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_big,
              maximum: def.maxLength.value,
              type: "array",
              inclusive: true,
              exact: false,
              message: def.maxLength.message
            });
            status.dirty();
          }
        }
        if (ctx.common.async) {
          return Promise.all([...ctx.data].map((item, i) => {
            return def.type._parseAsync(new ParseInputLazyPath(ctx, item, ctx.path, i));
          })).then((result2) => {
            return ParseStatus.mergeArray(status, result2);
          });
        }
        const result = [...ctx.data].map((item, i) => {
          return def.type._parseSync(new ParseInputLazyPath(ctx, item, ctx.path, i));
        });
        return ParseStatus.mergeArray(status, result);
      }
      get element() {
        return this._def.type;
      }
      min(minLength, message) {
        return new _ZodArray({
          ...this._def,
          minLength: { value: minLength, message: errorUtil.toString(message) }
        });
      }
      max(maxLength, message) {
        return new _ZodArray({
          ...this._def,
          maxLength: { value: maxLength, message: errorUtil.toString(message) }
        });
      }
      length(len, message) {
        return new _ZodArray({
          ...this._def,
          exactLength: { value: len, message: errorUtil.toString(message) }
        });
      }
      nonempty(message) {
        return this.min(1, message);
      }
    };
    ZodArray.create = (schema, params) => {
      return new ZodArray({
        type: schema,
        minLength: null,
        maxLength: null,
        exactLength: null,
        typeName: ZodFirstPartyTypeKind.ZodArray,
        ...processCreateParams(params)
      });
    };
    __name(deepPartialify, "deepPartialify");
    ZodObject = class _ZodObject extends ZodType {
      static {
        __name(this, "ZodObject");
      }
      constructor() {
        super(...arguments);
        this._cached = null;
        this.nonstrict = this.passthrough;
        this.augment = this.extend;
      }
      _getCached() {
        if (this._cached !== null)
          return this._cached;
        const shape = this._def.shape();
        const keys = util.objectKeys(shape);
        return this._cached = { shape, keys };
      }
      _parse(input) {
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.object) {
          const ctx2 = this._getOrReturnCtx(input);
          addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.object,
            received: ctx2.parsedType
          });
          return INVALID;
        }
        const { status, ctx } = this._processInputParams(input);
        const { shape, keys: shapeKeys } = this._getCached();
        const extraKeys = [];
        if (!(this._def.catchall instanceof ZodNever && this._def.unknownKeys === "strip")) {
          for (const key in ctx.data) {
            if (!shapeKeys.includes(key)) {
              extraKeys.push(key);
            }
          }
        }
        const pairs = [];
        for (const key of shapeKeys) {
          const keyValidator = shape[key];
          const value = ctx.data[key];
          pairs.push({
            key: { status: "valid", value: key },
            value: keyValidator._parse(new ParseInputLazyPath(ctx, value, ctx.path, key)),
            alwaysSet: key in ctx.data
          });
        }
        if (this._def.catchall instanceof ZodNever) {
          const unknownKeys = this._def.unknownKeys;
          if (unknownKeys === "passthrough") {
            for (const key of extraKeys) {
              pairs.push({
                key: { status: "valid", value: key },
                value: { status: "valid", value: ctx.data[key] }
              });
            }
          } else if (unknownKeys === "strict") {
            if (extraKeys.length > 0) {
              addIssueToContext(ctx, {
                code: ZodIssueCode.unrecognized_keys,
                keys: extraKeys
              });
              status.dirty();
            }
          } else if (unknownKeys === "strip") ;
          else {
            throw new Error(`Internal ZodObject error: invalid unknownKeys value.`);
          }
        } else {
          const catchall = this._def.catchall;
          for (const key of extraKeys) {
            const value = ctx.data[key];
            pairs.push({
              key: { status: "valid", value: key },
              value: catchall._parse(
                new ParseInputLazyPath(ctx, value, ctx.path, key)
                //, ctx.child(key), value, getParsedType(value)
              ),
              alwaysSet: key in ctx.data
            });
          }
        }
        if (ctx.common.async) {
          return Promise.resolve().then(async () => {
            const syncPairs = [];
            for (const pair2 of pairs) {
              const key = await pair2.key;
              const value = await pair2.value;
              syncPairs.push({
                key,
                value,
                alwaysSet: pair2.alwaysSet
              });
            }
            return syncPairs;
          }).then((syncPairs) => {
            return ParseStatus.mergeObjectSync(status, syncPairs);
          });
        } else {
          return ParseStatus.mergeObjectSync(status, pairs);
        }
      }
      get shape() {
        return this._def.shape();
      }
      strict(message) {
        errorUtil.errToObj;
        return new _ZodObject({
          ...this._def,
          unknownKeys: "strict",
          ...message !== void 0 ? {
            errorMap: /* @__PURE__ */ __name((issue, ctx) => {
              var _a, _b, _c, _d;
              const defaultError = (_c = (_b = (_a = this._def).errorMap) === null || _b === void 0 ? void 0 : _b.call(_a, issue, ctx).message) !== null && _c !== void 0 ? _c : ctx.defaultError;
              if (issue.code === "unrecognized_keys")
                return {
                  message: (_d = errorUtil.errToObj(message).message) !== null && _d !== void 0 ? _d : defaultError
                };
              return {
                message: defaultError
              };
            }, "errorMap")
          } : {}
        });
      }
      strip() {
        return new _ZodObject({
          ...this._def,
          unknownKeys: "strip"
        });
      }
      passthrough() {
        return new _ZodObject({
          ...this._def,
          unknownKeys: "passthrough"
        });
      }
      // const AugmentFactory =
      //   <Def extends ZodObjectDef>(def: Def) =>
      //   <Augmentation extends ZodRawShape>(
      //     augmentation: Augmentation
      //   ): ZodObject<
      //     extendShape<ReturnType<Def["shape"]>, Augmentation>,
      //     Def["unknownKeys"],
      //     Def["catchall"]
      //   > => {
      //     return new ZodObject({
      //       ...def,
      //       shape: () => ({
      //         ...def.shape(),
      //         ...augmentation,
      //       }),
      //     }) as any;
      //   };
      extend(augmentation) {
        return new _ZodObject({
          ...this._def,
          shape: /* @__PURE__ */ __name(() => ({
            ...this._def.shape(),
            ...augmentation
          }), "shape")
        });
      }
      /**
       * Prior to zod@1.0.12 there was a bug in the
       * inferred type of merged objects. Please
       * upgrade if you are experiencing issues.
       */
      merge(merging) {
        const merged = new _ZodObject({
          unknownKeys: merging._def.unknownKeys,
          catchall: merging._def.catchall,
          shape: /* @__PURE__ */ __name(() => ({
            ...this._def.shape(),
            ...merging._def.shape()
          }), "shape"),
          typeName: ZodFirstPartyTypeKind.ZodObject
        });
        return merged;
      }
      // merge<
      //   Incoming extends AnyZodObject,
      //   Augmentation extends Incoming["shape"],
      //   NewOutput extends {
      //     [k in keyof Augmentation | keyof Output]: k extends keyof Augmentation
      //       ? Augmentation[k]["_output"]
      //       : k extends keyof Output
      //       ? Output[k]
      //       : never;
      //   },
      //   NewInput extends {
      //     [k in keyof Augmentation | keyof Input]: k extends keyof Augmentation
      //       ? Augmentation[k]["_input"]
      //       : k extends keyof Input
      //       ? Input[k]
      //       : never;
      //   }
      // >(
      //   merging: Incoming
      // ): ZodObject<
      //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
      //   Incoming["_def"]["unknownKeys"],
      //   Incoming["_def"]["catchall"],
      //   NewOutput,
      //   NewInput
      // > {
      //   const merged: any = new ZodObject({
      //     unknownKeys: merging._def.unknownKeys,
      //     catchall: merging._def.catchall,
      //     shape: () =>
      //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
      //     typeName: ZodFirstPartyTypeKind.ZodObject,
      //   }) as any;
      //   return merged;
      // }
      setKey(key, schema) {
        return this.augment({ [key]: schema });
      }
      // merge<Incoming extends AnyZodObject>(
      //   merging: Incoming
      // ): //ZodObject<T & Incoming["_shape"], UnknownKeys, Catchall> = (merging) => {
      // ZodObject<
      //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
      //   Incoming["_def"]["unknownKeys"],
      //   Incoming["_def"]["catchall"]
      // > {
      //   // const mergedShape = objectUtil.mergeShapes(
      //   //   this._def.shape(),
      //   //   merging._def.shape()
      //   // );
      //   const merged: any = new ZodObject({
      //     unknownKeys: merging._def.unknownKeys,
      //     catchall: merging._def.catchall,
      //     shape: () =>
      //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
      //     typeName: ZodFirstPartyTypeKind.ZodObject,
      //   }) as any;
      //   return merged;
      // }
      catchall(index) {
        return new _ZodObject({
          ...this._def,
          catchall: index
        });
      }
      pick(mask) {
        const shape = {};
        util.objectKeys(mask).forEach((key) => {
          if (mask[key] && this.shape[key]) {
            shape[key] = this.shape[key];
          }
        });
        return new _ZodObject({
          ...this._def,
          shape: /* @__PURE__ */ __name(() => shape, "shape")
        });
      }
      omit(mask) {
        const shape = {};
        util.objectKeys(this.shape).forEach((key) => {
          if (!mask[key]) {
            shape[key] = this.shape[key];
          }
        });
        return new _ZodObject({
          ...this._def,
          shape: /* @__PURE__ */ __name(() => shape, "shape")
        });
      }
      /**
       * @deprecated
       */
      deepPartial() {
        return deepPartialify(this);
      }
      partial(mask) {
        const newShape = {};
        util.objectKeys(this.shape).forEach((key) => {
          const fieldSchema = this.shape[key];
          if (mask && !mask[key]) {
            newShape[key] = fieldSchema;
          } else {
            newShape[key] = fieldSchema.optional();
          }
        });
        return new _ZodObject({
          ...this._def,
          shape: /* @__PURE__ */ __name(() => newShape, "shape")
        });
      }
      required(mask) {
        const newShape = {};
        util.objectKeys(this.shape).forEach((key) => {
          if (mask && !mask[key]) {
            newShape[key] = this.shape[key];
          } else {
            const fieldSchema = this.shape[key];
            let newField = fieldSchema;
            while (newField instanceof ZodOptional) {
              newField = newField._def.innerType;
            }
            newShape[key] = newField;
          }
        });
        return new _ZodObject({
          ...this._def,
          shape: /* @__PURE__ */ __name(() => newShape, "shape")
        });
      }
      keyof() {
        return createZodEnum(util.objectKeys(this.shape));
      }
    };
    ZodObject.create = (shape, params) => {
      return new ZodObject({
        shape: /* @__PURE__ */ __name(() => shape, "shape"),
        unknownKeys: "strip",
        catchall: ZodNever.create(),
        typeName: ZodFirstPartyTypeKind.ZodObject,
        ...processCreateParams(params)
      });
    };
    ZodObject.strictCreate = (shape, params) => {
      return new ZodObject({
        shape: /* @__PURE__ */ __name(() => shape, "shape"),
        unknownKeys: "strict",
        catchall: ZodNever.create(),
        typeName: ZodFirstPartyTypeKind.ZodObject,
        ...processCreateParams(params)
      });
    };
    ZodObject.lazycreate = (shape, params) => {
      return new ZodObject({
        shape,
        unknownKeys: "strip",
        catchall: ZodNever.create(),
        typeName: ZodFirstPartyTypeKind.ZodObject,
        ...processCreateParams(params)
      });
    };
    ZodUnion = class extends ZodType {
      static {
        __name(this, "ZodUnion");
      }
      _parse(input) {
        const { ctx } = this._processInputParams(input);
        const options = this._def.options;
        function handleResults(results) {
          for (const result of results) {
            if (result.result.status === "valid") {
              return result.result;
            }
          }
          for (const result of results) {
            if (result.result.status === "dirty") {
              ctx.common.issues.push(...result.ctx.common.issues);
              return result.result;
            }
          }
          const unionErrors = results.map((result) => new ZodError(result.ctx.common.issues));
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_union,
            unionErrors
          });
          return INVALID;
        }
        __name(handleResults, "handleResults");
        if (ctx.common.async) {
          return Promise.all(options.map(async (option) => {
            const childCtx = {
              ...ctx,
              common: {
                ...ctx.common,
                issues: []
              },
              parent: null
            };
            return {
              result: await option._parseAsync({
                data: ctx.data,
                path: ctx.path,
                parent: childCtx
              }),
              ctx: childCtx
            };
          })).then(handleResults);
        } else {
          let dirty = void 0;
          const issues = [];
          for (const option of options) {
            const childCtx = {
              ...ctx,
              common: {
                ...ctx.common,
                issues: []
              },
              parent: null
            };
            const result = option._parseSync({
              data: ctx.data,
              path: ctx.path,
              parent: childCtx
            });
            if (result.status === "valid") {
              return result;
            } else if (result.status === "dirty" && !dirty) {
              dirty = { result, ctx: childCtx };
            }
            if (childCtx.common.issues.length) {
              issues.push(childCtx.common.issues);
            }
          }
          if (dirty) {
            ctx.common.issues.push(...dirty.ctx.common.issues);
            return dirty.result;
          }
          const unionErrors = issues.map((issues2) => new ZodError(issues2));
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_union,
            unionErrors
          });
          return INVALID;
        }
      }
      get options() {
        return this._def.options;
      }
    };
    ZodUnion.create = (types, params) => {
      return new ZodUnion({
        options: types,
        typeName: ZodFirstPartyTypeKind.ZodUnion,
        ...processCreateParams(params)
      });
    };
    getDiscriminator = /* @__PURE__ */ __name((type) => {
      if (type instanceof ZodLazy) {
        return getDiscriminator(type.schema);
      } else if (type instanceof ZodEffects) {
        return getDiscriminator(type.innerType());
      } else if (type instanceof ZodLiteral) {
        return [type.value];
      } else if (type instanceof ZodEnum) {
        return type.options;
      } else if (type instanceof ZodNativeEnum) {
        return util.objectValues(type.enum);
      } else if (type instanceof ZodDefault) {
        return getDiscriminator(type._def.innerType);
      } else if (type instanceof ZodUndefined) {
        return [void 0];
      } else if (type instanceof ZodNull) {
        return [null];
      } else if (type instanceof ZodOptional) {
        return [void 0, ...getDiscriminator(type.unwrap())];
      } else if (type instanceof ZodNullable) {
        return [null, ...getDiscriminator(type.unwrap())];
      } else if (type instanceof ZodBranded) {
        return getDiscriminator(type.unwrap());
      } else if (type instanceof ZodReadonly) {
        return getDiscriminator(type.unwrap());
      } else if (type instanceof ZodCatch) {
        return getDiscriminator(type._def.innerType);
      } else {
        return [];
      }
    }, "getDiscriminator");
    ZodDiscriminatedUnion = class _ZodDiscriminatedUnion extends ZodType {
      static {
        __name(this, "ZodDiscriminatedUnion");
      }
      _parse(input) {
        const { ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.object) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.object,
            received: ctx.parsedType
          });
          return INVALID;
        }
        const discriminator = this.discriminator;
        const discriminatorValue = ctx.data[discriminator];
        const option = this.optionsMap.get(discriminatorValue);
        if (!option) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_union_discriminator,
            options: Array.from(this.optionsMap.keys()),
            path: [discriminator]
          });
          return INVALID;
        }
        if (ctx.common.async) {
          return option._parseAsync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          });
        } else {
          return option._parseSync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          });
        }
      }
      get discriminator() {
        return this._def.discriminator;
      }
      get options() {
        return this._def.options;
      }
      get optionsMap() {
        return this._def.optionsMap;
      }
      /**
       * The constructor of the discriminated union schema. Its behaviour is very similar to that of the normal z.union() constructor.
       * However, it only allows a union of objects, all of which need to share a discriminator property. This property must
       * have a different value for each object in the union.
       * @param discriminator the name of the discriminator property
       * @param types an array of object schemas
       * @param params
       */
      static create(discriminator, options, params) {
        const optionsMap = /* @__PURE__ */ new Map();
        for (const type of options) {
          const discriminatorValues = getDiscriminator(type.shape[discriminator]);
          if (!discriminatorValues.length) {
            throw new Error(`A discriminator value for key \`${discriminator}\` could not be extracted from all schema options`);
          }
          for (const value of discriminatorValues) {
            if (optionsMap.has(value)) {
              throw new Error(`Discriminator property ${String(discriminator)} has duplicate value ${String(value)}`);
            }
            optionsMap.set(value, type);
          }
        }
        return new _ZodDiscriminatedUnion({
          typeName: ZodFirstPartyTypeKind.ZodDiscriminatedUnion,
          discriminator,
          options,
          optionsMap,
          ...processCreateParams(params)
        });
      }
    };
    __name(mergeValues, "mergeValues");
    ZodIntersection = class extends ZodType {
      static {
        __name(this, "ZodIntersection");
      }
      _parse(input) {
        const { status, ctx } = this._processInputParams(input);
        const handleParsed = /* @__PURE__ */ __name((parsedLeft, parsedRight) => {
          if (isAborted(parsedLeft) || isAborted(parsedRight)) {
            return INVALID;
          }
          const merged = mergeValues(parsedLeft.value, parsedRight.value);
          if (!merged.valid) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.invalid_intersection_types
            });
            return INVALID;
          }
          if (isDirty(parsedLeft) || isDirty(parsedRight)) {
            status.dirty();
          }
          return { status: status.value, value: merged.data };
        }, "handleParsed");
        if (ctx.common.async) {
          return Promise.all([
            this._def.left._parseAsync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            }),
            this._def.right._parseAsync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            })
          ]).then(([left, right]) => handleParsed(left, right));
        } else {
          return handleParsed(this._def.left._parseSync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          }), this._def.right._parseSync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          }));
        }
      }
    };
    ZodIntersection.create = (left, right, params) => {
      return new ZodIntersection({
        left,
        right,
        typeName: ZodFirstPartyTypeKind.ZodIntersection,
        ...processCreateParams(params)
      });
    };
    ZodTuple = class _ZodTuple extends ZodType {
      static {
        __name(this, "ZodTuple");
      }
      _parse(input) {
        const { status, ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.array) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.array,
            received: ctx.parsedType
          });
          return INVALID;
        }
        if (ctx.data.length < this._def.items.length) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            minimum: this._def.items.length,
            inclusive: true,
            exact: false,
            type: "array"
          });
          return INVALID;
        }
        const rest = this._def.rest;
        if (!rest && ctx.data.length > this._def.items.length) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            maximum: this._def.items.length,
            inclusive: true,
            exact: false,
            type: "array"
          });
          status.dirty();
        }
        const items = [...ctx.data].map((item, itemIndex) => {
          const schema = this._def.items[itemIndex] || this._def.rest;
          if (!schema)
            return null;
          return schema._parse(new ParseInputLazyPath(ctx, item, ctx.path, itemIndex));
        }).filter((x) => !!x);
        if (ctx.common.async) {
          return Promise.all(items).then((results) => {
            return ParseStatus.mergeArray(status, results);
          });
        } else {
          return ParseStatus.mergeArray(status, items);
        }
      }
      get items() {
        return this._def.items;
      }
      rest(rest) {
        return new _ZodTuple({
          ...this._def,
          rest
        });
      }
    };
    ZodTuple.create = (schemas, params) => {
      if (!Array.isArray(schemas)) {
        throw new Error("You must pass an array of schemas to z.tuple([ ... ])");
      }
      return new ZodTuple({
        items: schemas,
        typeName: ZodFirstPartyTypeKind.ZodTuple,
        rest: null,
        ...processCreateParams(params)
      });
    };
    ZodRecord = class _ZodRecord extends ZodType {
      static {
        __name(this, "ZodRecord");
      }
      get keySchema() {
        return this._def.keyType;
      }
      get valueSchema() {
        return this._def.valueType;
      }
      _parse(input) {
        const { status, ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.object) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.object,
            received: ctx.parsedType
          });
          return INVALID;
        }
        const pairs = [];
        const keyType = this._def.keyType;
        const valueType = this._def.valueType;
        for (const key in ctx.data) {
          pairs.push({
            key: keyType._parse(new ParseInputLazyPath(ctx, key, ctx.path, key)),
            value: valueType._parse(new ParseInputLazyPath(ctx, ctx.data[key], ctx.path, key)),
            alwaysSet: key in ctx.data
          });
        }
        if (ctx.common.async) {
          return ParseStatus.mergeObjectAsync(status, pairs);
        } else {
          return ParseStatus.mergeObjectSync(status, pairs);
        }
      }
      get element() {
        return this._def.valueType;
      }
      static create(first, second, third) {
        if (second instanceof ZodType) {
          return new _ZodRecord({
            keyType: first,
            valueType: second,
            typeName: ZodFirstPartyTypeKind.ZodRecord,
            ...processCreateParams(third)
          });
        }
        return new _ZodRecord({
          keyType: ZodString.create(),
          valueType: first,
          typeName: ZodFirstPartyTypeKind.ZodRecord,
          ...processCreateParams(second)
        });
      }
    };
    ZodMap = class extends ZodType {
      static {
        __name(this, "ZodMap");
      }
      get keySchema() {
        return this._def.keyType;
      }
      get valueSchema() {
        return this._def.valueType;
      }
      _parse(input) {
        const { status, ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.map) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.map,
            received: ctx.parsedType
          });
          return INVALID;
        }
        const keyType = this._def.keyType;
        const valueType = this._def.valueType;
        const pairs = [...ctx.data.entries()].map(([key, value], index) => {
          return {
            key: keyType._parse(new ParseInputLazyPath(ctx, key, ctx.path, [index, "key"])),
            value: valueType._parse(new ParseInputLazyPath(ctx, value, ctx.path, [index, "value"]))
          };
        });
        if (ctx.common.async) {
          const finalMap = /* @__PURE__ */ new Map();
          return Promise.resolve().then(async () => {
            for (const pair2 of pairs) {
              const key = await pair2.key;
              const value = await pair2.value;
              if (key.status === "aborted" || value.status === "aborted") {
                return INVALID;
              }
              if (key.status === "dirty" || value.status === "dirty") {
                status.dirty();
              }
              finalMap.set(key.value, value.value);
            }
            return { status: status.value, value: finalMap };
          });
        } else {
          const finalMap = /* @__PURE__ */ new Map();
          for (const pair2 of pairs) {
            const key = pair2.key;
            const value = pair2.value;
            if (key.status === "aborted" || value.status === "aborted") {
              return INVALID;
            }
            if (key.status === "dirty" || value.status === "dirty") {
              status.dirty();
            }
            finalMap.set(key.value, value.value);
          }
          return { status: status.value, value: finalMap };
        }
      }
    };
    ZodMap.create = (keyType, valueType, params) => {
      return new ZodMap({
        valueType,
        keyType,
        typeName: ZodFirstPartyTypeKind.ZodMap,
        ...processCreateParams(params)
      });
    };
    ZodSet = class _ZodSet extends ZodType {
      static {
        __name(this, "ZodSet");
      }
      _parse(input) {
        const { status, ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.set) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.set,
            received: ctx.parsedType
          });
          return INVALID;
        }
        const def = this._def;
        if (def.minSize !== null) {
          if (ctx.data.size < def.minSize.value) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_small,
              minimum: def.minSize.value,
              type: "set",
              inclusive: true,
              exact: false,
              message: def.minSize.message
            });
            status.dirty();
          }
        }
        if (def.maxSize !== null) {
          if (ctx.data.size > def.maxSize.value) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_big,
              maximum: def.maxSize.value,
              type: "set",
              inclusive: true,
              exact: false,
              message: def.maxSize.message
            });
            status.dirty();
          }
        }
        const valueType = this._def.valueType;
        function finalizeSet(elements2) {
          const parsedSet = /* @__PURE__ */ new Set();
          for (const element of elements2) {
            if (element.status === "aborted")
              return INVALID;
            if (element.status === "dirty")
              status.dirty();
            parsedSet.add(element.value);
          }
          return { status: status.value, value: parsedSet };
        }
        __name(finalizeSet, "finalizeSet");
        const elements = [...ctx.data.values()].map((item, i) => valueType._parse(new ParseInputLazyPath(ctx, item, ctx.path, i)));
        if (ctx.common.async) {
          return Promise.all(elements).then((elements2) => finalizeSet(elements2));
        } else {
          return finalizeSet(elements);
        }
      }
      min(minSize, message) {
        return new _ZodSet({
          ...this._def,
          minSize: { value: minSize, message: errorUtil.toString(message) }
        });
      }
      max(maxSize, message) {
        return new _ZodSet({
          ...this._def,
          maxSize: { value: maxSize, message: errorUtil.toString(message) }
        });
      }
      size(size, message) {
        return this.min(size, message).max(size, message);
      }
      nonempty(message) {
        return this.min(1, message);
      }
    };
    ZodSet.create = (valueType, params) => {
      return new ZodSet({
        valueType,
        minSize: null,
        maxSize: null,
        typeName: ZodFirstPartyTypeKind.ZodSet,
        ...processCreateParams(params)
      });
    };
    ZodFunction = class _ZodFunction extends ZodType {
      static {
        __name(this, "ZodFunction");
      }
      constructor() {
        super(...arguments);
        this.validate = this.implement;
      }
      _parse(input) {
        const { ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.function) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.function,
            received: ctx.parsedType
          });
          return INVALID;
        }
        function makeArgsIssue(args, error) {
          return makeIssue({
            data: args,
            path: ctx.path,
            errorMaps: [
              ctx.common.contextualErrorMap,
              ctx.schemaErrorMap,
              getErrorMap(),
              errorMap
            ].filter((x) => !!x),
            issueData: {
              code: ZodIssueCode.invalid_arguments,
              argumentsError: error
            }
          });
        }
        __name(makeArgsIssue, "makeArgsIssue");
        function makeReturnsIssue(returns, error) {
          return makeIssue({
            data: returns,
            path: ctx.path,
            errorMaps: [
              ctx.common.contextualErrorMap,
              ctx.schemaErrorMap,
              getErrorMap(),
              errorMap
            ].filter((x) => !!x),
            issueData: {
              code: ZodIssueCode.invalid_return_type,
              returnTypeError: error
            }
          });
        }
        __name(makeReturnsIssue, "makeReturnsIssue");
        const params = { errorMap: ctx.common.contextualErrorMap };
        const fn = ctx.data;
        if (this._def.returns instanceof ZodPromise) {
          const me = this;
          return OK(async function(...args) {
            const error = new ZodError([]);
            const parsedArgs = await me._def.args.parseAsync(args, params).catch((e) => {
              error.addIssue(makeArgsIssue(args, e));
              throw error;
            });
            const result = await Reflect.apply(fn, this, parsedArgs);
            const parsedReturns = await me._def.returns._def.type.parseAsync(result, params).catch((e) => {
              error.addIssue(makeReturnsIssue(result, e));
              throw error;
            });
            return parsedReturns;
          });
        } else {
          const me = this;
          return OK(function(...args) {
            const parsedArgs = me._def.args.safeParse(args, params);
            if (!parsedArgs.success) {
              throw new ZodError([makeArgsIssue(args, parsedArgs.error)]);
            }
            const result = Reflect.apply(fn, this, parsedArgs.data);
            const parsedReturns = me._def.returns.safeParse(result, params);
            if (!parsedReturns.success) {
              throw new ZodError([makeReturnsIssue(result, parsedReturns.error)]);
            }
            return parsedReturns.data;
          });
        }
      }
      parameters() {
        return this._def.args;
      }
      returnType() {
        return this._def.returns;
      }
      args(...items) {
        return new _ZodFunction({
          ...this._def,
          args: ZodTuple.create(items).rest(ZodUnknown.create())
        });
      }
      returns(returnType) {
        return new _ZodFunction({
          ...this._def,
          returns: returnType
        });
      }
      implement(func) {
        const validatedFunc = this.parse(func);
        return validatedFunc;
      }
      strictImplement(func) {
        const validatedFunc = this.parse(func);
        return validatedFunc;
      }
      static create(args, returns, params) {
        return new _ZodFunction({
          args: args ? args : ZodTuple.create([]).rest(ZodUnknown.create()),
          returns: returns || ZodUnknown.create(),
          typeName: ZodFirstPartyTypeKind.ZodFunction,
          ...processCreateParams(params)
        });
      }
    };
    ZodLazy = class extends ZodType {
      static {
        __name(this, "ZodLazy");
      }
      get schema() {
        return this._def.getter();
      }
      _parse(input) {
        const { ctx } = this._processInputParams(input);
        const lazySchema = this._def.getter();
        return lazySchema._parse({ data: ctx.data, path: ctx.path, parent: ctx });
      }
    };
    ZodLazy.create = (getter, params) => {
      return new ZodLazy({
        getter,
        typeName: ZodFirstPartyTypeKind.ZodLazy,
        ...processCreateParams(params)
      });
    };
    ZodLiteral = class extends ZodType {
      static {
        __name(this, "ZodLiteral");
      }
      _parse(input) {
        if (input.data !== this._def.value) {
          const ctx = this._getOrReturnCtx(input);
          addIssueToContext(ctx, {
            received: ctx.data,
            code: ZodIssueCode.invalid_literal,
            expected: this._def.value
          });
          return INVALID;
        }
        return { status: "valid", value: input.data };
      }
      get value() {
        return this._def.value;
      }
    };
    ZodLiteral.create = (value, params) => {
      return new ZodLiteral({
        value,
        typeName: ZodFirstPartyTypeKind.ZodLiteral,
        ...processCreateParams(params)
      });
    };
    __name(createZodEnum, "createZodEnum");
    ZodEnum = class _ZodEnum extends ZodType {
      static {
        __name(this, "ZodEnum");
      }
      constructor() {
        super(...arguments);
        _ZodEnum_cache.set(this, void 0);
      }
      _parse(input) {
        if (typeof input.data !== "string") {
          const ctx = this._getOrReturnCtx(input);
          const expectedValues = this._def.values;
          addIssueToContext(ctx, {
            expected: util.joinValues(expectedValues),
            received: ctx.parsedType,
            code: ZodIssueCode.invalid_type
          });
          return INVALID;
        }
        if (!__classPrivateFieldGet(this, _ZodEnum_cache, "f")) {
          __classPrivateFieldSet(this, _ZodEnum_cache, new Set(this._def.values), "f");
        }
        if (!__classPrivateFieldGet(this, _ZodEnum_cache, "f").has(input.data)) {
          const ctx = this._getOrReturnCtx(input);
          const expectedValues = this._def.values;
          addIssueToContext(ctx, {
            received: ctx.data,
            code: ZodIssueCode.invalid_enum_value,
            options: expectedValues
          });
          return INVALID;
        }
        return OK(input.data);
      }
      get options() {
        return this._def.values;
      }
      get enum() {
        const enumValues = {};
        for (const val of this._def.values) {
          enumValues[val] = val;
        }
        return enumValues;
      }
      get Values() {
        const enumValues = {};
        for (const val of this._def.values) {
          enumValues[val] = val;
        }
        return enumValues;
      }
      get Enum() {
        const enumValues = {};
        for (const val of this._def.values) {
          enumValues[val] = val;
        }
        return enumValues;
      }
      extract(values, newDef = this._def) {
        return _ZodEnum.create(values, {
          ...this._def,
          ...newDef
        });
      }
      exclude(values, newDef = this._def) {
        return _ZodEnum.create(this.options.filter((opt) => !values.includes(opt)), {
          ...this._def,
          ...newDef
        });
      }
    };
    _ZodEnum_cache = /* @__PURE__ */ new WeakMap();
    ZodEnum.create = createZodEnum;
    ZodNativeEnum = class extends ZodType {
      static {
        __name(this, "ZodNativeEnum");
      }
      constructor() {
        super(...arguments);
        _ZodNativeEnum_cache.set(this, void 0);
      }
      _parse(input) {
        const nativeEnumValues = util.getValidEnumValues(this._def.values);
        const ctx = this._getOrReturnCtx(input);
        if (ctx.parsedType !== ZodParsedType.string && ctx.parsedType !== ZodParsedType.number) {
          const expectedValues = util.objectValues(nativeEnumValues);
          addIssueToContext(ctx, {
            expected: util.joinValues(expectedValues),
            received: ctx.parsedType,
            code: ZodIssueCode.invalid_type
          });
          return INVALID;
        }
        if (!__classPrivateFieldGet(this, _ZodNativeEnum_cache, "f")) {
          __classPrivateFieldSet(this, _ZodNativeEnum_cache, new Set(util.getValidEnumValues(this._def.values)), "f");
        }
        if (!__classPrivateFieldGet(this, _ZodNativeEnum_cache, "f").has(input.data)) {
          const expectedValues = util.objectValues(nativeEnumValues);
          addIssueToContext(ctx, {
            received: ctx.data,
            code: ZodIssueCode.invalid_enum_value,
            options: expectedValues
          });
          return INVALID;
        }
        return OK(input.data);
      }
      get enum() {
        return this._def.values;
      }
    };
    _ZodNativeEnum_cache = /* @__PURE__ */ new WeakMap();
    ZodNativeEnum.create = (values, params) => {
      return new ZodNativeEnum({
        values,
        typeName: ZodFirstPartyTypeKind.ZodNativeEnum,
        ...processCreateParams(params)
      });
    };
    ZodPromise = class extends ZodType {
      static {
        __name(this, "ZodPromise");
      }
      unwrap() {
        return this._def.type;
      }
      _parse(input) {
        const { ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.promise && ctx.common.async === false) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.promise,
            received: ctx.parsedType
          });
          return INVALID;
        }
        const promisified = ctx.parsedType === ZodParsedType.promise ? ctx.data : Promise.resolve(ctx.data);
        return OK(promisified.then((data) => {
          return this._def.type.parseAsync(data, {
            path: ctx.path,
            errorMap: ctx.common.contextualErrorMap
          });
        }));
      }
    };
    ZodPromise.create = (schema, params) => {
      return new ZodPromise({
        type: schema,
        typeName: ZodFirstPartyTypeKind.ZodPromise,
        ...processCreateParams(params)
      });
    };
    ZodEffects = class extends ZodType {
      static {
        __name(this, "ZodEffects");
      }
      innerType() {
        return this._def.schema;
      }
      sourceType() {
        return this._def.schema._def.typeName === ZodFirstPartyTypeKind.ZodEffects ? this._def.schema.sourceType() : this._def.schema;
      }
      _parse(input) {
        const { status, ctx } = this._processInputParams(input);
        const effect = this._def.effect || null;
        const checkCtx = {
          addIssue: /* @__PURE__ */ __name((arg) => {
            addIssueToContext(ctx, arg);
            if (arg.fatal) {
              status.abort();
            } else {
              status.dirty();
            }
          }, "addIssue"),
          get path() {
            return ctx.path;
          }
        };
        checkCtx.addIssue = checkCtx.addIssue.bind(checkCtx);
        if (effect.type === "preprocess") {
          const processed = effect.transform(ctx.data, checkCtx);
          if (ctx.common.async) {
            return Promise.resolve(processed).then(async (processed2) => {
              if (status.value === "aborted")
                return INVALID;
              const result = await this._def.schema._parseAsync({
                data: processed2,
                path: ctx.path,
                parent: ctx
              });
              if (result.status === "aborted")
                return INVALID;
              if (result.status === "dirty")
                return DIRTY(result.value);
              if (status.value === "dirty")
                return DIRTY(result.value);
              return result;
            });
          } else {
            if (status.value === "aborted")
              return INVALID;
            const result = this._def.schema._parseSync({
              data: processed,
              path: ctx.path,
              parent: ctx
            });
            if (result.status === "aborted")
              return INVALID;
            if (result.status === "dirty")
              return DIRTY(result.value);
            if (status.value === "dirty")
              return DIRTY(result.value);
            return result;
          }
        }
        if (effect.type === "refinement") {
          const executeRefinement = /* @__PURE__ */ __name((acc) => {
            const result = effect.refinement(acc, checkCtx);
            if (ctx.common.async) {
              return Promise.resolve(result);
            }
            if (result instanceof Promise) {
              throw new Error("Async refinement encountered during synchronous parse operation. Use .parseAsync instead.");
            }
            return acc;
          }, "executeRefinement");
          if (ctx.common.async === false) {
            const inner = this._def.schema._parseSync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            });
            if (inner.status === "aborted")
              return INVALID;
            if (inner.status === "dirty")
              status.dirty();
            executeRefinement(inner.value);
            return { status: status.value, value: inner.value };
          } else {
            return this._def.schema._parseAsync({ data: ctx.data, path: ctx.path, parent: ctx }).then((inner) => {
              if (inner.status === "aborted")
                return INVALID;
              if (inner.status === "dirty")
                status.dirty();
              return executeRefinement(inner.value).then(() => {
                return { status: status.value, value: inner.value };
              });
            });
          }
        }
        if (effect.type === "transform") {
          if (ctx.common.async === false) {
            const base = this._def.schema._parseSync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            });
            if (!isValid2(base))
              return base;
            const result = effect.transform(base.value, checkCtx);
            if (result instanceof Promise) {
              throw new Error(`Asynchronous transform encountered during synchronous parse operation. Use .parseAsync instead.`);
            }
            return { status: status.value, value: result };
          } else {
            return this._def.schema._parseAsync({ data: ctx.data, path: ctx.path, parent: ctx }).then((base) => {
              if (!isValid2(base))
                return base;
              return Promise.resolve(effect.transform(base.value, checkCtx)).then((result) => ({ status: status.value, value: result }));
            });
          }
        }
        util.assertNever(effect);
      }
    };
    ZodEffects.create = (schema, effect, params) => {
      return new ZodEffects({
        schema,
        typeName: ZodFirstPartyTypeKind.ZodEffects,
        effect,
        ...processCreateParams(params)
      });
    };
    ZodEffects.createWithPreprocess = (preprocess, schema, params) => {
      return new ZodEffects({
        schema,
        effect: { type: "preprocess", transform: preprocess },
        typeName: ZodFirstPartyTypeKind.ZodEffects,
        ...processCreateParams(params)
      });
    };
    ZodOptional = class extends ZodType {
      static {
        __name(this, "ZodOptional");
      }
      _parse(input) {
        const parsedType = this._getType(input);
        if (parsedType === ZodParsedType.undefined) {
          return OK(void 0);
        }
        return this._def.innerType._parse(input);
      }
      unwrap() {
        return this._def.innerType;
      }
    };
    ZodOptional.create = (type, params) => {
      return new ZodOptional({
        innerType: type,
        typeName: ZodFirstPartyTypeKind.ZodOptional,
        ...processCreateParams(params)
      });
    };
    ZodNullable = class extends ZodType {
      static {
        __name(this, "ZodNullable");
      }
      _parse(input) {
        const parsedType = this._getType(input);
        if (parsedType === ZodParsedType.null) {
          return OK(null);
        }
        return this._def.innerType._parse(input);
      }
      unwrap() {
        return this._def.innerType;
      }
    };
    ZodNullable.create = (type, params) => {
      return new ZodNullable({
        innerType: type,
        typeName: ZodFirstPartyTypeKind.ZodNullable,
        ...processCreateParams(params)
      });
    };
    ZodDefault = class extends ZodType {
      static {
        __name(this, "ZodDefault");
      }
      _parse(input) {
        const { ctx } = this._processInputParams(input);
        let data = ctx.data;
        if (ctx.parsedType === ZodParsedType.undefined) {
          data = this._def.defaultValue();
        }
        return this._def.innerType._parse({
          data,
          path: ctx.path,
          parent: ctx
        });
      }
      removeDefault() {
        return this._def.innerType;
      }
    };
    ZodDefault.create = (type, params) => {
      return new ZodDefault({
        innerType: type,
        typeName: ZodFirstPartyTypeKind.ZodDefault,
        defaultValue: typeof params.default === "function" ? params.default : () => params.default,
        ...processCreateParams(params)
      });
    };
    ZodCatch = class extends ZodType {
      static {
        __name(this, "ZodCatch");
      }
      _parse(input) {
        const { ctx } = this._processInputParams(input);
        const newCtx = {
          ...ctx,
          common: {
            ...ctx.common,
            issues: []
          }
        };
        const result = this._def.innerType._parse({
          data: newCtx.data,
          path: newCtx.path,
          parent: {
            ...newCtx
          }
        });
        if (isAsync(result)) {
          return result.then((result2) => {
            return {
              status: "valid",
              value: result2.status === "valid" ? result2.value : this._def.catchValue({
                get error() {
                  return new ZodError(newCtx.common.issues);
                },
                input: newCtx.data
              })
            };
          });
        } else {
          return {
            status: "valid",
            value: result.status === "valid" ? result.value : this._def.catchValue({
              get error() {
                return new ZodError(newCtx.common.issues);
              },
              input: newCtx.data
            })
          };
        }
      }
      removeCatch() {
        return this._def.innerType;
      }
    };
    ZodCatch.create = (type, params) => {
      return new ZodCatch({
        innerType: type,
        typeName: ZodFirstPartyTypeKind.ZodCatch,
        catchValue: typeof params.catch === "function" ? params.catch : () => params.catch,
        ...processCreateParams(params)
      });
    };
    ZodNaN = class extends ZodType {
      static {
        __name(this, "ZodNaN");
      }
      _parse(input) {
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.nan) {
          const ctx = this._getOrReturnCtx(input);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.nan,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return { status: "valid", value: input.data };
      }
    };
    ZodNaN.create = (params) => {
      return new ZodNaN({
        typeName: ZodFirstPartyTypeKind.ZodNaN,
        ...processCreateParams(params)
      });
    };
    BRAND = Symbol("zod_brand");
    ZodBranded = class extends ZodType {
      static {
        __name(this, "ZodBranded");
      }
      _parse(input) {
        const { ctx } = this._processInputParams(input);
        const data = ctx.data;
        return this._def.type._parse({
          data,
          path: ctx.path,
          parent: ctx
        });
      }
      unwrap() {
        return this._def.type;
      }
    };
    ZodPipeline = class _ZodPipeline extends ZodType {
      static {
        __name(this, "ZodPipeline");
      }
      _parse(input) {
        const { status, ctx } = this._processInputParams(input);
        if (ctx.common.async) {
          const handleAsync = /* @__PURE__ */ __name(async () => {
            const inResult = await this._def.in._parseAsync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            });
            if (inResult.status === "aborted")
              return INVALID;
            if (inResult.status === "dirty") {
              status.dirty();
              return DIRTY(inResult.value);
            } else {
              return this._def.out._parseAsync({
                data: inResult.value,
                path: ctx.path,
                parent: ctx
              });
            }
          }, "handleAsync");
          return handleAsync();
        } else {
          const inResult = this._def.in._parseSync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          });
          if (inResult.status === "aborted")
            return INVALID;
          if (inResult.status === "dirty") {
            status.dirty();
            return {
              status: "dirty",
              value: inResult.value
            };
          } else {
            return this._def.out._parseSync({
              data: inResult.value,
              path: ctx.path,
              parent: ctx
            });
          }
        }
      }
      static create(a, b) {
        return new _ZodPipeline({
          in: a,
          out: b,
          typeName: ZodFirstPartyTypeKind.ZodPipeline
        });
      }
    };
    ZodReadonly = class extends ZodType {
      static {
        __name(this, "ZodReadonly");
      }
      _parse(input) {
        const result = this._def.innerType._parse(input);
        const freeze = /* @__PURE__ */ __name((data) => {
          if (isValid2(data)) {
            data.value = Object.freeze(data.value);
          }
          return data;
        }, "freeze");
        return isAsync(result) ? result.then((data) => freeze(data)) : freeze(result);
      }
      unwrap() {
        return this._def.innerType;
      }
    };
    ZodReadonly.create = (type, params) => {
      return new ZodReadonly({
        innerType: type,
        typeName: ZodFirstPartyTypeKind.ZodReadonly,
        ...processCreateParams(params)
      });
    };
    __name(cleanParams, "cleanParams");
    __name(custom, "custom");
    late = {
      object: ZodObject.lazycreate
    };
    (function(ZodFirstPartyTypeKind2) {
      ZodFirstPartyTypeKind2["ZodString"] = "ZodString";
      ZodFirstPartyTypeKind2["ZodNumber"] = "ZodNumber";
      ZodFirstPartyTypeKind2["ZodNaN"] = "ZodNaN";
      ZodFirstPartyTypeKind2["ZodBigInt"] = "ZodBigInt";
      ZodFirstPartyTypeKind2["ZodBoolean"] = "ZodBoolean";
      ZodFirstPartyTypeKind2["ZodDate"] = "ZodDate";
      ZodFirstPartyTypeKind2["ZodSymbol"] = "ZodSymbol";
      ZodFirstPartyTypeKind2["ZodUndefined"] = "ZodUndefined";
      ZodFirstPartyTypeKind2["ZodNull"] = "ZodNull";
      ZodFirstPartyTypeKind2["ZodAny"] = "ZodAny";
      ZodFirstPartyTypeKind2["ZodUnknown"] = "ZodUnknown";
      ZodFirstPartyTypeKind2["ZodNever"] = "ZodNever";
      ZodFirstPartyTypeKind2["ZodVoid"] = "ZodVoid";
      ZodFirstPartyTypeKind2["ZodArray"] = "ZodArray";
      ZodFirstPartyTypeKind2["ZodObject"] = "ZodObject";
      ZodFirstPartyTypeKind2["ZodUnion"] = "ZodUnion";
      ZodFirstPartyTypeKind2["ZodDiscriminatedUnion"] = "ZodDiscriminatedUnion";
      ZodFirstPartyTypeKind2["ZodIntersection"] = "ZodIntersection";
      ZodFirstPartyTypeKind2["ZodTuple"] = "ZodTuple";
      ZodFirstPartyTypeKind2["ZodRecord"] = "ZodRecord";
      ZodFirstPartyTypeKind2["ZodMap"] = "ZodMap";
      ZodFirstPartyTypeKind2["ZodSet"] = "ZodSet";
      ZodFirstPartyTypeKind2["ZodFunction"] = "ZodFunction";
      ZodFirstPartyTypeKind2["ZodLazy"] = "ZodLazy";
      ZodFirstPartyTypeKind2["ZodLiteral"] = "ZodLiteral";
      ZodFirstPartyTypeKind2["ZodEnum"] = "ZodEnum";
      ZodFirstPartyTypeKind2["ZodEffects"] = "ZodEffects";
      ZodFirstPartyTypeKind2["ZodNativeEnum"] = "ZodNativeEnum";
      ZodFirstPartyTypeKind2["ZodOptional"] = "ZodOptional";
      ZodFirstPartyTypeKind2["ZodNullable"] = "ZodNullable";
      ZodFirstPartyTypeKind2["ZodDefault"] = "ZodDefault";
      ZodFirstPartyTypeKind2["ZodCatch"] = "ZodCatch";
      ZodFirstPartyTypeKind2["ZodPromise"] = "ZodPromise";
      ZodFirstPartyTypeKind2["ZodBranded"] = "ZodBranded";
      ZodFirstPartyTypeKind2["ZodPipeline"] = "ZodPipeline";
      ZodFirstPartyTypeKind2["ZodReadonly"] = "ZodReadonly";
    })(ZodFirstPartyTypeKind || (ZodFirstPartyTypeKind = {}));
    instanceOfType = /* @__PURE__ */ __name((cls, params = {
      message: `Input not instance of ${cls.name}`
    }) => custom((data) => data instanceof cls, params), "instanceOfType");
    stringType = ZodString.create;
    numberType = ZodNumber.create;
    nanType = ZodNaN.create;
    bigIntType = ZodBigInt.create;
    booleanType = ZodBoolean.create;
    dateType = ZodDate.create;
    symbolType = ZodSymbol.create;
    undefinedType = ZodUndefined.create;
    nullType = ZodNull.create;
    anyType = ZodAny.create;
    unknownType = ZodUnknown.create;
    neverType = ZodNever.create;
    voidType = ZodVoid.create;
    arrayType = ZodArray.create;
    objectType = ZodObject.create;
    strictObjectType = ZodObject.strictCreate;
    unionType = ZodUnion.create;
    discriminatedUnionType = ZodDiscriminatedUnion.create;
    intersectionType = ZodIntersection.create;
    tupleType = ZodTuple.create;
    recordType = ZodRecord.create;
    mapType = ZodMap.create;
    setType = ZodSet.create;
    functionType = ZodFunction.create;
    lazyType = ZodLazy.create;
    literalType = ZodLiteral.create;
    enumType = ZodEnum.create;
    nativeEnumType = ZodNativeEnum.create;
    promiseType = ZodPromise.create;
    effectsType = ZodEffects.create;
    optionalType = ZodOptional.create;
    nullableType = ZodNullable.create;
    preprocessType = ZodEffects.createWithPreprocess;
    pipelineType = ZodPipeline.create;
    ostring = /* @__PURE__ */ __name(() => stringType().optional(), "ostring");
    onumber = /* @__PURE__ */ __name(() => numberType().optional(), "onumber");
    oboolean = /* @__PURE__ */ __name(() => booleanType().optional(), "oboolean");
    coerce = {
      string: /* @__PURE__ */ __name(((arg) => ZodString.create({ ...arg, coerce: true })), "string"),
      number: /* @__PURE__ */ __name(((arg) => ZodNumber.create({ ...arg, coerce: true })), "number"),
      boolean: /* @__PURE__ */ __name(((arg) => ZodBoolean.create({
        ...arg,
        coerce: true
      })), "boolean"),
      bigint: /* @__PURE__ */ __name(((arg) => ZodBigInt.create({ ...arg, coerce: true })), "bigint"),
      date: /* @__PURE__ */ __name(((arg) => ZodDate.create({ ...arg, coerce: true })), "date")
    };
    NEVER = INVALID;
    z = /* @__PURE__ */ Object.freeze({
      __proto__: null,
      defaultErrorMap: errorMap,
      setErrorMap,
      getErrorMap,
      makeIssue,
      EMPTY_PATH,
      addIssueToContext,
      ParseStatus,
      INVALID,
      DIRTY,
      OK,
      isAborted,
      isDirty,
      isValid: isValid2,
      isAsync,
      get util() {
        return util;
      },
      get objectUtil() {
        return objectUtil;
      },
      ZodParsedType,
      getParsedType,
      ZodType,
      datetimeRegex,
      ZodString,
      ZodNumber,
      ZodBigInt,
      ZodBoolean,
      ZodDate,
      ZodSymbol,
      ZodUndefined,
      ZodNull,
      ZodAny,
      ZodUnknown,
      ZodNever,
      ZodVoid,
      ZodArray,
      ZodObject,
      ZodUnion,
      ZodDiscriminatedUnion,
      ZodIntersection,
      ZodTuple,
      ZodRecord,
      ZodMap,
      ZodSet,
      ZodFunction,
      ZodLazy,
      ZodLiteral,
      ZodEnum,
      ZodNativeEnum,
      ZodPromise,
      ZodEffects,
      ZodTransformer: ZodEffects,
      ZodOptional,
      ZodNullable,
      ZodDefault,
      ZodCatch,
      ZodNaN,
      BRAND,
      ZodBranded,
      ZodPipeline,
      ZodReadonly,
      custom,
      Schema: ZodType,
      ZodSchema: ZodType,
      late,
      get ZodFirstPartyTypeKind() {
        return ZodFirstPartyTypeKind;
      },
      coerce,
      any: anyType,
      array: arrayType,
      bigint: bigIntType,
      boolean: booleanType,
      date: dateType,
      discriminatedUnion: discriminatedUnionType,
      effect: effectsType,
      "enum": enumType,
      "function": functionType,
      "instanceof": instanceOfType,
      intersection: intersectionType,
      lazy: lazyType,
      literal: literalType,
      map: mapType,
      nan: nanType,
      nativeEnum: nativeEnumType,
      never: neverType,
      "null": nullType,
      nullable: nullableType,
      number: numberType,
      object: objectType,
      oboolean,
      onumber,
      optional: optionalType,
      ostring,
      pipeline: pipelineType,
      preprocess: preprocessType,
      promise: promiseType,
      record: recordType,
      set: setType,
      strictObject: strictObjectType,
      string: stringType,
      symbol: symbolType,
      transformer: effectsType,
      tuple: tupleType,
      "undefined": undefinedType,
      union: unionType,
      unknown: unknownType,
      "void": voidType,
      NEVER,
      ZodIssueCode,
      quotelessJson,
      ZodError
    });
  }
});

// node_modules/drizzle-zod/index.mjs
function isColumnType(column, columnTypes) {
  return columnTypes.includes(column.columnType);
}
function isWithEnum(column) {
  return "enumValues" in column && Array.isArray(column.enumValues) && column.enumValues.length > 0;
}
function columnToSchema(column, factory) {
  const z$1 = factory?.zodInstance ?? z;
  const coerce2 = factory?.coerce ?? {};
  let schema;
  if (isWithEnum(column)) {
    schema = column.enumValues.length ? z$1.enum(column.enumValues) : z$1.string();
  }
  if (!schema) {
    if (isColumnType(column, ["PgGeometry", "PgPointTuple"])) {
      schema = z$1.tuple([z$1.number(), z$1.number()]);
    } else if (isColumnType(column, ["PgGeometryObject", "PgPointObject"])) {
      schema = z$1.object({ x: z$1.number(), y: z$1.number() });
    } else if (isColumnType(column, ["PgHalfVector", "PgVector"])) {
      schema = z$1.array(z$1.number());
      schema = column.dimensions ? schema.length(column.dimensions) : schema;
    } else if (isColumnType(column, ["PgLine"])) {
      schema = z$1.tuple([z$1.number(), z$1.number(), z$1.number()]);
    } else if (isColumnType(column, ["PgLineABC"])) {
      schema = z$1.object({
        a: z$1.number(),
        b: z$1.number(),
        c: z$1.number()
      });
    } else if (isColumnType(column, ["PgArray"])) {
      schema = z$1.array(columnToSchema(column.baseColumn, z$1));
      schema = column.size ? schema.length(column.size) : schema;
    } else if (column.dataType === "array") {
      schema = z$1.array(z$1.any());
    } else if (column.dataType === "number") {
      schema = numberColumnToSchema(column, z$1, coerce2);
    } else if (column.dataType === "bigint") {
      schema = bigintColumnToSchema(column, z$1, coerce2);
    } else if (column.dataType === "boolean") {
      schema = coerce2 === true || coerce2.boolean ? z$1.coerce.boolean() : z$1.boolean();
    } else if (column.dataType === "date") {
      schema = coerce2 === true || coerce2.date ? z$1.coerce.date() : z$1.date();
    } else if (column.dataType === "string") {
      schema = stringColumnToSchema(column, z$1, coerce2);
    } else if (column.dataType === "json") {
      schema = jsonSchema;
    } else if (column.dataType === "custom") {
      schema = z$1.any();
    } else if (column.dataType === "buffer") {
      schema = bufferSchema;
    }
  }
  if (!schema) {
    schema = z$1.any();
  }
  return schema;
}
function numberColumnToSchema(column, z2, coerce2) {
  let unsigned = column.getSQLType().includes("unsigned");
  let min;
  let max;
  let integer3 = false;
  if (isColumnType(column, ["MySqlTinyInt", "SingleStoreTinyInt"])) {
    min = unsigned ? 0 : CONSTANTS.INT8_MIN;
    max = unsigned ? CONSTANTS.INT8_UNSIGNED_MAX : CONSTANTS.INT8_MAX;
    integer3 = true;
  } else if (isColumnType(column, [
    "PgSmallInt",
    "PgSmallSerial",
    "MySqlSmallInt",
    "SingleStoreSmallInt"
  ])) {
    min = unsigned ? 0 : CONSTANTS.INT16_MIN;
    max = unsigned ? CONSTANTS.INT16_UNSIGNED_MAX : CONSTANTS.INT16_MAX;
    integer3 = true;
  } else if (isColumnType(column, [
    "PgReal",
    "MySqlFloat",
    "MySqlMediumInt",
    "SingleStoreMediumInt",
    "SingleStoreFloat"
  ])) {
    min = unsigned ? 0 : CONSTANTS.INT24_MIN;
    max = unsigned ? CONSTANTS.INT24_UNSIGNED_MAX : CONSTANTS.INT24_MAX;
    integer3 = isColumnType(column, ["MySqlMediumInt", "SingleStoreMediumInt"]);
  } else if (isColumnType(column, [
    "PgInteger",
    "PgSerial",
    "MySqlInt",
    "SingleStoreInt"
  ])) {
    min = unsigned ? 0 : CONSTANTS.INT32_MIN;
    max = unsigned ? CONSTANTS.INT32_UNSIGNED_MAX : CONSTANTS.INT32_MAX;
    integer3 = true;
  } else if (isColumnType(column, [
    "PgDoublePrecision",
    "MySqlReal",
    "MySqlDouble",
    "SingleStoreReal",
    "SingleStoreDouble",
    "SQLiteReal"
  ])) {
    min = unsigned ? 0 : CONSTANTS.INT48_MIN;
    max = unsigned ? CONSTANTS.INT48_UNSIGNED_MAX : CONSTANTS.INT48_MAX;
  } else if (isColumnType(column, [
    "PgBigInt53",
    "PgBigSerial53",
    "MySqlBigInt53",
    "MySqlSerial",
    "SingleStoreBigInt53",
    "SingleStoreSerial",
    "SQLiteInteger"
  ])) {
    unsigned = unsigned || isColumnType(column, ["MySqlSerial", "SingleStoreSerial"]);
    min = unsigned ? 0 : Number.MIN_SAFE_INTEGER;
    max = Number.MAX_SAFE_INTEGER;
    integer3 = true;
  } else if (isColumnType(column, ["MySqlYear", "SingleStoreYear"])) {
    min = 1901;
    max = 2155;
    integer3 = true;
  } else {
    min = Number.MIN_SAFE_INTEGER;
    max = Number.MAX_SAFE_INTEGER;
  }
  let schema = coerce2 === true || coerce2?.number ? z2.coerce.number() : z2.number();
  schema = schema.min(min).max(max);
  return integer3 ? schema.int() : schema;
}
function bigintColumnToSchema(column, z2, coerce2) {
  const unsigned = column.getSQLType().includes("unsigned");
  const min = unsigned ? 0n : CONSTANTS.INT64_MIN;
  const max = unsigned ? CONSTANTS.INT64_UNSIGNED_MAX : CONSTANTS.INT64_MAX;
  const schema = coerce2 === true || coerce2?.bigint ? z2.coerce.bigint() : z2.bigint();
  return schema.min(min).max(max);
}
function stringColumnToSchema(column, z2, coerce2) {
  if (isColumnType(column, ["PgUUID"])) {
    return z2.string().uuid();
  }
  let max;
  let regex;
  let fixed = false;
  if (isColumnType(column, ["PgVarchar", "SQLiteText"])) {
    max = column.length;
  } else if (isColumnType(column, ["MySqlVarChar", "SingleStoreVarChar"])) {
    max = column.length ?? CONSTANTS.INT16_UNSIGNED_MAX;
  } else if (isColumnType(column, ["MySqlText", "SingleStoreText"])) {
    if (column.textType === "longtext") {
      max = CONSTANTS.INT32_UNSIGNED_MAX;
    } else if (column.textType === "mediumtext") {
      max = CONSTANTS.INT24_UNSIGNED_MAX;
    } else if (column.textType === "text") {
      max = CONSTANTS.INT16_UNSIGNED_MAX;
    } else {
      max = CONSTANTS.INT8_UNSIGNED_MAX;
    }
  }
  if (isColumnType(column, [
    "PgChar",
    "MySqlChar",
    "SingleStoreChar"
  ])) {
    max = column.length;
    fixed = true;
  }
  if (isColumnType(column, ["PgBinaryVector"])) {
    regex = /^[01]+$/;
    max = column.dimensions;
  }
  let schema = coerce2 === true || coerce2?.string ? z2.coerce.string() : z2.string();
  schema = regex ? schema.regex(regex) : schema;
  return max && fixed ? schema.length(max) : max ? schema.max(max) : schema;
}
function getColumns(tableLike) {
  return isTable(tableLike) ? getTableColumns2(tableLike) : getViewSelectedFields(tableLike);
}
function handleColumns(columns, refinements, conditions, factory) {
  const columnSchemas = {};
  for (const [key, selected] of Object.entries(columns)) {
    if (!is2(selected, Column2) && !is2(selected, SQL2) && !is2(selected, SQL2.Aliased) && typeof selected === "object") {
      const columns2 = isTable(selected) || isView(selected) ? getColumns(selected) : selected;
      columnSchemas[key] = handleColumns(columns2, refinements[key] ?? {}, conditions, factory);
      continue;
    }
    const refinement = refinements[key];
    if (refinement !== void 0 && typeof refinement !== "function") {
      columnSchemas[key] = refinement;
      continue;
    }
    const column = is2(selected, Column2) ? selected : void 0;
    const schema = column ? columnToSchema(column, factory) : z.any();
    const refined = typeof refinement === "function" ? refinement(schema) : schema;
    if (conditions.never(column)) {
      continue;
    } else {
      columnSchemas[key] = refined;
    }
    if (column) {
      if (conditions.nullable(column)) {
        columnSchemas[key] = columnSchemas[key].nullable();
      }
      if (conditions.optional(column)) {
        columnSchemas[key] = columnSchemas[key].optional();
      }
    }
  }
  return z.object(columnSchemas);
}
var CONSTANTS, literalSchema, jsonSchema, bufferSchema, insertConditions, createInsertSchema;
var init_drizzle_zod = __esm({
  "node_modules/drizzle-zod/index.mjs"() {
    init_drizzle_orm();
    init_lib();
    CONSTANTS = {
      INT8_MIN: -128,
      INT8_MAX: 127,
      INT8_UNSIGNED_MAX: 255,
      INT16_MIN: -32768,
      INT16_MAX: 32767,
      INT16_UNSIGNED_MAX: 65535,
      INT24_MIN: -8388608,
      INT24_MAX: 8388607,
      INT24_UNSIGNED_MAX: 16777215,
      INT32_MIN: -2147483648,
      INT32_MAX: 2147483647,
      INT32_UNSIGNED_MAX: 4294967295,
      INT48_MIN: -140737488355328,
      INT48_MAX: 140737488355327,
      INT48_UNSIGNED_MAX: 281474976710655,
      INT64_MIN: -9223372036854775808n,
      INT64_MAX: 9223372036854775807n,
      INT64_UNSIGNED_MAX: 18446744073709551615n
    };
    __name(isColumnType, "isColumnType");
    __name(isWithEnum, "isWithEnum");
    literalSchema = z.union([z.string(), z.number(), z.boolean(), z.null()]);
    jsonSchema = z.union([literalSchema, z.record(z.any()), z.array(z.any())]);
    bufferSchema = z.custom((v) => v instanceof Buffer);
    __name(columnToSchema, "columnToSchema");
    __name(numberColumnToSchema, "numberColumnToSchema");
    __name(bigintColumnToSchema, "bigintColumnToSchema");
    __name(stringColumnToSchema, "stringColumnToSchema");
    __name(getColumns, "getColumns");
    __name(handleColumns, "handleColumns");
    insertConditions = {
      never: /* @__PURE__ */ __name((column) => column?.generated?.type === "always" || column?.generatedIdentity?.type === "always", "never"),
      optional: /* @__PURE__ */ __name((column) => !column.notNull || column.notNull && column.hasDefault, "optional"),
      nullable: /* @__PURE__ */ __name((column) => !column.notNull, "nullable")
    };
    createInsertSchema = /* @__PURE__ */ __name((entity, refine) => {
      const columns = getColumns(entity);
      return handleColumns(columns, refine ?? {}, insertConditions);
    }, "createInsertSchema");
  }
});

// shared/schema.ts
var schema_exports = {};
__export(schema_exports, {
  COMPETITION_END: () => COMPETITION_END,
  COMPETITION_NAME: () => COMPETITION_NAME,
  COMPETITION_SLUG: () => COMPETITION_SLUG,
  COMPETITION_START: () => COMPETITION_START,
  RARITY_CONFIG: () => RARITY_CONFIG,
  TREASURE_DEFINITIONS: () => TREASURE_DEFINITIONS,
  ZARAGOZA_BOUNDS: () => ZARAGOZA_BOUNDS,
  competitionStats: () => competitionStats,
  competitions: () => competitions,
  conquestMetrics: () => conquestMetrics,
  conquestMetricsRelations: () => conquestMetricsRelations,
  corosAccounts: () => corosAccounts,
  corosActivities: () => corosActivities,
  emailNotifications: () => emailNotifications,
  emailPreferences: () => emailPreferences,
  ephemeralPhotos: () => ephemeralPhotos,
  feedComments: () => feedComments,
  feedCommentsRelations: () => feedCommentsRelations,
  feedEvents: () => feedEvents,
  feedEventsRelations: () => feedEventsRelations,
  feedReactions: () => feedReactions,
  friendInvites: () => friendInvites,
  friendInvitesRelations: () => friendInvitesRelations,
  friendRequests: () => friendRequests,
  friendships: () => friendships,
  friendshipsRelations: () => friendshipsRelations,
  inactivityReminders: () => inactivityReminders,
  inactivityRemindersRelations: () => inactivityRemindersRelations,
  insertCompetitionSchema: () => insertCompetitionSchema,
  insertCompetitionStatsSchema: () => insertCompetitionStatsSchema,
  insertConquestMetricSchema: () => insertConquestMetricSchema,
  insertCorosAccountSchema: () => insertCorosAccountSchema,
  insertCorosActivitySchema: () => insertCorosActivitySchema,
  insertEmailNotificationSchema: () => insertEmailNotificationSchema,
  insertEmailPreferencesSchema: () => insertEmailPreferencesSchema,
  insertEphemeralPhotoSchema: () => insertEphemeralPhotoSchema,
  insertFeedCommentSchema: () => insertFeedCommentSchema,
  insertFeedEventSchema: () => insertFeedEventSchema,
  insertFeedReactionSchema: () => insertFeedReactionSchema,
  insertFriendInviteSchema: () => insertFriendInviteSchema,
  insertFriendRequestSchema: () => insertFriendRequestSchema,
  insertFriendshipSchema: () => insertFriendshipSchema,
  insertInactivityReminderSchema: () => insertInactivityReminderSchema,
  insertPolarAccountSchema: () => insertPolarAccountSchema,
  insertPolarActivitySchema: () => insertPolarActivitySchema,
  insertPushSubscriptionSchema: () => insertPushSubscriptionSchema,
  insertRouteSchema: () => insertRouteSchema,
  insertStravaAccountSchema: () => insertStravaAccountSchema,
  insertStravaActivitySchema: () => insertStravaActivitySchema,
  insertTerritoryFortificationSchema: () => insertTerritoryFortificationSchema,
  insertTerritoryLossNotificationSchema: () => insertTerritoryLossNotificationSchema,
  insertTerritorySchema: () => insertTerritorySchema,
  insertTreasureSchema: () => insertTreasureSchema,
  insertUserNicknameSchema: () => insertUserNicknameSchema,
  insertUserPowerSchema: () => insertUserPowerSchema,
  insertUserSchema: () => insertUserSchema,
  insertWeeklySummarySchema: () => insertWeeklySummarySchema,
  polarAccounts: () => polarAccounts,
  polarAccountsRelations: () => polarAccountsRelations,
  polarActivities: () => polarActivities,
  polarActivitiesRelations: () => polarActivitiesRelations,
  pushSubscriptions: () => pushSubscriptions,
  routes: () => routes,
  routesRelations: () => routesRelations,
  stravaAccounts: () => stravaAccounts,
  stravaAccountsRelations: () => stravaAccountsRelations,
  stravaActivities: () => stravaActivities,
  stravaActivitiesRelations: () => stravaActivitiesRelations,
  territories: () => territories,
  territoriesRelations: () => territoriesRelations,
  territoryFortifications: () => territoryFortifications,
  territoryLossNotifications: () => territoryLossNotifications,
  territoryLossNotificationsRelations: () => territoryLossNotificationsRelations,
  treasures: () => treasures,
  userNicknames: () => userNicknames,
  userPowers: () => userPowers,
  users: () => users,
  usersRelations: () => usersRelations,
  weeklySummaries: () => weeklySummaries
});
var users, routes, territories, conquestMetrics, friendships, friendInvites, friendRequests, pushSubscriptions, polarAccounts, polarActivities, stravaAccounts, stravaActivities, corosAccounts, corosActivities, ephemeralPhotos, emailNotifications, emailPreferences, inactivityReminders, territoryLossNotifications, feedEvents, feedComments, feedReactions, competitions, treasures, userPowers, competitionStats, weeklySummaries, userNicknames, territoryFortifications, usersRelations, polarAccountsRelations, polarActivitiesRelations, routesRelations, territoriesRelations, conquestMetricsRelations, friendshipsRelations, friendInvitesRelations, stravaAccountsRelations, stravaActivitiesRelations, inactivityRemindersRelations, territoryLossNotificationsRelations, feedEventsRelations, feedCommentsRelations, insertUserSchema, insertRouteSchema, insertTerritorySchema, insertFriendshipSchema, insertFriendInviteSchema, insertFriendRequestSchema, insertPolarAccountSchema, insertPolarActivitySchema, insertStravaAccountSchema, insertStravaActivitySchema, insertCorosAccountSchema, insertCorosActivitySchema, insertPushSubscriptionSchema, insertFeedEventSchema, insertFeedCommentSchema, insertFeedReactionSchema, insertConquestMetricSchema, insertEphemeralPhotoSchema, insertInactivityReminderSchema, insertTerritoryLossNotificationSchema, insertCompetitionSchema, insertTreasureSchema, insertUserPowerSchema, insertCompetitionStatsSchema, insertWeeklySummarySchema, insertUserNicknameSchema, insertTerritoryFortificationSchema, insertEmailNotificationSchema, insertEmailPreferencesSchema, TREASURE_DEFINITIONS, RARITY_CONFIG, ZARAGOZA_BOUNDS, COMPETITION_START, COMPETITION_END, COMPETITION_NAME, COMPETITION_SLUG;
var init_schema = __esm({
  "shared/schema.ts"() {
    "use strict";
    init_drizzle_orm();
    init_sqlite_core2();
    init_drizzle_zod();
    users = sqliteTable2("users", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      username: text2("username").notNull().unique(),
      email: text2("email").notNull().unique(),
      name: text2("name").notNull(),
      password: text2("password").notNull().default(""),
      // Hashed password (empty for legacy users)
      color: text2("color").notNull(),
      // Hex color for territory visualization
      avatar: text2("avatar"),
      // Avatar URL or placeholder
      totalArea: real2("total_area").notNull().default(0),
      // Total m² conquered
      emailVerified: integer2("email_verified", { mode: "boolean" }).notNull().default(false),
      verificationCode: text2("verification_code"),
      // 6-digit code
      verificationCodeExpiresAt: text2("verification_code_expires_at"),
      // ISO timestamp
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    routes = sqliteTable2("routes", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      name: text2("name").notNull(),
      coordinates: text2("coordinates").notNull(),
      // JSON string of Array<[number, number]>
      distance: real2("distance").notNull(),
      // meters
      duration: integer2("duration").notNull(),
      // seconds
      startedAt: text2("started_at").notNull(),
      completedAt: text2("completed_at").notNull(),
      ranTogetherWith: text2("ran_together_with")
      // JSON array of user IDs who ran together
    });
    territories = sqliteTable2("territories", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      routeId: text2("route_id").references(() => routes.id, { onDelete: "set null" }),
      geometry: text2("geometry").notNull(),
      // JSON string of GeoJSON polygon
      area: real2("area").notNull(),
      // square meters
      conqueredAt: text2("conquered_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    conquestMetrics = sqliteTable2("conquest_metrics", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      attackerId: text2("attacker_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      defenderId: text2("defender_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      areaStolen: real2("area_stolen").notNull(),
      // square meters
      routeId: text2("route_id").references(() => routes.id, { onDelete: "set null" }),
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    friendships = sqliteTable2("friendships", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      friendId: text2("friend_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    friendInvites = sqliteTable2("friend_invites", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      token: text2("token").notNull().unique(),
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`),
      expiresAt: text2("expires_at").notNull()
    });
    friendRequests = sqliteTable2("friend_requests", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      senderId: text2("sender_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      recipientId: text2("recipient_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      status: text2("status").notNull().default("pending"),
      // pending, accepted, rejected
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    pushSubscriptions = sqliteTable2("push_subscriptions", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      endpoint: text2("endpoint").notNull().unique(),
      p256dh: text2("p256dh").notNull(),
      auth: text2("auth").notNull(),
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    polarAccounts = sqliteTable2("polar_accounts", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }).unique(),
      polarUserId: integer2("polar_user_id").notNull().unique(),
      accessToken: text2("access_token").notNull(),
      memberId: text2("member_id"),
      registeredAt: text2("registered_at").default(sql2`CURRENT_TIMESTAMP`),
      lastSyncAt: text2("last_sync_at"),
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    polarActivities = sqliteTable2("polar_activities", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      polarExerciseId: text2("polar_exercise_id").notNull().unique(),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      routeId: text2("route_id").references(() => routes.id, { onDelete: "set null" }),
      territoryId: text2("territory_id").references(() => territories.id, { onDelete: "set null" }),
      name: text2("name").notNull(),
      activityType: text2("activity_type").notNull(),
      distance: real2("distance").notNull(),
      duration: integer2("duration").notNull(),
      startDate: text2("start_date").notNull(),
      summaryPolyline: text2("summary_polyline"),
      processed: integer2("processed").notNull().default(0),
      processedAt: text2("processed_at"),
      skipReason: text2("skip_reason"),
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    stravaAccounts = sqliteTable2("strava_accounts", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }).unique(),
      stravaAthleteId: integer2("strava_athlete_id").notNull().unique(),
      accessToken: text2("access_token").notNull(),
      refreshToken: text2("refresh_token").notNull(),
      expiresAt: text2("expires_at").notNull(),
      scope: text2("scope").notNull(),
      athleteData: text2("athlete_data"),
      // JSON string of athlete profile
      lastSyncAt: text2("last_sync_at"),
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    stravaActivities = sqliteTable2("strava_activities", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      stravaActivityId: integer2("strava_activity_id").notNull().unique(),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      routeId: text2("route_id").references(() => routes.id, { onDelete: "set null" }),
      territoryId: text2("territory_id").references(() => territories.id, { onDelete: "set null" }),
      name: text2("name").notNull(),
      activityType: text2("activity_type").notNull(),
      // Run, Walk, etc.
      distance: real2("distance").notNull(),
      // meters
      duration: integer2("duration").notNull(),
      // seconds (moving_time)
      startDate: text2("start_date").notNull(),
      summaryPolyline: text2("summary_polyline"),
      // Encoded polyline from Strava
      processed: integer2("processed").notNull().default(0),
      processedAt: text2("processed_at"),
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    corosAccounts = sqliteTable2("coros_accounts", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }).unique(),
      corosOpenId: text2("coros_open_id").notNull().unique(),
      accessToken: text2("access_token").notNull(),
      refreshToken: text2("refresh_token"),
      expiresAt: text2("expires_at"),
      lastSyncAt: text2("last_sync_at"),
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    corosActivities = sqliteTable2("coros_activities", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      corosWorkoutId: text2("coros_workout_id").notNull().unique(),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      routeId: text2("route_id").references(() => routes.id, { onDelete: "set null" }),
      territoryId: text2("territory_id").references(() => territories.id, { onDelete: "set null" }),
      name: text2("name").notNull(),
      activityType: text2("activity_type").notNull(),
      // Run, Trail Run, etc.
      distance: real2("distance").notNull(),
      // meters
      duration: integer2("duration").notNull(),
      // seconds
      startDate: text2("start_date").notNull(),
      summaryPolyline: text2("summary_polyline"),
      // GPS track
      processed: integer2("processed").notNull().default(0),
      processedAt: text2("processed_at"),
      skipReason: text2("skip_reason"),
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    ephemeralPhotos = sqliteTable2("ephemeral_photos", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      senderId: text2("sender_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      recipientId: text2("recipient_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      photoData: text2("photo_data").notNull(),
      // base64 compressed JPEG
      message: text2("message"),
      // optional short message
      areaStolen: real2("area_stolen"),
      // context: how much was stolen
      viewed: integer2("viewed", { mode: "boolean" }).notNull().default(false),
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`),
      expiresAt: text2("expires_at").notNull()
      // auto-delete after 24h
    });
    emailNotifications = sqliteTable2("email_notifications", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      notificationType: text2("notification_type").notNull(),
      // 'friend_request', 'friend_accepted', 'territory_conquered'
      relatedUserId: text2("related_user_id").references(() => users.id, { onDelete: "set null" }),
      subject: text2("subject").notNull(),
      body: text2("body").notNull(),
      areaStolen: real2("area_stolen"),
      // for territory_conquered type
      emailSentAt: text2("email_sent_at").notNull().default(sql2`CURRENT_TIMESTAMP`),
      openedAt: text2("opened_at"),
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    emailPreferences = sqliteTable2("email_preferences", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      userId: text2("user_id").notNull().unique().references(() => users.id, { onDelete: "cascade" }),
      friendRequestNotifications: integer2("friend_request_notifications").notNull().default(1),
      friendAcceptedNotifications: integer2("friend_accepted_notifications").notNull().default(1),
      territoryConqueredNotifications: integer2("territory_conquered_notifications").notNull().default(1),
      updatedAt: text2("updated_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    inactivityReminders = sqliteTable2("inactivity_reminders", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      messageIndex: integer2("message_index").notNull(),
      // Index in the INACTIVITY_REMINDER_MESSAGES array
      sentAt: text2("sent_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    territoryLossNotifications = sqliteTable2("territory_loss_notifications", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      messageIndex: integer2("message_index").notNull(),
      // Index in the TERRITORY_LOSS_MESSAGES array
      sentAt: text2("sent_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    feedEvents = sqliteTable2("feed_events", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      eventType: text2("event_type").notNull(),
      // 'activity' | 'territory_stolen' | 'personal_record' | 'ran_together' | 'treasure_found'
      routeId: text2("route_id").references(() => routes.id, { onDelete: "set null" }),
      victimId: text2("victim_id").references(() => users.id, { onDelete: "set null" }),
      areaStolen: real2("area_stolen"),
      // m² stolen from victim
      distance: real2("distance"),
      // meters
      duration: integer2("duration"),
      // seconds
      newArea: real2("new_area"),
      // m² of new territory
      ranTogetherWith: text2("ran_together_with"),
      // JSON array of { id, name }
      recordType: text2("record_type"),
      // 'longest_run' | 'fastest_pace' | 'biggest_conquest'
      recordValue: real2("record_value"),
      metadata: text2("metadata"),
      // JSON string for extra event-specific data (e.g. treasure info)
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    feedComments = sqliteTable2("feed_comments", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      feedEventId: text2("feed_event_id").notNull().references(() => feedEvents.id, { onDelete: "cascade" }),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      parentId: text2("parent_id"),
      // NULL for top-level, comment id for replies (1 level)
      content: text2("content").notNull(),
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    feedReactions = sqliteTable2("feed_reactions", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      targetType: text2("target_type").notNull(),
      // 'event' | 'comment'
      targetId: text2("target_id").notNull(),
      // feed_event.id or feed_comment.id
      reactionType: text2("reaction_type").notNull(),
      // 'like' | 'dislike'
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    competitions = sqliteTable2("competitions", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      name: text2("name").notNull(),
      slug: text2("slug").notNull().unique(),
      startsAt: text2("starts_at").notNull(),
      // ISO timestamp
      endsAt: text2("ends_at").notNull(),
      // ISO timestamp
      status: text2("status").notNull().default("upcoming"),
      // 'upcoming' | 'active' | 'finished'
      config: text2("config"),
      // JSON config (treasure spawn settings, etc.)
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    treasures = sqliteTable2("treasures", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      competitionId: text2("competition_id").notNull(),
      name: text2("name").notNull(),
      powerType: text2("power_type").notNull(),
      // 'shield' | 'double_area' | 'nickname' | 'steal_boost' | 'invisibility' | 'time_bomb' | 'magnet' | 'reveal'
      rarity: text2("rarity").notNull(),
      // 'common' | 'rare' | 'epic' | 'legendary'
      lat: real2("lat").notNull(),
      lng: real2("lng").notNull(),
      collectedBy: text2("collected_by").references(() => users.id, { onDelete: "set null" }),
      collectedAt: text2("collected_at"),
      spawnedAt: text2("spawned_at").notNull().default(sql2`CURRENT_TIMESTAMP`),
      expiresAt: text2("expires_at").notNull(),
      // Treasure disappears after this
      active: integer2("active", { mode: "boolean" }).notNull().default(true),
      zone: text2("zone")
      // Zone name where the treasure spawned
    });
    userPowers = sqliteTable2("user_powers", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      competitionId: text2("competition_id").notNull(),
      powerType: text2("power_type").notNull(),
      treasureId: text2("treasure_id").notNull(),
      status: text2("status").notNull().default("available"),
      // 'available' | 'active' | 'used' | 'expired'
      activatedAt: text2("activated_at"),
      usedAt: text2("used_at"),
      expiresAt: text2("expires_at"),
      // For time-limited powers once activated
      metadata: text2("metadata"),
      // JSON — e.g. { targetUserId, nickname } for 'nickname' power
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    competitionStats = sqliteTable2("competition_stats", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      competitionId: text2("competition_id").notNull(),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      totalArea: real2("total_area").notNull().default(0),
      totalDistance: real2("total_distance").notNull().default(0),
      totalDuration: integer2("total_duration").notNull().default(0),
      activitiesCount: integer2("activities_count").notNull().default(0),
      treasuresCollected: integer2("treasures_collected").notNull().default(0),
      areaStolen: real2("area_stolen").notNull().default(0),
      uniqueVictims: integer2("unique_victims").notNull().default(0),
      ranTogetherCount: integer2("ran_together_count").notNull().default(0),
      updatedAt: text2("updated_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    weeklySummaries = sqliteTable2("weekly_summaries", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      competitionId: text2("competition_id").notNull(),
      weekNumber: integer2("week_number").notNull(),
      data: text2("data").notNull(),
      // JSON with rankings per category
      generatedAt: text2("generated_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    userNicknames = sqliteTable2("user_nicknames", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      targetUserId: text2("target_user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      setByUserId: text2("set_by_user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      nickname: text2("nickname").notNull(),
      expiresAt: text2("expires_at").notNull(),
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    territoryFortifications = sqliteTable2("territory_fortifications", {
      id: text2("id").primaryKey().default(sql2`(lower(hex(randomblob(16))))`),
      userId: text2("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
      routeId: text2("route_id").references(() => routes.id, { onDelete: "cascade" }),
      geometry: text2("geometry").notNull(),
      // GeoJSON of the overlap zone
      area: real2("area").notNull().default(0),
      bboxMinLng: real2("bbox_min_lng"),
      bboxMinLat: real2("bbox_min_lat"),
      bboxMaxLng: real2("bbox_max_lng"),
      bboxMaxLat: real2("bbox_max_lat"),
      createdAt: text2("created_at").notNull().default(sql2`CURRENT_TIMESTAMP`)
    });
    usersRelations = relations(users, ({ many, one }) => ({
      routes: many(routes),
      territories: many(territories),
      friendships: many(friendships),
      friendInvites: many(friendInvites),
      polarAccount: one(polarAccounts),
      polarActivities: many(polarActivities),
      stravaAccount: one(stravaAccounts),
      stravaActivities: many(stravaActivities),
      feedEvents: many(feedEvents),
      feedComments: many(feedComments),
      inactivityReminders: many(inactivityReminders),
      territoryLossNotifications: many(territoryLossNotifications)
    }));
    polarAccountsRelations = relations(polarAccounts, ({ one }) => ({
      user: one(users, {
        fields: [polarAccounts.userId],
        references: [users.id]
      })
    }));
    polarActivitiesRelations = relations(polarActivities, ({ one }) => ({
      user: one(users, {
        fields: [polarActivities.userId],
        references: [users.id]
      }),
      route: one(routes, {
        fields: [polarActivities.routeId],
        references: [routes.id]
      }),
      territory: one(territories, {
        fields: [polarActivities.territoryId],
        references: [territories.id]
      })
    }));
    routesRelations = relations(routes, ({ one, many }) => ({
      user: one(users, {
        fields: [routes.userId],
        references: [users.id]
      }),
      territories: many(territories)
    }));
    territoriesRelations = relations(territories, ({ one }) => ({
      user: one(users, {
        fields: [territories.userId],
        references: [users.id]
      }),
      route: one(routes, {
        fields: [territories.routeId],
        references: [routes.id]
      })
    }));
    conquestMetricsRelations = relations(conquestMetrics, ({ one }) => ({
      attacker: one(users, {
        fields: [conquestMetrics.attackerId],
        references: [users.id]
      }),
      defender: one(users, {
        fields: [conquestMetrics.defenderId],
        references: [users.id]
      }),
      route: one(routes, {
        fields: [conquestMetrics.routeId],
        references: [routes.id]
      })
    }));
    friendshipsRelations = relations(friendships, ({ one }) => ({
      user: one(users, {
        fields: [friendships.userId],
        references: [users.id]
      }),
      friend: one(users, {
        fields: [friendships.friendId],
        references: [users.id]
      })
    }));
    friendInvitesRelations = relations(friendInvites, ({ one }) => ({
      user: one(users, {
        fields: [friendInvites.userId],
        references: [users.id]
      })
    }));
    stravaAccountsRelations = relations(stravaAccounts, ({ one }) => ({
      user: one(users, {
        fields: [stravaAccounts.userId],
        references: [users.id]
      })
    }));
    stravaActivitiesRelations = relations(stravaActivities, ({ one }) => ({
      user: one(users, {
        fields: [stravaActivities.userId],
        references: [users.id]
      }),
      route: one(routes, {
        fields: [stravaActivities.routeId],
        references: [routes.id]
      }),
      territory: one(territories, {
        fields: [stravaActivities.territoryId],
        references: [territories.id]
      })
    }));
    inactivityRemindersRelations = relations(inactivityReminders, ({ one }) => ({
      user: one(users, {
        fields: [inactivityReminders.userId],
        references: [users.id]
      })
    }));
    territoryLossNotificationsRelations = relations(territoryLossNotifications, ({ one }) => ({
      user: one(users, {
        fields: [territoryLossNotifications.userId],
        references: [users.id]
      })
    }));
    feedEventsRelations = relations(feedEvents, ({ one, many }) => ({
      user: one(users, {
        fields: [feedEvents.userId],
        references: [users.id]
      }),
      route: one(routes, {
        fields: [feedEvents.routeId],
        references: [routes.id]
      }),
      victim: one(users, {
        fields: [feedEvents.victimId],
        references: [users.id]
      }),
      comments: many(feedComments)
    }));
    feedCommentsRelations = relations(feedComments, ({ one }) => ({
      feedEvent: one(feedEvents, {
        fields: [feedComments.feedEventId],
        references: [feedEvents.id]
      }),
      user: one(users, {
        fields: [feedComments.userId],
        references: [users.id]
      })
    }));
    insertUserSchema = createInsertSchema(users).omit({
      id: true,
      totalArea: true,
      createdAt: true
    });
    insertRouteSchema = createInsertSchema(routes).omit({
      id: true
    });
    insertTerritorySchema = createInsertSchema(territories).omit({
      id: true,
      conqueredAt: true
    });
    insertFriendshipSchema = createInsertSchema(friendships).omit({
      id: true,
      createdAt: true
    });
    insertFriendInviteSchema = createInsertSchema(friendInvites).omit({
      id: true,
      createdAt: true
    });
    insertFriendRequestSchema = createInsertSchema(friendRequests).omit({
      id: true,
      createdAt: true,
      status: true
    });
    insertPolarAccountSchema = createInsertSchema(polarAccounts).omit({
      id: true,
      createdAt: true
    });
    insertPolarActivitySchema = createInsertSchema(polarActivities).omit({
      id: true,
      createdAt: true
    });
    insertStravaAccountSchema = createInsertSchema(stravaAccounts).omit({
      id: true,
      createdAt: true
    });
    insertStravaActivitySchema = createInsertSchema(stravaActivities).omit({
      id: true,
      createdAt: true
    });
    insertCorosAccountSchema = createInsertSchema(corosAccounts).omit({
      id: true,
      createdAt: true
    });
    insertCorosActivitySchema = createInsertSchema(corosActivities).omit({
      id: true,
      createdAt: true
    });
    insertPushSubscriptionSchema = createInsertSchema(pushSubscriptions).omit({
      id: true,
      createdAt: true
    });
    insertFeedEventSchema = createInsertSchema(feedEvents).omit({
      id: true,
      createdAt: true
    });
    insertFeedCommentSchema = createInsertSchema(feedComments).omit({
      id: true,
      createdAt: true
    });
    insertFeedReactionSchema = createInsertSchema(feedReactions).omit({
      id: true,
      createdAt: true
    });
    insertConquestMetricSchema = createInsertSchema(conquestMetrics).omit({
      id: true,
      createdAt: true
    });
    insertEphemeralPhotoSchema = createInsertSchema(ephemeralPhotos).omit({
      id: true,
      createdAt: true,
      viewed: true
    });
    insertInactivityReminderSchema = createInsertSchema(inactivityReminders).omit({
      id: true,
      sentAt: true
    });
    insertTerritoryLossNotificationSchema = createInsertSchema(territoryLossNotifications).omit({
      id: true,
      sentAt: true
    });
    insertCompetitionSchema = createInsertSchema(competitions).omit({
      id: true,
      createdAt: true,
      status: true
    });
    insertTreasureSchema = createInsertSchema(treasures).omit({
      id: true,
      collectedBy: true,
      collectedAt: true,
      active: true
    });
    insertUserPowerSchema = createInsertSchema(userPowers).omit({
      id: true,
      createdAt: true,
      status: true,
      activatedAt: true,
      usedAt: true
    });
    insertCompetitionStatsSchema = createInsertSchema(competitionStats).omit({
      id: true,
      updatedAt: true
    });
    insertWeeklySummarySchema = createInsertSchema(weeklySummaries).omit({
      id: true,
      generatedAt: true
    });
    insertUserNicknameSchema = createInsertSchema(userNicknames).omit({
      id: true,
      createdAt: true
    });
    insertTerritoryFortificationSchema = createInsertSchema(territoryFortifications).omit({
      id: true,
      createdAt: true
    });
    insertEmailNotificationSchema = createInsertSchema(emailNotifications);
    insertEmailPreferencesSchema = createInsertSchema(emailPreferences);
    TREASURE_DEFINITIONS = {
      shield: {
        powerType: "shield",
        name: "Escudo de Acero",
        description: "Tu siguiente territorio es inmune a robos durante 24h",
        rarity: "rare",
        emoji: "\u{1F6E1}\uFE0F",
        color: "#7C8CA1",
        duration: 24
      },
      double_area: {
        powerType: "double_area",
        name: "Doble Conquista",
        description: "Los km\xB2 de tu siguiente ruta cuentan x2",
        rarity: "epic",
        emoji: "\u26A1",
        color: "#F59E0B"
      },
      nickname: {
        powerType: "nickname",
        name: "Pluma del Troll",
        description: "Pon un apodo p\xFAblico a otro usuario durante 48h",
        rarity: "common",
        emoji: "\u270F\uFE0F",
        color: "#CD7F32",
        duration: 48
      },
      steal_boost: {
        powerType: "steal_boost",
        name: "Espada Voraz",
        description: "Robas un 50% m\xE1s de lo normal en tu siguiente ruta",
        rarity: "epic",
        emoji: "\u2694\uFE0F",
        color: "#F59E0B"
      },
      invisibility: {
        powerType: "invisibility",
        name: "Capa de Sombras",
        description: "Tu territorio es invisible en el mapa para otros durante 24h",
        rarity: "legendary",
        emoji: "\u{1F47B}",
        color: "#8B5CF6",
        duration: 24
      },
      time_bomb: {
        powerType: "time_bomb",
        name: "Bomba Temporal",
        description: "El usuario que te robe en las pr\xF3ximas 24h pierde el doble",
        rarity: "rare",
        emoji: "\u{1F4A3}",
        color: "#7C8CA1",
        duration: 24
      },
      magnet: {
        powerType: "magnet",
        name: "Im\xE1n de Tierras",
        description: "Tu siguiente ruta absorbe un 25% extra de territorios en su radio",
        rarity: "legendary",
        emoji: "\u{1F9F2}",
        color: "#8B5CF6"
      },
      reveal: {
        powerType: "reveal",
        name: "Ojo del Halc\xF3n",
        description: "Revela la ubicaci\xF3n del siguiente tesoro 1h antes que los dem\xE1s",
        rarity: "common",
        emoji: "\u{1F985}",
        color: "#CD7F32"
      },
      bulldozer: {
        powerType: "bulldozer",
        name: "El Arrasador",
        description: "Tu siguiente ruta ignora TODAS las fortalezas enemigas",
        rarity: "epic",
        emoji: "\u{1F69C}",
        color: "#F59E0B"
      },
      battering_ram: {
        powerType: "battering_ram",
        name: "Ariete de Guerra",
        description: "Tu siguiente ruta tiene fuerza \xD73: cada pasada rompe 3 capas de fortaleza",
        rarity: "legendary",
        emoji: "\u{1FA93}",
        color: "#8B5CF6"
      },
      wall: {
        powerType: "wall",
        name: "Muralla Imparable",
        description: "Durante 24h, cada carrera fortalece el doble (+1.0 en vez de +0.5)",
        rarity: "rare",
        emoji: "\u{1F9F1}",
        color: "#7C8CA1",
        duration: 24
      },
      sentinel: {
        powerType: "sentinel",
        name: "Centinela",
        description: "Durante 24h, recibes notificaci\xF3n instant\xE1nea cuando alguien intenta robar tu territorio",
        rarity: "epic",
        emoji: "\u{1F514}",
        color: "#F59E0B",
        duration: 24
      }
    };
    RARITY_CONFIG = {
      common: { label: "Com\xFAn", color: "#CD7F32", bgGradient: "from-amber-700 to-yellow-600", probability: 0.4 },
      rare: { label: "Raro", color: "#7C8CA1", bgGradient: "from-slate-400 to-blue-300", probability: 0.3 },
      epic: { label: "\xC9pico", color: "#F59E0B", bgGradient: "from-amber-400 to-orange-500", probability: 0.2 },
      legendary: { label: "Legendario", color: "#8B5CF6", bgGradient: "from-violet-500 to-purple-600", probability: 0.1 }
    };
    ZARAGOZA_BOUNDS = {
      north: 41.68,
      south: 41.61,
      east: -0.83,
      west: -0.94,
      center: { lat: 41.6488, lng: -0.8891 }
    };
    COMPETITION_START = "2026-03-02T09:00:00+01:00";
    COMPETITION_END = "2026-03-30T23:59:59+02:00";
    COMPETITION_NAME = "La Primera Conquista del Ebro";
    COMPETITION_SLUG = "la-primera-conquista-del-ebro";
  }
});

// worker/src/db.ts
function createDb(databaseUrl, authToken) {
  const client = createClient({
    url: databaseUrl,
    authToken
  });
  return drizzle(client, { schema: schema_exports });
}
var init_db3 = __esm({
  "worker/src/db.ts"() {
    "use strict";
    init_web2();
    init_libsql();
    init_schema();
    __name(createDb, "createDb");
  }
});

// worker/node_modules/@turf/helpers/dist/esm/index.js
function feature(geom, properties, options = {}) {
  const feat = { type: "Feature" };
  if (options.id === 0 || options.id) {
    feat.id = options.id;
  }
  if (options.bbox) {
    feat.bbox = options.bbox;
  }
  feat.properties = properties || {};
  feat.geometry = geom;
  return feat;
}
function point(coordinates, properties, options = {}) {
  if (!coordinates) {
    throw new Error("coordinates is required");
  }
  if (!Array.isArray(coordinates)) {
    throw new Error("coordinates must be an Array");
  }
  if (coordinates.length < 2) {
    throw new Error("coordinates must be at least 2 numbers long");
  }
  if (!isNumber(coordinates[0]) || !isNumber(coordinates[1])) {
    throw new Error("coordinates must contain numbers");
  }
  const geom = {
    type: "Point",
    coordinates
  };
  return feature(geom, properties, options);
}
function polygon(coordinates, properties, options = {}) {
  for (const ring of coordinates) {
    if (ring.length < 4) {
      throw new Error(
        "Each LinearRing of a Polygon must have 4 or more Positions."
      );
    }
    if (ring[ring.length - 1].length !== ring[0].length) {
      throw new Error("First and last Position are not equivalent.");
    }
    for (let j = 0; j < ring[ring.length - 1].length; j++) {
      if (ring[ring.length - 1][j] !== ring[0][j]) {
        throw new Error("First and last Position are not equivalent.");
      }
    }
  }
  const geom = {
    type: "Polygon",
    coordinates
  };
  return feature(geom, properties, options);
}
function lineString(coordinates, properties, options = {}) {
  if (coordinates.length < 2) {
    throw new Error("coordinates must be an array of two or more positions");
  }
  const geom = {
    type: "LineString",
    coordinates
  };
  return feature(geom, properties, options);
}
function featureCollection(features, options = {}) {
  const fc = { type: "FeatureCollection" };
  if (options.id) {
    fc.id = options.id;
  }
  if (options.bbox) {
    fc.bbox = options.bbox;
  }
  fc.features = features;
  return fc;
}
function multiPolygon(coordinates, properties, options = {}) {
  const geom = {
    type: "MultiPolygon",
    coordinates
  };
  return feature(geom, properties, options);
}
function radiansToLength(radians2, units = "kilometers") {
  const factor = factors[units];
  if (!factor) {
    throw new Error(units + " units is invalid");
  }
  return radians2 * factor;
}
function lengthToRadians(distance2, units = "kilometers") {
  const factor = factors[units];
  if (!factor) {
    throw new Error(units + " units is invalid");
  }
  return distance2 / factor;
}
function degreesToRadians(degrees2) {
  const normalisedDegrees = degrees2 % 360;
  return normalisedDegrees * Math.PI / 180;
}
function isNumber(num) {
  return !isNaN(num) && num !== null && !Array.isArray(num);
}
var earthRadius, factors;
var init_esm = __esm({
  "worker/node_modules/@turf/helpers/dist/esm/index.js"() {
    earthRadius = 63710088e-1;
    factors = {
      centimeters: earthRadius * 100,
      centimetres: earthRadius * 100,
      degrees: 360 / (2 * Math.PI),
      feet: earthRadius * 3.28084,
      inches: earthRadius * 39.37,
      kilometers: earthRadius / 1e3,
      kilometres: earthRadius / 1e3,
      meters: earthRadius,
      metres: earthRadius,
      miles: earthRadius / 1609.344,
      millimeters: earthRadius * 1e3,
      millimetres: earthRadius * 1e3,
      nauticalmiles: earthRadius / 1852,
      radians: 1,
      yards: earthRadius * 1.0936
    };
    __name(feature, "feature");
    __name(point, "point");
    __name(polygon, "polygon");
    __name(lineString, "lineString");
    __name(featureCollection, "featureCollection");
    __name(multiPolygon, "multiPolygon");
    __name(radiansToLength, "radiansToLength");
    __name(lengthToRadians, "lengthToRadians");
    __name(degreesToRadians, "degreesToRadians");
    __name(isNumber, "isNumber");
  }
});

// worker/node_modules/@turf/invariant/dist/esm/index.js
function getCoord(coord) {
  if (!coord) {
    throw new Error("coord is required");
  }
  if (!Array.isArray(coord)) {
    if (coord.type === "Feature" && coord.geometry !== null && coord.geometry.type === "Point") {
      return [...coord.geometry.coordinates];
    }
    if (coord.type === "Point") {
      return [...coord.coordinates];
    }
  }
  if (Array.isArray(coord) && coord.length >= 2 && !Array.isArray(coord[0]) && !Array.isArray(coord[1])) {
    return [...coord];
  }
  throw new Error("coord must be GeoJSON Point or an Array of numbers");
}
var init_esm2 = __esm({
  "worker/node_modules/@turf/invariant/dist/esm/index.js"() {
    __name(getCoord, "getCoord");
  }
});

// worker/node_modules/@turf/distance/dist/esm/index.js
function distance(from, to, options = {}) {
  var coordinates1 = getCoord(from);
  var coordinates2 = getCoord(to);
  var dLat = degreesToRadians(coordinates2[1] - coordinates1[1]);
  var dLon = degreesToRadians(coordinates2[0] - coordinates1[0]);
  var lat1 = degreesToRadians(coordinates1[1]);
  var lat2 = degreesToRadians(coordinates2[1]);
  var a = Math.pow(Math.sin(dLat / 2), 2) + Math.pow(Math.sin(dLon / 2), 2) * Math.cos(lat1) * Math.cos(lat2);
  return radiansToLength(
    2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a)),
    options.units
  );
}
var init_esm3 = __esm({
  "worker/node_modules/@turf/distance/dist/esm/index.js"() {
    init_esm2();
    init_esm();
    __name(distance, "distance");
  }
});

// worker/node_modules/@turf/meta/dist/esm/index.js
function coordEach(geojson, callback, excludeWrapCoord) {
  if (geojson === null) return;
  var j, k, l, geometry, stopG, coords, geometryMaybeCollection, wrapShrink = 0, coordIndex = 0, isGeometryCollection, type = geojson.type, isFeatureCollection = type === "FeatureCollection", isFeature = type === "Feature", stop = isFeatureCollection ? geojson.features.length : 1;
  for (var featureIndex = 0; featureIndex < stop; featureIndex++) {
    geometryMaybeCollection = isFeatureCollection ? geojson.features[featureIndex].geometry : isFeature ? geojson.geometry : geojson;
    isGeometryCollection = geometryMaybeCollection ? geometryMaybeCollection.type === "GeometryCollection" : false;
    stopG = isGeometryCollection ? geometryMaybeCollection.geometries.length : 1;
    for (var geomIndex = 0; geomIndex < stopG; geomIndex++) {
      var multiFeatureIndex = 0;
      var geometryIndex = 0;
      geometry = isGeometryCollection ? geometryMaybeCollection.geometries[geomIndex] : geometryMaybeCollection;
      if (geometry === null) continue;
      coords = geometry.coordinates;
      var geomType = geometry.type;
      wrapShrink = excludeWrapCoord && (geomType === "Polygon" || geomType === "MultiPolygon") ? 1 : 0;
      switch (geomType) {
        case null:
          break;
        case "Point":
          if (callback(
            coords,
            coordIndex,
            featureIndex,
            multiFeatureIndex,
            geometryIndex
          ) === false)
            return false;
          coordIndex++;
          multiFeatureIndex++;
          break;
        case "LineString":
        case "MultiPoint":
          for (j = 0; j < coords.length; j++) {
            if (callback(
              coords[j],
              coordIndex,
              featureIndex,
              multiFeatureIndex,
              geometryIndex
            ) === false)
              return false;
            coordIndex++;
            if (geomType === "MultiPoint") multiFeatureIndex++;
          }
          if (geomType === "LineString") multiFeatureIndex++;
          break;
        case "Polygon":
        case "MultiLineString":
          for (j = 0; j < coords.length; j++) {
            for (k = 0; k < coords[j].length - wrapShrink; k++) {
              if (callback(
                coords[j][k],
                coordIndex,
                featureIndex,
                multiFeatureIndex,
                geometryIndex
              ) === false)
                return false;
              coordIndex++;
            }
            if (geomType === "MultiLineString") multiFeatureIndex++;
            if (geomType === "Polygon") geometryIndex++;
          }
          if (geomType === "Polygon") multiFeatureIndex++;
          break;
        case "MultiPolygon":
          for (j = 0; j < coords.length; j++) {
            geometryIndex = 0;
            for (k = 0; k < coords[j].length; k++) {
              for (l = 0; l < coords[j][k].length - wrapShrink; l++) {
                if (callback(
                  coords[j][k][l],
                  coordIndex,
                  featureIndex,
                  multiFeatureIndex,
                  geometryIndex
                ) === false)
                  return false;
                coordIndex++;
              }
              geometryIndex++;
            }
            multiFeatureIndex++;
          }
          break;
        case "GeometryCollection":
          for (j = 0; j < geometry.geometries.length; j++)
            if (coordEach(geometry.geometries[j], callback, excludeWrapCoord) === false)
              return false;
          break;
        default:
          throw new Error("Unknown Geometry Type");
      }
    }
  }
}
function featureEach(geojson, callback) {
  if (geojson.type === "Feature") {
    callback(geojson, 0);
  } else if (geojson.type === "FeatureCollection") {
    for (var i = 0; i < geojson.features.length; i++) {
      if (callback(geojson.features[i], i) === false) break;
    }
  }
}
function geomEach(geojson, callback) {
  var i, j, g, geometry, stopG, geometryMaybeCollection, isGeometryCollection, featureProperties, featureBBox, featureId, featureIndex = 0, isFeatureCollection = geojson.type === "FeatureCollection", isFeature = geojson.type === "Feature", stop = isFeatureCollection ? geojson.features.length : 1;
  for (i = 0; i < stop; i++) {
    geometryMaybeCollection = isFeatureCollection ? geojson.features[i].geometry : isFeature ? geojson.geometry : geojson;
    featureProperties = isFeatureCollection ? geojson.features[i].properties : isFeature ? geojson.properties : {};
    featureBBox = isFeatureCollection ? geojson.features[i].bbox : isFeature ? geojson.bbox : void 0;
    featureId = isFeatureCollection ? geojson.features[i].id : isFeature ? geojson.id : void 0;
    isGeometryCollection = geometryMaybeCollection ? geometryMaybeCollection.type === "GeometryCollection" : false;
    stopG = isGeometryCollection ? geometryMaybeCollection.geometries.length : 1;
    for (g = 0; g < stopG; g++) {
      geometry = isGeometryCollection ? geometryMaybeCollection.geometries[g] : geometryMaybeCollection;
      if (geometry === null) {
        if (callback(
          null,
          featureIndex,
          featureProperties,
          featureBBox,
          featureId
        ) === false)
          return false;
        continue;
      }
      switch (geometry.type) {
        case "Point":
        case "LineString":
        case "MultiPoint":
        case "Polygon":
        case "MultiLineString":
        case "MultiPolygon": {
          if (callback(
            geometry,
            featureIndex,
            featureProperties,
            featureBBox,
            featureId
          ) === false)
            return false;
          break;
        }
        case "GeometryCollection": {
          for (j = 0; j < geometry.geometries.length; j++) {
            if (callback(
              geometry.geometries[j],
              featureIndex,
              featureProperties,
              featureBBox,
              featureId
            ) === false)
              return false;
          }
          break;
        }
        default:
          throw new Error("Unknown Geometry Type");
      }
    }
    featureIndex++;
  }
}
function geomReduce(geojson, callback, initialValue) {
  var previousValue = initialValue;
  geomEach(
    geojson,
    function(currentGeometry, featureIndex, featureProperties, featureBBox, featureId) {
      if (featureIndex === 0 && initialValue === void 0)
        previousValue = currentGeometry;
      else
        previousValue = callback(
          previousValue,
          currentGeometry,
          featureIndex,
          featureProperties,
          featureBBox,
          featureId
        );
    }
  );
  return previousValue;
}
function flattenEach(geojson, callback) {
  geomEach(geojson, function(geometry, featureIndex, properties, bbox2, id) {
    var type = geometry === null ? null : geometry.type;
    switch (type) {
      case null:
      case "Point":
      case "LineString":
      case "Polygon":
        if (callback(
          feature(geometry, properties, { bbox: bbox2, id }),
          featureIndex,
          0
        ) === false)
          return false;
        return;
    }
    var geomType;
    switch (type) {
      case "MultiPoint":
        geomType = "Point";
        break;
      case "MultiLineString":
        geomType = "LineString";
        break;
      case "MultiPolygon":
        geomType = "Polygon";
        break;
    }
    for (var multiFeatureIndex = 0; multiFeatureIndex < geometry.coordinates.length; multiFeatureIndex++) {
      var coordinate = geometry.coordinates[multiFeatureIndex];
      var geom = {
        type: geomType,
        coordinates: coordinate
      };
      if (callback(feature(geom, properties), featureIndex, multiFeatureIndex) === false)
        return false;
    }
  });
}
function segmentEach(geojson, callback) {
  flattenEach(geojson, function(feature2, featureIndex, multiFeatureIndex) {
    var segmentIndex = 0;
    if (!feature2.geometry) return;
    var type = feature2.geometry.type;
    if (type === "Point" || type === "MultiPoint") return;
    var previousCoords;
    var previousFeatureIndex = 0;
    var previousMultiIndex = 0;
    var prevGeomIndex = 0;
    if (coordEach(
      feature2,
      function(currentCoord, coordIndex, featureIndexCoord, multiPartIndexCoord, geometryIndex) {
        if (previousCoords === void 0 || featureIndex > previousFeatureIndex || multiPartIndexCoord > previousMultiIndex || geometryIndex > prevGeomIndex) {
          previousCoords = currentCoord;
          previousFeatureIndex = featureIndex;
          previousMultiIndex = multiPartIndexCoord;
          prevGeomIndex = geometryIndex;
          segmentIndex = 0;
          return;
        }
        var currentSegment = lineString(
          [previousCoords, currentCoord],
          feature2.properties
        );
        if (callback(
          currentSegment,
          featureIndex,
          multiFeatureIndex,
          geometryIndex,
          segmentIndex
        ) === false)
          return false;
        segmentIndex++;
        previousCoords = currentCoord;
      }
    ) === false)
      return false;
  });
}
function segmentReduce(geojson, callback, initialValue) {
  var previousValue = initialValue;
  var started = false;
  segmentEach(
    geojson,
    function(currentSegment, featureIndex, multiFeatureIndex, geometryIndex, segmentIndex) {
      if (started === false && initialValue === void 0)
        previousValue = currentSegment;
      else
        previousValue = callback(
          previousValue,
          currentSegment,
          featureIndex,
          multiFeatureIndex,
          geometryIndex,
          segmentIndex
        );
      started = true;
    }
  );
  return previousValue;
}
var init_esm4 = __esm({
  "worker/node_modules/@turf/meta/dist/esm/index.js"() {
    init_esm();
    __name(coordEach, "coordEach");
    __name(featureEach, "featureEach");
    __name(geomEach, "geomEach");
    __name(geomReduce, "geomReduce");
    __name(flattenEach, "flattenEach");
    __name(segmentEach, "segmentEach");
    __name(segmentReduce, "segmentReduce");
  }
});

// worker/node_modules/@turf/area/dist/esm/index.js
function area(geojson) {
  return geomReduce(
    geojson,
    (value, geom) => {
      return value + calculateArea(geom);
    },
    0
  );
}
function calculateArea(geom) {
  let total = 0;
  let i;
  switch (geom.type) {
    case "Polygon":
      return polygonArea(geom.coordinates);
    case "MultiPolygon":
      for (i = 0; i < geom.coordinates.length; i++) {
        total += polygonArea(geom.coordinates[i]);
      }
      return total;
    case "Point":
    case "MultiPoint":
    case "LineString":
    case "MultiLineString":
      return 0;
  }
  return 0;
}
function polygonArea(coords) {
  let total = 0;
  if (coords && coords.length > 0) {
    total += Math.abs(ringArea(coords[0]));
    for (let i = 1; i < coords.length; i++) {
      total -= Math.abs(ringArea(coords[i]));
    }
  }
  return total;
}
function ringArea(coords) {
  const coordsLength = coords.length - 1;
  if (coordsLength <= 2) return 0;
  let total = 0;
  let i = 0;
  while (i < coordsLength) {
    const lower = coords[i];
    const middle = coords[i + 1 === coordsLength ? 0 : i + 1];
    const upper = coords[i + 2 >= coordsLength ? (i + 2) % coordsLength : i + 2];
    const lowerX = lower[0] * PI_OVER_180;
    const middleY = middle[1] * PI_OVER_180;
    const upperX = upper[0] * PI_OVER_180;
    total += (upperX - lowerX) * Math.sin(middleY);
    i++;
  }
  return total * FACTOR;
}
var FACTOR, PI_OVER_180;
var init_esm5 = __esm({
  "worker/node_modules/@turf/area/dist/esm/index.js"() {
    init_esm();
    init_esm4();
    __name(area, "area");
    __name(calculateArea, "calculateArea");
    __name(polygonArea, "polygonArea");
    FACTOR = earthRadius * earthRadius / 2;
    PI_OVER_180 = Math.PI / 180;
    __name(ringArea, "ringArea");
  }
});

// worker/node_modules/@turf/bbox/dist/esm/index.js
function bbox(geojson, options = {}) {
  if (geojson.bbox != null && true !== options.recompute) {
    return geojson.bbox;
  }
  const result = [Infinity, Infinity, -Infinity, -Infinity];
  coordEach(geojson, (coord) => {
    if (result[0] > coord[0]) {
      result[0] = coord[0];
    }
    if (result[1] > coord[1]) {
      result[1] = coord[1];
    }
    if (result[2] < coord[0]) {
      result[2] = coord[0];
    }
    if (result[3] < coord[1]) {
      result[3] = coord[1];
    }
  });
  return result;
}
var init_esm6 = __esm({
  "worker/node_modules/@turf/bbox/dist/esm/index.js"() {
    init_esm4();
    __name(bbox, "bbox");
  }
});

// worker/node_modules/rbush/rbush.min.js
var require_rbush_min = __commonJS({
  "worker/node_modules/rbush/rbush.min.js"(exports, module) {
    !(function(t, i) {
      "object" == typeof exports && "undefined" != typeof module ? module.exports = i() : "function" == typeof define && define.amd ? define(i) : (t = t || self).RBush = i();
    })(exports, function() {
      "use strict";
      function t(t2, r2, e3, a2, h2) {
        !(/* @__PURE__ */ __name(function t3(n2, r3, e4, a3, h3) {
          for (; a3 > e4; ) {
            if (a3 - e4 > 600) {
              var o2 = a3 - e4 + 1, s2 = r3 - e4 + 1, l2 = Math.log(o2), f2 = 0.5 * Math.exp(2 * l2 / 3), u2 = 0.5 * Math.sqrt(l2 * f2 * (o2 - f2) / o2) * (s2 - o2 / 2 < 0 ? -1 : 1), m2 = Math.max(e4, Math.floor(r3 - s2 * f2 / o2 + u2)), c2 = Math.min(a3, Math.floor(r3 + (o2 - s2) * f2 / o2 + u2));
              t3(n2, r3, m2, c2, h3);
            }
            var p2 = n2[r3], d2 = e4, x = a3;
            for (i(n2, e4, r3), h3(n2[a3], p2) > 0 && i(n2, e4, a3); d2 < x; ) {
              for (i(n2, d2, x), d2++, x--; h3(n2[d2], p2) < 0; ) d2++;
              for (; h3(n2[x], p2) > 0; ) x--;
            }
            0 === h3(n2[e4], p2) ? i(n2, e4, x) : i(n2, ++x, a3), x <= r3 && (e4 = x + 1), r3 <= x && (a3 = x - 1);
          }
        }, "t"))(t2, r2, e3 || 0, a2 || t2.length - 1, h2 || n);
      }
      __name(t, "t");
      function i(t2, i2, n2) {
        var r2 = t2[i2];
        t2[i2] = t2[n2], t2[n2] = r2;
      }
      __name(i, "i");
      function n(t2, i2) {
        return t2 < i2 ? -1 : t2 > i2 ? 1 : 0;
      }
      __name(n, "n");
      var r = /* @__PURE__ */ __name(function(t2) {
        void 0 === t2 && (t2 = 9), this._maxEntries = Math.max(4, t2), this._minEntries = Math.max(2, Math.ceil(0.4 * this._maxEntries)), this.clear();
      }, "r");
      function e(t2, i2, n2) {
        if (!n2) return i2.indexOf(t2);
        for (var r2 = 0; r2 < i2.length; r2++) if (n2(t2, i2[r2])) return r2;
        return -1;
      }
      __name(e, "e");
      function a(t2, i2) {
        h(t2, 0, t2.children.length, i2, t2);
      }
      __name(a, "a");
      function h(t2, i2, n2, r2, e3) {
        e3 || (e3 = p(null)), e3.minX = 1 / 0, e3.minY = 1 / 0, e3.maxX = -1 / 0, e3.maxY = -1 / 0;
        for (var a2 = i2; a2 < n2; a2++) {
          var h2 = t2.children[a2];
          o(e3, t2.leaf ? r2(h2) : h2);
        }
        return e3;
      }
      __name(h, "h");
      function o(t2, i2) {
        return t2.minX = Math.min(t2.minX, i2.minX), t2.minY = Math.min(t2.minY, i2.minY), t2.maxX = Math.max(t2.maxX, i2.maxX), t2.maxY = Math.max(t2.maxY, i2.maxY), t2;
      }
      __name(o, "o");
      function s(t2, i2) {
        return t2.minX - i2.minX;
      }
      __name(s, "s");
      function l(t2, i2) {
        return t2.minY - i2.minY;
      }
      __name(l, "l");
      function f(t2) {
        return (t2.maxX - t2.minX) * (t2.maxY - t2.minY);
      }
      __name(f, "f");
      function u(t2) {
        return t2.maxX - t2.minX + (t2.maxY - t2.minY);
      }
      __name(u, "u");
      function m(t2, i2) {
        return t2.minX <= i2.minX && t2.minY <= i2.minY && i2.maxX <= t2.maxX && i2.maxY <= t2.maxY;
      }
      __name(m, "m");
      function c(t2, i2) {
        return i2.minX <= t2.maxX && i2.minY <= t2.maxY && i2.maxX >= t2.minX && i2.maxY >= t2.minY;
      }
      __name(c, "c");
      function p(t2) {
        return { children: t2, height: 1, leaf: true, minX: 1 / 0, minY: 1 / 0, maxX: -1 / 0, maxY: -1 / 0 };
      }
      __name(p, "p");
      function d(i2, n2, r2, e3, a2) {
        for (var h2 = [n2, r2]; h2.length; ) if (!((r2 = h2.pop()) - (n2 = h2.pop()) <= e3)) {
          var o2 = n2 + Math.ceil((r2 - n2) / e3 / 2) * e3;
          t(i2, o2, n2, r2, a2), h2.push(n2, o2, o2, r2);
        }
      }
      __name(d, "d");
      return r.prototype.all = function() {
        return this._all(this.data, []);
      }, r.prototype.search = function(t2) {
        var i2 = this.data, n2 = [];
        if (!c(t2, i2)) return n2;
        for (var r2 = this.toBBox, e3 = []; i2; ) {
          for (var a2 = 0; a2 < i2.children.length; a2++) {
            var h2 = i2.children[a2], o2 = i2.leaf ? r2(h2) : h2;
            c(t2, o2) && (i2.leaf ? n2.push(h2) : m(t2, o2) ? this._all(h2, n2) : e3.push(h2));
          }
          i2 = e3.pop();
        }
        return n2;
      }, r.prototype.collides = function(t2) {
        var i2 = this.data;
        if (!c(t2, i2)) return false;
        for (var n2 = []; i2; ) {
          for (var r2 = 0; r2 < i2.children.length; r2++) {
            var e3 = i2.children[r2], a2 = i2.leaf ? this.toBBox(e3) : e3;
            if (c(t2, a2)) {
              if (i2.leaf || m(t2, a2)) return true;
              n2.push(e3);
            }
          }
          i2 = n2.pop();
        }
        return false;
      }, r.prototype.load = function(t2) {
        if (!t2 || !t2.length) return this;
        if (t2.length < this._minEntries) {
          for (var i2 = 0; i2 < t2.length; i2++) this.insert(t2[i2]);
          return this;
        }
        var n2 = this._build(t2.slice(), 0, t2.length - 1, 0);
        if (this.data.children.length) if (this.data.height === n2.height) this._splitRoot(this.data, n2);
        else {
          if (this.data.height < n2.height) {
            var r2 = this.data;
            this.data = n2, n2 = r2;
          }
          this._insert(n2, this.data.height - n2.height - 1, true);
        }
        else this.data = n2;
        return this;
      }, r.prototype.insert = function(t2) {
        return t2 && this._insert(t2, this.data.height - 1), this;
      }, r.prototype.clear = function() {
        return this.data = p([]), this;
      }, r.prototype.remove = function(t2, i2) {
        if (!t2) return this;
        for (var n2, r2, a2, h2 = this.data, o2 = this.toBBox(t2), s2 = [], l2 = []; h2 || s2.length; ) {
          if (h2 || (h2 = s2.pop(), r2 = s2[s2.length - 1], n2 = l2.pop(), a2 = true), h2.leaf) {
            var f2 = e(t2, h2.children, i2);
            if (-1 !== f2) return h2.children.splice(f2, 1), s2.push(h2), this._condense(s2), this;
          }
          a2 || h2.leaf || !m(h2, o2) ? r2 ? (n2++, h2 = r2.children[n2], a2 = false) : h2 = null : (s2.push(h2), l2.push(n2), n2 = 0, r2 = h2, h2 = h2.children[0]);
        }
        return this;
      }, r.prototype.toBBox = function(t2) {
        return t2;
      }, r.prototype.compareMinX = function(t2, i2) {
        return t2.minX - i2.minX;
      }, r.prototype.compareMinY = function(t2, i2) {
        return t2.minY - i2.minY;
      }, r.prototype.toJSON = function() {
        return this.data;
      }, r.prototype.fromJSON = function(t2) {
        return this.data = t2, this;
      }, r.prototype._all = function(t2, i2) {
        for (var n2 = []; t2; ) t2.leaf ? i2.push.apply(i2, t2.children) : n2.push.apply(n2, t2.children), t2 = n2.pop();
        return i2;
      }, r.prototype._build = function(t2, i2, n2, r2) {
        var e3, h2 = n2 - i2 + 1, o2 = this._maxEntries;
        if (h2 <= o2) return a(e3 = p(t2.slice(i2, n2 + 1)), this.toBBox), e3;
        r2 || (r2 = Math.ceil(Math.log(h2) / Math.log(o2)), o2 = Math.ceil(h2 / Math.pow(o2, r2 - 1))), (e3 = p([])).leaf = false, e3.height = r2;
        var s2 = Math.ceil(h2 / o2), l2 = s2 * Math.ceil(Math.sqrt(o2));
        d(t2, i2, n2, l2, this.compareMinX);
        for (var f2 = i2; f2 <= n2; f2 += l2) {
          var u2 = Math.min(f2 + l2 - 1, n2);
          d(t2, f2, u2, s2, this.compareMinY);
          for (var m2 = f2; m2 <= u2; m2 += s2) {
            var c2 = Math.min(m2 + s2 - 1, u2);
            e3.children.push(this._build(t2, m2, c2, r2 - 1));
          }
        }
        return a(e3, this.toBBox), e3;
      }, r.prototype._chooseSubtree = function(t2, i2, n2, r2) {
        for (; r2.push(i2), !i2.leaf && r2.length - 1 !== n2; ) {
          for (var e3 = 1 / 0, a2 = 1 / 0, h2 = void 0, o2 = 0; o2 < i2.children.length; o2++) {
            var s2 = i2.children[o2], l2 = f(s2), u2 = (m2 = t2, c2 = s2, (Math.max(c2.maxX, m2.maxX) - Math.min(c2.minX, m2.minX)) * (Math.max(c2.maxY, m2.maxY) - Math.min(c2.minY, m2.minY)) - l2);
            u2 < a2 ? (a2 = u2, e3 = l2 < e3 ? l2 : e3, h2 = s2) : u2 === a2 && l2 < e3 && (e3 = l2, h2 = s2);
          }
          i2 = h2 || i2.children[0];
        }
        var m2, c2;
        return i2;
      }, r.prototype._insert = function(t2, i2, n2) {
        var r2 = n2 ? t2 : this.toBBox(t2), e3 = [], a2 = this._chooseSubtree(r2, this.data, i2, e3);
        for (a2.children.push(t2), o(a2, r2); i2 >= 0 && e3[i2].children.length > this._maxEntries; ) this._split(e3, i2), i2--;
        this._adjustParentBBoxes(r2, e3, i2);
      }, r.prototype._split = function(t2, i2) {
        var n2 = t2[i2], r2 = n2.children.length, e3 = this._minEntries;
        this._chooseSplitAxis(n2, e3, r2);
        var h2 = this._chooseSplitIndex(n2, e3, r2), o2 = p(n2.children.splice(h2, n2.children.length - h2));
        o2.height = n2.height, o2.leaf = n2.leaf, a(n2, this.toBBox), a(o2, this.toBBox), i2 ? t2[i2 - 1].children.push(o2) : this._splitRoot(n2, o2);
      }, r.prototype._splitRoot = function(t2, i2) {
        this.data = p([t2, i2]), this.data.height = t2.height + 1, this.data.leaf = false, a(this.data, this.toBBox);
      }, r.prototype._chooseSplitIndex = function(t2, i2, n2) {
        for (var r2, e3, a2, o2, s2, l2, u2, m2 = 1 / 0, c2 = 1 / 0, p2 = i2; p2 <= n2 - i2; p2++) {
          var d2 = h(t2, 0, p2, this.toBBox), x = h(t2, p2, n2, this.toBBox), v = (e3 = d2, a2 = x, o2 = void 0, s2 = void 0, l2 = void 0, u2 = void 0, o2 = Math.max(e3.minX, a2.minX), s2 = Math.max(e3.minY, a2.minY), l2 = Math.min(e3.maxX, a2.maxX), u2 = Math.min(e3.maxY, a2.maxY), Math.max(0, l2 - o2) * Math.max(0, u2 - s2)), M = f(d2) + f(x);
          v < m2 ? (m2 = v, r2 = p2, c2 = M < c2 ? M : c2) : v === m2 && M < c2 && (c2 = M, r2 = p2);
        }
        return r2 || n2 - i2;
      }, r.prototype._chooseSplitAxis = function(t2, i2, n2) {
        var r2 = t2.leaf ? this.compareMinX : s, e3 = t2.leaf ? this.compareMinY : l;
        this._allDistMargin(t2, i2, n2, r2) < this._allDistMargin(t2, i2, n2, e3) && t2.children.sort(r2);
      }, r.prototype._allDistMargin = function(t2, i2, n2, r2) {
        t2.children.sort(r2);
        for (var e3 = this.toBBox, a2 = h(t2, 0, i2, e3), s2 = h(t2, n2 - i2, n2, e3), l2 = u(a2) + u(s2), f2 = i2; f2 < n2 - i2; f2++) {
          var m2 = t2.children[f2];
          o(a2, t2.leaf ? e3(m2) : m2), l2 += u(a2);
        }
        for (var c2 = n2 - i2 - 1; c2 >= i2; c2--) {
          var p2 = t2.children[c2];
          o(s2, t2.leaf ? e3(p2) : p2), l2 += u(s2);
        }
        return l2;
      }, r.prototype._adjustParentBBoxes = function(t2, i2, n2) {
        for (var r2 = n2; r2 >= 0; r2--) o(i2[r2], t2);
      }, r.prototype._condense = function(t2) {
        for (var i2 = t2.length - 1, n2 = void 0; i2 >= 0; i2--) 0 === t2[i2].children.length ? i2 > 0 ? (n2 = t2[i2 - 1].children).splice(n2.indexOf(t2[i2]), 1) : this.clear() : a(t2[i2], this.toBBox);
      }, r;
    });
  }
});

// worker/node_modules/@turf/center/dist/esm/index.js
function center(geojson, options = {}) {
  const ext = bbox(geojson);
  const x = (ext[0] + ext[2]) / 2;
  const y = (ext[1] + ext[3]) / 2;
  return point([x, y], options.properties, options);
}
var init_esm7 = __esm({
  "worker/node_modules/@turf/center/dist/esm/index.js"() {
    init_esm6();
    init_esm();
    __name(center, "center");
  }
});

// worker/node_modules/@turf/jsts/dist/jsts.min.js
var require_jsts_min = __commonJS({
  "worker/node_modules/@turf/jsts/dist/jsts.min.js"(exports, module) {
    !(function(t, e) {
      "object" == typeof exports && "undefined" != typeof module ? module.exports = e() : "function" == typeof define && define.amd ? define(e) : (t = "undefined" != typeof globalThis ? globalThis : t || self).jsts = e();
    })(exports, (function() {
      "use strict";
      function t(t2, e3) {
        (null == e3 || e3 > t2.length) && (e3 = t2.length);
        for (var n2 = 0, i2 = Array(e3); n2 < e3; n2++) i2[n2] = t2[n2];
        return i2;
      }
      __name(t, "t");
      function e(t2, e3, n2) {
        return e3 = u(e3), (function(t3, e4) {
          if (e4 && ("object" == typeof e4 || "function" == typeof e4)) return e4;
          if (void 0 !== e4) throw new TypeError("Derived constructors may only return object or undefined");
          return (function(t4) {
            if (void 0 === t4) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t4;
          })(t3);
        })(t2, h() ? Reflect.construct(e3, n2 || [], u(t2).constructor) : e3.apply(t2, n2));
      }
      __name(e, "e");
      function n(t2, e3) {
        if (!(t2 instanceof e3)) throw new TypeError("Cannot call a class as a function");
      }
      __name(n, "n");
      function i(t2, e3, n2) {
        if (h()) return Reflect.construct.apply(null, arguments);
        var i2 = [null];
        i2.push.apply(i2, e3);
        var r2 = new (t2.bind.apply(t2, i2))();
        return n2 && c(r2, n2.prototype), r2;
      }
      __name(i, "i");
      function r(t2, e3) {
        for (var n2 = 0; n2 < e3.length; n2++) {
          var i2 = e3[n2];
          i2.enumerable = i2.enumerable || false, i2.configurable = true, "value" in i2 && (i2.writable = true), Object.defineProperty(t2, v(i2.key), i2);
        }
      }
      __name(r, "r");
      function s(t2, e3, n2) {
        return e3 && r(t2.prototype, e3), n2 && r(t2, n2), Object.defineProperty(t2, "prototype", { writable: false }), t2;
      }
      __name(s, "s");
      function a(t2, e3) {
        var n2 = "undefined" != typeof Symbol && t2[Symbol.iterator] || t2["@@iterator"];
        if (!n2) {
          if (Array.isArray(t2) || (n2 = y(t2)) || e3) {
            n2 && (t2 = n2);
            var i2 = 0, r2 = /* @__PURE__ */ __name(function() {
            }, "r");
            return { s: r2, n: /* @__PURE__ */ __name(function() {
              return i2 >= t2.length ? { done: true } : { done: false, value: t2[i2++] };
            }, "n"), e: /* @__PURE__ */ __name(function(t3) {
              throw t3;
            }, "e"), f: r2 };
          }
          throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
        }
        var s2, a2 = true, o2 = false;
        return { s: /* @__PURE__ */ __name(function() {
          n2 = n2.call(t2);
        }, "s"), n: /* @__PURE__ */ __name(function() {
          var t3 = n2.next();
          return a2 = t3.done, t3;
        }, "n"), e: /* @__PURE__ */ __name(function(t3) {
          o2 = true, s2 = t3;
        }, "e"), f: /* @__PURE__ */ __name(function() {
          try {
            a2 || null == n2.return || n2.return();
          } finally {
            if (o2) throw s2;
          }
        }, "f") };
      }
      __name(a, "a");
      function o() {
        return o = "undefined" != typeof Reflect && Reflect.get ? Reflect.get.bind() : function(t2, e3, n2) {
          var i2 = (function(t3, e4) {
            for (; !{}.hasOwnProperty.call(t3, e4) && null !== (t3 = u(t3)); ) ;
            return t3;
          })(t2, e3);
          if (i2) {
            var r2 = Object.getOwnPropertyDescriptor(i2, e3);
            return r2.get ? r2.get.call(arguments.length < 3 ? t2 : n2) : r2.value;
          }
        }, o.apply(null, arguments);
      }
      __name(o, "o");
      function u(t2) {
        return u = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(t3) {
          return t3.__proto__ || Object.getPrototypeOf(t3);
        }, u(t2);
      }
      __name(u, "u");
      function l(t2, e3) {
        if ("function" != typeof e3 && null !== e3) throw new TypeError("Super expression must either be null or a function");
        t2.prototype = Object.create(e3 && e3.prototype, { constructor: { value: t2, writable: true, configurable: true } }), Object.defineProperty(t2, "prototype", { writable: false }), e3 && c(t2, e3);
      }
      __name(l, "l");
      function h() {
        try {
          var t2 = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], (function() {
          })));
        } catch (t3) {
        }
        return (h = /* @__PURE__ */ __name(function() {
          return !!t2;
        }, "h"))();
      }
      __name(h, "h");
      function c(t2, e3) {
        return c = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(t3, e4) {
          return t3.__proto__ = e4, t3;
        }, c(t2, e3);
      }
      __name(c, "c");
      function f(t2, e3, n2, i2) {
        var r2 = o(u(1 & i2 ? t2.prototype : t2), e3, n2);
        return 2 & i2 && "function" == typeof r2 ? function(t3) {
          return r2.apply(n2, t3);
        } : r2;
      }
      __name(f, "f");
      function g(e3) {
        return (function(e4) {
          if (Array.isArray(e4)) return t(e4);
        })(e3) || (function(t2) {
          if ("undefined" != typeof Symbol && null != t2[Symbol.iterator] || null != t2["@@iterator"]) return Array.from(t2);
        })(e3) || y(e3) || (function() {
          throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
        })();
      }
      __name(g, "g");
      function v(t2) {
        var e3 = (function(t3, e4) {
          if ("object" != typeof t3 || !t3) return t3;
          var n2 = t3[Symbol.toPrimitive];
          if (void 0 !== n2) {
            var i2 = n2.call(t3, e4);
            if ("object" != typeof i2) return i2;
            throw new TypeError("@@toPrimitive must return a primitive value.");
          }
          return String(t3);
        })(t2, "string");
        return "symbol" == typeof e3 ? e3 : e3 + "";
      }
      __name(v, "v");
      function y(e3, n2) {
        if (e3) {
          if ("string" == typeof e3) return t(e3, n2);
          var i2 = {}.toString.call(e3).slice(8, -1);
          return "Object" === i2 && e3.constructor && (i2 = e3.constructor.name), "Map" === i2 || "Set" === i2 ? Array.from(e3) : "Arguments" === i2 || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(i2) ? t(e3, n2) : void 0;
        }
      }
      __name(y, "y");
      function d(t2) {
        var e3 = "function" == typeof Map ? /* @__PURE__ */ new Map() : void 0;
        return d = /* @__PURE__ */ __name(function(t3) {
          if (null === t3 || !(function(t4) {
            try {
              return -1 !== Function.toString.call(t4).indexOf("[native code]");
            } catch (e4) {
              return "function" == typeof t4;
            }
          })(t3)) return t3;
          if ("function" != typeof t3) throw new TypeError("Super expression must either be null or a function");
          if (void 0 !== e3) {
            if (e3.has(t3)) return e3.get(t3);
            e3.set(t3, n2);
          }
          function n2() {
            return i(t3, arguments, u(this).constructor);
          }
          __name(n2, "n");
          return n2.prototype = Object.create(t3.prototype, { constructor: { value: n2, enumerable: false, writable: true, configurable: true } }), c(n2, t3);
        }, "d"), d(t2);
      }
      __name(d, "d");
      var _ = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "getEndCapStyle", value: /* @__PURE__ */ __name(function() {
          return this._endCapStyle;
        }, "value") }, { key: "isSingleSided", value: /* @__PURE__ */ __name(function() {
          return this._isSingleSided;
        }, "value") }, { key: "setQuadrantSegments", value: /* @__PURE__ */ __name(function(e3) {
          this._quadrantSegments = e3, 0 === this._quadrantSegments && (this._joinStyle = t2.JOIN_BEVEL), this._quadrantSegments < 0 && (this._joinStyle = t2.JOIN_MITRE, this._mitreLimit = Math.abs(this._quadrantSegments)), e3 <= 0 && (this._quadrantSegments = 1), this._joinStyle !== t2.JOIN_ROUND && (this._quadrantSegments = t2.DEFAULT_QUADRANT_SEGMENTS);
        }, "value") }, { key: "getJoinStyle", value: /* @__PURE__ */ __name(function() {
          return this._joinStyle;
        }, "value") }, { key: "setJoinStyle", value: /* @__PURE__ */ __name(function(t3) {
          this._joinStyle = t3;
        }, "value") }, { key: "setSimplifyFactor", value: /* @__PURE__ */ __name(function(t3) {
          this._simplifyFactor = t3 < 0 ? 0 : t3;
        }, "value") }, { key: "getSimplifyFactor", value: /* @__PURE__ */ __name(function() {
          return this._simplifyFactor;
        }, "value") }, { key: "getQuadrantSegments", value: /* @__PURE__ */ __name(function() {
          return this._quadrantSegments;
        }, "value") }, { key: "setEndCapStyle", value: /* @__PURE__ */ __name(function(t3) {
          this._endCapStyle = t3;
        }, "value") }, { key: "getMitreLimit", value: /* @__PURE__ */ __name(function() {
          return this._mitreLimit;
        }, "value") }, { key: "setMitreLimit", value: /* @__PURE__ */ __name(function(t3) {
          this._mitreLimit = t3;
        }, "value") }, { key: "setSingleSided", value: /* @__PURE__ */ __name(function(t3) {
          this._isSingleSided = t3;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._quadrantSegments = t2.DEFAULT_QUADRANT_SEGMENTS, this._endCapStyle = t2.CAP_ROUND, this._joinStyle = t2.JOIN_ROUND, this._mitreLimit = t2.DEFAULT_MITRE_LIMIT, this._isSingleSided = false, this._simplifyFactor = t2.DEFAULT_SIMPLIFY_FACTOR, 0 === arguments.length) ;
          else if (1 === arguments.length) {
            var e3 = arguments[0];
            this.setQuadrantSegments(e3);
          } else if (2 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1];
            this.setQuadrantSegments(n2), this.setEndCapStyle(i2);
          } else if (4 === arguments.length) {
            var r2 = arguments[0], s2 = arguments[1], a2 = arguments[2], o2 = arguments[3];
            this.setQuadrantSegments(r2), this.setEndCapStyle(s2), this.setJoinStyle(a2), this.setMitreLimit(o2);
          }
        }, "value") }, { key: "bufferDistanceError", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = Math.PI / 2 / t3;
          return 1 - Math.cos(e3 / 2);
        }, "value") }]);
      })();
      _.CAP_ROUND = 1, _.CAP_FLAT = 2, _.CAP_SQUARE = 3, _.JOIN_ROUND = 1, _.JOIN_MITRE = 2, _.JOIN_BEVEL = 3, _.DEFAULT_QUADRANT_SEGMENTS = 8, _.DEFAULT_MITRE_LIMIT = 5, _.DEFAULT_SIMPLIFY_FACTOR = 0.01;
      var p = (function(t2) {
        function i2(t3) {
          var r2;
          return n(this, i2), (r2 = e(this, i2, [t3])).name = Object.keys({ Exception: i2 })[0], r2;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "toString", value: /* @__PURE__ */ __name(function() {
          return this.message;
        }, "value") }]);
      })(d(Error)), m = (function(t2) {
        function i2(t3) {
          var r2;
          return n(this, i2), (r2 = e(this, i2, [t3])).name = Object.keys({ IllegalArgumentException: i2 })[0], r2;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2);
      })(p), k = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "filter", value: /* @__PURE__ */ __name(function(t2) {
        }, "value") }]);
      })();
      function x() {
      }
      __name(x, "x");
      function I() {
      }
      __name(I, "I");
      function E() {
      }
      __name(E, "E");
      var N, T, S, L, C, R, w, O, b = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), null, [{ key: "equalsWithTolerance", value: /* @__PURE__ */ __name(function(t2, e3, n2) {
          return Math.abs(t2 - e3) <= n2;
        }, "value") }]);
      })(), M = (function() {
        return s(/* @__PURE__ */ __name((function t2(e3, i2) {
          n(this, t2), this.low = i2 || 0, this.high = e3 || 0;
        }), "t"), null, [{ key: "toBinaryString", value: /* @__PURE__ */ __name(function(t2) {
          var e3, n2 = "";
          for (e3 = 2147483648; e3 > 0; e3 >>>= 1) n2 += (t2.high & e3) === e3 ? "1" : "0";
          for (e3 = 2147483648; e3 > 0; e3 >>>= 1) n2 += (t2.low & e3) === e3 ? "1" : "0";
          return n2;
        }, "value") }]);
      })();
      function A() {
      }
      __name(A, "A");
      function P() {
      }
      __name(P, "P");
      A.NaN = NaN, A.isNaN = function(t2) {
        return Number.isNaN(t2);
      }, A.isInfinite = function(t2) {
        return !Number.isFinite(t2);
      }, A.MAX_VALUE = Number.MAX_VALUE, A.POSITIVE_INFINITY = Number.POSITIVE_INFINITY, A.NEGATIVE_INFINITY = Number.NEGATIVE_INFINITY, "function" == typeof Float64Array && "function" == typeof Int32Array ? (R = 2146435072, w = new Float64Array(1), O = new Int32Array(w.buffer), A.doubleToLongBits = function(t2) {
        w[0] = t2;
        var e3 = 0 | O[0], n2 = 0 | O[1];
        return (n2 & R) === R && 1048575 & n2 && 0 !== e3 && (e3 = 0, n2 = 2146959360), new M(n2, e3);
      }, A.longBitsToDouble = function(t2) {
        return O[0] = t2.low, O[1] = t2.high, w[0];
      }) : (N = 1023, T = Math.log2, S = Math.floor, L = Math.pow, C = (function() {
        for (var t2 = 53; t2 > 0; t2--) {
          var e3 = L(2, t2) - 1;
          if (S(T(e3)) + 1 === t2) return e3;
        }
        return 0;
      })(), A.doubleToLongBits = function(t2) {
        var e3, n2, i2, r2, s2, a2, o2, u2, l2;
        if (t2 < 0 || 1 / t2 === Number.NEGATIVE_INFINITY ? (a2 = 1 << 31, t2 = -t2) : a2 = 0, 0 === t2) return new M(u2 = a2, l2 = 0);
        if (t2 === 1 / 0) return new M(u2 = 2146435072 | a2, l2 = 0);
        if (t2 != t2) return new M(u2 = 2146959360, l2 = 0);
        if (r2 = 0, l2 = 0, (e3 = S(t2)) > 1) if (e3 <= C) (r2 = S(T(e3))) <= 20 ? (l2 = 0, u2 = e3 << 20 - r2 & 1048575) : (l2 = e3 % (n2 = L(2, i2 = r2 - 20)) << 32 - i2, u2 = e3 / n2 & 1048575);
        else for (i2 = e3, l2 = 0; 0 !== (i2 = S(n2 = i2 / 2)); ) r2++, l2 >>>= 1, l2 |= (1 & u2) << 31, u2 >>>= 1, n2 !== i2 && (u2 |= 524288);
        if (o2 = r2 + N, s2 = 0 === e3, e3 = t2 - e3, r2 < 52 && 0 !== e3) for (i2 = 0; ; ) {
          if ((n2 = 2 * e3) >= 1 ? (e3 = n2 - 1, s2 ? (o2--, s2 = false) : (i2 <<= 1, i2 |= 1, r2++)) : (e3 = n2, s2 ? 0 == --o2 && (r2++, s2 = false) : (i2 <<= 1, r2++)), 20 === r2) u2 |= i2, i2 = 0;
          else if (52 === r2) {
            l2 |= i2;
            break;
          }
          if (1 === n2) {
            r2 < 20 ? u2 |= i2 << 20 - r2 : r2 < 52 && (l2 |= i2 << 52 - r2);
            break;
          }
        }
        return u2 |= o2 << 20, new M(u2 |= a2, l2);
      }, A.longBitsToDouble = function(t2) {
        var e3, n2, i2, r2, s2 = t2.high, a2 = t2.low, o2 = s2 & 1 << 31 ? -1 : 1;
        for (i2 = ((2146435072 & s2) >> 20) - N, r2 = 0, n2 = 1 << 19, e3 = 1; e3 <= 20; e3++) s2 & n2 && (r2 += L(2, -e3)), n2 >>>= 1;
        for (n2 = 1 << 31, e3 = 21; e3 <= 52; e3++) a2 & n2 && (r2 += L(2, -e3)), n2 >>>= 1;
        if (-1023 === i2) {
          if (0 === r2) return 0 * o2;
          i2 = -1022;
        } else {
          if (1024 === i2) return 0 === r2 ? o2 / 0 : NaN;
          r2 += 1;
        }
        return o2 * r2 * L(2, i2);
      });
      var D = (function(t2) {
        function i2(t3) {
          var r2;
          return n(this, i2), (r2 = e(this, i2, [t3])).name = Object.keys({ RuntimeException: i2 })[0], r2;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2);
      })(p), F = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, null, [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) D.constructor_.call(this);
          else if (1 === arguments.length) {
            var t3 = arguments[0];
            D.constructor_.call(this, t3);
          }
        }, "value") }]);
      })(D), G = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "shouldNeverReachHere", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) t2.shouldNeverReachHere(null);
          else if (1 === arguments.length) {
            var e3 = arguments[0];
            throw new F("Should never reach here" + (null !== e3 ? ": " + e3 : ""));
          }
        }, "value") }, { key: "isTrue", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var e3 = arguments[0];
            t2.isTrue(e3, null);
          } else if (2 === arguments.length) {
            var n2 = arguments[1];
            if (!arguments[0]) throw null === n2 ? new F() : new F(n2);
          }
        }, "value") }, { key: "equals", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length) {
            var e3 = arguments[0], n2 = arguments[1];
            t2.equals(e3, n2, null);
          } else if (3 === arguments.length) {
            var i2 = arguments[0], r2 = arguments[1], s2 = arguments[2];
            if (!r2.equals(i2)) throw new F("Expected " + i2 + " but encountered " + r2 + (null !== s2 ? ": " + s2 : ""));
          }
        }, "value") }]);
      })(), q = new ArrayBuffer(8), Y = new Float64Array(q), z2 = new Int32Array(q), X = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "getM", value: /* @__PURE__ */ __name(function() {
          return A.NaN;
        }, "value") }, { key: "setOrdinate", value: /* @__PURE__ */ __name(function(e3, n2) {
          switch (e3) {
            case t2.X:
              this.x = n2;
              break;
            case t2.Y:
              this.y = n2;
              break;
            case t2.Z:
              this.setZ(n2);
              break;
            default:
              throw new m("Invalid ordinate index: " + e3);
          }
        }, "value") }, { key: "equals2D", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return this.x === t3.x && this.y === t3.y;
          }
          if (2 === arguments.length) {
            var e3 = arguments[0], n2 = arguments[1];
            return !!b.equalsWithTolerance(this.x, e3.x, n2) && !!b.equalsWithTolerance(this.y, e3.y, n2);
          }
        }, "value") }, { key: "setM", value: /* @__PURE__ */ __name(function(e3) {
          throw new m("Invalid ordinate index: " + t2.M);
        }, "value") }, { key: "getZ", value: /* @__PURE__ */ __name(function() {
          return this.z;
        }, "value") }, { key: "getOrdinate", value: /* @__PURE__ */ __name(function(e3) {
          switch (e3) {
            case t2.X:
              return this.x;
            case t2.Y:
              return this.y;
            case t2.Z:
              return this.getZ();
          }
          throw new m("Invalid ordinate index: " + e3);
        }, "value") }, { key: "equals3D", value: /* @__PURE__ */ __name(function(t3) {
          return this.x === t3.x && this.y === t3.y && (this.getZ() === t3.getZ() || A.isNaN(this.getZ()) && A.isNaN(t3.getZ()));
        }, "value") }, { key: "equals", value: /* @__PURE__ */ __name(function(e3) {
          return e3 instanceof t2 && this.equals2D(e3);
        }, "value") }, { key: "equalInZ", value: /* @__PURE__ */ __name(function(t3, e3) {
          return b.equalsWithTolerance(this.getZ(), t3.getZ(), e3);
        }, "value") }, { key: "setX", value: /* @__PURE__ */ __name(function(t3) {
          this.x = t3;
        }, "value") }, { key: "compareTo", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = t3;
          return this.x < e3.x ? -1 : this.x > e3.x ? 1 : this.y < e3.y ? -1 : this.y > e3.y ? 1 : 0;
        }, "value") }, { key: "getX", value: /* @__PURE__ */ __name(function() {
          return this.x;
        }, "value") }, { key: "setZ", value: /* @__PURE__ */ __name(function(t3) {
          this.z = t3;
        }, "value") }, { key: "clone", value: /* @__PURE__ */ __name(function() {
          try {
            return null;
          } catch (t3) {
            if (t3 instanceof CloneNotSupportedException) return G.shouldNeverReachHere("this shouldn't happen because this class is Cloneable"), null;
            throw t3;
          }
        }, "value") }, { key: "copy", value: /* @__PURE__ */ __name(function() {
          return new t2(this);
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          return "(" + this.x + ", " + this.y + ", " + this.getZ() + ")";
        }, "value") }, { key: "distance3D", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = this.x - t3.x, n2 = this.y - t3.y, i2 = this.getZ() - t3.getZ();
          return Math.sqrt(e3 * e3 + n2 * n2 + i2 * i2);
        }, "value") }, { key: "getY", value: /* @__PURE__ */ __name(function() {
          return this.y;
        }, "value") }, { key: "setY", value: /* @__PURE__ */ __name(function(t3) {
          this.y = t3;
        }, "value") }, { key: "distance", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = this.x - t3.x, n2 = this.y - t3.y;
          return Math.sqrt(e3 * e3 + n2 * n2);
        }, "value") }, { key: "hashCode", value: /* @__PURE__ */ __name(function() {
          var e3 = 17;
          return e3 = 37 * (e3 = 37 * e3 + t2.hashCode(this.x)) + t2.hashCode(this.y);
        }, "value") }, { key: "setCoordinate", value: /* @__PURE__ */ __name(function(t3) {
          this.x = t3.x, this.y = t3.y, this.z = t3.getZ();
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [x, I, E];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this.x = null, this.y = null, this.z = null, 0 === arguments.length) t2.constructor_.call(this, 0, 0);
          else if (1 === arguments.length) {
            var e3 = arguments[0];
            t2.constructor_.call(this, e3.x, e3.y, e3.getZ());
          } else if (2 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1];
            t2.constructor_.call(this, n2, i2, t2.NULL_ORDINATE);
          } else if (3 === arguments.length) {
            var r2 = arguments[0], s2 = arguments[1], a2 = arguments[2];
            this.x = r2, this.y = s2, this.z = a2;
          }
        }, "value") }, { key: "hashCode", value: /* @__PURE__ */ __name(function(t3) {
          return Y[0] = t3, z2[0] ^ z2[1];
        }, "value") }]);
      })(), B = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "compare", value: /* @__PURE__ */ __name(function(e3, n2) {
          var i2 = t2.compare(e3.x, n2.x);
          if (0 !== i2) return i2;
          var r2 = t2.compare(e3.y, n2.y);
          return 0 !== r2 ? r2 : this._dimensionsToTest <= 2 ? 0 : t2.compare(e3.getZ(), n2.getZ());
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [P];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._dimensionsToTest = 2, 0 === arguments.length) t2.constructor_.call(this, 2);
          else if (1 === arguments.length) {
            var e3 = arguments[0];
            if (2 !== e3 && 3 !== e3) throw new m("only 2 or 3 dimensions may be specified");
            this._dimensionsToTest = e3;
          }
        }, "value") }, { key: "compare", value: /* @__PURE__ */ __name(function(t3, e3) {
          return t3 < e3 ? -1 : t3 > e3 ? 1 : A.isNaN(t3) ? A.isNaN(e3) ? 0 : -1 : A.isNaN(e3) ? 1 : 0;
        }, "value") }]);
      })();
      X.DimensionalComparator = B, X.NULL_ORDINATE = A.NaN, X.X = 0, X.Y = 1, X.Z = 2, X.M = 3;
      var U = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "getArea", value: /* @__PURE__ */ __name(function() {
          return this.getWidth() * this.getHeight();
        }, "value") }, { key: "equals", value: /* @__PURE__ */ __name(function(e3) {
          if (!(e3 instanceof t2)) return false;
          var n2 = e3;
          return this.isNull() ? n2.isNull() : this._maxx === n2.getMaxX() && this._maxy === n2.getMaxY() && this._minx === n2.getMinX() && this._miny === n2.getMinY();
        }, "value") }, { key: "intersection", value: /* @__PURE__ */ __name(function(e3) {
          if (this.isNull() || e3.isNull() || !this.intersects(e3)) return new t2();
          var n2 = this._minx > e3._minx ? this._minx : e3._minx, i2 = this._miny > e3._miny ? this._miny : e3._miny;
          return new t2(n2, this._maxx < e3._maxx ? this._maxx : e3._maxx, i2, this._maxy < e3._maxy ? this._maxy : e3._maxy);
        }, "value") }, { key: "isNull", value: /* @__PURE__ */ __name(function() {
          return this._maxx < this._minx;
        }, "value") }, { key: "getMaxX", value: /* @__PURE__ */ __name(function() {
          return this._maxx;
        }, "value") }, { key: "covers", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            if (arguments[0] instanceof X) {
              var e3 = arguments[0];
              return this.covers(e3.x, e3.y);
            }
            if (arguments[0] instanceof t2) {
              var n2 = arguments[0];
              return !this.isNull() && !n2.isNull() && (n2.getMinX() >= this._minx && n2.getMaxX() <= this._maxx && n2.getMinY() >= this._miny && n2.getMaxY() <= this._maxy);
            }
          } else if (2 === arguments.length) {
            var i2 = arguments[0], r2 = arguments[1];
            return !this.isNull() && (i2 >= this._minx && i2 <= this._maxx && r2 >= this._miny && r2 <= this._maxy);
          }
        }, "value") }, { key: "intersects", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            if (arguments[0] instanceof t2) {
              var e3 = arguments[0];
              return !this.isNull() && !e3.isNull() && !(e3._minx > this._maxx || e3._maxx < this._minx || e3._miny > this._maxy || e3._maxy < this._miny);
            }
            if (arguments[0] instanceof X) {
              var n2 = arguments[0];
              return this.intersects(n2.x, n2.y);
            }
          } else if (2 === arguments.length) {
            if (arguments[0] instanceof X && arguments[1] instanceof X) {
              var i2 = arguments[0], r2 = arguments[1];
              return !this.isNull() && (!((i2.x < r2.x ? i2.x : r2.x) > this._maxx) && (!((i2.x > r2.x ? i2.x : r2.x) < this._minx) && (!((i2.y < r2.y ? i2.y : r2.y) > this._maxy) && !((i2.y > r2.y ? i2.y : r2.y) < this._miny))));
            }
            if ("number" == typeof arguments[0] && "number" == typeof arguments[1]) {
              var s2 = arguments[0], a2 = arguments[1];
              return !this.isNull() && !(s2 > this._maxx || s2 < this._minx || a2 > this._maxy || a2 < this._miny);
            }
          }
        }, "value") }, { key: "getMinY", value: /* @__PURE__ */ __name(function() {
          return this._miny;
        }, "value") }, { key: "getDiameter", value: /* @__PURE__ */ __name(function() {
          if (this.isNull()) return 0;
          var t3 = this.getWidth(), e3 = this.getHeight();
          return Math.sqrt(t3 * t3 + e3 * e3);
        }, "value") }, { key: "getMinX", value: /* @__PURE__ */ __name(function() {
          return this._minx;
        }, "value") }, { key: "expandToInclude", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            if (arguments[0] instanceof X) {
              var e3 = arguments[0];
              this.expandToInclude(e3.x, e3.y);
            } else if (arguments[0] instanceof t2) {
              var n2 = arguments[0];
              if (n2.isNull()) return null;
              this.isNull() ? (this._minx = n2.getMinX(), this._maxx = n2.getMaxX(), this._miny = n2.getMinY(), this._maxy = n2.getMaxY()) : (n2._minx < this._minx && (this._minx = n2._minx), n2._maxx > this._maxx && (this._maxx = n2._maxx), n2._miny < this._miny && (this._miny = n2._miny), n2._maxy > this._maxy && (this._maxy = n2._maxy));
            }
          } else if (2 === arguments.length) {
            var i2 = arguments[0], r2 = arguments[1];
            this.isNull() ? (this._minx = i2, this._maxx = i2, this._miny = r2, this._maxy = r2) : (i2 < this._minx && (this._minx = i2), i2 > this._maxx && (this._maxx = i2), r2 < this._miny && (this._miny = r2), r2 > this._maxy && (this._maxy = r2));
          }
        }, "value") }, { key: "minExtent", value: /* @__PURE__ */ __name(function() {
          if (this.isNull()) return 0;
          var t3 = this.getWidth(), e3 = this.getHeight();
          return t3 < e3 ? t3 : e3;
        }, "value") }, { key: "getWidth", value: /* @__PURE__ */ __name(function() {
          return this.isNull() ? 0 : this._maxx - this._minx;
        }, "value") }, { key: "compareTo", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = t3;
          return this.isNull() ? e3.isNull() ? 0 : -1 : e3.isNull() ? 1 : this._minx < e3._minx ? -1 : this._minx > e3._minx ? 1 : this._miny < e3._miny ? -1 : this._miny > e3._miny ? 1 : this._maxx < e3._maxx ? -1 : this._maxx > e3._maxx ? 1 : this._maxy < e3._maxy ? -1 : this._maxy > e3._maxy ? 1 : 0;
        }, "value") }, { key: "translate", value: /* @__PURE__ */ __name(function(t3, e3) {
          if (this.isNull()) return null;
          this.init(this.getMinX() + t3, this.getMaxX() + t3, this.getMinY() + e3, this.getMaxY() + e3);
        }, "value") }, { key: "copy", value: /* @__PURE__ */ __name(function() {
          return new t2(this);
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          return "Env[" + this._minx + " : " + this._maxx + ", " + this._miny + " : " + this._maxy + "]";
        }, "value") }, { key: "setToNull", value: /* @__PURE__ */ __name(function() {
          this._minx = 0, this._maxx = -1, this._miny = 0, this._maxy = -1;
        }, "value") }, { key: "disjoint", value: /* @__PURE__ */ __name(function(t3) {
          return !(!this.isNull() && !t3.isNull()) || (t3._minx > this._maxx || t3._maxx < this._minx || t3._miny > this._maxy || t3._maxy < this._miny);
        }, "value") }, { key: "getHeight", value: /* @__PURE__ */ __name(function() {
          return this.isNull() ? 0 : this._maxy - this._miny;
        }, "value") }, { key: "maxExtent", value: /* @__PURE__ */ __name(function() {
          if (this.isNull()) return 0;
          var t3 = this.getWidth(), e3 = this.getHeight();
          return t3 > e3 ? t3 : e3;
        }, "value") }, { key: "expandBy", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            this.expandBy(t3, t3);
          } else if (2 === arguments.length) {
            var e3 = arguments[0], n2 = arguments[1];
            if (this.isNull()) return null;
            this._minx -= e3, this._maxx += e3, this._miny -= n2, this._maxy += n2, (this._minx > this._maxx || this._miny > this._maxy) && this.setToNull();
          }
        }, "value") }, { key: "contains", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            if (arguments[0] instanceof t2) {
              var e3 = arguments[0];
              return this.covers(e3);
            }
            if (arguments[0] instanceof X) {
              var n2 = arguments[0];
              return this.covers(n2);
            }
          } else if (2 === arguments.length) {
            var i2 = arguments[0], r2 = arguments[1];
            return this.covers(i2, r2);
          }
        }, "value") }, { key: "centre", value: /* @__PURE__ */ __name(function() {
          return this.isNull() ? null : new X((this.getMinX() + this.getMaxX()) / 2, (this.getMinY() + this.getMaxY()) / 2);
        }, "value") }, { key: "init", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) this.setToNull();
          else if (1 === arguments.length) {
            if (arguments[0] instanceof X) {
              var e3 = arguments[0];
              this.init(e3.x, e3.x, e3.y, e3.y);
            } else if (arguments[0] instanceof t2) {
              var n2 = arguments[0];
              this._minx = n2._minx, this._maxx = n2._maxx, this._miny = n2._miny, this._maxy = n2._maxy;
            }
          } else if (2 === arguments.length) {
            var i2 = arguments[0], r2 = arguments[1];
            this.init(i2.x, r2.x, i2.y, r2.y);
          } else if (4 === arguments.length) {
            var s2 = arguments[0], a2 = arguments[1], o2 = arguments[2], u2 = arguments[3];
            s2 < a2 ? (this._minx = s2, this._maxx = a2) : (this._minx = a2, this._maxx = s2), o2 < u2 ? (this._miny = o2, this._maxy = u2) : (this._miny = u2, this._maxy = o2);
          }
        }, "value") }, { key: "getMaxY", value: /* @__PURE__ */ __name(function() {
          return this._maxy;
        }, "value") }, { key: "distance", value: /* @__PURE__ */ __name(function(t3) {
          if (this.intersects(t3)) return 0;
          var e3 = 0;
          this._maxx < t3._minx ? e3 = t3._minx - this._maxx : this._minx > t3._maxx && (e3 = this._minx - t3._maxx);
          var n2 = 0;
          return this._maxy < t3._miny ? n2 = t3._miny - this._maxy : this._miny > t3._maxy && (n2 = this._miny - t3._maxy), 0 === e3 ? n2 : 0 === n2 ? e3 : Math.sqrt(e3 * e3 + n2 * n2);
        }, "value") }, { key: "hashCode", value: /* @__PURE__ */ __name(function() {
          var t3 = 17;
          return t3 = 37 * (t3 = 37 * (t3 = 37 * (t3 = 37 * t3 + X.hashCode(this._minx)) + X.hashCode(this._maxx)) + X.hashCode(this._miny)) + X.hashCode(this._maxy);
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [x, E];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._minx = null, this._maxx = null, this._miny = null, this._maxy = null, 0 === arguments.length) this.init();
          else if (1 === arguments.length) {
            if (arguments[0] instanceof X) {
              var e3 = arguments[0];
              this.init(e3.x, e3.x, e3.y, e3.y);
            } else if (arguments[0] instanceof t2) {
              var n2 = arguments[0];
              this.init(n2);
            }
          } else if (2 === arguments.length) {
            var i2 = arguments[0], r2 = arguments[1];
            this.init(i2.x, r2.x, i2.y, r2.y);
          } else if (4 === arguments.length) {
            var s2 = arguments[0], a2 = arguments[1], o2 = arguments[2], u2 = arguments[3];
            this.init(s2, a2, o2, u2);
          }
        }, "value") }, { key: "intersects", value: /* @__PURE__ */ __name(function() {
          if (3 === arguments.length) {
            var t3 = arguments[0], e3 = arguments[1], n2 = arguments[2];
            return n2.x >= (t3.x < e3.x ? t3.x : e3.x) && n2.x <= (t3.x > e3.x ? t3.x : e3.x) && n2.y >= (t3.y < e3.y ? t3.y : e3.y) && n2.y <= (t3.y > e3.y ? t3.y : e3.y);
          }
          if (4 === arguments.length) {
            var i2 = arguments[0], r2 = arguments[1], s2 = arguments[2], a2 = arguments[3], o2 = Math.min(s2.x, a2.x), u2 = Math.max(s2.x, a2.x), l2 = Math.min(i2.x, r2.x), h2 = Math.max(i2.x, r2.x);
            return !(l2 > u2) && (!(h2 < o2) && (o2 = Math.min(s2.y, a2.y), u2 = Math.max(s2.y, a2.y), l2 = Math.min(i2.y, r2.y), h2 = Math.max(i2.y, r2.y), !(l2 > u2) && !(h2 < o2)));
          }
        }, "value") }]);
      })(), V = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "isGeometryCollection", value: /* @__PURE__ */ __name(function() {
          return this.getTypeCode() === t2.TYPECODE_GEOMETRYCOLLECTION;
        }, "value") }, { key: "getFactory", value: /* @__PURE__ */ __name(function() {
          return this._factory;
        }, "value") }, { key: "getGeometryN", value: /* @__PURE__ */ __name(function(t3) {
          return this;
        }, "value") }, { key: "getArea", value: /* @__PURE__ */ __name(function() {
          return 0;
        }, "value") }, { key: "isRectangle", value: /* @__PURE__ */ __name(function() {
          return false;
        }, "value") }, { key: "equalsExact", value: /* @__PURE__ */ __name(function(t3) {
          return this === t3 || this.equalsExact(t3, 0);
        }, "value") }, { key: "geometryChanged", value: /* @__PURE__ */ __name(function() {
          this.apply(t2.geometryChangedFilter);
        }, "value") }, { key: "geometryChangedAction", value: /* @__PURE__ */ __name(function() {
          this._envelope = null;
        }, "value") }, { key: "equalsNorm", value: /* @__PURE__ */ __name(function(t3) {
          return null !== t3 && this.norm().equalsExact(t3.norm());
        }, "value") }, { key: "getLength", value: /* @__PURE__ */ __name(function() {
          return 0;
        }, "value") }, { key: "getNumGeometries", value: /* @__PURE__ */ __name(function() {
          return 1;
        }, "value") }, { key: "compareTo", value: /* @__PURE__ */ __name(function() {
          var t3;
          if (1 === arguments.length) {
            var e3 = arguments[0];
            return t3 = e3, this.getTypeCode() !== t3.getTypeCode() ? this.getTypeCode() - t3.getTypeCode() : this.isEmpty() && t3.isEmpty() ? 0 : this.isEmpty() ? -1 : t3.isEmpty() ? 1 : this.compareToSameClass(e3);
          }
          if (2 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1];
            return t3 = n2, this.getTypeCode() !== t3.getTypeCode() ? this.getTypeCode() - t3.getTypeCode() : this.isEmpty() && t3.isEmpty() ? 0 : this.isEmpty() ? -1 : t3.isEmpty() ? 1 : this.compareToSameClass(n2, i2);
          }
        }, "value") }, { key: "getUserData", value: /* @__PURE__ */ __name(function() {
          return this._userData;
        }, "value") }, { key: "getSRID", value: /* @__PURE__ */ __name(function() {
          return this._SRID;
        }, "value") }, { key: "getEnvelope", value: /* @__PURE__ */ __name(function() {
          return this.getFactory().toGeometry(this.getEnvelopeInternal());
        }, "value") }, { key: "checkNotGeometryCollection", value: /* @__PURE__ */ __name(function(e3) {
          if (e3.getTypeCode() === t2.TYPECODE_GEOMETRYCOLLECTION) throw new m("This method does not support GeometryCollection arguments");
        }, "value") }, { key: "equal", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          return 0 === n2 ? t3.equals(e3) : t3.distance(e3) <= n2;
        }, "value") }, { key: "norm", value: /* @__PURE__ */ __name(function() {
          var t3 = this.copy();
          return t3.normalize(), t3;
        }, "value") }, { key: "reverse", value: /* @__PURE__ */ __name(function() {
          var t3 = this.reverseInternal();
          return null != this.envelope && (t3.envelope = this.envelope.copy()), t3.setSRID(this.getSRID()), t3;
        }, "value") }, { key: "copy", value: /* @__PURE__ */ __name(function() {
          var t3 = this.copyInternal();
          return t3.envelope = null == this._envelope ? null : this._envelope.copy(), t3._SRID = this._SRID, t3._userData = this._userData, t3;
        }, "value") }, { key: "getPrecisionModel", value: /* @__PURE__ */ __name(function() {
          return this._factory.getPrecisionModel();
        }, "value") }, { key: "getEnvelopeInternal", value: /* @__PURE__ */ __name(function() {
          return null === this._envelope && (this._envelope = this.computeEnvelopeInternal()), new U(this._envelope);
        }, "value") }, { key: "setSRID", value: /* @__PURE__ */ __name(function(t3) {
          this._SRID = t3;
        }, "value") }, { key: "setUserData", value: /* @__PURE__ */ __name(function(t3) {
          this._userData = t3;
        }, "value") }, { key: "compare", value: /* @__PURE__ */ __name(function(t3, e3) {
          for (var n2 = t3.iterator(), i2 = e3.iterator(); n2.hasNext() && i2.hasNext(); ) {
            var r2 = n2.next(), s2 = i2.next(), a2 = r2.compareTo(s2);
            if (0 !== a2) return a2;
          }
          return n2.hasNext() ? 1 : i2.hasNext() ? -1 : 0;
        }, "value") }, { key: "hashCode", value: /* @__PURE__ */ __name(function() {
          return this.getEnvelopeInternal().hashCode();
        }, "value") }, { key: "isEquivalentClass", value: /* @__PURE__ */ __name(function(t3) {
          return this.getClass() === t3.getClass();
        }, "value") }, { key: "isGeometryCollectionOrDerived", value: /* @__PURE__ */ __name(function() {
          return this.getTypeCode() === t2.TYPECODE_GEOMETRYCOLLECTION || this.getTypeCode() === t2.TYPECODE_MULTIPOINT || this.getTypeCode() === t2.TYPECODE_MULTILINESTRING || this.getTypeCode() === t2.TYPECODE_MULTIPOLYGON;
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [I, x, E];
        }, "get") }, { key: "getClass", value: /* @__PURE__ */ __name(function() {
          return t2;
        }, "value") }], [{ key: "hasNonEmptyElements", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = 0; e3 < t3.length; e3++) if (!t3[e3].isEmpty()) return true;
          return false;
        }, "value") }, { key: "hasNullElements", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = 0; e3 < t3.length; e3++) if (null === t3[e3]) return true;
          return false;
        }, "value") }]);
      })();
      V.constructor_ = function(t2) {
        t2 && (this._envelope = null, this._userData = null, this._factory = t2, this._SRID = t2.getSRID());
      }, V.TYPECODE_POINT = 0, V.TYPECODE_MULTIPOINT = 1, V.TYPECODE_LINESTRING = 2, V.TYPECODE_LINEARRING = 3, V.TYPECODE_MULTILINESTRING = 4, V.TYPECODE_POLYGON = 5, V.TYPECODE_MULTIPOLYGON = 6, V.TYPECODE_GEOMETRYCOLLECTION = 7, V.TYPENAME_POINT = "Point", V.TYPENAME_MULTIPOINT = "MultiPoint", V.TYPENAME_LINESTRING = "LineString", V.TYPENAME_LINEARRING = "LinearRing", V.TYPENAME_MULTILINESTRING = "MultiLineString", V.TYPENAME_POLYGON = "Polygon", V.TYPENAME_MULTIPOLYGON = "MultiPolygon", V.TYPENAME_GEOMETRYCOLLECTION = "GeometryCollection", V.geometryChangedFilter = { get interfaces_() {
        return [k];
      }, filter: /* @__PURE__ */ __name(function(t2) {
        t2.geometryChangedAction();
      }, "filter") };
      var H = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "toLocationSymbol", value: /* @__PURE__ */ __name(function(e3) {
          switch (e3) {
            case t2.EXTERIOR:
              return "e";
            case t2.BOUNDARY:
              return "b";
            case t2.INTERIOR:
              return "i";
            case t2.NONE:
              return "-";
          }
          throw new m("Unknown location value: " + e3);
        }, "value") }]);
      })();
      H.INTERIOR = 0, H.BOUNDARY = 1, H.EXTERIOR = 2, H.NONE = -1;
      var Z = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "add", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "addAll", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "isEmpty", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "iterator", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "size", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "toArray", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "remove", value: /* @__PURE__ */ __name(function() {
        }, "value") }]);
      })(), j = (function(t2) {
        function i2(t3) {
          var r2;
          return n(this, i2), (r2 = e(this, i2, [t3])).name = Object.keys({ NoSuchElementException: i2 })[0], r2;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2);
      })(p), W = (function(t2) {
        function i2(t3) {
          var r2;
          return n(this, i2), (r2 = e(this, i2, [t3])).name = Object.keys({ UnsupportedOperationException: i2 })[0], r2;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2);
      })(p), K = (function(t2) {
        function i2() {
          return n(this, i2), e(this, i2, arguments);
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "contains", value: /* @__PURE__ */ __name(function() {
        }, "value") }]);
      })(Z), J = (function(t2) {
        function i2(t3) {
          var r2;
          return n(this, i2), (r2 = e(this, i2)).map = /* @__PURE__ */ new Map(), t3 instanceof Z && r2.addAll(t3), r2;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "contains", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = t3.hashCode ? t3.hashCode() : t3;
          return !!this.map.has(e3);
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = t3.hashCode ? t3.hashCode() : t3;
          return !this.map.has(e3) && !!this.map.set(e3, t3);
        }, "value") }, { key: "addAll", value: /* @__PURE__ */ __name(function(t3) {
          var e3, n2 = a(t3);
          try {
            for (n2.s(); !(e3 = n2.n()).done; ) {
              var i3 = e3.value;
              this.add(i3);
            }
          } catch (t4) {
            n2.e(t4);
          } finally {
            n2.f();
          }
          return true;
        }, "value") }, { key: "remove", value: /* @__PURE__ */ __name(function() {
          throw new W();
        }, "value") }, { key: "size", value: /* @__PURE__ */ __name(function() {
          return this.map.size;
        }, "value") }, { key: "isEmpty", value: /* @__PURE__ */ __name(function() {
          return 0 === this.map.size;
        }, "value") }, { key: "toArray", value: /* @__PURE__ */ __name(function() {
          return Array.from(this.map.values());
        }, "value") }, { key: "iterator", value: /* @__PURE__ */ __name(function() {
          return new Q(this.map);
        }, "value") }, { key: Symbol.iterator, value: /* @__PURE__ */ __name(function() {
          return this.map;
        }, "value") }]);
      })(K), Q = (function() {
        return s(/* @__PURE__ */ __name((function t2(e3) {
          n(this, t2), this.iterator = e3.values();
          var i2 = this.iterator.next(), r2 = i2.done, s2 = i2.value;
          this.done = r2, this.value = s2;
        }), "t"), [{ key: "next", value: /* @__PURE__ */ __name(function() {
          if (this.done) throw new j();
          var t2 = this.value, e3 = this.iterator.next(), n2 = e3.done, i2 = e3.value;
          return this.done = n2, this.value = i2, t2;
        }, "value") }, { key: "hasNext", value: /* @__PURE__ */ __name(function() {
          return !this.done;
        }, "value") }, { key: "remove", value: /* @__PURE__ */ __name(function() {
          throw new W();
        }, "value") }]);
      })(), $ = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "opposite", value: /* @__PURE__ */ __name(function(e3) {
          return e3 === t2.LEFT ? t2.RIGHT : e3 === t2.RIGHT ? t2.LEFT : e3;
        }, "value") }]);
      })();
      $.ON = 0, $.LEFT = 1, $.RIGHT = 2;
      var tt = (function(t2) {
        function i2(t3) {
          var r2;
          return n(this, i2), (r2 = e(this, i2, [t3])).name = Object.keys({ EmptyStackException: i2 })[0], r2;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2);
      })(p), et = (function(t2) {
        function i2(t3) {
          var r2;
          return n(this, i2), (r2 = e(this, i2, [t3])).name = Object.keys({ IndexOutOfBoundsException: i2 })[0], r2;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2);
      })(p), nt = (function(t2) {
        function i2() {
          return n(this, i2), e(this, i2, arguments);
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "get", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "set", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "isEmpty", value: /* @__PURE__ */ __name(function() {
        }, "value") }]);
      })(Z), it = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), (t3 = e(this, i2)).array = [], t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "add", value: /* @__PURE__ */ __name(function(t3) {
          return this.array.push(t3), true;
        }, "value") }, { key: "get", value: /* @__PURE__ */ __name(function(t3) {
          if (t3 < 0 || t3 >= this.size()) throw new et();
          return this.array[t3];
        }, "value") }, { key: "push", value: /* @__PURE__ */ __name(function(t3) {
          return this.array.push(t3), t3;
        }, "value") }, { key: "pop", value: /* @__PURE__ */ __name(function() {
          if (0 === this.array.length) throw new tt();
          return this.array.pop();
        }, "value") }, { key: "peek", value: /* @__PURE__ */ __name(function() {
          if (0 === this.array.length) throw new tt();
          return this.array[this.array.length - 1];
        }, "value") }, { key: "empty", value: /* @__PURE__ */ __name(function() {
          return 0 === this.array.length;
        }, "value") }, { key: "isEmpty", value: /* @__PURE__ */ __name(function() {
          return this.empty();
        }, "value") }, { key: "search", value: /* @__PURE__ */ __name(function(t3) {
          return this.array.indexOf(t3);
        }, "value") }, { key: "size", value: /* @__PURE__ */ __name(function() {
          return this.array.length;
        }, "value") }, { key: "toArray", value: /* @__PURE__ */ __name(function() {
          return this.array.slice();
        }, "value") }]);
      })(nt);
      function rt(t2, e3) {
        return t2.interfaces_ && t2.interfaces_.indexOf(e3) > -1;
      }
      __name(rt, "rt");
      var st = (function() {
        return s(/* @__PURE__ */ __name((function t2(e3) {
          n(this, t2), this.str = e3;
        }), "t"), [{ key: "append", value: /* @__PURE__ */ __name(function(t2) {
          this.str += t2;
        }, "value") }, { key: "setCharAt", value: /* @__PURE__ */ __name(function(t2, e3) {
          this.str = this.str.substr(0, t2) + e3 + this.str.substr(t2 + 1);
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          return this.str;
        }, "value") }]);
      })(), at = (function() {
        function t2(e3) {
          n(this, t2), this.value = e3;
        }
        __name(t2, "t");
        return s(t2, [{ key: "intValue", value: /* @__PURE__ */ __name(function() {
          return this.value;
        }, "value") }, { key: "compareTo", value: /* @__PURE__ */ __name(function(t3) {
          return this.value < t3 ? -1 : this.value > t3 ? 1 : 0;
        }, "value") }], [{ key: "compare", value: /* @__PURE__ */ __name(function(t3, e3) {
          return t3 < e3 ? -1 : t3 > e3 ? 1 : 0;
        }, "value") }, { key: "isNan", value: /* @__PURE__ */ __name(function(t3) {
          return Number.isNaN(t3);
        }, "value") }, { key: "valueOf", value: /* @__PURE__ */ __name(function(e3) {
          return new t2(e3);
        }, "value") }]);
      })(), ot = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), null, [{ key: "isWhitespace", value: /* @__PURE__ */ __name(function(t2) {
          return t2 <= 32 && t2 >= 0 || 127 === t2;
        }, "value") }, { key: "toUpperCase", value: /* @__PURE__ */ __name(function(t2) {
          return t2.toUpperCase();
        }, "value") }]);
      })(), ut = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "le", value: /* @__PURE__ */ __name(function(t3) {
          return this._hi < t3._hi || this._hi === t3._hi && this._lo <= t3._lo;
        }, "value") }, { key: "extractSignificantDigits", value: /* @__PURE__ */ __name(function(e3, n2) {
          var i2 = this.abs(), r2 = t2.magnitude(i2._hi), s2 = t2.TEN.pow(r2);
          (i2 = i2.divide(s2)).gt(t2.TEN) ? (i2 = i2.divide(t2.TEN), r2 += 1) : i2.lt(t2.ONE) && (i2 = i2.multiply(t2.TEN), r2 -= 1);
          for (var a2 = r2 + 1, o2 = new st(), u2 = t2.MAX_PRINT_DIGITS - 1, l2 = 0; l2 <= u2; l2++) {
            e3 && l2 === a2 && o2.append(".");
            var h2 = Math.trunc(i2._hi);
            if (h2 < 0) break;
            var c2 = false, f2 = 0;
            h2 > 9 ? (c2 = true, f2 = "9") : f2 = "0" + h2, o2.append(f2), i2 = i2.subtract(t2.valueOf(h2)).multiply(t2.TEN), c2 && i2.selfAdd(t2.TEN);
            var g2 = true, v2 = t2.magnitude(i2._hi);
            if (v2 < 0 && Math.abs(v2) >= u2 - l2 && (g2 = false), !g2) break;
          }
          return n2[0] = r2, o2.toString();
        }, "value") }, { key: "sqr", value: /* @__PURE__ */ __name(function() {
          return this.multiply(this);
        }, "value") }, { key: "doubleValue", value: /* @__PURE__ */ __name(function() {
          return this._hi + this._lo;
        }, "value") }, { key: "subtract", value: /* @__PURE__ */ __name(function() {
          if (arguments[0] instanceof t2) {
            var e3 = arguments[0];
            return this.add(e3.negate());
          }
          if ("number" == typeof arguments[0]) {
            var n2 = arguments[0];
            return this.add(-n2);
          }
        }, "value") }, { key: "equals", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length && arguments[0] instanceof t2) {
            var e3 = arguments[0];
            return this._hi === e3._hi && this._lo === e3._lo;
          }
        }, "value") }, { key: "isZero", value: /* @__PURE__ */ __name(function() {
          return 0 === this._hi && 0 === this._lo;
        }, "value") }, { key: "selfSubtract", value: /* @__PURE__ */ __name(function() {
          if (arguments[0] instanceof t2) {
            var e3 = arguments[0];
            return this.isNaN() ? this : this.selfAdd(-e3._hi, -e3._lo);
          }
          if ("number" == typeof arguments[0]) {
            var n2 = arguments[0];
            return this.isNaN() ? this : this.selfAdd(-n2, 0);
          }
        }, "value") }, { key: "getSpecialNumberString", value: /* @__PURE__ */ __name(function() {
          return this.isZero() ? "0.0" : this.isNaN() ? "NaN " : null;
        }, "value") }, { key: "min", value: /* @__PURE__ */ __name(function(t3) {
          return this.le(t3) ? this : t3;
        }, "value") }, { key: "selfDivide", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            if (arguments[0] instanceof t2) {
              var e3 = arguments[0];
              return this.selfDivide(e3._hi, e3._lo);
            }
            if ("number" == typeof arguments[0]) {
              var n2 = arguments[0];
              return this.selfDivide(n2, 0);
            }
          } else if (2 === arguments.length) {
            var i2, r2, s2, a2, o2 = arguments[0], u2 = arguments[1], l2 = null, h2 = null, c2 = null, f2 = null;
            return s2 = this._hi / o2, f2 = (l2 = (c2 = t2.SPLIT * s2) - (l2 = c2 - s2)) * (h2 = (f2 = t2.SPLIT * o2) - (h2 = f2 - o2)) - (a2 = s2 * o2) + l2 * (r2 = o2 - h2) + (i2 = s2 - l2) * h2 + i2 * r2, f2 = s2 + (c2 = (this._hi - a2 - f2 + this._lo - s2 * u2) / o2), this._hi = f2, this._lo = s2 - f2 + c2, this;
          }
        }, "value") }, { key: "dump", value: /* @__PURE__ */ __name(function() {
          return "DD<" + this._hi + ", " + this._lo + ">";
        }, "value") }, { key: "divide", value: /* @__PURE__ */ __name(function() {
          if (arguments[0] instanceof t2) {
            var e3, n2, i2, r2, s2 = arguments[0], a2 = null, o2 = null, u2 = null, l2 = null;
            return e3 = (i2 = this._hi / s2._hi) - (a2 = (u2 = t2.SPLIT * i2) - (a2 = u2 - i2)), l2 = a2 * (o2 = (l2 = t2.SPLIT * s2._hi) - (o2 = l2 - s2._hi)) - (r2 = i2 * s2._hi) + a2 * (n2 = s2._hi - o2) + e3 * o2 + e3 * n2, new t2(l2 = i2 + (u2 = (this._hi - r2 - l2 + this._lo - i2 * s2._lo) / s2._hi), i2 - l2 + u2);
          }
          if ("number" == typeof arguments[0]) {
            var h2 = arguments[0];
            return A.isNaN(h2) ? t2.createNaN() : t2.copy(this).selfDivide(h2, 0);
          }
        }, "value") }, { key: "ge", value: /* @__PURE__ */ __name(function(t3) {
          return this._hi > t3._hi || this._hi === t3._hi && this._lo >= t3._lo;
        }, "value") }, { key: "pow", value: /* @__PURE__ */ __name(function(e3) {
          if (0 === e3) return t2.valueOf(1);
          var n2 = new t2(this), i2 = t2.valueOf(1), r2 = Math.abs(e3);
          if (r2 > 1) for (; r2 > 0; ) r2 % 2 == 1 && i2.selfMultiply(n2), (r2 /= 2) > 0 && (n2 = n2.sqr());
          else i2 = n2;
          return e3 < 0 ? i2.reciprocal() : i2;
        }, "value") }, { key: "ceil", value: /* @__PURE__ */ __name(function() {
          if (this.isNaN()) return t2.NaN;
          var e3 = Math.ceil(this._hi), n2 = 0;
          return e3 === this._hi && (n2 = Math.ceil(this._lo)), new t2(e3, n2);
        }, "value") }, { key: "compareTo", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = t3;
          return this._hi < e3._hi ? -1 : this._hi > e3._hi ? 1 : this._lo < e3._lo ? -1 : this._lo > e3._lo ? 1 : 0;
        }, "value") }, { key: "rint", value: /* @__PURE__ */ __name(function() {
          return this.isNaN() ? this : this.add(0.5).floor();
        }, "value") }, { key: "setValue", value: /* @__PURE__ */ __name(function() {
          if (arguments[0] instanceof t2) {
            var e3 = arguments[0];
            return this.init(e3), this;
          }
          if ("number" == typeof arguments[0]) {
            var n2 = arguments[0];
            return this.init(n2), this;
          }
        }, "value") }, { key: "max", value: /* @__PURE__ */ __name(function(t3) {
          return this.ge(t3) ? this : t3;
        }, "value") }, { key: "sqrt", value: /* @__PURE__ */ __name(function() {
          if (this.isZero()) return t2.valueOf(0);
          if (this.isNegative()) return t2.NaN;
          var e3 = 1 / Math.sqrt(this._hi), n2 = this._hi * e3, i2 = t2.valueOf(n2), r2 = this.subtract(i2.sqr())._hi * (0.5 * e3);
          return i2.add(r2);
        }, "value") }, { key: "selfAdd", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            if (arguments[0] instanceof t2) {
              var e3 = arguments[0];
              return this.selfAdd(e3._hi, e3._lo);
            }
            if ("number" == typeof arguments[0]) {
              var n2, i2, r2, s2, a2, o2 = arguments[0], u2 = null;
              return u2 = (r2 = this._hi + o2) - (s2 = r2 - this._hi), i2 = (a2 = (u2 = o2 - s2 + (this._hi - u2)) + this._lo) + (r2 - (n2 = r2 + a2)), this._hi = n2 + i2, this._lo = i2 + (n2 - this._hi), this;
            }
          } else if (2 === arguments.length) {
            var l2, h2, c2, f2, g2 = arguments[0], v2 = arguments[1], y2 = null, d2 = null, _2 = null;
            c2 = this._hi + g2, h2 = this._lo + v2, d2 = c2 - (_2 = c2 - this._hi), y2 = h2 - (f2 = h2 - this._lo);
            var p2 = (l2 = c2 + (_2 = (d2 = g2 - _2 + (this._hi - d2)) + h2)) + (_2 = (y2 = v2 - f2 + (this._lo - y2)) + (_2 + (c2 - l2))), m2 = _2 + (l2 - p2);
            return this._hi = p2, this._lo = m2, this;
          }
        }, "value") }, { key: "selfMultiply", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            if (arguments[0] instanceof t2) {
              var e3 = arguments[0];
              return this.selfMultiply(e3._hi, e3._lo);
            }
            if ("number" == typeof arguments[0]) {
              var n2 = arguments[0];
              return this.selfMultiply(n2, 0);
            }
          } else if (2 === arguments.length) {
            var i2, r2, s2 = arguments[0], a2 = arguments[1], o2 = null, u2 = null, l2 = null, h2 = null;
            o2 = (l2 = t2.SPLIT * this._hi) - this._hi, h2 = t2.SPLIT * s2, o2 = l2 - o2, i2 = this._hi - o2, u2 = h2 - s2;
            var c2 = (l2 = this._hi * s2) + (h2 = o2 * (u2 = h2 - u2) - l2 + o2 * (r2 = s2 - u2) + i2 * u2 + i2 * r2 + (this._hi * a2 + this._lo * s2)), f2 = h2 + (o2 = l2 - c2);
            return this._hi = c2, this._lo = f2, this;
          }
        }, "value") }, { key: "selfSqr", value: /* @__PURE__ */ __name(function() {
          return this.selfMultiply(this);
        }, "value") }, { key: "floor", value: /* @__PURE__ */ __name(function() {
          if (this.isNaN()) return t2.NaN;
          var e3 = Math.floor(this._hi), n2 = 0;
          return e3 === this._hi && (n2 = Math.floor(this._lo)), new t2(e3, n2);
        }, "value") }, { key: "negate", value: /* @__PURE__ */ __name(function() {
          return this.isNaN() ? this : new t2(-this._hi, -this._lo);
        }, "value") }, { key: "clone", value: /* @__PURE__ */ __name(function() {
          try {
            return null;
          } catch (t3) {
            if (t3 instanceof CloneNotSupportedException) return null;
            throw t3;
          }
        }, "value") }, { key: "multiply", value: /* @__PURE__ */ __name(function() {
          if (arguments[0] instanceof t2) {
            var e3 = arguments[0];
            return e3.isNaN() ? t2.createNaN() : t2.copy(this).selfMultiply(e3);
          }
          if ("number" == typeof arguments[0]) {
            var n2 = arguments[0];
            return A.isNaN(n2) ? t2.createNaN() : t2.copy(this).selfMultiply(n2, 0);
          }
        }, "value") }, { key: "isNaN", value: /* @__PURE__ */ __name(function() {
          return A.isNaN(this._hi);
        }, "value") }, { key: "intValue", value: /* @__PURE__ */ __name(function() {
          return Math.trunc(this._hi);
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          var e3 = t2.magnitude(this._hi);
          return e3 >= -3 && e3 <= 20 ? this.toStandardNotation() : this.toSciNotation();
        }, "value") }, { key: "toStandardNotation", value: /* @__PURE__ */ __name(function() {
          var e3 = this.getSpecialNumberString();
          if (null !== e3) return e3;
          var n2 = new Array(1).fill(null), i2 = this.extractSignificantDigits(true, n2), r2 = n2[0] + 1, s2 = i2;
          if ("." === i2.charAt(0)) s2 = "0" + i2;
          else if (r2 < 0) s2 = "0." + t2.stringOfChar("0", -r2) + i2;
          else if (-1 === i2.indexOf(".")) {
            var a2 = r2 - i2.length;
            s2 = i2 + t2.stringOfChar("0", a2) + ".0";
          }
          return this.isNegative() ? "-" + s2 : s2;
        }, "value") }, { key: "reciprocal", value: /* @__PURE__ */ __name(function() {
          var e3, n2, i2, r2, s2 = null, a2 = null, o2 = null, u2 = null;
          e3 = (i2 = 1 / this._hi) - (s2 = (o2 = t2.SPLIT * i2) - (s2 = o2 - i2)), a2 = (u2 = t2.SPLIT * this._hi) - this._hi;
          var l2 = i2 + (o2 = (1 - (r2 = i2 * this._hi) - (u2 = s2 * (a2 = u2 - a2) - r2 + s2 * (n2 = this._hi - a2) + e3 * a2 + e3 * n2) - i2 * this._lo) / this._hi);
          return new t2(l2, i2 - l2 + o2);
        }, "value") }, { key: "toSciNotation", value: /* @__PURE__ */ __name(function() {
          if (this.isZero()) return t2.SCI_NOT_ZERO;
          var e3 = this.getSpecialNumberString();
          if (null !== e3) return e3;
          var n2 = new Array(1).fill(null), i2 = this.extractSignificantDigits(false, n2), r2 = t2.SCI_NOT_EXPONENT_CHAR + n2[0];
          if ("0" === i2.charAt(0)) throw new IllegalStateException("Found leading zero: " + i2);
          var s2 = "";
          i2.length > 1 && (s2 = i2.substring(1));
          var a2 = i2.charAt(0) + "." + s2;
          return this.isNegative() ? "-" + a2 + r2 : a2 + r2;
        }, "value") }, { key: "abs", value: /* @__PURE__ */ __name(function() {
          return this.isNaN() ? t2.NaN : this.isNegative() ? this.negate() : new t2(this);
        }, "value") }, { key: "isPositive", value: /* @__PURE__ */ __name(function() {
          return this._hi > 0 || 0 === this._hi && this._lo > 0;
        }, "value") }, { key: "lt", value: /* @__PURE__ */ __name(function(t3) {
          return this._hi < t3._hi || this._hi === t3._hi && this._lo < t3._lo;
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function() {
          if (arguments[0] instanceof t2) {
            var e3 = arguments[0];
            return t2.copy(this).selfAdd(e3);
          }
          if ("number" == typeof arguments[0]) {
            var n2 = arguments[0];
            return t2.copy(this).selfAdd(n2);
          }
        }, "value") }, { key: "init", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            if ("number" == typeof arguments[0]) {
              var e3 = arguments[0];
              this._hi = e3, this._lo = 0;
            } else if (arguments[0] instanceof t2) {
              var n2 = arguments[0];
              this._hi = n2._hi, this._lo = n2._lo;
            }
          } else if (2 === arguments.length) {
            var i2 = arguments[0], r2 = arguments[1];
            this._hi = i2, this._lo = r2;
          }
        }, "value") }, { key: "gt", value: /* @__PURE__ */ __name(function(t3) {
          return this._hi > t3._hi || this._hi === t3._hi && this._lo > t3._lo;
        }, "value") }, { key: "isNegative", value: /* @__PURE__ */ __name(function() {
          return this._hi < 0 || 0 === this._hi && this._lo < 0;
        }, "value") }, { key: "trunc", value: /* @__PURE__ */ __name(function() {
          return this.isNaN() ? t2.NaN : this.isPositive() ? this.floor() : this.ceil();
        }, "value") }, { key: "signum", value: /* @__PURE__ */ __name(function() {
          return this._hi > 0 ? 1 : this._hi < 0 ? -1 : this._lo > 0 ? 1 : this._lo < 0 ? -1 : 0;
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [E, x, I];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._hi = 0, this._lo = 0, 0 === arguments.length) this.init(0);
          else if (1 === arguments.length) {
            if ("number" == typeof arguments[0]) {
              var e3 = arguments[0];
              this.init(e3);
            } else if (arguments[0] instanceof t2) {
              var n2 = arguments[0];
              this.init(n2);
            } else if ("string" == typeof arguments[0]) {
              var i2 = arguments[0];
              t2.constructor_.call(this, t2.parse(i2));
            }
          } else if (2 === arguments.length) {
            var r2 = arguments[0], s2 = arguments[1];
            this.init(r2, s2);
          }
        }, "value") }, { key: "determinant", value: /* @__PURE__ */ __name(function() {
          if ("number" == typeof arguments[3] && "number" == typeof arguments[2] && "number" == typeof arguments[0] && "number" == typeof arguments[1]) {
            var e3 = arguments[0], n2 = arguments[1], i2 = arguments[2], r2 = arguments[3];
            return t2.determinant(t2.valueOf(e3), t2.valueOf(n2), t2.valueOf(i2), t2.valueOf(r2));
          }
          if (arguments[3] instanceof t2 && arguments[2] instanceof t2 && arguments[0] instanceof t2 && arguments[1] instanceof t2) {
            var s2 = arguments[1], a2 = arguments[2], o2 = arguments[3];
            return arguments[0].multiply(o2).selfSubtract(s2.multiply(a2));
          }
        }, "value") }, { key: "sqr", value: /* @__PURE__ */ __name(function(e3) {
          return t2.valueOf(e3).selfMultiply(e3);
        }, "value") }, { key: "valueOf", value: /* @__PURE__ */ __name(function() {
          if ("string" == typeof arguments[0]) {
            var e3 = arguments[0];
            return t2.parse(e3);
          }
          if ("number" == typeof arguments[0]) return new t2(arguments[0]);
        }, "value") }, { key: "sqrt", value: /* @__PURE__ */ __name(function(e3) {
          return t2.valueOf(e3).sqrt();
        }, "value") }, { key: "parse", value: /* @__PURE__ */ __name(function(e3) {
          for (var n2 = 0, i2 = e3.length; ot.isWhitespace(e3.charAt(n2)); ) n2++;
          var r2 = false;
          if (n2 < i2) {
            var s2 = e3.charAt(n2);
            "-" !== s2 && "+" !== s2 || (n2++, "-" === s2 && (r2 = true));
          }
          for (var a2 = new t2(), o2 = 0, u2 = 0, l2 = 0, h2 = false; !(n2 >= i2); ) {
            var c2 = e3.charAt(n2);
            if (n2++, ot.isDigit(c2)) {
              var f2 = c2 - "0";
              a2.selfMultiply(t2.TEN), a2.selfAdd(f2), o2++;
            } else {
              if ("." !== c2) {
                if ("e" === c2 || "E" === c2) {
                  var g2 = e3.substring(n2);
                  try {
                    l2 = at.parseInt(g2);
                  } catch (t3) {
                    throw t3 instanceof NumberFormatException ? new NumberFormatException("Invalid exponent " + g2 + " in string " + e3) : t3;
                  }
                  break;
                }
                throw new NumberFormatException("Unexpected character '" + c2 + "' at position " + n2 + " in string " + e3);
              }
              u2 = o2, h2 = true;
            }
          }
          var v2 = a2;
          h2 || (u2 = o2);
          var y2 = o2 - u2 - l2;
          if (0 === y2) v2 = a2;
          else if (y2 > 0) {
            var d2 = t2.TEN.pow(y2);
            v2 = a2.divide(d2);
          } else if (y2 < 0) {
            var _2 = t2.TEN.pow(-y2);
            v2 = a2.multiply(_2);
          }
          return r2 ? v2.negate() : v2;
        }, "value") }, { key: "createNaN", value: /* @__PURE__ */ __name(function() {
          return new t2(A.NaN, A.NaN);
        }, "value") }, { key: "copy", value: /* @__PURE__ */ __name(function(e3) {
          return new t2(e3);
        }, "value") }, { key: "magnitude", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = Math.abs(t3), n2 = Math.log(e3) / Math.log(10), i2 = Math.trunc(Math.floor(n2));
          return 10 * Math.pow(10, i2) <= e3 && (i2 += 1), i2;
        }, "value") }, { key: "stringOfChar", value: /* @__PURE__ */ __name(function(t3, e3) {
          for (var n2 = new st(), i2 = 0; i2 < e3; i2++) n2.append(t3);
          return n2.toString();
        }, "value") }]);
      })();
      ut.PI = new ut(3.141592653589793, 12246467991473532e-32), ut.TWO_PI = new ut(6.283185307179586, 24492935982947064e-32), ut.PI_2 = new ut(1.5707963267948966, 6123233995736766e-32), ut.E = new ut(2.718281828459045, 14456468917292502e-32), ut.NaN = new ut(A.NaN, A.NaN), ut.EPS = 123259516440783e-46, ut.SPLIT = 134217729, ut.MAX_PRINT_DIGITS = 32, ut.TEN = ut.valueOf(10), ut.ONE = ut.valueOf(1), ut.SCI_NOT_EXPONENT_CHAR = "E", ut.SCI_NOT_ZERO = "0.0E0";
      var lt3 = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "orientationIndex", value: /* @__PURE__ */ __name(function(e3, n2, i2) {
          var r2 = t2.orientationIndexFilter(e3, n2, i2);
          if (r2 <= 1) return r2;
          var s2 = ut.valueOf(n2.x).selfAdd(-e3.x), a2 = ut.valueOf(n2.y).selfAdd(-e3.y), o2 = ut.valueOf(i2.x).selfAdd(-n2.x), u2 = ut.valueOf(i2.y).selfAdd(-n2.y);
          return s2.selfMultiply(u2).selfSubtract(a2.selfMultiply(o2)).signum();
        }, "value") }, { key: "signOfDet2x2", value: /* @__PURE__ */ __name(function() {
          if (arguments[3] instanceof ut && arguments[2] instanceof ut && arguments[0] instanceof ut && arguments[1] instanceof ut) {
            var t3 = arguments[1], e3 = arguments[2], n2 = arguments[3];
            return arguments[0].multiply(n2).selfSubtract(t3.multiply(e3)).signum();
          }
          if ("number" == typeof arguments[3] && "number" == typeof arguments[2] && "number" == typeof arguments[0] && "number" == typeof arguments[1]) {
            var i2 = arguments[0], r2 = arguments[1], s2 = arguments[2], a2 = arguments[3], o2 = ut.valueOf(i2), u2 = ut.valueOf(r2), l2 = ut.valueOf(s2), h2 = ut.valueOf(a2);
            return o2.multiply(h2).selfSubtract(u2.multiply(l2)).signum();
          }
        }, "value") }, { key: "intersection", value: /* @__PURE__ */ __name(function(t3, e3, n2, i2) {
          var r2 = new ut(t3.y).selfSubtract(e3.y), s2 = new ut(e3.x).selfSubtract(t3.x), a2 = new ut(t3.x).selfMultiply(e3.y).selfSubtract(new ut(e3.x).selfMultiply(t3.y)), o2 = new ut(n2.y).selfSubtract(i2.y), u2 = new ut(i2.x).selfSubtract(n2.x), l2 = new ut(n2.x).selfMultiply(i2.y).selfSubtract(new ut(i2.x).selfMultiply(n2.y)), h2 = s2.multiply(l2).selfSubtract(u2.multiply(a2)), c2 = o2.multiply(a2).selfSubtract(r2.multiply(l2)), f2 = r2.multiply(u2).selfSubtract(o2.multiply(s2)), g2 = h2.selfDivide(f2).doubleValue(), v2 = c2.selfDivide(f2).doubleValue();
          return A.isNaN(g2) || A.isInfinite(g2) || A.isNaN(v2) || A.isInfinite(v2) ? null : new X(g2, v2);
        }, "value") }, { key: "orientationIndexFilter", value: /* @__PURE__ */ __name(function(e3, n2, i2) {
          var r2 = null, s2 = (e3.x - i2.x) * (n2.y - i2.y), a2 = (e3.y - i2.y) * (n2.x - i2.x), o2 = s2 - a2;
          if (s2 > 0) {
            if (a2 <= 0) return t2.signum(o2);
            r2 = s2 + a2;
          } else {
            if (!(s2 < 0)) return t2.signum(o2);
            if (a2 >= 0) return t2.signum(o2);
            r2 = -s2 - a2;
          }
          var u2 = t2.DP_SAFE_EPSILON * r2;
          return o2 >= u2 || -o2 >= u2 ? t2.signum(o2) : 2;
        }, "value") }, { key: "signum", value: /* @__PURE__ */ __name(function(t3) {
          return t3 > 0 ? 1 : t3 < 0 ? -1 : 0;
        }, "value") }]);
      })();
      lt3.DP_SAFE_EPSILON = 1e-15;
      var ht = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "getM", value: /* @__PURE__ */ __name(function(t2) {
          if (this.hasM()) {
            var e3 = this.getDimension() - this.getMeasures();
            return this.getOrdinate(t2, e3);
          }
          return A.NaN;
        }, "value") }, { key: "setOrdinate", value: /* @__PURE__ */ __name(function(t2, e3, n2) {
        }, "value") }, { key: "getZ", value: /* @__PURE__ */ __name(function(t2) {
          return this.hasZ() ? this.getOrdinate(t2, 2) : A.NaN;
        }, "value") }, { key: "size", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "getOrdinate", value: /* @__PURE__ */ __name(function(t2, e3) {
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "getCoordinateCopy", value: /* @__PURE__ */ __name(function(t2) {
        }, "value") }, { key: "createCoordinate", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "getDimension", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "hasM", value: /* @__PURE__ */ __name(function() {
          return this.getMeasures() > 0;
        }, "value") }, { key: "getX", value: /* @__PURE__ */ __name(function(t2) {
        }, "value") }, { key: "hasZ", value: /* @__PURE__ */ __name(function() {
          return this.getDimension() - this.getMeasures() > 2;
        }, "value") }, { key: "getMeasures", value: /* @__PURE__ */ __name(function() {
          return 0;
        }, "value") }, { key: "expandEnvelope", value: /* @__PURE__ */ __name(function(t2) {
        }, "value") }, { key: "copy", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "getY", value: /* @__PURE__ */ __name(function(t2) {
        }, "value") }, { key: "toCoordinateArray", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [I];
        }, "get") }]);
      })();
      ht.X = 0, ht.Y = 1, ht.Z = 2, ht.M = 3;
      var ct = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "index", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          return lt3.orientationIndex(t3, e3, n2);
        }, "value") }, { key: "isCCW", value: /* @__PURE__ */ __name(function() {
          if (arguments[0] instanceof Array) {
            var e3 = arguments[0], n2 = e3.length - 1;
            if (n2 < 3) throw new m("Ring has fewer than 4 points, so orientation cannot be determined");
            for (var i2 = e3[0], r2 = 0, s2 = 1; s2 <= n2; s2++) {
              var a2 = e3[s2];
              a2.y > i2.y && (i2 = a2, r2 = s2);
            }
            var o2 = r2;
            do {
              (o2 -= 1) < 0 && (o2 = n2);
            } while (e3[o2].equals2D(i2) && o2 !== r2);
            var u2 = r2;
            do {
              u2 = (u2 + 1) % n2;
            } while (e3[u2].equals2D(i2) && u2 !== r2);
            var l2 = e3[o2], h2 = e3[u2];
            if (l2.equals2D(i2) || h2.equals2D(i2) || l2.equals2D(h2)) return false;
            var c2 = t2.index(l2, i2, h2);
            return 0 === c2 ? l2.x > h2.x : c2 > 0;
          }
          if (rt(arguments[0], ht)) {
            var f2 = arguments[0], g2 = f2.size() - 1;
            if (g2 < 3) throw new m("Ring has fewer than 4 points, so orientation cannot be determined");
            for (var v2 = f2.getCoordinate(0), y2 = 0, d2 = 1; d2 <= g2; d2++) {
              var _2 = f2.getCoordinate(d2);
              _2.y > v2.y && (v2 = _2, y2 = d2);
            }
            var p2 = null, k2 = y2;
            do {
              (k2 -= 1) < 0 && (k2 = g2), p2 = f2.getCoordinate(k2);
            } while (p2.equals2D(v2) && k2 !== y2);
            var x2 = null, I2 = y2;
            do {
              I2 = (I2 + 1) % g2, x2 = f2.getCoordinate(I2);
            } while (x2.equals2D(v2) && I2 !== y2);
            if (p2.equals2D(v2) || x2.equals2D(v2) || p2.equals2D(x2)) return false;
            var E2 = t2.index(p2, v2, x2);
            return 0 === E2 ? p2.x > x2.x : E2 > 0;
          }
        }, "value") }]);
      })();
      ct.CLOCKWISE = -1, ct.RIGHT = ct.CLOCKWISE, ct.COUNTERCLOCKWISE = 1, ct.LEFT = ct.COUNTERCLOCKWISE, ct.COLLINEAR = 0, ct.STRAIGHT = ct.COLLINEAR;
      var ft = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "getCoordinate", value: /* @__PURE__ */ __name(function() {
          return this._minCoord;
        }, "value") }, { key: "getRightmostSide", value: /* @__PURE__ */ __name(function(t2, e3) {
          var n2 = this.getRightmostSideOfSegment(t2, e3);
          return n2 < 0 && (n2 = this.getRightmostSideOfSegment(t2, e3 - 1)), n2 < 0 && (this._minCoord = null, this.checkForRightmostCoordinate(t2)), n2;
        }, "value") }, { key: "findRightmostEdgeAtVertex", value: /* @__PURE__ */ __name(function() {
          var t2 = this._minDe.getEdge().getCoordinates();
          G.isTrue(this._minIndex > 0 && this._minIndex < t2.length, "rightmost point expected to be interior vertex of edge");
          var e3 = t2[this._minIndex - 1], n2 = t2[this._minIndex + 1], i2 = ct.index(this._minCoord, n2, e3), r2 = false;
          (e3.y < this._minCoord.y && n2.y < this._minCoord.y && i2 === ct.COUNTERCLOCKWISE || e3.y > this._minCoord.y && n2.y > this._minCoord.y && i2 === ct.CLOCKWISE) && (r2 = true), r2 && (this._minIndex = this._minIndex - 1);
        }, "value") }, { key: "getRightmostSideOfSegment", value: /* @__PURE__ */ __name(function(t2, e3) {
          var n2 = t2.getEdge().getCoordinates();
          if (e3 < 0 || e3 + 1 >= n2.length) return -1;
          if (n2[e3].y === n2[e3 + 1].y) return -1;
          var i2 = $.LEFT;
          return n2[e3].y < n2[e3 + 1].y && (i2 = $.RIGHT), i2;
        }, "value") }, { key: "getEdge", value: /* @__PURE__ */ __name(function() {
          return this._orientedDe;
        }, "value") }, { key: "checkForRightmostCoordinate", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = t2.getEdge().getCoordinates(), n2 = 0; n2 < e3.length - 1; n2++) (null === this._minCoord || e3[n2].x > this._minCoord.x) && (this._minDe = t2, this._minIndex = n2, this._minCoord = e3[n2]);
        }, "value") }, { key: "findRightmostEdgeAtNode", value: /* @__PURE__ */ __name(function() {
          var t2 = this._minDe.getNode().getEdges();
          this._minDe = t2.getRightmostEdge(), this._minDe.isForward() || (this._minDe = this._minDe.getSym(), this._minIndex = this._minDe.getEdge().getCoordinates().length - 1);
        }, "value") }, { key: "findEdge", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = t2.iterator(); e3.hasNext(); ) {
            var n2 = e3.next();
            n2.isForward() && this.checkForRightmostCoordinate(n2);
          }
          G.isTrue(0 !== this._minIndex || this._minCoord.equals(this._minDe.getCoordinate()), "inconsistency in rightmost processing"), 0 === this._minIndex ? this.findRightmostEdgeAtNode() : this.findRightmostEdgeAtVertex(), this._orientedDe = this._minDe, this.getRightmostSide(this._minDe, this._minIndex) === $.LEFT && (this._orientedDe = this._minDe.getSym());
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._minIndex = -1, this._minCoord = null, this._minDe = null, this._orientedDe = null;
        }, "value") }]);
      })(), gt3 = (function(t2) {
        function i2(t3, r2) {
          var s2;
          return n(this, i2), (s2 = e(this, i2, [r2 ? t3 + " [ " + r2 + " ]" : t3])).pt = r2 ? new X(r2) : void 0, s2.name = Object.keys({ TopologyException: i2 })[0], s2;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "getCoordinate", value: /* @__PURE__ */ __name(function() {
          return this.pt;
        }, "value") }]);
      })(D), vt = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), this.array = [];
        }), "t"), [{ key: "addLast", value: /* @__PURE__ */ __name(function(t2) {
          this.array.push(t2);
        }, "value") }, { key: "removeFirst", value: /* @__PURE__ */ __name(function() {
          return this.array.shift();
        }, "value") }, { key: "isEmpty", value: /* @__PURE__ */ __name(function() {
          return 0 === this.array.length;
        }, "value") }]);
      })(), yt = (function(t2) {
        function i2(t3) {
          var r2;
          return n(this, i2), (r2 = e(this, i2)).array = [], t3 instanceof Z && r2.addAll(t3), r2;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [nt, Z];
        }, "get") }, { key: "ensureCapacity", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function(t3) {
          return 1 === arguments.length ? this.array.push(t3) : this.array.splice(arguments[0], 0, arguments[1]), true;
        }, "value") }, { key: "clear", value: /* @__PURE__ */ __name(function() {
          this.array = [];
        }, "value") }, { key: "addAll", value: /* @__PURE__ */ __name(function(t3) {
          var e3, n2 = a(t3);
          try {
            for (n2.s(); !(e3 = n2.n()).done; ) {
              var i3 = e3.value;
              this.array.push(i3);
            }
          } catch (t4) {
            n2.e(t4);
          } finally {
            n2.f();
          }
        }, "value") }, { key: "set", value: /* @__PURE__ */ __name(function(t3, e3) {
          var n2 = this.array[t3];
          return this.array[t3] = e3, n2;
        }, "value") }, { key: "iterator", value: /* @__PURE__ */ __name(function() {
          return new dt(this);
        }, "value") }, { key: "get", value: /* @__PURE__ */ __name(function(t3) {
          if (t3 < 0 || t3 >= this.size()) throw new et();
          return this.array[t3];
        }, "value") }, { key: "isEmpty", value: /* @__PURE__ */ __name(function() {
          return 0 === this.array.length;
        }, "value") }, { key: "sort", value: /* @__PURE__ */ __name(function(t3) {
          t3 ? this.array.sort((function(e3, n2) {
            return t3.compare(e3, n2);
          })) : this.array.sort();
        }, "value") }, { key: "size", value: /* @__PURE__ */ __name(function() {
          return this.array.length;
        }, "value") }, { key: "toArray", value: /* @__PURE__ */ __name(function() {
          return this.array.slice();
        }, "value") }, { key: "remove", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = 0, n2 = this.array.length; e3 < n2; e3++) if (this.array[e3] === t3) return !!this.array.splice(e3, 1);
          return false;
        }, "value") }, { key: Symbol.iterator, value: /* @__PURE__ */ __name(function() {
          return this.array.values();
        }, "value") }]);
      })(nt), dt = (function() {
        return s(/* @__PURE__ */ __name((function t2(e3) {
          n(this, t2), this.arrayList = e3, this.position = 0;
        }), "t"), [{ key: "next", value: /* @__PURE__ */ __name(function() {
          if (this.position === this.arrayList.size()) throw new j();
          return this.arrayList.get(this.position++);
        }, "value") }, { key: "hasNext", value: /* @__PURE__ */ __name(function() {
          return this.position < this.arrayList.size();
        }, "value") }, { key: "set", value: /* @__PURE__ */ __name(function(t2) {
          return this.arrayList.set(this.position - 1, t2);
        }, "value") }, { key: "remove", value: /* @__PURE__ */ __name(function() {
          this.arrayList.remove(this.arrayList.get(this.position));
        }, "value") }]);
      })(), _t = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "clearVisitedEdges", value: /* @__PURE__ */ __name(function() {
          for (var t2 = this._dirEdgeList.iterator(); t2.hasNext(); ) {
            t2.next().setVisited(false);
          }
        }, "value") }, { key: "getRightmostCoordinate", value: /* @__PURE__ */ __name(function() {
          return this._rightMostCoord;
        }, "value") }, { key: "computeNodeDepth", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = null, n2 = t2.getEdges().iterator(); n2.hasNext(); ) {
            var i2 = n2.next();
            if (i2.isVisited() || i2.getSym().isVisited()) {
              e3 = i2;
              break;
            }
          }
          if (null === e3) throw new gt3("unable to find edge to compute depths at " + t2.getCoordinate());
          t2.getEdges().computeDepths(e3);
          for (var r2 = t2.getEdges().iterator(); r2.hasNext(); ) {
            var s2 = r2.next();
            s2.setVisited(true), this.copySymDepths(s2);
          }
        }, "value") }, { key: "computeDepth", value: /* @__PURE__ */ __name(function(t2) {
          this.clearVisitedEdges();
          var e3 = this._finder.getEdge();
          e3.getNode(), e3.getLabel(), e3.setEdgeDepths($.RIGHT, t2), this.copySymDepths(e3), this.computeDepths(e3);
        }, "value") }, { key: "create", value: /* @__PURE__ */ __name(function(t2) {
          this.addReachable(t2), this._finder.findEdge(this._dirEdgeList), this._rightMostCoord = this._finder.getCoordinate();
        }, "value") }, { key: "findResultEdges", value: /* @__PURE__ */ __name(function() {
          for (var t2 = this._dirEdgeList.iterator(); t2.hasNext(); ) {
            var e3 = t2.next();
            e3.getDepth($.RIGHT) >= 1 && e3.getDepth($.LEFT) <= 0 && !e3.isInteriorAreaEdge() && e3.setInResult(true);
          }
        }, "value") }, { key: "computeDepths", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = new J(), n2 = new vt(), i2 = t2.getNode();
          for (n2.addLast(i2), e3.add(i2), t2.setVisited(true); !n2.isEmpty(); ) {
            var r2 = n2.removeFirst();
            e3.add(r2), this.computeNodeDepth(r2);
            for (var s2 = r2.getEdges().iterator(); s2.hasNext(); ) {
              var a2 = s2.next().getSym();
              if (!a2.isVisited()) {
                var o2 = a2.getNode();
                e3.contains(o2) || (n2.addLast(o2), e3.add(o2));
              }
            }
          }
        }, "value") }, { key: "compareTo", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = t2;
          return this._rightMostCoord.x < e3._rightMostCoord.x ? -1 : this._rightMostCoord.x > e3._rightMostCoord.x ? 1 : 0;
        }, "value") }, { key: "getEnvelope", value: /* @__PURE__ */ __name(function() {
          if (null === this._env) {
            for (var t2 = new U(), e3 = this._dirEdgeList.iterator(); e3.hasNext(); ) for (var n2 = e3.next().getEdge().getCoordinates(), i2 = 0; i2 < n2.length - 1; i2++) t2.expandToInclude(n2[i2]);
            this._env = t2;
          }
          return this._env;
        }, "value") }, { key: "addReachable", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = new it();
          for (e3.add(t2); !e3.empty(); ) {
            var n2 = e3.pop();
            this.add(n2, e3);
          }
        }, "value") }, { key: "copySymDepths", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = t2.getSym();
          e3.setDepth($.LEFT, t2.getDepth($.RIGHT)), e3.setDepth($.RIGHT, t2.getDepth($.LEFT));
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function(t2, e3) {
          t2.setVisited(true), this._nodes.add(t2);
          for (var n2 = t2.getEdges().iterator(); n2.hasNext(); ) {
            var i2 = n2.next();
            this._dirEdgeList.add(i2);
            var r2 = i2.getSym().getNode();
            r2.isVisited() || e3.push(r2);
          }
        }, "value") }, { key: "getNodes", value: /* @__PURE__ */ __name(function() {
          return this._nodes;
        }, "value") }, { key: "getDirectedEdges", value: /* @__PURE__ */ __name(function() {
          return this._dirEdgeList;
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [x];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._finder = null, this._dirEdgeList = new yt(), this._nodes = new yt(), this._rightMostCoord = null, this._env = null, this._finder = new ft();
        }, "value") }]);
      })(), pt = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), null, [{ key: "intersection", value: /* @__PURE__ */ __name(function(t2, e3, n2, i2) {
          var r2 = t2.x < e3.x ? t2.x : e3.x, s2 = t2.y < e3.y ? t2.y : e3.y, a2 = t2.x > e3.x ? t2.x : e3.x, o2 = t2.y > e3.y ? t2.y : e3.y, u2 = n2.x < i2.x ? n2.x : i2.x, l2 = n2.y < i2.y ? n2.y : i2.y, h2 = n2.x > i2.x ? n2.x : i2.x, c2 = n2.y > i2.y ? n2.y : i2.y, f2 = ((r2 > u2 ? r2 : u2) + (a2 < h2 ? a2 : h2)) / 2, g2 = ((s2 > l2 ? s2 : l2) + (o2 < c2 ? o2 : c2)) / 2, v2 = t2.x - f2, y2 = t2.y - g2, d2 = e3.x - f2, _2 = e3.y - g2, p2 = n2.x - f2, m2 = n2.y - g2, k2 = i2.x - f2, x2 = i2.y - g2, I2 = y2 - _2, E2 = d2 - v2, N2 = v2 * _2 - d2 * y2, T2 = m2 - x2, S2 = k2 - p2, L2 = p2 * x2 - k2 * m2, C2 = I2 * S2 - T2 * E2, R2 = (E2 * L2 - S2 * N2) / C2, w2 = (T2 * N2 - I2 * L2) / C2;
          return A.isNaN(R2) || A.isInfinite(R2) || A.isNaN(w2) || A.isInfinite(w2) ? null : new X(R2 + f2, w2 + g2);
        }, "value") }]);
      })(), mt = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), null, [{ key: "arraycopy", value: /* @__PURE__ */ __name(function(t2, e3, n2, i2, r2) {
          for (var s2 = 0, a2 = e3; a2 < e3 + r2; a2++) n2[i2 + s2] = t2[a2], s2++;
        }, "value") }, { key: "getProperty", value: /* @__PURE__ */ __name(function(t2) {
          return { "line.separator": "\n" }[t2];
        }, "value") }]);
      })(), kt = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "log10", value: /* @__PURE__ */ __name(function(e3) {
          var n2 = Math.log(e3);
          return A.isInfinite(n2) || A.isNaN(n2) ? n2 : n2 / t2.LOG_10;
        }, "value") }, { key: "min", value: /* @__PURE__ */ __name(function(t3, e3, n2, i2) {
          var r2 = t3;
          return e3 < r2 && (r2 = e3), n2 < r2 && (r2 = n2), i2 < r2 && (r2 = i2), r2;
        }, "value") }, { key: "clamp", value: /* @__PURE__ */ __name(function() {
          if ("number" == typeof arguments[2] && "number" == typeof arguments[0] && "number" == typeof arguments[1]) {
            var t3 = arguments[0], e3 = arguments[1], n2 = arguments[2];
            return t3 < e3 ? e3 : t3 > n2 ? n2 : t3;
          }
          if (Number.isInteger(arguments[2]) && Number.isInteger(arguments[0]) && Number.isInteger(arguments[1])) {
            var i2 = arguments[0], r2 = arguments[1], s2 = arguments[2];
            return i2 < r2 ? r2 : i2 > s2 ? s2 : i2;
          }
        }, "value") }, { key: "wrap", value: /* @__PURE__ */ __name(function(t3, e3) {
          return t3 < 0 ? e3 - -t3 % e3 : t3 % e3;
        }, "value") }, { key: "max", value: /* @__PURE__ */ __name(function() {
          if (3 === arguments.length) {
            var t3 = arguments[1], e3 = arguments[2], n2 = arguments[0];
            return t3 > n2 && (n2 = t3), e3 > n2 && (n2 = e3), n2;
          }
          if (4 === arguments.length) {
            var i2 = arguments[1], r2 = arguments[2], s2 = arguments[3], a2 = arguments[0];
            return i2 > a2 && (a2 = i2), r2 > a2 && (a2 = r2), s2 > a2 && (a2 = s2), a2;
          }
        }, "value") }, { key: "average", value: /* @__PURE__ */ __name(function(t3, e3) {
          return (t3 + e3) / 2;
        }, "value") }]);
      })();
      kt.LOG_10 = Math.log(10);
      var xt = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "segmentToSegment", value: /* @__PURE__ */ __name(function(e3, n2, i2, r2) {
          if (e3.equals(n2)) return t2.pointToSegment(e3, i2, r2);
          if (i2.equals(r2)) return t2.pointToSegment(r2, e3, n2);
          var s2 = false;
          if (U.intersects(e3, n2, i2, r2)) {
            var a2 = (n2.x - e3.x) * (r2.y - i2.y) - (n2.y - e3.y) * (r2.x - i2.x);
            if (0 === a2) s2 = true;
            else {
              var o2 = (e3.y - i2.y) * (r2.x - i2.x) - (e3.x - i2.x) * (r2.y - i2.y), u2 = ((e3.y - i2.y) * (n2.x - e3.x) - (e3.x - i2.x) * (n2.y - e3.y)) / a2, l2 = o2 / a2;
              (l2 < 0 || l2 > 1 || u2 < 0 || u2 > 1) && (s2 = true);
            }
          } else s2 = true;
          return s2 ? kt.min(t2.pointToSegment(e3, i2, r2), t2.pointToSegment(n2, i2, r2), t2.pointToSegment(i2, e3, n2), t2.pointToSegment(r2, e3, n2)) : 0;
        }, "value") }, { key: "pointToSegment", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          if (e3.x === n2.x && e3.y === n2.y) return t3.distance(e3);
          var i2 = (n2.x - e3.x) * (n2.x - e3.x) + (n2.y - e3.y) * (n2.y - e3.y), r2 = ((t3.x - e3.x) * (n2.x - e3.x) + (t3.y - e3.y) * (n2.y - e3.y)) / i2;
          if (r2 <= 0) return t3.distance(e3);
          if (r2 >= 1) return t3.distance(n2);
          var s2 = ((e3.y - t3.y) * (n2.x - e3.x) - (e3.x - t3.x) * (n2.y - e3.y)) / i2;
          return Math.abs(s2) * Math.sqrt(i2);
        }, "value") }, { key: "pointToLinePerpendicular", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          var i2 = (n2.x - e3.x) * (n2.x - e3.x) + (n2.y - e3.y) * (n2.y - e3.y), r2 = ((e3.y - t3.y) * (n2.x - e3.x) - (e3.x - t3.x) * (n2.y - e3.y)) / i2;
          return Math.abs(r2) * Math.sqrt(i2);
        }, "value") }, { key: "pointToSegmentString", value: /* @__PURE__ */ __name(function(e3, n2) {
          if (0 === n2.length) throw new m("Line array must contain at least one vertex");
          for (var i2 = e3.distance(n2[0]), r2 = 0; r2 < n2.length - 1; r2++) {
            var s2 = t2.pointToSegment(e3, n2[r2], n2[r2 + 1]);
            s2 < i2 && (i2 = s2);
          }
          return i2;
        }, "value") }]);
      })(), It = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "create", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) arguments[0] instanceof Array || rt(arguments[0], ht);
          else if (2 === arguments.length) ;
          else if (3 === arguments.length) {
            var t2 = arguments[0], e3 = arguments[1];
            return this.create(t2, e3);
          }
        }, "value") }]);
      })(), Et = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "filter", value: /* @__PURE__ */ __name(function(t2) {
        }, "value") }]);
      })(), Nt = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), null, [{ key: "ofLine", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = t2.size();
          if (e3 <= 1) return 0;
          var n2 = 0, i2 = new X();
          t2.getCoordinate(0, i2);
          for (var r2 = i2.x, s2 = i2.y, a2 = 1; a2 < e3; a2++) {
            t2.getCoordinate(a2, i2);
            var o2 = i2.x, u2 = i2.y, l2 = o2 - r2, h2 = u2 - s2;
            n2 += Math.sqrt(l2 * l2 + h2 * h2), r2 = o2, s2 = u2;
          }
          return n2;
        }, "value") }]);
      })(), Tt = s(/* @__PURE__ */ __name((function t2() {
        n(this, t2);
      }), "t")), St = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "copyCoord", value: /* @__PURE__ */ __name(function(t3, e3, n2, i2) {
          for (var r2 = Math.min(t3.getDimension(), n2.getDimension()), s2 = 0; s2 < r2; s2++) n2.setOrdinate(i2, s2, t3.getOrdinate(e3, s2));
        }, "value") }, { key: "isRing", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = t3.size();
          return 0 === e3 || !(e3 <= 3) && (t3.getOrdinate(0, ht.X) === t3.getOrdinate(e3 - 1, ht.X) && t3.getOrdinate(0, ht.Y) === t3.getOrdinate(e3 - 1, ht.Y));
        }, "value") }, { key: "scroll", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length) {
            if (rt(arguments[0], ht) && Number.isInteger(arguments[1])) {
              var e3 = arguments[0], n2 = arguments[1];
              t2.scroll(e3, n2, t2.isRing(e3));
            } else if (rt(arguments[0], ht) && arguments[1] instanceof X) {
              var i2 = arguments[0], r2 = arguments[1], s2 = t2.indexOf(r2, i2);
              if (s2 <= 0) return null;
              t2.scroll(i2, s2);
            }
          } else if (3 === arguments.length) {
            var a2 = arguments[0], o2 = arguments[1], u2 = arguments[2];
            if (o2 <= 0) return null;
            for (var l2 = a2.copy(), h2 = u2 ? a2.size() - 1 : a2.size(), c2 = 0; c2 < h2; c2++) for (var f2 = 0; f2 < a2.getDimension(); f2++) a2.setOrdinate(c2, f2, l2.getOrdinate((o2 + c2) % h2, f2));
            if (u2) for (var g2 = 0; g2 < a2.getDimension(); g2++) a2.setOrdinate(h2, g2, a2.getOrdinate(0, g2));
          }
        }, "value") }, { key: "isEqual", value: /* @__PURE__ */ __name(function(t3, e3) {
          var n2 = t3.size();
          if (n2 !== e3.size()) return false;
          for (var i2 = Math.min(t3.getDimension(), e3.getDimension()), r2 = 0; r2 < n2; r2++) for (var s2 = 0; s2 < i2; s2++) {
            var a2 = t3.getOrdinate(r2, s2), o2 = e3.getOrdinate(r2, s2);
            if (t3.getOrdinate(r2, s2) !== e3.getOrdinate(r2, s2) && (!A.isNaN(a2) || !A.isNaN(o2))) return false;
          }
          return true;
        }, "value") }, { key: "minCoordinateIndex", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var e3 = arguments[0];
            return t2.minCoordinateIndex(e3, 0, e3.size() - 1);
          }
          if (3 === arguments.length) {
            for (var n2 = arguments[0], i2 = arguments[2], r2 = -1, s2 = null, a2 = arguments[1]; a2 <= i2; a2++) {
              var o2 = n2.getCoordinate(a2);
              (null === s2 || s2.compareTo(o2) > 0) && (s2 = o2, r2 = a2);
            }
            return r2;
          }
        }, "value") }, { key: "extend", value: /* @__PURE__ */ __name(function(e3, n2, i2) {
          var r2 = e3.create(i2, n2.getDimension()), s2 = n2.size();
          if (t2.copy(n2, 0, r2, 0, s2), s2 > 0) for (var a2 = s2; a2 < i2; a2++) t2.copy(n2, s2 - 1, r2, a2, 1);
          return r2;
        }, "value") }, { key: "reverse", value: /* @__PURE__ */ __name(function(e3) {
          for (var n2 = e3.size() - 1, i2 = Math.trunc(n2 / 2), r2 = 0; r2 <= i2; r2++) t2.swap(e3, r2, n2 - r2);
        }, "value") }, { key: "swap", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          if (e3 === n2) return null;
          for (var i2 = 0; i2 < t3.getDimension(); i2++) {
            var r2 = t3.getOrdinate(e3, i2);
            t3.setOrdinate(e3, i2, t3.getOrdinate(n2, i2)), t3.setOrdinate(n2, i2, r2);
          }
        }, "value") }, { key: "copy", value: /* @__PURE__ */ __name(function(e3, n2, i2, r2, s2) {
          for (var a2 = 0; a2 < s2; a2++) t2.copyCoord(e3, n2 + a2, i2, r2 + a2);
        }, "value") }, { key: "ensureValidRing", value: /* @__PURE__ */ __name(function(e3, n2) {
          var i2 = n2.size();
          return 0 === i2 ? n2 : i2 <= 3 ? t2.createClosedRing(e3, n2, 4) : n2.getOrdinate(0, ht.X) === n2.getOrdinate(i2 - 1, ht.X) && n2.getOrdinate(0, ht.Y) === n2.getOrdinate(i2 - 1, ht.Y) ? n2 : t2.createClosedRing(e3, n2, i2 + 1);
        }, "value") }, { key: "indexOf", value: /* @__PURE__ */ __name(function(t3, e3) {
          for (var n2 = 0; n2 < e3.size(); n2++) if (t3.x === e3.getOrdinate(n2, ht.X) && t3.y === e3.getOrdinate(n2, ht.Y)) return n2;
          return -1;
        }, "value") }, { key: "createClosedRing", value: /* @__PURE__ */ __name(function(e3, n2, i2) {
          var r2 = e3.create(i2, n2.getDimension()), s2 = n2.size();
          t2.copy(n2, 0, r2, 0, s2);
          for (var a2 = s2; a2 < i2; a2++) t2.copy(n2, 0, r2, a2, 1);
          return r2;
        }, "value") }, { key: "minCoordinate", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = null, n2 = 0; n2 < t3.size(); n2++) {
            var i2 = t3.getCoordinate(n2);
            (null === e3 || e3.compareTo(i2) > 0) && (e3 = i2);
          }
          return e3;
        }, "value") }]);
      })(), Lt = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "toDimensionSymbol", value: /* @__PURE__ */ __name(function(e3) {
          switch (e3) {
            case t2.FALSE:
              return t2.SYM_FALSE;
            case t2.TRUE:
              return t2.SYM_TRUE;
            case t2.DONTCARE:
              return t2.SYM_DONTCARE;
            case t2.P:
              return t2.SYM_P;
            case t2.L:
              return t2.SYM_L;
            case t2.A:
              return t2.SYM_A;
          }
          throw new m("Unknown dimension value: " + e3);
        }, "value") }, { key: "toDimensionValue", value: /* @__PURE__ */ __name(function(e3) {
          switch (ot.toUpperCase(e3)) {
            case t2.SYM_FALSE:
              return t2.FALSE;
            case t2.SYM_TRUE:
              return t2.TRUE;
            case t2.SYM_DONTCARE:
              return t2.DONTCARE;
            case t2.SYM_P:
              return t2.P;
            case t2.SYM_L:
              return t2.L;
            case t2.SYM_A:
              return t2.A;
          }
          throw new m("Unknown dimension symbol: " + e3);
        }, "value") }]);
      })();
      Lt.P = 0, Lt.L = 1, Lt.A = 2, Lt.FALSE = -1, Lt.TRUE = -2, Lt.DONTCARE = -3, Lt.SYM_FALSE = "F", Lt.SYM_TRUE = "T", Lt.SYM_DONTCARE = "*", Lt.SYM_P = "0", Lt.SYM_L = "1", Lt.SYM_A = "2";
      var Ct = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "filter", value: /* @__PURE__ */ __name(function(t2) {
        }, "value") }]);
      })(), Rt = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "filter", value: /* @__PURE__ */ __name(function(t2, e3) {
        }, "value") }, { key: "isDone", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "isGeometryChanged", value: /* @__PURE__ */ __name(function() {
        }, "value") }]);
      })(), wt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "computeEnvelopeInternal", value: /* @__PURE__ */ __name(function() {
          return this.isEmpty() ? new U() : this._points.expandEnvelope(new U());
        }, "value") }, { key: "isRing", value: /* @__PURE__ */ __name(function() {
          return this.isClosed() && this.isSimple();
        }, "value") }, { key: "getCoordinates", value: /* @__PURE__ */ __name(function() {
          return this._points.toCoordinateArray();
        }, "value") }, { key: "copyInternal", value: /* @__PURE__ */ __name(function() {
          return new i2(this._points.copy(), this._factory);
        }, "value") }, { key: "equalsExact", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length && "number" == typeof arguments[1] && arguments[0] instanceof V) {
            var t3 = arguments[0], e3 = arguments[1];
            if (!this.isEquivalentClass(t3)) return false;
            var n2 = t3;
            if (this._points.size() !== n2._points.size()) return false;
            for (var r2 = 0; r2 < this._points.size(); r2++) if (!this.equal(this._points.getCoordinate(r2), n2._points.getCoordinate(r2), e3)) return false;
            return true;
          }
          return f(i2, "equalsExact", this, 1).apply(this, arguments);
        }, "value") }, { key: "normalize", value: /* @__PURE__ */ __name(function() {
          for (var t3 = 0; t3 < Math.trunc(this._points.size() / 2); t3++) {
            var e3 = this._points.size() - 1 - t3;
            if (!this._points.getCoordinate(t3).equals(this._points.getCoordinate(e3))) {
              if (this._points.getCoordinate(t3).compareTo(this._points.getCoordinate(e3)) > 0) {
                var n2 = this._points.copy();
                St.reverse(n2), this._points = n2;
              }
              return null;
            }
          }
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function() {
          return this.isEmpty() ? null : this._points.getCoordinate(0);
        }, "value") }, { key: "getBoundaryDimension", value: /* @__PURE__ */ __name(function() {
          return this.isClosed() ? Lt.FALSE : 0;
        }, "value") }, { key: "isClosed", value: /* @__PURE__ */ __name(function() {
          return !this.isEmpty() && this.getCoordinateN(0).equals2D(this.getCoordinateN(this.getNumPoints() - 1));
        }, "value") }, { key: "reverseInternal", value: /* @__PURE__ */ __name(function() {
          var t3 = this._points.copy();
          return St.reverse(t3), this.getFactory().createLineString(t3);
        }, "value") }, { key: "getEndPoint", value: /* @__PURE__ */ __name(function() {
          return this.isEmpty() ? null : this.getPointN(this.getNumPoints() - 1);
        }, "value") }, { key: "getTypeCode", value: /* @__PURE__ */ __name(function() {
          return V.TYPECODE_LINESTRING;
        }, "value") }, { key: "getDimension", value: /* @__PURE__ */ __name(function() {
          return 1;
        }, "value") }, { key: "getLength", value: /* @__PURE__ */ __name(function() {
          return Nt.ofLine(this._points);
        }, "value") }, { key: "getNumPoints", value: /* @__PURE__ */ __name(function() {
          return this._points.size();
        }, "value") }, { key: "compareToSameClass", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            for (var t3 = arguments[0], e3 = 0, n2 = 0; e3 < this._points.size() && n2 < t3._points.size(); ) {
              var i3 = this._points.getCoordinate(e3).compareTo(t3._points.getCoordinate(n2));
              if (0 !== i3) return i3;
              e3++, n2++;
            }
            return e3 < this._points.size() ? 1 : n2 < t3._points.size() ? -1 : 0;
          }
          if (2 === arguments.length) {
            var r2 = arguments[0];
            return arguments[1].compare(this._points, r2._points);
          }
        }, "value") }, { key: "apply", value: /* @__PURE__ */ __name(function() {
          if (rt(arguments[0], Et)) for (var t3 = arguments[0], e3 = 0; e3 < this._points.size(); e3++) t3.filter(this._points.getCoordinate(e3));
          else if (rt(arguments[0], Rt)) {
            var n2 = arguments[0];
            if (0 === this._points.size()) return null;
            for (var i3 = 0; i3 < this._points.size() && (n2.filter(this._points, i3), !n2.isDone()); i3++) ;
            n2.isGeometryChanged() && this.geometryChanged();
          } else if (rt(arguments[0], Ct)) {
            arguments[0].filter(this);
          } else if (rt(arguments[0], k)) {
            arguments[0].filter(this);
          }
        }, "value") }, { key: "getBoundary", value: /* @__PURE__ */ __name(function() {
          throw new W();
        }, "value") }, { key: "isEquivalentClass", value: /* @__PURE__ */ __name(function(t3) {
          return t3 instanceof i2;
        }, "value") }, { key: "getCoordinateN", value: /* @__PURE__ */ __name(function(t3) {
          return this._points.getCoordinate(t3);
        }, "value") }, { key: "getGeometryType", value: /* @__PURE__ */ __name(function() {
          return V.TYPENAME_LINESTRING;
        }, "value") }, { key: "getCoordinateSequence", value: /* @__PURE__ */ __name(function() {
          return this._points;
        }, "value") }, { key: "isEmpty", value: /* @__PURE__ */ __name(function() {
          return 0 === this._points.size();
        }, "value") }, { key: "init", value: /* @__PURE__ */ __name(function(t3) {
          if (null === t3 && (t3 = this.getFactory().getCoordinateSequenceFactory().create([])), 1 === t3.size()) throw new m("Invalid number of points in LineString (found " + t3.size() + " - must be 0 or >= 2)");
          this._points = t3;
        }, "value") }, { key: "isCoordinate", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = 0; e3 < this._points.size(); e3++) if (this._points.getCoordinate(e3).equals(t3)) return true;
          return false;
        }, "value") }, { key: "getStartPoint", value: /* @__PURE__ */ __name(function() {
          return this.isEmpty() ? null : this.getPointN(0);
        }, "value") }, { key: "getPointN", value: /* @__PURE__ */ __name(function(t3) {
          return this.getFactory().createPoint(this._points.getCoordinate(t3));
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [Tt];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._points = null, 0 === arguments.length) ;
          else if (2 === arguments.length) {
            var t3 = arguments[0], e3 = arguments[1];
            V.constructor_.call(this, e3), this.init(t3);
          }
        }, "value") }]);
      })(V), Ot = s(/* @__PURE__ */ __name((function t2() {
        n(this, t2);
      }), "t")), bt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "computeEnvelopeInternal", value: /* @__PURE__ */ __name(function() {
          if (this.isEmpty()) return new U();
          var t3 = new U();
          return t3.expandToInclude(this._coordinates.getX(0), this._coordinates.getY(0)), t3;
        }, "value") }, { key: "getCoordinates", value: /* @__PURE__ */ __name(function() {
          return this.isEmpty() ? [] : [this.getCoordinate()];
        }, "value") }, { key: "copyInternal", value: /* @__PURE__ */ __name(function() {
          return new i2(this._coordinates.copy(), this._factory);
        }, "value") }, { key: "equalsExact", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length && "number" == typeof arguments[1] && arguments[0] instanceof V) {
            var t3 = arguments[0], e3 = arguments[1];
            return !!this.isEquivalentClass(t3) && (!(!this.isEmpty() || !t3.isEmpty()) || this.isEmpty() === t3.isEmpty() && this.equal(t3.getCoordinate(), this.getCoordinate(), e3));
          }
          return f(i2, "equalsExact", this, 1).apply(this, arguments);
        }, "value") }, { key: "normalize", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function() {
          return 0 !== this._coordinates.size() ? this._coordinates.getCoordinate(0) : null;
        }, "value") }, { key: "getBoundaryDimension", value: /* @__PURE__ */ __name(function() {
          return Lt.FALSE;
        }, "value") }, { key: "reverseInternal", value: /* @__PURE__ */ __name(function() {
          return this.getFactory().createPoint(this._coordinates.copy());
        }, "value") }, { key: "getTypeCode", value: /* @__PURE__ */ __name(function() {
          return V.TYPECODE_POINT;
        }, "value") }, { key: "getDimension", value: /* @__PURE__ */ __name(function() {
          return 0;
        }, "value") }, { key: "getNumPoints", value: /* @__PURE__ */ __name(function() {
          return this.isEmpty() ? 0 : 1;
        }, "value") }, { key: "getX", value: /* @__PURE__ */ __name(function() {
          if (null === this.getCoordinate()) throw new IllegalStateException("getX called on empty Point");
          return this.getCoordinate().x;
        }, "value") }, { key: "compareToSameClass", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return this.getCoordinate().compareTo(t3.getCoordinate());
          }
          if (2 === arguments.length) {
            var e3 = arguments[0];
            return arguments[1].compare(this._coordinates, e3._coordinates);
          }
        }, "value") }, { key: "apply", value: /* @__PURE__ */ __name(function() {
          if (rt(arguments[0], Et)) {
            var t3 = arguments[0];
            if (this.isEmpty()) return null;
            t3.filter(this.getCoordinate());
          } else if (rt(arguments[0], Rt)) {
            var e3 = arguments[0];
            if (this.isEmpty()) return null;
            e3.filter(this._coordinates, 0), e3.isGeometryChanged() && this.geometryChanged();
          } else if (rt(arguments[0], Ct)) {
            arguments[0].filter(this);
          } else if (rt(arguments[0], k)) {
            arguments[0].filter(this);
          }
        }, "value") }, { key: "getBoundary", value: /* @__PURE__ */ __name(function() {
          return this.getFactory().createGeometryCollection();
        }, "value") }, { key: "getGeometryType", value: /* @__PURE__ */ __name(function() {
          return V.TYPENAME_POINT;
        }, "value") }, { key: "getCoordinateSequence", value: /* @__PURE__ */ __name(function() {
          return this._coordinates;
        }, "value") }, { key: "getY", value: /* @__PURE__ */ __name(function() {
          if (null === this.getCoordinate()) throw new IllegalStateException("getY called on empty Point");
          return this.getCoordinate().y;
        }, "value") }, { key: "isEmpty", value: /* @__PURE__ */ __name(function() {
          return 0 === this._coordinates.size();
        }, "value") }, { key: "init", value: /* @__PURE__ */ __name(function(t3) {
          null === t3 && (t3 = this.getFactory().getCoordinateSequenceFactory().create([])), G.isTrue(t3.size() <= 1), this._coordinates = t3;
        }, "value") }, { key: "isSimple", value: /* @__PURE__ */ __name(function() {
          return true;
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [Ot];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._coordinates = null;
          var t3 = arguments[0], e3 = arguments[1];
          V.constructor_.call(this, e3), this.init(t3);
        }, "value") }]);
      })(V), Mt = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "ofRing", value: /* @__PURE__ */ __name(function() {
          if (arguments[0] instanceof Array) {
            var e3 = arguments[0];
            return Math.abs(t2.ofRingSigned(e3));
          }
          if (rt(arguments[0], ht)) {
            var n2 = arguments[0];
            return Math.abs(t2.ofRingSigned(n2));
          }
        }, "value") }, { key: "ofRingSigned", value: /* @__PURE__ */ __name(function() {
          if (arguments[0] instanceof Array) {
            var t3 = arguments[0];
            if (t3.length < 3) return 0;
            for (var e3 = 0, n2 = t3[0].x, i2 = 1; i2 < t3.length - 1; i2++) {
              var r2 = t3[i2].x - n2, s2 = t3[i2 + 1].y;
              e3 += r2 * (t3[i2 - 1].y - s2);
            }
            return e3 / 2;
          }
          if (rt(arguments[0], ht)) {
            var a2 = arguments[0], o2 = a2.size();
            if (o2 < 3) return 0;
            var u2 = new X(), l2 = new X(), h2 = new X();
            a2.getCoordinate(0, l2), a2.getCoordinate(1, h2);
            var c2 = l2.x;
            h2.x -= c2;
            for (var f2 = 0, g2 = 1; g2 < o2 - 1; g2++) u2.y = l2.y, l2.x = h2.x, l2.y = h2.y, a2.getCoordinate(g2 + 1, h2), h2.x -= c2, f2 += l2.x * (u2.y - h2.y);
            return f2 / 2;
          }
        }, "value") }]);
      })(), At = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), null, [{ key: "sort", value: /* @__PURE__ */ __name(function() {
          var t2 = arguments, e3 = arguments[0];
          if (1 === arguments.length) e3.sort((function(t3, e4) {
            return t3.compareTo(e4);
          }));
          else if (2 === arguments.length) e3.sort((function(e4, n3) {
            return t2[1].compare(e4, n3);
          }));
          else if (3 === arguments.length) {
            var n2 = e3.slice(arguments[1], arguments[2]);
            n2.sort();
            var i2 = e3.slice(0, arguments[1]).concat(n2, e3.slice(arguments[2], e3.length));
            e3.splice(0, e3.length);
            var r2, s2 = a(i2);
            try {
              for (s2.s(); !(r2 = s2.n()).done; ) {
                var o2 = r2.value;
                e3.push(o2);
              }
            } catch (t3) {
              s2.e(t3);
            } finally {
              s2.f();
            }
          } else if (4 === arguments.length) {
            var u2 = e3.slice(arguments[1], arguments[2]);
            u2.sort((function(e4, n3) {
              return t2[3].compare(e4, n3);
            }));
            var l2 = e3.slice(0, arguments[1]).concat(u2, e3.slice(arguments[2], e3.length));
            e3.splice(0, e3.length);
            var h2, c2 = a(l2);
            try {
              for (c2.s(); !(h2 = c2.n()).done; ) {
                var f2 = h2.value;
                e3.push(f2);
              }
            } catch (t3) {
              c2.e(t3);
            } finally {
              c2.f();
            }
          }
        }, "value") }, { key: "asList", value: /* @__PURE__ */ __name(function(t2) {
          var e3, n2 = new yt(), i2 = a(t2);
          try {
            for (i2.s(); !(e3 = i2.n()).done; ) {
              var r2 = e3.value;
              n2.add(r2);
            }
          } catch (t3) {
            i2.e(t3);
          } finally {
            i2.f();
          }
          return n2;
        }, "value") }, { key: "copyOf", value: /* @__PURE__ */ __name(function(t2, e3) {
          return t2.slice(0, e3);
        }, "value") }]);
      })(), Pt = s(/* @__PURE__ */ __name((function t2() {
        n(this, t2);
      }), "t")), Dt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "computeEnvelopeInternal", value: /* @__PURE__ */ __name(function() {
          return this._shell.getEnvelopeInternal();
        }, "value") }, { key: "getCoordinates", value: /* @__PURE__ */ __name(function() {
          if (this.isEmpty()) return [];
          for (var t3 = new Array(this.getNumPoints()).fill(null), e3 = -1, n2 = this._shell.getCoordinates(), i3 = 0; i3 < n2.length; i3++) t3[++e3] = n2[i3];
          for (var r2 = 0; r2 < this._holes.length; r2++) for (var s2 = this._holes[r2].getCoordinates(), a2 = 0; a2 < s2.length; a2++) t3[++e3] = s2[a2];
          return t3;
        }, "value") }, { key: "getArea", value: /* @__PURE__ */ __name(function() {
          var t3 = 0;
          t3 += Mt.ofRing(this._shell.getCoordinateSequence());
          for (var e3 = 0; e3 < this._holes.length; e3++) t3 -= Mt.ofRing(this._holes[e3].getCoordinateSequence());
          return t3;
        }, "value") }, { key: "copyInternal", value: /* @__PURE__ */ __name(function() {
          for (var t3 = this._shell.copy(), e3 = new Array(this._holes.length).fill(null), n2 = 0; n2 < this._holes.length; n2++) e3[n2] = this._holes[n2].copy();
          return new i2(t3, e3, this._factory);
        }, "value") }, { key: "isRectangle", value: /* @__PURE__ */ __name(function() {
          if (0 !== this.getNumInteriorRing()) return false;
          if (null === this._shell) return false;
          if (5 !== this._shell.getNumPoints()) return false;
          for (var t3 = this._shell.getCoordinateSequence(), e3 = this.getEnvelopeInternal(), n2 = 0; n2 < 5; n2++) {
            var i3 = t3.getX(n2);
            if (i3 !== e3.getMinX() && i3 !== e3.getMaxX()) return false;
            var r2 = t3.getY(n2);
            if (r2 !== e3.getMinY() && r2 !== e3.getMaxY()) return false;
          }
          for (var s2 = t3.getX(0), a2 = t3.getY(0), o2 = 1; o2 <= 4; o2++) {
            var u2 = t3.getX(o2), l2 = t3.getY(o2);
            if (u2 !== s2 === (l2 !== a2)) return false;
            s2 = u2, a2 = l2;
          }
          return true;
        }, "value") }, { key: "equalsExact", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length && "number" == typeof arguments[1] && arguments[0] instanceof V) {
            var t3 = arguments[0], e3 = arguments[1];
            if (!this.isEquivalentClass(t3)) return false;
            var n2 = t3, r2 = this._shell, s2 = n2._shell;
            if (!r2.equalsExact(s2, e3)) return false;
            if (this._holes.length !== n2._holes.length) return false;
            for (var a2 = 0; a2 < this._holes.length; a2++) if (!this._holes[a2].equalsExact(n2._holes[a2], e3)) return false;
            return true;
          }
          return f(i2, "equalsExact", this, 1).apply(this, arguments);
        }, "value") }, { key: "normalize", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) {
            this._shell = this.normalized(this._shell, true);
            for (var t3 = 0; t3 < this._holes.length; t3++) this._holes[t3] = this.normalized(this._holes[t3], false);
            At.sort(this._holes);
          } else if (2 === arguments.length) {
            var e3 = arguments[0], n2 = arguments[1];
            if (e3.isEmpty()) return null;
            var i3 = e3.getCoordinateSequence(), r2 = St.minCoordinateIndex(i3, 0, i3.size() - 2);
            St.scroll(i3, r2, true), ct.isCCW(i3) === n2 && St.reverse(i3);
          }
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function() {
          return this._shell.getCoordinate();
        }, "value") }, { key: "getNumInteriorRing", value: /* @__PURE__ */ __name(function() {
          return this._holes.length;
        }, "value") }, { key: "getBoundaryDimension", value: /* @__PURE__ */ __name(function() {
          return 1;
        }, "value") }, { key: "reverseInternal", value: /* @__PURE__ */ __name(function() {
          for (var t3 = this.getExteriorRing().reverse(), e3 = new Array(this.getNumInteriorRing()).fill(null), n2 = 0; n2 < e3.length; n2++) e3[n2] = this.getInteriorRingN(n2).reverse();
          return this.getFactory().createPolygon(t3, e3);
        }, "value") }, { key: "getTypeCode", value: /* @__PURE__ */ __name(function() {
          return V.TYPECODE_POLYGON;
        }, "value") }, { key: "getDimension", value: /* @__PURE__ */ __name(function() {
          return 2;
        }, "value") }, { key: "getLength", value: /* @__PURE__ */ __name(function() {
          var t3 = 0;
          t3 += this._shell.getLength();
          for (var e3 = 0; e3 < this._holes.length; e3++) t3 += this._holes[e3].getLength();
          return t3;
        }, "value") }, { key: "getNumPoints", value: /* @__PURE__ */ __name(function() {
          for (var t3 = this._shell.getNumPoints(), e3 = 0; e3 < this._holes.length; e3++) t3 += this._holes[e3].getNumPoints();
          return t3;
        }, "value") }, { key: "convexHull", value: /* @__PURE__ */ __name(function() {
          return this.getExteriorRing().convexHull();
        }, "value") }, { key: "normalized", value: /* @__PURE__ */ __name(function(t3, e3) {
          var n2 = t3.copy();
          return this.normalize(n2, e3), n2;
        }, "value") }, { key: "compareToSameClass", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t3 = arguments[0], e3 = this._shell, n2 = t3._shell;
            return e3.compareToSameClass(n2);
          }
          if (2 === arguments.length) {
            var i3 = arguments[1], r2 = arguments[0], s2 = this._shell, a2 = r2._shell, o2 = s2.compareToSameClass(a2, i3);
            if (0 !== o2) return o2;
            for (var u2 = this.getNumInteriorRing(), l2 = r2.getNumInteriorRing(), h2 = 0; h2 < u2 && h2 < l2; ) {
              var c2 = this.getInteriorRingN(h2), f2 = r2.getInteriorRingN(h2), g2 = c2.compareToSameClass(f2, i3);
              if (0 !== g2) return g2;
              h2++;
            }
            return h2 < u2 ? 1 : h2 < l2 ? -1 : 0;
          }
        }, "value") }, { key: "apply", value: /* @__PURE__ */ __name(function() {
          if (rt(arguments[0], Et)) {
            var t3 = arguments[0];
            this._shell.apply(t3);
            for (var e3 = 0; e3 < this._holes.length; e3++) this._holes[e3].apply(t3);
          } else if (rt(arguments[0], Rt)) {
            var n2 = arguments[0];
            if (this._shell.apply(n2), !n2.isDone()) for (var i3 = 0; i3 < this._holes.length && (this._holes[i3].apply(n2), !n2.isDone()); i3++) ;
            n2.isGeometryChanged() && this.geometryChanged();
          } else if (rt(arguments[0], Ct)) {
            arguments[0].filter(this);
          } else if (rt(arguments[0], k)) {
            var r2 = arguments[0];
            r2.filter(this), this._shell.apply(r2);
            for (var s2 = 0; s2 < this._holes.length; s2++) this._holes[s2].apply(r2);
          }
        }, "value") }, { key: "getBoundary", value: /* @__PURE__ */ __name(function() {
          if (this.isEmpty()) return this.getFactory().createMultiLineString();
          var t3 = new Array(this._holes.length + 1).fill(null);
          t3[0] = this._shell;
          for (var e3 = 0; e3 < this._holes.length; e3++) t3[e3 + 1] = this._holes[e3];
          return t3.length <= 1 ? this.getFactory().createLinearRing(t3[0].getCoordinateSequence()) : this.getFactory().createMultiLineString(t3);
        }, "value") }, { key: "getGeometryType", value: /* @__PURE__ */ __name(function() {
          return V.TYPENAME_POLYGON;
        }, "value") }, { key: "getExteriorRing", value: /* @__PURE__ */ __name(function() {
          return this._shell;
        }, "value") }, { key: "isEmpty", value: /* @__PURE__ */ __name(function() {
          return this._shell.isEmpty();
        }, "value") }, { key: "getInteriorRingN", value: /* @__PURE__ */ __name(function(t3) {
          return this._holes[t3];
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [Pt];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._shell = null, this._holes = null;
          var t3 = arguments[0], e3 = arguments[1], n2 = arguments[2];
          if (V.constructor_.call(this, n2), null === t3 && (t3 = this.getFactory().createLinearRing()), null === e3 && (e3 = []), V.hasNullElements(e3)) throw new m("holes must not contain null elements");
          if (t3.isEmpty() && V.hasNonEmptyElements(e3)) throw new m("shell is empty but holes are not");
          this._shell = t3, this._holes = e3;
        }, "value") }]);
      })(V), Ft = (function(t2) {
        function i2() {
          return n(this, i2), e(this, i2, arguments);
        }
        __name(i2, "i");
        return l(i2, t2), s(i2);
      })(K), Gt = (function(t2) {
        function i2(t3) {
          var r2;
          return n(this, i2), (r2 = e(this, i2)).array = [], t3 instanceof Z && r2.addAll(t3), r2;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "contains", value: /* @__PURE__ */ __name(function(t3) {
          var e3, n2 = a(this.array);
          try {
            for (n2.s(); !(e3 = n2.n()).done; ) {
              if (0 === e3.value.compareTo(t3)) return true;
            }
          } catch (t4) {
            n2.e(t4);
          } finally {
            n2.f();
          }
          return false;
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function(t3) {
          if (this.contains(t3)) return false;
          for (var e3 = 0, n2 = this.array.length; e3 < n2; e3++) {
            if (1 === this.array[e3].compareTo(t3)) return !!this.array.splice(e3, 0, t3);
          }
          return this.array.push(t3), true;
        }, "value") }, { key: "addAll", value: /* @__PURE__ */ __name(function(t3) {
          var e3, n2 = a(t3);
          try {
            for (n2.s(); !(e3 = n2.n()).done; ) {
              var i3 = e3.value;
              this.add(i3);
            }
          } catch (t4) {
            n2.e(t4);
          } finally {
            n2.f();
          }
          return true;
        }, "value") }, { key: "remove", value: /* @__PURE__ */ __name(function() {
          throw new W();
        }, "value") }, { key: "size", value: /* @__PURE__ */ __name(function() {
          return this.array.length;
        }, "value") }, { key: "isEmpty", value: /* @__PURE__ */ __name(function() {
          return 0 === this.array.length;
        }, "value") }, { key: "toArray", value: /* @__PURE__ */ __name(function() {
          return this.array.slice();
        }, "value") }, { key: "iterator", value: /* @__PURE__ */ __name(function() {
          return new qt(this.array);
        }, "value") }]);
      })(Ft), qt = (function() {
        return s(/* @__PURE__ */ __name((function t2(e3) {
          n(this, t2), this.array = e3, this.position = 0;
        }), "t"), [{ key: "next", value: /* @__PURE__ */ __name(function() {
          if (this.position === this.array.length) throw new j();
          return this.array[this.position++];
        }, "value") }, { key: "hasNext", value: /* @__PURE__ */ __name(function() {
          return this.position < this.array.length;
        }, "value") }, { key: "remove", value: /* @__PURE__ */ __name(function() {
          throw new W();
        }, "value") }]);
      })(), Yt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "computeEnvelopeInternal", value: /* @__PURE__ */ __name(function() {
          for (var t3 = new U(), e3 = 0; e3 < this._geometries.length; e3++) t3.expandToInclude(this._geometries[e3].getEnvelopeInternal());
          return t3;
        }, "value") }, { key: "getGeometryN", value: /* @__PURE__ */ __name(function(t3) {
          return this._geometries[t3];
        }, "value") }, { key: "getCoordinates", value: /* @__PURE__ */ __name(function() {
          for (var t3 = new Array(this.getNumPoints()).fill(null), e3 = -1, n2 = 0; n2 < this._geometries.length; n2++) for (var i3 = this._geometries[n2].getCoordinates(), r2 = 0; r2 < i3.length; r2++) t3[++e3] = i3[r2];
          return t3;
        }, "value") }, { key: "getArea", value: /* @__PURE__ */ __name(function() {
          for (var t3 = 0, e3 = 0; e3 < this._geometries.length; e3++) t3 += this._geometries[e3].getArea();
          return t3;
        }, "value") }, { key: "copyInternal", value: /* @__PURE__ */ __name(function() {
          for (var t3 = new Array(this._geometries.length).fill(null), e3 = 0; e3 < t3.length; e3++) t3[e3] = this._geometries[e3].copy();
          return new i2(t3, this._factory);
        }, "value") }, { key: "equalsExact", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length && "number" == typeof arguments[1] && arguments[0] instanceof V) {
            var t3 = arguments[0], e3 = arguments[1];
            if (!this.isEquivalentClass(t3)) return false;
            var n2 = t3;
            if (this._geometries.length !== n2._geometries.length) return false;
            for (var r2 = 0; r2 < this._geometries.length; r2++) if (!this._geometries[r2].equalsExact(n2._geometries[r2], e3)) return false;
            return true;
          }
          return f(i2, "equalsExact", this, 1).apply(this, arguments);
        }, "value") }, { key: "normalize", value: /* @__PURE__ */ __name(function() {
          for (var t3 = 0; t3 < this._geometries.length; t3++) this._geometries[t3].normalize();
          At.sort(this._geometries);
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function() {
          return this.isEmpty() ? null : this._geometries[0].getCoordinate();
        }, "value") }, { key: "getBoundaryDimension", value: /* @__PURE__ */ __name(function() {
          for (var t3 = Lt.FALSE, e3 = 0; e3 < this._geometries.length; e3++) t3 = Math.max(t3, this._geometries[e3].getBoundaryDimension());
          return t3;
        }, "value") }, { key: "reverseInternal", value: /* @__PURE__ */ __name(function() {
          for (var t3 = this._geometries.length, e3 = new yt(t3), n2 = 0; n2 < t3; n2++) e3.add(this._geometries[n2].reverse());
          return this.getFactory().buildGeometry(e3);
        }, "value") }, { key: "getTypeCode", value: /* @__PURE__ */ __name(function() {
          return V.TYPECODE_GEOMETRYCOLLECTION;
        }, "value") }, { key: "getDimension", value: /* @__PURE__ */ __name(function() {
          for (var t3 = Lt.FALSE, e3 = 0; e3 < this._geometries.length; e3++) t3 = Math.max(t3, this._geometries[e3].getDimension());
          return t3;
        }, "value") }, { key: "getLength", value: /* @__PURE__ */ __name(function() {
          for (var t3 = 0, e3 = 0; e3 < this._geometries.length; e3++) t3 += this._geometries[e3].getLength();
          return t3;
        }, "value") }, { key: "getNumPoints", value: /* @__PURE__ */ __name(function() {
          for (var t3 = 0, e3 = 0; e3 < this._geometries.length; e3++) t3 += this._geometries[e3].getNumPoints();
          return t3;
        }, "value") }, { key: "getNumGeometries", value: /* @__PURE__ */ __name(function() {
          return this._geometries.length;
        }, "value") }, { key: "compareToSameClass", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t3 = arguments[0], e3 = new Gt(At.asList(this._geometries)), n2 = new Gt(At.asList(t3._geometries));
            return this.compare(e3, n2);
          }
          if (2 === arguments.length) {
            for (var i3 = arguments[1], r2 = arguments[0], s2 = this.getNumGeometries(), a2 = r2.getNumGeometries(), o2 = 0; o2 < s2 && o2 < a2; ) {
              var u2 = this.getGeometryN(o2), l2 = r2.getGeometryN(o2), h2 = u2.compareToSameClass(l2, i3);
              if (0 !== h2) return h2;
              o2++;
            }
            return o2 < s2 ? 1 : o2 < a2 ? -1 : 0;
          }
        }, "value") }, { key: "apply", value: /* @__PURE__ */ __name(function() {
          if (rt(arguments[0], Et)) for (var t3 = arguments[0], e3 = 0; e3 < this._geometries.length; e3++) this._geometries[e3].apply(t3);
          else if (rt(arguments[0], Rt)) {
            var n2 = arguments[0];
            if (0 === this._geometries.length) return null;
            for (var i3 = 0; i3 < this._geometries.length && (this._geometries[i3].apply(n2), !n2.isDone()); i3++) ;
            n2.isGeometryChanged() && this.geometryChanged();
          } else if (rt(arguments[0], Ct)) {
            var r2 = arguments[0];
            r2.filter(this);
            for (var s2 = 0; s2 < this._geometries.length; s2++) this._geometries[s2].apply(r2);
          } else if (rt(arguments[0], k)) {
            var a2 = arguments[0];
            a2.filter(this);
            for (var o2 = 0; o2 < this._geometries.length; o2++) this._geometries[o2].apply(a2);
          }
        }, "value") }, { key: "getBoundary", value: /* @__PURE__ */ __name(function() {
          return V.checkNotGeometryCollection(this), G.shouldNeverReachHere(), null;
        }, "value") }, { key: "getGeometryType", value: /* @__PURE__ */ __name(function() {
          return V.TYPENAME_GEOMETRYCOLLECTION;
        }, "value") }, { key: "isEmpty", value: /* @__PURE__ */ __name(function() {
          for (var t3 = 0; t3 < this._geometries.length; t3++) if (!this._geometries[t3].isEmpty()) return false;
          return true;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._geometries = null, 0 === arguments.length) ;
          else if (2 === arguments.length) {
            var t3 = arguments[0], e3 = arguments[1];
            if (V.constructor_.call(this, e3), null === t3 && (t3 = []), V.hasNullElements(t3)) throw new m("geometries must not contain null elements");
            this._geometries = t3;
          }
        }, "value") }]);
      })(V), zt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "copyInternal", value: /* @__PURE__ */ __name(function() {
          for (var t3 = new Array(this._geometries.length).fill(null), e3 = 0; e3 < t3.length; e3++) t3[e3] = this._geometries[e3].copy();
          return new i2(t3, this._factory);
        }, "value") }, { key: "isValid", value: /* @__PURE__ */ __name(function() {
          return true;
        }, "value") }, { key: "equalsExact", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length && "number" == typeof arguments[1] && arguments[0] instanceof V) {
            var t3 = arguments[0], e3 = arguments[1];
            return !!this.isEquivalentClass(t3) && f(i2, "equalsExact", this, 1).call(this, t3, e3);
          }
          return f(i2, "equalsExact", this, 1).apply(this, arguments);
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length && Number.isInteger(arguments[0])) {
            var t3 = arguments[0];
            return this._geometries[t3].getCoordinate();
          }
          return f(i2, "getCoordinate", this, 1).apply(this, arguments);
        }, "value") }, { key: "getBoundaryDimension", value: /* @__PURE__ */ __name(function() {
          return Lt.FALSE;
        }, "value") }, { key: "getTypeCode", value: /* @__PURE__ */ __name(function() {
          return V.TYPECODE_MULTIPOINT;
        }, "value") }, { key: "getDimension", value: /* @__PURE__ */ __name(function() {
          return 0;
        }, "value") }, { key: "getBoundary", value: /* @__PURE__ */ __name(function() {
          return this.getFactory().createGeometryCollection();
        }, "value") }, { key: "getGeometryType", value: /* @__PURE__ */ __name(function() {
          return V.TYPENAME_MULTIPOINT;
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [Ot];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          var t3 = arguments[0], e3 = arguments[1];
          Yt.constructor_.call(this, t3, e3);
        }, "value") }]);
      })(Yt), Xt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "copyInternal", value: /* @__PURE__ */ __name(function() {
          return new i2(this._points.copy(), this._factory);
        }, "value") }, { key: "getBoundaryDimension", value: /* @__PURE__ */ __name(function() {
          return Lt.FALSE;
        }, "value") }, { key: "isClosed", value: /* @__PURE__ */ __name(function() {
          return !!this.isEmpty() || f(i2, "isClosed", this, 1).call(this);
        }, "value") }, { key: "reverseInternal", value: /* @__PURE__ */ __name(function() {
          var t3 = this._points.copy();
          return St.reverse(t3), this.getFactory().createLinearRing(t3);
        }, "value") }, { key: "getTypeCode", value: /* @__PURE__ */ __name(function() {
          return V.TYPECODE_LINEARRING;
        }, "value") }, { key: "validateConstruction", value: /* @__PURE__ */ __name(function() {
          if (!this.isEmpty() && !f(i2, "isClosed", this, 1).call(this)) throw new m("Points of LinearRing do not form a closed linestring");
          if (this.getCoordinateSequence().size() >= 1 && this.getCoordinateSequence().size() < i2.MINIMUM_VALID_SIZE) throw new m("Invalid number of points in LinearRing (found " + this.getCoordinateSequence().size() + " - must be 0 or >= 4)");
        }, "value") }, { key: "getGeometryType", value: /* @__PURE__ */ __name(function() {
          return V.TYPENAME_LINEARRING;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          var t3 = arguments[0], e3 = arguments[1];
          wt.constructor_.call(this, t3, e3), this.validateConstruction();
        }, "value") }]);
      })(wt);
      Xt.MINIMUM_VALID_SIZE = 4;
      var Bt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "setOrdinate", value: /* @__PURE__ */ __name(function(t3, e3) {
          switch (t3) {
            case i2.X:
              this.x = e3;
              break;
            case i2.Y:
              this.y = e3;
              break;
            default:
              throw new m("Invalid ordinate index: " + t3);
          }
        }, "value") }, { key: "getZ", value: /* @__PURE__ */ __name(function() {
          return X.NULL_ORDINATE;
        }, "value") }, { key: "getOrdinate", value: /* @__PURE__ */ __name(function(t3) {
          switch (t3) {
            case i2.X:
              return this.x;
            case i2.Y:
              return this.y;
          }
          throw new m("Invalid ordinate index: " + t3);
        }, "value") }, { key: "setZ", value: /* @__PURE__ */ __name(function(t3) {
          throw new m("CoordinateXY dimension 2 does not support z-ordinate");
        }, "value") }, { key: "copy", value: /* @__PURE__ */ __name(function() {
          return new i2(this);
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          return "(" + this.x + ", " + this.y + ")";
        }, "value") }, { key: "setCoordinate", value: /* @__PURE__ */ __name(function(t3) {
          this.x = t3.x, this.y = t3.y, this.z = t3.getZ();
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) X.constructor_.call(this);
          else if (1 === arguments.length) {
            if (arguments[0] instanceof i2) {
              var t3 = arguments[0];
              X.constructor_.call(this, t3.x, t3.y);
            } else if (arguments[0] instanceof X) {
              var e3 = arguments[0];
              X.constructor_.call(this, e3.x, e3.y);
            }
          } else if (2 === arguments.length) {
            var n2 = arguments[0], r2 = arguments[1];
            X.constructor_.call(this, n2, r2, X.NULL_ORDINATE);
          }
        }, "value") }]);
      })(X);
      Bt.X = 0, Bt.Y = 1, Bt.Z = -1, Bt.M = -1;
      var Ut = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "getM", value: /* @__PURE__ */ __name(function() {
          return this._m;
        }, "value") }, { key: "setOrdinate", value: /* @__PURE__ */ __name(function(t3, e3) {
          switch (t3) {
            case i2.X:
              this.x = e3;
              break;
            case i2.Y:
              this.y = e3;
              break;
            case i2.M:
              this._m = e3;
              break;
            default:
              throw new m("Invalid ordinate index: " + t3);
          }
        }, "value") }, { key: "setM", value: /* @__PURE__ */ __name(function(t3) {
          this._m = t3;
        }, "value") }, { key: "getZ", value: /* @__PURE__ */ __name(function() {
          return X.NULL_ORDINATE;
        }, "value") }, { key: "getOrdinate", value: /* @__PURE__ */ __name(function(t3) {
          switch (t3) {
            case i2.X:
              return this.x;
            case i2.Y:
              return this.y;
            case i2.M:
              return this._m;
          }
          throw new m("Invalid ordinate index: " + t3);
        }, "value") }, { key: "setZ", value: /* @__PURE__ */ __name(function(t3) {
          throw new m("CoordinateXY dimension 2 does not support z-ordinate");
        }, "value") }, { key: "copy", value: /* @__PURE__ */ __name(function() {
          return new i2(this);
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          return "(" + this.x + ", " + this.y + " m=" + this.getM() + ")";
        }, "value") }, { key: "setCoordinate", value: /* @__PURE__ */ __name(function(t3) {
          this.x = t3.x, this.y = t3.y, this.z = t3.getZ(), this._m = t3.getM();
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._m = null, 0 === arguments.length) X.constructor_.call(this), this._m = 0;
          else if (1 === arguments.length) {
            if (arguments[0] instanceof i2) {
              var t3 = arguments[0];
              X.constructor_.call(this, t3.x, t3.y), this._m = t3._m;
            } else if (arguments[0] instanceof X) {
              var e3 = arguments[0];
              X.constructor_.call(this, e3.x, e3.y), this._m = this.getM();
            }
          } else if (3 === arguments.length) {
            var n2 = arguments[0], r2 = arguments[1], s2 = arguments[2];
            X.constructor_.call(this, n2, r2, X.NULL_ORDINATE), this._m = s2;
          }
        }, "value") }]);
      })(X);
      Ut.X = 0, Ut.Y = 1, Ut.Z = -1, Ut.M = 2;
      var Vt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "getM", value: /* @__PURE__ */ __name(function() {
          return this._m;
        }, "value") }, { key: "setOrdinate", value: /* @__PURE__ */ __name(function(t3, e3) {
          switch (t3) {
            case X.X:
              this.x = e3;
              break;
            case X.Y:
              this.y = e3;
              break;
            case X.Z:
              this.z = e3;
              break;
            case X.M:
              this._m = e3;
              break;
            default:
              throw new m("Invalid ordinate index: " + t3);
          }
        }, "value") }, { key: "setM", value: /* @__PURE__ */ __name(function(t3) {
          this._m = t3;
        }, "value") }, { key: "getOrdinate", value: /* @__PURE__ */ __name(function(t3) {
          switch (t3) {
            case X.X:
              return this.x;
            case X.Y:
              return this.y;
            case X.Z:
              return this.getZ();
            case X.M:
              return this.getM();
          }
          throw new m("Invalid ordinate index: " + t3);
        }, "value") }, { key: "copy", value: /* @__PURE__ */ __name(function() {
          return new i2(this);
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          return "(" + this.x + ", " + this.y + ", " + this.getZ() + " m=" + this.getM() + ")";
        }, "value") }, { key: "setCoordinate", value: /* @__PURE__ */ __name(function(t3) {
          this.x = t3.x, this.y = t3.y, this.z = t3.getZ(), this._m = t3.getM();
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._m = null, 0 === arguments.length) X.constructor_.call(this), this._m = 0;
          else if (1 === arguments.length) {
            if (arguments[0] instanceof i2) {
              var t3 = arguments[0];
              X.constructor_.call(this, t3), this._m = t3._m;
            } else if (arguments[0] instanceof X) {
              var e3 = arguments[0];
              X.constructor_.call(this, e3), this._m = this.getM();
            }
          } else if (4 === arguments.length) {
            var n2 = arguments[0], r2 = arguments[1], s2 = arguments[2], a2 = arguments[3];
            X.constructor_.call(this, n2, r2, s2), this._m = a2;
          }
        }, "value") }]);
      })(X), Ht = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "measures", value: /* @__PURE__ */ __name(function(t3) {
          return t3 instanceof Bt ? 0 : t3 instanceof Ut || t3 instanceof Vt ? 1 : 0;
        }, "value") }, { key: "dimension", value: /* @__PURE__ */ __name(function(t3) {
          return t3 instanceof Bt ? 2 : t3 instanceof Ut ? 3 : t3 instanceof Vt ? 4 : 3;
        }, "value") }, { key: "create", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var e3 = arguments[0];
            return t2.create(e3, 0);
          }
          if (2 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1];
            return 2 === n2 ? new Bt() : 3 === n2 && 0 === i2 ? new X() : 3 === n2 && 1 === i2 ? new Ut() : 4 === n2 && 1 === i2 ? new Vt() : new X();
          }
        }, "value") }]);
      })(), Zt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "getCoordinate", value: /* @__PURE__ */ __name(function(t3) {
          return this.get(t3);
        }, "value") }, { key: "addAll", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length && "boolean" == typeof arguments[1] && rt(arguments[0], Z)) {
            for (var t3 = arguments[1], e3 = false, n2 = arguments[0].iterator(); n2.hasNext(); ) this.add(n2.next(), t3), e3 = true;
            return e3;
          }
          return f(i2, "addAll", this, 1).apply(this, arguments);
        }, "value") }, { key: "clone", value: /* @__PURE__ */ __name(function() {
          for (var t3 = f(i2, "clone", this, 1).call(this), e3 = 0; e3 < this.size(); e3++) t3.add(e3, this.get(e3).clone());
          return t3;
        }, "value") }, { key: "toCoordinateArray", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) return this.toArray(i2.coordArrayType);
          if (1 === arguments.length) {
            if (arguments[0]) return this.toArray(i2.coordArrayType);
            for (var t3 = this.size(), e3 = new Array(t3).fill(null), n2 = 0; n2 < t3; n2++) e3[n2] = this.get(t3 - n2 - 1);
            return e3;
          }
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return f(i2, "add", this, 1).call(this, t3);
          }
          if (2 === arguments.length) {
            if (arguments[0] instanceof Array && "boolean" == typeof arguments[1]) {
              var e3 = arguments[0], n2 = arguments[1];
              return this.add(e3, n2, true), true;
            }
            if (arguments[0] instanceof X && "boolean" == typeof arguments[1]) {
              var r2 = arguments[0];
              if (!arguments[1] && this.size() >= 1) {
                if (this.get(this.size() - 1).equals2D(r2)) return null;
              }
              f(i2, "add", this, 1).call(this, r2);
            } else if (arguments[0] instanceof Object && "boolean" == typeof arguments[1]) {
              var s2 = arguments[0], a2 = arguments[1];
              return this.add(s2, a2), true;
            }
          } else if (3 === arguments.length) {
            if ("boolean" == typeof arguments[2] && arguments[0] instanceof Array && "boolean" == typeof arguments[1]) {
              var o2 = arguments[0], u2 = arguments[1];
              if (arguments[2]) for (var l2 = 0; l2 < o2.length; l2++) this.add(o2[l2], u2);
              else for (var h2 = o2.length - 1; h2 >= 0; h2--) this.add(o2[h2], u2);
              return true;
            }
            if ("boolean" == typeof arguments[2] && Number.isInteger(arguments[0]) && arguments[1] instanceof X) {
              var c2 = arguments[0], g2 = arguments[1];
              if (!arguments[2]) {
                var v2 = this.size();
                if (v2 > 0) {
                  if (c2 > 0) {
                    if (this.get(c2 - 1).equals2D(g2)) return null;
                  }
                  if (c2 < v2) {
                    if (this.get(c2).equals2D(g2)) return null;
                  }
                }
              }
              f(i2, "add", this, 1).call(this, c2, g2);
            }
          } else if (4 === arguments.length) {
            var y2 = arguments[0], d2 = arguments[1], _2 = arguments[2], p2 = arguments[3], m2 = 1;
            _2 > p2 && (m2 = -1);
            for (var k2 = _2; k2 !== p2; k2 += m2) this.add(y2[k2], d2);
            return true;
          }
        }, "value") }, { key: "closeRing", value: /* @__PURE__ */ __name(function() {
          if (this.size() > 0) {
            var t3 = this.get(0).copy();
            this.add(t3, false);
          }
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) ;
          else if (1 === arguments.length) {
            var t3 = arguments[0];
            this.ensureCapacity(t3.length), this.add(t3, true);
          } else if (2 === arguments.length) {
            var e3 = arguments[0], n2 = arguments[1];
            this.ensureCapacity(e3.length), this.add(e3, n2);
          }
        }, "value") }]);
      })(yt);
      Zt.coordArrayType = new Array(0).fill(null);
      var jt = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "isRing", value: /* @__PURE__ */ __name(function(t3) {
          return !(t3.length < 4) && !!t3[0].equals2D(t3[t3.length - 1]);
        }, "value") }, { key: "ptNotInList", value: /* @__PURE__ */ __name(function(e3, n2) {
          for (var i2 = 0; i2 < e3.length; i2++) {
            var r2 = e3[i2];
            if (t2.indexOf(r2, n2) < 0) return r2;
          }
          return null;
        }, "value") }, { key: "scroll", value: /* @__PURE__ */ __name(function(e3, n2) {
          var i2 = t2.indexOf(n2, e3);
          if (i2 < 0) return null;
          var r2 = new Array(e3.length).fill(null);
          mt.arraycopy(e3, i2, r2, 0, e3.length - i2), mt.arraycopy(e3, 0, r2, e3.length - i2, i2), mt.arraycopy(r2, 0, e3, 0, e3.length);
        }, "value") }, { key: "equals", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length) {
            var t3 = arguments[0], e3 = arguments[1];
            if (t3 === e3) return true;
            if (null === t3 || null === e3) return false;
            if (t3.length !== e3.length) return false;
            for (var n2 = 0; n2 < t3.length; n2++) if (!t3[n2].equals(e3[n2])) return false;
            return true;
          }
          if (3 === arguments.length) {
            var i2 = arguments[0], r2 = arguments[1], s2 = arguments[2];
            if (i2 === r2) return true;
            if (null === i2 || null === r2) return false;
            if (i2.length !== r2.length) return false;
            for (var a2 = 0; a2 < i2.length; a2++) if (0 !== s2.compare(i2[a2], r2[a2])) return false;
            return true;
          }
        }, "value") }, { key: "intersection", value: /* @__PURE__ */ __name(function(t3, e3) {
          for (var n2 = new Zt(), i2 = 0; i2 < t3.length; i2++) e3.intersects(t3[i2]) && n2.add(t3[i2], true);
          return n2.toCoordinateArray();
        }, "value") }, { key: "measures", value: /* @__PURE__ */ __name(function(t3) {
          if (null === t3 || 0 === t3.length) return 0;
          var e3, n2 = 0, i2 = a(t3);
          try {
            for (i2.s(); !(e3 = i2.n()).done; ) {
              var r2 = e3.value;
              n2 = Math.max(n2, Ht.measures(r2));
            }
          } catch (t4) {
            i2.e(t4);
          } finally {
            i2.f();
          }
          return n2;
        }, "value") }, { key: "hasRepeatedPoints", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = 1; e3 < t3.length; e3++) if (t3[e3 - 1].equals(t3[e3])) return true;
          return false;
        }, "value") }, { key: "removeRepeatedPoints", value: /* @__PURE__ */ __name(function(e3) {
          return t2.hasRepeatedPoints(e3) ? new Zt(e3, false).toCoordinateArray() : e3;
        }, "value") }, { key: "reverse", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = t3.length - 1, n2 = Math.trunc(e3 / 2), i2 = 0; i2 <= n2; i2++) {
            var r2 = t3[i2];
            t3[i2] = t3[e3 - i2], t3[e3 - i2] = r2;
          }
        }, "value") }, { key: "removeNull", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = 0, n2 = 0; n2 < t3.length; n2++) null !== t3[n2] && e3++;
          var i2 = new Array(e3).fill(null);
          if (0 === e3) return i2;
          for (var r2 = 0, s2 = 0; s2 < t3.length; s2++) null !== t3[s2] && (i2[r2++] = t3[s2]);
          return i2;
        }, "value") }, { key: "copyDeep", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            for (var t3 = arguments[0], e3 = new Array(t3.length).fill(null), n2 = 0; n2 < t3.length; n2++) e3[n2] = t3[n2].copy();
            return e3;
          }
          if (5 === arguments.length) for (var i2 = arguments[0], r2 = arguments[1], s2 = arguments[2], a2 = arguments[3], o2 = arguments[4], u2 = 0; u2 < o2; u2++) s2[a2 + u2] = i2[r2 + u2].copy();
        }, "value") }, { key: "isEqualReversed", value: /* @__PURE__ */ __name(function(t3, e3) {
          for (var n2 = 0; n2 < t3.length; n2++) {
            var i2 = t3[n2], r2 = e3[t3.length - n2 - 1];
            if (0 !== i2.compareTo(r2)) return false;
          }
          return true;
        }, "value") }, { key: "envelope", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = new U(), n2 = 0; n2 < t3.length; n2++) e3.expandToInclude(t3[n2]);
          return e3;
        }, "value") }, { key: "toCoordinateArray", value: /* @__PURE__ */ __name(function(e3) {
          return e3.toArray(t2.coordArrayType);
        }, "value") }, { key: "dimension", value: /* @__PURE__ */ __name(function(t3) {
          if (null === t3 || 0 === t3.length) return 3;
          var e3, n2 = 0, i2 = a(t3);
          try {
            for (i2.s(); !(e3 = i2.n()).done; ) {
              var r2 = e3.value;
              n2 = Math.max(n2, Ht.dimension(r2));
            }
          } catch (t4) {
            i2.e(t4);
          } finally {
            i2.f();
          }
          return n2;
        }, "value") }, { key: "atLeastNCoordinatesOrNothing", value: /* @__PURE__ */ __name(function(t3, e3) {
          return e3.length >= t3 ? e3 : [];
        }, "value") }, { key: "indexOf", value: /* @__PURE__ */ __name(function(t3, e3) {
          for (var n2 = 0; n2 < e3.length; n2++) if (t3.equals(e3[n2])) return n2;
          return -1;
        }, "value") }, { key: "increasingDirection", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = 0; e3 < Math.trunc(t3.length / 2); e3++) {
            var n2 = t3.length - 1 - e3, i2 = t3[e3].compareTo(t3[n2]);
            if (0 !== i2) return i2;
          }
          return 1;
        }, "value") }, { key: "compare", value: /* @__PURE__ */ __name(function(t3, e3) {
          for (var n2 = 0; n2 < t3.length && n2 < e3.length; ) {
            var i2 = t3[n2].compareTo(e3[n2]);
            if (0 !== i2) return i2;
            n2++;
          }
          return n2 < e3.length ? -1 : n2 < t3.length ? 1 : 0;
        }, "value") }, { key: "minCoordinate", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = null, n2 = 0; n2 < t3.length; n2++) (null === e3 || e3.compareTo(t3[n2]) > 0) && (e3 = t3[n2]);
          return e3;
        }, "value") }, { key: "extract", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          e3 = kt.clamp(e3, 0, t3.length);
          var i2 = (n2 = kt.clamp(n2, -1, t3.length)) - e3 + 1;
          n2 < 0 && (i2 = 0), e3 >= t3.length && (i2 = 0), n2 < e3 && (i2 = 0);
          var r2 = new Array(i2).fill(null);
          if (0 === i2) return r2;
          for (var s2 = 0, a2 = e3; a2 <= n2; a2++) r2[s2++] = t3[a2];
          return r2;
        }, "value") }]);
      })(), Wt = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "compare", value: /* @__PURE__ */ __name(function(t2, e3) {
          var n2 = t2, i2 = e3;
          return jt.compare(n2, i2);
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [P];
        }, "get") }]);
      })(), Kt = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "compare", value: /* @__PURE__ */ __name(function(t2, e3) {
          var n2 = t2, i2 = e3;
          if (n2.length < i2.length) return -1;
          if (n2.length > i2.length) return 1;
          if (0 === n2.length) return 0;
          var r2 = jt.compare(n2, i2);
          return jt.isEqualReversed(n2, i2) ? 0 : r2;
        }, "value") }, { key: "OLDcompare", value: /* @__PURE__ */ __name(function(t2, e3) {
          var n2 = t2, i2 = e3;
          if (n2.length < i2.length) return -1;
          if (n2.length > i2.length) return 1;
          if (0 === n2.length) return 0;
          for (var r2 = jt.increasingDirection(n2), s2 = jt.increasingDirection(i2), a2 = r2 > 0 ? 0 : n2.length - 1, o2 = s2 > 0 ? 0 : n2.length - 1, u2 = 0; u2 < n2.length; u2++) {
            var l2 = n2[a2].compareTo(i2[o2]);
            if (0 !== l2) return l2;
            a2 += r2, o2 += s2;
          }
          return 0;
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [P];
        }, "get") }]);
      })();
      jt.ForwardComparator = Wt, jt.BidirectionalComparator = Kt, jt.coordArrayType = new Array(0).fill(null);
      var Jt = (function() {
        return s(/* @__PURE__ */ __name((function t2(e3) {
          n(this, t2), this.str = e3;
        }), "t"), [{ key: "append", value: /* @__PURE__ */ __name(function(t2) {
          this.str += t2;
        }, "value") }, { key: "setCharAt", value: /* @__PURE__ */ __name(function(t2, e3) {
          this.str = this.str.substr(0, t2) + e3 + this.str.substr(t2 + 1);
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          return this.str;
        }, "value") }]);
      })(), Qt = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "getM", value: /* @__PURE__ */ __name(function(t3) {
          return this.hasM() ? this._coordinates[t3].getM() : A.NaN;
        }, "value") }, { key: "setOrdinate", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          switch (e3) {
            case ht.X:
              this._coordinates[t3].x = n2;
              break;
            case ht.Y:
              this._coordinates[t3].y = n2;
              break;
            default:
              this._coordinates[t3].setOrdinate(e3, n2);
          }
        }, "value") }, { key: "getZ", value: /* @__PURE__ */ __name(function(t3) {
          return this.hasZ() ? this._coordinates[t3].getZ() : A.NaN;
        }, "value") }, { key: "size", value: /* @__PURE__ */ __name(function() {
          return this._coordinates.length;
        }, "value") }, { key: "getOrdinate", value: /* @__PURE__ */ __name(function(t3, e3) {
          switch (e3) {
            case ht.X:
              return this._coordinates[t3].x;
            case ht.Y:
              return this._coordinates[t3].y;
            default:
              return this._coordinates[t3].getOrdinate(e3);
          }
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return this._coordinates[t3];
          }
          if (2 === arguments.length) {
            var e3 = arguments[0];
            arguments[1].setCoordinate(this._coordinates[e3]);
          }
        }, "value") }, { key: "getCoordinateCopy", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = this.createCoordinate();
          return e3.setCoordinate(this._coordinates[t3]), e3;
        }, "value") }, { key: "createCoordinate", value: /* @__PURE__ */ __name(function() {
          return Ht.create(this.getDimension(), this.getMeasures());
        }, "value") }, { key: "getDimension", value: /* @__PURE__ */ __name(function() {
          return this._dimension;
        }, "value") }, { key: "getX", value: /* @__PURE__ */ __name(function(t3) {
          return this._coordinates[t3].x;
        }, "value") }, { key: "getMeasures", value: /* @__PURE__ */ __name(function() {
          return this._measures;
        }, "value") }, { key: "expandEnvelope", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = 0; e3 < this._coordinates.length; e3++) t3.expandToInclude(this._coordinates[e3]);
          return t3;
        }, "value") }, { key: "copy", value: /* @__PURE__ */ __name(function() {
          for (var e3 = new Array(this.size()).fill(null), n2 = 0; n2 < this._coordinates.length; n2++) {
            var i2 = this.createCoordinate();
            i2.setCoordinate(this._coordinates[n2]), e3[n2] = i2;
          }
          return new t2(e3, this._dimension, this._measures);
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          if (this._coordinates.length > 0) {
            var t3 = new Jt(17 * this._coordinates.length);
            t3.append("("), t3.append(this._coordinates[0]);
            for (var e3 = 1; e3 < this._coordinates.length; e3++) t3.append(", "), t3.append(this._coordinates[e3]);
            return t3.append(")"), t3.toString();
          }
          return "()";
        }, "value") }, { key: "getY", value: /* @__PURE__ */ __name(function(t3) {
          return this._coordinates[t3].y;
        }, "value") }, { key: "toCoordinateArray", value: /* @__PURE__ */ __name(function() {
          return this._coordinates;
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [ht, E];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._dimension = 3, this._measures = 0, this._coordinates = null, 1 === arguments.length) {
            if (arguments[0] instanceof Array) {
              var e3 = arguments[0];
              t2.constructor_.call(this, e3, jt.dimension(e3), jt.measures(e3));
            } else if (Number.isInteger(arguments[0])) {
              var n2 = arguments[0];
              this._coordinates = new Array(n2).fill(null);
              for (var i2 = 0; i2 < n2; i2++) this._coordinates[i2] = new X();
            } else if (rt(arguments[0], ht)) {
              var r2 = arguments[0];
              if (null === r2) return this._coordinates = new Array(0).fill(null), null;
              this._dimension = r2.getDimension(), this._measures = r2.getMeasures(), this._coordinates = new Array(r2.size()).fill(null);
              for (var s2 = 0; s2 < this._coordinates.length; s2++) this._coordinates[s2] = r2.getCoordinateCopy(s2);
            }
          } else if (2 === arguments.length) {
            if (arguments[0] instanceof Array && Number.isInteger(arguments[1])) {
              var a2 = arguments[0], o2 = arguments[1];
              t2.constructor_.call(this, a2, o2, jt.measures(a2));
            } else if (Number.isInteger(arguments[0]) && Number.isInteger(arguments[1])) {
              var u2 = arguments[0], l2 = arguments[1];
              this._coordinates = new Array(u2).fill(null), this._dimension = l2;
              for (var h2 = 0; h2 < u2; h2++) this._coordinates[h2] = Ht.create(l2);
            }
          } else if (3 === arguments.length) {
            if (Number.isInteger(arguments[2]) && arguments[0] instanceof Array && Number.isInteger(arguments[1])) {
              var c2 = arguments[0], f2 = arguments[1], g2 = arguments[2];
              this._dimension = f2, this._measures = g2, this._coordinates = null === c2 ? new Array(0).fill(null) : c2;
            } else if (Number.isInteger(arguments[2]) && Number.isInteger(arguments[0]) && Number.isInteger(arguments[1])) {
              var v2 = arguments[0], y2 = arguments[1], d2 = arguments[2];
              this._coordinates = new Array(v2).fill(null), this._dimension = y2, this._measures = d2;
              for (var _2 = 0; _2 < v2; _2++) this._coordinates[_2] = this.createCoordinate();
            }
          }
        }, "value") }]);
      })(), $t = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, [{ key: "readResolve", value: /* @__PURE__ */ __name(function() {
          return t2.instance();
        }, "value") }, { key: "create", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            if (arguments[0] instanceof Array) return new Qt(arguments[0]);
            if (rt(arguments[0], ht)) return new Qt(arguments[0]);
          } else {
            if (2 === arguments.length) {
              var t3 = arguments[1];
              return t3 > 3 && (t3 = 3), t3 < 2 && (t3 = 2), new Qt(arguments[0], t3);
            }
            if (3 === arguments.length) {
              var e3 = arguments[2], n2 = arguments[1] - e3;
              return e3 > 1 && (e3 = 1), n2 > 3 && (n2 = 3), n2 < 2 && (n2 = 2), new Qt(arguments[0], n2 + e3, e3);
            }
          }
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [It, E];
        }, "get") }], [{ key: "instance", value: /* @__PURE__ */ __name(function() {
          return t2.instanceObject;
        }, "value") }]);
      })();
      $t.instanceObject = new $t();
      var te = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "copyInternal", value: /* @__PURE__ */ __name(function() {
          for (var t3 = new Array(this._geometries.length).fill(null), e3 = 0; e3 < t3.length; e3++) t3[e3] = this._geometries[e3].copy();
          return new i2(t3, this._factory);
        }, "value") }, { key: "equalsExact", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length && "number" == typeof arguments[1] && arguments[0] instanceof V) {
            var t3 = arguments[0], e3 = arguments[1];
            return !!this.isEquivalentClass(t3) && f(i2, "equalsExact", this, 1).call(this, t3, e3);
          }
          return f(i2, "equalsExact", this, 1).apply(this, arguments);
        }, "value") }, { key: "getBoundaryDimension", value: /* @__PURE__ */ __name(function() {
          return 1;
        }, "value") }, { key: "getTypeCode", value: /* @__PURE__ */ __name(function() {
          return V.TYPECODE_MULTIPOLYGON;
        }, "value") }, { key: "getDimension", value: /* @__PURE__ */ __name(function() {
          return 2;
        }, "value") }, { key: "getBoundary", value: /* @__PURE__ */ __name(function() {
          if (this.isEmpty()) return this.getFactory().createMultiLineString();
          for (var t3 = new yt(), e3 = 0; e3 < this._geometries.length; e3++) for (var n2 = this._geometries[e3].getBoundary(), i3 = 0; i3 < n2.getNumGeometries(); i3++) t3.add(n2.getGeometryN(i3));
          var r2 = new Array(t3.size()).fill(null);
          return this.getFactory().createMultiLineString(t3.toArray(r2));
        }, "value") }, { key: "getGeometryType", value: /* @__PURE__ */ __name(function() {
          return V.TYPENAME_MULTIPOLYGON;
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [Pt];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          var t3 = arguments[0], e3 = arguments[1];
          Yt.constructor_.call(this, t3, e3);
        }, "value") }]);
      })(Yt), ee = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "get", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "put", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "size", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "values", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "entrySet", value: /* @__PURE__ */ __name(function() {
        }, "value") }]);
      })(), ne3 = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), (t3 = e(this, i2)).map = /* @__PURE__ */ new Map(), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "get", value: /* @__PURE__ */ __name(function(t3) {
          return this.map.get(t3) || null;
        }, "value") }, { key: "put", value: /* @__PURE__ */ __name(function(t3, e3) {
          return this.map.set(t3, e3), e3;
        }, "value") }, { key: "values", value: /* @__PURE__ */ __name(function() {
          for (var t3 = new yt(), e3 = this.map.values(), n2 = e3.next(); !n2.done; ) t3.add(n2.value), n2 = e3.next();
          return t3;
        }, "value") }, { key: "entrySet", value: /* @__PURE__ */ __name(function() {
          var t3 = new J();
          return this.map.entries().forEach((function(e3) {
            return t3.add(e3);
          })), t3;
        }, "value") }, { key: "size", value: /* @__PURE__ */ __name(function() {
          return this.map.size();
        }, "value") }]);
      })(ee), ie = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "equals", value: /* @__PURE__ */ __name(function(e3) {
          if (!(e3 instanceof t2)) return false;
          var n2 = e3;
          return this._modelType === n2._modelType && this._scale === n2._scale;
        }, "value") }, { key: "compareTo", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = t3, n2 = this.getMaximumSignificantDigits(), i2 = e3.getMaximumSignificantDigits();
          return at.compare(n2, i2);
        }, "value") }, { key: "getScale", value: /* @__PURE__ */ __name(function() {
          return this._scale;
        }, "value") }, { key: "isFloating", value: /* @__PURE__ */ __name(function() {
          return this._modelType === t2.FLOATING || this._modelType === t2.FLOATING_SINGLE;
        }, "value") }, { key: "getType", value: /* @__PURE__ */ __name(function() {
          return this._modelType;
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          var e3 = "UNKNOWN";
          return this._modelType === t2.FLOATING ? e3 = "Floating" : this._modelType === t2.FLOATING_SINGLE ? e3 = "Floating-Single" : this._modelType === t2.FIXED && (e3 = "Fixed (Scale=" + this.getScale() + ")"), e3;
        }, "value") }, { key: "makePrecise", value: /* @__PURE__ */ __name(function() {
          if ("number" == typeof arguments[0]) {
            var e3 = arguments[0];
            return A.isNaN(e3) || this._modelType === t2.FLOATING_SINGLE ? e3 : this._modelType === t2.FIXED ? Math.round(e3 * this._scale) / this._scale : e3;
          }
          if (arguments[0] instanceof X) {
            var n2 = arguments[0];
            if (this._modelType === t2.FLOATING) return null;
            n2.x = this.makePrecise(n2.x), n2.y = this.makePrecise(n2.y);
          }
        }, "value") }, { key: "getMaximumSignificantDigits", value: /* @__PURE__ */ __name(function() {
          var e3 = 16;
          return this._modelType === t2.FLOATING ? e3 = 16 : this._modelType === t2.FLOATING_SINGLE ? e3 = 6 : this._modelType === t2.FIXED && (e3 = 1 + Math.trunc(Math.ceil(Math.log(this.getScale()) / Math.log(10)))), e3;
        }, "value") }, { key: "setScale", value: /* @__PURE__ */ __name(function(t3) {
          this._scale = Math.abs(t3);
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [E, x];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._modelType = null, this._scale = null, 0 === arguments.length) this._modelType = t2.FLOATING;
          else if (1 === arguments.length) {
            if (arguments[0] instanceof re) {
              var e3 = arguments[0];
              this._modelType = e3, e3 === t2.FIXED && this.setScale(1);
            } else if ("number" == typeof arguments[0]) {
              var n2 = arguments[0];
              this._modelType = t2.FIXED, this.setScale(n2);
            } else if (arguments[0] instanceof t2) {
              var i2 = arguments[0];
              this._modelType = i2._modelType, this._scale = i2._scale;
            }
          }
        }, "value") }, { key: "mostPrecise", value: /* @__PURE__ */ __name(function(t3, e3) {
          return t3.compareTo(e3) >= 0 ? t3 : e3;
        }, "value") }]);
      })(), re = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "readResolve", value: /* @__PURE__ */ __name(function() {
          return t2.nameToTypeMap.get(this._name);
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          return this._name;
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [E];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._name = null;
          var e3 = arguments[0];
          this._name = e3, t2.nameToTypeMap.put(e3, this);
        }, "value") }]);
      })();
      re.nameToTypeMap = new ne3(), ie.Type = re, ie.FIXED = new re("FIXED"), ie.FLOATING = new re("FLOATING"), ie.FLOATING_SINGLE = new re("FLOATING SINGLE"), ie.maximumPreciseValue = 9007199254740992;
      var se = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "copyInternal", value: /* @__PURE__ */ __name(function() {
          for (var t3 = new Array(this._geometries.length).fill(null), e3 = 0; e3 < t3.length; e3++) t3[e3] = this._geometries[e3].copy();
          return new i2(t3, this._factory);
        }, "value") }, { key: "equalsExact", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length && "number" == typeof arguments[1] && arguments[0] instanceof V) {
            var t3 = arguments[0], e3 = arguments[1];
            return !!this.isEquivalentClass(t3) && f(i2, "equalsExact", this, 1).call(this, t3, e3);
          }
          return f(i2, "equalsExact", this, 1).apply(this, arguments);
        }, "value") }, { key: "getBoundaryDimension", value: /* @__PURE__ */ __name(function() {
          return this.isClosed() ? Lt.FALSE : 0;
        }, "value") }, { key: "isClosed", value: /* @__PURE__ */ __name(function() {
          if (this.isEmpty()) return false;
          for (var t3 = 0; t3 < this._geometries.length; t3++) if (!this._geometries[t3].isClosed()) return false;
          return true;
        }, "value") }, { key: "getTypeCode", value: /* @__PURE__ */ __name(function() {
          return V.TYPECODE_MULTILINESTRING;
        }, "value") }, { key: "getDimension", value: /* @__PURE__ */ __name(function() {
          return 1;
        }, "value") }, { key: "getBoundary", value: /* @__PURE__ */ __name(function() {
          throw new W();
        }, "value") }, { key: "getGeometryType", value: /* @__PURE__ */ __name(function() {
          return V.TYPENAME_MULTILINESTRING;
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [Tt];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          var t3 = arguments[0], e3 = arguments[1];
          Yt.constructor_.call(this, t3, e3);
        }, "value") }]);
      })(Yt), ae = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "createEmpty", value: /* @__PURE__ */ __name(function(t3) {
          switch (t3) {
            case -1:
              return this.createGeometryCollection();
            case 0:
              return this.createPoint();
            case 1:
              return this.createLineString();
            case 2:
              return this.createPolygon();
            default:
              throw new m("Invalid dimension: " + t3);
          }
        }, "value") }, { key: "toGeometry", value: /* @__PURE__ */ __name(function(t3) {
          return t3.isNull() ? this.createPoint() : t3.getMinX() === t3.getMaxX() && t3.getMinY() === t3.getMaxY() ? this.createPoint(new X(t3.getMinX(), t3.getMinY())) : t3.getMinX() === t3.getMaxX() || t3.getMinY() === t3.getMaxY() ? this.createLineString([new X(t3.getMinX(), t3.getMinY()), new X(t3.getMaxX(), t3.getMaxY())]) : this.createPolygon(this.createLinearRing([new X(t3.getMinX(), t3.getMinY()), new X(t3.getMinX(), t3.getMaxY()), new X(t3.getMaxX(), t3.getMaxY()), new X(t3.getMaxX(), t3.getMinY()), new X(t3.getMinX(), t3.getMinY())]), null);
        }, "value") }, { key: "createLineString", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) return this.createLineString(this.getCoordinateSequenceFactory().create([]));
          if (1 === arguments.length) {
            if (arguments[0] instanceof Array) {
              var t3 = arguments[0];
              return this.createLineString(null !== t3 ? this.getCoordinateSequenceFactory().create(t3) : null);
            }
            if (rt(arguments[0], ht)) return new wt(arguments[0], this);
          }
        }, "value") }, { key: "createMultiLineString", value: /* @__PURE__ */ __name(function() {
          return 0 === arguments.length ? new se(null, this) : 1 === arguments.length ? new se(arguments[0], this) : void 0;
        }, "value") }, { key: "buildGeometry", value: /* @__PURE__ */ __name(function(e3) {
          for (var n2 = null, i2 = false, r2 = false, s2 = e3.iterator(); s2.hasNext(); ) {
            var a2 = s2.next(), o2 = a2.getTypeCode();
            null === n2 && (n2 = o2), o2 !== n2 && (i2 = true), a2 instanceof Yt && (r2 = true);
          }
          if (null === n2) return this.createGeometryCollection();
          if (i2 || r2) return this.createGeometryCollection(t2.toGeometryArray(e3));
          var u2 = e3.iterator().next();
          if (e3.size() > 1) {
            if (u2 instanceof Dt) return this.createMultiPolygon(t2.toPolygonArray(e3));
            if (u2 instanceof wt) return this.createMultiLineString(t2.toLineStringArray(e3));
            if (u2 instanceof bt) return this.createMultiPoint(t2.toPointArray(e3));
            G.shouldNeverReachHere("Unhandled geometry type: " + u2.getGeometryType());
          }
          return u2;
        }, "value") }, { key: "createMultiPointFromCoords", value: /* @__PURE__ */ __name(function(t3) {
          return this.createMultiPoint(null !== t3 ? this.getCoordinateSequenceFactory().create(t3) : null);
        }, "value") }, { key: "createPoint", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) return this.createPoint(this.getCoordinateSequenceFactory().create([]));
          if (1 === arguments.length) {
            if (arguments[0] instanceof X) {
              var t3 = arguments[0];
              return this.createPoint(null !== t3 ? this.getCoordinateSequenceFactory().create([t3]) : null);
            }
            if (rt(arguments[0], ht)) return new bt(arguments[0], this);
          }
        }, "value") }, { key: "getCoordinateSequenceFactory", value: /* @__PURE__ */ __name(function() {
          return this._coordinateSequenceFactory;
        }, "value") }, { key: "createPolygon", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) return this.createPolygon(null, null);
          if (1 === arguments.length) {
            if (rt(arguments[0], ht)) {
              var t3 = arguments[0];
              return this.createPolygon(this.createLinearRing(t3));
            }
            if (arguments[0] instanceof Array) {
              var e3 = arguments[0];
              return this.createPolygon(this.createLinearRing(e3));
            }
            if (arguments[0] instanceof Xt) {
              var n2 = arguments[0];
              return this.createPolygon(n2, null);
            }
          } else if (2 === arguments.length) {
            return new Dt(arguments[0], arguments[1], this);
          }
        }, "value") }, { key: "getSRID", value: /* @__PURE__ */ __name(function() {
          return this._SRID;
        }, "value") }, { key: "createGeometryCollection", value: /* @__PURE__ */ __name(function() {
          return 0 === arguments.length ? new Yt(null, this) : 1 === arguments.length ? new Yt(arguments[0], this) : void 0;
        }, "value") }, { key: "getPrecisionModel", value: /* @__PURE__ */ __name(function() {
          return this._precisionModel;
        }, "value") }, { key: "createLinearRing", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) return this.createLinearRing(this.getCoordinateSequenceFactory().create([]));
          if (1 === arguments.length) {
            if (arguments[0] instanceof Array) {
              var t3 = arguments[0];
              return this.createLinearRing(null !== t3 ? this.getCoordinateSequenceFactory().create(t3) : null);
            }
            if (rt(arguments[0], ht)) return new Xt(arguments[0], this);
          }
        }, "value") }, { key: "createMultiPolygon", value: /* @__PURE__ */ __name(function() {
          return 0 === arguments.length ? new te(null, this) : 1 === arguments.length ? new te(arguments[0], this) : void 0;
        }, "value") }, { key: "createMultiPoint", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) return new zt(null, this);
          if (1 === arguments.length) {
            if (arguments[0] instanceof Array) return new zt(arguments[0], this);
            if (rt(arguments[0], ht)) {
              var t3 = arguments[0];
              if (null === t3) return this.createMultiPoint(new Array(0).fill(null));
              for (var e3 = new Array(t3.size()).fill(null), n2 = 0; n2 < t3.size(); n2++) {
                var i2 = this.getCoordinateSequenceFactory().create(1, t3.getDimension(), t3.getMeasures());
                St.copy(t3, n2, i2, 0, 1), e3[n2] = this.createPoint(i2);
              }
              return this.createMultiPoint(e3);
            }
          }
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [E];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._precisionModel = null, this._coordinateSequenceFactory = null, this._SRID = null, 0 === arguments.length) t2.constructor_.call(this, new ie(), 0);
          else if (1 === arguments.length) {
            if (rt(arguments[0], It)) {
              var e3 = arguments[0];
              t2.constructor_.call(this, new ie(), 0, e3);
            } else if (arguments[0] instanceof ie) {
              var n2 = arguments[0];
              t2.constructor_.call(this, n2, 0, t2.getDefaultCoordinateSequenceFactory());
            }
          } else if (2 === arguments.length) {
            var i2 = arguments[0], r2 = arguments[1];
            t2.constructor_.call(this, i2, r2, t2.getDefaultCoordinateSequenceFactory());
          } else if (3 === arguments.length) {
            var s2 = arguments[0], a2 = arguments[1], o2 = arguments[2];
            this._precisionModel = s2, this._coordinateSequenceFactory = o2, this._SRID = a2;
          }
        }, "value") }, { key: "toMultiPolygonArray", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = new Array(t3.size()).fill(null);
          return t3.toArray(e3);
        }, "value") }, { key: "toGeometryArray", value: /* @__PURE__ */ __name(function(t3) {
          if (null === t3) return null;
          var e3 = new Array(t3.size()).fill(null);
          return t3.toArray(e3);
        }, "value") }, { key: "getDefaultCoordinateSequenceFactory", value: /* @__PURE__ */ __name(function() {
          return $t.instance();
        }, "value") }, { key: "toMultiLineStringArray", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = new Array(t3.size()).fill(null);
          return t3.toArray(e3);
        }, "value") }, { key: "toLineStringArray", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = new Array(t3.size()).fill(null);
          return t3.toArray(e3);
        }, "value") }, { key: "toMultiPointArray", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = new Array(t3.size()).fill(null);
          return t3.toArray(e3);
        }, "value") }, { key: "toLinearRingArray", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = new Array(t3.size()).fill(null);
          return t3.toArray(e3);
        }, "value") }, { key: "toPointArray", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = new Array(t3.size()).fill(null);
          return t3.toArray(e3);
        }, "value") }, { key: "toPolygonArray", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = new Array(t3.size()).fill(null);
          return t3.toArray(e3);
        }, "value") }, { key: "createPointFromInternalCoord", value: /* @__PURE__ */ __name(function(t3, e3) {
          return e3.getPrecisionModel().makePrecise(t3), e3.getFactory().createPoint(t3);
        }, "value") }]);
      })(), oe = "XY", ue = "XYZ", le = "XYM", he = "XYZM", ce = { POINT: "Point", LINE_STRING: "LineString", LINEAR_RING: "LinearRing", POLYGON: "Polygon", MULTI_POINT: "MultiPoint", MULTI_LINE_STRING: "MultiLineString", MULTI_POLYGON: "MultiPolygon", GEOMETRY_COLLECTION: "GeometryCollection", CIRCLE: "Circle" }, fe = "EMPTY", ge = 1, ve = 2, ye = 3, de = 4, _e = 5, pe = 6;
      for (var me in ce) ce[me].toUpperCase();
      var ke = (function() {
        return s(/* @__PURE__ */ __name((function t2(e3) {
          n(this, t2), this.wkt = e3, this.index_ = -1;
        }), "t"), [{ key: "isAlpha_", value: /* @__PURE__ */ __name(function(t2) {
          return t2 >= "a" && t2 <= "z" || t2 >= "A" && t2 <= "Z";
        }, "value") }, { key: "isNumeric_", value: /* @__PURE__ */ __name(function(t2, e3) {
          return t2 >= "0" && t2 <= "9" || "." == t2 && !(void 0 !== e3 && e3);
        }, "value") }, { key: "isWhiteSpace_", value: /* @__PURE__ */ __name(function(t2) {
          return " " == t2 || "	" == t2 || "\r" == t2 || "\n" == t2;
        }, "value") }, { key: "nextChar_", value: /* @__PURE__ */ __name(function() {
          return this.wkt.charAt(++this.index_);
        }, "value") }, { key: "nextToken", value: /* @__PURE__ */ __name(function() {
          var t2, e3 = this.nextChar_(), n2 = this.index_, i2 = e3;
          if ("(" == e3) t2 = ve;
          else if ("," == e3) t2 = _e;
          else if (")" == e3) t2 = ye;
          else if (this.isNumeric_(e3) || "-" == e3) t2 = de, i2 = this.readNumber_();
          else if (this.isAlpha_(e3)) t2 = ge, i2 = this.readText_();
          else {
            if (this.isWhiteSpace_(e3)) return this.nextToken();
            if ("" !== e3) throw new Error("Unexpected character: " + e3);
            t2 = pe;
          }
          return { position: n2, value: i2, type: t2 };
        }, "value") }, { key: "readNumber_", value: /* @__PURE__ */ __name(function() {
          var t2, e3 = this.index_, n2 = false, i2 = false;
          do {
            "." == t2 ? n2 = true : "e" != t2 && "E" != t2 || (i2 = true), t2 = this.nextChar_();
          } while (this.isNumeric_(t2, n2) || !i2 && ("e" == t2 || "E" == t2) || i2 && ("-" == t2 || "+" == t2));
          return parseFloat(this.wkt.substring(e3, this.index_--));
        }, "value") }, { key: "readText_", value: /* @__PURE__ */ __name(function() {
          var t2, e3 = this.index_;
          do {
            t2 = this.nextChar_();
          } while (this.isAlpha_(t2));
          return this.wkt.substring(e3, this.index_--).toUpperCase();
        }, "value") }]);
      })(), xe = (function() {
        return s(/* @__PURE__ */ __name((function t2(e3, i2) {
          n(this, t2), this.lexer_ = e3, this.token_, this.layout_ = oe, this.factory = i2;
        }), "t"), [{ key: "consume_", value: /* @__PURE__ */ __name(function() {
          this.token_ = this.lexer_.nextToken();
        }, "value") }, { key: "isTokenType", value: /* @__PURE__ */ __name(function(t2) {
          return this.token_.type == t2;
        }, "value") }, { key: "match", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = this.isTokenType(t2);
          return e3 && this.consume_(), e3;
        }, "value") }, { key: "parse", value: /* @__PURE__ */ __name(function() {
          return this.consume_(), this.parseGeometry_();
        }, "value") }, { key: "parseGeometryLayout_", value: /* @__PURE__ */ __name(function() {
          var t2 = oe, e3 = this.token_;
          if (this.isTokenType(ge)) {
            var n2 = e3.value;
            "Z" === n2 ? t2 = ue : "M" === n2 ? t2 = le : "ZM" === n2 && (t2 = he), t2 !== oe && this.consume_();
          }
          return t2;
        }, "value") }, { key: "parseGeometryCollectionText_", value: /* @__PURE__ */ __name(function() {
          if (this.match(ve)) {
            var t2 = [];
            do {
              t2.push(this.parseGeometry_());
            } while (this.match(_e));
            if (this.match(ye)) return t2;
          } else if (this.isEmptyGeometry_()) return [];
          throw new Error(this.formatErrorMessage_());
        }, "value") }, { key: "parsePointText_", value: /* @__PURE__ */ __name(function() {
          if (this.match(ve)) {
            var t2 = this.parsePoint_();
            if (this.match(ye)) return t2;
          } else if (this.isEmptyGeometry_()) return null;
          throw new Error(this.formatErrorMessage_());
        }, "value") }, { key: "parseLineStringText_", value: /* @__PURE__ */ __name(function() {
          if (this.match(ve)) {
            var t2 = this.parsePointList_();
            if (this.match(ye)) return t2;
          } else if (this.isEmptyGeometry_()) return [];
          throw new Error(this.formatErrorMessage_());
        }, "value") }, { key: "parsePolygonText_", value: /* @__PURE__ */ __name(function() {
          if (this.match(ve)) {
            var t2 = this.parseLineStringTextList_();
            if (this.match(ye)) return t2;
          } else if (this.isEmptyGeometry_()) return [];
          throw new Error(this.formatErrorMessage_());
        }, "value") }, { key: "parseMultiPointText_", value: /* @__PURE__ */ __name(function() {
          var t2;
          if (this.match(ve)) {
            if (t2 = this.token_.type == ve ? this.parsePointTextList_() : this.parsePointList_(), this.match(ye)) return t2;
          } else if (this.isEmptyGeometry_()) return [];
          throw new Error(this.formatErrorMessage_());
        }, "value") }, { key: "parseMultiLineStringText_", value: /* @__PURE__ */ __name(function() {
          if (this.match(ve)) {
            var t2 = this.parseLineStringTextList_();
            if (this.match(ye)) return t2;
          } else if (this.isEmptyGeometry_()) return [];
          throw new Error(this.formatErrorMessage_());
        }, "value") }, { key: "parseMultiPolygonText_", value: /* @__PURE__ */ __name(function() {
          if (this.match(ve)) {
            var t2 = this.parsePolygonTextList_();
            if (this.match(ye)) return t2;
          } else if (this.isEmptyGeometry_()) return [];
          throw new Error(this.formatErrorMessage_());
        }, "value") }, { key: "parsePoint_", value: /* @__PURE__ */ __name(function() {
          for (var t2 = [], e3 = this.layout_.length, n2 = 0; n2 < e3; ++n2) {
            var i2 = this.token_;
            if (!this.match(de)) break;
            t2.push(i2.value);
          }
          if (t2.length == e3) return t2;
          throw new Error(this.formatErrorMessage_());
        }, "value") }, { key: "parsePointList_", value: /* @__PURE__ */ __name(function() {
          for (var t2 = [this.parsePoint_()]; this.match(_e); ) t2.push(this.parsePoint_());
          return t2;
        }, "value") }, { key: "parsePointTextList_", value: /* @__PURE__ */ __name(function() {
          for (var t2 = [this.parsePointText_()]; this.match(_e); ) t2.push(this.parsePointText_());
          return t2;
        }, "value") }, { key: "parseLineStringTextList_", value: /* @__PURE__ */ __name(function() {
          for (var t2 = [this.parseLineStringText_()]; this.match(_e); ) t2.push(this.parseLineStringText_());
          return t2;
        }, "value") }, { key: "parsePolygonTextList_", value: /* @__PURE__ */ __name(function() {
          for (var t2 = [this.parsePolygonText_()]; this.match(_e); ) t2.push(this.parsePolygonText_());
          return t2;
        }, "value") }, { key: "isEmptyGeometry_", value: /* @__PURE__ */ __name(function() {
          var t2 = this.isTokenType(ge) && this.token_.value == fe;
          return t2 && this.consume_(), t2;
        }, "value") }, { key: "formatErrorMessage_", value: /* @__PURE__ */ __name(function() {
          return "Unexpected `" + this.token_.value + "` at position " + this.token_.position + " in `" + this.lexer_.wkt + "`";
        }, "value") }, { key: "parseGeometry_", value: /* @__PURE__ */ __name(function() {
          var t2 = this.factory, e3 = /* @__PURE__ */ __name(function(t3) {
            return i(X, g(t3));
          }, "e"), n2 = /* @__PURE__ */ __name(function(n3) {
            var i2 = n3.map((function(n4) {
              return t2.createLinearRing(n4.map(e3));
            }));
            return i2.length > 1 ? t2.createPolygon(i2[0], i2.slice(1)) : t2.createPolygon(i2[0]);
          }, "n"), r2 = this.token_;
          if (this.match(ge)) {
            var s2 = r2.value;
            if (this.layout_ = this.parseGeometryLayout_(), "GEOMETRYCOLLECTION" == s2) {
              var a2 = this.parseGeometryCollectionText_();
              return t2.createGeometryCollection(a2);
            }
            switch (s2) {
              case "POINT":
                var o2 = this.parsePointText_();
                return o2 ? t2.createPoint(i(X, g(o2))) : t2.createPoint();
              case "LINESTRING":
                var u2 = this.parseLineStringText_().map(e3);
                return t2.createLineString(u2);
              case "LINEARRING":
                var l2 = this.parseLineStringText_().map(e3);
                return t2.createLinearRing(l2);
              case "POLYGON":
                var h2 = this.parsePolygonText_();
                return h2 && 0 !== h2.length ? n2(h2) : t2.createPolygon();
              case "MULTIPOINT":
                var c2 = this.parseMultiPointText_();
                if (!c2 || 0 === c2.length) return t2.createMultiPoint();
                var f2 = c2.map(e3).map((function(e4) {
                  return t2.createPoint(e4);
                }));
                return t2.createMultiPoint(f2);
              case "MULTILINESTRING":
                var v2 = this.parseMultiLineStringText_().map((function(n3) {
                  return t2.createLineString(n3.map(e3));
                }));
                return t2.createMultiLineString(v2);
              case "MULTIPOLYGON":
                var y2 = this.parseMultiPolygonText_();
                if (!y2 || 0 === y2.length) return t2.createMultiPolygon();
                var d2 = y2.map(n2);
                return t2.createMultiPolygon(d2);
              default:
                throw new Error("Invalid geometry type: " + s2);
            }
          }
          throw new Error(this.formatErrorMessage_());
        }, "value") }]);
      })();
      function Ie(t2) {
        if (t2.isEmpty()) return "";
        var e3 = t2.getCoordinate(), n2 = [e3.x, e3.y];
        return void 0 === e3.z || Number.isNaN(e3.z) || n2.push(e3.z), void 0 === e3.m || Number.isNaN(e3.m) || n2.push(e3.m), n2.join(" ");
      }
      __name(Ie, "Ie");
      function Ee(t2) {
        for (var e3 = t2.getCoordinates().map((function(t3) {
          var e4 = [t3.x, t3.y];
          return void 0 === t3.z || Number.isNaN(t3.z) || e4.push(t3.z), void 0 === t3.m || Number.isNaN(t3.m) || e4.push(t3.m), e4;
        })), n2 = [], i2 = 0, r2 = e3.length; i2 < r2; ++i2) n2.push(e3[i2].join(" "));
        return n2.join(", ");
      }
      __name(Ee, "Ee");
      function Ne(t2) {
        var e3 = [];
        e3.push("(" + Ee(t2.getExteriorRing()) + ")");
        for (var n2 = 0, i2 = t2.getNumInteriorRing(); n2 < i2; ++n2) e3.push("(" + Ee(t2.getInteriorRingN(n2)) + ")");
        return e3.join(", ");
      }
      __name(Ne, "Ne");
      var Te = { Point: Ie, LineString: Ee, LinearRing: Ee, Polygon: Ne, MultiPoint: /* @__PURE__ */ __name(function(t2) {
        for (var e3 = [], n2 = 0, i2 = t2.getNumGeometries(); n2 < i2; ++n2) e3.push("(" + Ie(t2.getGeometryN(n2)) + ")");
        return e3.join(", ");
      }, "MultiPoint"), MultiLineString: /* @__PURE__ */ __name(function(t2) {
        for (var e3 = [], n2 = 0, i2 = t2.getNumGeometries(); n2 < i2; ++n2) e3.push("(" + Ee(t2.getGeometryN(n2)) + ")");
        return e3.join(", ");
      }, "MultiLineString"), MultiPolygon: /* @__PURE__ */ __name(function(t2) {
        for (var e3 = [], n2 = 0, i2 = t2.getNumGeometries(); n2 < i2; ++n2) e3.push("(" + Ne(t2.getGeometryN(n2)) + ")");
        return e3.join(", ");
      }, "MultiPolygon"), GeometryCollection: /* @__PURE__ */ __name(function(t2) {
        for (var e3 = [], n2 = 0, i2 = t2.getNumGeometries(); n2 < i2; ++n2) e3.push(Se(t2.getGeometryN(n2)));
        return e3.join(", ");
      }, "GeometryCollection") };
      function Se(t2) {
        var e3 = t2.getGeometryType(), n2 = Te[e3];
        e3 = e3.toUpperCase();
        var i2 = (function(t3) {
          var e4 = "";
          if (t3.isEmpty()) return e4;
          var n3 = t3.getCoordinate();
          return void 0 === n3.z || Number.isNaN(n3.z) || (e4 += "Z"), void 0 === n3.m || Number.isNaN(n3.m) || (e4 += "M"), e4;
        })(t2);
        return i2.length > 0 && (e3 += " " + i2), t2.isEmpty() ? e3 + " " + fe : e3 + " (" + n2(t2) + ")";
      }
      __name(Se, "Se");
      var Le = (function() {
        return s(/* @__PURE__ */ __name((function t2(e3) {
          n(this, t2), this.geometryFactory = e3 || new ae(), this.precisionModel = this.geometryFactory.getPrecisionModel();
        }), "t"), [{ key: "read", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = new ke(t2);
          return new xe(e3, this.geometryFactory).parse();
        }, "value") }, { key: "write", value: /* @__PURE__ */ __name(function(t2) {
          return Se(t2);
        }, "value") }]);
      })(), Ce = (function() {
        return s(/* @__PURE__ */ __name((function t2(e3) {
          n(this, t2), this.parser = new Le(e3);
        }), "t"), [{ key: "write", value: /* @__PURE__ */ __name(function(t2) {
          return this.parser.write(t2);
        }, "value") }], [{ key: "toLineString", value: /* @__PURE__ */ __name(function(t2, e3) {
          if (2 !== arguments.length) throw new Error("Not implemented");
          return "LINESTRING ( " + t2.x + " " + t2.y + ", " + e3.x + " " + e3.y + " )";
        }, "value") }]);
      })(), Re = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "getIndexAlongSegment", value: /* @__PURE__ */ __name(function(t3, e3) {
          return this.computeIntLineIndex(), this._intLineIndex[t3][e3];
        }, "value") }, { key: "getTopologySummary", value: /* @__PURE__ */ __name(function() {
          var t3 = new Jt();
          return this.isEndPoint() && t3.append(" endpoint"), this._isProper && t3.append(" proper"), this.isCollinear() && t3.append(" collinear"), t3.toString();
        }, "value") }, { key: "computeIntersection", value: /* @__PURE__ */ __name(function(t3, e3, n2, i2) {
          this._inputLines[0][0] = t3, this._inputLines[0][1] = e3, this._inputLines[1][0] = n2, this._inputLines[1][1] = i2, this._result = this.computeIntersect(t3, e3, n2, i2);
        }, "value") }, { key: "getIntersectionNum", value: /* @__PURE__ */ __name(function() {
          return this._result;
        }, "value") }, { key: "computeIntLineIndex", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) null === this._intLineIndex && (this._intLineIndex = Array(2).fill().map((function() {
            return Array(2);
          })), this.computeIntLineIndex(0), this.computeIntLineIndex(1));
          else if (1 === arguments.length) {
            var t3 = arguments[0];
            this.getEdgeDistance(t3, 0) > this.getEdgeDistance(t3, 1) ? (this._intLineIndex[t3][0] = 0, this._intLineIndex[t3][1] = 1) : (this._intLineIndex[t3][0] = 1, this._intLineIndex[t3][1] = 0);
          }
        }, "value") }, { key: "isProper", value: /* @__PURE__ */ __name(function() {
          return this.hasIntersection() && this._isProper;
        }, "value") }, { key: "setPrecisionModel", value: /* @__PURE__ */ __name(function(t3) {
          this._precisionModel = t3;
        }, "value") }, { key: "isInteriorIntersection", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) return !!this.isInteriorIntersection(0) || !!this.isInteriorIntersection(1);
          if (1 === arguments.length) {
            for (var t3 = arguments[0], e3 = 0; e3 < this._result; e3++) if (!this._intPt[e3].equals2D(this._inputLines[t3][0]) && !this._intPt[e3].equals2D(this._inputLines[t3][1])) return true;
            return false;
          }
        }, "value") }, { key: "getIntersection", value: /* @__PURE__ */ __name(function(t3) {
          return this._intPt[t3];
        }, "value") }, { key: "isEndPoint", value: /* @__PURE__ */ __name(function() {
          return this.hasIntersection() && !this._isProper;
        }, "value") }, { key: "hasIntersection", value: /* @__PURE__ */ __name(function() {
          return this._result !== t2.NO_INTERSECTION;
        }, "value") }, { key: "getEdgeDistance", value: /* @__PURE__ */ __name(function(e3, n2) {
          return t2.computeEdgeDistance(this._intPt[n2], this._inputLines[e3][0], this._inputLines[e3][1]);
        }, "value") }, { key: "isCollinear", value: /* @__PURE__ */ __name(function() {
          return this._result === t2.COLLINEAR_INTERSECTION;
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          return Ce.toLineString(this._inputLines[0][0], this._inputLines[0][1]) + " - " + Ce.toLineString(this._inputLines[1][0], this._inputLines[1][1]) + this.getTopologySummary();
        }, "value") }, { key: "getEndpoint", value: /* @__PURE__ */ __name(function(t3, e3) {
          return this._inputLines[t3][e3];
        }, "value") }, { key: "isIntersection", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = 0; e3 < this._result; e3++) if (this._intPt[e3].equals2D(t3)) return true;
          return false;
        }, "value") }, { key: "getIntersectionAlongSegment", value: /* @__PURE__ */ __name(function(t3, e3) {
          return this.computeIntLineIndex(), this._intPt[this._intLineIndex[t3][e3]];
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._result = null, this._inputLines = Array(2).fill().map((function() {
            return Array(2);
          })), this._intPt = new Array(2).fill(null), this._intLineIndex = null, this._isProper = null, this._pa = null, this._pb = null, this._precisionModel = null, this._intPt[0] = new X(), this._intPt[1] = new X(), this._pa = this._intPt[0], this._pb = this._intPt[1], this._result = 0;
        }, "value") }, { key: "computeEdgeDistance", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          var i2 = Math.abs(n2.x - e3.x), r2 = Math.abs(n2.y - e3.y), s2 = -1;
          if (t3.equals(e3)) s2 = 0;
          else if (t3.equals(n2)) s2 = i2 > r2 ? i2 : r2;
          else {
            var a2 = Math.abs(t3.x - e3.x), o2 = Math.abs(t3.y - e3.y);
            0 !== (s2 = i2 > r2 ? a2 : o2) || t3.equals(e3) || (s2 = Math.max(a2, o2));
          }
          return G.isTrue(!(0 === s2 && !t3.equals(e3)), "Bad distance calculation"), s2;
        }, "value") }, { key: "nonRobustComputeEdgeDistance", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          var i2 = t3.x - e3.x, r2 = t3.y - e3.y, s2 = Math.sqrt(i2 * i2 + r2 * r2);
          return G.isTrue(!(0 === s2 && !t3.equals(e3)), "Invalid distance calculation"), s2;
        }, "value") }]);
      })();
      Re.DONT_INTERSECT = 0, Re.DO_INTERSECT = 1, Re.COLLINEAR = 2, Re.NO_INTERSECTION = 0, Re.POINT_INTERSECTION = 1, Re.COLLINEAR_INTERSECTION = 2;
      var we = (function(t2) {
        function i2() {
          return n(this, i2), e(this, i2);
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "isInSegmentEnvelopes", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = new U(this._inputLines[0][0], this._inputLines[0][1]), n2 = new U(this._inputLines[1][0], this._inputLines[1][1]);
          return e3.contains(t3) && n2.contains(t3);
        }, "value") }, { key: "computeIntersection", value: /* @__PURE__ */ __name(function() {
          if (3 !== arguments.length) return f(i2, "computeIntersection", this, 1).apply(this, arguments);
          var t3 = arguments[0], e3 = arguments[1], n2 = arguments[2];
          if (this._isProper = false, U.intersects(e3, n2, t3) && 0 === ct.index(e3, n2, t3) && 0 === ct.index(n2, e3, t3)) return this._isProper = true, (t3.equals(e3) || t3.equals(n2)) && (this._isProper = false), this._result = Re.POINT_INTERSECTION, null;
          this._result = Re.NO_INTERSECTION;
        }, "value") }, { key: "intersection", value: /* @__PURE__ */ __name(function(t3, e3, n2, r2) {
          var s2 = this.intersectionSafe(t3, e3, n2, r2);
          return this.isInSegmentEnvelopes(s2) || (s2 = new X(i2.nearestEndpoint(t3, e3, n2, r2))), null !== this._precisionModel && this._precisionModel.makePrecise(s2), s2;
        }, "value") }, { key: "checkDD", value: /* @__PURE__ */ __name(function(t3, e3, n2, i3, r2) {
          var s2 = lt3.intersection(t3, e3, n2, i3), a2 = this.isInSegmentEnvelopes(s2);
          mt.out.println("DD in env = " + a2 + "  --------------------- " + s2), r2.distance(s2) > 1e-4 && mt.out.println("Distance = " + r2.distance(s2));
        }, "value") }, { key: "intersectionSafe", value: /* @__PURE__ */ __name(function(t3, e3, n2, r2) {
          var s2 = pt.intersection(t3, e3, n2, r2);
          return null === s2 && (s2 = i2.nearestEndpoint(t3, e3, n2, r2)), s2;
        }, "value") }, { key: "computeCollinearIntersection", value: /* @__PURE__ */ __name(function(t3, e3, n2, i3) {
          var r2 = U.intersects(t3, e3, n2), s2 = U.intersects(t3, e3, i3), a2 = U.intersects(n2, i3, t3), o2 = U.intersects(n2, i3, e3);
          return r2 && s2 ? (this._intPt[0] = n2, this._intPt[1] = i3, Re.COLLINEAR_INTERSECTION) : a2 && o2 ? (this._intPt[0] = t3, this._intPt[1] = e3, Re.COLLINEAR_INTERSECTION) : r2 && a2 ? (this._intPt[0] = n2, this._intPt[1] = t3, !n2.equals(t3) || s2 || o2 ? Re.COLLINEAR_INTERSECTION : Re.POINT_INTERSECTION) : r2 && o2 ? (this._intPt[0] = n2, this._intPt[1] = e3, !n2.equals(e3) || s2 || a2 ? Re.COLLINEAR_INTERSECTION : Re.POINT_INTERSECTION) : s2 && a2 ? (this._intPt[0] = i3, this._intPt[1] = t3, !i3.equals(t3) || r2 || o2 ? Re.COLLINEAR_INTERSECTION : Re.POINT_INTERSECTION) : s2 && o2 ? (this._intPt[0] = i3, this._intPt[1] = e3, !i3.equals(e3) || r2 || a2 ? Re.COLLINEAR_INTERSECTION : Re.POINT_INTERSECTION) : Re.NO_INTERSECTION;
        }, "value") }, { key: "computeIntersect", value: /* @__PURE__ */ __name(function(t3, e3, n2, i3) {
          if (this._isProper = false, !U.intersects(t3, e3, n2, i3)) return Re.NO_INTERSECTION;
          var r2 = ct.index(t3, e3, n2), s2 = ct.index(t3, e3, i3);
          if (r2 > 0 && s2 > 0 || r2 < 0 && s2 < 0) return Re.NO_INTERSECTION;
          var a2 = ct.index(n2, i3, t3), o2 = ct.index(n2, i3, e3);
          return a2 > 0 && o2 > 0 || a2 < 0 && o2 < 0 ? Re.NO_INTERSECTION : 0 === r2 && 0 === s2 && 0 === a2 && 0 === o2 ? this.computeCollinearIntersection(t3, e3, n2, i3) : (0 === r2 || 0 === s2 || 0 === a2 || 0 === o2 ? (this._isProper = false, t3.equals2D(n2) || t3.equals2D(i3) ? this._intPt[0] = t3 : e3.equals2D(n2) || e3.equals2D(i3) ? this._intPt[0] = e3 : 0 === r2 ? this._intPt[0] = new X(n2) : 0 === s2 ? this._intPt[0] = new X(i3) : 0 === a2 ? this._intPt[0] = new X(t3) : 0 === o2 && (this._intPt[0] = new X(e3))) : (this._isProper = true, this._intPt[0] = this.intersection(t3, e3, n2, i3)), Re.POINT_INTERSECTION);
        }, "value") }], [{ key: "nearestEndpoint", value: /* @__PURE__ */ __name(function(t3, e3, n2, i3) {
          var r2 = t3, s2 = xt.pointToSegment(t3, n2, i3), a2 = xt.pointToSegment(e3, n2, i3);
          return a2 < s2 && (s2 = a2, r2 = e3), (a2 = xt.pointToSegment(n2, t3, e3)) < s2 && (s2 = a2, r2 = n2), (a2 = xt.pointToSegment(i3, t3, e3)) < s2 && (s2 = a2, r2 = i3), r2;
        }, "value") }]);
      })(Re), Oe = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "countSegment", value: /* @__PURE__ */ __name(function(t3, e3) {
          if (t3.x < this._p.x && e3.x < this._p.x) return null;
          if (this._p.x === e3.x && this._p.y === e3.y) return this._isPointOnSegment = true, null;
          if (t3.y === this._p.y && e3.y === this._p.y) {
            var n2 = t3.x, i2 = e3.x;
            return n2 > i2 && (n2 = e3.x, i2 = t3.x), this._p.x >= n2 && this._p.x <= i2 && (this._isPointOnSegment = true), null;
          }
          if (t3.y > this._p.y && e3.y <= this._p.y || e3.y > this._p.y && t3.y <= this._p.y) {
            var r2 = ct.index(t3, e3, this._p);
            if (r2 === ct.COLLINEAR) return this._isPointOnSegment = true, null;
            e3.y < t3.y && (r2 = -r2), r2 === ct.LEFT && this._crossingCount++;
          }
        }, "value") }, { key: "isPointInPolygon", value: /* @__PURE__ */ __name(function() {
          return this.getLocation() !== H.EXTERIOR;
        }, "value") }, { key: "getLocation", value: /* @__PURE__ */ __name(function() {
          return this._isPointOnSegment ? H.BOUNDARY : this._crossingCount % 2 == 1 ? H.INTERIOR : H.EXTERIOR;
        }, "value") }, { key: "isOnSegment", value: /* @__PURE__ */ __name(function() {
          return this._isPointOnSegment;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._p = null, this._crossingCount = 0, this._isPointOnSegment = false;
          var t3 = arguments[0];
          this._p = t3;
        }, "value") }, { key: "locatePointInRing", value: /* @__PURE__ */ __name(function() {
          if (arguments[0] instanceof X && rt(arguments[1], ht)) {
            for (var e3 = arguments[1], n2 = new t2(arguments[0]), i2 = new X(), r2 = new X(), s2 = 1; s2 < e3.size(); s2++) if (e3.getCoordinate(s2, i2), e3.getCoordinate(s2 - 1, r2), n2.countSegment(i2, r2), n2.isOnSegment()) return n2.getLocation();
            return n2.getLocation();
          }
          if (arguments[0] instanceof X && arguments[1] instanceof Array) {
            for (var a2 = arguments[1], o2 = new t2(arguments[0]), u2 = 1; u2 < a2.length; u2++) {
              var l2 = a2[u2], h2 = a2[u2 - 1];
              if (o2.countSegment(l2, h2), o2.isOnSegment()) return o2.getLocation();
            }
            return o2.getLocation();
          }
        }, "value") }]);
      })(), be = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "isOnLine", value: /* @__PURE__ */ __name(function() {
          if (arguments[0] instanceof X && rt(arguments[1], ht)) {
            for (var t3 = arguments[0], e3 = arguments[1], n2 = new we(), i2 = new X(), r2 = new X(), s2 = e3.size(), a2 = 1; a2 < s2; a2++) if (e3.getCoordinate(a2 - 1, i2), e3.getCoordinate(a2, r2), n2.computeIntersection(t3, i2, r2), n2.hasIntersection()) return true;
            return false;
          }
          if (arguments[0] instanceof X && arguments[1] instanceof Array) {
            for (var o2 = arguments[0], u2 = arguments[1], l2 = new we(), h2 = 1; h2 < u2.length; h2++) {
              var c2 = u2[h2 - 1], f2 = u2[h2];
              if (l2.computeIntersection(o2, c2, f2), l2.hasIntersection()) return true;
            }
            return false;
          }
        }, "value") }, { key: "locateInRing", value: /* @__PURE__ */ __name(function(t3, e3) {
          return Oe.locatePointInRing(t3, e3);
        }, "value") }, { key: "isInRing", value: /* @__PURE__ */ __name(function(e3, n2) {
          return t2.locateInRing(e3, n2) !== H.EXTERIOR;
        }, "value") }]);
      })(), Me = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "setAllLocations", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = 0; e3 < this.location.length; e3++) this.location[e3] = t3;
        }, "value") }, { key: "isNull", value: /* @__PURE__ */ __name(function() {
          for (var t3 = 0; t3 < this.location.length; t3++) if (this.location[t3] !== H.NONE) return false;
          return true;
        }, "value") }, { key: "setAllLocationsIfNull", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = 0; e3 < this.location.length; e3++) this.location[e3] === H.NONE && (this.location[e3] = t3);
        }, "value") }, { key: "isLine", value: /* @__PURE__ */ __name(function() {
          return 1 === this.location.length;
        }, "value") }, { key: "merge", value: /* @__PURE__ */ __name(function(t3) {
          if (t3.location.length > this.location.length) {
            var e3 = new Array(3).fill(null);
            e3[$.ON] = this.location[$.ON], e3[$.LEFT] = H.NONE, e3[$.RIGHT] = H.NONE, this.location = e3;
          }
          for (var n2 = 0; n2 < this.location.length; n2++) this.location[n2] === H.NONE && n2 < t3.location.length && (this.location[n2] = t3.location[n2]);
        }, "value") }, { key: "getLocations", value: /* @__PURE__ */ __name(function() {
          return this.location;
        }, "value") }, { key: "flip", value: /* @__PURE__ */ __name(function() {
          if (this.location.length <= 1) return null;
          var t3 = this.location[$.LEFT];
          this.location[$.LEFT] = this.location[$.RIGHT], this.location[$.RIGHT] = t3;
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          var t3 = new st();
          return this.location.length > 1 && t3.append(H.toLocationSymbol(this.location[$.LEFT])), t3.append(H.toLocationSymbol(this.location[$.ON])), this.location.length > 1 && t3.append(H.toLocationSymbol(this.location[$.RIGHT])), t3.toString();
        }, "value") }, { key: "setLocations", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          this.location[$.ON] = t3, this.location[$.LEFT] = e3, this.location[$.RIGHT] = n2;
        }, "value") }, { key: "get", value: /* @__PURE__ */ __name(function(t3) {
          return t3 < this.location.length ? this.location[t3] : H.NONE;
        }, "value") }, { key: "isArea", value: /* @__PURE__ */ __name(function() {
          return this.location.length > 1;
        }, "value") }, { key: "isAnyNull", value: /* @__PURE__ */ __name(function() {
          for (var t3 = 0; t3 < this.location.length; t3++) if (this.location[t3] === H.NONE) return true;
          return false;
        }, "value") }, { key: "setLocation", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            this.setLocation($.ON, t3);
          } else if (2 === arguments.length) {
            var e3 = arguments[0], n2 = arguments[1];
            this.location[e3] = n2;
          }
        }, "value") }, { key: "init", value: /* @__PURE__ */ __name(function(t3) {
          this.location = new Array(t3).fill(null), this.setAllLocations(H.NONE);
        }, "value") }, { key: "isEqualOnSide", value: /* @__PURE__ */ __name(function(t3, e3) {
          return this.location[e3] === t3.location[e3];
        }, "value") }, { key: "allPositionsEqual", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = 0; e3 < this.location.length; e3++) if (this.location[e3] !== t3) return false;
          return true;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this.location = null, 1 === arguments.length) {
            if (arguments[0] instanceof Array) {
              var e3 = arguments[0];
              this.init(e3.length);
            } else if (Number.isInteger(arguments[0])) {
              var n2 = arguments[0];
              this.init(1), this.location[$.ON] = n2;
            } else if (arguments[0] instanceof t2) {
              var i2 = arguments[0];
              if (this.init(i2.location.length), null !== i2) for (var r2 = 0; r2 < this.location.length; r2++) this.location[r2] = i2.location[r2];
            }
          } else if (3 === arguments.length) {
            var s2 = arguments[0], a2 = arguments[1], o2 = arguments[2];
            this.init(3), this.location[$.ON] = s2, this.location[$.LEFT] = a2, this.location[$.RIGHT] = o2;
          }
        }, "value") }]);
      })(), Ae = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "getGeometryCount", value: /* @__PURE__ */ __name(function() {
          var t3 = 0;
          return this.elt[0].isNull() || t3++, this.elt[1].isNull() || t3++, t3;
        }, "value") }, { key: "setAllLocations", value: /* @__PURE__ */ __name(function(t3, e3) {
          this.elt[t3].setAllLocations(e3);
        }, "value") }, { key: "isNull", value: /* @__PURE__ */ __name(function(t3) {
          return this.elt[t3].isNull();
        }, "value") }, { key: "setAllLocationsIfNull", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            this.setAllLocationsIfNull(0, t3), this.setAllLocationsIfNull(1, t3);
          } else if (2 === arguments.length) {
            var e3 = arguments[0], n2 = arguments[1];
            this.elt[e3].setAllLocationsIfNull(n2);
          }
        }, "value") }, { key: "isLine", value: /* @__PURE__ */ __name(function(t3) {
          return this.elt[t3].isLine();
        }, "value") }, { key: "merge", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = 0; e3 < 2; e3++) null === this.elt[e3] && null !== t3.elt[e3] ? this.elt[e3] = new Me(t3.elt[e3]) : this.elt[e3].merge(t3.elt[e3]);
        }, "value") }, { key: "flip", value: /* @__PURE__ */ __name(function() {
          this.elt[0].flip(), this.elt[1].flip();
        }, "value") }, { key: "getLocation", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return this.elt[t3].get($.ON);
          }
          if (2 === arguments.length) {
            var e3 = arguments[0], n2 = arguments[1];
            return this.elt[e3].get(n2);
          }
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          var t3 = new st();
          return null !== this.elt[0] && (t3.append("A:"), t3.append(this.elt[0].toString())), null !== this.elt[1] && (t3.append(" B:"), t3.append(this.elt[1].toString())), t3.toString();
        }, "value") }, { key: "isArea", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) return this.elt[0].isArea() || this.elt[1].isArea();
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return this.elt[t3].isArea();
          }
        }, "value") }, { key: "isAnyNull", value: /* @__PURE__ */ __name(function(t3) {
          return this.elt[t3].isAnyNull();
        }, "value") }, { key: "setLocation", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length) {
            var t3 = arguments[0], e3 = arguments[1];
            this.elt[t3].setLocation($.ON, e3);
          } else if (3 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1], r2 = arguments[2];
            this.elt[n2].setLocation(i2, r2);
          }
        }, "value") }, { key: "isEqualOnSide", value: /* @__PURE__ */ __name(function(t3, e3) {
          return this.elt[0].isEqualOnSide(t3.elt[0], e3) && this.elt[1].isEqualOnSide(t3.elt[1], e3);
        }, "value") }, { key: "allPositionsEqual", value: /* @__PURE__ */ __name(function(t3, e3) {
          return this.elt[t3].allPositionsEqual(e3);
        }, "value") }, { key: "toLine", value: /* @__PURE__ */ __name(function(t3) {
          this.elt[t3].isArea() && (this.elt[t3] = new Me(this.elt[t3].location[0]));
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this.elt = new Array(2).fill(null), 1 === arguments.length) {
            if (Number.isInteger(arguments[0])) {
              var e3 = arguments[0];
              this.elt[0] = new Me(e3), this.elt[1] = new Me(e3);
            } else if (arguments[0] instanceof t2) {
              var n2 = arguments[0];
              this.elt[0] = new Me(n2.elt[0]), this.elt[1] = new Me(n2.elt[1]);
            }
          } else if (2 === arguments.length) {
            var i2 = arguments[0], r2 = arguments[1];
            this.elt[0] = new Me(H.NONE), this.elt[1] = new Me(H.NONE), this.elt[i2].setLocation(r2);
          } else if (3 === arguments.length) {
            var s2 = arguments[0], a2 = arguments[1], o2 = arguments[2];
            this.elt[0] = new Me(s2, a2, o2), this.elt[1] = new Me(s2, a2, o2);
          } else if (4 === arguments.length) {
            var u2 = arguments[0], l2 = arguments[1], h2 = arguments[2], c2 = arguments[3];
            this.elt[0] = new Me(H.NONE, H.NONE, H.NONE), this.elt[1] = new Me(H.NONE, H.NONE, H.NONE), this.elt[u2].setLocations(l2, h2, c2);
          }
        }, "value") }, { key: "toLineLabel", value: /* @__PURE__ */ __name(function(e3) {
          for (var n2 = new t2(H.NONE), i2 = 0; i2 < 2; i2++) n2.setLocation(i2, e3.getLocation(i2));
          return n2;
        }, "value") }]);
      })(), Pe = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "computeRing", value: /* @__PURE__ */ __name(function() {
          if (null !== this._ring) return null;
          for (var t2 = new Array(this._pts.size()).fill(null), e3 = 0; e3 < this._pts.size(); e3++) t2[e3] = this._pts.get(e3);
          this._ring = this._geometryFactory.createLinearRing(t2), this._isHole = ct.isCCW(this._ring.getCoordinates());
        }, "value") }, { key: "isIsolated", value: /* @__PURE__ */ __name(function() {
          return 1 === this._label.getGeometryCount();
        }, "value") }, { key: "computePoints", value: /* @__PURE__ */ __name(function(t2) {
          this._startDe = t2;
          var e3 = t2, n2 = true;
          do {
            if (null === e3) throw new gt3("Found null DirectedEdge");
            if (e3.getEdgeRing() === this) throw new gt3("Directed Edge visited twice during ring-building at " + e3.getCoordinate());
            this._edges.add(e3);
            var i2 = e3.getLabel();
            G.isTrue(i2.isArea()), this.mergeLabel(i2), this.addPoints(e3.getEdge(), e3.isForward(), n2), n2 = false, this.setEdgeRing(e3, this), e3 = this.getNext(e3);
          } while (e3 !== this._startDe);
        }, "value") }, { key: "getLinearRing", value: /* @__PURE__ */ __name(function() {
          return this._ring;
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function(t2) {
          return this._pts.get(t2);
        }, "value") }, { key: "computeMaxNodeDegree", value: /* @__PURE__ */ __name(function() {
          this._maxNodeDegree = 0;
          var t2 = this._startDe;
          do {
            var e3 = t2.getNode().getEdges().getOutgoingDegree(this);
            e3 > this._maxNodeDegree && (this._maxNodeDegree = e3), t2 = this.getNext(t2);
          } while (t2 !== this._startDe);
          this._maxNodeDegree *= 2;
        }, "value") }, { key: "addPoints", value: /* @__PURE__ */ __name(function(t2, e3, n2) {
          var i2 = t2.getCoordinates();
          if (e3) {
            var r2 = 1;
            n2 && (r2 = 0);
            for (var s2 = r2; s2 < i2.length; s2++) this._pts.add(i2[s2]);
          } else {
            var a2 = i2.length - 2;
            n2 && (a2 = i2.length - 1);
            for (var o2 = a2; o2 >= 0; o2--) this._pts.add(i2[o2]);
          }
        }, "value") }, { key: "isHole", value: /* @__PURE__ */ __name(function() {
          return this._isHole;
        }, "value") }, { key: "setInResult", value: /* @__PURE__ */ __name(function() {
          var t2 = this._startDe;
          do {
            t2.getEdge().setInResult(true), t2 = t2.getNext();
          } while (t2 !== this._startDe);
        }, "value") }, { key: "containsPoint", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = this.getLinearRing();
          if (!e3.getEnvelopeInternal().contains(t2)) return false;
          if (!be.isInRing(t2, e3.getCoordinates())) return false;
          for (var n2 = this._holes.iterator(); n2.hasNext(); ) {
            if (n2.next().containsPoint(t2)) return false;
          }
          return true;
        }, "value") }, { key: "addHole", value: /* @__PURE__ */ __name(function(t2) {
          this._holes.add(t2);
        }, "value") }, { key: "isShell", value: /* @__PURE__ */ __name(function() {
          return null === this._shell;
        }, "value") }, { key: "getLabel", value: /* @__PURE__ */ __name(function() {
          return this._label;
        }, "value") }, { key: "getEdges", value: /* @__PURE__ */ __name(function() {
          return this._edges;
        }, "value") }, { key: "getMaxNodeDegree", value: /* @__PURE__ */ __name(function() {
          return this._maxNodeDegree < 0 && this.computeMaxNodeDegree(), this._maxNodeDegree;
        }, "value") }, { key: "getShell", value: /* @__PURE__ */ __name(function() {
          return this._shell;
        }, "value") }, { key: "mergeLabel", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t2 = arguments[0];
            this.mergeLabel(t2, 0), this.mergeLabel(t2, 1);
          } else if (2 === arguments.length) {
            var e3 = arguments[1], n2 = arguments[0].getLocation(e3, $.RIGHT);
            if (n2 === H.NONE) return null;
            if (this._label.getLocation(e3) === H.NONE) return this._label.setLocation(e3, n2), null;
          }
        }, "value") }, { key: "setShell", value: /* @__PURE__ */ __name(function(t2) {
          this._shell = t2, null !== t2 && t2.addHole(this);
        }, "value") }, { key: "toPolygon", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = new Array(this._holes.size()).fill(null), n2 = 0; n2 < this._holes.size(); n2++) e3[n2] = this._holes.get(n2).getLinearRing();
          return t2.createPolygon(this.getLinearRing(), e3);
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._startDe = null, this._maxNodeDegree = -1, this._edges = new yt(), this._pts = new yt(), this._label = new Ae(H.NONE), this._ring = null, this._isHole = null, this._shell = null, this._holes = new yt(), this._geometryFactory = null, 0 === arguments.length) ;
          else if (2 === arguments.length) {
            var t2 = arguments[0], e3 = arguments[1];
            this._geometryFactory = e3, this.computePoints(t2), this.computeRing();
          }
        }, "value") }]);
      })(), De = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "setEdgeRing", value: /* @__PURE__ */ __name(function(t3, e3) {
          t3.setMinEdgeRing(e3);
        }, "value") }, { key: "getNext", value: /* @__PURE__ */ __name(function(t3) {
          return t3.getNextMin();
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          var t3 = arguments[0], e3 = arguments[1];
          Pe.constructor_.call(this, t3, e3);
        }, "value") }]);
      })(Pe), Fe = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "buildMinimalRings", value: /* @__PURE__ */ __name(function() {
          var t3 = new yt(), e3 = this._startDe;
          do {
            if (null === e3.getMinEdgeRing()) {
              var n2 = new De(e3, this._geometryFactory);
              t3.add(n2);
            }
            e3 = e3.getNext();
          } while (e3 !== this._startDe);
          return t3;
        }, "value") }, { key: "setEdgeRing", value: /* @__PURE__ */ __name(function(t3, e3) {
          t3.setEdgeRing(e3);
        }, "value") }, { key: "linkDirectedEdgesForMinimalEdgeRings", value: /* @__PURE__ */ __name(function() {
          var t3 = this._startDe;
          do {
            t3.getNode().getEdges().linkMinimalDirectedEdges(this), t3 = t3.getNext();
          } while (t3 !== this._startDe);
        }, "value") }, { key: "getNext", value: /* @__PURE__ */ __name(function(t3) {
          return t3.getNext();
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          var t3 = arguments[0], e3 = arguments[1];
          Pe.constructor_.call(this, t3, e3);
        }, "value") }]);
      })(Pe), Ge = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "setVisited", value: /* @__PURE__ */ __name(function(t2) {
          this._isVisited = t2;
        }, "value") }, { key: "setInResult", value: /* @__PURE__ */ __name(function(t2) {
          this._isInResult = t2;
        }, "value") }, { key: "isCovered", value: /* @__PURE__ */ __name(function() {
          return this._isCovered;
        }, "value") }, { key: "isCoveredSet", value: /* @__PURE__ */ __name(function() {
          return this._isCoveredSet;
        }, "value") }, { key: "setLabel", value: /* @__PURE__ */ __name(function(t2) {
          this._label = t2;
        }, "value") }, { key: "getLabel", value: /* @__PURE__ */ __name(function() {
          return this._label;
        }, "value") }, { key: "setCovered", value: /* @__PURE__ */ __name(function(t2) {
          this._isCovered = t2, this._isCoveredSet = true;
        }, "value") }, { key: "updateIM", value: /* @__PURE__ */ __name(function(t2) {
          G.isTrue(this._label.getGeometryCount() >= 2, "found partial label"), this.computeIM(t2);
        }, "value") }, { key: "isInResult", value: /* @__PURE__ */ __name(function() {
          return this._isInResult;
        }, "value") }, { key: "isVisited", value: /* @__PURE__ */ __name(function() {
          return this._isVisited;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._label = null, this._isInResult = false, this._isCovered = false, this._isCoveredSet = false, this._isVisited = false, 0 === arguments.length) ;
          else if (1 === arguments.length) {
            var t2 = arguments[0];
            this._label = t2;
          }
        }, "value") }]);
      })(), qe = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "isIncidentEdgeInResult", value: /* @__PURE__ */ __name(function() {
          for (var t3 = this.getEdges().getEdges().iterator(); t3.hasNext(); ) {
            if (t3.next().getEdge().isInResult()) return true;
          }
          return false;
        }, "value") }, { key: "isIsolated", value: /* @__PURE__ */ __name(function() {
          return 1 === this._label.getGeometryCount();
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function() {
          return this._coord;
        }, "value") }, { key: "print", value: /* @__PURE__ */ __name(function(t3) {
          t3.println("node " + this._coord + " lbl: " + this._label);
        }, "value") }, { key: "computeIM", value: /* @__PURE__ */ __name(function(t3) {
        }, "value") }, { key: "computeMergedLocation", value: /* @__PURE__ */ __name(function(t3, e3) {
          var n2 = H.NONE;
          if (n2 = this._label.getLocation(e3), !t3.isNull(e3)) {
            var i3 = t3.getLocation(e3);
            n2 !== H.BOUNDARY && (n2 = i3);
          }
          return n2;
        }, "value") }, { key: "setLabel", value: /* @__PURE__ */ __name(function() {
          if (2 !== arguments.length || !Number.isInteger(arguments[1]) || !Number.isInteger(arguments[0])) return f(i2, "setLabel", this, 1).apply(this, arguments);
          var t3 = arguments[0], e3 = arguments[1];
          null === this._label ? this._label = new Ae(t3, e3) : this._label.setLocation(t3, e3);
        }, "value") }, { key: "getEdges", value: /* @__PURE__ */ __name(function() {
          return this._edges;
        }, "value") }, { key: "mergeLabel", value: /* @__PURE__ */ __name(function() {
          if (arguments[0] instanceof i2) {
            var t3 = arguments[0];
            this.mergeLabel(t3._label);
          } else if (arguments[0] instanceof Ae) for (var e3 = arguments[0], n2 = 0; n2 < 2; n2++) {
            var r2 = this.computeMergedLocation(e3, n2);
            this._label.getLocation(n2) === H.NONE && this._label.setLocation(n2, r2);
          }
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function(t3) {
          this._edges.insert(t3), t3.setNode(this);
        }, "value") }, { key: "setLabelBoundary", value: /* @__PURE__ */ __name(function(t3) {
          if (null === this._label) return null;
          var e3 = H.NONE;
          null !== this._label && (e3 = this._label.getLocation(t3));
          var n2 = null;
          switch (e3) {
            case H.BOUNDARY:
              n2 = H.INTERIOR;
              break;
            case H.INTERIOR:
            default:
              n2 = H.BOUNDARY;
          }
          this._label.setLocation(t3, n2);
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._coord = null, this._edges = null;
          var t3 = arguments[0], e3 = arguments[1];
          this._coord = t3, this._edges = e3, this._label = new Ae(0, H.NONE);
        }, "value") }]);
      })(Ge), Ye = (function(t2) {
        function i2() {
          return n(this, i2), e(this, i2, arguments);
        }
        __name(i2, "i");
        return l(i2, t2), s(i2);
      })(ee);
      function ze(t2) {
        return null == t2 ? 0 : t2.color;
      }
      __name(ze, "ze");
      function Xe(t2) {
        return null == t2 ? null : t2.parent;
      }
      __name(Xe, "Xe");
      function Be(t2, e3) {
        null !== t2 && (t2.color = e3);
      }
      __name(Be, "Be");
      function Ue(t2) {
        return null == t2 ? null : t2.left;
      }
      __name(Ue, "Ue");
      function Ve(t2) {
        return null == t2 ? null : t2.right;
      }
      __name(Ve, "Ve");
      var He = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), (t3 = e(this, i2)).root_ = null, t3.size_ = 0, t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "get", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = this.root_; null !== e3; ) {
            var n2 = t3.compareTo(e3.key);
            if (n2 < 0) e3 = e3.left;
            else {
              if (!(n2 > 0)) return e3.value;
              e3 = e3.right;
            }
          }
          return null;
        }, "value") }, { key: "put", value: /* @__PURE__ */ __name(function(t3, e3) {
          if (null === this.root_) return this.root_ = { key: t3, value: e3, left: null, right: null, parent: null, color: 0, getValue: /* @__PURE__ */ __name(function() {
            return this.value;
          }, "getValue"), getKey: /* @__PURE__ */ __name(function() {
            return this.key;
          }, "getKey") }, this.size_ = 1, null;
          var n2, i3, r2 = this.root_;
          do {
            if (n2 = r2, (i3 = t3.compareTo(r2.key)) < 0) r2 = r2.left;
            else {
              if (!(i3 > 0)) {
                var s2 = r2.value;
                return r2.value = e3, s2;
              }
              r2 = r2.right;
            }
          } while (null !== r2);
          var a2 = { key: t3, left: null, right: null, value: e3, parent: n2, color: 0, getValue: /* @__PURE__ */ __name(function() {
            return this.value;
          }, "getValue"), getKey: /* @__PURE__ */ __name(function() {
            return this.key;
          }, "getKey") };
          return i3 < 0 ? n2.left = a2 : n2.right = a2, this.fixAfterInsertion(a2), this.size_++, null;
        }, "value") }, { key: "fixAfterInsertion", value: /* @__PURE__ */ __name(function(t3) {
          var e3;
          for (t3.color = 1; null != t3 && t3 !== this.root_ && 1 === t3.parent.color; ) Xe(t3) === Ue(Xe(Xe(t3))) ? 1 === ze(e3 = Ve(Xe(Xe(t3)))) ? (Be(Xe(t3), 0), Be(e3, 0), Be(Xe(Xe(t3)), 1), t3 = Xe(Xe(t3))) : (t3 === Ve(Xe(t3)) && (t3 = Xe(t3), this.rotateLeft(t3)), Be(Xe(t3), 0), Be(Xe(Xe(t3)), 1), this.rotateRight(Xe(Xe(t3)))) : 1 === ze(e3 = Ue(Xe(Xe(t3)))) ? (Be(Xe(t3), 0), Be(e3, 0), Be(Xe(Xe(t3)), 1), t3 = Xe(Xe(t3))) : (t3 === Ue(Xe(t3)) && (t3 = Xe(t3), this.rotateRight(t3)), Be(Xe(t3), 0), Be(Xe(Xe(t3)), 1), this.rotateLeft(Xe(Xe(t3))));
          this.root_.color = 0;
        }, "value") }, { key: "values", value: /* @__PURE__ */ __name(function() {
          var t3 = new yt(), e3 = this.getFirstEntry();
          if (null !== e3) for (t3.add(e3.value); null !== (e3 = i2.successor(e3)); ) t3.add(e3.value);
          return t3;
        }, "value") }, { key: "entrySet", value: /* @__PURE__ */ __name(function() {
          var t3 = new J(), e3 = this.getFirstEntry();
          if (null !== e3) for (t3.add(e3); null !== (e3 = i2.successor(e3)); ) t3.add(e3);
          return t3;
        }, "value") }, { key: "rotateLeft", value: /* @__PURE__ */ __name(function(t3) {
          if (null != t3) {
            var e3 = t3.right;
            t3.right = e3.left, null != e3.left && (e3.left.parent = t3), e3.parent = t3.parent, null == t3.parent ? this.root_ = e3 : t3.parent.left === t3 ? t3.parent.left = e3 : t3.parent.right = e3, e3.left = t3, t3.parent = e3;
          }
        }, "value") }, { key: "rotateRight", value: /* @__PURE__ */ __name(function(t3) {
          if (null != t3) {
            var e3 = t3.left;
            t3.left = e3.right, null != e3.right && (e3.right.parent = t3), e3.parent = t3.parent, null == t3.parent ? this.root_ = e3 : t3.parent.right === t3 ? t3.parent.right = e3 : t3.parent.left = e3, e3.right = t3, t3.parent = e3;
          }
        }, "value") }, { key: "getFirstEntry", value: /* @__PURE__ */ __name(function() {
          var t3 = this.root_;
          if (null != t3) for (; null != t3.left; ) t3 = t3.left;
          return t3;
        }, "value") }, { key: "size", value: /* @__PURE__ */ __name(function() {
          return this.size_;
        }, "value") }, { key: "containsKey", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = this.root_; null !== e3; ) {
            var n2 = t3.compareTo(e3.key);
            if (n2 < 0) e3 = e3.left;
            else {
              if (!(n2 > 0)) return true;
              e3 = e3.right;
            }
          }
          return false;
        }, "value") }], [{ key: "successor", value: /* @__PURE__ */ __name(function(t3) {
          var e3;
          if (null === t3) return null;
          if (null !== t3.right) {
            for (e3 = t3.right; null !== e3.left; ) e3 = e3.left;
            return e3;
          }
          e3 = t3.parent;
          for (var n2 = t3; null !== e3 && n2 === e3.right; ) n2 = e3, e3 = e3.parent;
          return e3;
        }, "value") }]);
      })(Ye), Ze = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "find", value: /* @__PURE__ */ __name(function(t2) {
          return this.nodeMap.get(t2);
        }, "value") }, { key: "addNode", value: /* @__PURE__ */ __name(function() {
          if (arguments[0] instanceof X) {
            var t2 = arguments[0], e3 = this.nodeMap.get(t2);
            return null === e3 && (e3 = this.nodeFact.createNode(t2), this.nodeMap.put(t2, e3)), e3;
          }
          if (arguments[0] instanceof qe) {
            var n2 = arguments[0], i2 = this.nodeMap.get(n2.getCoordinate());
            return null === i2 ? (this.nodeMap.put(n2.getCoordinate(), n2), n2) : (i2.mergeLabel(n2), i2);
          }
        }, "value") }, { key: "print", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = this.iterator(); e3.hasNext(); ) {
            e3.next().print(t2);
          }
        }, "value") }, { key: "iterator", value: /* @__PURE__ */ __name(function() {
          return this.nodeMap.values().iterator();
        }, "value") }, { key: "values", value: /* @__PURE__ */ __name(function() {
          return this.nodeMap.values();
        }, "value") }, { key: "getBoundaryNodes", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = new yt(), n2 = this.iterator(); n2.hasNext(); ) {
            var i2 = n2.next();
            i2.getLabel().getLocation(t2) === H.BOUNDARY && e3.add(i2);
          }
          return e3;
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = t2.getCoordinate();
          this.addNode(e3).add(t2);
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this.nodeMap = new He(), this.nodeFact = null;
          var t2 = arguments[0];
          this.nodeFact = t2;
        }, "value") }]);
      })(), je = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "isNorthern", value: /* @__PURE__ */ __name(function(e3) {
          return e3 === t2.NE || e3 === t2.NW;
        }, "value") }, { key: "isOpposite", value: /* @__PURE__ */ __name(function(t3, e3) {
          return t3 !== e3 && 2 === (t3 - e3 + 4) % 4;
        }, "value") }, { key: "commonHalfPlane", value: /* @__PURE__ */ __name(function(t3, e3) {
          if (t3 === e3) return t3;
          if (2 === (t3 - e3 + 4) % 4) return -1;
          var n2 = t3 < e3 ? t3 : e3;
          return 0 === n2 && 3 === (t3 > e3 ? t3 : e3) ? 3 : n2;
        }, "value") }, { key: "isInHalfPlane", value: /* @__PURE__ */ __name(function(e3, n2) {
          return n2 === t2.SE ? e3 === t2.SE || e3 === t2.SW : e3 === n2 || e3 === n2 + 1;
        }, "value") }, { key: "quadrant", value: /* @__PURE__ */ __name(function() {
          if ("number" == typeof arguments[0] && "number" == typeof arguments[1]) {
            var e3 = arguments[0], n2 = arguments[1];
            if (0 === e3 && 0 === n2) throw new m("Cannot compute the quadrant for point ( " + e3 + ", " + n2 + " )");
            return e3 >= 0 ? n2 >= 0 ? t2.NE : t2.SE : n2 >= 0 ? t2.NW : t2.SW;
          }
          if (arguments[0] instanceof X && arguments[1] instanceof X) {
            var i2 = arguments[0], r2 = arguments[1];
            if (r2.x === i2.x && r2.y === i2.y) throw new m("Cannot compute the quadrant for two identical points " + i2);
            return r2.x >= i2.x ? r2.y >= i2.y ? t2.NE : t2.SE : r2.y >= i2.y ? t2.NW : t2.SW;
          }
        }, "value") }]);
      })();
      je.NE = 0, je.NW = 1, je.SW = 2, je.SE = 3;
      var We = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "compareDirection", value: /* @__PURE__ */ __name(function(t3) {
          return this._dx === t3._dx && this._dy === t3._dy ? 0 : this._quadrant > t3._quadrant ? 1 : this._quadrant < t3._quadrant ? -1 : ct.index(t3._p0, t3._p1, this._p1);
        }, "value") }, { key: "getDy", value: /* @__PURE__ */ __name(function() {
          return this._dy;
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function() {
          return this._p0;
        }, "value") }, { key: "setNode", value: /* @__PURE__ */ __name(function(t3) {
          this._node = t3;
        }, "value") }, { key: "print", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = Math.atan2(this._dy, this._dx), n2 = this.getClass().getName(), i2 = n2.lastIndexOf("."), r2 = n2.substring(i2 + 1);
          t3.print("  " + r2 + ": " + this._p0 + " - " + this._p1 + " " + this._quadrant + ":" + e3 + "   " + this._label);
        }, "value") }, { key: "compareTo", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = t3;
          return this.compareDirection(e3);
        }, "value") }, { key: "getDirectedCoordinate", value: /* @__PURE__ */ __name(function() {
          return this._p1;
        }, "value") }, { key: "getDx", value: /* @__PURE__ */ __name(function() {
          return this._dx;
        }, "value") }, { key: "getLabel", value: /* @__PURE__ */ __name(function() {
          return this._label;
        }, "value") }, { key: "getEdge", value: /* @__PURE__ */ __name(function() {
          return this._edge;
        }, "value") }, { key: "getQuadrant", value: /* @__PURE__ */ __name(function() {
          return this._quadrant;
        }, "value") }, { key: "getNode", value: /* @__PURE__ */ __name(function() {
          return this._node;
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          var t3 = Math.atan2(this._dy, this._dx), e3 = this.getClass().getName(), n2 = e3.lastIndexOf(".");
          return "  " + e3.substring(n2 + 1) + ": " + this._p0 + " - " + this._p1 + " " + this._quadrant + ":" + t3 + "   " + this._label;
        }, "value") }, { key: "computeLabel", value: /* @__PURE__ */ __name(function(t3) {
        }, "value") }, { key: "init", value: /* @__PURE__ */ __name(function(t3, e3) {
          this._p0 = t3, this._p1 = e3, this._dx = e3.x - t3.x, this._dy = e3.y - t3.y, this._quadrant = je.quadrant(this._dx, this._dy), G.isTrue(!(0 === this._dx && 0 === this._dy), "EdgeEnd with identical endpoints found");
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [x];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._edge = null, this._label = null, this._node = null, this._p0 = null, this._p1 = null, this._dx = null, this._dy = null, this._quadrant = null, 1 === arguments.length) {
            var e3 = arguments[0];
            this._edge = e3;
          } else if (3 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1], r2 = arguments[2];
            t2.constructor_.call(this, n2, i2, r2, null);
          } else if (4 === arguments.length) {
            var s2 = arguments[0], a2 = arguments[1], o2 = arguments[2], u2 = arguments[3];
            t2.constructor_.call(this, s2), this.init(a2, o2), this._label = u2;
          }
        }, "value") }]);
      })(), Ke = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "getNextMin", value: /* @__PURE__ */ __name(function() {
          return this._nextMin;
        }, "value") }, { key: "getDepth", value: /* @__PURE__ */ __name(function(t3) {
          return this._depth[t3];
        }, "value") }, { key: "setVisited", value: /* @__PURE__ */ __name(function(t3) {
          this._isVisited = t3;
        }, "value") }, { key: "computeDirectedLabel", value: /* @__PURE__ */ __name(function() {
          this._label = new Ae(this._edge.getLabel()), this._isForward || this._label.flip();
        }, "value") }, { key: "getNext", value: /* @__PURE__ */ __name(function() {
          return this._next;
        }, "value") }, { key: "setDepth", value: /* @__PURE__ */ __name(function(t3, e3) {
          if (-999 !== this._depth[t3] && this._depth[t3] !== e3) throw new gt3("assigned depths do not match", this.getCoordinate());
          this._depth[t3] = e3;
        }, "value") }, { key: "isInteriorAreaEdge", value: /* @__PURE__ */ __name(function() {
          for (var t3 = true, e3 = 0; e3 < 2; e3++) this._label.isArea(e3) && this._label.getLocation(e3, $.LEFT) === H.INTERIOR && this._label.getLocation(e3, $.RIGHT) === H.INTERIOR || (t3 = false);
          return t3;
        }, "value") }, { key: "setNextMin", value: /* @__PURE__ */ __name(function(t3) {
          this._nextMin = t3;
        }, "value") }, { key: "print", value: /* @__PURE__ */ __name(function(t3) {
          f(i2, "print", this, 1).call(this, t3), t3.print(" " + this._depth[$.LEFT] + "/" + this._depth[$.RIGHT]), t3.print(" (" + this.getDepthDelta() + ")"), this._isInResult && t3.print(" inResult");
        }, "value") }, { key: "setMinEdgeRing", value: /* @__PURE__ */ __name(function(t3) {
          this._minEdgeRing = t3;
        }, "value") }, { key: "isLineEdge", value: /* @__PURE__ */ __name(function() {
          var t3 = this._label.isLine(0) || this._label.isLine(1), e3 = !this._label.isArea(0) || this._label.allPositionsEqual(0, H.EXTERIOR), n2 = !this._label.isArea(1) || this._label.allPositionsEqual(1, H.EXTERIOR);
          return t3 && e3 && n2;
        }, "value") }, { key: "setEdgeRing", value: /* @__PURE__ */ __name(function(t3) {
          this._edgeRing = t3;
        }, "value") }, { key: "getMinEdgeRing", value: /* @__PURE__ */ __name(function() {
          return this._minEdgeRing;
        }, "value") }, { key: "getDepthDelta", value: /* @__PURE__ */ __name(function() {
          var t3 = this._edge.getDepthDelta();
          return this._isForward || (t3 = -t3), t3;
        }, "value") }, { key: "setInResult", value: /* @__PURE__ */ __name(function(t3) {
          this._isInResult = t3;
        }, "value") }, { key: "getSym", value: /* @__PURE__ */ __name(function() {
          return this._sym;
        }, "value") }, { key: "isForward", value: /* @__PURE__ */ __name(function() {
          return this._isForward;
        }, "value") }, { key: "getEdge", value: /* @__PURE__ */ __name(function() {
          return this._edge;
        }, "value") }, { key: "printEdge", value: /* @__PURE__ */ __name(function(t3) {
          this.print(t3), t3.print(" "), this._isForward ? this._edge.print(t3) : this._edge.printReverse(t3);
        }, "value") }, { key: "setSym", value: /* @__PURE__ */ __name(function(t3) {
          this._sym = t3;
        }, "value") }, { key: "setVisitedEdge", value: /* @__PURE__ */ __name(function(t3) {
          this.setVisited(t3), this._sym.setVisited(t3);
        }, "value") }, { key: "setEdgeDepths", value: /* @__PURE__ */ __name(function(t3, e3) {
          var n2 = this.getEdge().getDepthDelta();
          this._isForward || (n2 = -n2);
          var i3 = 1;
          t3 === $.LEFT && (i3 = -1);
          var r2 = $.opposite(t3), s2 = e3 + n2 * i3;
          this.setDepth(t3, e3), this.setDepth(r2, s2);
        }, "value") }, { key: "getEdgeRing", value: /* @__PURE__ */ __name(function() {
          return this._edgeRing;
        }, "value") }, { key: "isInResult", value: /* @__PURE__ */ __name(function() {
          return this._isInResult;
        }, "value") }, { key: "setNext", value: /* @__PURE__ */ __name(function(t3) {
          this._next = t3;
        }, "value") }, { key: "isVisited", value: /* @__PURE__ */ __name(function() {
          return this._isVisited;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._isForward = null, this._isInResult = false, this._isVisited = false, this._sym = null, this._next = null, this._nextMin = null, this._edgeRing = null, this._minEdgeRing = null, this._depth = [0, -999, -999];
          var t3 = arguments[0], e3 = arguments[1];
          if (We.constructor_.call(this, t3), this._isForward = e3, e3) this.init(t3.getCoordinate(0), t3.getCoordinate(1));
          else {
            var n2 = t3.getNumPoints() - 1;
            this.init(t3.getCoordinate(n2), t3.getCoordinate(n2 - 1));
          }
          this.computeDirectedLabel();
        }, "value") }, { key: "depthFactor", value: /* @__PURE__ */ __name(function(t3, e3) {
          return t3 === H.EXTERIOR && e3 === H.INTERIOR ? 1 : t3 === H.INTERIOR && e3 === H.EXTERIOR ? -1 : 0;
        }, "value") }]);
      })(We), Je = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "createNode", value: /* @__PURE__ */ __name(function(t2) {
          return new qe(t2, null);
        }, "value") }]);
      })(), Qe = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "printEdges", value: /* @__PURE__ */ __name(function(t2) {
          t2.println("Edges:");
          for (var e3 = 0; e3 < this._edges.size(); e3++) {
            t2.println("edge " + e3 + ":");
            var n2 = this._edges.get(e3);
            n2.print(t2), n2.eiList.print(t2);
          }
        }, "value") }, { key: "find", value: /* @__PURE__ */ __name(function(t2) {
          return this._nodes.find(t2);
        }, "value") }, { key: "addNode", value: /* @__PURE__ */ __name(function() {
          if (arguments[0] instanceof qe) {
            var t2 = arguments[0];
            return this._nodes.addNode(t2);
          }
          if (arguments[0] instanceof X) {
            var e3 = arguments[0];
            return this._nodes.addNode(e3);
          }
        }, "value") }, { key: "getNodeIterator", value: /* @__PURE__ */ __name(function() {
          return this._nodes.iterator();
        }, "value") }, { key: "linkResultDirectedEdges", value: /* @__PURE__ */ __name(function() {
          for (var t2 = this._nodes.iterator(); t2.hasNext(); ) {
            t2.next().getEdges().linkResultDirectedEdges();
          }
        }, "value") }, { key: "debugPrintln", value: /* @__PURE__ */ __name(function(t2) {
          mt.out.println(t2);
        }, "value") }, { key: "isBoundaryNode", value: /* @__PURE__ */ __name(function(t2, e3) {
          var n2 = this._nodes.find(e3);
          if (null === n2) return false;
          var i2 = n2.getLabel();
          return null !== i2 && i2.getLocation(t2) === H.BOUNDARY;
        }, "value") }, { key: "linkAllDirectedEdges", value: /* @__PURE__ */ __name(function() {
          for (var t2 = this._nodes.iterator(); t2.hasNext(); ) {
            t2.next().getEdges().linkAllDirectedEdges();
          }
        }, "value") }, { key: "matchInSameDirection", value: /* @__PURE__ */ __name(function(t2, e3, n2, i2) {
          return !!t2.equals(n2) && (ct.index(t2, e3, i2) === ct.COLLINEAR && je.quadrant(t2, e3) === je.quadrant(n2, i2));
        }, "value") }, { key: "getEdgeEnds", value: /* @__PURE__ */ __name(function() {
          return this._edgeEndList;
        }, "value") }, { key: "debugPrint", value: /* @__PURE__ */ __name(function(t2) {
          mt.out.print(t2);
        }, "value") }, { key: "getEdgeIterator", value: /* @__PURE__ */ __name(function() {
          return this._edges.iterator();
        }, "value") }, { key: "findEdgeInSameDirection", value: /* @__PURE__ */ __name(function(t2, e3) {
          for (var n2 = 0; n2 < this._edges.size(); n2++) {
            var i2 = this._edges.get(n2), r2 = i2.getCoordinates();
            if (this.matchInSameDirection(t2, e3, r2[0], r2[1])) return i2;
            if (this.matchInSameDirection(t2, e3, r2[r2.length - 1], r2[r2.length - 2])) return i2;
          }
          return null;
        }, "value") }, { key: "insertEdge", value: /* @__PURE__ */ __name(function(t2) {
          this._edges.add(t2);
        }, "value") }, { key: "findEdgeEnd", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = this.getEdgeEnds().iterator(); e3.hasNext(); ) {
            var n2 = e3.next();
            if (n2.getEdge() === t2) return n2;
          }
          return null;
        }, "value") }, { key: "addEdges", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = t2.iterator(); e3.hasNext(); ) {
            var n2 = e3.next();
            this._edges.add(n2);
            var i2 = new Ke(n2, true), r2 = new Ke(n2, false);
            i2.setSym(r2), r2.setSym(i2), this.add(i2), this.add(r2);
          }
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function(t2) {
          this._nodes.add(t2), this._edgeEndList.add(t2);
        }, "value") }, { key: "getNodes", value: /* @__PURE__ */ __name(function() {
          return this._nodes.values();
        }, "value") }, { key: "findEdge", value: /* @__PURE__ */ __name(function(t2, e3) {
          for (var n2 = 0; n2 < this._edges.size(); n2++) {
            var i2 = this._edges.get(n2), r2 = i2.getCoordinates();
            if (t2.equals(r2[0]) && e3.equals(r2[1])) return i2;
          }
          return null;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._edges = new yt(), this._nodes = null, this._edgeEndList = new yt(), 0 === arguments.length) this._nodes = new Ze(new Je());
          else if (1 === arguments.length) {
            var t2 = arguments[0];
            this._nodes = new Ze(t2);
          }
        }, "value") }, { key: "linkResultDirectedEdges", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = t2.iterator(); e3.hasNext(); ) {
            e3.next().getEdges().linkResultDirectedEdges();
          }
        }, "value") }]);
      })(), $e = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "sortShellsAndHoles", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          for (var i2 = t3.iterator(); i2.hasNext(); ) {
            var r2 = i2.next();
            r2.isHole() ? n2.add(r2) : e3.add(r2);
          }
        }, "value") }, { key: "computePolygons", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = new yt(), n2 = t3.iterator(); n2.hasNext(); ) {
            var i2 = n2.next().toPolygon(this._geometryFactory);
            e3.add(i2);
          }
          return e3;
        }, "value") }, { key: "placeFreeHoles", value: /* @__PURE__ */ __name(function(e3, n2) {
          for (var i2 = n2.iterator(); i2.hasNext(); ) {
            var r2 = i2.next();
            if (null === r2.getShell()) {
              var s2 = t2.findEdgeRingContaining(r2, e3);
              if (null === s2) throw new gt3("unable to assign hole to a shell", r2.getCoordinate(0));
              r2.setShell(s2);
            }
          }
        }, "value") }, { key: "buildMinimalEdgeRings", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          for (var i2 = new yt(), r2 = t3.iterator(); r2.hasNext(); ) {
            var s2 = r2.next();
            if (s2.getMaxNodeDegree() > 2) {
              s2.linkDirectedEdgesForMinimalEdgeRings();
              var a2 = s2.buildMinimalRings(), o2 = this.findShell(a2);
              null !== o2 ? (this.placePolygonHoles(o2, a2), e3.add(o2)) : n2.addAll(a2);
            } else i2.add(s2);
          }
          return i2;
        }, "value") }, { key: "buildMaximalEdgeRings", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = new yt(), n2 = t3.iterator(); n2.hasNext(); ) {
            var i2 = n2.next();
            if (i2.isInResult() && i2.getLabel().isArea() && null === i2.getEdgeRing()) {
              var r2 = new Fe(i2, this._geometryFactory);
              e3.add(r2), r2.setInResult();
            }
          }
          return e3;
        }, "value") }, { key: "placePolygonHoles", value: /* @__PURE__ */ __name(function(t3, e3) {
          for (var n2 = e3.iterator(); n2.hasNext(); ) {
            var i2 = n2.next();
            i2.isHole() && i2.setShell(t3);
          }
        }, "value") }, { key: "getPolygons", value: /* @__PURE__ */ __name(function() {
          return this.computePolygons(this._shellList);
        }, "value") }, { key: "findShell", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = 0, n2 = null, i2 = t3.iterator(); i2.hasNext(); ) {
            var r2 = i2.next();
            r2.isHole() || (n2 = r2, e3++);
          }
          return G.isTrue(e3 <= 1, "found two shells in MinimalEdgeRing list"), n2;
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            this.add(t3.getEdgeEnds(), t3.getNodes());
          } else if (2 === arguments.length) {
            var e3 = arguments[0], n2 = arguments[1];
            Qe.linkResultDirectedEdges(n2);
            var i2 = this.buildMaximalEdgeRings(e3), r2 = new yt(), s2 = this.buildMinimalEdgeRings(i2, this._shellList, r2);
            this.sortShellsAndHoles(s2, this._shellList, r2), this.placeFreeHoles(this._shellList, r2);
          }
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._geometryFactory = null, this._shellList = new yt();
          var t3 = arguments[0];
          this._geometryFactory = t3;
        }, "value") }, { key: "findEdgeRingContaining", value: /* @__PURE__ */ __name(function(t3, e3) {
          for (var n2 = t3.getLinearRing(), i2 = n2.getEnvelopeInternal(), r2 = n2.getCoordinateN(0), s2 = null, a2 = null, o2 = e3.iterator(); o2.hasNext(); ) {
            var u2 = o2.next(), l2 = u2.getLinearRing(), h2 = l2.getEnvelopeInternal();
            if (!h2.equals(i2) && h2.contains(i2)) {
              r2 = jt.ptNotInList(n2.getCoordinates(), l2.getCoordinates());
              var c2 = false;
              be.isInRing(r2, l2.getCoordinates()) && (c2 = true), c2 && (null === s2 || a2.contains(h2)) && (a2 = (s2 = u2).getLinearRing().getEnvelopeInternal());
            }
          }
          return s2;
        }, "value") }]);
      })(), tn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "getBounds", value: /* @__PURE__ */ __name(function() {
        }, "value") }]);
      })(), en = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "getItem", value: /* @__PURE__ */ __name(function() {
          return this._item;
        }, "value") }, { key: "getBounds", value: /* @__PURE__ */ __name(function() {
          return this._bounds;
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [tn, E];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._bounds = null, this._item = null;
          var t2 = arguments[0], e3 = arguments[1];
          this._bounds = t2, this._item = e3;
        }, "value") }]);
      })(), nn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "poll", value: /* @__PURE__ */ __name(function() {
          if (this.isEmpty()) return null;
          var t2 = this._items.get(1);
          return this._items.set(1, this._items.get(this._size)), this._size -= 1, this.reorder(1), t2;
        }, "value") }, { key: "size", value: /* @__PURE__ */ __name(function() {
          return this._size;
        }, "value") }, { key: "reorder", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = null, n2 = this._items.get(t2); 2 * t2 <= this._size && ((e3 = 2 * t2) !== this._size && this._items.get(e3 + 1).compareTo(this._items.get(e3)) < 0 && e3++, this._items.get(e3).compareTo(n2) < 0); t2 = e3) this._items.set(t2, this._items.get(e3));
          this._items.set(t2, n2);
        }, "value") }, { key: "clear", value: /* @__PURE__ */ __name(function() {
          this._size = 0, this._items.clear();
        }, "value") }, { key: "peek", value: /* @__PURE__ */ __name(function() {
          return this.isEmpty() ? null : this._items.get(1);
        }, "value") }, { key: "isEmpty", value: /* @__PURE__ */ __name(function() {
          return 0 === this._size;
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function(t2) {
          this._items.add(null), this._size += 1;
          var e3 = this._size;
          for (this._items.set(0, t2); t2.compareTo(this._items.get(Math.trunc(e3 / 2))) < 0; e3 /= 2) this._items.set(e3, this._items.get(Math.trunc(e3 / 2)));
          this._items.set(e3, t2);
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._size = null, this._items = null, this._size = 0, this._items = new yt(), this._items.add(null);
        }, "value") }]);
      })(), rn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "insert", value: /* @__PURE__ */ __name(function(t2, e3) {
        }, "value") }, { key: "remove", value: /* @__PURE__ */ __name(function(t2, e3) {
        }, "value") }, { key: "query", value: /* @__PURE__ */ __name(function() {
        }, "value") }]);
      })(), sn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "getLevel", value: /* @__PURE__ */ __name(function() {
          return this._level;
        }, "value") }, { key: "size", value: /* @__PURE__ */ __name(function() {
          return this._childBoundables.size();
        }, "value") }, { key: "getChildBoundables", value: /* @__PURE__ */ __name(function() {
          return this._childBoundables;
        }, "value") }, { key: "addChildBoundable", value: /* @__PURE__ */ __name(function(t2) {
          G.isTrue(null === this._bounds), this._childBoundables.add(t2);
        }, "value") }, { key: "isEmpty", value: /* @__PURE__ */ __name(function() {
          return this._childBoundables.isEmpty();
        }, "value") }, { key: "getBounds", value: /* @__PURE__ */ __name(function() {
          return null === this._bounds && (this._bounds = this.computeBounds()), this._bounds;
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [tn, E];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._childBoundables = new yt(), this._bounds = null, this._level = null, 0 === arguments.length) ;
          else if (1 === arguments.length) {
            var t2 = arguments[0];
            this._level = t2;
          }
        }, "value") }]);
      })(), an = { reverseOrder: /* @__PURE__ */ __name(function() {
        return { compare: /* @__PURE__ */ __name(function(t2, e3) {
          return e3.compareTo(t2);
        }, "compare") };
      }, "reverseOrder"), min: /* @__PURE__ */ __name(function(t2) {
        return an.sort(t2), t2.get(0);
      }, "min"), sort: /* @__PURE__ */ __name(function(t2, e3) {
        var n2 = t2.toArray();
        e3 ? At.sort(n2, e3) : At.sort(n2);
        for (var i2 = t2.iterator(), r2 = 0, s2 = n2.length; r2 < s2; r2++) i2.next(), i2.set(n2[r2]);
      }, "sort"), singletonList: /* @__PURE__ */ __name(function(t2) {
        var e3 = new yt();
        return e3.add(t2), e3;
      }, "singletonList") }, on = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "maxDistance", value: /* @__PURE__ */ __name(function(e3, n2, i2, r2, s2, a2, o2, u2) {
          var l2 = t2.distance(e3, n2, s2, a2);
          return l2 = Math.max(l2, t2.distance(e3, n2, o2, u2)), l2 = Math.max(l2, t2.distance(i2, r2, s2, a2)), l2 = Math.max(l2, t2.distance(i2, r2, o2, u2));
        }, "value") }, { key: "distance", value: /* @__PURE__ */ __name(function(t3, e3, n2, i2) {
          var r2 = n2 - t3, s2 = i2 - e3;
          return Math.sqrt(r2 * r2 + s2 * s2);
        }, "value") }, { key: "maximumDistance", value: /* @__PURE__ */ __name(function(e3, n2) {
          var i2 = Math.min(e3.getMinX(), n2.getMinX()), r2 = Math.min(e3.getMinY(), n2.getMinY()), s2 = Math.max(e3.getMaxX(), n2.getMaxX()), a2 = Math.max(e3.getMaxY(), n2.getMaxY());
          return t2.distance(i2, r2, s2, a2);
        }, "value") }, { key: "minMaxDistance", value: /* @__PURE__ */ __name(function(e3, n2) {
          var i2 = e3.getMinX(), r2 = e3.getMinY(), s2 = e3.getMaxX(), a2 = e3.getMaxY(), o2 = n2.getMinX(), u2 = n2.getMinY(), l2 = n2.getMaxX(), h2 = n2.getMaxY(), c2 = t2.maxDistance(i2, r2, i2, a2, o2, u2, o2, h2);
          return c2 = Math.min(c2, t2.maxDistance(i2, r2, i2, a2, o2, u2, l2, u2)), c2 = Math.min(c2, t2.maxDistance(i2, r2, i2, a2, l2, h2, o2, h2)), c2 = Math.min(c2, t2.maxDistance(i2, r2, i2, a2, l2, h2, l2, u2)), c2 = Math.min(c2, t2.maxDistance(i2, r2, s2, r2, o2, u2, o2, h2)), c2 = Math.min(c2, t2.maxDistance(i2, r2, s2, r2, o2, u2, l2, u2)), c2 = Math.min(c2, t2.maxDistance(i2, r2, s2, r2, l2, h2, o2, h2)), c2 = Math.min(c2, t2.maxDistance(i2, r2, s2, r2, l2, h2, l2, u2)), c2 = Math.min(c2, t2.maxDistance(s2, a2, i2, a2, o2, u2, o2, h2)), c2 = Math.min(c2, t2.maxDistance(s2, a2, i2, a2, o2, u2, l2, u2)), c2 = Math.min(c2, t2.maxDistance(s2, a2, i2, a2, l2, h2, o2, h2)), c2 = Math.min(c2, t2.maxDistance(s2, a2, i2, a2, l2, h2, l2, u2)), c2 = Math.min(c2, t2.maxDistance(s2, a2, s2, r2, o2, u2, o2, h2)), c2 = Math.min(c2, t2.maxDistance(s2, a2, s2, r2, o2, u2, l2, u2)), c2 = Math.min(c2, t2.maxDistance(s2, a2, s2, r2, l2, h2, o2, h2)), c2 = Math.min(c2, t2.maxDistance(s2, a2, s2, r2, l2, h2, l2, u2));
        }, "value") }]);
      })(), un = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "maximumDistance", value: /* @__PURE__ */ __name(function() {
          return on.maximumDistance(this._boundable1.getBounds(), this._boundable2.getBounds());
        }, "value") }, { key: "expandToQueue", value: /* @__PURE__ */ __name(function(e3, n2) {
          var i2 = t2.isComposite(this._boundable1), r2 = t2.isComposite(this._boundable2);
          if (i2 && r2) return t2.area(this._boundable1) > t2.area(this._boundable2) ? (this.expand(this._boundable1, this._boundable2, false, e3, n2), null) : (this.expand(this._boundable2, this._boundable1, true, e3, n2), null);
          if (i2) return this.expand(this._boundable1, this._boundable2, false, e3, n2), null;
          if (r2) return this.expand(this._boundable2, this._boundable1, true, e3, n2), null;
          throw new m("neither boundable is composite");
        }, "value") }, { key: "isLeaves", value: /* @__PURE__ */ __name(function() {
          return !(t2.isComposite(this._boundable1) || t2.isComposite(this._boundable2));
        }, "value") }, { key: "compareTo", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = t3;
          return this._distance < e3._distance ? -1 : this._distance > e3._distance ? 1 : 0;
        }, "value") }, { key: "expand", value: /* @__PURE__ */ __name(function(e3, n2, i2, r2, s2) {
          for (var a2 = e3.getChildBoundables().iterator(); a2.hasNext(); ) {
            var o2 = a2.next(), u2 = null;
            (u2 = i2 ? new t2(n2, o2, this._itemDistance) : new t2(o2, n2, this._itemDistance)).getDistance() < s2 && r2.add(u2);
          }
        }, "value") }, { key: "getBoundable", value: /* @__PURE__ */ __name(function(t3) {
          return 0 === t3 ? this._boundable1 : this._boundable2;
        }, "value") }, { key: "getDistance", value: /* @__PURE__ */ __name(function() {
          return this._distance;
        }, "value") }, { key: "distance", value: /* @__PURE__ */ __name(function() {
          return this.isLeaves() ? this._itemDistance.distance(this._boundable1, this._boundable2) : this._boundable1.getBounds().distance(this._boundable2.getBounds());
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [x];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._boundable1 = null, this._boundable2 = null, this._distance = null, this._itemDistance = null;
          var t3 = arguments[0], e3 = arguments[1], n2 = arguments[2];
          this._boundable1 = t3, this._boundable2 = e3, this._itemDistance = n2, this._distance = this.distance();
        }, "value") }, { key: "area", value: /* @__PURE__ */ __name(function(t3) {
          return t3.getBounds().getArea();
        }, "value") }, { key: "isComposite", value: /* @__PURE__ */ __name(function(t3) {
          return t3 instanceof sn;
        }, "value") }]);
      })(), ln = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "visitItem", value: /* @__PURE__ */ __name(function(t2) {
        }, "value") }]);
      })(), hn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "queryInternal", value: /* @__PURE__ */ __name(function() {
          if (rt(arguments[2], ln) && arguments[0] instanceof Object && arguments[1] instanceof sn) for (var t3 = arguments[0], e3 = arguments[2], n2 = arguments[1].getChildBoundables(), i2 = 0; i2 < n2.size(); i2++) {
            var r2 = n2.get(i2);
            this.getIntersectsOp().intersects(r2.getBounds(), t3) && (r2 instanceof sn ? this.queryInternal(t3, r2, e3) : r2 instanceof en ? e3.visitItem(r2.getItem()) : G.shouldNeverReachHere());
          }
          else if (rt(arguments[2], nt) && arguments[0] instanceof Object && arguments[1] instanceof sn) for (var s2 = arguments[0], a2 = arguments[2], o2 = arguments[1].getChildBoundables(), u2 = 0; u2 < o2.size(); u2++) {
            var l2 = o2.get(u2);
            this.getIntersectsOp().intersects(l2.getBounds(), s2) && (l2 instanceof sn ? this.queryInternal(s2, l2, a2) : l2 instanceof en ? a2.add(l2.getItem()) : G.shouldNeverReachHere());
          }
        }, "value") }, { key: "getNodeCapacity", value: /* @__PURE__ */ __name(function() {
          return this._nodeCapacity;
        }, "value") }, { key: "lastNode", value: /* @__PURE__ */ __name(function(t3) {
          return t3.get(t3.size() - 1);
        }, "value") }, { key: "size", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) return this.isEmpty() ? 0 : (this.build(), this.size(this._root));
          if (1 === arguments.length) {
            for (var t3 = 0, e3 = arguments[0].getChildBoundables().iterator(); e3.hasNext(); ) {
              var n2 = e3.next();
              n2 instanceof sn ? t3 += this.size(n2) : n2 instanceof en && (t3 += 1);
            }
            return t3;
          }
        }, "value") }, { key: "removeItem", value: /* @__PURE__ */ __name(function(t3, e3) {
          for (var n2 = null, i2 = t3.getChildBoundables().iterator(); i2.hasNext(); ) {
            var r2 = i2.next();
            r2 instanceof en && r2.getItem() === e3 && (n2 = r2);
          }
          return null !== n2 && (t3.getChildBoundables().remove(n2), true);
        }, "value") }, { key: "itemsTree", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) {
            this.build();
            var t3 = this.itemsTree(this._root);
            return null === t3 ? new yt() : t3;
          }
          if (1 === arguments.length) {
            for (var e3 = arguments[0], n2 = new yt(), i2 = e3.getChildBoundables().iterator(); i2.hasNext(); ) {
              var r2 = i2.next();
              if (r2 instanceof sn) {
                var s2 = this.itemsTree(r2);
                null !== s2 && n2.add(s2);
              } else r2 instanceof en ? n2.add(r2.getItem()) : G.shouldNeverReachHere();
            }
            return n2.size() <= 0 ? null : n2;
          }
        }, "value") }, { key: "insert", value: /* @__PURE__ */ __name(function(t3, e3) {
          G.isTrue(!this._built, "Cannot insert items into an STR packed R-tree after it has been built."), this._itemBoundables.add(new en(t3, e3));
        }, "value") }, { key: "boundablesAtLevel", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t3 = arguments[0], e3 = new yt();
            return this.boundablesAtLevel(t3, this._root, e3), e3;
          }
          if (3 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1], r2 = arguments[2];
            if (G.isTrue(n2 > -2), i2.getLevel() === n2) return r2.add(i2), null;
            for (var s2 = i2.getChildBoundables().iterator(); s2.hasNext(); ) {
              var a2 = s2.next();
              a2 instanceof sn ? this.boundablesAtLevel(n2, a2, r2) : (G.isTrue(a2 instanceof en), -1 === n2 && r2.add(a2));
            }
            return null;
          }
        }, "value") }, { key: "query", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            this.build();
            var e3 = new yt();
            return this.isEmpty() || this.getIntersectsOp().intersects(this._root.getBounds(), t3) && this.queryInternal(t3, this._root, e3), e3;
          }
          if (2 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1];
            if (this.build(), this.isEmpty()) return null;
            this.getIntersectsOp().intersects(this._root.getBounds(), n2) && this.queryInternal(n2, this._root, i2);
          }
        }, "value") }, { key: "build", value: /* @__PURE__ */ __name(function() {
          if (this._built) return null;
          this._root = this._itemBoundables.isEmpty() ? this.createNode(0) : this.createHigherLevels(this._itemBoundables, -1), this._itemBoundables = null, this._built = true;
        }, "value") }, { key: "getRoot", value: /* @__PURE__ */ __name(function() {
          return this.build(), this._root;
        }, "value") }, { key: "remove", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length) {
            var t3 = arguments[0], e3 = arguments[1];
            return this.build(), !!this.getIntersectsOp().intersects(this._root.getBounds(), t3) && this.remove(t3, this._root, e3);
          }
          if (3 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1], r2 = arguments[2], s2 = this.removeItem(i2, r2);
            if (s2) return true;
            for (var a2 = null, o2 = i2.getChildBoundables().iterator(); o2.hasNext(); ) {
              var u2 = o2.next();
              if (this.getIntersectsOp().intersects(u2.getBounds(), n2) && (u2 instanceof sn && (s2 = this.remove(n2, u2, r2)))) {
                a2 = u2;
                break;
              }
            }
            return null !== a2 && a2.getChildBoundables().isEmpty() && i2.getChildBoundables().remove(a2), s2;
          }
        }, "value") }, { key: "createHigherLevels", value: /* @__PURE__ */ __name(function(t3, e3) {
          G.isTrue(!t3.isEmpty());
          var n2 = this.createParentBoundables(t3, e3 + 1);
          return 1 === n2.size() ? n2.get(0) : this.createHigherLevels(n2, e3 + 1);
        }, "value") }, { key: "depth", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) return this.isEmpty() ? 0 : (this.build(), this.depth(this._root));
          if (1 === arguments.length) {
            for (var t3 = 0, e3 = arguments[0].getChildBoundables().iterator(); e3.hasNext(); ) {
              var n2 = e3.next();
              if (n2 instanceof sn) {
                var i2 = this.depth(n2);
                i2 > t3 && (t3 = i2);
              }
            }
            return t3 + 1;
          }
        }, "value") }, { key: "createParentBoundables", value: /* @__PURE__ */ __name(function(t3, e3) {
          G.isTrue(!t3.isEmpty());
          var n2 = new yt();
          n2.add(this.createNode(e3));
          var i2 = new yt(t3);
          an.sort(i2, this.getComparator());
          for (var r2 = i2.iterator(); r2.hasNext(); ) {
            var s2 = r2.next();
            this.lastNode(n2).getChildBoundables().size() === this.getNodeCapacity() && n2.add(this.createNode(e3)), this.lastNode(n2).addChildBoundable(s2);
          }
          return n2;
        }, "value") }, { key: "isEmpty", value: /* @__PURE__ */ __name(function() {
          return this._built ? this._root.isEmpty() : this._itemBoundables.isEmpty();
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [E];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._root = null, this._built = false, this._itemBoundables = new yt(), this._nodeCapacity = null, 0 === arguments.length) t2.constructor_.call(this, t2.DEFAULT_NODE_CAPACITY);
          else if (1 === arguments.length) {
            var e3 = arguments[0];
            G.isTrue(e3 > 1, "Node capacity must be greater than 1"), this._nodeCapacity = e3;
          }
        }, "value") }, { key: "compareDoubles", value: /* @__PURE__ */ __name(function(t3, e3) {
          return t3 > e3 ? 1 : t3 < e3 ? -1 : 0;
        }, "value") }]);
      })();
      hn.IntersectsOp = function() {
      }, hn.DEFAULT_NODE_CAPACITY = 10;
      var cn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "distance", value: /* @__PURE__ */ __name(function(t2, e3) {
        }, "value") }]);
      })(), fn = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "createParentBoundablesFromVerticalSlices", value: /* @__PURE__ */ __name(function(t3, e3) {
          G.isTrue(t3.length > 0);
          for (var n2 = new yt(), i3 = 0; i3 < t3.length; i3++) n2.addAll(this.createParentBoundablesFromVerticalSlice(t3[i3], e3));
          return n2;
        }, "value") }, { key: "nearestNeighbourK", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length) {
            var t3 = arguments[0], e3 = arguments[1];
            return this.nearestNeighbourK(t3, A.POSITIVE_INFINITY, e3);
          }
          if (3 === arguments.length) {
            var n2 = arguments[0], r2 = arguments[2], s2 = arguments[1], a2 = new nn();
            a2.add(n2);
            for (var o2 = new nn(); !a2.isEmpty() && s2 >= 0; ) {
              var u2 = a2.poll(), l2 = u2.getDistance();
              if (l2 >= s2) break;
              if (u2.isLeaves()) if (o2.size() < r2) o2.add(u2);
              else o2.peek().getDistance() > l2 && (o2.poll(), o2.add(u2)), s2 = o2.peek().getDistance();
              else u2.expandToQueue(a2, s2);
            }
            return i2.getItems(o2);
          }
        }, "value") }, { key: "createNode", value: /* @__PURE__ */ __name(function(t3) {
          return new gn(t3);
        }, "value") }, { key: "size", value: /* @__PURE__ */ __name(function() {
          return 0 === arguments.length ? f(i2, "size", this, 1).call(this) : f(i2, "size", this, 1).apply(this, arguments);
        }, "value") }, { key: "insert", value: /* @__PURE__ */ __name(function() {
          if (!(2 === arguments.length && arguments[1] instanceof Object && arguments[0] instanceof U)) return f(i2, "insert", this, 1).apply(this, arguments);
          var t3 = arguments[0], e3 = arguments[1];
          if (t3.isNull()) return null;
          f(i2, "insert", this, 1).call(this, t3, e3);
        }, "value") }, { key: "getIntersectsOp", value: /* @__PURE__ */ __name(function() {
          return i2.intersectsOp;
        }, "value") }, { key: "verticalSlices", value: /* @__PURE__ */ __name(function(t3, e3) {
          for (var n2 = Math.trunc(Math.ceil(t3.size() / e3)), i3 = new Array(e3).fill(null), r2 = t3.iterator(), s2 = 0; s2 < e3; s2++) {
            i3[s2] = new yt();
            for (var a2 = 0; r2.hasNext() && a2 < n2; ) {
              var o2 = r2.next();
              i3[s2].add(o2), a2++;
            }
          }
          return i3;
        }, "value") }, { key: "query", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return f(i2, "query", this, 1).call(this, t3);
          }
          if (2 === arguments.length) {
            var e3 = arguments[0], n2 = arguments[1];
            f(i2, "query", this, 1).call(this, e3, n2);
          }
        }, "value") }, { key: "getComparator", value: /* @__PURE__ */ __name(function() {
          return i2.yComparator;
        }, "value") }, { key: "createParentBoundablesFromVerticalSlice", value: /* @__PURE__ */ __name(function(t3, e3) {
          return f(i2, "createParentBoundables", this, 1).call(this, t3, e3);
        }, "value") }, { key: "remove", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length && arguments[1] instanceof Object && arguments[0] instanceof U) {
            var t3 = arguments[0], e3 = arguments[1];
            return f(i2, "remove", this, 1).call(this, t3, e3);
          }
          return f(i2, "remove", this, 1).apply(this, arguments);
        }, "value") }, { key: "depth", value: /* @__PURE__ */ __name(function() {
          return 0 === arguments.length ? f(i2, "depth", this, 1).call(this) : f(i2, "depth", this, 1).apply(this, arguments);
        }, "value") }, { key: "createParentBoundables", value: /* @__PURE__ */ __name(function(t3, e3) {
          G.isTrue(!t3.isEmpty());
          var n2 = Math.trunc(Math.ceil(t3.size() / this.getNodeCapacity())), r2 = new yt(t3);
          an.sort(r2, i2.xComparator);
          var s2 = this.verticalSlices(r2, Math.trunc(Math.ceil(Math.sqrt(n2))));
          return this.createParentBoundablesFromVerticalSlices(s2, e3);
        }, "value") }, { key: "nearestNeighbour", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            if (rt(arguments[0], cn)) {
              var t3 = arguments[0];
              if (this.isEmpty()) return null;
              var e3 = new un(this.getRoot(), this.getRoot(), t3);
              return this.nearestNeighbour(e3);
            }
            if (arguments[0] instanceof un) {
              var n2 = arguments[0], i3 = A.POSITIVE_INFINITY, r2 = null, s2 = new nn();
              for (s2.add(n2); !s2.isEmpty() && i3 > 0; ) {
                var a2 = s2.poll(), o2 = a2.getDistance();
                if (o2 >= i3) break;
                a2.isLeaves() ? (i3 = o2, r2 = a2) : a2.expandToQueue(s2, i3);
              }
              return null === r2 ? null : [r2.getBoundable(0).getItem(), r2.getBoundable(1).getItem()];
            }
          } else {
            if (2 === arguments.length) {
              var u2 = arguments[0], l2 = arguments[1];
              if (this.isEmpty() || u2.isEmpty()) return null;
              var h2 = new un(this.getRoot(), u2.getRoot(), l2);
              return this.nearestNeighbour(h2);
            }
            if (3 === arguments.length) {
              var c2 = arguments[2], f2 = new en(arguments[0], arguments[1]), g2 = new un(this.getRoot(), f2, c2);
              return this.nearestNeighbour(g2)[0];
            }
            if (4 === arguments.length) {
              var v2 = arguments[2], y2 = arguments[3], d2 = new en(arguments[0], arguments[1]), _2 = new un(this.getRoot(), d2, v2);
              return this.nearestNeighbourK(_2, y2);
            }
          }
        }, "value") }, { key: "isWithinDistance", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length) {
            var t3 = arguments[0], e3 = arguments[1], n2 = A.POSITIVE_INFINITY, i3 = new nn();
            for (i3.add(t3); !i3.isEmpty(); ) {
              var r2 = i3.poll(), s2 = r2.getDistance();
              if (s2 > e3) return false;
              if (r2.maximumDistance() <= e3) return true;
              if (r2.isLeaves()) {
                if ((n2 = s2) <= e3) return true;
              } else r2.expandToQueue(i3, n2);
            }
            return false;
          }
          if (3 === arguments.length) {
            var a2 = arguments[0], o2 = arguments[1], u2 = arguments[2], l2 = new un(this.getRoot(), a2.getRoot(), o2);
            return this.isWithinDistance(l2, u2);
          }
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [rn, E];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) i2.constructor_.call(this, i2.DEFAULT_NODE_CAPACITY);
          else if (1 === arguments.length) {
            var t3 = arguments[0];
            hn.constructor_.call(this, t3);
          }
        }, "value") }, { key: "centreX", value: /* @__PURE__ */ __name(function(t3) {
          return i2.avg(t3.getMinX(), t3.getMaxX());
        }, "value") }, { key: "avg", value: /* @__PURE__ */ __name(function(t3, e3) {
          return (t3 + e3) / 2;
        }, "value") }, { key: "getItems", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = new Array(t3.size()).fill(null), n2 = 0; !t3.isEmpty(); ) {
            var i3 = t3.poll();
            e3[n2] = i3.getBoundable(0).getItem(), n2++;
          }
          return e3;
        }, "value") }, { key: "centreY", value: /* @__PURE__ */ __name(function(t3) {
          return i2.avg(t3.getMinY(), t3.getMaxY());
        }, "value") }]);
      })(hn), gn = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "computeBounds", value: /* @__PURE__ */ __name(function() {
          for (var t3 = null, e3 = this.getChildBoundables().iterator(); e3.hasNext(); ) {
            var n2 = e3.next();
            null === t3 ? t3 = new U(n2.getBounds()) : t3.expandToInclude(n2.getBounds());
          }
          return t3;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          var t3 = arguments[0];
          sn.constructor_.call(this, t3);
        }, "value") }]);
      })(sn);
      fn.STRtreeNode = gn, fn.xComparator = new ((function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [P];
        }, "get") }, { key: "compare", value: /* @__PURE__ */ __name(function(t2, e3) {
          return hn.compareDoubles(fn.centreX(t2.getBounds()), fn.centreX(e3.getBounds()));
        }, "value") }]);
      })())(), fn.yComparator = new ((function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [P];
        }, "get") }, { key: "compare", value: /* @__PURE__ */ __name(function(t2, e3) {
          return hn.compareDoubles(fn.centreY(t2.getBounds()), fn.centreY(e3.getBounds()));
        }, "value") }]);
      })())(), fn.intersectsOp = new ((function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [IntersectsOp];
        }, "get") }, { key: "intersects", value: /* @__PURE__ */ __name(function(t2, e3) {
          return t2.intersects(e3);
        }, "value") }]);
      })())(), fn.DEFAULT_NODE_CAPACITY = 10;
      var vn = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "relativeSign", value: /* @__PURE__ */ __name(function(t3, e3) {
          return t3 < e3 ? -1 : t3 > e3 ? 1 : 0;
        }, "value") }, { key: "compare", value: /* @__PURE__ */ __name(function(e3, n2, i2) {
          if (n2.equals2D(i2)) return 0;
          var r2 = t2.relativeSign(n2.x, i2.x), s2 = t2.relativeSign(n2.y, i2.y);
          switch (e3) {
            case 0:
              return t2.compareValue(r2, s2);
            case 1:
              return t2.compareValue(s2, r2);
            case 2:
              return t2.compareValue(s2, -r2);
            case 3:
              return t2.compareValue(-r2, s2);
            case 4:
              return t2.compareValue(-r2, -s2);
            case 5:
              return t2.compareValue(-s2, -r2);
            case 6:
              return t2.compareValue(-s2, r2);
            case 7:
              return t2.compareValue(r2, -s2);
          }
          return G.shouldNeverReachHere("invalid octant value"), 0;
        }, "value") }, { key: "compareValue", value: /* @__PURE__ */ __name(function(t3, e3) {
          return t3 < 0 ? -1 : t3 > 0 ? 1 : e3 < 0 ? -1 : e3 > 0 ? 1 : 0;
        }, "value") }]);
      })(), yn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "getCoordinate", value: /* @__PURE__ */ __name(function() {
          return this.coord;
        }, "value") }, { key: "print", value: /* @__PURE__ */ __name(function(t2) {
          t2.print(this.coord), t2.print(" seg # = " + this.segmentIndex);
        }, "value") }, { key: "compareTo", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = t2;
          return this.segmentIndex < e3.segmentIndex ? -1 : this.segmentIndex > e3.segmentIndex ? 1 : this.coord.equals2D(e3.coord) ? 0 : this._isInterior ? e3._isInterior ? vn.compare(this._segmentOctant, this.coord, e3.coord) : 1 : -1;
        }, "value") }, { key: "isEndPoint", value: /* @__PURE__ */ __name(function(t2) {
          return 0 === this.segmentIndex && !this._isInterior || this.segmentIndex === t2;
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          return this.segmentIndex + ":" + this.coord.toString();
        }, "value") }, { key: "isInterior", value: /* @__PURE__ */ __name(function() {
          return this._isInterior;
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [x];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._segString = null, this.coord = null, this.segmentIndex = null, this._segmentOctant = null, this._isInterior = null;
          var t2 = arguments[0], e3 = arguments[1], n2 = arguments[2], i2 = arguments[3];
          this._segString = t2, this.coord = new X(e3), this.segmentIndex = n2, this._segmentOctant = i2, this._isInterior = !e3.equals2D(t2.getCoordinate(n2));
        }, "value") }]);
      })(), dn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "hasNext", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "next", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "remove", value: /* @__PURE__ */ __name(function() {
        }, "value") }]);
      })(), _n = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "getSplitCoordinates", value: /* @__PURE__ */ __name(function() {
          var t2 = new Zt();
          this.addEndpoints();
          for (var e3 = this.iterator(), n2 = e3.next(); e3.hasNext(); ) {
            var i2 = e3.next();
            this.addEdgeCoordinates(n2, i2, t2), n2 = i2;
          }
          return t2.toCoordinateArray();
        }, "value") }, { key: "addCollapsedNodes", value: /* @__PURE__ */ __name(function() {
          var t2 = new yt();
          this.findCollapsesFromInsertedNodes(t2), this.findCollapsesFromExistingVertices(t2);
          for (var e3 = t2.iterator(); e3.hasNext(); ) {
            var n2 = e3.next().intValue();
            this.add(this._edge.getCoordinate(n2), n2);
          }
        }, "value") }, { key: "createSplitEdgePts", value: /* @__PURE__ */ __name(function(t2, e3) {
          var n2 = e3.segmentIndex - t2.segmentIndex + 2;
          if (2 === n2) return [new X(t2.coord), new X(e3.coord)];
          var i2 = this._edge.getCoordinate(e3.segmentIndex), r2 = e3.isInterior() || !e3.coord.equals2D(i2);
          r2 || n2--;
          var s2 = new Array(n2).fill(null), a2 = 0;
          s2[a2++] = new X(t2.coord);
          for (var o2 = t2.segmentIndex + 1; o2 <= e3.segmentIndex; o2++) s2[a2++] = this._edge.getCoordinate(o2);
          return r2 && (s2[a2] = new X(e3.coord)), s2;
        }, "value") }, { key: "print", value: /* @__PURE__ */ __name(function(t2) {
          t2.println("Intersections:");
          for (var e3 = this.iterator(); e3.hasNext(); ) {
            e3.next().print(t2);
          }
        }, "value") }, { key: "findCollapsesFromExistingVertices", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = 0; e3 < this._edge.size() - 2; e3++) {
            var n2 = this._edge.getCoordinate(e3);
            this._edge.getCoordinate(e3 + 1);
            var i2 = this._edge.getCoordinate(e3 + 2);
            n2.equals2D(i2) && t2.add(at.valueOf(e3 + 1));
          }
        }, "value") }, { key: "addEdgeCoordinates", value: /* @__PURE__ */ __name(function(t2, e3, n2) {
          var i2 = this.createSplitEdgePts(t2, e3);
          n2.add(i2, false);
        }, "value") }, { key: "iterator", value: /* @__PURE__ */ __name(function() {
          return this._nodeMap.values().iterator();
        }, "value") }, { key: "addSplitEdges", value: /* @__PURE__ */ __name(function(t2) {
          this.addEndpoints(), this.addCollapsedNodes();
          for (var e3 = this.iterator(), n2 = e3.next(); e3.hasNext(); ) {
            var i2 = e3.next(), r2 = this.createSplitEdge(n2, i2);
            t2.add(r2), n2 = i2;
          }
        }, "value") }, { key: "findCollapseIndex", value: /* @__PURE__ */ __name(function(t2, e3, n2) {
          if (!t2.coord.equals2D(e3.coord)) return false;
          var i2 = e3.segmentIndex - t2.segmentIndex;
          return e3.isInterior() || i2--, 1 === i2 && (n2[0] = t2.segmentIndex + 1, true);
        }, "value") }, { key: "findCollapsesFromInsertedNodes", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = new Array(1).fill(null), n2 = this.iterator(), i2 = n2.next(); n2.hasNext(); ) {
            var r2 = n2.next();
            this.findCollapseIndex(i2, r2, e3) && t2.add(at.valueOf(e3[0])), i2 = r2;
          }
        }, "value") }, { key: "getEdge", value: /* @__PURE__ */ __name(function() {
          return this._edge;
        }, "value") }, { key: "addEndpoints", value: /* @__PURE__ */ __name(function() {
          var t2 = this._edge.size() - 1;
          this.add(this._edge.getCoordinate(0), 0), this.add(this._edge.getCoordinate(t2), t2);
        }, "value") }, { key: "createSplitEdge", value: /* @__PURE__ */ __name(function(t2, e3) {
          var n2 = this.createSplitEdgePts(t2, e3);
          return new xn(n2, this._edge.getData());
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function(t2, e3) {
          var n2 = new yn(this._edge, t2, e3, this._edge.getSegmentOctant(e3)), i2 = this._nodeMap.get(n2);
          return null !== i2 ? (G.isTrue(i2.coord.equals2D(t2), "Found equal nodes with different coordinates"), i2) : (this._nodeMap.put(n2, n2), n2);
        }, "value") }, { key: "checkSplitEdgesCorrectness", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = this._edge.getCoordinates(), n2 = t2.get(0).getCoordinate(0);
          if (!n2.equals2D(e3[0])) throw new D("bad split edge start point at " + n2);
          var i2 = t2.get(t2.size() - 1).getCoordinates(), r2 = i2[i2.length - 1];
          if (!r2.equals2D(e3[e3.length - 1])) throw new D("bad split edge end point at " + r2);
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._nodeMap = new He(), this._edge = null;
          var t2 = arguments[0];
          this._edge = t2;
        }, "value") }]);
      })(), pn = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "octant", value: /* @__PURE__ */ __name(function() {
          if ("number" == typeof arguments[0] && "number" == typeof arguments[1]) {
            var e3 = arguments[0], n2 = arguments[1];
            if (0 === e3 && 0 === n2) throw new m("Cannot compute the octant for point ( " + e3 + ", " + n2 + " )");
            var i2 = Math.abs(e3), r2 = Math.abs(n2);
            return e3 >= 0 ? n2 >= 0 ? i2 >= r2 ? 0 : 1 : i2 >= r2 ? 7 : 6 : n2 >= 0 ? i2 >= r2 ? 3 : 2 : i2 >= r2 ? 4 : 5;
          }
          if (arguments[0] instanceof X && arguments[1] instanceof X) {
            var s2 = arguments[0], a2 = arguments[1], o2 = a2.x - s2.x, u2 = a2.y - s2.y;
            if (0 === o2 && 0 === u2) throw new m("Cannot compute the octant for two identical points " + s2);
            return t2.octant(o2, u2);
          }
        }, "value") }]);
      })(), mn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "getCoordinates", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "size", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function(t2) {
        }, "value") }, { key: "isClosed", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "setData", value: /* @__PURE__ */ __name(function(t2) {
        }, "value") }, { key: "getData", value: /* @__PURE__ */ __name(function() {
        }, "value") }]);
      })(), kn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "addIntersection", value: /* @__PURE__ */ __name(function(t2, e3) {
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [mn];
        }, "get") }]);
      })(), xn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "getCoordinates", value: /* @__PURE__ */ __name(function() {
          return this._pts;
        }, "value") }, { key: "size", value: /* @__PURE__ */ __name(function() {
          return this._pts.length;
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function(t3) {
          return this._pts[t3];
        }, "value") }, { key: "isClosed", value: /* @__PURE__ */ __name(function() {
          return this._pts[0].equals(this._pts[this._pts.length - 1]);
        }, "value") }, { key: "getSegmentOctant", value: /* @__PURE__ */ __name(function(t3) {
          return t3 === this._pts.length - 1 ? -1 : this.safeOctant(this.getCoordinate(t3), this.getCoordinate(t3 + 1));
        }, "value") }, { key: "setData", value: /* @__PURE__ */ __name(function(t3) {
          this._data = t3;
        }, "value") }, { key: "safeOctant", value: /* @__PURE__ */ __name(function(t3, e3) {
          return t3.equals2D(e3) ? 0 : pn.octant(t3, e3);
        }, "value") }, { key: "getData", value: /* @__PURE__ */ __name(function() {
          return this._data;
        }, "value") }, { key: "addIntersection", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length) {
            var t3 = arguments[0], e3 = arguments[1];
            this.addIntersectionNode(t3, e3);
          } else if (4 === arguments.length) {
            var n2 = arguments[1], i2 = arguments[3], r2 = new X(arguments[0].getIntersection(i2));
            this.addIntersection(r2, n2);
          }
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          return Ce.toLineString(new Qt(this._pts));
        }, "value") }, { key: "getNodeList", value: /* @__PURE__ */ __name(function() {
          return this._nodeList;
        }, "value") }, { key: "addIntersectionNode", value: /* @__PURE__ */ __name(function(t3, e3) {
          var n2 = e3, i2 = n2 + 1;
          if (i2 < this._pts.length) {
            var r2 = this._pts[i2];
            t3.equals2D(r2) && (n2 = i2);
          }
          return this._nodeList.add(t3, n2);
        }, "value") }, { key: "addIntersections", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          for (var i2 = 0; i2 < t3.getIntersectionNum(); i2++) this.addIntersection(t3, e3, n2, i2);
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [kn];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._nodeList = new _n(this), this._pts = null, this._data = null;
          var t3 = arguments[0], e3 = arguments[1];
          this._pts = t3, this._data = e3;
        }, "value") }, { key: "getNodedSubstrings", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var e3 = arguments[0], n2 = new yt();
            return t2.getNodedSubstrings(e3, n2), n2;
          }
          if (2 === arguments.length) for (var i2 = arguments[1], r2 = arguments[0].iterator(); r2.hasNext(); ) {
            r2.next().getNodeList().addSplitEdges(i2);
          }
        }, "value") }]);
      })(), In = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "minX", value: /* @__PURE__ */ __name(function() {
          return Math.min(this.p0.x, this.p1.x);
        }, "value") }, { key: "orientationIndex", value: /* @__PURE__ */ __name(function() {
          if (arguments[0] instanceof t2) {
            var e3 = arguments[0], n2 = ct.index(this.p0, this.p1, e3.p0), i2 = ct.index(this.p0, this.p1, e3.p1);
            return n2 >= 0 && i2 >= 0 || n2 <= 0 && i2 <= 0 ? Math.max(n2, i2) : 0;
          }
          if (arguments[0] instanceof X) {
            var r2 = arguments[0];
            return ct.index(this.p0, this.p1, r2);
          }
        }, "value") }, { key: "toGeometry", value: /* @__PURE__ */ __name(function(t3) {
          return t3.createLineString([this.p0, this.p1]);
        }, "value") }, { key: "isVertical", value: /* @__PURE__ */ __name(function() {
          return this.p0.x === this.p1.x;
        }, "value") }, { key: "equals", value: /* @__PURE__ */ __name(function(e3) {
          if (!(e3 instanceof t2)) return false;
          var n2 = e3;
          return this.p0.equals(n2.p0) && this.p1.equals(n2.p1);
        }, "value") }, { key: "intersection", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = new we();
          return e3.computeIntersection(this.p0, this.p1, t3.p0, t3.p1), e3.hasIntersection() ? e3.getIntersection(0) : null;
        }, "value") }, { key: "project", value: /* @__PURE__ */ __name(function() {
          if (arguments[0] instanceof X) {
            var e3 = arguments[0];
            if (e3.equals(this.p0) || e3.equals(this.p1)) return new X(e3);
            var n2 = this.projectionFactor(e3), i2 = new X();
            return i2.x = this.p0.x + n2 * (this.p1.x - this.p0.x), i2.y = this.p0.y + n2 * (this.p1.y - this.p0.y), i2;
          }
          if (arguments[0] instanceof t2) {
            var r2 = arguments[0], s2 = this.projectionFactor(r2.p0), a2 = this.projectionFactor(r2.p1);
            if (s2 >= 1 && a2 >= 1) return null;
            if (s2 <= 0 && a2 <= 0) return null;
            var o2 = this.project(r2.p0);
            s2 < 0 && (o2 = this.p0), s2 > 1 && (o2 = this.p1);
            var u2 = this.project(r2.p1);
            return a2 < 0 && (u2 = this.p0), a2 > 1 && (u2 = this.p1), new t2(o2, u2);
          }
        }, "value") }, { key: "normalize", value: /* @__PURE__ */ __name(function() {
          this.p1.compareTo(this.p0) < 0 && this.reverse();
        }, "value") }, { key: "angle", value: /* @__PURE__ */ __name(function() {
          return Math.atan2(this.p1.y - this.p0.y, this.p1.x - this.p0.x);
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function(t3) {
          return 0 === t3 ? this.p0 : this.p1;
        }, "value") }, { key: "distancePerpendicular", value: /* @__PURE__ */ __name(function(t3) {
          return xt.pointToLinePerpendicular(t3, this.p0, this.p1);
        }, "value") }, { key: "minY", value: /* @__PURE__ */ __name(function() {
          return Math.min(this.p0.y, this.p1.y);
        }, "value") }, { key: "midPoint", value: /* @__PURE__ */ __name(function() {
          return t2.midPoint(this.p0, this.p1);
        }, "value") }, { key: "projectionFactor", value: /* @__PURE__ */ __name(function(t3) {
          if (t3.equals(this.p0)) return 0;
          if (t3.equals(this.p1)) return 1;
          var e3 = this.p1.x - this.p0.x, n2 = this.p1.y - this.p0.y, i2 = e3 * e3 + n2 * n2;
          return i2 <= 0 ? A.NaN : ((t3.x - this.p0.x) * e3 + (t3.y - this.p0.y) * n2) / i2;
        }, "value") }, { key: "closestPoints", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = this.intersection(t3);
          if (null !== e3) return [e3, e3];
          var n2 = new Array(2).fill(null), i2 = A.MAX_VALUE, r2 = null, s2 = this.closestPoint(t3.p0);
          i2 = s2.distance(t3.p0), n2[0] = s2, n2[1] = t3.p0;
          var a2 = this.closestPoint(t3.p1);
          (r2 = a2.distance(t3.p1)) < i2 && (i2 = r2, n2[0] = a2, n2[1] = t3.p1);
          var o2 = t3.closestPoint(this.p0);
          (r2 = o2.distance(this.p0)) < i2 && (i2 = r2, n2[0] = this.p0, n2[1] = o2);
          var u2 = t3.closestPoint(this.p1);
          return (r2 = u2.distance(this.p1)) < i2 && (i2 = r2, n2[0] = this.p1, n2[1] = u2), n2;
        }, "value") }, { key: "closestPoint", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = this.projectionFactor(t3);
          return e3 > 0 && e3 < 1 ? this.project(t3) : this.p0.distance(t3) < this.p1.distance(t3) ? this.p0 : this.p1;
        }, "value") }, { key: "maxX", value: /* @__PURE__ */ __name(function() {
          return Math.max(this.p0.x, this.p1.x);
        }, "value") }, { key: "getLength", value: /* @__PURE__ */ __name(function() {
          return this.p0.distance(this.p1);
        }, "value") }, { key: "compareTo", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = t3, n2 = this.p0.compareTo(e3.p0);
          return 0 !== n2 ? n2 : this.p1.compareTo(e3.p1);
        }, "value") }, { key: "reverse", value: /* @__PURE__ */ __name(function() {
          var t3 = this.p0;
          this.p0 = this.p1, this.p1 = t3;
        }, "value") }, { key: "equalsTopo", value: /* @__PURE__ */ __name(function(t3) {
          return this.p0.equals(t3.p0) && this.p1.equals(t3.p1) || this.p0.equals(t3.p1) && this.p1.equals(t3.p0);
        }, "value") }, { key: "lineIntersection", value: /* @__PURE__ */ __name(function(t3) {
          return pt.intersection(this.p0, this.p1, t3.p0, t3.p1);
        }, "value") }, { key: "maxY", value: /* @__PURE__ */ __name(function() {
          return Math.max(this.p0.y, this.p1.y);
        }, "value") }, { key: "pointAlongOffset", value: /* @__PURE__ */ __name(function(t3, e3) {
          var n2 = this.p0.x + t3 * (this.p1.x - this.p0.x), i2 = this.p0.y + t3 * (this.p1.y - this.p0.y), r2 = this.p1.x - this.p0.x, s2 = this.p1.y - this.p0.y, a2 = Math.sqrt(r2 * r2 + s2 * s2), o2 = 0, u2 = 0;
          if (0 !== e3) {
            if (a2 <= 0) throw new IllegalStateException("Cannot compute offset from zero-length line segment");
            o2 = e3 * r2 / a2, u2 = e3 * s2 / a2;
          }
          return new X(n2 - u2, i2 + o2);
        }, "value") }, { key: "setCoordinates", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            this.setCoordinates(t3.p0, t3.p1);
          } else if (2 === arguments.length) {
            var e3 = arguments[0], n2 = arguments[1];
            this.p0.x = e3.x, this.p0.y = e3.y, this.p1.x = n2.x, this.p1.y = n2.y;
          }
        }, "value") }, { key: "segmentFraction", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = this.projectionFactor(t3);
          return e3 < 0 ? e3 = 0 : (e3 > 1 || A.isNaN(e3)) && (e3 = 1), e3;
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          return "LINESTRING( " + this.p0.x + " " + this.p0.y + ", " + this.p1.x + " " + this.p1.y + ")";
        }, "value") }, { key: "isHorizontal", value: /* @__PURE__ */ __name(function() {
          return this.p0.y === this.p1.y;
        }, "value") }, { key: "reflect", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = this.p1.getY() - this.p0.getY(), n2 = this.p0.getX() - this.p1.getX(), i2 = this.p0.getY() * (this.p1.getX() - this.p0.getX()) - this.p0.getX() * (this.p1.getY() - this.p0.getY()), r2 = e3 * e3 + n2 * n2, s2 = e3 * e3 - n2 * n2, a2 = t3.getX(), o2 = t3.getY();
          return new X((-s2 * a2 - 2 * e3 * n2 * o2 - 2 * e3 * i2) / r2, (s2 * o2 - 2 * e3 * n2 * a2 - 2 * n2 * i2) / r2);
        }, "value") }, { key: "distance", value: /* @__PURE__ */ __name(function() {
          if (arguments[0] instanceof t2) {
            var e3 = arguments[0];
            return xt.segmentToSegment(this.p0, this.p1, e3.p0, e3.p1);
          }
          if (arguments[0] instanceof X) {
            var n2 = arguments[0];
            return xt.pointToSegment(n2, this.p0, this.p1);
          }
        }, "value") }, { key: "pointAlong", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = new X();
          return e3.x = this.p0.x + t3 * (this.p1.x - this.p0.x), e3.y = this.p0.y + t3 * (this.p1.y - this.p0.y), e3;
        }, "value") }, { key: "hashCode", value: /* @__PURE__ */ __name(function() {
          var t3 = A.doubleToLongBits(this.p0.x);
          t3 ^= 31 * A.doubleToLongBits(this.p0.y);
          var e3 = Math.trunc(t3) ^ Math.trunc(t3 >> 32), n2 = A.doubleToLongBits(this.p1.x);
          return n2 ^= 31 * A.doubleToLongBits(this.p1.y), e3 ^ (Math.trunc(n2) ^ Math.trunc(n2 >> 32));
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [x, E];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this.p0 = null, this.p1 = null, 0 === arguments.length) t2.constructor_.call(this, new X(), new X());
          else if (1 === arguments.length) {
            var e3 = arguments[0];
            t2.constructor_.call(this, e3.p0, e3.p1);
          } else if (2 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1];
            this.p0 = n2, this.p1 = i2;
          } else if (4 === arguments.length) {
            var r2 = arguments[0], s2 = arguments[1], a2 = arguments[2], o2 = arguments[3];
            t2.constructor_.call(this, new X(r2, s2), new X(a2, o2));
          }
        }, "value") }, { key: "midPoint", value: /* @__PURE__ */ __name(function(t3, e3) {
          return new X((t3.x + e3.x) / 2, (t3.y + e3.y) / 2);
        }, "value") }]);
      })(), En = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "overlap", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length) ;
          else if (4 === arguments.length) {
            var t2 = arguments[1], e3 = arguments[2], n2 = arguments[3];
            arguments[0].getLineSegment(t2, this._overlapSeg1), e3.getLineSegment(n2, this._overlapSeg2), this.overlap(this._overlapSeg1, this._overlapSeg2);
          }
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._overlapSeg1 = new In(), this._overlapSeg2 = new In();
        }, "value") }]);
      })(), Nn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "getLineSegment", value: /* @__PURE__ */ __name(function(t2, e3) {
          e3.p0 = this._pts[t2], e3.p1 = this._pts[t2 + 1];
        }, "value") }, { key: "computeSelect", value: /* @__PURE__ */ __name(function(t2, e3, n2, i2) {
          var r2 = this._pts[e3], s2 = this._pts[n2];
          if (n2 - e3 == 1) return i2.select(this, e3), null;
          if (!t2.intersects(r2, s2)) return null;
          var a2 = Math.trunc((e3 + n2) / 2);
          e3 < a2 && this.computeSelect(t2, e3, a2, i2), a2 < n2 && this.computeSelect(t2, a2, n2, i2);
        }, "value") }, { key: "getCoordinates", value: /* @__PURE__ */ __name(function() {
          for (var t2 = new Array(this._end - this._start + 1).fill(null), e3 = 0, n2 = this._start; n2 <= this._end; n2++) t2[e3++] = this._pts[n2];
          return t2;
        }, "value") }, { key: "computeOverlaps", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length) {
            var t2 = arguments[0], e3 = arguments[1];
            this.computeOverlaps(this._start, this._end, t2, t2._start, t2._end, e3);
          } else if (6 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1], r2 = arguments[2], s2 = arguments[3], a2 = arguments[4], o2 = arguments[5];
            if (i2 - n2 == 1 && a2 - s2 == 1) return o2.overlap(this, n2, r2, s2), null;
            if (!this.overlaps(n2, i2, r2, s2, a2)) return null;
            var u2 = Math.trunc((n2 + i2) / 2), l2 = Math.trunc((s2 + a2) / 2);
            n2 < u2 && (s2 < l2 && this.computeOverlaps(n2, u2, r2, s2, l2, o2), l2 < a2 && this.computeOverlaps(n2, u2, r2, l2, a2, o2)), u2 < i2 && (s2 < l2 && this.computeOverlaps(u2, i2, r2, s2, l2, o2), l2 < a2 && this.computeOverlaps(u2, i2, r2, l2, a2, o2));
          }
        }, "value") }, { key: "setId", value: /* @__PURE__ */ __name(function(t2) {
          this._id = t2;
        }, "value") }, { key: "select", value: /* @__PURE__ */ __name(function(t2, e3) {
          this.computeSelect(t2, this._start, this._end, e3);
        }, "value") }, { key: "getEnvelope", value: /* @__PURE__ */ __name(function() {
          if (null === this._env) {
            var t2 = this._pts[this._start], e3 = this._pts[this._end];
            this._env = new U(t2, e3);
          }
          return this._env;
        }, "value") }, { key: "overlaps", value: /* @__PURE__ */ __name(function(t2, e3, n2, i2, r2) {
          return U.intersects(this._pts[t2], this._pts[e3], n2._pts[i2], n2._pts[r2]);
        }, "value") }, { key: "getEndIndex", value: /* @__PURE__ */ __name(function() {
          return this._end;
        }, "value") }, { key: "getStartIndex", value: /* @__PURE__ */ __name(function() {
          return this._start;
        }, "value") }, { key: "getContext", value: /* @__PURE__ */ __name(function() {
          return this._context;
        }, "value") }, { key: "getId", value: /* @__PURE__ */ __name(function() {
          return this._id;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._pts = null, this._start = null, this._end = null, this._env = null, this._context = null, this._id = null;
          var t2 = arguments[0], e3 = arguments[1], n2 = arguments[2], i2 = arguments[3];
          this._pts = t2, this._start = e3, this._end = n2, this._context = i2;
        }, "value") }]);
      })(), Tn = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "findChainEnd", value: /* @__PURE__ */ __name(function(t3, e3) {
          for (var n2 = e3; n2 < t3.length - 1 && t3[n2].equals2D(t3[n2 + 1]); ) n2++;
          if (n2 >= t3.length - 1) return t3.length - 1;
          for (var i2 = je.quadrant(t3[n2], t3[n2 + 1]), r2 = e3 + 1; r2 < t3.length; ) {
            if (!t3[r2 - 1].equals2D(t3[r2])) {
              if (je.quadrant(t3[r2 - 1], t3[r2]) !== i2) break;
            }
            r2++;
          }
          return r2 - 1;
        }, "value") }, { key: "getChains", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var e3 = arguments[0];
            return t2.getChains(e3, null);
          }
          if (2 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1], r2 = new yt(), s2 = 0;
            do {
              var a2 = t2.findChainEnd(n2, s2), o2 = new Nn(n2, s2, a2, i2);
              r2.add(o2), s2 = a2;
            } while (s2 < n2.length - 1);
            return r2;
          }
        }, "value") }]);
      })(), Sn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "computeNodes", value: /* @__PURE__ */ __name(function(t2) {
        }, "value") }, { key: "getNodedSubstrings", value: /* @__PURE__ */ __name(function() {
        }, "value") }]);
      })(), Ln = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "setSegmentIntersector", value: /* @__PURE__ */ __name(function(t2) {
          this._segInt = t2;
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [Sn];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._segInt = null, 0 === arguments.length) ;
          else if (1 === arguments.length) {
            var t2 = arguments[0];
            this.setSegmentIntersector(t2);
          }
        }, "value") }]);
      })(), Cn = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "getMonotoneChains", value: /* @__PURE__ */ __name(function() {
          return this._monoChains;
        }, "value") }, { key: "getNodedSubstrings", value: /* @__PURE__ */ __name(function() {
          return xn.getNodedSubstrings(this._nodedSegStrings);
        }, "value") }, { key: "getIndex", value: /* @__PURE__ */ __name(function() {
          return this._index;
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = Tn.getChains(t3.getCoordinates(), t3).iterator(); e3.hasNext(); ) {
            var n2 = e3.next();
            n2.setId(this._idCounter++), this._index.insert(n2.getEnvelope(), n2), this._monoChains.add(n2);
          }
        }, "value") }, { key: "computeNodes", value: /* @__PURE__ */ __name(function(t3) {
          this._nodedSegStrings = t3;
          for (var e3 = t3.iterator(); e3.hasNext(); ) this.add(e3.next());
          this.intersectChains();
        }, "value") }, { key: "intersectChains", value: /* @__PURE__ */ __name(function() {
          for (var t3 = new Rn(this._segInt), e3 = this._monoChains.iterator(); e3.hasNext(); ) for (var n2 = e3.next(), i3 = this._index.query(n2.getEnvelope()).iterator(); i3.hasNext(); ) {
            var r2 = i3.next();
            if (r2.getId() > n2.getId() && (n2.computeOverlaps(r2, t3), this._nOverlaps++), this._segInt.isDone()) return null;
          }
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._monoChains = new yt(), this._index = new fn(), this._idCounter = 0, this._nodedSegStrings = null, this._nOverlaps = 0, 0 === arguments.length) ;
          else if (1 === arguments.length) {
            var t3 = arguments[0];
            Ln.constructor_.call(this, t3);
          }
        }, "value") }]);
      })(Ln), Rn = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "overlap", value: /* @__PURE__ */ __name(function() {
          if (4 !== arguments.length) return f(i2, "overlap", this, 1).apply(this, arguments);
          var t3 = arguments[1], e3 = arguments[2], n2 = arguments[3], r2 = arguments[0].getContext(), s2 = e3.getContext();
          this._si.processIntersections(r2, t3, s2, n2);
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._si = null;
          var t3 = arguments[0];
          this._si = t3;
        }, "value") }]);
      })(En);
      Cn.SegmentOverlapAction = Rn;
      var wn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "isDeletable", value: /* @__PURE__ */ __name(function(t3, e3, n2, i2) {
          var r2 = this._inputLine[t3], s2 = this._inputLine[e3], a2 = this._inputLine[n2];
          return !!this.isConcave(r2, s2, a2) && (!!this.isShallow(r2, s2, a2, i2) && this.isShallowSampled(r2, s2, t3, n2, i2));
        }, "value") }, { key: "deleteShallowConcavities", value: /* @__PURE__ */ __name(function() {
          for (var e3 = 1, n2 = this.findNextNonDeletedIndex(e3), i2 = this.findNextNonDeletedIndex(n2), r2 = false; i2 < this._inputLine.length; ) {
            var s2 = false;
            this.isDeletable(e3, n2, i2, this._distanceTol) && (this._isDeleted[n2] = t2.DELETE, s2 = true, r2 = true), e3 = s2 ? i2 : n2, n2 = this.findNextNonDeletedIndex(e3), i2 = this.findNextNonDeletedIndex(n2);
          }
          return r2;
        }, "value") }, { key: "isShallowConcavity", value: /* @__PURE__ */ __name(function(t3, e3, n2, i2) {
          return ct.index(t3, e3, n2) === this._angleOrientation && xt.pointToSegment(e3, t3, n2) < i2;
        }, "value") }, { key: "isShallowSampled", value: /* @__PURE__ */ __name(function(e3, n2, i2, r2, s2) {
          var a2 = Math.trunc((r2 - i2) / t2.NUM_PTS_TO_CHECK);
          a2 <= 0 && (a2 = 1);
          for (var o2 = i2; o2 < r2; o2 += a2) if (!this.isShallow(e3, n2, this._inputLine[o2], s2)) return false;
          return true;
        }, "value") }, { key: "isConcave", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          var i2 = ct.index(t3, e3, n2) === this._angleOrientation;
          return i2;
        }, "value") }, { key: "simplify", value: /* @__PURE__ */ __name(function(t3) {
          this._distanceTol = Math.abs(t3), t3 < 0 && (this._angleOrientation = ct.CLOCKWISE), this._isDeleted = new Array(this._inputLine.length).fill(null);
          var e3 = false;
          do {
            e3 = this.deleteShallowConcavities();
          } while (e3);
          return this.collapseLine();
        }, "value") }, { key: "findNextNonDeletedIndex", value: /* @__PURE__ */ __name(function(e3) {
          for (var n2 = e3 + 1; n2 < this._inputLine.length && this._isDeleted[n2] === t2.DELETE; ) n2++;
          return n2;
        }, "value") }, { key: "isShallow", value: /* @__PURE__ */ __name(function(t3, e3, n2, i2) {
          return xt.pointToSegment(e3, t3, n2) < i2;
        }, "value") }, { key: "collapseLine", value: /* @__PURE__ */ __name(function() {
          for (var e3 = new Zt(), n2 = 0; n2 < this._inputLine.length; n2++) this._isDeleted[n2] !== t2.DELETE && e3.add(this._inputLine[n2]);
          return e3.toCoordinateArray();
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._inputLine = null, this._distanceTol = null, this._isDeleted = null, this._angleOrientation = ct.COUNTERCLOCKWISE;
          var t3 = arguments[0];
          this._inputLine = t3;
        }, "value") }, { key: "simplify", value: /* @__PURE__ */ __name(function(e3, n2) {
          return new t2(e3).simplify(n2);
        }, "value") }]);
      })();
      wn.INIT = 0, wn.DELETE = 1, wn.KEEP = 1, wn.NUM_PTS_TO_CHECK = 10;
      var On = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "getCoordinates", value: /* @__PURE__ */ __name(function() {
          return this._ptList.toArray(t2.COORDINATE_ARRAY_TYPE);
        }, "value") }, { key: "setPrecisionModel", value: /* @__PURE__ */ __name(function(t3) {
          this._precisionModel = t3;
        }, "value") }, { key: "addPt", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = new X(t3);
          if (this._precisionModel.makePrecise(e3), this.isRedundant(e3)) return null;
          this._ptList.add(e3);
        }, "value") }, { key: "reverse", value: /* @__PURE__ */ __name(function() {
        }, "value") }, { key: "addPts", value: /* @__PURE__ */ __name(function(t3, e3) {
          if (e3) for (var n2 = 0; n2 < t3.length; n2++) this.addPt(t3[n2]);
          else for (var i2 = t3.length - 1; i2 >= 0; i2--) this.addPt(t3[i2]);
        }, "value") }, { key: "isRedundant", value: /* @__PURE__ */ __name(function(t3) {
          if (this._ptList.size() < 1) return false;
          var e3 = this._ptList.get(this._ptList.size() - 1);
          return t3.distance(e3) < this._minimimVertexDistance;
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          return new ae().createLineString(this.getCoordinates()).toString();
        }, "value") }, { key: "closeRing", value: /* @__PURE__ */ __name(function() {
          if (this._ptList.size() < 1) return null;
          var t3 = new X(this._ptList.get(0)), e3 = this._ptList.get(this._ptList.size() - 1);
          if (t3.equals(e3)) return null;
          this._ptList.add(t3);
        }, "value") }, { key: "setMinimumVertexDistance", value: /* @__PURE__ */ __name(function(t3) {
          this._minimimVertexDistance = t3;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._ptList = null, this._precisionModel = null, this._minimimVertexDistance = 0, this._ptList = new yt();
        }, "value") }]);
      })();
      On.COORDINATE_ARRAY_TYPE = new Array(0).fill(null);
      var bn = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, null, [{ key: "toDegrees", value: /* @__PURE__ */ __name(function(t3) {
          return 180 * t3 / Math.PI;
        }, "value") }, { key: "normalize", value: /* @__PURE__ */ __name(function(e3) {
          for (; e3 > Math.PI; ) e3 -= t2.PI_TIMES_2;
          for (; e3 <= -Math.PI; ) e3 += t2.PI_TIMES_2;
          return e3;
        }, "value") }, { key: "angle", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return Math.atan2(t3.y, t3.x);
          }
          if (2 === arguments.length) {
            var e3 = arguments[0], n2 = arguments[1], i2 = n2.x - e3.x, r2 = n2.y - e3.y;
            return Math.atan2(r2, i2);
          }
        }, "value") }, { key: "isAcute", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          var i2 = t3.x - e3.x, r2 = t3.y - e3.y;
          return i2 * (n2.x - e3.x) + r2 * (n2.y - e3.y) > 0;
        }, "value") }, { key: "isObtuse", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          var i2 = t3.x - e3.x, r2 = t3.y - e3.y;
          return i2 * (n2.x - e3.x) + r2 * (n2.y - e3.y) < 0;
        }, "value") }, { key: "interiorAngle", value: /* @__PURE__ */ __name(function(e3, n2, i2) {
          var r2 = t2.angle(n2, e3), s2 = t2.angle(n2, i2);
          return Math.abs(s2 - r2);
        }, "value") }, { key: "normalizePositive", value: /* @__PURE__ */ __name(function(e3) {
          if (e3 < 0) {
            for (; e3 < 0; ) e3 += t2.PI_TIMES_2;
            e3 >= t2.PI_TIMES_2 && (e3 = 0);
          } else {
            for (; e3 >= t2.PI_TIMES_2; ) e3 -= t2.PI_TIMES_2;
            e3 < 0 && (e3 = 0);
          }
          return e3;
        }, "value") }, { key: "angleBetween", value: /* @__PURE__ */ __name(function(e3, n2, i2) {
          var r2 = t2.angle(n2, e3), s2 = t2.angle(n2, i2);
          return t2.diff(r2, s2);
        }, "value") }, { key: "diff", value: /* @__PURE__ */ __name(function(t3, e3) {
          var n2 = null;
          return (n2 = t3 < e3 ? e3 - t3 : t3 - e3) > Math.PI && (n2 = 2 * Math.PI - n2), n2;
        }, "value") }, { key: "toRadians", value: /* @__PURE__ */ __name(function(t3) {
          return t3 * Math.PI / 180;
        }, "value") }, { key: "getTurn", value: /* @__PURE__ */ __name(function(e3, n2) {
          var i2 = Math.sin(n2 - e3);
          return i2 > 0 ? t2.COUNTERCLOCKWISE : i2 < 0 ? t2.CLOCKWISE : t2.NONE;
        }, "value") }, { key: "angleBetweenOriented", value: /* @__PURE__ */ __name(function(e3, n2, i2) {
          var r2 = t2.angle(n2, e3), s2 = t2.angle(n2, i2) - r2;
          return s2 <= -Math.PI ? s2 + t2.PI_TIMES_2 : s2 > Math.PI ? s2 - t2.PI_TIMES_2 : s2;
        }, "value") }]);
      })();
      bn.PI_TIMES_2 = 2 * Math.PI, bn.PI_OVER_2 = Math.PI / 2, bn.PI_OVER_4 = Math.PI / 4, bn.COUNTERCLOCKWISE = ct.COUNTERCLOCKWISE, bn.CLOCKWISE = ct.CLOCKWISE, bn.NONE = ct.COLLINEAR;
      var Mn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "addNextSegment", value: /* @__PURE__ */ __name(function(t3, e3) {
          if (this._s0 = this._s1, this._s1 = this._s2, this._s2 = t3, this._seg0.setCoordinates(this._s0, this._s1), this.computeOffsetSegment(this._seg0, this._side, this._distance, this._offset0), this._seg1.setCoordinates(this._s1, this._s2), this.computeOffsetSegment(this._seg1, this._side, this._distance, this._offset1), this._s1.equals(this._s2)) return null;
          var n2 = ct.index(this._s0, this._s1, this._s2), i2 = n2 === ct.CLOCKWISE && this._side === $.LEFT || n2 === ct.COUNTERCLOCKWISE && this._side === $.RIGHT;
          0 === n2 ? this.addCollinear(e3) : i2 ? this.addOutsideTurn(n2, e3) : this.addInsideTurn(n2, e3);
        }, "value") }, { key: "addLineEndCap", value: /* @__PURE__ */ __name(function(t3, e3) {
          var n2 = new In(t3, e3), i2 = new In();
          this.computeOffsetSegment(n2, $.LEFT, this._distance, i2);
          var r2 = new In();
          this.computeOffsetSegment(n2, $.RIGHT, this._distance, r2);
          var s2 = e3.x - t3.x, a2 = e3.y - t3.y, o2 = Math.atan2(a2, s2);
          switch (this._bufParams.getEndCapStyle()) {
            case _.CAP_ROUND:
              this._segList.addPt(i2.p1), this.addDirectedFillet(e3, o2 + Math.PI / 2, o2 - Math.PI / 2, ct.CLOCKWISE, this._distance), this._segList.addPt(r2.p1);
              break;
            case _.CAP_FLAT:
              this._segList.addPt(i2.p1), this._segList.addPt(r2.p1);
              break;
            case _.CAP_SQUARE:
              var u2 = new X();
              u2.x = Math.abs(this._distance) * Math.cos(o2), u2.y = Math.abs(this._distance) * Math.sin(o2);
              var l2 = new X(i2.p1.x + u2.x, i2.p1.y + u2.y), h2 = new X(r2.p1.x + u2.x, r2.p1.y + u2.y);
              this._segList.addPt(l2), this._segList.addPt(h2);
          }
        }, "value") }, { key: "getCoordinates", value: /* @__PURE__ */ __name(function() {
          return this._segList.getCoordinates();
        }, "value") }, { key: "addMitreJoin", value: /* @__PURE__ */ __name(function(t3, e3, n2, i2) {
          var r2 = pt.intersection(e3.p0, e3.p1, n2.p0, n2.p1);
          if (null !== r2 && (i2 <= 0 ? 1 : r2.distance(t3) / Math.abs(i2)) <= this._bufParams.getMitreLimit()) return this._segList.addPt(r2), null;
          this.addLimitedMitreJoin(e3, n2, i2, this._bufParams.getMitreLimit());
        }, "value") }, { key: "addOutsideTurn", value: /* @__PURE__ */ __name(function(e3, n2) {
          if (this._offset0.p1.distance(this._offset1.p0) < this._distance * t2.OFFSET_SEGMENT_SEPARATION_FACTOR) return this._segList.addPt(this._offset0.p1), null;
          this._bufParams.getJoinStyle() === _.JOIN_MITRE ? this.addMitreJoin(this._s1, this._offset0, this._offset1, this._distance) : this._bufParams.getJoinStyle() === _.JOIN_BEVEL ? this.addBevelJoin(this._offset0, this._offset1) : (n2 && this._segList.addPt(this._offset0.p1), this.addCornerFillet(this._s1, this._offset0.p1, this._offset1.p0, e3, this._distance), this._segList.addPt(this._offset1.p0));
        }, "value") }, { key: "createSquare", value: /* @__PURE__ */ __name(function(t3) {
          this._segList.addPt(new X(t3.x + this._distance, t3.y + this._distance)), this._segList.addPt(new X(t3.x + this._distance, t3.y - this._distance)), this._segList.addPt(new X(t3.x - this._distance, t3.y - this._distance)), this._segList.addPt(new X(t3.x - this._distance, t3.y + this._distance)), this._segList.closeRing();
        }, "value") }, { key: "addSegments", value: /* @__PURE__ */ __name(function(t3, e3) {
          this._segList.addPts(t3, e3);
        }, "value") }, { key: "addFirstSegment", value: /* @__PURE__ */ __name(function() {
          this._segList.addPt(this._offset1.p0);
        }, "value") }, { key: "addCornerFillet", value: /* @__PURE__ */ __name(function(t3, e3, n2, i2, r2) {
          var s2 = e3.x - t3.x, a2 = e3.y - t3.y, o2 = Math.atan2(a2, s2), u2 = n2.x - t3.x, l2 = n2.y - t3.y, h2 = Math.atan2(l2, u2);
          i2 === ct.CLOCKWISE ? o2 <= h2 && (o2 += 2 * Math.PI) : o2 >= h2 && (o2 -= 2 * Math.PI), this._segList.addPt(e3), this.addDirectedFillet(t3, o2, h2, i2, r2), this._segList.addPt(n2);
        }, "value") }, { key: "addLastSegment", value: /* @__PURE__ */ __name(function() {
          this._segList.addPt(this._offset1.p1);
        }, "value") }, { key: "initSideSegments", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          this._s1 = t3, this._s2 = e3, this._side = n2, this._seg1.setCoordinates(t3, e3), this.computeOffsetSegment(this._seg1, n2, this._distance, this._offset1);
        }, "value") }, { key: "addLimitedMitreJoin", value: /* @__PURE__ */ __name(function(t3, e3, n2, i2) {
          var r2 = this._seg0.p1, s2 = bn.angle(r2, this._seg0.p0), a2 = bn.angleBetweenOriented(this._seg0.p0, r2, this._seg1.p1) / 2, o2 = bn.normalize(s2 + a2), u2 = bn.normalize(o2 + Math.PI), l2 = i2 * n2, h2 = n2 - l2 * Math.abs(Math.sin(a2)), c2 = r2.x + l2 * Math.cos(u2), f2 = r2.y + l2 * Math.sin(u2), g2 = new X(c2, f2), v2 = new In(r2, g2), y2 = v2.pointAlongOffset(1, h2), d2 = v2.pointAlongOffset(1, -h2);
          this._side === $.LEFT ? (this._segList.addPt(y2), this._segList.addPt(d2)) : (this._segList.addPt(d2), this._segList.addPt(y2));
        }, "value") }, { key: "addDirectedFillet", value: /* @__PURE__ */ __name(function(t3, e3, n2, i2, r2) {
          var s2 = i2 === ct.CLOCKWISE ? -1 : 1, a2 = Math.abs(e3 - n2), o2 = Math.trunc(a2 / this._filletAngleQuantum + 0.5);
          if (o2 < 1) return null;
          for (var u2 = a2 / o2, l2 = new X(), h2 = 0; h2 < o2; h2++) {
            var c2 = e3 + s2 * h2 * u2;
            l2.x = t3.x + r2 * Math.cos(c2), l2.y = t3.y + r2 * Math.sin(c2), this._segList.addPt(l2);
          }
        }, "value") }, { key: "computeOffsetSegment", value: /* @__PURE__ */ __name(function(t3, e3, n2, i2) {
          var r2 = e3 === $.LEFT ? 1 : -1, s2 = t3.p1.x - t3.p0.x, a2 = t3.p1.y - t3.p0.y, o2 = Math.sqrt(s2 * s2 + a2 * a2), u2 = r2 * n2 * s2 / o2, l2 = r2 * n2 * a2 / o2;
          i2.p0.x = t3.p0.x - l2, i2.p0.y = t3.p0.y + u2, i2.p1.x = t3.p1.x - l2, i2.p1.y = t3.p1.y + u2;
        }, "value") }, { key: "addInsideTurn", value: /* @__PURE__ */ __name(function(e3, n2) {
          if (this._li.computeIntersection(this._offset0.p0, this._offset0.p1, this._offset1.p0, this._offset1.p1), this._li.hasIntersection()) this._segList.addPt(this._li.getIntersection(0));
          else if (this._hasNarrowConcaveAngle = true, this._offset0.p1.distance(this._offset1.p0) < this._distance * t2.INSIDE_TURN_VERTEX_SNAP_DISTANCE_FACTOR) this._segList.addPt(this._offset0.p1);
          else {
            if (this._segList.addPt(this._offset0.p1), this._closingSegLengthFactor > 0) {
              var i2 = new X((this._closingSegLengthFactor * this._offset0.p1.x + this._s1.x) / (this._closingSegLengthFactor + 1), (this._closingSegLengthFactor * this._offset0.p1.y + this._s1.y) / (this._closingSegLengthFactor + 1));
              this._segList.addPt(i2);
              var r2 = new X((this._closingSegLengthFactor * this._offset1.p0.x + this._s1.x) / (this._closingSegLengthFactor + 1), (this._closingSegLengthFactor * this._offset1.p0.y + this._s1.y) / (this._closingSegLengthFactor + 1));
              this._segList.addPt(r2);
            } else this._segList.addPt(this._s1);
            this._segList.addPt(this._offset1.p0);
          }
        }, "value") }, { key: "createCircle", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = new X(t3.x + this._distance, t3.y);
          this._segList.addPt(e3), this.addDirectedFillet(t3, 0, 2 * Math.PI, -1, this._distance), this._segList.closeRing();
        }, "value") }, { key: "addBevelJoin", value: /* @__PURE__ */ __name(function(t3, e3) {
          this._segList.addPt(t3.p1), this._segList.addPt(e3.p0);
        }, "value") }, { key: "init", value: /* @__PURE__ */ __name(function(e3) {
          this._distance = e3, this._maxCurveSegmentError = e3 * (1 - Math.cos(this._filletAngleQuantum / 2)), this._segList = new On(), this._segList.setPrecisionModel(this._precisionModel), this._segList.setMinimumVertexDistance(e3 * t2.CURVE_VERTEX_SNAP_DISTANCE_FACTOR);
        }, "value") }, { key: "addCollinear", value: /* @__PURE__ */ __name(function(t3) {
          this._li.computeIntersection(this._s0, this._s1, this._s1, this._s2), this._li.getIntersectionNum() >= 2 && (this._bufParams.getJoinStyle() === _.JOIN_BEVEL || this._bufParams.getJoinStyle() === _.JOIN_MITRE ? (t3 && this._segList.addPt(this._offset0.p1), this._segList.addPt(this._offset1.p0)) : this.addCornerFillet(this._s1, this._offset0.p1, this._offset1.p0, ct.CLOCKWISE, this._distance));
        }, "value") }, { key: "closeRing", value: /* @__PURE__ */ __name(function() {
          this._segList.closeRing();
        }, "value") }, { key: "hasNarrowConcaveAngle", value: /* @__PURE__ */ __name(function() {
          return this._hasNarrowConcaveAngle;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._maxCurveSegmentError = 0, this._filletAngleQuantum = null, this._closingSegLengthFactor = 1, this._segList = null, this._distance = 0, this._precisionModel = null, this._bufParams = null, this._li = null, this._s0 = null, this._s1 = null, this._s2 = null, this._seg0 = new In(), this._seg1 = new In(), this._offset0 = new In(), this._offset1 = new In(), this._side = 0, this._hasNarrowConcaveAngle = false;
          var e3 = arguments[0], n2 = arguments[1], i2 = arguments[2];
          this._precisionModel = e3, this._bufParams = n2, this._li = new we(), this._filletAngleQuantum = Math.PI / 2 / n2.getQuadrantSegments(), n2.getQuadrantSegments() >= 8 && n2.getJoinStyle() === _.JOIN_ROUND && (this._closingSegLengthFactor = t2.MAX_CLOSING_SEG_LEN_FACTOR), this.init(i2);
        }, "value") }]);
      })();
      Mn.OFFSET_SEGMENT_SEPARATION_FACTOR = 1e-3, Mn.INSIDE_TURN_VERTEX_SNAP_DISTANCE_FACTOR = 1e-3, Mn.CURVE_VERTEX_SNAP_DISTANCE_FACTOR = 1e-6, Mn.MAX_CLOSING_SEG_LEN_FACTOR = 80;
      var An = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "getOffsetCurve", value: /* @__PURE__ */ __name(function(t3, e3) {
          if (this._distance = e3, 0 === e3) return null;
          var n2 = e3 < 0, i2 = Math.abs(e3), r2 = this.getSegGen(i2);
          t3.length <= 1 ? this.computePointCurve(t3[0], r2) : this.computeOffsetCurve(t3, n2, r2);
          var s2 = r2.getCoordinates();
          return n2 && jt.reverse(s2), s2;
        }, "value") }, { key: "computeSingleSidedBufferCurve", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          var i2 = this.simplifyTolerance(this._distance);
          if (e3) {
            n2.addSegments(t3, true);
            var r2 = wn.simplify(t3, -i2), s2 = r2.length - 1;
            n2.initSideSegments(r2[s2], r2[s2 - 1], $.LEFT), n2.addFirstSegment();
            for (var a2 = s2 - 2; a2 >= 0; a2--) n2.addNextSegment(r2[a2], true);
          } else {
            n2.addSegments(t3, false);
            var o2 = wn.simplify(t3, i2), u2 = o2.length - 1;
            n2.initSideSegments(o2[0], o2[1], $.LEFT), n2.addFirstSegment();
            for (var l2 = 2; l2 <= u2; l2++) n2.addNextSegment(o2[l2], true);
          }
          n2.addLastSegment(), n2.closeRing();
        }, "value") }, { key: "computeRingBufferCurve", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          var i2 = this.simplifyTolerance(this._distance);
          e3 === $.RIGHT && (i2 = -i2);
          var r2 = wn.simplify(t3, i2), s2 = r2.length - 1;
          n2.initSideSegments(r2[s2 - 1], r2[0], e3);
          for (var a2 = 1; a2 <= s2; a2++) {
            var o2 = 1 !== a2;
            n2.addNextSegment(r2[a2], o2);
          }
          n2.closeRing();
        }, "value") }, { key: "computeLineBufferCurve", value: /* @__PURE__ */ __name(function(t3, e3) {
          var n2 = this.simplifyTolerance(this._distance), i2 = wn.simplify(t3, n2), r2 = i2.length - 1;
          e3.initSideSegments(i2[0], i2[1], $.LEFT);
          for (var s2 = 2; s2 <= r2; s2++) e3.addNextSegment(i2[s2], true);
          e3.addLastSegment(), e3.addLineEndCap(i2[r2 - 1], i2[r2]);
          var a2 = wn.simplify(t3, -n2), o2 = a2.length - 1;
          e3.initSideSegments(a2[o2], a2[o2 - 1], $.LEFT);
          for (var u2 = o2 - 2; u2 >= 0; u2--) e3.addNextSegment(a2[u2], true);
          e3.addLastSegment(), e3.addLineEndCap(a2[1], a2[0]), e3.closeRing();
        }, "value") }, { key: "computePointCurve", value: /* @__PURE__ */ __name(function(t3, e3) {
          switch (this._bufParams.getEndCapStyle()) {
            case _.CAP_ROUND:
              e3.createCircle(t3);
              break;
            case _.CAP_SQUARE:
              e3.createSquare(t3);
          }
        }, "value") }, { key: "getLineCurve", value: /* @__PURE__ */ __name(function(t3, e3) {
          if (this._distance = e3, this.isLineOffsetEmpty(e3)) return null;
          var n2 = Math.abs(e3), i2 = this.getSegGen(n2);
          if (t3.length <= 1) this.computePointCurve(t3[0], i2);
          else if (this._bufParams.isSingleSided()) {
            var r2 = e3 < 0;
            this.computeSingleSidedBufferCurve(t3, r2, i2);
          } else this.computeLineBufferCurve(t3, i2);
          return i2.getCoordinates();
        }, "value") }, { key: "getBufferParameters", value: /* @__PURE__ */ __name(function() {
          return this._bufParams;
        }, "value") }, { key: "simplifyTolerance", value: /* @__PURE__ */ __name(function(t3) {
          return t3 * this._bufParams.getSimplifyFactor();
        }, "value") }, { key: "getRingCurve", value: /* @__PURE__ */ __name(function(e3, n2, i2) {
          if (this._distance = i2, e3.length <= 2) return this.getLineCurve(e3, i2);
          if (0 === i2) return t2.copyCoordinates(e3);
          var r2 = this.getSegGen(i2);
          return this.computeRingBufferCurve(e3, n2, r2), r2.getCoordinates();
        }, "value") }, { key: "computeOffsetCurve", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          var i2 = this.simplifyTolerance(this._distance);
          if (e3) {
            var r2 = wn.simplify(t3, -i2), s2 = r2.length - 1;
            n2.initSideSegments(r2[s2], r2[s2 - 1], $.LEFT), n2.addFirstSegment();
            for (var a2 = s2 - 2; a2 >= 0; a2--) n2.addNextSegment(r2[a2], true);
          } else {
            var o2 = wn.simplify(t3, i2), u2 = o2.length - 1;
            n2.initSideSegments(o2[0], o2[1], $.LEFT), n2.addFirstSegment();
            for (var l2 = 2; l2 <= u2; l2++) n2.addNextSegment(o2[l2], true);
          }
          n2.addLastSegment();
        }, "value") }, { key: "isLineOffsetEmpty", value: /* @__PURE__ */ __name(function(t3) {
          return 0 === t3 || t3 < 0 && !this._bufParams.isSingleSided();
        }, "value") }, { key: "getSegGen", value: /* @__PURE__ */ __name(function(t3) {
          return new Mn(this._precisionModel, this._bufParams, t3);
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._distance = 0, this._precisionModel = null, this._bufParams = null;
          var t3 = arguments[0], e3 = arguments[1];
          this._precisionModel = t3, this._bufParams = e3;
        }, "value") }, { key: "copyCoordinates", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = new Array(t3.length).fill(null), n2 = 0; n2 < e3.length; n2++) e3[n2] = new X(t3[n2]);
          return e3;
        }, "value") }]);
      })(), Pn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "findStabbedSegments", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            for (var t2 = arguments[0], e3 = new yt(), n2 = this._subgraphs.iterator(); n2.hasNext(); ) {
              var i2 = n2.next(), r2 = i2.getEnvelope();
              t2.y < r2.getMinY() || t2.y > r2.getMaxY() || this.findStabbedSegments(t2, i2.getDirectedEdges(), e3);
            }
            return e3;
          }
          if (3 === arguments.length) {
            if (rt(arguments[2], nt) && arguments[0] instanceof X && arguments[1] instanceof Ke) for (var s2 = arguments[0], a2 = arguments[1], o2 = arguments[2], u2 = a2.getEdge().getCoordinates(), l2 = 0; l2 < u2.length - 1; l2++) {
              if (this._seg.p0 = u2[l2], this._seg.p1 = u2[l2 + 1], this._seg.p0.y > this._seg.p1.y && this._seg.reverse(), !(Math.max(this._seg.p0.x, this._seg.p1.x) < s2.x || this._seg.isHorizontal() || s2.y < this._seg.p0.y || s2.y > this._seg.p1.y || ct.index(this._seg.p0, this._seg.p1, s2) === ct.RIGHT)) {
                var h2 = a2.getDepth($.LEFT);
                this._seg.p0.equals(u2[l2]) || (h2 = a2.getDepth($.RIGHT));
                var c2 = new Dn(this._seg, h2);
                o2.add(c2);
              }
            }
            else if (rt(arguments[2], nt) && arguments[0] instanceof X && rt(arguments[1], nt)) for (var f2 = arguments[0], g2 = arguments[2], v2 = arguments[1].iterator(); v2.hasNext(); ) {
              var y2 = v2.next();
              y2.isForward() && this.findStabbedSegments(f2, y2, g2);
            }
          }
        }, "value") }, { key: "getDepth", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = this.findStabbedSegments(t2);
          return 0 === e3.size() ? 0 : an.min(e3)._leftDepth;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._subgraphs = null, this._seg = new In();
          var t2 = arguments[0];
          this._subgraphs = t2;
        }, "value") }]);
      })(), Dn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "compareTo", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = t2;
          if (this._upwardSeg.minX() >= e3._upwardSeg.maxX()) return 1;
          if (this._upwardSeg.maxX() <= e3._upwardSeg.minX()) return -1;
          var n2 = this._upwardSeg.orientationIndex(e3._upwardSeg);
          return 0 !== n2 || 0 !== (n2 = -1 * e3._upwardSeg.orientationIndex(this._upwardSeg)) ? n2 : this._upwardSeg.compareTo(e3._upwardSeg);
        }, "value") }, { key: "compareX", value: /* @__PURE__ */ __name(function(t2, e3) {
          var n2 = t2.p0.compareTo(e3.p0);
          return 0 !== n2 ? n2 : t2.p1.compareTo(e3.p1);
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          return this._upwardSeg.toString();
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [x];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._upwardSeg = null, this._leftDepth = null;
          var t2 = arguments[0], e3 = arguments[1];
          this._upwardSeg = new In(t2), this._leftDepth = e3;
        }, "value") }]);
      })();
      Pn.DepthSegment = Dn;
      var Fn = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, null, [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          p.constructor_.call(this, "Projective point not representable on the Cartesian plane.");
        }, "value") }]);
      })(p), Gn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "getY", value: /* @__PURE__ */ __name(function() {
          var t3 = this.y / this.w;
          if (A.isNaN(t3) || A.isInfinite(t3)) throw new Fn();
          return t3;
        }, "value") }, { key: "getX", value: /* @__PURE__ */ __name(function() {
          var t3 = this.x / this.w;
          if (A.isNaN(t3) || A.isInfinite(t3)) throw new Fn();
          return t3;
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function() {
          var t3 = new X();
          return t3.x = this.getX(), t3.y = this.getY(), t3;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this.x = null, this.y = null, this.w = null, 0 === arguments.length) this.x = 0, this.y = 0, this.w = 1;
          else if (1 === arguments.length) {
            var e3 = arguments[0];
            this.x = e3.x, this.y = e3.y, this.w = 1;
          } else if (2 === arguments.length) {
            if ("number" == typeof arguments[0] && "number" == typeof arguments[1]) {
              var n2 = arguments[0], i2 = arguments[1];
              this.x = n2, this.y = i2, this.w = 1;
            } else if (arguments[0] instanceof t2 && arguments[1] instanceof t2) {
              var r2 = arguments[0], s2 = arguments[1];
              this.x = r2.y * s2.w - s2.y * r2.w, this.y = s2.x * r2.w - r2.x * s2.w, this.w = r2.x * s2.y - s2.x * r2.y;
            } else if (arguments[0] instanceof X && arguments[1] instanceof X) {
              var a2 = arguments[0], o2 = arguments[1];
              this.x = a2.y - o2.y, this.y = o2.x - a2.x, this.w = a2.x * o2.y - o2.x * a2.y;
            }
          } else if (3 === arguments.length) {
            var u2 = arguments[0], l2 = arguments[1], h2 = arguments[2];
            this.x = u2, this.y = l2, this.w = h2;
          } else if (4 === arguments.length) {
            var c2 = arguments[0], f2 = arguments[1], g2 = arguments[2], v2 = arguments[3], y2 = c2.y - f2.y, d2 = f2.x - c2.x, _2 = c2.x * f2.y - f2.x * c2.y, p2 = g2.y - v2.y, m2 = v2.x - g2.x, k2 = g2.x * v2.y - v2.x * g2.y;
            this.x = d2 * k2 - m2 * _2, this.y = p2 * _2 - y2 * k2, this.w = y2 * m2 - p2 * d2;
          }
        }, "value") }]);
      })(), qn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "area", value: /* @__PURE__ */ __name(function() {
          return t2.area(this.p0, this.p1, this.p2);
        }, "value") }, { key: "signedArea", value: /* @__PURE__ */ __name(function() {
          return t2.signedArea(this.p0, this.p1, this.p2);
        }, "value") }, { key: "interpolateZ", value: /* @__PURE__ */ __name(function(e3) {
          if (null === e3) throw new m("Supplied point is null.");
          return t2.interpolateZ(e3, this.p0, this.p1, this.p2);
        }, "value") }, { key: "longestSideLength", value: /* @__PURE__ */ __name(function() {
          return t2.longestSideLength(this.p0, this.p1, this.p2);
        }, "value") }, { key: "isAcute", value: /* @__PURE__ */ __name(function() {
          return t2.isAcute(this.p0, this.p1, this.p2);
        }, "value") }, { key: "circumcentre", value: /* @__PURE__ */ __name(function() {
          return t2.circumcentre(this.p0, this.p1, this.p2);
        }, "value") }, { key: "area3D", value: /* @__PURE__ */ __name(function() {
          return t2.area3D(this.p0, this.p1, this.p2);
        }, "value") }, { key: "centroid", value: /* @__PURE__ */ __name(function() {
          return t2.centroid(this.p0, this.p1, this.p2);
        }, "value") }, { key: "inCentre", value: /* @__PURE__ */ __name(function() {
          return t2.inCentre(this.p0, this.p1, this.p2);
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this.p0 = null, this.p1 = null, this.p2 = null;
          var t3 = arguments[0], e3 = arguments[1], n2 = arguments[2];
          this.p0 = t3, this.p1 = e3, this.p2 = n2;
        }, "value") }, { key: "area", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          return Math.abs(((n2.x - t3.x) * (e3.y - t3.y) - (e3.x - t3.x) * (n2.y - t3.y)) / 2);
        }, "value") }, { key: "signedArea", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          return ((n2.x - t3.x) * (e3.y - t3.y) - (e3.x - t3.x) * (n2.y - t3.y)) / 2;
        }, "value") }, { key: "det", value: /* @__PURE__ */ __name(function(t3, e3, n2, i2) {
          return t3 * i2 - e3 * n2;
        }, "value") }, { key: "interpolateZ", value: /* @__PURE__ */ __name(function(t3, e3, n2, i2) {
          var r2 = e3.x, s2 = e3.y, a2 = n2.x - r2, o2 = i2.x - r2, u2 = n2.y - s2, l2 = i2.y - s2, h2 = a2 * l2 - o2 * u2, c2 = t3.x - r2, f2 = t3.y - s2, g2 = (l2 * c2 - o2 * f2) / h2, v2 = (-u2 * c2 + a2 * f2) / h2;
          return e3.getZ() + g2 * (n2.getZ() - e3.getZ()) + v2 * (i2.getZ() - e3.getZ());
        }, "value") }, { key: "longestSideLength", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          var i2 = t3.distance(e3), r2 = e3.distance(n2), s2 = n2.distance(t3), a2 = i2;
          return r2 > a2 && (a2 = r2), s2 > a2 && (a2 = s2), a2;
        }, "value") }, { key: "circumcentreDD", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          var i2 = ut.valueOf(t3.x).subtract(n2.x), r2 = ut.valueOf(t3.y).subtract(n2.y), s2 = ut.valueOf(e3.x).subtract(n2.x), a2 = ut.valueOf(e3.y).subtract(n2.y), o2 = ut.determinant(i2, r2, s2, a2).multiply(2), u2 = i2.sqr().add(r2.sqr()), l2 = s2.sqr().add(a2.sqr()), h2 = ut.determinant(r2, u2, a2, l2), c2 = ut.determinant(i2, u2, s2, l2), f2 = ut.valueOf(n2.x).subtract(h2.divide(o2)).doubleValue(), g2 = ut.valueOf(n2.y).add(c2.divide(o2)).doubleValue();
          return new X(f2, g2);
        }, "value") }, { key: "isAcute", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          return !!bn.isAcute(t3, e3, n2) && (!!bn.isAcute(e3, n2, t3) && !!bn.isAcute(n2, t3, e3));
        }, "value") }, { key: "circumcentre", value: /* @__PURE__ */ __name(function(e3, n2, i2) {
          var r2 = i2.x, s2 = i2.y, a2 = e3.x - r2, o2 = e3.y - s2, u2 = n2.x - r2, l2 = n2.y - s2, h2 = 2 * t2.det(a2, o2, u2, l2), c2 = t2.det(o2, a2 * a2 + o2 * o2, l2, u2 * u2 + l2 * l2), f2 = t2.det(a2, a2 * a2 + o2 * o2, u2, u2 * u2 + l2 * l2);
          return new X(r2 - c2 / h2, s2 + f2 / h2);
        }, "value") }, { key: "perpendicularBisector", value: /* @__PURE__ */ __name(function(t3, e3) {
          var n2 = e3.x - t3.x, i2 = e3.y - t3.y, r2 = new Gn(t3.x + n2 / 2, t3.y + i2 / 2, 1), s2 = new Gn(t3.x - i2 + n2 / 2, t3.y + n2 + i2 / 2, 1);
          return new Gn(r2, s2);
        }, "value") }, { key: "angleBisector", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          var i2 = e3.distance(t3), r2 = i2 / (i2 + e3.distance(n2)), s2 = n2.x - t3.x, a2 = n2.y - t3.y;
          return new X(t3.x + r2 * s2, t3.y + r2 * a2);
        }, "value") }, { key: "area3D", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          var i2 = e3.x - t3.x, r2 = e3.y - t3.y, s2 = e3.getZ() - t3.getZ(), a2 = n2.x - t3.x, o2 = n2.y - t3.y, u2 = n2.getZ() - t3.getZ(), l2 = r2 * u2 - s2 * o2, h2 = s2 * a2 - i2 * u2, c2 = i2 * o2 - r2 * a2, f2 = l2 * l2 + h2 * h2 + c2 * c2, g2 = Math.sqrt(f2) / 2;
          return g2;
        }, "value") }, { key: "centroid", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          var i2 = (t3.x + e3.x + n2.x) / 3, r2 = (t3.y + e3.y + n2.y) / 3;
          return new X(i2, r2);
        }, "value") }, { key: "inCentre", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          var i2 = e3.distance(n2), r2 = t3.distance(n2), s2 = t3.distance(e3), a2 = i2 + r2 + s2, o2 = (i2 * t3.x + r2 * e3.x + s2 * n2.x) / a2, u2 = (i2 * t3.y + r2 * e3.y + s2 * n2.y) / a2;
          return new X(o2, u2);
        }, "value") }]);
      })(), Yn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "addRingSide", value: /* @__PURE__ */ __name(function(t2, e3, n2, i2, r2) {
          if (0 === e3 && t2.length < Xt.MINIMUM_VALID_SIZE) return null;
          var s2 = i2, a2 = r2;
          t2.length >= Xt.MINIMUM_VALID_SIZE && ct.isCCW(t2) && (s2 = r2, a2 = i2, n2 = $.opposite(n2));
          var o2 = this._curveBuilder.getRingCurve(t2, n2, e3);
          this.addCurve(o2, s2, a2);
        }, "value") }, { key: "addRingBothSides", value: /* @__PURE__ */ __name(function(t2, e3) {
          this.addRingSide(t2, e3, $.LEFT, H.EXTERIOR, H.INTERIOR), this.addRingSide(t2, e3, $.RIGHT, H.INTERIOR, H.EXTERIOR);
        }, "value") }, { key: "addPoint", value: /* @__PURE__ */ __name(function(t2) {
          if (this._distance <= 0) return null;
          var e3 = t2.getCoordinates(), n2 = this._curveBuilder.getLineCurve(e3, this._distance);
          this.addCurve(n2, H.EXTERIOR, H.INTERIOR);
        }, "value") }, { key: "addPolygon", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = this._distance, n2 = $.LEFT;
          this._distance < 0 && (e3 = -this._distance, n2 = $.RIGHT);
          var i2 = t2.getExteriorRing(), r2 = jt.removeRepeatedPoints(i2.getCoordinates());
          if (this._distance < 0 && this.isErodedCompletely(i2, this._distance)) return null;
          if (this._distance <= 0 && r2.length < 3) return null;
          this.addRingSide(r2, e3, n2, H.EXTERIOR, H.INTERIOR);
          for (var s2 = 0; s2 < t2.getNumInteriorRing(); s2++) {
            var a2 = t2.getInteriorRingN(s2), o2 = jt.removeRepeatedPoints(a2.getCoordinates());
            this._distance > 0 && this.isErodedCompletely(a2, -this._distance) || this.addRingSide(o2, e3, $.opposite(n2), H.INTERIOR, H.EXTERIOR);
          }
        }, "value") }, { key: "isTriangleErodedCompletely", value: /* @__PURE__ */ __name(function(t2, e3) {
          var n2 = new qn(t2[0], t2[1], t2[2]), i2 = n2.inCentre();
          return xt.pointToSegment(i2, n2.p0, n2.p1) < Math.abs(e3);
        }, "value") }, { key: "addLineString", value: /* @__PURE__ */ __name(function(t2) {
          if (this._curveBuilder.isLineOffsetEmpty(this._distance)) return null;
          var e3 = jt.removeRepeatedPoints(t2.getCoordinates());
          if (jt.isRing(e3) && !this._curveBuilder.getBufferParameters().isSingleSided()) this.addRingBothSides(e3, this._distance);
          else {
            var n2 = this._curveBuilder.getLineCurve(e3, this._distance);
            this.addCurve(n2, H.EXTERIOR, H.INTERIOR);
          }
        }, "value") }, { key: "addCurve", value: /* @__PURE__ */ __name(function(t2, e3, n2) {
          if (null === t2 || t2.length < 2) return null;
          var i2 = new xn(t2, new Ae(0, H.BOUNDARY, e3, n2));
          this._curveList.add(i2);
        }, "value") }, { key: "getCurves", value: /* @__PURE__ */ __name(function() {
          return this.add(this._inputGeom), this._curveList;
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function(t2) {
          if (t2.isEmpty()) return null;
          if (t2 instanceof Dt) this.addPolygon(t2);
          else if (t2 instanceof wt) this.addLineString(t2);
          else if (t2 instanceof bt) this.addPoint(t2);
          else if (t2 instanceof zt) this.addCollection(t2);
          else if (t2 instanceof se) this.addCollection(t2);
          else if (t2 instanceof te) this.addCollection(t2);
          else {
            if (!(t2 instanceof Yt)) throw new W(t2.getGeometryType());
            this.addCollection(t2);
          }
        }, "value") }, { key: "isErodedCompletely", value: /* @__PURE__ */ __name(function(t2, e3) {
          var n2 = t2.getCoordinates();
          if (n2.length < 4) return e3 < 0;
          if (4 === n2.length) return this.isTriangleErodedCompletely(n2, e3);
          var i2 = t2.getEnvelopeInternal(), r2 = Math.min(i2.getHeight(), i2.getWidth());
          return e3 < 0 && 2 * Math.abs(e3) > r2;
        }, "value") }, { key: "addCollection", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = 0; e3 < t2.getNumGeometries(); e3++) {
            var n2 = t2.getGeometryN(e3);
            this.add(n2);
          }
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._inputGeom = null, this._distance = null, this._curveBuilder = null, this._curveList = new yt();
          var t2 = arguments[0], e3 = arguments[1], n2 = arguments[2];
          this._inputGeom = t2, this._distance = e3, this._curveBuilder = n2;
        }, "value") }]);
      })(), zn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "locate", value: /* @__PURE__ */ __name(function(t2) {
        }, "value") }]);
      })(), Xn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "next", value: /* @__PURE__ */ __name(function() {
          if (this._atStart) return this._atStart = false, t2.isAtomic(this._parent) && this._index++, this._parent;
          if (null !== this._subcollectionIterator) {
            if (this._subcollectionIterator.hasNext()) return this._subcollectionIterator.next();
            this._subcollectionIterator = null;
          }
          if (this._index >= this._max) throw new j();
          var e3 = this._parent.getGeometryN(this._index++);
          return e3 instanceof Yt ? (this._subcollectionIterator = new t2(e3), this._subcollectionIterator.next()) : e3;
        }, "value") }, { key: "remove", value: /* @__PURE__ */ __name(function() {
          throw new W(this.getClass().getName());
        }, "value") }, { key: "hasNext", value: /* @__PURE__ */ __name(function() {
          if (this._atStart) return true;
          if (null !== this._subcollectionIterator) {
            if (this._subcollectionIterator.hasNext()) return true;
            this._subcollectionIterator = null;
          }
          return !(this._index >= this._max);
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [dn];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._parent = null, this._atStart = null, this._max = null, this._index = null, this._subcollectionIterator = null;
          var t3 = arguments[0];
          this._parent = t3, this._atStart = true, this._index = 0, this._max = t3.getNumGeometries();
        }, "value") }, { key: "isAtomic", value: /* @__PURE__ */ __name(function(t3) {
          return !(t3 instanceof Yt);
        }, "value") }]);
      })(), Bn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "locate", value: /* @__PURE__ */ __name(function(e3) {
          return t2.locate(e3, this._geom);
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [zn];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._geom = null;
          var t3 = arguments[0];
          this._geom = t3;
        }, "value") }, { key: "locatePointInPolygon", value: /* @__PURE__ */ __name(function(e3, n2) {
          if (n2.isEmpty()) return H.EXTERIOR;
          var i2 = n2.getExteriorRing(), r2 = t2.locatePointInRing(e3, i2);
          if (r2 !== H.INTERIOR) return r2;
          for (var s2 = 0; s2 < n2.getNumInteriorRing(); s2++) {
            var a2 = n2.getInteriorRingN(s2), o2 = t2.locatePointInRing(e3, a2);
            if (o2 === H.BOUNDARY) return H.BOUNDARY;
            if (o2 === H.INTERIOR) return H.EXTERIOR;
          }
          return H.INTERIOR;
        }, "value") }, { key: "locatePointInRing", value: /* @__PURE__ */ __name(function(t3, e3) {
          return e3.getEnvelopeInternal().intersects(t3) ? be.locateInRing(t3, e3.getCoordinates()) : H.EXTERIOR;
        }, "value") }, { key: "containsPointInPolygon", value: /* @__PURE__ */ __name(function(e3, n2) {
          return H.EXTERIOR !== t2.locatePointInPolygon(e3, n2);
        }, "value") }, { key: "locateInGeometry", value: /* @__PURE__ */ __name(function(e3, n2) {
          if (n2 instanceof Dt) return t2.locatePointInPolygon(e3, n2);
          if (n2 instanceof Yt) for (var i2 = new Xn(n2); i2.hasNext(); ) {
            var r2 = i2.next();
            if (r2 !== n2) {
              var s2 = t2.locateInGeometry(e3, r2);
              if (s2 !== H.EXTERIOR) return s2;
            }
          }
          return H.EXTERIOR;
        }, "value") }, { key: "isContained", value: /* @__PURE__ */ __name(function(e3, n2) {
          return H.EXTERIOR !== t2.locate(e3, n2);
        }, "value") }, { key: "locate", value: /* @__PURE__ */ __name(function(e3, n2) {
          return n2.isEmpty() ? H.EXTERIOR : n2.getEnvelopeInternal().intersects(e3) ? t2.locateInGeometry(e3, n2) : H.EXTERIOR;
        }, "value") }]);
      })(), Un = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "getNextCW", value: /* @__PURE__ */ __name(function(t2) {
          this.getEdges();
          var e3 = this._edgeList.indexOf(t2), n2 = e3 - 1;
          return 0 === e3 && (n2 = this._edgeList.size() - 1), this._edgeList.get(n2);
        }, "value") }, { key: "propagateSideLabels", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = H.NONE, n2 = this.iterator(); n2.hasNext(); ) {
            var i2 = n2.next().getLabel();
            i2.isArea(t2) && i2.getLocation(t2, $.LEFT) !== H.NONE && (e3 = i2.getLocation(t2, $.LEFT));
          }
          if (e3 === H.NONE) return null;
          for (var r2 = e3, s2 = this.iterator(); s2.hasNext(); ) {
            var a2 = s2.next(), o2 = a2.getLabel();
            if (o2.getLocation(t2, $.ON) === H.NONE && o2.setLocation(t2, $.ON, r2), o2.isArea(t2)) {
              var u2 = o2.getLocation(t2, $.LEFT), l2 = o2.getLocation(t2, $.RIGHT);
              if (l2 !== H.NONE) {
                if (l2 !== r2) throw new gt3("side location conflict", a2.getCoordinate());
                u2 === H.NONE && G.shouldNeverReachHere("found single null side (at " + a2.getCoordinate() + ")"), r2 = u2;
              } else G.isTrue(o2.getLocation(t2, $.LEFT) === H.NONE, "found single null side"), o2.setLocation(t2, $.RIGHT, r2), o2.setLocation(t2, $.LEFT, r2);
            }
          }
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function() {
          var t2 = this.iterator();
          return t2.hasNext() ? t2.next().getCoordinate() : null;
        }, "value") }, { key: "print", value: /* @__PURE__ */ __name(function(t2) {
          mt.out.println("EdgeEndStar:   " + this.getCoordinate());
          for (var e3 = this.iterator(); e3.hasNext(); ) {
            e3.next().print(t2);
          }
        }, "value") }, { key: "isAreaLabelsConsistent", value: /* @__PURE__ */ __name(function(t2) {
          return this.computeEdgeEndLabels(t2.getBoundaryNodeRule()), this.checkAreaLabelsConsistent(0);
        }, "value") }, { key: "checkAreaLabelsConsistent", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = this.getEdges();
          if (e3.size() <= 0) return true;
          var n2 = e3.size() - 1, i2 = e3.get(n2).getLabel().getLocation(t2, $.LEFT);
          G.isTrue(i2 !== H.NONE, "Found unlabelled area edge");
          for (var r2 = i2, s2 = this.iterator(); s2.hasNext(); ) {
            var a2 = s2.next().getLabel();
            G.isTrue(a2.isArea(t2), "Found non-area edge");
            var o2 = a2.getLocation(t2, $.LEFT), u2 = a2.getLocation(t2, $.RIGHT);
            if (o2 === u2) return false;
            if (u2 !== r2) return false;
            r2 = o2;
          }
          return true;
        }, "value") }, { key: "findIndex", value: /* @__PURE__ */ __name(function(t2) {
          this.iterator();
          for (var e3 = 0; e3 < this._edgeList.size(); e3++) {
            if (this._edgeList.get(e3) === t2) return e3;
          }
          return -1;
        }, "value") }, { key: "iterator", value: /* @__PURE__ */ __name(function() {
          return this.getEdges().iterator();
        }, "value") }, { key: "getEdges", value: /* @__PURE__ */ __name(function() {
          return null === this._edgeList && (this._edgeList = new yt(this._edgeMap.values())), this._edgeList;
        }, "value") }, { key: "getLocation", value: /* @__PURE__ */ __name(function(t2, e3, n2) {
          return this._ptInAreaLocation[t2] === H.NONE && (this._ptInAreaLocation[t2] = Bn.locate(e3, n2[t2].getGeometry())), this._ptInAreaLocation[t2];
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          var t2 = new st();
          t2.append("EdgeEndStar:   " + this.getCoordinate()), t2.append("\n");
          for (var e3 = this.iterator(); e3.hasNext(); ) {
            var n2 = e3.next();
            t2.append(n2), t2.append("\n");
          }
          return t2.toString();
        }, "value") }, { key: "computeEdgeEndLabels", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = this.iterator(); e3.hasNext(); ) {
            e3.next().computeLabel(t2);
          }
        }, "value") }, { key: "computeLabelling", value: /* @__PURE__ */ __name(function(t2) {
          this.computeEdgeEndLabels(t2[0].getBoundaryNodeRule()), this.propagateSideLabels(0), this.propagateSideLabels(1);
          for (var e3 = [false, false], n2 = this.iterator(); n2.hasNext(); ) for (var i2 = n2.next().getLabel(), r2 = 0; r2 < 2; r2++) i2.isLine(r2) && i2.getLocation(r2) === H.BOUNDARY && (e3[r2] = true);
          for (var s2 = this.iterator(); s2.hasNext(); ) for (var a2 = s2.next(), o2 = a2.getLabel(), u2 = 0; u2 < 2; u2++) if (o2.isAnyNull(u2)) {
            var l2 = H.NONE;
            if (e3[u2]) l2 = H.EXTERIOR;
            else {
              var h2 = a2.getCoordinate();
              l2 = this.getLocation(u2, h2, t2);
            }
            o2.setAllLocationsIfNull(u2, l2);
          }
        }, "value") }, { key: "getDegree", value: /* @__PURE__ */ __name(function() {
          return this._edgeMap.size();
        }, "value") }, { key: "insertEdgeEnd", value: /* @__PURE__ */ __name(function(t2, e3) {
          this._edgeMap.put(t2, e3), this._edgeList = null;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._edgeMap = new He(), this._edgeList = null, this._ptInAreaLocation = [H.NONE, H.NONE];
        }, "value") }]);
      })(), Vn = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "linkResultDirectedEdges", value: /* @__PURE__ */ __name(function() {
          this.getResultAreaEdges();
          for (var t3 = null, e3 = null, n2 = this._SCANNING_FOR_INCOMING, i3 = 0; i3 < this._resultAreaEdgeList.size(); i3++) {
            var r2 = this._resultAreaEdgeList.get(i3), s2 = r2.getSym();
            if (r2.getLabel().isArea()) switch (null === t3 && r2.isInResult() && (t3 = r2), n2) {
              case this._SCANNING_FOR_INCOMING:
                if (!s2.isInResult()) continue;
                e3 = s2, n2 = this._LINKING_TO_OUTGOING;
                break;
              case this._LINKING_TO_OUTGOING:
                if (!r2.isInResult()) continue;
                e3.setNext(r2), n2 = this._SCANNING_FOR_INCOMING;
            }
          }
          if (n2 === this._LINKING_TO_OUTGOING) {
            if (null === t3) throw new gt3("no outgoing dirEdge found", this.getCoordinate());
            G.isTrue(t3.isInResult(), "unable to link last incoming dirEdge"), e3.setNext(t3);
          }
        }, "value") }, { key: "insert", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = t3;
          this.insertEdgeEnd(e3, e3);
        }, "value") }, { key: "getRightmostEdge", value: /* @__PURE__ */ __name(function() {
          var t3 = this.getEdges(), e3 = t3.size();
          if (e3 < 1) return null;
          var n2 = t3.get(0);
          if (1 === e3) return n2;
          var i3 = t3.get(e3 - 1), r2 = n2.getQuadrant(), s2 = i3.getQuadrant();
          return je.isNorthern(r2) && je.isNorthern(s2) ? n2 : je.isNorthern(r2) || je.isNorthern(s2) ? 0 !== n2.getDy() ? n2 : 0 !== i3.getDy() ? i3 : (G.shouldNeverReachHere("found two horizontal edges incident on node"), null) : i3;
        }, "value") }, { key: "print", value: /* @__PURE__ */ __name(function(t3) {
          mt.out.println("DirectedEdgeStar: " + this.getCoordinate());
          for (var e3 = this.iterator(); e3.hasNext(); ) {
            var n2 = e3.next();
            t3.print("out "), n2.print(t3), t3.println(), t3.print("in "), n2.getSym().print(t3), t3.println();
          }
        }, "value") }, { key: "getResultAreaEdges", value: /* @__PURE__ */ __name(function() {
          if (null !== this._resultAreaEdgeList) return this._resultAreaEdgeList;
          this._resultAreaEdgeList = new yt();
          for (var t3 = this.iterator(); t3.hasNext(); ) {
            var e3 = t3.next();
            (e3.isInResult() || e3.getSym().isInResult()) && this._resultAreaEdgeList.add(e3);
          }
          return this._resultAreaEdgeList;
        }, "value") }, { key: "updateLabelling", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = this.iterator(); e3.hasNext(); ) {
            var n2 = e3.next().getLabel();
            n2.setAllLocationsIfNull(0, t3.getLocation(0)), n2.setAllLocationsIfNull(1, t3.getLocation(1));
          }
        }, "value") }, { key: "linkAllDirectedEdges", value: /* @__PURE__ */ __name(function() {
          this.getEdges();
          for (var t3 = null, e3 = null, n2 = this._edgeList.size() - 1; n2 >= 0; n2--) {
            var i3 = this._edgeList.get(n2), r2 = i3.getSym();
            null === e3 && (e3 = r2), null !== t3 && r2.setNext(t3), t3 = i3;
          }
          e3.setNext(t3);
        }, "value") }, { key: "computeDepths", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t3 = arguments[0], e3 = this.findIndex(t3), n2 = t3.getDepth($.LEFT), i3 = t3.getDepth($.RIGHT), r2 = this.computeDepths(e3 + 1, this._edgeList.size(), n2);
            if (this.computeDepths(0, e3, r2) !== i3) throw new gt3("depth mismatch at " + t3.getCoordinate());
          } else if (3 === arguments.length) {
            for (var s2 = arguments[1], a2 = arguments[2], o2 = arguments[0]; o2 < s2; o2++) {
              var u2 = this._edgeList.get(o2);
              u2.setEdgeDepths($.RIGHT, a2), a2 = u2.getDepth($.LEFT);
            }
            return a2;
          }
        }, "value") }, { key: "mergeSymLabels", value: /* @__PURE__ */ __name(function() {
          for (var t3 = this.iterator(); t3.hasNext(); ) {
            var e3 = t3.next();
            e3.getLabel().merge(e3.getSym().getLabel());
          }
        }, "value") }, { key: "linkMinimalDirectedEdges", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = null, n2 = null, i3 = this._SCANNING_FOR_INCOMING, r2 = this._resultAreaEdgeList.size() - 1; r2 >= 0; r2--) {
            var s2 = this._resultAreaEdgeList.get(r2), a2 = s2.getSym();
            switch (null === e3 && s2.getEdgeRing() === t3 && (e3 = s2), i3) {
              case this._SCANNING_FOR_INCOMING:
                if (a2.getEdgeRing() !== t3) continue;
                n2 = a2, i3 = this._LINKING_TO_OUTGOING;
                break;
              case this._LINKING_TO_OUTGOING:
                if (s2.getEdgeRing() !== t3) continue;
                n2.setNextMin(s2), i3 = this._SCANNING_FOR_INCOMING;
            }
          }
          i3 === this._LINKING_TO_OUTGOING && (G.isTrue(null !== e3, "found null for first outgoing dirEdge"), G.isTrue(e3.getEdgeRing() === t3, "unable to link last incoming dirEdge"), n2.setNextMin(e3));
        }, "value") }, { key: "getOutgoingDegree", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) {
            for (var t3 = 0, e3 = this.iterator(); e3.hasNext(); ) {
              e3.next().isInResult() && t3++;
            }
            return t3;
          }
          if (1 === arguments.length) {
            for (var n2 = arguments[0], i3 = 0, r2 = this.iterator(); r2.hasNext(); ) {
              r2.next().getEdgeRing() === n2 && i3++;
            }
            return i3;
          }
        }, "value") }, { key: "getLabel", value: /* @__PURE__ */ __name(function() {
          return this._label;
        }, "value") }, { key: "findCoveredLineEdges", value: /* @__PURE__ */ __name(function() {
          for (var t3 = H.NONE, e3 = this.iterator(); e3.hasNext(); ) {
            var n2 = e3.next(), i3 = n2.getSym();
            if (!n2.isLineEdge()) {
              if (n2.isInResult()) {
                t3 = H.INTERIOR;
                break;
              }
              if (i3.isInResult()) {
                t3 = H.EXTERIOR;
                break;
              }
            }
          }
          if (t3 === H.NONE) return null;
          for (var r2 = t3, s2 = this.iterator(); s2.hasNext(); ) {
            var a2 = s2.next(), o2 = a2.getSym();
            a2.isLineEdge() ? a2.getEdge().setCovered(r2 === H.INTERIOR) : (a2.isInResult() && (r2 = H.EXTERIOR), o2.isInResult() && (r2 = H.INTERIOR));
          }
        }, "value") }, { key: "computeLabelling", value: /* @__PURE__ */ __name(function(t3) {
          f(i2, "computeLabelling", this, 1).call(this, t3), this._label = new Ae(H.NONE);
          for (var e3 = this.iterator(); e3.hasNext(); ) for (var n2 = e3.next().getEdge().getLabel(), r2 = 0; r2 < 2; r2++) {
            var s2 = n2.getLocation(r2);
            s2 !== H.INTERIOR && s2 !== H.BOUNDARY || this._label.setLocation(r2, H.INTERIOR);
          }
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._resultAreaEdgeList = null, this._label = null, this._SCANNING_FOR_INCOMING = 1, this._LINKING_TO_OUTGOING = 2;
        }, "value") }]);
      })(Un), Hn = (function(t2) {
        function i2() {
          return n(this, i2), e(this, i2);
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "createNode", value: /* @__PURE__ */ __name(function(t3) {
          return new qe(t3, new Vn());
        }, "value") }]);
      })(Je), Zn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "compareTo", value: /* @__PURE__ */ __name(function(e3) {
          var n2 = e3;
          return t2.compareOriented(this._pts, this._orientation, n2._pts, n2._orientation);
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [x];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._pts = null, this._orientation = null;
          var e3 = arguments[0];
          this._pts = e3, this._orientation = t2.orientation(e3);
        }, "value") }, { key: "orientation", value: /* @__PURE__ */ __name(function(t3) {
          return 1 === jt.increasingDirection(t3);
        }, "value") }, { key: "compareOriented", value: /* @__PURE__ */ __name(function(t3, e3, n2, i2) {
          for (var r2 = e3 ? 1 : -1, s2 = i2 ? 1 : -1, a2 = e3 ? t3.length : -1, o2 = i2 ? n2.length : -1, u2 = e3 ? 0 : t3.length - 1, l2 = i2 ? 0 : n2.length - 1; ; ) {
            var h2 = t3[u2].compareTo(n2[l2]);
            if (0 !== h2) return h2;
            var c2 = (u2 += r2) === a2, f2 = (l2 += s2) === o2;
            if (c2 && !f2) return -1;
            if (!c2 && f2) return 1;
            if (c2 && f2) return 0;
          }
        }, "value") }]);
      })(), jn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "print", value: /* @__PURE__ */ __name(function(t2) {
          t2.print("MULTILINESTRING ( ");
          for (var e3 = 0; e3 < this._edges.size(); e3++) {
            var n2 = this._edges.get(e3);
            e3 > 0 && t2.print(","), t2.print("(");
            for (var i2 = n2.getCoordinates(), r2 = 0; r2 < i2.length; r2++) r2 > 0 && t2.print(","), t2.print(i2[r2].x + " " + i2[r2].y);
            t2.println(")");
          }
          t2.print(")  ");
        }, "value") }, { key: "addAll", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = t2.iterator(); e3.hasNext(); ) this.add(e3.next());
        }, "value") }, { key: "findEdgeIndex", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = 0; e3 < this._edges.size(); e3++) if (this._edges.get(e3).equals(t2)) return e3;
          return -1;
        }, "value") }, { key: "iterator", value: /* @__PURE__ */ __name(function() {
          return this._edges.iterator();
        }, "value") }, { key: "getEdges", value: /* @__PURE__ */ __name(function() {
          return this._edges;
        }, "value") }, { key: "get", value: /* @__PURE__ */ __name(function(t2) {
          return this._edges.get(t2);
        }, "value") }, { key: "findEqualEdge", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = new Zn(t2.getCoordinates());
          return this._ocaMap.get(e3);
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function(t2) {
          this._edges.add(t2);
          var e3 = new Zn(t2.getCoordinates());
          this._ocaMap.put(e3, t2);
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._edges = new yt(), this._ocaMap = new He();
        }, "value") }]);
      })(), Wn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2);
        }), "t"), [{ key: "processIntersections", value: /* @__PURE__ */ __name(function(t2, e3, n2, i2) {
        }, "value") }, { key: "isDone", value: /* @__PURE__ */ __name(function() {
        }, "value") }]);
      })(), Kn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "isTrivialIntersection", value: /* @__PURE__ */ __name(function(e3, n2, i2, r2) {
          if (e3 === i2 && 1 === this._li.getIntersectionNum()) {
            if (t2.isAdjacentSegments(n2, r2)) return true;
            if (e3.isClosed()) {
              var s2 = e3.size() - 1;
              if (0 === n2 && r2 === s2 || 0 === r2 && n2 === s2) return true;
            }
          }
          return false;
        }, "value") }, { key: "getProperIntersectionPoint", value: /* @__PURE__ */ __name(function() {
          return this._properIntersectionPoint;
        }, "value") }, { key: "hasProperInteriorIntersection", value: /* @__PURE__ */ __name(function() {
          return this._hasProperInterior;
        }, "value") }, { key: "getLineIntersector", value: /* @__PURE__ */ __name(function() {
          return this._li;
        }, "value") }, { key: "hasProperIntersection", value: /* @__PURE__ */ __name(function() {
          return this._hasProper;
        }, "value") }, { key: "processIntersections", value: /* @__PURE__ */ __name(function(t3, e3, n2, i2) {
          if (t3 === n2 && e3 === i2) return null;
          this.numTests++;
          var r2 = t3.getCoordinates()[e3], s2 = t3.getCoordinates()[e3 + 1], a2 = n2.getCoordinates()[i2], o2 = n2.getCoordinates()[i2 + 1];
          this._li.computeIntersection(r2, s2, a2, o2), this._li.hasIntersection() && (this.numIntersections++, this._li.isInteriorIntersection() && (this.numInteriorIntersections++, this._hasInterior = true), this.isTrivialIntersection(t3, e3, n2, i2) || (this._hasIntersection = true, t3.addIntersections(this._li, e3, 0), n2.addIntersections(this._li, i2, 1), this._li.isProper() && (this.numProperIntersections++, this._hasProper = true, this._hasProperInterior = true)));
        }, "value") }, { key: "hasIntersection", value: /* @__PURE__ */ __name(function() {
          return this._hasIntersection;
        }, "value") }, { key: "isDone", value: /* @__PURE__ */ __name(function() {
          return false;
        }, "value") }, { key: "hasInteriorIntersection", value: /* @__PURE__ */ __name(function() {
          return this._hasInterior;
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [Wn];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._hasIntersection = false, this._hasProper = false, this._hasProperInterior = false, this._hasInterior = false, this._properIntersectionPoint = null, this._li = null, this._isSelfIntersection = null, this.numIntersections = 0, this.numInteriorIntersections = 0, this.numProperIntersections = 0, this.numTests = 0;
          var t3 = arguments[0];
          this._li = t3;
        }, "value") }, { key: "isAdjacentSegments", value: /* @__PURE__ */ __name(function(t3, e3) {
          return 1 === Math.abs(t3 - e3);
        }, "value") }]);
      })(), Jn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "getSegmentIndex", value: /* @__PURE__ */ __name(function() {
          return this.segmentIndex;
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function() {
          return this.coord;
        }, "value") }, { key: "print", value: /* @__PURE__ */ __name(function(t2) {
          t2.print(this.coord), t2.print(" seg # = " + this.segmentIndex), t2.println(" dist = " + this.dist);
        }, "value") }, { key: "compareTo", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = t2;
          return this.compare(e3.segmentIndex, e3.dist);
        }, "value") }, { key: "isEndPoint", value: /* @__PURE__ */ __name(function(t2) {
          return 0 === this.segmentIndex && 0 === this.dist || this.segmentIndex === t2;
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          return this.coord + " seg # = " + this.segmentIndex + " dist = " + this.dist;
        }, "value") }, { key: "getDistance", value: /* @__PURE__ */ __name(function() {
          return this.dist;
        }, "value") }, { key: "compare", value: /* @__PURE__ */ __name(function(t2, e3) {
          return this.segmentIndex < t2 ? -1 : this.segmentIndex > t2 ? 1 : this.dist < e3 ? -1 : this.dist > e3 ? 1 : 0;
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [x];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this.coord = null, this.segmentIndex = null, this.dist = null;
          var t2 = arguments[0], e3 = arguments[1], n2 = arguments[2];
          this.coord = new X(t2), this.segmentIndex = e3, this.dist = n2;
        }, "value") }]);
      })(), Qn = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "print", value: /* @__PURE__ */ __name(function(t2) {
          t2.println("Intersections:");
          for (var e3 = this.iterator(); e3.hasNext(); ) {
            e3.next().print(t2);
          }
        }, "value") }, { key: "iterator", value: /* @__PURE__ */ __name(function() {
          return this._nodeMap.values().iterator();
        }, "value") }, { key: "addSplitEdges", value: /* @__PURE__ */ __name(function(t2) {
          this.addEndpoints();
          for (var e3 = this.iterator(), n2 = e3.next(); e3.hasNext(); ) {
            var i2 = e3.next(), r2 = this.createSplitEdge(n2, i2);
            t2.add(r2), n2 = i2;
          }
        }, "value") }, { key: "addEndpoints", value: /* @__PURE__ */ __name(function() {
          var t2 = this.edge.pts.length - 1;
          this.add(this.edge.pts[0], 0, 0), this.add(this.edge.pts[t2], t2, 0);
        }, "value") }, { key: "createSplitEdge", value: /* @__PURE__ */ __name(function(t2, e3) {
          var n2 = e3.segmentIndex - t2.segmentIndex + 2, i2 = this.edge.pts[e3.segmentIndex], r2 = e3.dist > 0 || !e3.coord.equals2D(i2);
          r2 || n2--;
          var s2 = new Array(n2).fill(null), a2 = 0;
          s2[a2++] = new X(t2.coord);
          for (var o2 = t2.segmentIndex + 1; o2 <= e3.segmentIndex; o2++) s2[a2++] = this.edge.pts[o2];
          return r2 && (s2[a2] = e3.coord), new ri(s2, new Ae(this.edge._label));
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function(t2, e3, n2) {
          var i2 = new Jn(t2, e3, n2), r2 = this._nodeMap.get(i2);
          return null !== r2 ? r2 : (this._nodeMap.put(i2, i2), i2);
        }, "value") }, { key: "isIntersection", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = this.iterator(); e3.hasNext(); ) {
            if (e3.next().coord.equals(t2)) return true;
          }
          return false;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._nodeMap = new He(), this.edge = null;
          var t2 = arguments[0];
          this.edge = t2;
        }, "value") }]);
      })(), $n = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "isIntersects", value: /* @__PURE__ */ __name(function() {
          return !this.isDisjoint();
        }, "value") }, { key: "isCovers", value: /* @__PURE__ */ __name(function() {
          return (t2.isTrue(this._matrix[H.INTERIOR][H.INTERIOR]) || t2.isTrue(this._matrix[H.INTERIOR][H.BOUNDARY]) || t2.isTrue(this._matrix[H.BOUNDARY][H.INTERIOR]) || t2.isTrue(this._matrix[H.BOUNDARY][H.BOUNDARY])) && this._matrix[H.EXTERIOR][H.INTERIOR] === Lt.FALSE && this._matrix[H.EXTERIOR][H.BOUNDARY] === Lt.FALSE;
        }, "value") }, { key: "isCoveredBy", value: /* @__PURE__ */ __name(function() {
          return (t2.isTrue(this._matrix[H.INTERIOR][H.INTERIOR]) || t2.isTrue(this._matrix[H.INTERIOR][H.BOUNDARY]) || t2.isTrue(this._matrix[H.BOUNDARY][H.INTERIOR]) || t2.isTrue(this._matrix[H.BOUNDARY][H.BOUNDARY])) && this._matrix[H.INTERIOR][H.EXTERIOR] === Lt.FALSE && this._matrix[H.BOUNDARY][H.EXTERIOR] === Lt.FALSE;
        }, "value") }, { key: "set", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) for (var t3 = arguments[0], e3 = 0; e3 < t3.length; e3++) {
            var n2 = Math.trunc(e3 / 3), i2 = e3 % 3;
            this._matrix[n2][i2] = Lt.toDimensionValue(t3.charAt(e3));
          }
          else if (3 === arguments.length) {
            var r2 = arguments[0], s2 = arguments[1], a2 = arguments[2];
            this._matrix[r2][s2] = a2;
          }
        }, "value") }, { key: "isContains", value: /* @__PURE__ */ __name(function() {
          return t2.isTrue(this._matrix[H.INTERIOR][H.INTERIOR]) && this._matrix[H.EXTERIOR][H.INTERIOR] === Lt.FALSE && this._matrix[H.EXTERIOR][H.BOUNDARY] === Lt.FALSE;
        }, "value") }, { key: "setAtLeast", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) for (var t3 = arguments[0], e3 = 0; e3 < t3.length; e3++) {
            var n2 = Math.trunc(e3 / 3), i2 = e3 % 3;
            this.setAtLeast(n2, i2, Lt.toDimensionValue(t3.charAt(e3)));
          }
          else if (3 === arguments.length) {
            var r2 = arguments[0], s2 = arguments[1], a2 = arguments[2];
            this._matrix[r2][s2] < a2 && (this._matrix[r2][s2] = a2);
          }
        }, "value") }, { key: "setAtLeastIfValid", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          t3 >= 0 && e3 >= 0 && this.setAtLeast(t3, e3, n2);
        }, "value") }, { key: "isWithin", value: /* @__PURE__ */ __name(function() {
          return t2.isTrue(this._matrix[H.INTERIOR][H.INTERIOR]) && this._matrix[H.INTERIOR][H.EXTERIOR] === Lt.FALSE && this._matrix[H.BOUNDARY][H.EXTERIOR] === Lt.FALSE;
        }, "value") }, { key: "isTouches", value: /* @__PURE__ */ __name(function(e3, n2) {
          return e3 > n2 ? this.isTouches(n2, e3) : (e3 === Lt.A && n2 === Lt.A || e3 === Lt.L && n2 === Lt.L || e3 === Lt.L && n2 === Lt.A || e3 === Lt.P && n2 === Lt.A || e3 === Lt.P && n2 === Lt.L) && (this._matrix[H.INTERIOR][H.INTERIOR] === Lt.FALSE && (t2.isTrue(this._matrix[H.INTERIOR][H.BOUNDARY]) || t2.isTrue(this._matrix[H.BOUNDARY][H.INTERIOR]) || t2.isTrue(this._matrix[H.BOUNDARY][H.BOUNDARY])));
        }, "value") }, { key: "isOverlaps", value: /* @__PURE__ */ __name(function(e3, n2) {
          return e3 === Lt.P && n2 === Lt.P || e3 === Lt.A && n2 === Lt.A ? t2.isTrue(this._matrix[H.INTERIOR][H.INTERIOR]) && t2.isTrue(this._matrix[H.INTERIOR][H.EXTERIOR]) && t2.isTrue(this._matrix[H.EXTERIOR][H.INTERIOR]) : e3 === Lt.L && n2 === Lt.L && (1 === this._matrix[H.INTERIOR][H.INTERIOR] && t2.isTrue(this._matrix[H.INTERIOR][H.EXTERIOR]) && t2.isTrue(this._matrix[H.EXTERIOR][H.INTERIOR]));
        }, "value") }, { key: "isEquals", value: /* @__PURE__ */ __name(function(e3, n2) {
          return e3 === n2 && (t2.isTrue(this._matrix[H.INTERIOR][H.INTERIOR]) && this._matrix[H.INTERIOR][H.EXTERIOR] === Lt.FALSE && this._matrix[H.BOUNDARY][H.EXTERIOR] === Lt.FALSE && this._matrix[H.EXTERIOR][H.INTERIOR] === Lt.FALSE && this._matrix[H.EXTERIOR][H.BOUNDARY] === Lt.FALSE);
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          for (var t3 = new Jt("123456789"), e3 = 0; e3 < 3; e3++) for (var n2 = 0; n2 < 3; n2++) t3.setCharAt(3 * e3 + n2, Lt.toDimensionSymbol(this._matrix[e3][n2]));
          return t3.toString();
        }, "value") }, { key: "setAll", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = 0; e3 < 3; e3++) for (var n2 = 0; n2 < 3; n2++) this._matrix[e3][n2] = t3;
        }, "value") }, { key: "get", value: /* @__PURE__ */ __name(function(t3, e3) {
          return this._matrix[t3][e3];
        }, "value") }, { key: "transpose", value: /* @__PURE__ */ __name(function() {
          var t3 = this._matrix[1][0];
          return this._matrix[1][0] = this._matrix[0][1], this._matrix[0][1] = t3, t3 = this._matrix[2][0], this._matrix[2][0] = this._matrix[0][2], this._matrix[0][2] = t3, t3 = this._matrix[2][1], this._matrix[2][1] = this._matrix[1][2], this._matrix[1][2] = t3, this;
        }, "value") }, { key: "matches", value: /* @__PURE__ */ __name(function(e3) {
          if (9 !== e3.length) throw new m("Should be length 9: " + e3);
          for (var n2 = 0; n2 < 3; n2++) for (var i2 = 0; i2 < 3; i2++) if (!t2.matches(this._matrix[n2][i2], e3.charAt(3 * n2 + i2))) return false;
          return true;
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = 0; e3 < 3; e3++) for (var n2 = 0; n2 < 3; n2++) this.setAtLeast(e3, n2, t3.get(e3, n2));
        }, "value") }, { key: "isDisjoint", value: /* @__PURE__ */ __name(function() {
          return this._matrix[H.INTERIOR][H.INTERIOR] === Lt.FALSE && this._matrix[H.INTERIOR][H.BOUNDARY] === Lt.FALSE && this._matrix[H.BOUNDARY][H.INTERIOR] === Lt.FALSE && this._matrix[H.BOUNDARY][H.BOUNDARY] === Lt.FALSE;
        }, "value") }, { key: "isCrosses", value: /* @__PURE__ */ __name(function(e3, n2) {
          return e3 === Lt.P && n2 === Lt.L || e3 === Lt.P && n2 === Lt.A || e3 === Lt.L && n2 === Lt.A ? t2.isTrue(this._matrix[H.INTERIOR][H.INTERIOR]) && t2.isTrue(this._matrix[H.INTERIOR][H.EXTERIOR]) : e3 === Lt.L && n2 === Lt.P || e3 === Lt.A && n2 === Lt.P || e3 === Lt.A && n2 === Lt.L ? t2.isTrue(this._matrix[H.INTERIOR][H.INTERIOR]) && t2.isTrue(this._matrix[H.EXTERIOR][H.INTERIOR]) : e3 === Lt.L && n2 === Lt.L && 0 === this._matrix[H.INTERIOR][H.INTERIOR];
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [I];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._matrix = null, 0 === arguments.length) this._matrix = Array(3).fill().map((function() {
            return Array(3);
          })), this.setAll(Lt.FALSE);
          else if (1 === arguments.length) {
            if ("string" == typeof arguments[0]) {
              var e3 = arguments[0];
              t2.constructor_.call(this), this.set(e3);
            } else if (arguments[0] instanceof t2) {
              var n2 = arguments[0];
              t2.constructor_.call(this), this._matrix[H.INTERIOR][H.INTERIOR] = n2._matrix[H.INTERIOR][H.INTERIOR], this._matrix[H.INTERIOR][H.BOUNDARY] = n2._matrix[H.INTERIOR][H.BOUNDARY], this._matrix[H.INTERIOR][H.EXTERIOR] = n2._matrix[H.INTERIOR][H.EXTERIOR], this._matrix[H.BOUNDARY][H.INTERIOR] = n2._matrix[H.BOUNDARY][H.INTERIOR], this._matrix[H.BOUNDARY][H.BOUNDARY] = n2._matrix[H.BOUNDARY][H.BOUNDARY], this._matrix[H.BOUNDARY][H.EXTERIOR] = n2._matrix[H.BOUNDARY][H.EXTERIOR], this._matrix[H.EXTERIOR][H.INTERIOR] = n2._matrix[H.EXTERIOR][H.INTERIOR], this._matrix[H.EXTERIOR][H.BOUNDARY] = n2._matrix[H.EXTERIOR][H.BOUNDARY], this._matrix[H.EXTERIOR][H.EXTERIOR] = n2._matrix[H.EXTERIOR][H.EXTERIOR];
            }
          }
        }, "value") }, { key: "matches", value: /* @__PURE__ */ __name(function() {
          if (Number.isInteger(arguments[0]) && "string" == typeof arguments[1]) {
            var e3 = arguments[0], n2 = arguments[1];
            return n2 === Lt.SYM_DONTCARE || (n2 === Lt.SYM_TRUE && (e3 >= 0 || e3 === Lt.TRUE) || (n2 === Lt.SYM_FALSE && e3 === Lt.FALSE || (n2 === Lt.SYM_P && e3 === Lt.P || (n2 === Lt.SYM_L && e3 === Lt.L || n2 === Lt.SYM_A && e3 === Lt.A))));
          }
          if ("string" == typeof arguments[0] && "string" == typeof arguments[1]) {
            var i2 = arguments[1];
            return new t2(arguments[0]).matches(i2);
          }
        }, "value") }, { key: "isTrue", value: /* @__PURE__ */ __name(function(t3) {
          return t3 >= 0 || t3 === Lt.TRUE;
        }, "value") }]);
      })(), ti = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "size", value: /* @__PURE__ */ __name(function() {
          return this._size;
        }, "value") }, { key: "addAll", value: /* @__PURE__ */ __name(function(t3) {
          return null === t3 || 0 === t3.length ? null : (this.ensureCapacity(this._size + t3.length), mt.arraycopy(t3, 0, this._data, this._size, t3.length), void (this._size += t3.length));
        }, "value") }, { key: "ensureCapacity", value: /* @__PURE__ */ __name(function(t3) {
          if (t3 <= this._data.length) return null;
          var e3 = Math.max(t3, 2 * this._data.length);
          this._data = At.copyOf(this._data, e3);
        }, "value") }, { key: "toArray", value: /* @__PURE__ */ __name(function() {
          var t3 = new Array(this._size).fill(null);
          return mt.arraycopy(this._data, 0, t3, 0, this._size), t3;
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function(t3) {
          this.ensureCapacity(this._size + 1), this._data[this._size] = t3, ++this._size;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._data = null, this._size = 0, 0 === arguments.length) t2.constructor_.call(this, 10);
          else if (1 === arguments.length) {
            var e3 = arguments[0];
            this._data = new Array(e3).fill(null);
          }
        }, "value") }]);
      })(), ei = (function() {
        function t2() {
          n(this, t2);
        }
        __name(t2, "t");
        return s(t2, [{ key: "getChainStartIndices", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = 0, n2 = new ti(Math.trunc(t3.length / 2));
          n2.add(e3);
          do {
            var i2 = this.findChainEnd(t3, e3);
            n2.add(i2), e3 = i2;
          } while (e3 < t3.length - 1);
          return n2.toArray();
        }, "value") }, { key: "findChainEnd", value: /* @__PURE__ */ __name(function(t3, e3) {
          for (var n2 = je.quadrant(t3[e3], t3[e3 + 1]), i2 = e3 + 1; i2 < t3.length; ) {
            if (je.quadrant(t3[i2 - 1], t3[i2]) !== n2) break;
            i2++;
          }
          return i2 - 1;
        }, "value") }, { key: "OLDgetChainStartIndices", value: /* @__PURE__ */ __name(function(e3) {
          var n2 = 0, i2 = new yt();
          i2.add(n2);
          do {
            var r2 = this.findChainEnd(e3, n2);
            i2.add(r2), n2 = r2;
          } while (n2 < e3.length - 1);
          return t2.toIntArray(i2);
        }, "value") }], [{ key: "toIntArray", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = new Array(t3.size()).fill(null), n2 = 0; n2 < e3.length; n2++) e3[n2] = t3.get(n2).intValue();
          return e3;
        }, "value") }]);
      })(), ni = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "getCoordinates", value: /* @__PURE__ */ __name(function() {
          return this.pts;
        }, "value") }, { key: "getMaxX", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = this.pts[this.startIndex[t2]].x, n2 = this.pts[this.startIndex[t2 + 1]].x;
          return e3 > n2 ? e3 : n2;
        }, "value") }, { key: "getMinX", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = this.pts[this.startIndex[t2]].x, n2 = this.pts[this.startIndex[t2 + 1]].x;
          return e3 < n2 ? e3 : n2;
        }, "value") }, { key: "computeIntersectsForChain", value: /* @__PURE__ */ __name(function() {
          if (4 === arguments.length) {
            var t2 = arguments[0], e3 = arguments[1], n2 = arguments[2], i2 = arguments[3];
            this.computeIntersectsForChain(this.startIndex[t2], this.startIndex[t2 + 1], e3, e3.startIndex[n2], e3.startIndex[n2 + 1], i2);
          } else if (6 === arguments.length) {
            var r2 = arguments[0], s2 = arguments[1], a2 = arguments[2], o2 = arguments[3], u2 = arguments[4], l2 = arguments[5];
            if (s2 - r2 == 1 && u2 - o2 == 1) return l2.addIntersections(this.e, r2, a2.e, o2), null;
            if (!this.overlaps(r2, s2, a2, o2, u2)) return null;
            var h2 = Math.trunc((r2 + s2) / 2), c2 = Math.trunc((o2 + u2) / 2);
            r2 < h2 && (o2 < c2 && this.computeIntersectsForChain(r2, h2, a2, o2, c2, l2), c2 < u2 && this.computeIntersectsForChain(r2, h2, a2, c2, u2, l2)), h2 < s2 && (o2 < c2 && this.computeIntersectsForChain(h2, s2, a2, o2, c2, l2), c2 < u2 && this.computeIntersectsForChain(h2, s2, a2, c2, u2, l2));
          }
        }, "value") }, { key: "overlaps", value: /* @__PURE__ */ __name(function(t2, e3, n2, i2, r2) {
          return U.intersects(this.pts[t2], this.pts[e3], n2.pts[i2], n2.pts[r2]);
        }, "value") }, { key: "getStartIndexes", value: /* @__PURE__ */ __name(function() {
          return this.startIndex;
        }, "value") }, { key: "computeIntersects", value: /* @__PURE__ */ __name(function(t2, e3) {
          for (var n2 = 0; n2 < this.startIndex.length - 1; n2++) for (var i2 = 0; i2 < t2.startIndex.length - 1; i2++) this.computeIntersectsForChain(n2, t2, i2, e3);
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this.e = null, this.pts = null, this.startIndex = null;
          var t2 = arguments[0];
          this.e = t2, this.pts = t2.getCoordinates();
          var e3 = new ei();
          this.startIndex = e3.getChainStartIndices(this.pts);
        }, "value") }]);
      })(), ii = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "getDepth", value: /* @__PURE__ */ __name(function(t3, e3) {
          return this._depth[t3][e3];
        }, "value") }, { key: "setDepth", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          this._depth[t3][e3] = n2;
        }, "value") }, { key: "isNull", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) {
            for (var e3 = 0; e3 < 2; e3++) for (var n2 = 0; n2 < 3; n2++) if (this._depth[e3][n2] !== t2.NULL_VALUE) return false;
            return true;
          }
          if (1 === arguments.length) {
            var i2 = arguments[0];
            return this._depth[i2][1] === t2.NULL_VALUE;
          }
          if (2 === arguments.length) {
            var r2 = arguments[0], s2 = arguments[1];
            return this._depth[r2][s2] === t2.NULL_VALUE;
          }
        }, "value") }, { key: "normalize", value: /* @__PURE__ */ __name(function() {
          for (var t3 = 0; t3 < 2; t3++) if (!this.isNull(t3)) {
            var e3 = this._depth[t3][1];
            this._depth[t3][2] < e3 && (e3 = this._depth[t3][2]), e3 < 0 && (e3 = 0);
            for (var n2 = 1; n2 < 3; n2++) {
              var i2 = 0;
              this._depth[t3][n2] > e3 && (i2 = 1), this._depth[t3][n2] = i2;
            }
          }
        }, "value") }, { key: "getDelta", value: /* @__PURE__ */ __name(function(t3) {
          return this._depth[t3][$.RIGHT] - this._depth[t3][$.LEFT];
        }, "value") }, { key: "getLocation", value: /* @__PURE__ */ __name(function(t3, e3) {
          return this._depth[t3][e3] <= 0 ? H.EXTERIOR : H.INTERIOR;
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          return "A: " + this._depth[0][1] + "," + this._depth[0][2] + " B: " + this._depth[1][1] + "," + this._depth[1][2];
        }, "value") }, { key: "add", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) for (var e3 = arguments[0], n2 = 0; n2 < 2; n2++) for (var i2 = 1; i2 < 3; i2++) {
            var r2 = e3.getLocation(n2, i2);
            r2 !== H.EXTERIOR && r2 !== H.INTERIOR || (this.isNull(n2, i2) ? this._depth[n2][i2] = t2.depthAtLocation(r2) : this._depth[n2][i2] += t2.depthAtLocation(r2));
          }
          else if (3 === arguments.length) {
            var s2 = arguments[0], a2 = arguments[1];
            arguments[2] === H.INTERIOR && this._depth[s2][a2]++;
          }
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._depth = Array(2).fill().map((function() {
            return Array(3);
          }));
          for (var e3 = 0; e3 < 2; e3++) for (var n2 = 0; n2 < 3; n2++) this._depth[e3][n2] = t2.NULL_VALUE;
        }, "value") }, { key: "depthAtLocation", value: /* @__PURE__ */ __name(function(e3) {
          return e3 === H.EXTERIOR ? 0 : e3 === H.INTERIOR ? 1 : t2.NULL_VALUE;
        }, "value") }]);
      })();
      ii.NULL_VALUE = -1;
      var ri = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "getDepth", value: /* @__PURE__ */ __name(function() {
          return this._depth;
        }, "value") }, { key: "getCollapsedEdge", value: /* @__PURE__ */ __name(function() {
          var t3 = new Array(2).fill(null);
          return t3[0] = this.pts[0], t3[1] = this.pts[1], new i2(t3, Ae.toLineLabel(this._label));
        }, "value") }, { key: "isIsolated", value: /* @__PURE__ */ __name(function() {
          return this._isIsolated;
        }, "value") }, { key: "getCoordinates", value: /* @__PURE__ */ __name(function() {
          return this.pts;
        }, "value") }, { key: "setIsolated", value: /* @__PURE__ */ __name(function(t3) {
          this._isIsolated = t3;
        }, "value") }, { key: "setName", value: /* @__PURE__ */ __name(function(t3) {
          this._name = t3;
        }, "value") }, { key: "equals", value: /* @__PURE__ */ __name(function(t3) {
          if (!(t3 instanceof i2)) return false;
          var e3 = t3;
          if (this.pts.length !== e3.pts.length) return false;
          for (var n2 = true, r2 = true, s2 = this.pts.length, a2 = 0; a2 < this.pts.length; a2++) if (this.pts[a2].equals2D(e3.pts[a2]) || (n2 = false), this.pts[a2].equals2D(e3.pts[--s2]) || (r2 = false), !n2 && !r2) return false;
          return true;
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) return this.pts.length > 0 ? this.pts[0] : null;
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return this.pts[t3];
          }
        }, "value") }, { key: "print", value: /* @__PURE__ */ __name(function(t3) {
          t3.print("edge " + this._name + ": "), t3.print("LINESTRING (");
          for (var e3 = 0; e3 < this.pts.length; e3++) e3 > 0 && t3.print(","), t3.print(this.pts[e3].x + " " + this.pts[e3].y);
          t3.print(")  " + this._label + " " + this._depthDelta);
        }, "value") }, { key: "computeIM", value: /* @__PURE__ */ __name(function(t3) {
          i2.updateIM(this._label, t3);
        }, "value") }, { key: "isCollapsed", value: /* @__PURE__ */ __name(function() {
          return !!this._label.isArea() && (3 === this.pts.length && !!this.pts[0].equals(this.pts[2]));
        }, "value") }, { key: "isClosed", value: /* @__PURE__ */ __name(function() {
          return this.pts[0].equals(this.pts[this.pts.length - 1]);
        }, "value") }, { key: "getMaximumSegmentIndex", value: /* @__PURE__ */ __name(function() {
          return this.pts.length - 1;
        }, "value") }, { key: "getDepthDelta", value: /* @__PURE__ */ __name(function() {
          return this._depthDelta;
        }, "value") }, { key: "getNumPoints", value: /* @__PURE__ */ __name(function() {
          return this.pts.length;
        }, "value") }, { key: "printReverse", value: /* @__PURE__ */ __name(function(t3) {
          t3.print("edge " + this._name + ": ");
          for (var e3 = this.pts.length - 1; e3 >= 0; e3--) t3.print(this.pts[e3] + " ");
          t3.println("");
        }, "value") }, { key: "getMonotoneChainEdge", value: /* @__PURE__ */ __name(function() {
          return null === this._mce && (this._mce = new ni(this)), this._mce;
        }, "value") }, { key: "getEnvelope", value: /* @__PURE__ */ __name(function() {
          if (null === this._env) {
            this._env = new U();
            for (var t3 = 0; t3 < this.pts.length; t3++) this._env.expandToInclude(this.pts[t3]);
          }
          return this._env;
        }, "value") }, { key: "addIntersection", value: /* @__PURE__ */ __name(function(t3, e3, n2, i3) {
          var r2 = new X(t3.getIntersection(i3)), s2 = e3, a2 = t3.getEdgeDistance(n2, i3), o2 = s2 + 1;
          if (o2 < this.pts.length) {
            var u2 = this.pts[o2];
            r2.equals2D(u2) && (s2 = o2, a2 = 0);
          }
          this.eiList.add(r2, s2, a2);
        }, "value") }, { key: "toString", value: /* @__PURE__ */ __name(function() {
          var t3 = new Jt();
          t3.append("edge " + this._name + ": "), t3.append("LINESTRING (");
          for (var e3 = 0; e3 < this.pts.length; e3++) e3 > 0 && t3.append(","), t3.append(this.pts[e3].x + " " + this.pts[e3].y);
          return t3.append(")  " + this._label + " " + this._depthDelta), t3.toString();
        }, "value") }, { key: "isPointwiseEqual", value: /* @__PURE__ */ __name(function(t3) {
          if (this.pts.length !== t3.pts.length) return false;
          for (var e3 = 0; e3 < this.pts.length; e3++) if (!this.pts[e3].equals2D(t3.pts[e3])) return false;
          return true;
        }, "value") }, { key: "setDepthDelta", value: /* @__PURE__ */ __name(function(t3) {
          this._depthDelta = t3;
        }, "value") }, { key: "getEdgeIntersectionList", value: /* @__PURE__ */ __name(function() {
          return this.eiList;
        }, "value") }, { key: "addIntersections", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          for (var i3 = 0; i3 < t3.getIntersectionNum(); i3++) this.addIntersection(t3, e3, n2, i3);
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this.pts = null, this._env = null, this.eiList = new Qn(this), this._name = null, this._mce = null, this._isIsolated = true, this._depth = new ii(), this._depthDelta = 0, 1 === arguments.length) {
            var t3 = arguments[0];
            i2.constructor_.call(this, t3, null);
          } else if (2 === arguments.length) {
            var e3 = arguments[0], n2 = arguments[1];
            this.pts = e3, this._label = n2;
          }
        }, "value") }, { key: "updateIM", value: /* @__PURE__ */ __name(function() {
          if (!(2 === arguments.length && arguments[1] instanceof $n && arguments[0] instanceof Ae)) return f(i2, "updateIM", this).apply(this, arguments);
          var t3 = arguments[0], e3 = arguments[1];
          e3.setAtLeastIfValid(t3.getLocation(0, $.ON), t3.getLocation(1, $.ON), 1), t3.isArea() && (e3.setAtLeastIfValid(t3.getLocation(0, $.LEFT), t3.getLocation(1, $.LEFT), 2), e3.setAtLeastIfValid(t3.getLocation(0, $.RIGHT), t3.getLocation(1, $.RIGHT), 2));
        }, "value") }]);
      })(Ge), si = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "setWorkingPrecisionModel", value: /* @__PURE__ */ __name(function(t3) {
          this._workingPrecisionModel = t3;
        }, "value") }, { key: "insertUniqueEdge", value: /* @__PURE__ */ __name(function(e3) {
          var n2 = this._edgeList.findEqualEdge(e3);
          if (null !== n2) {
            var i2 = n2.getLabel(), r2 = e3.getLabel();
            n2.isPointwiseEqual(e3) || (r2 = new Ae(e3.getLabel())).flip(), i2.merge(r2);
            var s2 = t2.depthDelta(r2), a2 = n2.getDepthDelta() + s2;
            n2.setDepthDelta(a2);
          } else this._edgeList.add(e3), e3.setDepthDelta(t2.depthDelta(e3.getLabel()));
        }, "value") }, { key: "buildSubgraphs", value: /* @__PURE__ */ __name(function(t3, e3) {
          for (var n2 = new yt(), i2 = t3.iterator(); i2.hasNext(); ) {
            var r2 = i2.next(), s2 = r2.getRightmostCoordinate(), a2 = new Pn(n2).getDepth(s2);
            r2.computeDepth(a2), r2.findResultEdges(), n2.add(r2), e3.add(r2.getDirectedEdges(), r2.getNodes());
          }
        }, "value") }, { key: "createSubgraphs", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = new yt(), n2 = t3.getNodes().iterator(); n2.hasNext(); ) {
            var i2 = n2.next();
            if (!i2.isVisited()) {
              var r2 = new _t();
              r2.create(i2), e3.add(r2);
            }
          }
          return an.sort(e3, an.reverseOrder()), e3;
        }, "value") }, { key: "createEmptyResultGeometry", value: /* @__PURE__ */ __name(function() {
          return this._geomFact.createPolygon();
        }, "value") }, { key: "getNoder", value: /* @__PURE__ */ __name(function(t3) {
          if (null !== this._workingNoder) return this._workingNoder;
          var e3 = new Cn(), n2 = new we();
          return n2.setPrecisionModel(t3), e3.setSegmentIntersector(new Kn(n2)), e3;
        }, "value") }, { key: "buffer", value: /* @__PURE__ */ __name(function(t3, e3) {
          var n2 = this._workingPrecisionModel;
          null === n2 && (n2 = t3.getPrecisionModel()), this._geomFact = t3.getFactory();
          var i2 = new An(n2, this._bufParams), r2 = new Yn(t3, e3, i2).getCurves();
          if (r2.size() <= 0) return this.createEmptyResultGeometry();
          this.computeNodedEdges(r2, n2), this._graph = new Qe(new Hn()), this._graph.addEdges(this._edgeList.getEdges());
          var s2 = this.createSubgraphs(this._graph), a2 = new $e(this._geomFact);
          this.buildSubgraphs(s2, a2);
          var o2 = a2.getPolygons();
          return o2.size() <= 0 ? this.createEmptyResultGeometry() : this._geomFact.buildGeometry(o2);
        }, "value") }, { key: "computeNodedEdges", value: /* @__PURE__ */ __name(function(t3, e3) {
          var n2 = this.getNoder(e3);
          n2.computeNodes(t3);
          for (var i2 = n2.getNodedSubstrings().iterator(); i2.hasNext(); ) {
            var r2 = i2.next(), s2 = r2.getCoordinates();
            if (2 !== s2.length || !s2[0].equals2D(s2[1])) {
              var a2 = r2.getData(), o2 = new ri(r2.getCoordinates(), new Ae(a2));
              this.insertUniqueEdge(o2);
            }
          }
        }, "value") }, { key: "setNoder", value: /* @__PURE__ */ __name(function(t3) {
          this._workingNoder = t3;
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._bufParams = null, this._workingPrecisionModel = null, this._workingNoder = null, this._geomFact = null, this._graph = null, this._edgeList = new jn();
          var t3 = arguments[0];
          this._bufParams = t3;
        }, "value") }, { key: "depthDelta", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = t3.getLocation(0, $.LEFT), n2 = t3.getLocation(0, $.RIGHT);
          return e3 === H.INTERIOR && n2 === H.EXTERIOR ? 1 : e3 === H.EXTERIOR && n2 === H.INTERIOR ? -1 : 0;
        }, "value") }, { key: "convertSegStrings", value: /* @__PURE__ */ __name(function(t3) {
          for (var e3 = new ae(), n2 = new yt(); t3.hasNext(); ) {
            var i2 = t3.next(), r2 = e3.createLineString(i2.getCoordinates());
            n2.add(r2);
          }
          return e3.buildGeometry(n2);
        }, "value") }]);
      })(), ai = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "rescale", value: /* @__PURE__ */ __name(function() {
          if (rt(arguments[0], Z)) for (var t3 = arguments[0].iterator(); t3.hasNext(); ) {
            var e3 = t3.next();
            this.rescale(e3.getCoordinates());
          }
          else if (arguments[0] instanceof Array) {
            for (var n2 = arguments[0], i2 = 0; i2 < n2.length; i2++) n2[i2].x = n2[i2].x / this._scaleFactor + this._offsetX, n2[i2].y = n2[i2].y / this._scaleFactor + this._offsetY;
            2 === n2.length && n2[0].equals2D(n2[1]) && mt.out.println(n2);
          }
        }, "value") }, { key: "scale", value: /* @__PURE__ */ __name(function() {
          if (rt(arguments[0], Z)) {
            for (var t3 = arguments[0], e3 = new yt(t3.size()), n2 = t3.iterator(); n2.hasNext(); ) {
              var i2 = n2.next();
              e3.add(new xn(this.scale(i2.getCoordinates()), i2.getData()));
            }
            return e3;
          }
          if (arguments[0] instanceof Array) {
            for (var r2 = arguments[0], s2 = new Array(r2.length).fill(null), a2 = 0; a2 < r2.length; a2++) s2[a2] = new X(Math.round((r2[a2].x - this._offsetX) * this._scaleFactor), Math.round((r2[a2].y - this._offsetY) * this._scaleFactor), r2[a2].getZ());
            return jt.removeRepeatedPoints(s2);
          }
        }, "value") }, { key: "isIntegerPrecision", value: /* @__PURE__ */ __name(function() {
          return 1 === this._scaleFactor;
        }, "value") }, { key: "getNodedSubstrings", value: /* @__PURE__ */ __name(function() {
          var t3 = this._noder.getNodedSubstrings();
          return this._isScaled && this.rescale(t3), t3;
        }, "value") }, { key: "computeNodes", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = t3;
          this._isScaled && (e3 = this.scale(t3)), this._noder.computeNodes(e3);
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [Sn];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._noder = null, this._scaleFactor = null, this._offsetX = null, this._offsetY = null, this._isScaled = false, 2 === arguments.length) {
            var e3 = arguments[0], n2 = arguments[1];
            t2.constructor_.call(this, e3, n2, 0, 0);
          } else if (4 === arguments.length) {
            var i2 = arguments[0], r2 = arguments[1];
            this._noder = i2, this._scaleFactor = r2, this._isScaled = !this.isIntegerPrecision();
          }
        }, "value") }]);
      })(), oi = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "checkEndPtVertexIntersections", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) for (var t3 = this._segStrings.iterator(); t3.hasNext(); ) {
            var e3 = t3.next().getCoordinates();
            this.checkEndPtVertexIntersections(e3[0], this._segStrings), this.checkEndPtVertexIntersections(e3[e3.length - 1], this._segStrings);
          }
          else if (2 === arguments.length) {
            for (var n2 = arguments[0], i2 = arguments[1].iterator(); i2.hasNext(); ) for (var r2 = i2.next().getCoordinates(), s2 = 1; s2 < r2.length - 1; s2++) if (r2[s2].equals(n2)) throw new D("found endpt/interior pt intersection at index " + s2 + " :pt " + n2);
          }
        }, "value") }, { key: "checkInteriorIntersections", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) for (var t3 = this._segStrings.iterator(); t3.hasNext(); ) for (var e3 = t3.next(), n2 = this._segStrings.iterator(); n2.hasNext(); ) {
            var i2 = n2.next();
            this.checkInteriorIntersections(e3, i2);
          }
          else if (2 === arguments.length) for (var r2 = arguments[0], s2 = arguments[1], a2 = r2.getCoordinates(), o2 = s2.getCoordinates(), u2 = 0; u2 < a2.length - 1; u2++) for (var l2 = 0; l2 < o2.length - 1; l2++) this.checkInteriorIntersections(r2, u2, s2, l2);
          else if (4 === arguments.length) {
            var h2 = arguments[0], c2 = arguments[1], f2 = arguments[2], g2 = arguments[3];
            if (h2 === f2 && c2 === g2) return null;
            var v2 = h2.getCoordinates()[c2], y2 = h2.getCoordinates()[c2 + 1], d2 = f2.getCoordinates()[g2], _2 = f2.getCoordinates()[g2 + 1];
            if (this._li.computeIntersection(v2, y2, d2, _2), this._li.hasIntersection() && (this._li.isProper() || this.hasInteriorIntersection(this._li, v2, y2) || this.hasInteriorIntersection(this._li, d2, _2))) throw new D("found non-noded intersection at " + v2 + "-" + y2 + " and " + d2 + "-" + _2);
          }
        }, "value") }, { key: "checkValid", value: /* @__PURE__ */ __name(function() {
          this.checkEndPtVertexIntersections(), this.checkInteriorIntersections(), this.checkCollapses();
        }, "value") }, { key: "checkCollapses", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) for (var t3 = this._segStrings.iterator(); t3.hasNext(); ) {
            var e3 = t3.next();
            this.checkCollapses(e3);
          }
          else if (1 === arguments.length) for (var n2 = arguments[0].getCoordinates(), i2 = 0; i2 < n2.length - 2; i2++) this.checkCollapse(n2[i2], n2[i2 + 1], n2[i2 + 2]);
        }, "value") }, { key: "hasInteriorIntersection", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          for (var i2 = 0; i2 < t3.getIntersectionNum(); i2++) {
            var r2 = t3.getIntersection(i2);
            if (!r2.equals(e3) && !r2.equals(n2)) return true;
          }
          return false;
        }, "value") }, { key: "checkCollapse", value: /* @__PURE__ */ __name(function(e3, n2, i2) {
          if (e3.equals(i2)) throw new D("found non-noded collapse at " + t2.fact.createLineString([e3, n2, i2]));
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._li = new we(), this._segStrings = null;
          var t3 = arguments[0];
          this._segStrings = t3;
        }, "value") }]);
      })();
      oi.fact = new ae();
      var ui = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "intersectsScaled", value: /* @__PURE__ */ __name(function(t3, e3) {
          var n2 = Math.min(t3.x, e3.x), i2 = Math.max(t3.x, e3.x), r2 = Math.min(t3.y, e3.y), s2 = Math.max(t3.y, e3.y), a2 = this._maxx < n2 || this._minx > i2 || this._maxy < r2 || this._miny > s2;
          if (a2) return false;
          var o2 = this.intersectsToleranceSquare(t3, e3);
          return G.isTrue(!(a2 && o2), "Found bad envelope test"), o2;
        }, "value") }, { key: "initCorners", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = 0.5;
          this._minx = t3.x - e3, this._maxx = t3.x + e3, this._miny = t3.y - e3, this._maxy = t3.y + e3, this._corner[0] = new X(this._maxx, this._maxy), this._corner[1] = new X(this._minx, this._maxy), this._corner[2] = new X(this._minx, this._miny), this._corner[3] = new X(this._maxx, this._miny);
        }, "value") }, { key: "intersects", value: /* @__PURE__ */ __name(function(t3, e3) {
          return 1 === this._scaleFactor ? this.intersectsScaled(t3, e3) : (this.copyScaled(t3, this._p0Scaled), this.copyScaled(e3, this._p1Scaled), this.intersectsScaled(this._p0Scaled, this._p1Scaled));
        }, "value") }, { key: "scale", value: /* @__PURE__ */ __name(function(t3) {
          return Math.round(t3 * this._scaleFactor);
        }, "value") }, { key: "getCoordinate", value: /* @__PURE__ */ __name(function() {
          return this._originalPt;
        }, "value") }, { key: "copyScaled", value: /* @__PURE__ */ __name(function(t3, e3) {
          e3.x = this.scale(t3.x), e3.y = this.scale(t3.y);
        }, "value") }, { key: "getSafeEnvelope", value: /* @__PURE__ */ __name(function() {
          if (null === this._safeEnv) {
            var e3 = t2.SAFE_ENV_EXPANSION_FACTOR / this._scaleFactor;
            this._safeEnv = new U(this._originalPt.x - e3, this._originalPt.x + e3, this._originalPt.y - e3, this._originalPt.y + e3);
          }
          return this._safeEnv;
        }, "value") }, { key: "intersectsPixelClosure", value: /* @__PURE__ */ __name(function(t3, e3) {
          return this._li.computeIntersection(t3, e3, this._corner[0], this._corner[1]), !!this._li.hasIntersection() || (this._li.computeIntersection(t3, e3, this._corner[1], this._corner[2]), !!this._li.hasIntersection() || (this._li.computeIntersection(t3, e3, this._corner[2], this._corner[3]), !!this._li.hasIntersection() || (this._li.computeIntersection(t3, e3, this._corner[3], this._corner[0]), !!this._li.hasIntersection())));
        }, "value") }, { key: "intersectsToleranceSquare", value: /* @__PURE__ */ __name(function(t3, e3) {
          var n2 = false, i2 = false;
          return this._li.computeIntersection(t3, e3, this._corner[0], this._corner[1]), !!this._li.isProper() || (this._li.computeIntersection(t3, e3, this._corner[1], this._corner[2]), !!this._li.isProper() || (this._li.hasIntersection() && (n2 = true), this._li.computeIntersection(t3, e3, this._corner[2], this._corner[3]), !!this._li.isProper() || (this._li.hasIntersection() && (i2 = true), this._li.computeIntersection(t3, e3, this._corner[3], this._corner[0]), !!this._li.isProper() || (!(!n2 || !i2) || (!!t3.equals(this._pt) || !!e3.equals(this._pt))))));
        }, "value") }, { key: "addSnappedNode", value: /* @__PURE__ */ __name(function(t3, e3) {
          var n2 = t3.getCoordinate(e3), i2 = t3.getCoordinate(e3 + 1);
          return !!this.intersects(n2, i2) && (t3.addIntersection(this.getCoordinate(), e3), true);
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._li = null, this._pt = null, this._originalPt = null, this._ptScaled = null, this._p0Scaled = null, this._p1Scaled = null, this._scaleFactor = null, this._minx = null, this._maxx = null, this._miny = null, this._maxy = null, this._corner = new Array(4).fill(null), this._safeEnv = null;
          var t3 = arguments[0], e3 = arguments[1], n2 = arguments[2];
          if (this._originalPt = t3, this._pt = t3, this._scaleFactor = e3, this._li = n2, e3 <= 0) throw new m("Scale factor must be non-zero");
          1 !== e3 && (this._pt = new X(this.scale(t3.x), this.scale(t3.y)), this._p0Scaled = new X(), this._p1Scaled = new X()), this.initCorners(this._pt);
        }, "value") }]);
      })();
      ui.SAFE_ENV_EXPANSION_FACTOR = 0.75;
      var li = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "select", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) ;
          else if (2 === arguments.length) {
            var t2 = arguments[1];
            arguments[0].getLineSegment(t2, this.selectedSegment), this.select(this.selectedSegment);
          }
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this.selectedSegment = new In();
        }, "value") }]);
      })(), hi = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "snap", value: /* @__PURE__ */ __name(function() {
          if (1 === arguments.length) {
            var t2 = arguments[0];
            return this.snap(t2, null, -1);
          }
          if (3 === arguments.length) {
            var e3 = arguments[0], i2 = arguments[1], r2 = arguments[2], a2 = e3.getSafeEnvelope(), o2 = new ci(e3, i2, r2);
            return this._index.query(a2, new ((function() {
              return s(/* @__PURE__ */ __name((function t3() {
                n(this, t3);
              }), "t"), [{ key: "interfaces_", get: /* @__PURE__ */ __name(function() {
                return [ln];
              }, "get") }, { key: "visitItem", value: /* @__PURE__ */ __name(function(t3) {
                t3.select(a2, o2);
              }, "value") }]);
            })())()), o2.isNodeAdded();
          }
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._index = null;
          var t2 = arguments[0];
          this._index = t2;
        }, "value") }]);
      })(), ci = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        __name(i2, "i");
        return l(i2, t2), s(i2, [{ key: "isNodeAdded", value: /* @__PURE__ */ __name(function() {
          return this._isNodeAdded;
        }, "value") }, { key: "select", value: /* @__PURE__ */ __name(function() {
          if (!(2 === arguments.length && Number.isInteger(arguments[1]) && arguments[0] instanceof Nn)) return f(i2, "select", this, 1).apply(this, arguments);
          var t3 = arguments[1], e3 = arguments[0].getContext();
          if (this._parentEdge === e3 && (t3 === this._hotPixelVertexIndex || t3 + 1 === this._hotPixelVertexIndex)) return null;
          this._isNodeAdded |= this._hotPixel.addSnappedNode(e3, t3);
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._hotPixel = null, this._parentEdge = null, this._hotPixelVertexIndex = null, this._isNodeAdded = false;
          var t3 = arguments[0], e3 = arguments[1], n2 = arguments[2];
          this._hotPixel = t3, this._parentEdge = e3, this._hotPixelVertexIndex = n2;
        }, "value") }]);
      })(li);
      hi.HotPixelSnapAction = ci;
      var fi = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "processIntersections", value: /* @__PURE__ */ __name(function(t2, e3, n2, i2) {
          if (t2 === n2 && e3 === i2) return null;
          var r2 = t2.getCoordinates()[e3], s2 = t2.getCoordinates()[e3 + 1], a2 = n2.getCoordinates()[i2], o2 = n2.getCoordinates()[i2 + 1];
          if (this._li.computeIntersection(r2, s2, a2, o2), this._li.hasIntersection() && this._li.isInteriorIntersection()) {
            for (var u2 = 0; u2 < this._li.getIntersectionNum(); u2++) this._interiorIntersections.add(this._li.getIntersection(u2));
            t2.addIntersections(this._li, e3, 0), n2.addIntersections(this._li, i2, 1);
          }
        }, "value") }, { key: "isDone", value: /* @__PURE__ */ __name(function() {
          return false;
        }, "value") }, { key: "getInteriorIntersections", value: /* @__PURE__ */ __name(function() {
          return this._interiorIntersections;
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [Wn];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._li = null, this._interiorIntersections = null;
          var t2 = arguments[0];
          this._li = t2, this._interiorIntersections = new yt();
        }, "value") }]);
      })(), gi = (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), "t"), [{ key: "checkCorrectness", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = xn.getNodedSubstrings(t2), n2 = new oi(e3);
          try {
            n2.checkValid();
          } catch (t3) {
            if (!(t3 instanceof p)) throw t3;
            t3.printStackTrace();
          }
        }, "value") }, { key: "getNodedSubstrings", value: /* @__PURE__ */ __name(function() {
          return xn.getNodedSubstrings(this._nodedSegStrings);
        }, "value") }, { key: "snapRound", value: /* @__PURE__ */ __name(function(t2, e3) {
          var n2 = this.findInteriorIntersections(t2, e3);
          this.computeIntersectionSnaps(n2), this.computeVertexSnaps(t2);
        }, "value") }, { key: "findInteriorIntersections", value: /* @__PURE__ */ __name(function(t2, e3) {
          var n2 = new fi(e3);
          return this._noder.setSegmentIntersector(n2), this._noder.computeNodes(t2), n2.getInteriorIntersections();
        }, "value") }, { key: "computeVertexSnaps", value: /* @__PURE__ */ __name(function() {
          if (rt(arguments[0], Z)) for (var t2 = arguments[0].iterator(); t2.hasNext(); ) {
            var e3 = t2.next();
            this.computeVertexSnaps(e3);
          }
          else if (arguments[0] instanceof xn) for (var n2 = arguments[0], i2 = n2.getCoordinates(), r2 = 0; r2 < i2.length; r2++) {
            var s2 = new ui(i2[r2], this._scaleFactor, this._li);
            this._pointSnapper.snap(s2, n2, r2) && n2.addIntersection(i2[r2], r2);
          }
        }, "value") }, { key: "computeNodes", value: /* @__PURE__ */ __name(function(t2) {
          this._nodedSegStrings = t2, this._noder = new Cn(), this._pointSnapper = new hi(this._noder.getIndex()), this.snapRound(t2, this._li);
        }, "value") }, { key: "computeIntersectionSnaps", value: /* @__PURE__ */ __name(function(t2) {
          for (var e3 = t2.iterator(); e3.hasNext(); ) {
            var n2 = e3.next(), i2 = new ui(n2, this._scaleFactor, this._li);
            this._pointSnapper.snap(i2);
          }
        }, "value") }, { key: "interfaces_", get: /* @__PURE__ */ __name(function() {
          return [Sn];
        }, "get") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          this._pm = null, this._li = null, this._scaleFactor = null, this._noder = null, this._pointSnapper = null, this._nodedSegStrings = null;
          var t2 = arguments[0];
          this._pm = t2, this._li = new we(), this._li.setPrecisionModel(t2), this._scaleFactor = t2.getScale();
        }, "value") }]);
      })(), vi = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        __name(t2, "t");
        return s(t2, [{ key: "bufferFixedPrecision", value: /* @__PURE__ */ __name(function(t3) {
          var e3 = new ai(new gi(new ie(1)), t3.getScale()), n2 = new si(this._bufParams);
          n2.setWorkingPrecisionModel(t3), n2.setNoder(e3), this._resultGeometry = n2.buffer(this._argGeom, this._distance);
        }, "value") }, { key: "bufferReducedPrecision", value: /* @__PURE__ */ __name(function() {
          if (0 === arguments.length) {
            for (var e3 = t2.MAX_PRECISION_DIGITS; e3 >= 0; e3--) {
              try {
                this.bufferReducedPrecision(e3);
              } catch (t3) {
                if (!(t3 instanceof gt3)) throw t3;
                this._saveException = t3;
              }
              if (null !== this._resultGeometry) return null;
            }
            throw this._saveException;
          }
          if (1 === arguments.length) {
            var n2 = arguments[0], i2 = t2.precisionScaleFactor(this._argGeom, this._distance, n2), r2 = new ie(i2);
            this.bufferFixedPrecision(r2);
          }
        }, "value") }, { key: "computeGeometry", value: /* @__PURE__ */ __name(function() {
          if (this.bufferOriginalPrecision(), null !== this._resultGeometry) return null;
          var t3 = this._argGeom.getFactory().getPrecisionModel();
          t3.getType() === ie.FIXED ? this.bufferFixedPrecision(t3) : this.bufferReducedPrecision();
        }, "value") }, { key: "setQuadrantSegments", value: /* @__PURE__ */ __name(function(t3) {
          this._bufParams.setQuadrantSegments(t3);
        }, "value") }, { key: "bufferOriginalPrecision", value: /* @__PURE__ */ __name(function() {
          try {
            var t3 = new si(this._bufParams);
            this._resultGeometry = t3.buffer(this._argGeom, this._distance);
          } catch (t4) {
            if (!(t4 instanceof D)) throw t4;
            this._saveException = t4;
          }
        }, "value") }, { key: "getResultGeometry", value: /* @__PURE__ */ __name(function(t3) {
          return this._distance = t3, this.computeGeometry(), this._resultGeometry;
        }, "value") }, { key: "setEndCapStyle", value: /* @__PURE__ */ __name(function(t3) {
          this._bufParams.setEndCapStyle(t3);
        }, "value") }], [{ key: "constructor_", value: /* @__PURE__ */ __name(function() {
          if (this._argGeom = null, this._distance = null, this._bufParams = new _(), this._resultGeometry = null, this._saveException = null, 1 === arguments.length) {
            var t3 = arguments[0];
            this._argGeom = t3;
          } else if (2 === arguments.length) {
            var e3 = arguments[0], n2 = arguments[1];
            this._argGeom = e3, this._bufParams = n2;
          }
        }, "value") }, { key: "bufferOp", value: /* @__PURE__ */ __name(function() {
          if (2 === arguments.length) {
            var e3 = arguments[1];
            return new t2(arguments[0]).getResultGeometry(e3);
          }
          if (3 === arguments.length) {
            if (Number.isInteger(arguments[2]) && arguments[0] instanceof V && "number" == typeof arguments[1]) {
              var n2 = arguments[1], i2 = arguments[2], r2 = new t2(arguments[0]);
              return r2.setQuadrantSegments(i2), r2.getResultGeometry(n2);
            }
            if (arguments[2] instanceof _ && arguments[0] instanceof V && "number" == typeof arguments[1]) {
              var s2 = arguments[1];
              return new t2(arguments[0], arguments[2]).getResultGeometry(s2);
            }
          } else if (4 === arguments.length) {
            var a2 = arguments[1], o2 = arguments[2], u2 = arguments[3], l2 = new t2(arguments[0]);
            return l2.setQuadrantSegments(o2), l2.setEndCapStyle(u2), l2.getResultGeometry(a2);
          }
        }, "value") }, { key: "precisionScaleFactor", value: /* @__PURE__ */ __name(function(t3, e3, n2) {
          var i2 = t3.getEnvelopeInternal(), r2 = kt.max(Math.abs(i2.getMaxX()), Math.abs(i2.getMaxY()), Math.abs(i2.getMinX()), Math.abs(i2.getMinY())) + 2 * (e3 > 0 ? e3 : 0), s2 = n2 - Math.trunc(Math.log(r2) / Math.log(10) + 1);
          return Math.pow(10, s2);
        }, "value") }]);
      })();
      vi.CAP_ROUND = _.CAP_ROUND, vi.CAP_BUTT = _.CAP_FLAT, vi.CAP_FLAT = _.CAP_FLAT, vi.CAP_SQUARE = _.CAP_SQUARE, vi.MAX_PRECISION_DIGITS = 12;
      var yi = ["Point", "MultiPoint", "LineString", "MultiLineString", "Polygon", "MultiPolygon"], di = (function() {
        return s(/* @__PURE__ */ __name((function t2(e3) {
          n(this, t2), this.geometryFactory = e3 || new ae();
        }), "t"), [{ key: "read", value: /* @__PURE__ */ __name(function(t2) {
          var e3, n2 = (e3 = "string" == typeof t2 ? JSON.parse(t2) : t2).type;
          if (!_i[n2]) throw new Error("Unknown GeoJSON type: " + e3.type);
          return -1 !== yi.indexOf(n2) ? _i[n2].call(this, e3.coordinates) : "GeometryCollection" === n2 ? _i[n2].call(this, e3.geometries) : _i[n2].call(this, e3);
        }, "value") }, { key: "write", value: /* @__PURE__ */ __name(function(t2) {
          var e3 = t2.getGeometryType();
          if (!pi2[e3]) throw new Error("Geometry is not supported");
          return pi2[e3].call(this, t2);
        }, "value") }]);
      })(), _i = { Feature: /* @__PURE__ */ __name(function(t2) {
        var e3 = {};
        for (var n2 in t2) e3[n2] = t2[n2];
        if (t2.geometry) {
          var i2 = t2.geometry.type;
          if (!_i[i2]) throw new Error("Unknown GeoJSON type: " + t2.type);
          e3.geometry = this.read(t2.geometry);
        }
        return t2.bbox && (e3.bbox = _i.bbox.call(this, t2.bbox)), e3;
      }, "Feature"), FeatureCollection: /* @__PURE__ */ __name(function(t2) {
        var e3 = {};
        if (t2.features) {
          e3.features = [];
          for (var n2 = 0; n2 < t2.features.length; ++n2) e3.features.push(this.read(t2.features[n2]));
        }
        return t2.bbox && (e3.bbox = this.parse.bbox.call(this, t2.bbox)), e3;
      }, "FeatureCollection"), coordinates: /* @__PURE__ */ __name(function(t2) {
        for (var e3 = [], n2 = 0; n2 < t2.length; ++n2) {
          var r2 = t2[n2];
          e3.push(i(X, g(r2)));
        }
        return e3;
      }, "coordinates"), bbox: /* @__PURE__ */ __name(function(t2) {
        return this.geometryFactory.createLinearRing([new X(t2[0], t2[1]), new X(t2[2], t2[1]), new X(t2[2], t2[3]), new X(t2[0], t2[3]), new X(t2[0], t2[1])]);
      }, "bbox"), Point: /* @__PURE__ */ __name(function(t2) {
        var e3 = i(X, g(t2));
        return this.geometryFactory.createPoint(e3);
      }, "Point"), MultiPoint: /* @__PURE__ */ __name(function(t2) {
        for (var e3 = [], n2 = 0; n2 < t2.length; ++n2) e3.push(_i.Point.call(this, t2[n2]));
        return this.geometryFactory.createMultiPoint(e3);
      }, "MultiPoint"), LineString: /* @__PURE__ */ __name(function(t2) {
        var e3 = _i.coordinates.call(this, t2);
        return this.geometryFactory.createLineString(e3);
      }, "LineString"), MultiLineString: /* @__PURE__ */ __name(function(t2) {
        for (var e3 = [], n2 = 0; n2 < t2.length; ++n2) e3.push(_i.LineString.call(this, t2[n2]));
        return this.geometryFactory.createMultiLineString(e3);
      }, "MultiLineString"), Polygon: /* @__PURE__ */ __name(function(t2) {
        for (var e3 = _i.coordinates.call(this, t2[0]), n2 = this.geometryFactory.createLinearRing(e3), i2 = [], r2 = 1; r2 < t2.length; ++r2) {
          var s2 = t2[r2], a2 = _i.coordinates.call(this, s2), o2 = this.geometryFactory.createLinearRing(a2);
          i2.push(o2);
        }
        return this.geometryFactory.createPolygon(n2, i2);
      }, "Polygon"), MultiPolygon: /* @__PURE__ */ __name(function(t2) {
        for (var e3 = [], n2 = 0; n2 < t2.length; ++n2) {
          var i2 = t2[n2];
          e3.push(_i.Polygon.call(this, i2));
        }
        return this.geometryFactory.createMultiPolygon(e3);
      }, "MultiPolygon"), GeometryCollection: /* @__PURE__ */ __name(function(t2) {
        for (var e3 = [], n2 = 0; n2 < t2.length; ++n2) {
          var i2 = t2[n2];
          e3.push(this.read(i2));
        }
        return this.geometryFactory.createGeometryCollection(e3);
      }, "GeometryCollection") }, pi2 = { coordinate: /* @__PURE__ */ __name(function(t2) {
        var e3 = [t2.x, t2.y];
        return t2.z && e3.push(t2.z), t2.m && e3.push(t2.m), e3;
      }, "coordinate"), Point: /* @__PURE__ */ __name(function(t2) {
        return { type: "Point", coordinates: pi2.coordinate.call(this, t2.getCoordinate()) };
      }, "Point"), MultiPoint: /* @__PURE__ */ __name(function(t2) {
        for (var e3 = [], n2 = 0; n2 < t2._geometries.length; ++n2) {
          var i2 = t2._geometries[n2], r2 = pi2.Point.call(this, i2);
          e3.push(r2.coordinates);
        }
        return { type: "MultiPoint", coordinates: e3 };
      }, "MultiPoint"), LineString: /* @__PURE__ */ __name(function(t2) {
        for (var e3 = [], n2 = t2.getCoordinates(), i2 = 0; i2 < n2.length; ++i2) {
          var r2 = n2[i2];
          e3.push(pi2.coordinate.call(this, r2));
        }
        return { type: "LineString", coordinates: e3 };
      }, "LineString"), MultiLineString: /* @__PURE__ */ __name(function(t2) {
        for (var e3 = [], n2 = 0; n2 < t2._geometries.length; ++n2) {
          var i2 = t2._geometries[n2], r2 = pi2.LineString.call(this, i2);
          e3.push(r2.coordinates);
        }
        return { type: "MultiLineString", coordinates: e3 };
      }, "MultiLineString"), Polygon: /* @__PURE__ */ __name(function(t2) {
        var e3 = [], n2 = pi2.LineString.call(this, t2._shell);
        e3.push(n2.coordinates);
        for (var i2 = 0; i2 < t2._holes.length; ++i2) {
          var r2 = t2._holes[i2], s2 = pi2.LineString.call(this, r2);
          e3.push(s2.coordinates);
        }
        return { type: "Polygon", coordinates: e3 };
      }, "Polygon"), MultiPolygon: /* @__PURE__ */ __name(function(t2) {
        for (var e3 = [], n2 = 0; n2 < t2._geometries.length; ++n2) {
          var i2 = t2._geometries[n2], r2 = pi2.Polygon.call(this, i2);
          e3.push(r2.coordinates);
        }
        return { type: "MultiPolygon", coordinates: e3 };
      }, "MultiPolygon"), GeometryCollection: /* @__PURE__ */ __name(function(t2) {
        for (var e3 = [], n2 = 0; n2 < t2._geometries.length; ++n2) {
          var i2 = t2._geometries[n2], r2 = i2.getGeometryType();
          e3.push(pi2[r2].call(this, i2));
        }
        return { type: "GeometryCollection", geometries: e3 };
      }, "GeometryCollection") };
      return { BufferOp: vi, GeoJSONReader: (function() {
        return s(/* @__PURE__ */ __name((function t2(e3) {
          n(this, t2), this.parser = new di(e3 || new ae());
        }), "t"), [{ key: "read", value: /* @__PURE__ */ __name(function(t2) {
          return this.parser.read(t2);
        }, "value") }]);
      })(), GeoJSONWriter: (function() {
        return s(/* @__PURE__ */ __name((function t2() {
          n(this, t2), this.parser = new di(this.geometryFactory);
        }), "t"), [{ key: "write", value: /* @__PURE__ */ __name(function(t2) {
          return this.parser.write(t2);
        }, "value") }]);
      })() };
    }));
  }
});

// worker/node_modules/d3-geo/src/adder.js
function adder_default() {
  return new Adder();
}
function Adder() {
  this.reset();
}
function add(adder, a, b) {
  var x = adder.s = a + b, bv = x - a, av = x - bv;
  adder.t = a - av + (b - bv);
}
var temp;
var init_adder = __esm({
  "worker/node_modules/d3-geo/src/adder.js"() {
    __name(adder_default, "default");
    __name(Adder, "Adder");
    Adder.prototype = {
      constructor: Adder,
      reset: /* @__PURE__ */ __name(function() {
        this.s = // rounded value
        this.t = 0;
      }, "reset"),
      add: /* @__PURE__ */ __name(function(y) {
        add(temp, y, this.t);
        add(this, temp.s, this.s);
        if (this.s) this.t += temp.t;
        else this.s = temp.t;
      }, "add"),
      valueOf: /* @__PURE__ */ __name(function() {
        return this.s;
      }, "valueOf")
    };
    temp = new Adder();
    __name(add, "add");
  }
});

// worker/node_modules/d3-geo/src/math.js
function acos(x) {
  return x > 1 ? 0 : x < -1 ? pi : Math.acos(x);
}
function asin(x) {
  return x > 1 ? halfPi : x < -1 ? -halfPi : Math.asin(x);
}
var epsilon, pi, halfPi, quarterPi, tau, degrees, radians, abs, atan, atan2, cos, exp, log2, sin, sqrt, tan;
var init_math = __esm({
  "worker/node_modules/d3-geo/src/math.js"() {
    epsilon = 1e-6;
    pi = Math.PI;
    halfPi = pi / 2;
    quarterPi = pi / 4;
    tau = pi * 2;
    degrees = 180 / pi;
    radians = pi / 180;
    abs = Math.abs;
    atan = Math.atan;
    atan2 = Math.atan2;
    cos = Math.cos;
    exp = Math.exp;
    log2 = Math.log;
    sin = Math.sin;
    sqrt = Math.sqrt;
    tan = Math.tan;
    __name(acos, "acos");
    __name(asin, "asin");
  }
});

// worker/node_modules/d3-geo/src/noop.js
function noop() {
}
var init_noop = __esm({
  "worker/node_modules/d3-geo/src/noop.js"() {
    __name(noop, "noop");
  }
});

// worker/node_modules/d3-geo/src/stream.js
function streamGeometry(geometry, stream) {
  if (geometry && streamGeometryType.hasOwnProperty(geometry.type)) {
    streamGeometryType[geometry.type](geometry, stream);
  }
}
function streamLine(coordinates, stream, closed) {
  var i = -1, n = coordinates.length - closed, coordinate;
  stream.lineStart();
  while (++i < n) coordinate = coordinates[i], stream.point(coordinate[0], coordinate[1], coordinate[2]);
  stream.lineEnd();
}
function streamPolygon(coordinates, stream) {
  var i = -1, n = coordinates.length;
  stream.polygonStart();
  while (++i < n) streamLine(coordinates[i], stream, 1);
  stream.polygonEnd();
}
function stream_default(object2, stream) {
  if (object2 && streamObjectType.hasOwnProperty(object2.type)) {
    streamObjectType[object2.type](object2, stream);
  } else {
    streamGeometry(object2, stream);
  }
}
var streamObjectType, streamGeometryType;
var init_stream4 = __esm({
  "worker/node_modules/d3-geo/src/stream.js"() {
    __name(streamGeometry, "streamGeometry");
    streamObjectType = {
      Feature: /* @__PURE__ */ __name(function(object2, stream) {
        streamGeometry(object2.geometry, stream);
      }, "Feature"),
      FeatureCollection: /* @__PURE__ */ __name(function(object2, stream) {
        var features = object2.features, i = -1, n = features.length;
        while (++i < n) streamGeometry(features[i].geometry, stream);
      }, "FeatureCollection")
    };
    streamGeometryType = {
      Sphere: /* @__PURE__ */ __name(function(object2, stream) {
        stream.sphere();
      }, "Sphere"),
      Point: /* @__PURE__ */ __name(function(object2, stream) {
        object2 = object2.coordinates;
        stream.point(object2[0], object2[1], object2[2]);
      }, "Point"),
      MultiPoint: /* @__PURE__ */ __name(function(object2, stream) {
        var coordinates = object2.coordinates, i = -1, n = coordinates.length;
        while (++i < n) object2 = coordinates[i], stream.point(object2[0], object2[1], object2[2]);
      }, "MultiPoint"),
      LineString: /* @__PURE__ */ __name(function(object2, stream) {
        streamLine(object2.coordinates, stream, 0);
      }, "LineString"),
      MultiLineString: /* @__PURE__ */ __name(function(object2, stream) {
        var coordinates = object2.coordinates, i = -1, n = coordinates.length;
        while (++i < n) streamLine(coordinates[i], stream, 0);
      }, "MultiLineString"),
      Polygon: /* @__PURE__ */ __name(function(object2, stream) {
        streamPolygon(object2.coordinates, stream);
      }, "Polygon"),
      MultiPolygon: /* @__PURE__ */ __name(function(object2, stream) {
        var coordinates = object2.coordinates, i = -1, n = coordinates.length;
        while (++i < n) streamPolygon(coordinates[i], stream);
      }, "MultiPolygon"),
      GeometryCollection: /* @__PURE__ */ __name(function(object2, stream) {
        var geometries = object2.geometries, i = -1, n = geometries.length;
        while (++i < n) streamGeometry(geometries[i], stream);
      }, "GeometryCollection")
    };
    __name(streamLine, "streamLine");
    __name(streamPolygon, "streamPolygon");
    __name(stream_default, "default");
  }
});

// worker/node_modules/d3-geo/src/area.js
var areaRingSum, areaSum;
var init_area = __esm({
  "worker/node_modules/d3-geo/src/area.js"() {
    init_adder();
    init_math();
    init_noop();
    init_stream4();
    areaRingSum = adder_default();
    areaSum = adder_default();
  }
});

// worker/node_modules/d3-geo/src/cartesian.js
function spherical(cartesian2) {
  return [atan2(cartesian2[1], cartesian2[0]), asin(cartesian2[2])];
}
function cartesian(spherical2) {
  var lambda = spherical2[0], phi = spherical2[1], cosPhi = cos(phi);
  return [cosPhi * cos(lambda), cosPhi * sin(lambda), sin(phi)];
}
function cartesianDot(a, b) {
  return a[0] * b[0] + a[1] * b[1] + a[2] * b[2];
}
function cartesianCross(a, b) {
  return [a[1] * b[2] - a[2] * b[1], a[2] * b[0] - a[0] * b[2], a[0] * b[1] - a[1] * b[0]];
}
function cartesianAddInPlace(a, b) {
  a[0] += b[0], a[1] += b[1], a[2] += b[2];
}
function cartesianScale(vector, k) {
  return [vector[0] * k, vector[1] * k, vector[2] * k];
}
function cartesianNormalizeInPlace(d) {
  var l = sqrt(d[0] * d[0] + d[1] * d[1] + d[2] * d[2]);
  d[0] /= l, d[1] /= l, d[2] /= l;
}
var init_cartesian = __esm({
  "worker/node_modules/d3-geo/src/cartesian.js"() {
    init_math();
    __name(spherical, "spherical");
    __name(cartesian, "cartesian");
    __name(cartesianDot, "cartesianDot");
    __name(cartesianCross, "cartesianCross");
    __name(cartesianAddInPlace, "cartesianAddInPlace");
    __name(cartesianScale, "cartesianScale");
    __name(cartesianNormalizeInPlace, "cartesianNormalizeInPlace");
  }
});

// worker/node_modules/d3-geo/src/bounds.js
var deltaSum;
var init_bounds = __esm({
  "worker/node_modules/d3-geo/src/bounds.js"() {
    init_adder();
    init_area();
    init_cartesian();
    init_math();
    init_stream4();
    deltaSum = adder_default();
  }
});

// worker/node_modules/d3-geo/src/centroid.js
var init_centroid = __esm({
  "worker/node_modules/d3-geo/src/centroid.js"() {
    init_math();
    init_noop();
    init_stream4();
  }
});

// worker/node_modules/d3-geo/src/constant.js
var init_constant = __esm({
  "worker/node_modules/d3-geo/src/constant.js"() {
  }
});

// worker/node_modules/d3-geo/src/compose.js
function compose_default(a, b) {
  function compose2(x, y) {
    return x = a(x, y), b(x[0], x[1]);
  }
  __name(compose2, "compose");
  if (a.invert && b.invert) compose2.invert = function(x, y) {
    return x = b.invert(x, y), x && a.invert(x[0], x[1]);
  };
  return compose2;
}
var init_compose = __esm({
  "worker/node_modules/d3-geo/src/compose.js"() {
    __name(compose_default, "default");
  }
});

// worker/node_modules/d3-geo/src/rotation.js
function rotationIdentity(lambda, phi) {
  return [lambda > pi ? lambda - tau : lambda < -pi ? lambda + tau : lambda, phi];
}
function rotateRadians(deltaLambda, deltaPhi, deltaGamma) {
  return (deltaLambda %= tau) ? deltaPhi || deltaGamma ? compose_default(rotationLambda(deltaLambda), rotationPhiGamma(deltaPhi, deltaGamma)) : rotationLambda(deltaLambda) : deltaPhi || deltaGamma ? rotationPhiGamma(deltaPhi, deltaGamma) : rotationIdentity;
}
function forwardRotationLambda(deltaLambda) {
  return function(lambda, phi) {
    return lambda += deltaLambda, [lambda > pi ? lambda - tau : lambda < -pi ? lambda + tau : lambda, phi];
  };
}
function rotationLambda(deltaLambda) {
  var rotation = forwardRotationLambda(deltaLambda);
  rotation.invert = forwardRotationLambda(-deltaLambda);
  return rotation;
}
function rotationPhiGamma(deltaPhi, deltaGamma) {
  var cosDeltaPhi = cos(deltaPhi), sinDeltaPhi = sin(deltaPhi), cosDeltaGamma = cos(deltaGamma), sinDeltaGamma = sin(deltaGamma);
  function rotation(lambda, phi) {
    var cosPhi = cos(phi), x = cos(lambda) * cosPhi, y = sin(lambda) * cosPhi, z2 = sin(phi), k = z2 * cosDeltaPhi + x * sinDeltaPhi;
    return [
      atan2(y * cosDeltaGamma - k * sinDeltaGamma, x * cosDeltaPhi - z2 * sinDeltaPhi),
      asin(k * cosDeltaGamma + y * sinDeltaGamma)
    ];
  }
  __name(rotation, "rotation");
  rotation.invert = function(lambda, phi) {
    var cosPhi = cos(phi), x = cos(lambda) * cosPhi, y = sin(lambda) * cosPhi, z2 = sin(phi), k = z2 * cosDeltaGamma - y * sinDeltaGamma;
    return [
      atan2(y * cosDeltaGamma + z2 * sinDeltaGamma, x * cosDeltaPhi + k * sinDeltaPhi),
      asin(k * cosDeltaPhi - x * sinDeltaPhi)
    ];
  };
  return rotation;
}
var init_rotation = __esm({
  "worker/node_modules/d3-geo/src/rotation.js"() {
    init_compose();
    init_math();
    __name(rotationIdentity, "rotationIdentity");
    rotationIdentity.invert = rotationIdentity;
    __name(rotateRadians, "rotateRadians");
    __name(forwardRotationLambda, "forwardRotationLambda");
    __name(rotationLambda, "rotationLambda");
    __name(rotationPhiGamma, "rotationPhiGamma");
  }
});

// worker/node_modules/d3-geo/src/circle.js
function circleStream(stream, radius, delta, direction, t0, t1) {
  if (!delta) return;
  var cosRadius = cos(radius), sinRadius = sin(radius), step = direction * delta;
  if (t0 == null) {
    t0 = radius + direction * tau;
    t1 = radius - step / 2;
  } else {
    t0 = circleRadius(cosRadius, t0);
    t1 = circleRadius(cosRadius, t1);
    if (direction > 0 ? t0 < t1 : t0 > t1) t0 += direction * tau;
  }
  for (var point2, t = t0; direction > 0 ? t > t1 : t < t1; t -= step) {
    point2 = spherical([cosRadius, -sinRadius * cos(t), -sinRadius * sin(t)]);
    stream.point(point2[0], point2[1]);
  }
}
function circleRadius(cosRadius, point2) {
  point2 = cartesian(point2), point2[0] -= cosRadius;
  cartesianNormalizeInPlace(point2);
  var radius = acos(-point2[1]);
  return ((-point2[2] < 0 ? -radius : radius) + tau - epsilon) % tau;
}
var init_circle = __esm({
  "worker/node_modules/d3-geo/src/circle.js"() {
    init_cartesian();
    init_constant();
    init_math();
    init_rotation();
    __name(circleStream, "circleStream");
    __name(circleRadius, "circleRadius");
  }
});

// worker/node_modules/d3-geo/src/clip/buffer.js
function buffer_default() {
  var lines = [], line;
  return {
    point: /* @__PURE__ */ __name(function(x, y) {
      line.push([x, y]);
    }, "point"),
    lineStart: /* @__PURE__ */ __name(function() {
      lines.push(line = []);
    }, "lineStart"),
    lineEnd: noop,
    rejoin: /* @__PURE__ */ __name(function() {
      if (lines.length > 1) lines.push(lines.pop().concat(lines.shift()));
    }, "rejoin"),
    result: /* @__PURE__ */ __name(function() {
      var result = lines;
      lines = [];
      line = null;
      return result;
    }, "result")
  };
}
var init_buffer = __esm({
  "worker/node_modules/d3-geo/src/clip/buffer.js"() {
    init_noop();
    __name(buffer_default, "default");
  }
});

// worker/node_modules/d3-geo/src/clip/line.js
function line_default(a, b, x02, y02, x12, y12) {
  var ax = a[0], ay = a[1], bx = b[0], by = b[1], t0 = 0, t1 = 1, dx = bx - ax, dy = by - ay, r;
  r = x02 - ax;
  if (!dx && r > 0) return;
  r /= dx;
  if (dx < 0) {
    if (r < t0) return;
    if (r < t1) t1 = r;
  } else if (dx > 0) {
    if (r > t1) return;
    if (r > t0) t0 = r;
  }
  r = x12 - ax;
  if (!dx && r < 0) return;
  r /= dx;
  if (dx < 0) {
    if (r > t1) return;
    if (r > t0) t0 = r;
  } else if (dx > 0) {
    if (r < t0) return;
    if (r < t1) t1 = r;
  }
  r = y02 - ay;
  if (!dy && r > 0) return;
  r /= dy;
  if (dy < 0) {
    if (r < t0) return;
    if (r < t1) t1 = r;
  } else if (dy > 0) {
    if (r > t1) return;
    if (r > t0) t0 = r;
  }
  r = y12 - ay;
  if (!dy && r < 0) return;
  r /= dy;
  if (dy < 0) {
    if (r > t1) return;
    if (r > t0) t0 = r;
  } else if (dy > 0) {
    if (r < t0) return;
    if (r < t1) t1 = r;
  }
  if (t0 > 0) a[0] = ax + t0 * dx, a[1] = ay + t0 * dy;
  if (t1 < 1) b[0] = ax + t1 * dx, b[1] = ay + t1 * dy;
  return true;
}
var init_line = __esm({
  "worker/node_modules/d3-geo/src/clip/line.js"() {
    __name(line_default, "default");
  }
});

// worker/node_modules/d3-geo/src/pointEqual.js
function pointEqual_default(a, b) {
  return abs(a[0] - b[0]) < epsilon && abs(a[1] - b[1]) < epsilon;
}
var init_pointEqual = __esm({
  "worker/node_modules/d3-geo/src/pointEqual.js"() {
    init_math();
    __name(pointEqual_default, "default");
  }
});

// worker/node_modules/d3-geo/src/clip/polygon.js
function Intersection(point2, points, other, entry) {
  this.x = point2;
  this.z = points;
  this.o = other;
  this.e = entry;
  this.v = false;
  this.n = this.p = null;
}
function polygon_default(segments, compareIntersection2, startInside, interpolate, stream) {
  var subject = [], clip = [], i, n;
  segments.forEach(function(segment) {
    if ((n2 = segment.length - 1) <= 0) return;
    var n2, p0 = segment[0], p1 = segment[n2], x;
    if (pointEqual_default(p0, p1)) {
      stream.lineStart();
      for (i = 0; i < n2; ++i) stream.point((p0 = segment[i])[0], p0[1]);
      stream.lineEnd();
      return;
    }
    subject.push(x = new Intersection(p0, segment, null, true));
    clip.push(x.o = new Intersection(p0, null, x, false));
    subject.push(x = new Intersection(p1, segment, null, false));
    clip.push(x.o = new Intersection(p1, null, x, true));
  });
  if (!subject.length) return;
  clip.sort(compareIntersection2);
  link(subject);
  link(clip);
  for (i = 0, n = clip.length; i < n; ++i) {
    clip[i].e = startInside = !startInside;
  }
  var start = subject[0], points, point2;
  while (1) {
    var current = start, isSubject = true;
    while (current.v) if ((current = current.n) === start) return;
    points = current.z;
    stream.lineStart();
    do {
      current.v = current.o.v = true;
      if (current.e) {
        if (isSubject) {
          for (i = 0, n = points.length; i < n; ++i) stream.point((point2 = points[i])[0], point2[1]);
        } else {
          interpolate(current.x, current.n.x, 1, stream);
        }
        current = current.n;
      } else {
        if (isSubject) {
          points = current.p.z;
          for (i = points.length - 1; i >= 0; --i) stream.point((point2 = points[i])[0], point2[1]);
        } else {
          interpolate(current.x, current.p.x, -1, stream);
        }
        current = current.p;
      }
      current = current.o;
      points = current.z;
      isSubject = !isSubject;
    } while (!current.v);
    stream.lineEnd();
  }
}
function link(array3) {
  if (!(n = array3.length)) return;
  var n, i = 0, a = array3[0], b;
  while (++i < n) {
    a.n = b = array3[i];
    b.p = a;
    a = b;
  }
  a.n = b = array3[0];
  b.p = a;
}
var init_polygon = __esm({
  "worker/node_modules/d3-geo/src/clip/polygon.js"() {
    init_pointEqual();
    __name(Intersection, "Intersection");
    __name(polygon_default, "default");
    __name(link, "link");
  }
});

// worker/node_modules/d3-array/src/ascending.js
function ascending_default(a, b) {
  return a < b ? -1 : a > b ? 1 : a >= b ? 0 : NaN;
}
var init_ascending = __esm({
  "worker/node_modules/d3-array/src/ascending.js"() {
    __name(ascending_default, "default");
  }
});

// worker/node_modules/d3-array/src/bisector.js
function bisector_default(compare2) {
  if (compare2.length === 1) compare2 = ascendingComparator(compare2);
  return {
    left: /* @__PURE__ */ __name(function(a, x, lo, hi) {
      if (lo == null) lo = 0;
      if (hi == null) hi = a.length;
      while (lo < hi) {
        var mid = lo + hi >>> 1;
        if (compare2(a[mid], x) < 0) lo = mid + 1;
        else hi = mid;
      }
      return lo;
    }, "left"),
    right: /* @__PURE__ */ __name(function(a, x, lo, hi) {
      if (lo == null) lo = 0;
      if (hi == null) hi = a.length;
      while (lo < hi) {
        var mid = lo + hi >>> 1;
        if (compare2(a[mid], x) > 0) hi = mid;
        else lo = mid + 1;
      }
      return lo;
    }, "right")
  };
}
function ascendingComparator(f) {
  return function(d, x) {
    return ascending_default(f(d), x);
  };
}
var init_bisector = __esm({
  "worker/node_modules/d3-array/src/bisector.js"() {
    init_ascending();
    __name(bisector_default, "default");
    __name(ascendingComparator, "ascendingComparator");
  }
});

// worker/node_modules/d3-array/src/bisect.js
var ascendingBisect, bisectRight, bisectLeft;
var init_bisect = __esm({
  "worker/node_modules/d3-array/src/bisect.js"() {
    init_ascending();
    init_bisector();
    ascendingBisect = bisector_default(ascending_default);
    bisectRight = ascendingBisect.right;
    bisectLeft = ascendingBisect.left;
  }
});

// worker/node_modules/d3-array/src/pairs.js
var init_pairs = __esm({
  "worker/node_modules/d3-array/src/pairs.js"() {
  }
});

// worker/node_modules/d3-array/src/cross.js
var init_cross = __esm({
  "worker/node_modules/d3-array/src/cross.js"() {
    init_pairs();
  }
});

// worker/node_modules/d3-array/src/descending.js
var init_descending = __esm({
  "worker/node_modules/d3-array/src/descending.js"() {
  }
});

// worker/node_modules/d3-array/src/number.js
var init_number = __esm({
  "worker/node_modules/d3-array/src/number.js"() {
  }
});

// worker/node_modules/d3-array/src/variance.js
var init_variance = __esm({
  "worker/node_modules/d3-array/src/variance.js"() {
    init_number();
  }
});

// worker/node_modules/d3-array/src/deviation.js
var init_deviation = __esm({
  "worker/node_modules/d3-array/src/deviation.js"() {
    init_variance();
  }
});

// worker/node_modules/d3-array/src/extent.js
var init_extent = __esm({
  "worker/node_modules/d3-array/src/extent.js"() {
  }
});

// worker/node_modules/d3-array/src/array.js
var array2, slice, map;
var init_array3 = __esm({
  "worker/node_modules/d3-array/src/array.js"() {
    array2 = Array.prototype;
    slice = array2.slice;
    map = array2.map;
  }
});

// worker/node_modules/d3-array/src/constant.js
var init_constant2 = __esm({
  "worker/node_modules/d3-array/src/constant.js"() {
  }
});

// worker/node_modules/d3-array/src/identity.js
var init_identity = __esm({
  "worker/node_modules/d3-array/src/identity.js"() {
  }
});

// worker/node_modules/d3-array/src/range.js
var init_range = __esm({
  "worker/node_modules/d3-array/src/range.js"() {
  }
});

// worker/node_modules/d3-array/src/ticks.js
var e10, e5, e2;
var init_ticks = __esm({
  "worker/node_modules/d3-array/src/ticks.js"() {
    e10 = Math.sqrt(50);
    e5 = Math.sqrt(10);
    e2 = Math.sqrt(2);
  }
});

// worker/node_modules/d3-array/src/threshold/sturges.js
var init_sturges = __esm({
  "worker/node_modules/d3-array/src/threshold/sturges.js"() {
  }
});

// worker/node_modules/d3-array/src/histogram.js
var init_histogram = __esm({
  "worker/node_modules/d3-array/src/histogram.js"() {
    init_array3();
    init_bisect();
    init_constant2();
    init_extent();
    init_identity();
    init_range();
    init_ticks();
    init_sturges();
  }
});

// worker/node_modules/d3-array/src/quantile.js
var init_quantile = __esm({
  "worker/node_modules/d3-array/src/quantile.js"() {
    init_number();
  }
});

// worker/node_modules/d3-array/src/threshold/freedmanDiaconis.js
var init_freedmanDiaconis = __esm({
  "worker/node_modules/d3-array/src/threshold/freedmanDiaconis.js"() {
    init_array3();
    init_ascending();
    init_number();
    init_quantile();
  }
});

// worker/node_modules/d3-array/src/threshold/scott.js
var init_scott = __esm({
  "worker/node_modules/d3-array/src/threshold/scott.js"() {
    init_deviation();
  }
});

// worker/node_modules/d3-array/src/max.js
var init_max = __esm({
  "worker/node_modules/d3-array/src/max.js"() {
  }
});

// worker/node_modules/d3-array/src/mean.js
var init_mean = __esm({
  "worker/node_modules/d3-array/src/mean.js"() {
    init_number();
  }
});

// worker/node_modules/d3-array/src/median.js
var init_median = __esm({
  "worker/node_modules/d3-array/src/median.js"() {
    init_ascending();
    init_number();
    init_quantile();
  }
});

// worker/node_modules/d3-array/src/merge.js
function merge_default(arrays) {
  var n = arrays.length, m, i = -1, j = 0, merged, array3;
  while (++i < n) j += arrays[i].length;
  merged = new Array(j);
  while (--n >= 0) {
    array3 = arrays[n];
    m = array3.length;
    while (--m >= 0) {
      merged[--j] = array3[m];
    }
  }
  return merged;
}
var init_merge = __esm({
  "worker/node_modules/d3-array/src/merge.js"() {
    __name(merge_default, "default");
  }
});

// worker/node_modules/d3-array/src/min.js
var init_min = __esm({
  "worker/node_modules/d3-array/src/min.js"() {
  }
});

// worker/node_modules/d3-array/src/permute.js
var init_permute = __esm({
  "worker/node_modules/d3-array/src/permute.js"() {
  }
});

// worker/node_modules/d3-array/src/scan.js
var init_scan = __esm({
  "worker/node_modules/d3-array/src/scan.js"() {
    init_ascending();
  }
});

// worker/node_modules/d3-array/src/shuffle.js
var init_shuffle = __esm({
  "worker/node_modules/d3-array/src/shuffle.js"() {
  }
});

// worker/node_modules/d3-array/src/sum.js
var init_sum = __esm({
  "worker/node_modules/d3-array/src/sum.js"() {
  }
});

// worker/node_modules/d3-array/src/transpose.js
var init_transpose = __esm({
  "worker/node_modules/d3-array/src/transpose.js"() {
    init_min();
  }
});

// worker/node_modules/d3-array/src/zip.js
var init_zip = __esm({
  "worker/node_modules/d3-array/src/zip.js"() {
    init_transpose();
  }
});

// worker/node_modules/d3-array/src/index.js
var init_src = __esm({
  "worker/node_modules/d3-array/src/index.js"() {
    init_bisect();
    init_ascending();
    init_bisector();
    init_cross();
    init_descending();
    init_deviation();
    init_extent();
    init_histogram();
    init_freedmanDiaconis();
    init_scott();
    init_sturges();
    init_max();
    init_mean();
    init_median();
    init_merge();
    init_min();
    init_pairs();
    init_permute();
    init_quantile();
    init_range();
    init_scan();
    init_shuffle();
    init_sum();
    init_ticks();
    init_transpose();
    init_variance();
    init_zip();
  }
});

// worker/node_modules/d3-geo/src/clip/extent.js
function clipExtent(x02, y02, x12, y12) {
  function visible(x, y) {
    return x02 <= x && x <= x12 && y02 <= y && y <= y12;
  }
  __name(visible, "visible");
  function interpolate(from, to, direction, stream) {
    var a = 0, a1 = 0;
    if (from == null || (a = corner(from, direction)) !== (a1 = corner(to, direction)) || comparePoint(from, to) < 0 ^ direction > 0) {
      do
        stream.point(a === 0 || a === 3 ? x02 : x12, a > 1 ? y12 : y02);
      while ((a = (a + direction + 4) % 4) !== a1);
    } else {
      stream.point(to[0], to[1]);
    }
  }
  __name(interpolate, "interpolate");
  function corner(p, direction) {
    return abs(p[0] - x02) < epsilon ? direction > 0 ? 0 : 3 : abs(p[0] - x12) < epsilon ? direction > 0 ? 2 : 1 : abs(p[1] - y02) < epsilon ? direction > 0 ? 1 : 0 : direction > 0 ? 3 : 2;
  }
  __name(corner, "corner");
  function compareIntersection2(a, b) {
    return comparePoint(a.x, b.x);
  }
  __name(compareIntersection2, "compareIntersection");
  function comparePoint(a, b) {
    var ca = corner(a, 1), cb = corner(b, 1);
    return ca !== cb ? ca - cb : ca === 0 ? b[1] - a[1] : ca === 1 ? a[0] - b[0] : ca === 2 ? a[1] - b[1] : b[0] - a[0];
  }
  __name(comparePoint, "comparePoint");
  return function(stream) {
    var activeStream = stream, bufferStream = buffer_default(), segments, polygon2, ring, x__, y__, v__, x_, y_, v_, first, clean;
    var clipStream = {
      point: point2,
      lineStart,
      lineEnd,
      polygonStart,
      polygonEnd
    };
    function point2(x, y) {
      if (visible(x, y)) activeStream.point(x, y);
    }
    __name(point2, "point");
    function polygonInside() {
      var winding = 0;
      for (var i = 0, n = polygon2.length; i < n; ++i) {
        for (var ring2 = polygon2[i], j = 1, m = ring2.length, point3 = ring2[0], a0, a1, b0 = point3[0], b1 = point3[1]; j < m; ++j) {
          a0 = b0, a1 = b1, point3 = ring2[j], b0 = point3[0], b1 = point3[1];
          if (a1 <= y12) {
            if (b1 > y12 && (b0 - a0) * (y12 - a1) > (b1 - a1) * (x02 - a0)) ++winding;
          } else {
            if (b1 <= y12 && (b0 - a0) * (y12 - a1) < (b1 - a1) * (x02 - a0)) --winding;
          }
        }
      }
      return winding;
    }
    __name(polygonInside, "polygonInside");
    function polygonStart() {
      activeStream = bufferStream, segments = [], polygon2 = [], clean = true;
    }
    __name(polygonStart, "polygonStart");
    function polygonEnd() {
      var startInside = polygonInside(), cleanInside = clean && startInside, visible2 = (segments = merge_default(segments)).length;
      if (cleanInside || visible2) {
        stream.polygonStart();
        if (cleanInside) {
          stream.lineStart();
          interpolate(null, null, 1, stream);
          stream.lineEnd();
        }
        if (visible2) {
          polygon_default(segments, compareIntersection2, startInside, interpolate, stream);
        }
        stream.polygonEnd();
      }
      activeStream = stream, segments = polygon2 = ring = null;
    }
    __name(polygonEnd, "polygonEnd");
    function lineStart() {
      clipStream.point = linePoint;
      if (polygon2) polygon2.push(ring = []);
      first = true;
      v_ = false;
      x_ = y_ = NaN;
    }
    __name(lineStart, "lineStart");
    function lineEnd() {
      if (segments) {
        linePoint(x__, y__);
        if (v__ && v_) bufferStream.rejoin();
        segments.push(bufferStream.result());
      }
      clipStream.point = point2;
      if (v_) activeStream.lineEnd();
    }
    __name(lineEnd, "lineEnd");
    function linePoint(x, y) {
      var v = visible(x, y);
      if (polygon2) ring.push([x, y]);
      if (first) {
        x__ = x, y__ = y, v__ = v;
        first = false;
        if (v) {
          activeStream.lineStart();
          activeStream.point(x, y);
        }
      } else {
        if (v && v_) activeStream.point(x, y);
        else {
          var a = [x_ = Math.max(clipMin, Math.min(clipMax, x_)), y_ = Math.max(clipMin, Math.min(clipMax, y_))], b = [x = Math.max(clipMin, Math.min(clipMax, x)), y = Math.max(clipMin, Math.min(clipMax, y))];
          if (line_default(a, b, x02, y02, x12, y12)) {
            if (!v_) {
              activeStream.lineStart();
              activeStream.point(a[0], a[1]);
            }
            activeStream.point(b[0], b[1]);
            if (!v) activeStream.lineEnd();
            clean = false;
          } else if (v) {
            activeStream.lineStart();
            activeStream.point(x, y);
            clean = false;
          }
        }
      }
      x_ = x, y_ = y, v_ = v;
    }
    __name(linePoint, "linePoint");
    return clipStream;
  };
}
var clipMax, clipMin;
var init_extent2 = __esm({
  "worker/node_modules/d3-geo/src/clip/extent.js"() {
    init_math();
    init_buffer();
    init_line();
    init_polygon();
    init_src();
    clipMax = 1e9;
    clipMin = -clipMax;
    __name(clipExtent, "clipExtent");
  }
});

// worker/node_modules/d3-geo/src/polygonContains.js
function polygonContains_default(polygon2, point2) {
  var lambda = point2[0], phi = point2[1], normal = [sin(lambda), -cos(lambda), 0], angle = 0, winding = 0;
  sum.reset();
  for (var i = 0, n = polygon2.length; i < n; ++i) {
    if (!(m = (ring = polygon2[i]).length)) continue;
    var ring, m, point0 = ring[m - 1], lambda0 = point0[0], phi0 = point0[1] / 2 + quarterPi, sinPhi0 = sin(phi0), cosPhi0 = cos(phi0);
    for (var j = 0; j < m; ++j, lambda0 = lambda1, sinPhi0 = sinPhi1, cosPhi0 = cosPhi1, point0 = point1) {
      var point1 = ring[j], lambda1 = point1[0], phi1 = point1[1] / 2 + quarterPi, sinPhi1 = sin(phi1), cosPhi1 = cos(phi1), delta = lambda1 - lambda0, sign2 = delta >= 0 ? 1 : -1, absDelta = sign2 * delta, antimeridian = absDelta > pi, k = sinPhi0 * sinPhi1;
      sum.add(atan2(k * sign2 * sin(absDelta), cosPhi0 * cosPhi1 + k * cos(absDelta)));
      angle += antimeridian ? delta + sign2 * tau : delta;
      if (antimeridian ^ lambda0 >= lambda ^ lambda1 >= lambda) {
        var arc = cartesianCross(cartesian(point0), cartesian(point1));
        cartesianNormalizeInPlace(arc);
        var intersection3 = cartesianCross(normal, arc);
        cartesianNormalizeInPlace(intersection3);
        var phiArc = (antimeridian ^ delta >= 0 ? -1 : 1) * asin(intersection3[2]);
        if (phi > phiArc || phi === phiArc && (arc[0] || arc[1])) {
          winding += antimeridian ^ delta >= 0 ? 1 : -1;
        }
      }
    }
  }
  return (angle < -epsilon || angle < epsilon && sum < -epsilon) ^ winding & 1;
}
var sum;
var init_polygonContains = __esm({
  "worker/node_modules/d3-geo/src/polygonContains.js"() {
    init_adder();
    init_cartesian();
    init_math();
    sum = adder_default();
    __name(polygonContains_default, "default");
  }
});

// worker/node_modules/d3-geo/src/length.js
var lengthSum;
var init_length = __esm({
  "worker/node_modules/d3-geo/src/length.js"() {
    init_adder();
    init_math();
    init_noop();
    init_stream4();
    lengthSum = adder_default();
  }
});

// worker/node_modules/d3-geo/src/distance.js
var init_distance = __esm({
  "worker/node_modules/d3-geo/src/distance.js"() {
    init_length();
  }
});

// worker/node_modules/d3-geo/src/contains.js
var init_contains = __esm({
  "worker/node_modules/d3-geo/src/contains.js"() {
    init_polygonContains();
    init_distance();
    init_math();
  }
});

// worker/node_modules/d3-geo/src/graticule.js
var init_graticule = __esm({
  "worker/node_modules/d3-geo/src/graticule.js"() {
    init_src();
    init_math();
  }
});

// worker/node_modules/d3-geo/src/interpolate.js
var init_interpolate = __esm({
  "worker/node_modules/d3-geo/src/interpolate.js"() {
    init_math();
  }
});

// worker/node_modules/d3-geo/src/identity.js
function identity_default2(x) {
  return x;
}
var init_identity2 = __esm({
  "worker/node_modules/d3-geo/src/identity.js"() {
    __name(identity_default2, "default");
  }
});

// worker/node_modules/d3-geo/src/path/area.js
var areaSum2, areaRingSum2;
var init_area2 = __esm({
  "worker/node_modules/d3-geo/src/path/area.js"() {
    init_adder();
    init_math();
    init_noop();
    areaSum2 = adder_default();
    areaRingSum2 = adder_default();
  }
});

// worker/node_modules/d3-geo/src/path/bounds.js
function boundsPoint(x, y) {
  if (x < x0) x0 = x;
  if (x > x1) x1 = x;
  if (y < y0) y0 = y;
  if (y > y1) y1 = y;
}
var x0, y0, x1, y1, boundsStream, bounds_default;
var init_bounds2 = __esm({
  "worker/node_modules/d3-geo/src/path/bounds.js"() {
    init_noop();
    x0 = Infinity;
    y0 = x0;
    x1 = -x0;
    y1 = x1;
    boundsStream = {
      point: boundsPoint,
      lineStart: noop,
      lineEnd: noop,
      polygonStart: noop,
      polygonEnd: noop,
      result: /* @__PURE__ */ __name(function() {
        var bounds = [[x0, y0], [x1, y1]];
        x1 = y1 = -(y0 = x0 = Infinity);
        return bounds;
      }, "result")
    };
    __name(boundsPoint, "boundsPoint");
    bounds_default = boundsStream;
  }
});

// worker/node_modules/d3-geo/src/path/centroid.js
var init_centroid2 = __esm({
  "worker/node_modules/d3-geo/src/path/centroid.js"() {
    init_math();
  }
});

// worker/node_modules/d3-geo/src/path/context.js
function PathContext(context) {
  this._context = context;
}
var init_context = __esm({
  "worker/node_modules/d3-geo/src/path/context.js"() {
    init_math();
    init_noop();
    __name(PathContext, "PathContext");
    PathContext.prototype = {
      _radius: 4.5,
      pointRadius: /* @__PURE__ */ __name(function(_) {
        return this._radius = _, this;
      }, "pointRadius"),
      polygonStart: /* @__PURE__ */ __name(function() {
        this._line = 0;
      }, "polygonStart"),
      polygonEnd: /* @__PURE__ */ __name(function() {
        this._line = NaN;
      }, "polygonEnd"),
      lineStart: /* @__PURE__ */ __name(function() {
        this._point = 0;
      }, "lineStart"),
      lineEnd: /* @__PURE__ */ __name(function() {
        if (this._line === 0) this._context.closePath();
        this._point = NaN;
      }, "lineEnd"),
      point: /* @__PURE__ */ __name(function(x, y) {
        switch (this._point) {
          case 0: {
            this._context.moveTo(x, y);
            this._point = 1;
            break;
          }
          case 1: {
            this._context.lineTo(x, y);
            break;
          }
          default: {
            this._context.moveTo(x + this._radius, y);
            this._context.arc(x, y, this._radius, 0, tau);
            break;
          }
        }
      }, "point"),
      result: noop
    };
  }
});

// worker/node_modules/d3-geo/src/path/measure.js
var lengthSum2;
var init_measure = __esm({
  "worker/node_modules/d3-geo/src/path/measure.js"() {
    init_adder();
    init_math();
    init_noop();
    lengthSum2 = adder_default();
  }
});

// worker/node_modules/d3-geo/src/path/string.js
function PathString() {
  this._string = [];
}
function circle(radius) {
  return "m0," + radius + "a" + radius + "," + radius + " 0 1,1 0," + -2 * radius + "a" + radius + "," + radius + " 0 1,1 0," + 2 * radius + "z";
}
var init_string = __esm({
  "worker/node_modules/d3-geo/src/path/string.js"() {
    __name(PathString, "PathString");
    PathString.prototype = {
      _radius: 4.5,
      _circle: circle(4.5),
      pointRadius: /* @__PURE__ */ __name(function(_) {
        if ((_ = +_) !== this._radius) this._radius = _, this._circle = null;
        return this;
      }, "pointRadius"),
      polygonStart: /* @__PURE__ */ __name(function() {
        this._line = 0;
      }, "polygonStart"),
      polygonEnd: /* @__PURE__ */ __name(function() {
        this._line = NaN;
      }, "polygonEnd"),
      lineStart: /* @__PURE__ */ __name(function() {
        this._point = 0;
      }, "lineStart"),
      lineEnd: /* @__PURE__ */ __name(function() {
        if (this._line === 0) this._string.push("Z");
        this._point = NaN;
      }, "lineEnd"),
      point: /* @__PURE__ */ __name(function(x, y) {
        switch (this._point) {
          case 0: {
            this._string.push("M", x, ",", y);
            this._point = 1;
            break;
          }
          case 1: {
            this._string.push("L", x, ",", y);
            break;
          }
          default: {
            if (this._circle == null) this._circle = circle(this._radius);
            this._string.push("M", x, ",", y, this._circle);
            break;
          }
        }
      }, "point"),
      result: /* @__PURE__ */ __name(function() {
        if (this._string.length) {
          var result = this._string.join("");
          this._string = [];
          return result;
        } else {
          return null;
        }
      }, "result")
    };
    __name(circle, "circle");
  }
});

// worker/node_modules/d3-geo/src/path/index.js
var init_path = __esm({
  "worker/node_modules/d3-geo/src/path/index.js"() {
    init_identity2();
    init_stream4();
    init_area2();
    init_bounds2();
    init_centroid2();
    init_context();
    init_measure();
    init_string();
  }
});

// worker/node_modules/d3-geo/src/clip/index.js
function clip_default(pointVisible, clipLine, interpolate, start) {
  return function(rotate, sink) {
    var line = clipLine(sink), rotatedStart = rotate.invert(start[0], start[1]), ringBuffer = buffer_default(), ringSink = clipLine(ringBuffer), polygonStarted = false, polygon2, segments, ring;
    var clip = {
      point: point2,
      lineStart,
      lineEnd,
      polygonStart: /* @__PURE__ */ __name(function() {
        clip.point = pointRing;
        clip.lineStart = ringStart;
        clip.lineEnd = ringEnd;
        segments = [];
        polygon2 = [];
      }, "polygonStart"),
      polygonEnd: /* @__PURE__ */ __name(function() {
        clip.point = point2;
        clip.lineStart = lineStart;
        clip.lineEnd = lineEnd;
        segments = merge_default(segments);
        var startInside = polygonContains_default(polygon2, rotatedStart);
        if (segments.length) {
          if (!polygonStarted) sink.polygonStart(), polygonStarted = true;
          polygon_default(segments, compareIntersection, startInside, interpolate, sink);
        } else if (startInside) {
          if (!polygonStarted) sink.polygonStart(), polygonStarted = true;
          sink.lineStart();
          interpolate(null, null, 1, sink);
          sink.lineEnd();
        }
        if (polygonStarted) sink.polygonEnd(), polygonStarted = false;
        segments = polygon2 = null;
      }, "polygonEnd"),
      sphere: /* @__PURE__ */ __name(function() {
        sink.polygonStart();
        sink.lineStart();
        interpolate(null, null, 1, sink);
        sink.lineEnd();
        sink.polygonEnd();
      }, "sphere")
    };
    function point2(lambda, phi) {
      var point3 = rotate(lambda, phi);
      if (pointVisible(lambda = point3[0], phi = point3[1])) sink.point(lambda, phi);
    }
    __name(point2, "point");
    function pointLine(lambda, phi) {
      var point3 = rotate(lambda, phi);
      line.point(point3[0], point3[1]);
    }
    __name(pointLine, "pointLine");
    function lineStart() {
      clip.point = pointLine;
      line.lineStart();
    }
    __name(lineStart, "lineStart");
    function lineEnd() {
      clip.point = point2;
      line.lineEnd();
    }
    __name(lineEnd, "lineEnd");
    function pointRing(lambda, phi) {
      ring.push([lambda, phi]);
      var point3 = rotate(lambda, phi);
      ringSink.point(point3[0], point3[1]);
    }
    __name(pointRing, "pointRing");
    function ringStart() {
      ringSink.lineStart();
      ring = [];
    }
    __name(ringStart, "ringStart");
    function ringEnd() {
      pointRing(ring[0][0], ring[0][1]);
      ringSink.lineEnd();
      var clean = ringSink.clean(), ringSegments = ringBuffer.result(), i, n = ringSegments.length, m, segment, point3;
      ring.pop();
      polygon2.push(ring);
      ring = null;
      if (!n) return;
      if (clean & 1) {
        segment = ringSegments[0];
        if ((m = segment.length - 1) > 0) {
          if (!polygonStarted) sink.polygonStart(), polygonStarted = true;
          sink.lineStart();
          for (i = 0; i < m; ++i) sink.point((point3 = segment[i])[0], point3[1]);
          sink.lineEnd();
        }
        return;
      }
      if (n > 1 && clean & 2) ringSegments.push(ringSegments.pop().concat(ringSegments.shift()));
      segments.push(ringSegments.filter(validSegment));
    }
    __name(ringEnd, "ringEnd");
    return clip;
  };
}
function validSegment(segment) {
  return segment.length > 1;
}
function compareIntersection(a, b) {
  return ((a = a.x)[0] < 0 ? a[1] - halfPi - epsilon : halfPi - a[1]) - ((b = b.x)[0] < 0 ? b[1] - halfPi - epsilon : halfPi - b[1]);
}
var init_clip = __esm({
  "worker/node_modules/d3-geo/src/clip/index.js"() {
    init_buffer();
    init_polygon();
    init_math();
    init_polygonContains();
    init_src();
    __name(clip_default, "default");
    __name(validSegment, "validSegment");
    __name(compareIntersection, "compareIntersection");
  }
});

// worker/node_modules/d3-geo/src/clip/antimeridian.js
function clipAntimeridianLine(stream) {
  var lambda0 = NaN, phi0 = NaN, sign0 = NaN, clean;
  return {
    lineStart: /* @__PURE__ */ __name(function() {
      stream.lineStart();
      clean = 1;
    }, "lineStart"),
    point: /* @__PURE__ */ __name(function(lambda1, phi1) {
      var sign1 = lambda1 > 0 ? pi : -pi, delta = abs(lambda1 - lambda0);
      if (abs(delta - pi) < epsilon) {
        stream.point(lambda0, phi0 = (phi0 + phi1) / 2 > 0 ? halfPi : -halfPi);
        stream.point(sign0, phi0);
        stream.lineEnd();
        stream.lineStart();
        stream.point(sign1, phi0);
        stream.point(lambda1, phi0);
        clean = 0;
      } else if (sign0 !== sign1 && delta >= pi) {
        if (abs(lambda0 - sign0) < epsilon) lambda0 -= sign0 * epsilon;
        if (abs(lambda1 - sign1) < epsilon) lambda1 -= sign1 * epsilon;
        phi0 = clipAntimeridianIntersect(lambda0, phi0, lambda1, phi1);
        stream.point(sign0, phi0);
        stream.lineEnd();
        stream.lineStart();
        stream.point(sign1, phi0);
        clean = 0;
      }
      stream.point(lambda0 = lambda1, phi0 = phi1);
      sign0 = sign1;
    }, "point"),
    lineEnd: /* @__PURE__ */ __name(function() {
      stream.lineEnd();
      lambda0 = phi0 = NaN;
    }, "lineEnd"),
    clean: /* @__PURE__ */ __name(function() {
      return 2 - clean;
    }, "clean")
  };
}
function clipAntimeridianIntersect(lambda0, phi0, lambda1, phi1) {
  var cosPhi0, cosPhi1, sinLambda0Lambda1 = sin(lambda0 - lambda1);
  return abs(sinLambda0Lambda1) > epsilon ? atan((sin(phi0) * (cosPhi1 = cos(phi1)) * sin(lambda1) - sin(phi1) * (cosPhi0 = cos(phi0)) * sin(lambda0)) / (cosPhi0 * cosPhi1 * sinLambda0Lambda1)) : (phi0 + phi1) / 2;
}
function clipAntimeridianInterpolate(from, to, direction, stream) {
  var phi;
  if (from == null) {
    phi = direction * halfPi;
    stream.point(-pi, phi);
    stream.point(0, phi);
    stream.point(pi, phi);
    stream.point(pi, 0);
    stream.point(pi, -phi);
    stream.point(0, -phi);
    stream.point(-pi, -phi);
    stream.point(-pi, 0);
    stream.point(-pi, phi);
  } else if (abs(from[0] - to[0]) > epsilon) {
    var lambda = from[0] < to[0] ? pi : -pi;
    phi = direction * lambda / 2;
    stream.point(-lambda, phi);
    stream.point(0, phi);
    stream.point(lambda, phi);
  } else {
    stream.point(to[0], to[1]);
  }
}
var antimeridian_default;
var init_antimeridian = __esm({
  "worker/node_modules/d3-geo/src/clip/antimeridian.js"() {
    init_clip();
    init_math();
    antimeridian_default = clip_default(
      function() {
        return true;
      },
      clipAntimeridianLine,
      clipAntimeridianInterpolate,
      [-pi, -halfPi]
    );
    __name(clipAntimeridianLine, "clipAntimeridianLine");
    __name(clipAntimeridianIntersect, "clipAntimeridianIntersect");
    __name(clipAntimeridianInterpolate, "clipAntimeridianInterpolate");
  }
});

// worker/node_modules/d3-geo/src/clip/circle.js
function circle_default(radius, delta) {
  var cr = cos(radius), smallRadius = cr > 0, notHemisphere = abs(cr) > epsilon;
  function interpolate(from, to, direction, stream) {
    circleStream(stream, radius, delta, direction, from, to);
  }
  __name(interpolate, "interpolate");
  function visible(lambda, phi) {
    return cos(lambda) * cos(phi) > cr;
  }
  __name(visible, "visible");
  function clipLine(stream) {
    var point0, c0, v0, v00, clean;
    return {
      lineStart: /* @__PURE__ */ __name(function() {
        v00 = v0 = false;
        clean = 1;
      }, "lineStart"),
      point: /* @__PURE__ */ __name(function(lambda, phi) {
        var point1 = [lambda, phi], point2, v = visible(lambda, phi), c = smallRadius ? v ? 0 : code(lambda, phi) : v ? code(lambda + (lambda < 0 ? pi : -pi), phi) : 0;
        if (!point0 && (v00 = v0 = v)) stream.lineStart();
        if (v !== v0) {
          point2 = intersect4(point0, point1);
          if (!point2 || pointEqual_default(point0, point2) || pointEqual_default(point1, point2)) {
            point1[0] += epsilon;
            point1[1] += epsilon;
            v = visible(point1[0], point1[1]);
          }
        }
        if (v !== v0) {
          clean = 0;
          if (v) {
            stream.lineStart();
            point2 = intersect4(point1, point0);
            stream.point(point2[0], point2[1]);
          } else {
            point2 = intersect4(point0, point1);
            stream.point(point2[0], point2[1]);
            stream.lineEnd();
          }
          point0 = point2;
        } else if (notHemisphere && point0 && smallRadius ^ v) {
          var t;
          if (!(c & c0) && (t = intersect4(point1, point0, true))) {
            clean = 0;
            if (smallRadius) {
              stream.lineStart();
              stream.point(t[0][0], t[0][1]);
              stream.point(t[1][0], t[1][1]);
              stream.lineEnd();
            } else {
              stream.point(t[1][0], t[1][1]);
              stream.lineEnd();
              stream.lineStart();
              stream.point(t[0][0], t[0][1]);
            }
          }
        }
        if (v && (!point0 || !pointEqual_default(point0, point1))) {
          stream.point(point1[0], point1[1]);
        }
        point0 = point1, v0 = v, c0 = c;
      }, "point"),
      lineEnd: /* @__PURE__ */ __name(function() {
        if (v0) stream.lineEnd();
        point0 = null;
      }, "lineEnd"),
      // Rejoin first and last segments if there were intersections and the first
      // and last points were visible.
      clean: /* @__PURE__ */ __name(function() {
        return clean | (v00 && v0) << 1;
      }, "clean")
    };
  }
  __name(clipLine, "clipLine");
  function intersect4(a, b, two) {
    var pa = cartesian(a), pb = cartesian(b);
    var n1 = [1, 0, 0], n2 = cartesianCross(pa, pb), n2n2 = cartesianDot(n2, n2), n1n2 = n2[0], determinant = n2n2 - n1n2 * n1n2;
    if (!determinant) return !two && a;
    var c1 = cr * n2n2 / determinant, c2 = -cr * n1n2 / determinant, n1xn2 = cartesianCross(n1, n2), A = cartesianScale(n1, c1), B = cartesianScale(n2, c2);
    cartesianAddInPlace(A, B);
    var u = n1xn2, w = cartesianDot(A, u), uu = cartesianDot(u, u), t2 = w * w - uu * (cartesianDot(A, A) - 1);
    if (t2 < 0) return;
    var t = sqrt(t2), q = cartesianScale(u, (-w - t) / uu);
    cartesianAddInPlace(q, A);
    q = spherical(q);
    if (!two) return q;
    var lambda0 = a[0], lambda1 = b[0], phi0 = a[1], phi1 = b[1], z2;
    if (lambda1 < lambda0) z2 = lambda0, lambda0 = lambda1, lambda1 = z2;
    var delta2 = lambda1 - lambda0, polar = abs(delta2 - pi) < epsilon, meridian = polar || delta2 < epsilon;
    if (!polar && phi1 < phi0) z2 = phi0, phi0 = phi1, phi1 = z2;
    if (meridian ? polar ? phi0 + phi1 > 0 ^ q[1] < (abs(q[0] - lambda0) < epsilon ? phi0 : phi1) : phi0 <= q[1] && q[1] <= phi1 : delta2 > pi ^ (lambda0 <= q[0] && q[0] <= lambda1)) {
      var q1 = cartesianScale(u, (-w + t) / uu);
      cartesianAddInPlace(q1, A);
      return [q, spherical(q1)];
    }
  }
  __name(intersect4, "intersect");
  function code(lambda, phi) {
    var r = smallRadius ? radius : pi - radius, code2 = 0;
    if (lambda < -r) code2 |= 1;
    else if (lambda > r) code2 |= 2;
    if (phi < -r) code2 |= 4;
    else if (phi > r) code2 |= 8;
    return code2;
  }
  __name(code, "code");
  return clip_default(visible, clipLine, interpolate, smallRadius ? [0, -radius] : [-pi, radius - pi]);
}
var init_circle2 = __esm({
  "worker/node_modules/d3-geo/src/clip/circle.js"() {
    init_cartesian();
    init_circle();
    init_math();
    init_pointEqual();
    init_clip();
    __name(circle_default, "default");
  }
});

// worker/node_modules/d3-geo/src/transform.js
function transformer(methods) {
  return function(stream) {
    var s = new TransformStream();
    for (var key in methods) s[key] = methods[key];
    s.stream = stream;
    return s;
  };
}
function TransformStream() {
}
var init_transform = __esm({
  "worker/node_modules/d3-geo/src/transform.js"() {
    __name(transformer, "transformer");
    __name(TransformStream, "TransformStream");
    TransformStream.prototype = {
      constructor: TransformStream,
      point: /* @__PURE__ */ __name(function(x, y) {
        this.stream.point(x, y);
      }, "point"),
      sphere: /* @__PURE__ */ __name(function() {
        this.stream.sphere();
      }, "sphere"),
      lineStart: /* @__PURE__ */ __name(function() {
        this.stream.lineStart();
      }, "lineStart"),
      lineEnd: /* @__PURE__ */ __name(function() {
        this.stream.lineEnd();
      }, "lineEnd"),
      polygonStart: /* @__PURE__ */ __name(function() {
        this.stream.polygonStart();
      }, "polygonStart"),
      polygonEnd: /* @__PURE__ */ __name(function() {
        this.stream.polygonEnd();
      }, "polygonEnd")
    };
  }
});

// worker/node_modules/d3-geo/src/projection/fit.js
function fitExtent(projection2, extent, object2) {
  var w = extent[1][0] - extent[0][0], h = extent[1][1] - extent[0][1], clip = projection2.clipExtent && projection2.clipExtent();
  projection2.scale(150).translate([0, 0]);
  if (clip != null) projection2.clipExtent(null);
  stream_default(object2, projection2.stream(bounds_default));
  var b = bounds_default.result(), k = Math.min(w / (b[1][0] - b[0][0]), h / (b[1][1] - b[0][1])), x = +extent[0][0] + (w - k * (b[1][0] + b[0][0])) / 2, y = +extent[0][1] + (h - k * (b[1][1] + b[0][1])) / 2;
  if (clip != null) projection2.clipExtent(clip);
  return projection2.scale(k * 150).translate([x, y]);
}
function fitSize(projection2, size, object2) {
  return fitExtent(projection2, [[0, 0], size], object2);
}
var init_fit = __esm({
  "worker/node_modules/d3-geo/src/projection/fit.js"() {
    init_stream4();
    init_bounds2();
    __name(fitExtent, "fitExtent");
    __name(fitSize, "fitSize");
  }
});

// worker/node_modules/d3-geo/src/projection/resample.js
function resample_default(project, delta2) {
  return +delta2 ? resample(project, delta2) : resampleNone(project);
}
function resampleNone(project) {
  return transformer({
    point: /* @__PURE__ */ __name(function(x, y) {
      x = project(x, y);
      this.stream.point(x[0], x[1]);
    }, "point")
  });
}
function resample(project, delta2) {
  function resampleLineTo(x02, y02, lambda0, a0, b0, c0, x12, y12, lambda1, a1, b1, c1, depth, stream) {
    var dx = x12 - x02, dy = y12 - y02, d2 = dx * dx + dy * dy;
    if (d2 > 4 * delta2 && depth--) {
      var a = a0 + a1, b = b0 + b1, c = c0 + c1, m = sqrt(a * a + b * b + c * c), phi2 = asin(c /= m), lambda2 = abs(abs(c) - 1) < epsilon || abs(lambda0 - lambda1) < epsilon ? (lambda0 + lambda1) / 2 : atan2(b, a), p = project(lambda2, phi2), x2 = p[0], y2 = p[1], dx2 = x2 - x02, dy2 = y2 - y02, dz = dy * dx2 - dx * dy2;
      if (dz * dz / d2 > delta2 || abs((dx * dx2 + dy * dy2) / d2 - 0.5) > 0.3 || a0 * a1 + b0 * b1 + c0 * c1 < cosMinDistance) {
        resampleLineTo(x02, y02, lambda0, a0, b0, c0, x2, y2, lambda2, a /= m, b /= m, c, depth, stream);
        stream.point(x2, y2);
        resampleLineTo(x2, y2, lambda2, a, b, c, x12, y12, lambda1, a1, b1, c1, depth, stream);
      }
    }
  }
  __name(resampleLineTo, "resampleLineTo");
  return function(stream) {
    var lambda00, x00, y00, a00, b00, c00, lambda0, x02, y02, a0, b0, c0;
    var resampleStream = {
      point: point2,
      lineStart,
      lineEnd,
      polygonStart: /* @__PURE__ */ __name(function() {
        stream.polygonStart();
        resampleStream.lineStart = ringStart;
      }, "polygonStart"),
      polygonEnd: /* @__PURE__ */ __name(function() {
        stream.polygonEnd();
        resampleStream.lineStart = lineStart;
      }, "polygonEnd")
    };
    function point2(x, y) {
      x = project(x, y);
      stream.point(x[0], x[1]);
    }
    __name(point2, "point");
    function lineStart() {
      x02 = NaN;
      resampleStream.point = linePoint;
      stream.lineStart();
    }
    __name(lineStart, "lineStart");
    function linePoint(lambda, phi) {
      var c = cartesian([lambda, phi]), p = project(lambda, phi);
      resampleLineTo(x02, y02, lambda0, a0, b0, c0, x02 = p[0], y02 = p[1], lambda0 = lambda, a0 = c[0], b0 = c[1], c0 = c[2], maxDepth, stream);
      stream.point(x02, y02);
    }
    __name(linePoint, "linePoint");
    function lineEnd() {
      resampleStream.point = point2;
      stream.lineEnd();
    }
    __name(lineEnd, "lineEnd");
    function ringStart() {
      lineStart();
      resampleStream.point = ringPoint;
      resampleStream.lineEnd = ringEnd;
    }
    __name(ringStart, "ringStart");
    function ringPoint(lambda, phi) {
      linePoint(lambda00 = lambda, phi), x00 = x02, y00 = y02, a00 = a0, b00 = b0, c00 = c0;
      resampleStream.point = linePoint;
    }
    __name(ringPoint, "ringPoint");
    function ringEnd() {
      resampleLineTo(x02, y02, lambda0, a0, b0, c0, x00, y00, lambda00, a00, b00, c00, maxDepth, stream);
      resampleStream.lineEnd = lineEnd;
      lineEnd();
    }
    __name(ringEnd, "ringEnd");
    return resampleStream;
  };
}
var maxDepth, cosMinDistance;
var init_resample = __esm({
  "worker/node_modules/d3-geo/src/projection/resample.js"() {
    init_cartesian();
    init_math();
    init_transform();
    maxDepth = 16;
    cosMinDistance = cos(30 * radians);
    __name(resample_default, "default");
    __name(resampleNone, "resampleNone");
    __name(resample, "resample");
  }
});

// worker/node_modules/d3-geo/src/projection/index.js
function projection(project) {
  return projectionMutator(function() {
    return project;
  })();
}
function projectionMutator(projectAt) {
  var project, k = 150, x = 480, y = 250, dx, dy, lambda = 0, phi = 0, deltaLambda = 0, deltaPhi = 0, deltaGamma = 0, rotate, projectRotate, theta = null, preclip = antimeridian_default, x02 = null, y02, x12, y12, postclip = identity_default2, delta2 = 0.5, projectResample = resample_default(projectTransform, delta2), cache, cacheStream;
  function projection2(point2) {
    point2 = projectRotate(point2[0] * radians, point2[1] * radians);
    return [point2[0] * k + dx, dy - point2[1] * k];
  }
  __name(projection2, "projection");
  function invert(point2) {
    point2 = projectRotate.invert((point2[0] - dx) / k, (dy - point2[1]) / k);
    return point2 && [point2[0] * degrees, point2[1] * degrees];
  }
  __name(invert, "invert");
  function projectTransform(x2, y2) {
    return x2 = project(x2, y2), [x2[0] * k + dx, dy - x2[1] * k];
  }
  __name(projectTransform, "projectTransform");
  projection2.stream = function(stream) {
    return cache && cacheStream === stream ? cache : cache = transformRadians(preclip(rotate, projectResample(postclip(cacheStream = stream))));
  };
  projection2.clipAngle = function(_) {
    return arguments.length ? (preclip = +_ ? circle_default(theta = _ * radians, 6 * radians) : (theta = null, antimeridian_default), reset()) : theta * degrees;
  };
  projection2.clipExtent = function(_) {
    return arguments.length ? (postclip = _ == null ? (x02 = y02 = x12 = y12 = null, identity_default2) : clipExtent(x02 = +_[0][0], y02 = +_[0][1], x12 = +_[1][0], y12 = +_[1][1]), reset()) : x02 == null ? null : [[x02, y02], [x12, y12]];
  };
  projection2.scale = function(_) {
    return arguments.length ? (k = +_, recenter()) : k;
  };
  projection2.translate = function(_) {
    return arguments.length ? (x = +_[0], y = +_[1], recenter()) : [x, y];
  };
  projection2.center = function(_) {
    return arguments.length ? (lambda = _[0] % 360 * radians, phi = _[1] % 360 * radians, recenter()) : [lambda * degrees, phi * degrees];
  };
  projection2.rotate = function(_) {
    return arguments.length ? (deltaLambda = _[0] % 360 * radians, deltaPhi = _[1] % 360 * radians, deltaGamma = _.length > 2 ? _[2] % 360 * radians : 0, recenter()) : [deltaLambda * degrees, deltaPhi * degrees, deltaGamma * degrees];
  };
  projection2.precision = function(_) {
    return arguments.length ? (projectResample = resample_default(projectTransform, delta2 = _ * _), reset()) : sqrt(delta2);
  };
  projection2.fitExtent = function(extent, object2) {
    return fitExtent(projection2, extent, object2);
  };
  projection2.fitSize = function(size, object2) {
    return fitSize(projection2, size, object2);
  };
  function recenter() {
    projectRotate = compose_default(rotate = rotateRadians(deltaLambda, deltaPhi, deltaGamma), project);
    var center2 = project(lambda, phi);
    dx = x - center2[0] * k;
    dy = y + center2[1] * k;
    return reset();
  }
  __name(recenter, "recenter");
  function reset() {
    cache = cacheStream = null;
    return projection2;
  }
  __name(reset, "reset");
  return function() {
    project = projectAt.apply(this, arguments);
    projection2.invert = project.invert && invert;
    return recenter();
  };
}
var transformRadians;
var init_projection = __esm({
  "worker/node_modules/d3-geo/src/projection/index.js"() {
    init_antimeridian();
    init_circle2();
    init_extent2();
    init_compose();
    init_identity2();
    init_math();
    init_rotation();
    init_transform();
    init_fit();
    init_resample();
    transformRadians = transformer({
      point: /* @__PURE__ */ __name(function(x, y) {
        this.stream.point(x * radians, y * radians);
      }, "point")
    });
    __name(projection, "projection");
    __name(projectionMutator, "projectionMutator");
  }
});

// worker/node_modules/d3-geo/src/projection/conic.js
var init_conic = __esm({
  "worker/node_modules/d3-geo/src/projection/conic.js"() {
    init_math();
    init_projection();
  }
});

// worker/node_modules/d3-geo/src/projection/cylindricalEqualArea.js
var init_cylindricalEqualArea = __esm({
  "worker/node_modules/d3-geo/src/projection/cylindricalEqualArea.js"() {
    init_math();
  }
});

// worker/node_modules/d3-geo/src/projection/conicEqualArea.js
var init_conicEqualArea = __esm({
  "worker/node_modules/d3-geo/src/projection/conicEqualArea.js"() {
    init_math();
    init_conic();
    init_cylindricalEqualArea();
  }
});

// worker/node_modules/d3-geo/src/projection/albers.js
var init_albers = __esm({
  "worker/node_modules/d3-geo/src/projection/albers.js"() {
    init_conicEqualArea();
  }
});

// worker/node_modules/d3-geo/src/projection/albersUsa.js
var init_albersUsa = __esm({
  "worker/node_modules/d3-geo/src/projection/albersUsa.js"() {
    init_math();
    init_albers();
    init_conicEqualArea();
    init_fit();
  }
});

// worker/node_modules/d3-geo/src/projection/azimuthal.js
function azimuthalRaw(scale) {
  return function(x, y) {
    var cx = cos(x), cy = cos(y), k = scale(cx * cy);
    return [
      k * cy * sin(x),
      k * sin(y)
    ];
  };
}
function azimuthalInvert(angle) {
  return function(x, y) {
    var z2 = sqrt(x * x + y * y), c = angle(z2), sc = sin(c), cc = cos(c);
    return [
      atan2(x * sc, z2 * cc),
      asin(z2 && y * sc / z2)
    ];
  };
}
var init_azimuthal = __esm({
  "worker/node_modules/d3-geo/src/projection/azimuthal.js"() {
    init_math();
    __name(azimuthalRaw, "azimuthalRaw");
    __name(azimuthalInvert, "azimuthalInvert");
  }
});

// worker/node_modules/d3-geo/src/projection/azimuthalEqualArea.js
var azimuthalEqualAreaRaw;
var init_azimuthalEqualArea = __esm({
  "worker/node_modules/d3-geo/src/projection/azimuthalEqualArea.js"() {
    init_math();
    init_azimuthal();
    init_projection();
    azimuthalEqualAreaRaw = azimuthalRaw(function(cxcy) {
      return sqrt(2 / (1 + cxcy));
    });
    azimuthalEqualAreaRaw.invert = azimuthalInvert(function(z2) {
      return 2 * asin(z2 / 2);
    });
  }
});

// worker/node_modules/d3-geo/src/projection/azimuthalEquidistant.js
function azimuthalEquidistant_default() {
  return projection(azimuthalEquidistantRaw).scale(79.4188).clipAngle(180 - 1e-3);
}
var azimuthalEquidistantRaw;
var init_azimuthalEquidistant = __esm({
  "worker/node_modules/d3-geo/src/projection/azimuthalEquidistant.js"() {
    init_math();
    init_azimuthal();
    init_projection();
    azimuthalEquidistantRaw = azimuthalRaw(function(c) {
      return (c = acos(c)) && c / sin(c);
    });
    azimuthalEquidistantRaw.invert = azimuthalInvert(function(z2) {
      return z2;
    });
    __name(azimuthalEquidistant_default, "default");
  }
});

// worker/node_modules/d3-geo/src/projection/mercator.js
function mercatorRaw(lambda, phi) {
  return [lambda, log2(tan((halfPi + phi) / 2))];
}
var init_mercator = __esm({
  "worker/node_modules/d3-geo/src/projection/mercator.js"() {
    init_math();
    init_rotation();
    init_projection();
    __name(mercatorRaw, "mercatorRaw");
    mercatorRaw.invert = function(x, y) {
      return [x, 2 * atan(exp(y)) - halfPi];
    };
  }
});

// worker/node_modules/d3-geo/src/projection/conicConformal.js
var init_conicConformal = __esm({
  "worker/node_modules/d3-geo/src/projection/conicConformal.js"() {
    init_math();
    init_conic();
    init_mercator();
  }
});

// worker/node_modules/d3-geo/src/projection/equirectangular.js
function equirectangularRaw(lambda, phi) {
  return [lambda, phi];
}
var init_equirectangular = __esm({
  "worker/node_modules/d3-geo/src/projection/equirectangular.js"() {
    init_projection();
    __name(equirectangularRaw, "equirectangularRaw");
    equirectangularRaw.invert = equirectangularRaw;
  }
});

// worker/node_modules/d3-geo/src/projection/conicEquidistant.js
var init_conicEquidistant = __esm({
  "worker/node_modules/d3-geo/src/projection/conicEquidistant.js"() {
    init_math();
    init_conic();
    init_equirectangular();
  }
});

// worker/node_modules/d3-geo/src/projection/gnomonic.js
function gnomonicRaw(x, y) {
  var cy = cos(y), k = cos(x) * cy;
  return [cy * sin(x) / k, sin(y) / k];
}
var init_gnomonic = __esm({
  "worker/node_modules/d3-geo/src/projection/gnomonic.js"() {
    init_math();
    init_azimuthal();
    init_projection();
    __name(gnomonicRaw, "gnomonicRaw");
    gnomonicRaw.invert = azimuthalInvert(atan);
  }
});

// worker/node_modules/d3-geo/src/projection/identity.js
var init_identity3 = __esm({
  "worker/node_modules/d3-geo/src/projection/identity.js"() {
    init_extent2();
    init_identity2();
    init_transform();
    init_fit();
  }
});

// worker/node_modules/d3-geo/src/projection/naturalEarth1.js
function naturalEarth1Raw(lambda, phi) {
  var phi2 = phi * phi, phi4 = phi2 * phi2;
  return [
    lambda * (0.8707 - 0.131979 * phi2 + phi4 * (-0.013791 + phi4 * (3971e-6 * phi2 - 1529e-6 * phi4))),
    phi * (1.007226 + phi2 * (0.015085 + phi4 * (-0.044475 + 0.028874 * phi2 - 5916e-6 * phi4)))
  ];
}
var init_naturalEarth1 = __esm({
  "worker/node_modules/d3-geo/src/projection/naturalEarth1.js"() {
    init_projection();
    init_math();
    __name(naturalEarth1Raw, "naturalEarth1Raw");
    naturalEarth1Raw.invert = function(x, y) {
      var phi = y, i = 25, delta;
      do {
        var phi2 = phi * phi, phi4 = phi2 * phi2;
        phi -= delta = (phi * (1.007226 + phi2 * (0.015085 + phi4 * (-0.044475 + 0.028874 * phi2 - 5916e-6 * phi4))) - y) / (1.007226 + phi2 * (0.015085 * 3 + phi4 * (-0.044475 * 7 + 0.028874 * 9 * phi2 - 5916e-6 * 11 * phi4)));
      } while (abs(delta) > epsilon && --i > 0);
      return [
        x / (0.8707 + (phi2 = phi * phi) * (-0.131979 + phi2 * (-0.013791 + phi2 * phi2 * phi2 * (3971e-6 - 1529e-6 * phi2)))),
        phi
      ];
    };
  }
});

// worker/node_modules/d3-geo/src/projection/orthographic.js
function orthographicRaw(x, y) {
  return [cos(y) * sin(x), sin(y)];
}
var init_orthographic = __esm({
  "worker/node_modules/d3-geo/src/projection/orthographic.js"() {
    init_math();
    init_azimuthal();
    init_projection();
    __name(orthographicRaw, "orthographicRaw");
    orthographicRaw.invert = azimuthalInvert(asin);
  }
});

// worker/node_modules/d3-geo/src/projection/stereographic.js
function stereographicRaw(x, y) {
  var cy = cos(y), k = 1 + cos(x) * cy;
  return [cy * sin(x) / k, sin(y) / k];
}
var init_stereographic = __esm({
  "worker/node_modules/d3-geo/src/projection/stereographic.js"() {
    init_math();
    init_azimuthal();
    init_projection();
    __name(stereographicRaw, "stereographicRaw");
    stereographicRaw.invert = azimuthalInvert(function(z2) {
      return 2 * atan(z2);
    });
  }
});

// worker/node_modules/d3-geo/src/projection/transverseMercator.js
function transverseMercatorRaw(lambda, phi) {
  return [log2(tan((halfPi + phi) / 2)), -lambda];
}
var init_transverseMercator = __esm({
  "worker/node_modules/d3-geo/src/projection/transverseMercator.js"() {
    init_math();
    init_mercator();
    __name(transverseMercatorRaw, "transverseMercatorRaw");
    transverseMercatorRaw.invert = function(x, y) {
      return [-y, 2 * atan(exp(x)) - halfPi];
    };
  }
});

// worker/node_modules/d3-geo/index.js
var init_d3_geo = __esm({
  "worker/node_modules/d3-geo/index.js"() {
    init_area();
    init_bounds();
    init_centroid();
    init_circle();
    init_extent2();
    init_contains();
    init_distance();
    init_graticule();
    init_interpolate();
    init_length();
    init_path();
    init_albers();
    init_albersUsa();
    init_azimuthalEqualArea();
    init_azimuthalEquidistant();
    init_conicConformal();
    init_conicEqualArea();
    init_conicEquidistant();
    init_equirectangular();
    init_gnomonic();
    init_identity3();
    init_projection();
    init_mercator();
    init_naturalEarth1();
    init_orthographic();
    init_stereographic();
    init_transverseMercator();
    init_rotation();
    init_stream4();
    init_transform();
  }
});

// worker/node_modules/@turf/buffer/dist/esm/index.js
function buffer(geojson, radius, options) {
  options = options || {};
  var units = options.units || "kilometers";
  var steps = options.steps || 8;
  if (!geojson) throw new Error("geojson is required");
  if (typeof options !== "object") throw new Error("options must be an object");
  if (typeof steps !== "number") throw new Error("steps must be an number");
  if (radius === void 0) throw new Error("radius is required");
  if (steps <= 0) throw new Error("steps must be greater than 0");
  var results = [];
  switch (geojson.type) {
    case "GeometryCollection":
      geomEach(geojson, function(geometry) {
        var buffered = bufferFeature(geometry, radius, units, steps);
        if (buffered) results.push(buffered);
      });
      return featureCollection(results);
    case "FeatureCollection":
      featureEach(geojson, function(feature2) {
        var multiBuffered = bufferFeature(feature2, radius, units, steps);
        if (multiBuffered) {
          featureEach(multiBuffered, function(buffered) {
            if (buffered) results.push(buffered);
          });
        }
      });
      return featureCollection(results);
  }
  return bufferFeature(geojson, radius, units, steps);
}
function bufferFeature(geojson, radius, units, steps) {
  var properties = geojson.properties || {};
  var geometry = geojson.type === "Feature" ? geojson.geometry : geojson;
  if (geometry.type === "GeometryCollection") {
    var results = [];
    geomEach(geojson, function(geometry2) {
      var buffered2 = bufferFeature(geometry2, radius, units, steps);
      if (buffered2) results.push(buffered2);
    });
    return featureCollection(results);
  }
  var projection2 = defineProjection(geometry);
  var projected = {
    type: geometry.type,
    coordinates: projectCoords(geometry.coordinates, projection2)
  };
  var reader = new GeoJSONReader();
  var geom = reader.read(projected);
  var distance2 = radiansToLength(lengthToRadians(radius, units), "meters");
  var buffered = BufferOp.bufferOp(geom, distance2, steps);
  var writer = new GeoJSONWriter();
  buffered = writer.write(buffered);
  if (coordsIsNaN(buffered.coordinates)) return void 0;
  var result = {
    type: buffered.type,
    coordinates: unprojectCoords(buffered.coordinates, projection2)
  };
  return feature(result, properties);
}
function coordsIsNaN(coords) {
  if (Array.isArray(coords[0])) return coordsIsNaN(coords[0]);
  return isNaN(coords[0]);
}
function projectCoords(coords, proj) {
  if (typeof coords[0] !== "object") return proj(coords);
  return coords.map(function(coord) {
    return projectCoords(coord, proj);
  });
}
function unprojectCoords(coords, proj) {
  if (typeof coords[0] !== "object") return proj.invert(coords);
  return coords.map(function(coord) {
    return unprojectCoords(coord, proj);
  });
}
function defineProjection(geojson) {
  var coords = center(geojson).geometry.coordinates;
  var rotation = [-coords[0], -coords[1]];
  return azimuthalEquidistant_default().rotate(rotation).scale(earthRadius);
}
var import_jsts, BufferOp, GeoJSONReader, GeoJSONWriter;
var init_esm8 = __esm({
  "worker/node_modules/@turf/buffer/dist/esm/index.js"() {
    init_esm7();
    import_jsts = __toESM(require_jsts_min(), 1);
    init_esm4();
    init_d3_geo();
    init_esm();
    ({ BufferOp, GeoJSONReader, GeoJSONWriter } = import_jsts.default);
    __name(buffer, "buffer");
    __name(bufferFeature, "bufferFeature");
    __name(coordsIsNaN, "coordsIsNaN");
    __name(projectCoords, "projectCoords");
    __name(unprojectCoords, "unprojectCoords");
    __name(defineProjection, "defineProjection");
  }
});

// worker/node_modules/tinyqueue/tinyqueue.js
var require_tinyqueue = __commonJS({
  "worker/node_modules/tinyqueue/tinyqueue.js"(exports, module) {
    (function(global2, factory) {
      typeof exports === "object" && typeof module !== "undefined" ? module.exports = factory() : typeof define === "function" && define.amd ? define(factory) : (global2 = global2 || self, global2.TinyQueue = factory());
    })(exports, function() {
      "use strict";
      var TinyQueue = /* @__PURE__ */ __name(function TinyQueue2(data, compare2) {
        if (data === void 0) data = [];
        if (compare2 === void 0) compare2 = defaultCompare;
        this.data = data;
        this.length = this.data.length;
        this.compare = compare2;
        if (this.length > 0) {
          for (var i = (this.length >> 1) - 1; i >= 0; i--) {
            this._down(i);
          }
        }
      }, "TinyQueue");
      TinyQueue.prototype.push = /* @__PURE__ */ __name(function push(item) {
        this.data.push(item);
        this.length++;
        this._up(this.length - 1);
      }, "push");
      TinyQueue.prototype.pop = /* @__PURE__ */ __name(function pop() {
        if (this.length === 0) {
          return void 0;
        }
        var top = this.data[0];
        var bottom = this.data.pop();
        this.length--;
        if (this.length > 0) {
          this.data[0] = bottom;
          this._down(0);
        }
        return top;
      }, "pop");
      TinyQueue.prototype.peek = /* @__PURE__ */ __name(function peek() {
        return this.data[0];
      }, "peek");
      TinyQueue.prototype._up = /* @__PURE__ */ __name(function _up(pos) {
        var ref = this;
        var data = ref.data;
        var compare2 = ref.compare;
        var item = data[pos];
        while (pos > 0) {
          var parent = pos - 1 >> 1;
          var current = data[parent];
          if (compare2(item, current) >= 0) {
            break;
          }
          data[pos] = current;
          pos = parent;
        }
        data[pos] = item;
      }, "_up");
      TinyQueue.prototype._down = /* @__PURE__ */ __name(function _down(pos) {
        var ref = this;
        var data = ref.data;
        var compare2 = ref.compare;
        var halfLength = this.length >> 1;
        var item = data[pos];
        while (pos < halfLength) {
          var left = (pos << 1) + 1;
          var best = data[left];
          var right = left + 1;
          if (right < this.length && compare2(data[right], best) < 0) {
            left = right;
            best = data[right];
          }
          if (compare2(best, item) >= 0) {
            break;
          }
          data[pos] = best;
          pos = left;
        }
        data[pos] = item;
      }, "_down");
      function defaultCompare(a, b) {
        return a < b ? -1 : a > b ? 1 : 0;
      }
      __name(defaultCompare, "defaultCompare");
      return TinyQueue;
    });
  }
});

// worker/node_modules/point-in-polygon/flat.js
var require_flat = __commonJS({
  "worker/node_modules/point-in-polygon/flat.js"(exports, module) {
    module.exports = /* @__PURE__ */ __name(function pointInPolygonFlat(point2, vs, start, end) {
      var x = point2[0], y = point2[1];
      var inside = false;
      if (start === void 0) start = 0;
      if (end === void 0) end = vs.length;
      var len = (end - start) / 2;
      for (var i = 0, j = len - 1; i < len; j = i++) {
        var xi = vs[start + i * 2 + 0], yi = vs[start + i * 2 + 1];
        var xj = vs[start + j * 2 + 0], yj = vs[start + j * 2 + 1];
        var intersect4 = yi > y !== yj > y && x < (xj - xi) * (y - yi) / (yj - yi) + xi;
        if (intersect4) inside = !inside;
      }
      return inside;
    }, "pointInPolygonFlat");
  }
});

// worker/node_modules/point-in-polygon/nested.js
var require_nested = __commonJS({
  "worker/node_modules/point-in-polygon/nested.js"(exports, module) {
    module.exports = /* @__PURE__ */ __name(function pointInPolygonNested(point2, vs, start, end) {
      var x = point2[0], y = point2[1];
      var inside = false;
      if (start === void 0) start = 0;
      if (end === void 0) end = vs.length;
      var len = end - start;
      for (var i = 0, j = len - 1; i < len; j = i++) {
        var xi = vs[i + start][0], yi = vs[i + start][1];
        var xj = vs[j + start][0], yj = vs[j + start][1];
        var intersect4 = yi > y !== yj > y && x < (xj - xi) * (y - yi) / (yj - yi) + xi;
        if (intersect4) inside = !inside;
      }
      return inside;
    }, "pointInPolygonNested");
  }
});

// worker/node_modules/point-in-polygon/index.js
var require_point_in_polygon = __commonJS({
  "worker/node_modules/point-in-polygon/index.js"(exports, module) {
    var pointInPolygonFlat = require_flat();
    var pointInPolygonNested = require_nested();
    module.exports = /* @__PURE__ */ __name(function pointInPolygon(point2, vs, start, end) {
      if (vs.length > 0 && Array.isArray(vs[0])) {
        return pointInPolygonNested(point2, vs, start, end);
      } else {
        return pointInPolygonFlat(point2, vs, start, end);
      }
    }, "pointInPolygon");
    module.exports.nested = pointInPolygonNested;
    module.exports.flat = pointInPolygonFlat;
  }
});

// worker/node_modules/robust-predicates/umd/orient2d.min.js
var require_orient2d_min = __commonJS({
  "worker/node_modules/robust-predicates/umd/orient2d.min.js"(exports, module) {
    !(function(t, e) {
      "object" == typeof exports && "undefined" != typeof module ? e(exports) : "function" == typeof define && define.amd ? define(["exports"], e) : e((t = t || self).predicates = {});
    })(exports, function(t) {
      "use strict";
      const e = 134217729, n = 33306690738754706e-32;
      function r(t2, e3, n2, r2, o2) {
        let f2, i2, u2, c2, s2 = e3[0], a2 = r2[0], d2 = 0, l2 = 0;
        a2 > s2 == a2 > -s2 ? (f2 = s2, s2 = e3[++d2]) : (f2 = a2, a2 = r2[++l2]);
        let p = 0;
        if (d2 < t2 && l2 < n2) for (a2 > s2 == a2 > -s2 ? (u2 = f2 - ((i2 = s2 + f2) - s2), s2 = e3[++d2]) : (u2 = f2 - ((i2 = a2 + f2) - a2), a2 = r2[++l2]), f2 = i2, 0 !== u2 && (o2[p++] = u2); d2 < t2 && l2 < n2; ) a2 > s2 == a2 > -s2 ? (u2 = f2 - ((i2 = f2 + s2) - (c2 = i2 - f2)) + (s2 - c2), s2 = e3[++d2]) : (u2 = f2 - ((i2 = f2 + a2) - (c2 = i2 - f2)) + (a2 - c2), a2 = r2[++l2]), f2 = i2, 0 !== u2 && (o2[p++] = u2);
        for (; d2 < t2; ) u2 = f2 - ((i2 = f2 + s2) - (c2 = i2 - f2)) + (s2 - c2), s2 = e3[++d2], f2 = i2, 0 !== u2 && (o2[p++] = u2);
        for (; l2 < n2; ) u2 = f2 - ((i2 = f2 + a2) - (c2 = i2 - f2)) + (a2 - c2), a2 = r2[++l2], f2 = i2, 0 !== u2 && (o2[p++] = u2);
        return 0 === f2 && 0 !== p || (o2[p++] = f2), p;
      }
      __name(r, "r");
      function o(t2) {
        return new Float64Array(t2);
      }
      __name(o, "o");
      const f = 33306690738754716e-32, i = 22204460492503146e-32, u = 11093356479670487e-47, c = o(4), s = o(8), a = o(12), d = o(16), l = o(4);
      t.orient2d = function(t2, o2, p, b, y, h) {
        const M = (o2 - h) * (p - y), x = (t2 - y) * (b - h), j = M - x;
        if (0 === M || 0 === x || M > 0 != x > 0) return j;
        const m = Math.abs(M + x);
        return Math.abs(j) >= f * m ? j : -(function(t3, o3, f2, p2, b2, y2, h2) {
          let M2, x2, j2, m2, _, v, w, A, F, O, P, g, k, q, z2, B, C, D;
          const E = t3 - b2, G = f2 - b2, H = o3 - y2, I = p2 - y2;
          _ = (z2 = (A = E - (w = (v = e * E) - (v - E))) * (O = I - (F = (v = e * I) - (v - I))) - ((q = E * I) - w * F - A * F - w * O)) - (P = z2 - (C = (A = H - (w = (v = e * H) - (v - H))) * (O = G - (F = (v = e * G) - (v - G))) - ((B = H * G) - w * F - A * F - w * O))), c[0] = z2 - (P + _) + (_ - C), _ = (k = q - ((g = q + P) - (_ = g - q)) + (P - _)) - (P = k - B), c[1] = k - (P + _) + (_ - B), _ = (D = g + P) - g, c[2] = g - (D - _) + (P - _), c[3] = D;
          let J = (function(t4, e3) {
            let n2 = e3[0];
            for (let r2 = 1; r2 < t4; r2++) n2 += e3[r2];
            return n2;
          })(4, c), K = i * h2;
          if (J >= K || -J >= K) return J;
          if (M2 = t3 - (E + (_ = t3 - E)) + (_ - b2), j2 = f2 - (G + (_ = f2 - G)) + (_ - b2), x2 = o3 - (H + (_ = o3 - H)) + (_ - y2), m2 = p2 - (I + (_ = p2 - I)) + (_ - y2), 0 === M2 && 0 === x2 && 0 === j2 && 0 === m2) return J;
          if (K = u * h2 + n * Math.abs(J), (J += E * m2 + I * M2 - (H * j2 + G * x2)) >= K || -J >= K) return J;
          _ = (z2 = (A = M2 - (w = (v = e * M2) - (v - M2))) * (O = I - (F = (v = e * I) - (v - I))) - ((q = M2 * I) - w * F - A * F - w * O)) - (P = z2 - (C = (A = x2 - (w = (v = e * x2) - (v - x2))) * (O = G - (F = (v = e * G) - (v - G))) - ((B = x2 * G) - w * F - A * F - w * O))), l[0] = z2 - (P + _) + (_ - C), _ = (k = q - ((g = q + P) - (_ = g - q)) + (P - _)) - (P = k - B), l[1] = k - (P + _) + (_ - B), _ = (D = g + P) - g, l[2] = g - (D - _) + (P - _), l[3] = D;
          const L = r(4, c, 4, l, s);
          _ = (z2 = (A = E - (w = (v = e * E) - (v - E))) * (O = m2 - (F = (v = e * m2) - (v - m2))) - ((q = E * m2) - w * F - A * F - w * O)) - (P = z2 - (C = (A = H - (w = (v = e * H) - (v - H))) * (O = j2 - (F = (v = e * j2) - (v - j2))) - ((B = H * j2) - w * F - A * F - w * O))), l[0] = z2 - (P + _) + (_ - C), _ = (k = q - ((g = q + P) - (_ = g - q)) + (P - _)) - (P = k - B), l[1] = k - (P + _) + (_ - B), _ = (D = g + P) - g, l[2] = g - (D - _) + (P - _), l[3] = D;
          const N = r(L, s, 4, l, a);
          _ = (z2 = (A = M2 - (w = (v = e * M2) - (v - M2))) * (O = m2 - (F = (v = e * m2) - (v - m2))) - ((q = M2 * m2) - w * F - A * F - w * O)) - (P = z2 - (C = (A = x2 - (w = (v = e * x2) - (v - x2))) * (O = j2 - (F = (v = e * j2) - (v - j2))) - ((B = x2 * j2) - w * F - A * F - w * O))), l[0] = z2 - (P + _) + (_ - C), _ = (k = q - ((g = q + P) - (_ = g - q)) + (P - _)) - (P = k - B), l[1] = k - (P + _) + (_ - B), _ = (D = g + P) - g, l[2] = g - (D - _) + (P - _), l[3] = D;
          const Q = r(N, a, 4, l, d);
          return d[Q - 1];
        })(t2, o2, p, b, y, h, m);
      }, t.orient2dfast = function(t2, e3, n2, r2, o2, f2) {
        return (e3 - f2) * (n2 - o2) - (t2 - o2) * (r2 - f2);
      }, Object.defineProperty(t, "__esModule", { value: true });
    });
  }
});

// worker/node_modules/concaveman/index.js
var require_concaveman = __commonJS({
  "worker/node_modules/concaveman/index.js"(exports, module) {
    "use strict";
    var RBush = require_rbush_min();
    var Queue2 = require_tinyqueue();
    var pointInPolygon = require_point_in_polygon();
    var orient = require_orient2d_min().orient2d;
    if (Queue2.default) {
      Queue2 = Queue2.default;
    }
    module.exports = concaveman2;
    module.exports.default = concaveman2;
    function concaveman2(points, concavity, lengthThreshold) {
      concavity = Math.max(0, concavity === void 0 ? 2 : concavity);
      lengthThreshold = lengthThreshold || 0;
      var hull = fastConvexHull(points);
      var tree = new RBush(16);
      tree.toBBox = function(a2) {
        return {
          minX: a2[0],
          minY: a2[1],
          maxX: a2[0],
          maxY: a2[1]
        };
      };
      tree.compareMinX = function(a2, b2) {
        return a2[0] - b2[0];
      };
      tree.compareMinY = function(a2, b2) {
        return a2[1] - b2[1];
      };
      tree.load(points);
      var queue = [];
      for (var i = 0, last; i < hull.length; i++) {
        var p = hull[i];
        tree.remove(p);
        last = insertNode(p, last);
        queue.push(last);
      }
      var segTree = new RBush(16);
      for (i = 0; i < queue.length; i++) segTree.insert(updateBBox(queue[i]));
      var sqConcavity = concavity * concavity;
      var sqLenThreshold = lengthThreshold * lengthThreshold;
      while (queue.length) {
        var node = queue.shift();
        var a = node.p;
        var b = node.next.p;
        var sqLen = getSqDist(a, b);
        if (sqLen < sqLenThreshold) continue;
        var maxSqLen = sqLen / sqConcavity;
        p = findCandidate(tree, node.prev.p, a, b, node.next.next.p, maxSqLen, segTree);
        if (p && Math.min(getSqDist(p, a), getSqDist(p, b)) <= maxSqLen) {
          queue.push(node);
          queue.push(insertNode(p, node));
          tree.remove(p);
          segTree.remove(node);
          segTree.insert(updateBBox(node));
          segTree.insert(updateBBox(node.next));
        }
      }
      node = last;
      var concave = [];
      do {
        concave.push(node.p);
        node = node.next;
      } while (node !== last);
      concave.push(node.p);
      return concave;
    }
    __name(concaveman2, "concaveman");
    function findCandidate(tree, a, b, c, d, maxDist, segTree) {
      var queue = new Queue2([], compareDist);
      var node = tree.data;
      while (node) {
        for (var i = 0; i < node.children.length; i++) {
          var child = node.children[i];
          var dist = node.leaf ? sqSegDist(child, b, c) : sqSegBoxDist(b, c, child);
          if (dist > maxDist) continue;
          queue.push({
            node: child,
            dist
          });
        }
        while (queue.length && !queue.peek().node.children) {
          var item = queue.pop();
          var p = item.node;
          var d0 = sqSegDist(p, a, b);
          var d1 = sqSegDist(p, c, d);
          if (item.dist < d0 && item.dist < d1 && noIntersections(b, p, segTree) && noIntersections(c, p, segTree)) return p;
        }
        node = queue.pop();
        if (node) node = node.node;
      }
      return null;
    }
    __name(findCandidate, "findCandidate");
    function compareDist(a, b) {
      return a.dist - b.dist;
    }
    __name(compareDist, "compareDist");
    function sqSegBoxDist(a, b, bbox2) {
      if (inside(a, bbox2) || inside(b, bbox2)) return 0;
      var d1 = sqSegSegDist(a[0], a[1], b[0], b[1], bbox2.minX, bbox2.minY, bbox2.maxX, bbox2.minY);
      if (d1 === 0) return 0;
      var d2 = sqSegSegDist(a[0], a[1], b[0], b[1], bbox2.minX, bbox2.minY, bbox2.minX, bbox2.maxY);
      if (d2 === 0) return 0;
      var d3 = sqSegSegDist(a[0], a[1], b[0], b[1], bbox2.maxX, bbox2.minY, bbox2.maxX, bbox2.maxY);
      if (d3 === 0) return 0;
      var d4 = sqSegSegDist(a[0], a[1], b[0], b[1], bbox2.minX, bbox2.maxY, bbox2.maxX, bbox2.maxY);
      if (d4 === 0) return 0;
      return Math.min(d1, d2, d3, d4);
    }
    __name(sqSegBoxDist, "sqSegBoxDist");
    function inside(a, bbox2) {
      return a[0] >= bbox2.minX && a[0] <= bbox2.maxX && a[1] >= bbox2.minY && a[1] <= bbox2.maxY;
    }
    __name(inside, "inside");
    function noIntersections(a, b, segTree) {
      var minX = Math.min(a[0], b[0]);
      var minY = Math.min(a[1], b[1]);
      var maxX = Math.max(a[0], b[0]);
      var maxY = Math.max(a[1], b[1]);
      var edges = segTree.search({ minX, minY, maxX, maxY });
      for (var i = 0; i < edges.length; i++) {
        if (intersects(edges[i].p, edges[i].next.p, a, b)) return false;
      }
      return true;
    }
    __name(noIntersections, "noIntersections");
    function cross(p1, p2, p3) {
      return orient(p1[0], p1[1], p2[0], p2[1], p3[0], p3[1]);
    }
    __name(cross, "cross");
    function intersects(p1, q1, p2, q2) {
      return p1 !== q2 && q1 !== p2 && cross(p1, q1, p2) > 0 !== cross(p1, q1, q2) > 0 && cross(p2, q2, p1) > 0 !== cross(p2, q2, q1) > 0;
    }
    __name(intersects, "intersects");
    function updateBBox(node) {
      var p1 = node.p;
      var p2 = node.next.p;
      node.minX = Math.min(p1[0], p2[0]);
      node.minY = Math.min(p1[1], p2[1]);
      node.maxX = Math.max(p1[0], p2[0]);
      node.maxY = Math.max(p1[1], p2[1]);
      return node;
    }
    __name(updateBBox, "updateBBox");
    function fastConvexHull(points) {
      var left = points[0];
      var top = points[0];
      var right = points[0];
      var bottom = points[0];
      for (var i = 0; i < points.length; i++) {
        var p = points[i];
        if (p[0] < left[0]) left = p;
        if (p[0] > right[0]) right = p;
        if (p[1] < top[1]) top = p;
        if (p[1] > bottom[1]) bottom = p;
      }
      var cull = [left, top, right, bottom];
      var filtered = cull.slice();
      for (i = 0; i < points.length; i++) {
        if (!pointInPolygon(points[i], cull)) filtered.push(points[i]);
      }
      return convexHull(filtered);
    }
    __name(fastConvexHull, "fastConvexHull");
    function insertNode(p, prev) {
      var node = {
        p,
        prev: null,
        next: null,
        minX: 0,
        minY: 0,
        maxX: 0,
        maxY: 0
      };
      if (!prev) {
        node.prev = node;
        node.next = node;
      } else {
        node.next = prev.next;
        node.prev = prev;
        prev.next.prev = node;
        prev.next = node;
      }
      return node;
    }
    __name(insertNode, "insertNode");
    function getSqDist(p1, p2) {
      var dx = p1[0] - p2[0], dy = p1[1] - p2[1];
      return dx * dx + dy * dy;
    }
    __name(getSqDist, "getSqDist");
    function sqSegDist(p, p1, p2) {
      var x = p1[0], y = p1[1], dx = p2[0] - x, dy = p2[1] - y;
      if (dx !== 0 || dy !== 0) {
        var t = ((p[0] - x) * dx + (p[1] - y) * dy) / (dx * dx + dy * dy);
        if (t > 1) {
          x = p2[0];
          y = p2[1];
        } else if (t > 0) {
          x += dx * t;
          y += dy * t;
        }
      }
      dx = p[0] - x;
      dy = p[1] - y;
      return dx * dx + dy * dy;
    }
    __name(sqSegDist, "sqSegDist");
    function sqSegSegDist(x02, y02, x12, y12, x2, y2, x3, y3) {
      var ux = x12 - x02;
      var uy = y12 - y02;
      var vx = x3 - x2;
      var vy = y3 - y2;
      var wx = x02 - x2;
      var wy = y02 - y2;
      var a = ux * ux + uy * uy;
      var b = ux * vx + uy * vy;
      var c = vx * vx + vy * vy;
      var d = ux * wx + uy * wy;
      var e = vx * wx + vy * wy;
      var D = a * c - b * b;
      var sc, sN, tc, tN;
      var sD = D;
      var tD = D;
      if (D === 0) {
        sN = 0;
        sD = 1;
        tN = e;
        tD = c;
      } else {
        sN = b * e - c * d;
        tN = a * e - b * d;
        if (sN < 0) {
          sN = 0;
          tN = e;
          tD = c;
        } else if (sN > sD) {
          sN = sD;
          tN = e + b;
          tD = c;
        }
      }
      if (tN < 0) {
        tN = 0;
        if (-d < 0) sN = 0;
        else if (-d > a) sN = sD;
        else {
          sN = -d;
          sD = a;
        }
      } else if (tN > tD) {
        tN = tD;
        if (-d + b < 0) sN = 0;
        else if (-d + b > a) sN = sD;
        else {
          sN = -d + b;
          sD = a;
        }
      }
      sc = sN === 0 ? 0 : sN / sD;
      tc = tN === 0 ? 0 : tN / tD;
      var cx = (1 - sc) * x02 + sc * x12;
      var cy = (1 - sc) * y02 + sc * y12;
      var cx2 = (1 - tc) * x2 + tc * x3;
      var cy2 = (1 - tc) * y2 + tc * y3;
      var dx = cx2 - cx;
      var dy = cy2 - cy;
      return dx * dx + dy * dy;
    }
    __name(sqSegSegDist, "sqSegSegDist");
    function compareByX(a, b) {
      return a[0] === b[0] ? a[1] - b[1] : a[0] - b[0];
    }
    __name(compareByX, "compareByX");
    function convexHull(points) {
      points.sort(compareByX);
      var lower = [];
      for (var i = 0; i < points.length; i++) {
        while (lower.length >= 2 && cross(lower[lower.length - 2], lower[lower.length - 1], points[i]) <= 0) {
          lower.pop();
        }
        lower.push(points[i]);
      }
      var upper = [];
      for (var ii = points.length - 1; ii >= 0; ii--) {
        while (upper.length >= 2 && cross(upper[upper.length - 2], upper[upper.length - 1], points[ii]) <= 0) {
          upper.pop();
        }
        upper.push(points[ii]);
      }
      upper.pop();
      lower.pop();
      return lower.concat(upper);
    }
    __name(convexHull, "convexHull");
  }
});

// worker/node_modules/@turf/convex/dist/esm/index.js
function convex(geojson, options = {}) {
  options.concavity = options.concavity || Infinity;
  const points = [];
  coordEach(geojson, (coord) => {
    points.push([coord[0], coord[1]]);
  });
  if (!points.length) {
    return null;
  }
  const convexHull = (0, import_concaveman.default)(points, options.concavity);
  if (convexHull.length > 3) {
    return polygon([convexHull]);
  }
  return null;
}
var import_concaveman;
var init_esm9 = __esm({
  "worker/node_modules/@turf/convex/dist/esm/index.js"() {
    init_esm();
    init_esm4();
    import_concaveman = __toESM(require_concaveman(), 1);
    __name(convex, "convex");
  }
});

// worker/node_modules/@turf/clone/dist/esm/index.js
var init_esm10 = __esm({
  "worker/node_modules/@turf/clone/dist/esm/index.js"() {
  }
});

// worker/node_modules/@turf/clusters/dist/esm/index.js
var init_esm11 = __esm({
  "worker/node_modules/@turf/clusters/dist/esm/index.js"() {
  }
});

// worker/node_modules/bignumber.js/bignumber.mjs
function clone(configObject) {
  var div, convertBase, parseNumeric, P = BigNumber2.prototype = { constructor: BigNumber2, toString: null, valueOf: null }, ONE = new BigNumber2(1), DECIMAL_PLACES = 20, ROUNDING_MODE = 4, TO_EXP_NEG = -7, TO_EXP_POS = 21, MIN_EXP = -1e7, MAX_EXP = 1e7, CRYPTO = false, MODULO_MODE = 1, POW_PRECISION = 0, FORMAT = {
    prefix: "",
    groupSize: 3,
    secondaryGroupSize: 0,
    groupSeparator: ",",
    decimalSeparator: ".",
    fractionGroupSize: 0,
    fractionGroupSeparator: "\xA0",
    // non-breaking space
    suffix: ""
  }, ALPHABET = "0123456789abcdefghijklmnopqrstuvwxyz", alphabetHasNormalDecimalDigits = true;
  function BigNumber2(v, b) {
    var alphabet, c, caseChanged, e, i, isNum, len, str, x = this;
    if (!(x instanceof BigNumber2)) return new BigNumber2(v, b);
    if (b == null) {
      if (v && v._isBigNumber === true) {
        x.s = v.s;
        if (!v.c || v.e > MAX_EXP) {
          x.c = x.e = null;
        } else if (v.e < MIN_EXP) {
          x.c = [x.e = 0];
        } else {
          x.e = v.e;
          x.c = v.c.slice();
        }
        return;
      }
      if ((isNum = typeof v == "number") && v * 0 == 0) {
        x.s = 1 / v < 0 ? (v = -v, -1) : 1;
        if (v === ~~v) {
          for (e = 0, i = v; i >= 10; i /= 10, e++) ;
          if (e > MAX_EXP) {
            x.c = x.e = null;
          } else {
            x.e = e;
            x.c = [v];
          }
          return;
        }
        str = String(v);
      } else {
        if (!isNumeric.test(str = String(v))) return parseNumeric(x, str, isNum);
        x.s = str.charCodeAt(0) == 45 ? (str = str.slice(1), -1) : 1;
      }
      if ((e = str.indexOf(".")) > -1) str = str.replace(".", "");
      if ((i = str.search(/e/i)) > 0) {
        if (e < 0) e = i;
        e += +str.slice(i + 1);
        str = str.substring(0, i);
      } else if (e < 0) {
        e = str.length;
      }
    } else {
      intCheck(b, 2, ALPHABET.length, "Base");
      if (b == 10 && alphabetHasNormalDecimalDigits) {
        x = new BigNumber2(v);
        return round(x, DECIMAL_PLACES + x.e + 1, ROUNDING_MODE);
      }
      str = String(v);
      if (isNum = typeof v == "number") {
        if (v * 0 != 0) return parseNumeric(x, str, isNum, b);
        x.s = 1 / v < 0 ? (str = str.slice(1), -1) : 1;
        if (BigNumber2.DEBUG && str.replace(/^0\.0*|\./, "").length > 15) {
          throw Error(tooManyDigits + v);
        }
      } else {
        x.s = str.charCodeAt(0) === 45 ? (str = str.slice(1), -1) : 1;
      }
      alphabet = ALPHABET.slice(0, b);
      e = i = 0;
      for (len = str.length; i < len; i++) {
        if (alphabet.indexOf(c = str.charAt(i)) < 0) {
          if (c == ".") {
            if (i > e) {
              e = len;
              continue;
            }
          } else if (!caseChanged) {
            if (str == str.toUpperCase() && (str = str.toLowerCase()) || str == str.toLowerCase() && (str = str.toUpperCase())) {
              caseChanged = true;
              i = -1;
              e = 0;
              continue;
            }
          }
          return parseNumeric(x, String(v), isNum, b);
        }
      }
      isNum = false;
      str = convertBase(str, b, 10, x.s);
      if ((e = str.indexOf(".")) > -1) str = str.replace(".", "");
      else e = str.length;
    }
    for (i = 0; str.charCodeAt(i) === 48; i++) ;
    for (len = str.length; str.charCodeAt(--len) === 48; ) ;
    if (str = str.slice(i, ++len)) {
      len -= i;
      if (isNum && BigNumber2.DEBUG && len > 15 && (v > MAX_SAFE_INTEGER || v !== mathfloor(v))) {
        throw Error(tooManyDigits + x.s * v);
      }
      if ((e = e - i - 1) > MAX_EXP) {
        x.c = x.e = null;
      } else if (e < MIN_EXP) {
        x.c = [x.e = 0];
      } else {
        x.e = e;
        x.c = [];
        i = (e + 1) % LOG_BASE;
        if (e < 0) i += LOG_BASE;
        if (i < len) {
          if (i) x.c.push(+str.slice(0, i));
          for (len -= LOG_BASE; i < len; ) {
            x.c.push(+str.slice(i, i += LOG_BASE));
          }
          i = LOG_BASE - (str = str.slice(i)).length;
        } else {
          i -= len;
        }
        for (; i--; str += "0") ;
        x.c.push(+str);
      }
    } else {
      x.c = [x.e = 0];
    }
  }
  __name(BigNumber2, "BigNumber");
  BigNumber2.clone = clone;
  BigNumber2.ROUND_UP = 0;
  BigNumber2.ROUND_DOWN = 1;
  BigNumber2.ROUND_CEIL = 2;
  BigNumber2.ROUND_FLOOR = 3;
  BigNumber2.ROUND_HALF_UP = 4;
  BigNumber2.ROUND_HALF_DOWN = 5;
  BigNumber2.ROUND_HALF_EVEN = 6;
  BigNumber2.ROUND_HALF_CEIL = 7;
  BigNumber2.ROUND_HALF_FLOOR = 8;
  BigNumber2.EUCLID = 9;
  BigNumber2.config = BigNumber2.set = function(obj) {
    var p, v;
    if (obj != null) {
      if (typeof obj == "object") {
        if (obj.hasOwnProperty(p = "DECIMAL_PLACES")) {
          v = obj[p];
          intCheck(v, 0, MAX, p);
          DECIMAL_PLACES = v;
        }
        if (obj.hasOwnProperty(p = "ROUNDING_MODE")) {
          v = obj[p];
          intCheck(v, 0, 8, p);
          ROUNDING_MODE = v;
        }
        if (obj.hasOwnProperty(p = "EXPONENTIAL_AT")) {
          v = obj[p];
          if (v && v.pop) {
            intCheck(v[0], -MAX, 0, p);
            intCheck(v[1], 0, MAX, p);
            TO_EXP_NEG = v[0];
            TO_EXP_POS = v[1];
          } else {
            intCheck(v, -MAX, MAX, p);
            TO_EXP_NEG = -(TO_EXP_POS = v < 0 ? -v : v);
          }
        }
        if (obj.hasOwnProperty(p = "RANGE")) {
          v = obj[p];
          if (v && v.pop) {
            intCheck(v[0], -MAX, -1, p);
            intCheck(v[1], 1, MAX, p);
            MIN_EXP = v[0];
            MAX_EXP = v[1];
          } else {
            intCheck(v, -MAX, MAX, p);
            if (v) {
              MIN_EXP = -(MAX_EXP = v < 0 ? -v : v);
            } else {
              throw Error(bignumberError + p + " cannot be zero: " + v);
            }
          }
        }
        if (obj.hasOwnProperty(p = "CRYPTO")) {
          v = obj[p];
          if (v === !!v) {
            if (v) {
              if (typeof crypto != "undefined" && crypto && (crypto.getRandomValues || crypto.randomBytes)) {
                CRYPTO = v;
              } else {
                CRYPTO = !v;
                throw Error(bignumberError + "crypto unavailable");
              }
            } else {
              CRYPTO = v;
            }
          } else {
            throw Error(bignumberError + p + " not true or false: " + v);
          }
        }
        if (obj.hasOwnProperty(p = "MODULO_MODE")) {
          v = obj[p];
          intCheck(v, 0, 9, p);
          MODULO_MODE = v;
        }
        if (obj.hasOwnProperty(p = "POW_PRECISION")) {
          v = obj[p];
          intCheck(v, 0, MAX, p);
          POW_PRECISION = v;
        }
        if (obj.hasOwnProperty(p = "FORMAT")) {
          v = obj[p];
          if (typeof v == "object") FORMAT = v;
          else throw Error(bignumberError + p + " not an object: " + v);
        }
        if (obj.hasOwnProperty(p = "ALPHABET")) {
          v = obj[p];
          if (typeof v == "string" && !/^.?$|[+\-.\s]|(.).*\1/.test(v)) {
            alphabetHasNormalDecimalDigits = v.slice(0, 10) == "0123456789";
            ALPHABET = v;
          } else {
            throw Error(bignumberError + p + " invalid: " + v);
          }
        }
      } else {
        throw Error(bignumberError + "Object expected: " + obj);
      }
    }
    return {
      DECIMAL_PLACES,
      ROUNDING_MODE,
      EXPONENTIAL_AT: [TO_EXP_NEG, TO_EXP_POS],
      RANGE: [MIN_EXP, MAX_EXP],
      CRYPTO,
      MODULO_MODE,
      POW_PRECISION,
      FORMAT,
      ALPHABET
    };
  };
  BigNumber2.isBigNumber = function(v) {
    if (!v || v._isBigNumber !== true) return false;
    if (!BigNumber2.DEBUG) return true;
    var i, n, c = v.c, e = v.e, s = v.s;
    out: if ({}.toString.call(c) == "[object Array]") {
      if ((s === 1 || s === -1) && e >= -MAX && e <= MAX && e === mathfloor(e)) {
        if (c[0] === 0) {
          if (e === 0 && c.length === 1) return true;
          break out;
        }
        i = (e + 1) % LOG_BASE;
        if (i < 1) i += LOG_BASE;
        if (String(c[0]).length == i) {
          for (i = 0; i < c.length; i++) {
            n = c[i];
            if (n < 0 || n >= BASE || n !== mathfloor(n)) break out;
          }
          if (n !== 0) return true;
        }
      }
    } else if (c === null && e === null && (s === null || s === 1 || s === -1)) {
      return true;
    }
    throw Error(bignumberError + "Invalid BigNumber: " + v);
  };
  BigNumber2.maximum = BigNumber2.max = function() {
    return maxOrMin(arguments, -1);
  };
  BigNumber2.minimum = BigNumber2.min = function() {
    return maxOrMin(arguments, 1);
  };
  BigNumber2.random = (function() {
    var pow2_53 = 9007199254740992;
    var random53bitInt = Math.random() * pow2_53 & 2097151 ? function() {
      return mathfloor(Math.random() * pow2_53);
    } : function() {
      return (Math.random() * 1073741824 | 0) * 8388608 + (Math.random() * 8388608 | 0);
    };
    return function(dp) {
      var a, b, e, k, v, i = 0, c = [], rand = new BigNumber2(ONE);
      if (dp == null) dp = DECIMAL_PLACES;
      else intCheck(dp, 0, MAX);
      k = mathceil(dp / LOG_BASE);
      if (CRYPTO) {
        if (crypto.getRandomValues) {
          a = crypto.getRandomValues(new Uint32Array(k *= 2));
          for (; i < k; ) {
            v = a[i] * 131072 + (a[i + 1] >>> 11);
            if (v >= 9e15) {
              b = crypto.getRandomValues(new Uint32Array(2));
              a[i] = b[0];
              a[i + 1] = b[1];
            } else {
              c.push(v % 1e14);
              i += 2;
            }
          }
          i = k / 2;
        } else if (crypto.randomBytes) {
          a = crypto.randomBytes(k *= 7);
          for (; i < k; ) {
            v = (a[i] & 31) * 281474976710656 + a[i + 1] * 1099511627776 + a[i + 2] * 4294967296 + a[i + 3] * 16777216 + (a[i + 4] << 16) + (a[i + 5] << 8) + a[i + 6];
            if (v >= 9e15) {
              crypto.randomBytes(7).copy(a, i);
            } else {
              c.push(v % 1e14);
              i += 7;
            }
          }
          i = k / 7;
        } else {
          CRYPTO = false;
          throw Error(bignumberError + "crypto unavailable");
        }
      }
      if (!CRYPTO) {
        for (; i < k; ) {
          v = random53bitInt();
          if (v < 9e15) c[i++] = v % 1e14;
        }
      }
      k = c[--i];
      dp %= LOG_BASE;
      if (k && dp) {
        v = POWS_TEN[LOG_BASE - dp];
        c[i] = mathfloor(k / v) * v;
      }
      for (; c[i] === 0; c.pop(), i--) ;
      if (i < 0) {
        c = [e = 0];
      } else {
        for (e = -1; c[0] === 0; c.splice(0, 1), e -= LOG_BASE) ;
        for (i = 1, v = c[0]; v >= 10; v /= 10, i++) ;
        if (i < LOG_BASE) e -= LOG_BASE - i;
      }
      rand.e = e;
      rand.c = c;
      return rand;
    };
  })();
  BigNumber2.sum = function() {
    var i = 1, args = arguments, sum2 = new BigNumber2(args[0]);
    for (; i < args.length; ) sum2 = sum2.plus(args[i++]);
    return sum2;
  };
  convertBase = /* @__PURE__ */ (function() {
    var decimal = "0123456789";
    function toBaseOut(str, baseIn, baseOut, alphabet) {
      var j, arr = [0], arrL, i = 0, len = str.length;
      for (; i < len; ) {
        for (arrL = arr.length; arrL--; arr[arrL] *= baseIn) ;
        arr[0] += alphabet.indexOf(str.charAt(i++));
        for (j = 0; j < arr.length; j++) {
          if (arr[j] > baseOut - 1) {
            if (arr[j + 1] == null) arr[j + 1] = 0;
            arr[j + 1] += arr[j] / baseOut | 0;
            arr[j] %= baseOut;
          }
        }
      }
      return arr.reverse();
    }
    __name(toBaseOut, "toBaseOut");
    return function(str, baseIn, baseOut, sign2, callerIsToString) {
      var alphabet, d, e, k, r, x, xc, y, i = str.indexOf("."), dp = DECIMAL_PLACES, rm = ROUNDING_MODE;
      if (i >= 0) {
        k = POW_PRECISION;
        POW_PRECISION = 0;
        str = str.replace(".", "");
        y = new BigNumber2(baseIn);
        x = y.pow(str.length - i);
        POW_PRECISION = k;
        y.c = toBaseOut(
          toFixedPoint(coeffToString(x.c), x.e, "0"),
          10,
          baseOut,
          decimal
        );
        y.e = y.c.length;
      }
      xc = toBaseOut(str, baseIn, baseOut, callerIsToString ? (alphabet = ALPHABET, decimal) : (alphabet = decimal, ALPHABET));
      e = k = xc.length;
      for (; xc[--k] == 0; xc.pop()) ;
      if (!xc[0]) return alphabet.charAt(0);
      if (i < 0) {
        --e;
      } else {
        x.c = xc;
        x.e = e;
        x.s = sign2;
        x = div(x, y, dp, rm, baseOut);
        xc = x.c;
        r = x.r;
        e = x.e;
      }
      d = e + dp + 1;
      i = xc[d];
      k = baseOut / 2;
      r = r || d < 0 || xc[d + 1] != null;
      r = rm < 4 ? (i != null || r) && (rm == 0 || rm == (x.s < 0 ? 3 : 2)) : i > k || i == k && (rm == 4 || r || rm == 6 && xc[d - 1] & 1 || rm == (x.s < 0 ? 8 : 7));
      if (d < 1 || !xc[0]) {
        str = r ? toFixedPoint(alphabet.charAt(1), -dp, alphabet.charAt(0)) : alphabet.charAt(0);
      } else {
        xc.length = d;
        if (r) {
          for (--baseOut; ++xc[--d] > baseOut; ) {
            xc[d] = 0;
            if (!d) {
              ++e;
              xc = [1].concat(xc);
            }
          }
        }
        for (k = xc.length; !xc[--k]; ) ;
        for (i = 0, str = ""; i <= k; str += alphabet.charAt(xc[i++])) ;
        str = toFixedPoint(str, e, alphabet.charAt(0));
      }
      return str;
    };
  })();
  div = /* @__PURE__ */ (function() {
    function multiply(x, k, base) {
      var m, temp2, xlo, xhi, carry = 0, i = x.length, klo = k % SQRT_BASE, khi = k / SQRT_BASE | 0;
      for (x = x.slice(); i--; ) {
        xlo = x[i] % SQRT_BASE;
        xhi = x[i] / SQRT_BASE | 0;
        m = khi * xlo + xhi * klo;
        temp2 = klo * xlo + m % SQRT_BASE * SQRT_BASE + carry;
        carry = (temp2 / base | 0) + (m / SQRT_BASE | 0) + khi * xhi;
        x[i] = temp2 % base;
      }
      if (carry) x = [carry].concat(x);
      return x;
    }
    __name(multiply, "multiply");
    function compare2(a, b, aL, bL) {
      var i, cmp;
      if (aL != bL) {
        cmp = aL > bL ? 1 : -1;
      } else {
        for (i = cmp = 0; i < aL; i++) {
          if (a[i] != b[i]) {
            cmp = a[i] > b[i] ? 1 : -1;
            break;
          }
        }
      }
      return cmp;
    }
    __name(compare2, "compare");
    function subtract(a, b, aL, base) {
      var i = 0;
      for (; aL--; ) {
        a[aL] -= i;
        i = a[aL] < b[aL] ? 1 : 0;
        a[aL] = i * base + a[aL] - b[aL];
      }
      for (; !a[0] && a.length > 1; a.splice(0, 1)) ;
    }
    __name(subtract, "subtract");
    return function(x, y, dp, rm, base) {
      var cmp, e, i, more, n, prod, prodL, q, qc, rem, remL, rem0, xi, xL, yc0, yL, yz, s = x.s == y.s ? 1 : -1, xc = x.c, yc = y.c;
      if (!xc || !xc[0] || !yc || !yc[0]) {
        return new BigNumber2(
          // Return NaN if either NaN, or both Infinity or 0.
          !x.s || !y.s || (xc ? yc && xc[0] == yc[0] : !yc) ? NaN : (
            // Return ±0 if x is ±0 or y is ±Infinity, or return ±Infinity as y is ±0.
            xc && xc[0] == 0 || !yc ? s * 0 : s / 0
          )
        );
      }
      q = new BigNumber2(s);
      qc = q.c = [];
      e = x.e - y.e;
      s = dp + e + 1;
      if (!base) {
        base = BASE;
        e = bitFloor(x.e / LOG_BASE) - bitFloor(y.e / LOG_BASE);
        s = s / LOG_BASE | 0;
      }
      for (i = 0; yc[i] == (xc[i] || 0); i++) ;
      if (yc[i] > (xc[i] || 0)) e--;
      if (s < 0) {
        qc.push(1);
        more = true;
      } else {
        xL = xc.length;
        yL = yc.length;
        i = 0;
        s += 2;
        n = mathfloor(base / (yc[0] + 1));
        if (n > 1) {
          yc = multiply(yc, n, base);
          xc = multiply(xc, n, base);
          yL = yc.length;
          xL = xc.length;
        }
        xi = yL;
        rem = xc.slice(0, yL);
        remL = rem.length;
        for (; remL < yL; rem[remL++] = 0) ;
        yz = yc.slice();
        yz = [0].concat(yz);
        yc0 = yc[0];
        if (yc[1] >= base / 2) yc0++;
        do {
          n = 0;
          cmp = compare2(yc, rem, yL, remL);
          if (cmp < 0) {
            rem0 = rem[0];
            if (yL != remL) rem0 = rem0 * base + (rem[1] || 0);
            n = mathfloor(rem0 / yc0);
            if (n > 1) {
              if (n >= base) n = base - 1;
              prod = multiply(yc, n, base);
              prodL = prod.length;
              remL = rem.length;
              while (compare2(prod, rem, prodL, remL) == 1) {
                n--;
                subtract(prod, yL < prodL ? yz : yc, prodL, base);
                prodL = prod.length;
                cmp = 1;
              }
            } else {
              if (n == 0) {
                cmp = n = 1;
              }
              prod = yc.slice();
              prodL = prod.length;
            }
            if (prodL < remL) prod = [0].concat(prod);
            subtract(rem, prod, remL, base);
            remL = rem.length;
            if (cmp == -1) {
              while (compare2(yc, rem, yL, remL) < 1) {
                n++;
                subtract(rem, yL < remL ? yz : yc, remL, base);
                remL = rem.length;
              }
            }
          } else if (cmp === 0) {
            n++;
            rem = [0];
          }
          qc[i++] = n;
          if (rem[0]) {
            rem[remL++] = xc[xi] || 0;
          } else {
            rem = [xc[xi]];
            remL = 1;
          }
        } while ((xi++ < xL || rem[0] != null) && s--);
        more = rem[0] != null;
        if (!qc[0]) qc.splice(0, 1);
      }
      if (base == BASE) {
        for (i = 1, s = qc[0]; s >= 10; s /= 10, i++) ;
        round(q, dp + (q.e = i + e * LOG_BASE - 1) + 1, rm, more);
      } else {
        q.e = e;
        q.r = +more;
      }
      return q;
    };
  })();
  function format(n, i, rm, id) {
    var c0, e, ne3, len, str;
    if (rm == null) rm = ROUNDING_MODE;
    else intCheck(rm, 0, 8);
    if (!n.c) return n.toString();
    c0 = n.c[0];
    ne3 = n.e;
    if (i == null) {
      str = coeffToString(n.c);
      str = id == 1 || id == 2 && (ne3 <= TO_EXP_NEG || ne3 >= TO_EXP_POS) ? toExponential(str, ne3) : toFixedPoint(str, ne3, "0");
    } else {
      n = round(new BigNumber2(n), i, rm);
      e = n.e;
      str = coeffToString(n.c);
      len = str.length;
      if (id == 1 || id == 2 && (i <= e || e <= TO_EXP_NEG)) {
        for (; len < i; str += "0", len++) ;
        str = toExponential(str, e);
      } else {
        i -= ne3 + (id === 2 && e > ne3);
        str = toFixedPoint(str, e, "0");
        if (e + 1 > len) {
          if (--i > 0) for (str += "."; i--; str += "0") ;
        } else {
          i += e - len;
          if (i > 0) {
            if (e + 1 == len) str += ".";
            for (; i--; str += "0") ;
          }
        }
      }
    }
    return n.s < 0 && c0 ? "-" + str : str;
  }
  __name(format, "format");
  function maxOrMin(args, n) {
    var k, y, i = 1, x = new BigNumber2(args[0]);
    for (; i < args.length; i++) {
      y = new BigNumber2(args[i]);
      if (!y.s || (k = compare(x, y)) === n || k === 0 && x.s === n) {
        x = y;
      }
    }
    return x;
  }
  __name(maxOrMin, "maxOrMin");
  function normalise(n, c, e) {
    var i = 1, j = c.length;
    for (; !c[--j]; c.pop()) ;
    for (j = c[0]; j >= 10; j /= 10, i++) ;
    if ((e = i + e * LOG_BASE - 1) > MAX_EXP) {
      n.c = n.e = null;
    } else if (e < MIN_EXP) {
      n.c = [n.e = 0];
    } else {
      n.e = e;
      n.c = c;
    }
    return n;
  }
  __name(normalise, "normalise");
  parseNumeric = /* @__PURE__ */ (function() {
    var basePrefix = /^(-?)0([xbo])(?=\w[\w.]*$)/i, dotAfter = /^([^.]+)\.$/, dotBefore = /^\.([^.]+)$/, isInfinityOrNaN = /^-?(Infinity|NaN)$/, whitespaceOrPlus = /^\s*\+(?=[\w.])|^\s+|\s+$/g;
    return function(x, str, isNum, b) {
      var base, s = isNum ? str : str.replace(whitespaceOrPlus, "");
      if (isInfinityOrNaN.test(s)) {
        x.s = isNaN(s) ? null : s < 0 ? -1 : 1;
      } else {
        if (!isNum) {
          s = s.replace(basePrefix, function(m, p1, p2) {
            base = (p2 = p2.toLowerCase()) == "x" ? 16 : p2 == "b" ? 2 : 8;
            return !b || b == base ? p1 : m;
          });
          if (b) {
            base = b;
            s = s.replace(dotAfter, "$1").replace(dotBefore, "0.$1");
          }
          if (str != s) return new BigNumber2(s, base);
        }
        if (BigNumber2.DEBUG) {
          throw Error(bignumberError + "Not a" + (b ? " base " + b : "") + " number: " + str);
        }
        x.s = null;
      }
      x.c = x.e = null;
    };
  })();
  function round(x, sd, rm, r) {
    var d, i, j, k, n, ni, rd, xc = x.c, pows10 = POWS_TEN;
    if (xc) {
      out: {
        for (d = 1, k = xc[0]; k >= 10; k /= 10, d++) ;
        i = sd - d;
        if (i < 0) {
          i += LOG_BASE;
          j = sd;
          n = xc[ni = 0];
          rd = mathfloor(n / pows10[d - j - 1] % 10);
        } else {
          ni = mathceil((i + 1) / LOG_BASE);
          if (ni >= xc.length) {
            if (r) {
              for (; xc.length <= ni; xc.push(0)) ;
              n = rd = 0;
              d = 1;
              i %= LOG_BASE;
              j = i - LOG_BASE + 1;
            } else {
              break out;
            }
          } else {
            n = k = xc[ni];
            for (d = 1; k >= 10; k /= 10, d++) ;
            i %= LOG_BASE;
            j = i - LOG_BASE + d;
            rd = j < 0 ? 0 : mathfloor(n / pows10[d - j - 1] % 10);
          }
        }
        r = r || sd < 0 || // Are there any non-zero digits after the rounding digit?
        // The expression  n % pows10[d - j - 1]  returns all digits of n to the right
        // of the digit at j, e.g. if n is 908714 and j is 2, the expression gives 714.
        xc[ni + 1] != null || (j < 0 ? n : n % pows10[d - j - 1]);
        r = rm < 4 ? (rd || r) && (rm == 0 || rm == (x.s < 0 ? 3 : 2)) : rd > 5 || rd == 5 && (rm == 4 || r || rm == 6 && // Check whether the digit to the left of the rounding digit is odd.
        (i > 0 ? j > 0 ? n / pows10[d - j] : 0 : xc[ni - 1]) % 10 & 1 || rm == (x.s < 0 ? 8 : 7));
        if (sd < 1 || !xc[0]) {
          xc.length = 0;
          if (r) {
            sd -= x.e + 1;
            xc[0] = pows10[(LOG_BASE - sd % LOG_BASE) % LOG_BASE];
            x.e = -sd || 0;
          } else {
            xc[0] = x.e = 0;
          }
          return x;
        }
        if (i == 0) {
          xc.length = ni;
          k = 1;
          ni--;
        } else {
          xc.length = ni + 1;
          k = pows10[LOG_BASE - i];
          xc[ni] = j > 0 ? mathfloor(n / pows10[d - j] % pows10[j]) * k : 0;
        }
        if (r) {
          for (; ; ) {
            if (ni == 0) {
              for (i = 1, j = xc[0]; j >= 10; j /= 10, i++) ;
              j = xc[0] += k;
              for (k = 1; j >= 10; j /= 10, k++) ;
              if (i != k) {
                x.e++;
                if (xc[0] == BASE) xc[0] = 1;
              }
              break;
            } else {
              xc[ni] += k;
              if (xc[ni] != BASE) break;
              xc[ni--] = 0;
              k = 1;
            }
          }
        }
        for (i = xc.length; xc[--i] === 0; xc.pop()) ;
      }
      if (x.e > MAX_EXP) {
        x.c = x.e = null;
      } else if (x.e < MIN_EXP) {
        x.c = [x.e = 0];
      }
    }
    return x;
  }
  __name(round, "round");
  function valueOf(n) {
    var str, e = n.e;
    if (e === null) return n.toString();
    str = coeffToString(n.c);
    str = e <= TO_EXP_NEG || e >= TO_EXP_POS ? toExponential(str, e) : toFixedPoint(str, e, "0");
    return n.s < 0 ? "-" + str : str;
  }
  __name(valueOf, "valueOf");
  P.absoluteValue = P.abs = function() {
    var x = new BigNumber2(this);
    if (x.s < 0) x.s = 1;
    return x;
  };
  P.comparedTo = function(y, b) {
    return compare(this, new BigNumber2(y, b));
  };
  P.decimalPlaces = P.dp = function(dp, rm) {
    var c, n, v, x = this;
    if (dp != null) {
      intCheck(dp, 0, MAX);
      if (rm == null) rm = ROUNDING_MODE;
      else intCheck(rm, 0, 8);
      return round(new BigNumber2(x), dp + x.e + 1, rm);
    }
    if (!(c = x.c)) return null;
    n = ((v = c.length - 1) - bitFloor(this.e / LOG_BASE)) * LOG_BASE;
    if (v = c[v]) for (; v % 10 == 0; v /= 10, n--) ;
    if (n < 0) n = 0;
    return n;
  };
  P.dividedBy = P.div = function(y, b) {
    return div(this, new BigNumber2(y, b), DECIMAL_PLACES, ROUNDING_MODE);
  };
  P.dividedToIntegerBy = P.idiv = function(y, b) {
    return div(this, new BigNumber2(y, b), 0, 1);
  };
  P.exponentiatedBy = P.pow = function(n, m) {
    var half, isModExp, i, k, more, nIsBig, nIsNeg, nIsOdd, y, x = this;
    n = new BigNumber2(n);
    if (n.c && !n.isInteger()) {
      throw Error(bignumberError + "Exponent not an integer: " + valueOf(n));
    }
    if (m != null) m = new BigNumber2(m);
    nIsBig = n.e > 14;
    if (!x.c || !x.c[0] || x.c[0] == 1 && !x.e && x.c.length == 1 || !n.c || !n.c[0]) {
      y = new BigNumber2(Math.pow(+valueOf(x), nIsBig ? n.s * (2 - isOdd(n)) : +valueOf(n)));
      return m ? y.mod(m) : y;
    }
    nIsNeg = n.s < 0;
    if (m) {
      if (m.c ? !m.c[0] : !m.s) return new BigNumber2(NaN);
      isModExp = !nIsNeg && x.isInteger() && m.isInteger();
      if (isModExp) x = x.mod(m);
    } else if (n.e > 9 && (x.e > 0 || x.e < -1 || (x.e == 0 ? x.c[0] > 1 || nIsBig && x.c[1] >= 24e7 : x.c[0] < 8e13 || nIsBig && x.c[0] <= 9999975e7))) {
      k = x.s < 0 && isOdd(n) ? -0 : 0;
      if (x.e > -1) k = 1 / k;
      return new BigNumber2(nIsNeg ? 1 / k : k);
    } else if (POW_PRECISION) {
      k = mathceil(POW_PRECISION / LOG_BASE + 2);
    }
    if (nIsBig) {
      half = new BigNumber2(0.5);
      if (nIsNeg) n.s = 1;
      nIsOdd = isOdd(n);
    } else {
      i = Math.abs(+valueOf(n));
      nIsOdd = i % 2;
    }
    y = new BigNumber2(ONE);
    for (; ; ) {
      if (nIsOdd) {
        y = y.times(x);
        if (!y.c) break;
        if (k) {
          if (y.c.length > k) y.c.length = k;
        } else if (isModExp) {
          y = y.mod(m);
        }
      }
      if (i) {
        i = mathfloor(i / 2);
        if (i === 0) break;
        nIsOdd = i % 2;
      } else {
        n = n.times(half);
        round(n, n.e + 1, 1);
        if (n.e > 14) {
          nIsOdd = isOdd(n);
        } else {
          i = +valueOf(n);
          if (i === 0) break;
          nIsOdd = i % 2;
        }
      }
      x = x.times(x);
      if (k) {
        if (x.c && x.c.length > k) x.c.length = k;
      } else if (isModExp) {
        x = x.mod(m);
      }
    }
    if (isModExp) return y;
    if (nIsNeg) y = ONE.div(y);
    return m ? y.mod(m) : k ? round(y, POW_PRECISION, ROUNDING_MODE, more) : y;
  };
  P.integerValue = function(rm) {
    var n = new BigNumber2(this);
    if (rm == null) rm = ROUNDING_MODE;
    else intCheck(rm, 0, 8);
    return round(n, n.e + 1, rm);
  };
  P.isEqualTo = P.eq = function(y, b) {
    return compare(this, new BigNumber2(y, b)) === 0;
  };
  P.isFinite = function() {
    return !!this.c;
  };
  P.isGreaterThan = P.gt = function(y, b) {
    return compare(this, new BigNumber2(y, b)) > 0;
  };
  P.isGreaterThanOrEqualTo = P.gte = function(y, b) {
    return (b = compare(this, new BigNumber2(y, b))) === 1 || b === 0;
  };
  P.isInteger = function() {
    return !!this.c && bitFloor(this.e / LOG_BASE) > this.c.length - 2;
  };
  P.isLessThan = P.lt = function(y, b) {
    return compare(this, new BigNumber2(y, b)) < 0;
  };
  P.isLessThanOrEqualTo = P.lte = function(y, b) {
    return (b = compare(this, new BigNumber2(y, b))) === -1 || b === 0;
  };
  P.isNaN = function() {
    return !this.s;
  };
  P.isNegative = function() {
    return this.s < 0;
  };
  P.isPositive = function() {
    return this.s > 0;
  };
  P.isZero = function() {
    return !!this.c && this.c[0] == 0;
  };
  P.minus = function(y, b) {
    var i, j, t, xLTy, x = this, a = x.s;
    y = new BigNumber2(y, b);
    b = y.s;
    if (!a || !b) return new BigNumber2(NaN);
    if (a != b) {
      y.s = -b;
      return x.plus(y);
    }
    var xe = x.e / LOG_BASE, ye = y.e / LOG_BASE, xc = x.c, yc = y.c;
    if (!xe || !ye) {
      if (!xc || !yc) return xc ? (y.s = -b, y) : new BigNumber2(yc ? x : NaN);
      if (!xc[0] || !yc[0]) {
        return yc[0] ? (y.s = -b, y) : new BigNumber2(xc[0] ? x : (
          // IEEE 754 (2008) 6.3: n - n = -0 when rounding to -Infinity
          ROUNDING_MODE == 3 ? -0 : 0
        ));
      }
    }
    xe = bitFloor(xe);
    ye = bitFloor(ye);
    xc = xc.slice();
    if (a = xe - ye) {
      if (xLTy = a < 0) {
        a = -a;
        t = xc;
      } else {
        ye = xe;
        t = yc;
      }
      t.reverse();
      for (b = a; b--; t.push(0)) ;
      t.reverse();
    } else {
      j = (xLTy = (a = xc.length) < (b = yc.length)) ? a : b;
      for (a = b = 0; b < j; b++) {
        if (xc[b] != yc[b]) {
          xLTy = xc[b] < yc[b];
          break;
        }
      }
    }
    if (xLTy) {
      t = xc;
      xc = yc;
      yc = t;
      y.s = -y.s;
    }
    b = (j = yc.length) - (i = xc.length);
    if (b > 0) for (; b--; xc[i++] = 0) ;
    b = BASE - 1;
    for (; j > a; ) {
      if (xc[--j] < yc[j]) {
        for (i = j; i && !xc[--i]; xc[i] = b) ;
        --xc[i];
        xc[j] += BASE;
      }
      xc[j] -= yc[j];
    }
    for (; xc[0] == 0; xc.splice(0, 1), --ye) ;
    if (!xc[0]) {
      y.s = ROUNDING_MODE == 3 ? -1 : 1;
      y.c = [y.e = 0];
      return y;
    }
    return normalise(y, xc, ye);
  };
  P.modulo = P.mod = function(y, b) {
    var q, s, x = this;
    y = new BigNumber2(y, b);
    if (!x.c || !y.s || y.c && !y.c[0]) {
      return new BigNumber2(NaN);
    } else if (!y.c || x.c && !x.c[0]) {
      return new BigNumber2(x);
    }
    if (MODULO_MODE == 9) {
      s = y.s;
      y.s = 1;
      q = div(x, y, 0, 3);
      y.s = s;
      q.s *= s;
    } else {
      q = div(x, y, 0, MODULO_MODE);
    }
    y = x.minus(q.times(y));
    if (!y.c[0] && MODULO_MODE == 1) y.s = x.s;
    return y;
  };
  P.multipliedBy = P.times = function(y, b) {
    var c, e, i, j, k, m, xcL, xlo, xhi, ycL, ylo, yhi, zc, base, sqrtBase, x = this, xc = x.c, yc = (y = new BigNumber2(y, b)).c;
    if (!xc || !yc || !xc[0] || !yc[0]) {
      if (!x.s || !y.s || xc && !xc[0] && !yc || yc && !yc[0] && !xc) {
        y.c = y.e = y.s = null;
      } else {
        y.s *= x.s;
        if (!xc || !yc) {
          y.c = y.e = null;
        } else {
          y.c = [0];
          y.e = 0;
        }
      }
      return y;
    }
    e = bitFloor(x.e / LOG_BASE) + bitFloor(y.e / LOG_BASE);
    y.s *= x.s;
    xcL = xc.length;
    ycL = yc.length;
    if (xcL < ycL) {
      zc = xc;
      xc = yc;
      yc = zc;
      i = xcL;
      xcL = ycL;
      ycL = i;
    }
    for (i = xcL + ycL, zc = []; i--; zc.push(0)) ;
    base = BASE;
    sqrtBase = SQRT_BASE;
    for (i = ycL; --i >= 0; ) {
      c = 0;
      ylo = yc[i] % sqrtBase;
      yhi = yc[i] / sqrtBase | 0;
      for (k = xcL, j = i + k; j > i; ) {
        xlo = xc[--k] % sqrtBase;
        xhi = xc[k] / sqrtBase | 0;
        m = yhi * xlo + xhi * ylo;
        xlo = ylo * xlo + m % sqrtBase * sqrtBase + zc[j] + c;
        c = (xlo / base | 0) + (m / sqrtBase | 0) + yhi * xhi;
        zc[j--] = xlo % base;
      }
      zc[j] = c;
    }
    if (c) {
      ++e;
    } else {
      zc.splice(0, 1);
    }
    return normalise(y, zc, e);
  };
  P.negated = function() {
    var x = new BigNumber2(this);
    x.s = -x.s || null;
    return x;
  };
  P.plus = function(y, b) {
    var t, x = this, a = x.s;
    y = new BigNumber2(y, b);
    b = y.s;
    if (!a || !b) return new BigNumber2(NaN);
    if (a != b) {
      y.s = -b;
      return x.minus(y);
    }
    var xe = x.e / LOG_BASE, ye = y.e / LOG_BASE, xc = x.c, yc = y.c;
    if (!xe || !ye) {
      if (!xc || !yc) return new BigNumber2(a / 0);
      if (!xc[0] || !yc[0]) return yc[0] ? y : new BigNumber2(xc[0] ? x : a * 0);
    }
    xe = bitFloor(xe);
    ye = bitFloor(ye);
    xc = xc.slice();
    if (a = xe - ye) {
      if (a > 0) {
        ye = xe;
        t = yc;
      } else {
        a = -a;
        t = xc;
      }
      t.reverse();
      for (; a--; t.push(0)) ;
      t.reverse();
    }
    a = xc.length;
    b = yc.length;
    if (a - b < 0) {
      t = yc;
      yc = xc;
      xc = t;
      b = a;
    }
    for (a = 0; b; ) {
      a = (xc[--b] = xc[b] + yc[b] + a) / BASE | 0;
      xc[b] = BASE === xc[b] ? 0 : xc[b] % BASE;
    }
    if (a) {
      xc = [a].concat(xc);
      ++ye;
    }
    return normalise(y, xc, ye);
  };
  P.precision = P.sd = function(sd, rm) {
    var c, n, v, x = this;
    if (sd != null && sd !== !!sd) {
      intCheck(sd, 1, MAX);
      if (rm == null) rm = ROUNDING_MODE;
      else intCheck(rm, 0, 8);
      return round(new BigNumber2(x), sd, rm);
    }
    if (!(c = x.c)) return null;
    v = c.length - 1;
    n = v * LOG_BASE + 1;
    if (v = c[v]) {
      for (; v % 10 == 0; v /= 10, n--) ;
      for (v = c[0]; v >= 10; v /= 10, n++) ;
    }
    if (sd && x.e + 1 > n) n = x.e + 1;
    return n;
  };
  P.shiftedBy = function(k) {
    intCheck(k, -MAX_SAFE_INTEGER, MAX_SAFE_INTEGER);
    return this.times("1e" + k);
  };
  P.squareRoot = P.sqrt = function() {
    var m, n, r, rep, t, x = this, c = x.c, s = x.s, e = x.e, dp = DECIMAL_PLACES + 4, half = new BigNumber2("0.5");
    if (s !== 1 || !c || !c[0]) {
      return new BigNumber2(!s || s < 0 && (!c || c[0]) ? NaN : c ? x : 1 / 0);
    }
    s = Math.sqrt(+valueOf(x));
    if (s == 0 || s == 1 / 0) {
      n = coeffToString(c);
      if ((n.length + e) % 2 == 0) n += "0";
      s = Math.sqrt(+n);
      e = bitFloor((e + 1) / 2) - (e < 0 || e % 2);
      if (s == 1 / 0) {
        n = "5e" + e;
      } else {
        n = s.toExponential();
        n = n.slice(0, n.indexOf("e") + 1) + e;
      }
      r = new BigNumber2(n);
    } else {
      r = new BigNumber2(s + "");
    }
    if (r.c[0]) {
      e = r.e;
      s = e + dp;
      if (s < 3) s = 0;
      for (; ; ) {
        t = r;
        r = half.times(t.plus(div(x, t, dp, 1)));
        if (coeffToString(t.c).slice(0, s) === (n = coeffToString(r.c)).slice(0, s)) {
          if (r.e < e) --s;
          n = n.slice(s - 3, s + 1);
          if (n == "9999" || !rep && n == "4999") {
            if (!rep) {
              round(t, t.e + DECIMAL_PLACES + 2, 0);
              if (t.times(t).eq(x)) {
                r = t;
                break;
              }
            }
            dp += 4;
            s += 4;
            rep = 1;
          } else {
            if (!+n || !+n.slice(1) && n.charAt(0) == "5") {
              round(r, r.e + DECIMAL_PLACES + 2, 1);
              m = !r.times(r).eq(x);
            }
            break;
          }
        }
      }
    }
    return round(r, r.e + DECIMAL_PLACES + 1, ROUNDING_MODE, m);
  };
  P.toExponential = function(dp, rm) {
    if (dp != null) {
      intCheck(dp, 0, MAX);
      dp++;
    }
    return format(this, dp, rm, 1);
  };
  P.toFixed = function(dp, rm) {
    if (dp != null) {
      intCheck(dp, 0, MAX);
      dp = dp + this.e + 1;
    }
    return format(this, dp, rm);
  };
  P.toFormat = function(dp, rm, format2) {
    var str, x = this;
    if (format2 == null) {
      if (dp != null && rm && typeof rm == "object") {
        format2 = rm;
        rm = null;
      } else if (dp && typeof dp == "object") {
        format2 = dp;
        dp = rm = null;
      } else {
        format2 = FORMAT;
      }
    } else if (typeof format2 != "object") {
      throw Error(bignumberError + "Argument not an object: " + format2);
    }
    str = x.toFixed(dp, rm);
    if (x.c) {
      var i, arr = str.split("."), g1 = +format2.groupSize, g2 = +format2.secondaryGroupSize, groupSeparator = format2.groupSeparator || "", intPart = arr[0], fractionPart = arr[1], isNeg = x.s < 0, intDigits = isNeg ? intPart.slice(1) : intPart, len = intDigits.length;
      if (g2) {
        i = g1;
        g1 = g2;
        g2 = i;
        len -= i;
      }
      if (g1 > 0 && len > 0) {
        i = len % g1 || g1;
        intPart = intDigits.substr(0, i);
        for (; i < len; i += g1) intPart += groupSeparator + intDigits.substr(i, g1);
        if (g2 > 0) intPart += groupSeparator + intDigits.slice(i);
        if (isNeg) intPart = "-" + intPart;
      }
      str = fractionPart ? intPart + (format2.decimalSeparator || "") + ((g2 = +format2.fractionGroupSize) ? fractionPart.replace(
        new RegExp("\\d{" + g2 + "}\\B", "g"),
        "$&" + (format2.fractionGroupSeparator || "")
      ) : fractionPart) : intPart;
    }
    return (format2.prefix || "") + str + (format2.suffix || "");
  };
  P.toFraction = function(md) {
    var d, d0, d1, d2, e, exp2, n, n0, n1, q, r, s, x = this, xc = x.c;
    if (md != null) {
      n = new BigNumber2(md);
      if (!n.isInteger() && (n.c || n.s !== 1) || n.lt(ONE)) {
        throw Error(bignumberError + "Argument " + (n.isInteger() ? "out of range: " : "not an integer: ") + valueOf(n));
      }
    }
    if (!xc) return new BigNumber2(x);
    d = new BigNumber2(ONE);
    n1 = d0 = new BigNumber2(ONE);
    d1 = n0 = new BigNumber2(ONE);
    s = coeffToString(xc);
    e = d.e = s.length - x.e - 1;
    d.c[0] = POWS_TEN[(exp2 = e % LOG_BASE) < 0 ? LOG_BASE + exp2 : exp2];
    md = !md || n.comparedTo(d) > 0 ? e > 0 ? d : n1 : n;
    exp2 = MAX_EXP;
    MAX_EXP = 1 / 0;
    n = new BigNumber2(s);
    n0.c[0] = 0;
    for (; ; ) {
      q = div(n, d, 0, 1);
      d2 = d0.plus(q.times(d1));
      if (d2.comparedTo(md) == 1) break;
      d0 = d1;
      d1 = d2;
      n1 = n0.plus(q.times(d2 = n1));
      n0 = d2;
      d = n.minus(q.times(d2 = d));
      n = d2;
    }
    d2 = div(md.minus(d0), d1, 0, 1);
    n0 = n0.plus(d2.times(n1));
    d0 = d0.plus(d2.times(d1));
    n0.s = n1.s = x.s;
    e = e * 2;
    r = div(n1, d1, e, ROUNDING_MODE).minus(x).abs().comparedTo(
      div(n0, d0, e, ROUNDING_MODE).minus(x).abs()
    ) < 1 ? [n1, d1] : [n0, d0];
    MAX_EXP = exp2;
    return r;
  };
  P.toNumber = function() {
    return +valueOf(this);
  };
  P.toPrecision = function(sd, rm) {
    if (sd != null) intCheck(sd, 1, MAX);
    return format(this, sd, rm, 2);
  };
  P.toString = function(b) {
    var str, n = this, s = n.s, e = n.e;
    if (e === null) {
      if (s) {
        str = "Infinity";
        if (s < 0) str = "-" + str;
      } else {
        str = "NaN";
      }
    } else {
      if (b == null) {
        str = e <= TO_EXP_NEG || e >= TO_EXP_POS ? toExponential(coeffToString(n.c), e) : toFixedPoint(coeffToString(n.c), e, "0");
      } else if (b === 10 && alphabetHasNormalDecimalDigits) {
        n = round(new BigNumber2(n), DECIMAL_PLACES + e + 1, ROUNDING_MODE);
        str = toFixedPoint(coeffToString(n.c), n.e, "0");
      } else {
        intCheck(b, 2, ALPHABET.length, "Base");
        str = convertBase(toFixedPoint(coeffToString(n.c), e, "0"), 10, b, s, true);
      }
      if (s < 0 && n.c[0]) str = "-" + str;
    }
    return str;
  };
  P.valueOf = P.toJSON = function() {
    return valueOf(this);
  };
  P._isBigNumber = true;
  P[Symbol.toStringTag] = "BigNumber";
  P[Symbol.for("nodejs.util.inspect.custom")] = P.valueOf;
  if (configObject != null) BigNumber2.set(configObject);
  return BigNumber2;
}
function bitFloor(n) {
  var i = n | 0;
  return n > 0 || n === i ? i : i - 1;
}
function coeffToString(a) {
  var s, z2, i = 1, j = a.length, r = a[0] + "";
  for (; i < j; ) {
    s = a[i++] + "";
    z2 = LOG_BASE - s.length;
    for (; z2--; s = "0" + s) ;
    r += s;
  }
  for (j = r.length; r.charCodeAt(--j) === 48; ) ;
  return r.slice(0, j + 1 || 1);
}
function compare(x, y) {
  var a, b, xc = x.c, yc = y.c, i = x.s, j = y.s, k = x.e, l = y.e;
  if (!i || !j) return null;
  a = xc && !xc[0];
  b = yc && !yc[0];
  if (a || b) return a ? b ? 0 : -j : i;
  if (i != j) return i;
  a = i < 0;
  b = k == l;
  if (!xc || !yc) return b ? 0 : !xc ^ a ? 1 : -1;
  if (!b) return k > l ^ a ? 1 : -1;
  j = (k = xc.length) < (l = yc.length) ? k : l;
  for (i = 0; i < j; i++) if (xc[i] != yc[i]) return xc[i] > yc[i] ^ a ? 1 : -1;
  return k == l ? 0 : k > l ^ a ? 1 : -1;
}
function intCheck(n, min, max, name) {
  if (n < min || n > max || n !== mathfloor(n)) {
    throw Error(bignumberError + (name || "Argument") + (typeof n == "number" ? n < min || n > max ? " out of range: " : " not an integer: " : " not a primitive number: ") + String(n));
  }
}
function isOdd(n) {
  var k = n.c.length - 1;
  return bitFloor(n.e / LOG_BASE) == k && n.c[k] % 2 != 0;
}
function toExponential(str, e) {
  return (str.length > 1 ? str.charAt(0) + "." + str.slice(1) : str) + (e < 0 ? "e" : "e+") + e;
}
function toFixedPoint(str, e, z2) {
  var len, zs;
  if (e < 0) {
    for (zs = z2 + "."; ++e; zs += z2) ;
    str = zs + str;
  } else {
    len = str.length;
    if (++e > len) {
      for (zs = z2, e -= len; --e; zs += z2) ;
      str += zs;
    } else if (e < len) {
      str = str.slice(0, e) + "." + str.slice(e);
    }
  }
  return str;
}
var isNumeric, mathceil, mathfloor, bignumberError, tooManyDigits, BASE, LOG_BASE, MAX_SAFE_INTEGER, POWS_TEN, SQRT_BASE, MAX, BigNumber, bignumber_default;
var init_bignumber = __esm({
  "worker/node_modules/bignumber.js/bignumber.mjs"() {
    isNumeric = /^-?(?:\d+(?:\.\d*)?|\.\d+)(?:e[+-]?\d+)?$/i;
    mathceil = Math.ceil;
    mathfloor = Math.floor;
    bignumberError = "[BigNumber Error] ";
    tooManyDigits = bignumberError + "Number primitive has more than 15 significant digits: ";
    BASE = 1e14;
    LOG_BASE = 14;
    MAX_SAFE_INTEGER = 9007199254740991;
    POWS_TEN = [1, 10, 100, 1e3, 1e4, 1e5, 1e6, 1e7, 1e8, 1e9, 1e10, 1e11, 1e12, 1e13];
    SQRT_BASE = 1e7;
    MAX = 1e9;
    __name(clone, "clone");
    __name(bitFloor, "bitFloor");
    __name(coeffToString, "coeffToString");
    __name(compare, "compare");
    __name(intCheck, "intCheck");
    __name(isOdd, "isOdd");
    __name(toExponential, "toExponential");
    __name(toFixedPoint, "toFixedPoint");
    BigNumber = clone();
    bignumber_default = BigNumber;
  }
});

// worker/node_modules/splaytree-ts/dist/esm/index.js
var SplayTreeNode, SplayTreeSetNode, SplayTree, SplayTreeSet, SplayTreeIterableIterator, SplayTreeKeyIterableIterator, SplayTreeSetEntryIterableIterator;
var init_esm12 = __esm({
  "worker/node_modules/splaytree-ts/dist/esm/index.js"() {
    SplayTreeNode = class {
      static {
        __name(this, "SplayTreeNode");
      }
      key;
      left = null;
      right = null;
      constructor(key) {
        this.key = key;
      }
    };
    SplayTreeSetNode = class extends SplayTreeNode {
      static {
        __name(this, "SplayTreeSetNode");
      }
      constructor(key) {
        super(key);
      }
    };
    SplayTree = class {
      static {
        __name(this, "SplayTree");
      }
      size = 0;
      modificationCount = 0;
      splayCount = 0;
      splay(key) {
        const root = this.root;
        if (root == null) {
          this.compare(key, key);
          return -1;
        }
        let right = null;
        let newTreeRight = null;
        let left = null;
        let newTreeLeft = null;
        let current = root;
        const compare2 = this.compare;
        let comp;
        while (true) {
          comp = compare2(current.key, key);
          if (comp > 0) {
            let currentLeft = current.left;
            if (currentLeft == null) break;
            comp = compare2(currentLeft.key, key);
            if (comp > 0) {
              current.left = currentLeft.right;
              currentLeft.right = current;
              current = currentLeft;
              currentLeft = current.left;
              if (currentLeft == null) break;
            }
            if (right == null) {
              newTreeRight = current;
            } else {
              right.left = current;
            }
            right = current;
            current = currentLeft;
          } else if (comp < 0) {
            let currentRight = current.right;
            if (currentRight == null) break;
            comp = compare2(currentRight.key, key);
            if (comp < 0) {
              current.right = currentRight.left;
              currentRight.left = current;
              current = currentRight;
              currentRight = current.right;
              if (currentRight == null) break;
            }
            if (left == null) {
              newTreeLeft = current;
            } else {
              left.right = current;
            }
            left = current;
            current = currentRight;
          } else {
            break;
          }
        }
        if (left != null) {
          left.right = current.left;
          current.left = newTreeLeft;
        }
        if (right != null) {
          right.left = current.right;
          current.right = newTreeRight;
        }
        if (this.root !== current) {
          this.root = current;
          this.splayCount++;
        }
        return comp;
      }
      splayMin(node) {
        let current = node;
        let nextLeft = current.left;
        while (nextLeft != null) {
          const left = nextLeft;
          current.left = left.right;
          left.right = current;
          current = left;
          nextLeft = current.left;
        }
        return current;
      }
      splayMax(node) {
        let current = node;
        let nextRight = current.right;
        while (nextRight != null) {
          const right = nextRight;
          current.right = right.left;
          right.left = current;
          current = right;
          nextRight = current.right;
        }
        return current;
      }
      _delete(key) {
        if (this.root == null) return null;
        const comp = this.splay(key);
        if (comp != 0) return null;
        let root = this.root;
        const result = root;
        const left = root.left;
        this.size--;
        if (left == null) {
          this.root = root.right;
        } else {
          const right = root.right;
          root = this.splayMax(left);
          root.right = right;
          this.root = root;
        }
        this.modificationCount++;
        return result;
      }
      addNewRoot(node, comp) {
        this.size++;
        this.modificationCount++;
        const root = this.root;
        if (root == null) {
          this.root = node;
          return;
        }
        if (comp < 0) {
          node.left = root;
          node.right = root.right;
          root.right = null;
        } else {
          node.right = root;
          node.left = root.left;
          root.left = null;
        }
        this.root = node;
      }
      _first() {
        const root = this.root;
        if (root == null) return null;
        this.root = this.splayMin(root);
        return this.root;
      }
      _last() {
        const root = this.root;
        if (root == null) return null;
        this.root = this.splayMax(root);
        return this.root;
      }
      clear() {
        this.root = null;
        this.size = 0;
        this.modificationCount++;
      }
      has(key) {
        return this.validKey(key) && this.splay(key) == 0;
      }
      defaultCompare() {
        return (a, b) => a < b ? -1 : a > b ? 1 : 0;
      }
      wrap() {
        return {
          getRoot: /* @__PURE__ */ __name(() => {
            return this.root;
          }, "getRoot"),
          setRoot: /* @__PURE__ */ __name((root) => {
            this.root = root;
          }, "setRoot"),
          getSize: /* @__PURE__ */ __name(() => {
            return this.size;
          }, "getSize"),
          getModificationCount: /* @__PURE__ */ __name(() => {
            return this.modificationCount;
          }, "getModificationCount"),
          getSplayCount: /* @__PURE__ */ __name(() => {
            return this.splayCount;
          }, "getSplayCount"),
          setSplayCount: /* @__PURE__ */ __name((count) => {
            this.splayCount = count;
          }, "setSplayCount"),
          splay: /* @__PURE__ */ __name((key) => {
            return this.splay(key);
          }, "splay"),
          has: /* @__PURE__ */ __name((key) => {
            return this.has(key);
          }, "has")
        };
      }
    };
    SplayTreeSet = class _SplayTreeSet extends SplayTree {
      static {
        __name(this, "_SplayTreeSet");
      }
      root = null;
      compare;
      validKey;
      constructor(compare2, isValidKey) {
        super();
        this.compare = compare2 ?? this.defaultCompare();
        this.validKey = isValidKey ?? ((v) => v != null && v != void 0);
      }
      delete(element) {
        if (!this.validKey(element)) return false;
        return this._delete(element) != null;
      }
      deleteAll(elements) {
        for (const element of elements) {
          this.delete(element);
        }
      }
      forEach(f) {
        const nodes = this[Symbol.iterator]();
        let result;
        while (result = nodes.next(), !result.done) {
          f(result.value, result.value, this);
        }
      }
      add(element) {
        const compare2 = this.splay(element);
        if (compare2 != 0) this.addNewRoot(new SplayTreeSetNode(element), compare2);
        return this;
      }
      addAndReturn(element) {
        const compare2 = this.splay(element);
        if (compare2 != 0) this.addNewRoot(new SplayTreeSetNode(element), compare2);
        return this.root.key;
      }
      addAll(elements) {
        for (const element of elements) {
          this.add(element);
        }
      }
      isEmpty() {
        return this.root == null;
      }
      isNotEmpty() {
        return this.root != null;
      }
      single() {
        if (this.size == 0) throw "Bad state: No element";
        if (this.size > 1) throw "Bad state: Too many element";
        return this.root.key;
      }
      first() {
        if (this.size == 0) throw "Bad state: No element";
        return this._first().key;
      }
      last() {
        if (this.size == 0) throw "Bad state: No element";
        return this._last().key;
      }
      lastBefore(element) {
        if (element == null) throw "Invalid arguments(s)";
        if (this.root == null) return null;
        const comp = this.splay(element);
        if (comp < 0) return this.root.key;
        let node = this.root.left;
        if (node == null) return null;
        let nodeRight = node.right;
        while (nodeRight != null) {
          node = nodeRight;
          nodeRight = node.right;
        }
        return node.key;
      }
      firstAfter(element) {
        if (element == null) throw "Invalid arguments(s)";
        if (this.root == null) return null;
        const comp = this.splay(element);
        if (comp > 0) return this.root.key;
        let node = this.root.right;
        if (node == null) return null;
        let nodeLeft = node.left;
        while (nodeLeft != null) {
          node = nodeLeft;
          nodeLeft = node.left;
        }
        return node.key;
      }
      retainAll(elements) {
        const retainSet = new _SplayTreeSet(this.compare, this.validKey);
        const modificationCount = this.modificationCount;
        for (const object2 of elements) {
          if (modificationCount != this.modificationCount) {
            throw "Concurrent modification during iteration.";
          }
          if (this.validKey(object2) && this.splay(object2) == 0) {
            retainSet.add(this.root.key);
          }
        }
        if (retainSet.size != this.size) {
          this.root = retainSet.root;
          this.size = retainSet.size;
          this.modificationCount++;
        }
      }
      lookup(object2) {
        if (!this.validKey(object2)) return null;
        const comp = this.splay(object2);
        if (comp != 0) return null;
        return this.root.key;
      }
      intersection(other) {
        const result = new _SplayTreeSet(this.compare, this.validKey);
        for (const element of this) {
          if (other.has(element)) result.add(element);
        }
        return result;
      }
      difference(other) {
        const result = new _SplayTreeSet(this.compare, this.validKey);
        for (const element of this) {
          if (!other.has(element)) result.add(element);
        }
        return result;
      }
      union(other) {
        const u = this.clone();
        u.addAll(other);
        return u;
      }
      clone() {
        const set2 = new _SplayTreeSet(this.compare, this.validKey);
        set2.size = this.size;
        set2.root = this.copyNode(this.root);
        return set2;
      }
      copyNode(node) {
        if (node == null) return null;
        function copyChildren(node2, dest) {
          let left;
          let right;
          do {
            left = node2.left;
            right = node2.right;
            if (left != null) {
              const newLeft = new SplayTreeSetNode(left.key);
              dest.left = newLeft;
              copyChildren(left, newLeft);
            }
            if (right != null) {
              const newRight = new SplayTreeSetNode(right.key);
              dest.right = newRight;
              node2 = right;
              dest = newRight;
            }
          } while (right != null);
        }
        __name(copyChildren, "copyChildren");
        const result = new SplayTreeSetNode(node.key);
        copyChildren(node, result);
        return result;
      }
      toSet() {
        return this.clone();
      }
      entries() {
        return new SplayTreeSetEntryIterableIterator(this.wrap());
      }
      keys() {
        return this[Symbol.iterator]();
      }
      values() {
        return this[Symbol.iterator]();
      }
      [Symbol.iterator]() {
        return new SplayTreeKeyIterableIterator(this.wrap());
      }
      [Symbol.toStringTag] = "[object Set]";
    };
    SplayTreeIterableIterator = class {
      static {
        __name(this, "SplayTreeIterableIterator");
      }
      tree;
      path = new Array();
      modificationCount = null;
      splayCount;
      constructor(tree) {
        this.tree = tree;
        this.splayCount = tree.getSplayCount();
      }
      [Symbol.iterator]() {
        return this;
      }
      next() {
        if (this.moveNext()) return { done: false, value: this.current() };
        return { done: true, value: null };
      }
      current() {
        if (!this.path.length) return null;
        const node = this.path[this.path.length - 1];
        return this.getValue(node);
      }
      rebuildPath(key) {
        this.path.splice(0, this.path.length);
        this.tree.splay(key);
        this.path.push(this.tree.getRoot());
        this.splayCount = this.tree.getSplayCount();
      }
      findLeftMostDescendent(node) {
        while (node != null) {
          this.path.push(node);
          node = node.left;
        }
      }
      moveNext() {
        if (this.modificationCount != this.tree.getModificationCount()) {
          if (this.modificationCount == null) {
            this.modificationCount = this.tree.getModificationCount();
            let node2 = this.tree.getRoot();
            while (node2 != null) {
              this.path.push(node2);
              node2 = node2.left;
            }
            return this.path.length > 0;
          }
          throw "Concurrent modification during iteration.";
        }
        if (!this.path.length) return false;
        if (this.splayCount != this.tree.getSplayCount()) {
          this.rebuildPath(this.path[this.path.length - 1].key);
        }
        let node = this.path[this.path.length - 1];
        let next = node.right;
        if (next != null) {
          while (next != null) {
            this.path.push(next);
            next = next.left;
          }
          return true;
        }
        this.path.pop();
        while (this.path.length && this.path[this.path.length - 1].right === node) {
          node = this.path.pop();
        }
        return this.path.length > 0;
      }
    };
    SplayTreeKeyIterableIterator = class extends SplayTreeIterableIterator {
      static {
        __name(this, "SplayTreeKeyIterableIterator");
      }
      getValue(node) {
        return node.key;
      }
    };
    SplayTreeSetEntryIterableIterator = class extends SplayTreeIterableIterator {
      static {
        __name(this, "SplayTreeSetEntryIterableIterator");
      }
      getValue(node) {
        return [node.key, node.key];
      }
    };
  }
});

// worker/node_modules/polyclip-ts/dist/esm/index.js
function orient_default(eps) {
  const almostCollinear = eps ? (area2, ax, ay, cx, cy) => area2.exponentiatedBy(2).isLessThanOrEqualTo(
    cx.minus(ax).exponentiatedBy(2).plus(cy.minus(ay).exponentiatedBy(2)).times(eps)
  ) : constant_default3(false);
  return (a, b, c) => {
    const ax = a.x, ay = a.y, cx = c.x, cy = c.y;
    const area2 = ay.minus(cy).times(b.x.minus(cx)).minus(ax.minus(cx).times(b.y.minus(cy)));
    if (almostCollinear(area2, ax, ay, cx, cy)) return 0;
    return area2.comparedTo(0);
  };
}
var constant_default3, compare_default, identity_default4, snap_default, set, precision, isInBbox, getBboxOverlap, crossProduct, dotProduct, length, sineOfAngle, cosineOfAngle, horizontalIntersection, verticalIntersection, intersection, SweepEvent, RingOut, PolyOut, MultiPolyOut, SweepLine, Operation, operation, operation_default, segmentId, Segment, RingIn, PolyIn, MultiPolyIn, union3, intersection2, difference, setPrecision;
var init_esm13 = __esm({
  "worker/node_modules/polyclip-ts/dist/esm/index.js"() {
    init_bignumber();
    init_bignumber();
    init_esm12();
    init_esm12();
    init_esm12();
    constant_default3 = /* @__PURE__ */ __name((x) => {
      return () => {
        return x;
      };
    }, "constant_default");
    compare_default = /* @__PURE__ */ __name((eps) => {
      const almostEqual = eps ? (a, b) => b.minus(a).abs().isLessThanOrEqualTo(eps) : constant_default3(false);
      return (a, b) => {
        if (almostEqual(a, b)) return 0;
        return a.comparedTo(b);
      };
    }, "compare_default");
    __name(orient_default, "orient_default");
    identity_default4 = /* @__PURE__ */ __name((x) => {
      return x;
    }, "identity_default");
    snap_default = /* @__PURE__ */ __name((eps) => {
      if (eps) {
        const xTree = new SplayTreeSet(compare_default(eps));
        const yTree = new SplayTreeSet(compare_default(eps));
        const snapCoord = /* @__PURE__ */ __name((coord, tree) => {
          return tree.addAndReturn(coord);
        }, "snapCoord");
        const snap = /* @__PURE__ */ __name((v) => {
          return {
            x: snapCoord(v.x, xTree),
            y: snapCoord(v.y, yTree)
          };
        }, "snap");
        snap({ x: new bignumber_default(0), y: new bignumber_default(0) });
        return snap;
      }
      return identity_default4;
    }, "snap_default");
    set = /* @__PURE__ */ __name((eps) => {
      return {
        set: /* @__PURE__ */ __name((eps2) => {
          precision = set(eps2);
        }, "set"),
        reset: /* @__PURE__ */ __name(() => set(eps), "reset"),
        compare: compare_default(eps),
        snap: snap_default(eps),
        orient: orient_default(eps)
      };
    }, "set");
    precision = set();
    isInBbox = /* @__PURE__ */ __name((bbox2, point2) => {
      return bbox2.ll.x.isLessThanOrEqualTo(point2.x) && point2.x.isLessThanOrEqualTo(bbox2.ur.x) && bbox2.ll.y.isLessThanOrEqualTo(point2.y) && point2.y.isLessThanOrEqualTo(bbox2.ur.y);
    }, "isInBbox");
    getBboxOverlap = /* @__PURE__ */ __name((b1, b2) => {
      if (b2.ur.x.isLessThan(b1.ll.x) || b1.ur.x.isLessThan(b2.ll.x) || b2.ur.y.isLessThan(b1.ll.y) || b1.ur.y.isLessThan(b2.ll.y))
        return null;
      const lowerX = b1.ll.x.isLessThan(b2.ll.x) ? b2.ll.x : b1.ll.x;
      const upperX = b1.ur.x.isLessThan(b2.ur.x) ? b1.ur.x : b2.ur.x;
      const lowerY = b1.ll.y.isLessThan(b2.ll.y) ? b2.ll.y : b1.ll.y;
      const upperY = b1.ur.y.isLessThan(b2.ur.y) ? b1.ur.y : b2.ur.y;
      return { ll: { x: lowerX, y: lowerY }, ur: { x: upperX, y: upperY } };
    }, "getBboxOverlap");
    crossProduct = /* @__PURE__ */ __name((a, b) => a.x.times(b.y).minus(a.y.times(b.x)), "crossProduct");
    dotProduct = /* @__PURE__ */ __name((a, b) => a.x.times(b.x).plus(a.y.times(b.y)), "dotProduct");
    length = /* @__PURE__ */ __name((v) => dotProduct(v, v).sqrt(), "length");
    sineOfAngle = /* @__PURE__ */ __name((pShared, pBase, pAngle) => {
      const vBase = { x: pBase.x.minus(pShared.x), y: pBase.y.minus(pShared.y) };
      const vAngle = { x: pAngle.x.minus(pShared.x), y: pAngle.y.minus(pShared.y) };
      return crossProduct(vAngle, vBase).div(length(vAngle)).div(length(vBase));
    }, "sineOfAngle");
    cosineOfAngle = /* @__PURE__ */ __name((pShared, pBase, pAngle) => {
      const vBase = { x: pBase.x.minus(pShared.x), y: pBase.y.minus(pShared.y) };
      const vAngle = { x: pAngle.x.minus(pShared.x), y: pAngle.y.minus(pShared.y) };
      return dotProduct(vAngle, vBase).div(length(vAngle)).div(length(vBase));
    }, "cosineOfAngle");
    horizontalIntersection = /* @__PURE__ */ __name((pt, v, y) => {
      if (v.y.isZero()) return null;
      return { x: pt.x.plus(v.x.div(v.y).times(y.minus(pt.y))), y };
    }, "horizontalIntersection");
    verticalIntersection = /* @__PURE__ */ __name((pt, v, x) => {
      if (v.x.isZero()) return null;
      return { x, y: pt.y.plus(v.y.div(v.x).times(x.minus(pt.x))) };
    }, "verticalIntersection");
    intersection = /* @__PURE__ */ __name((pt1, v1, pt2, v2) => {
      if (v1.x.isZero()) return verticalIntersection(pt2, v2, pt1.x);
      if (v2.x.isZero()) return verticalIntersection(pt1, v1, pt2.x);
      if (v1.y.isZero()) return horizontalIntersection(pt2, v2, pt1.y);
      if (v2.y.isZero()) return horizontalIntersection(pt1, v1, pt2.y);
      const kross = crossProduct(v1, v2);
      if (kross.isZero()) return null;
      const ve = { x: pt2.x.minus(pt1.x), y: pt2.y.minus(pt1.y) };
      const d1 = crossProduct(ve, v1).div(kross);
      const d2 = crossProduct(ve, v2).div(kross);
      const x12 = pt1.x.plus(d2.times(v1.x)), x2 = pt2.x.plus(d1.times(v2.x));
      const y12 = pt1.y.plus(d2.times(v1.y)), y2 = pt2.y.plus(d1.times(v2.y));
      const x = x12.plus(x2).div(2);
      const y = y12.plus(y2).div(2);
      return { x, y };
    }, "intersection");
    SweepEvent = class _SweepEvent {
      static {
        __name(this, "_SweepEvent");
      }
      point;
      isLeft;
      segment;
      otherSE;
      consumedBy;
      // for ordering sweep events in the sweep event queue
      static compare(a, b) {
        const ptCmp = _SweepEvent.comparePoints(a.point, b.point);
        if (ptCmp !== 0) return ptCmp;
        if (a.point !== b.point) a.link(b);
        if (a.isLeft !== b.isLeft) return a.isLeft ? 1 : -1;
        return Segment.compare(a.segment, b.segment);
      }
      // for ordering points in sweep line order
      static comparePoints(aPt, bPt) {
        if (aPt.x.isLessThan(bPt.x)) return -1;
        if (aPt.x.isGreaterThan(bPt.x)) return 1;
        if (aPt.y.isLessThan(bPt.y)) return -1;
        if (aPt.y.isGreaterThan(bPt.y)) return 1;
        return 0;
      }
      // Warning: 'point' input will be modified and re-used (for performance)
      constructor(point2, isLeft) {
        if (point2.events === void 0) point2.events = [this];
        else point2.events.push(this);
        this.point = point2;
        this.isLeft = isLeft;
      }
      link(other) {
        if (other.point === this.point) {
          throw new Error("Tried to link already linked events");
        }
        const otherEvents = other.point.events;
        for (let i = 0, iMax = otherEvents.length; i < iMax; i++) {
          const evt = otherEvents[i];
          this.point.events.push(evt);
          evt.point = this.point;
        }
        this.checkForConsuming();
      }
      /* Do a pass over our linked events and check to see if any pair
       * of segments match, and should be consumed. */
      checkForConsuming() {
        const numEvents = this.point.events.length;
        for (let i = 0; i < numEvents; i++) {
          const evt1 = this.point.events[i];
          if (evt1.segment.consumedBy !== void 0) continue;
          for (let j = i + 1; j < numEvents; j++) {
            const evt2 = this.point.events[j];
            if (evt2.consumedBy !== void 0) continue;
            if (evt1.otherSE.point.events !== evt2.otherSE.point.events) continue;
            evt1.segment.consume(evt2.segment);
          }
        }
      }
      getAvailableLinkedEvents() {
        const events = [];
        for (let i = 0, iMax = this.point.events.length; i < iMax; i++) {
          const evt = this.point.events[i];
          if (evt !== this && !evt.segment.ringOut && evt.segment.isInResult()) {
            events.push(evt);
          }
        }
        return events;
      }
      /**
       * Returns a comparator function for sorting linked events that will
       * favor the event that will give us the smallest left-side angle.
       * All ring construction starts as low as possible heading to the right,
       * so by always turning left as sharp as possible we'll get polygons
       * without uncessary loops & holes.
       *
       * The comparator function has a compute cache such that it avoids
       * re-computing already-computed values.
       */
      getLeftmostComparator(baseEvent) {
        const cache = /* @__PURE__ */ new Map();
        const fillCache = /* @__PURE__ */ __name((linkedEvent) => {
          const nextEvent = linkedEvent.otherSE;
          cache.set(linkedEvent, {
            sine: sineOfAngle(this.point, baseEvent.point, nextEvent.point),
            cosine: cosineOfAngle(this.point, baseEvent.point, nextEvent.point)
          });
        }, "fillCache");
        return (a, b) => {
          if (!cache.has(a)) fillCache(a);
          if (!cache.has(b)) fillCache(b);
          const { sine: asine, cosine: acosine } = cache.get(a);
          const { sine: bsine, cosine: bcosine } = cache.get(b);
          if (asine.isGreaterThanOrEqualTo(0) && bsine.isGreaterThanOrEqualTo(0)) {
            if (acosine.isLessThan(bcosine)) return 1;
            if (acosine.isGreaterThan(bcosine)) return -1;
            return 0;
          }
          if (asine.isLessThan(0) && bsine.isLessThan(0)) {
            if (acosine.isLessThan(bcosine)) return -1;
            if (acosine.isGreaterThan(bcosine)) return 1;
            return 0;
          }
          if (bsine.isLessThan(asine)) return -1;
          if (bsine.isGreaterThan(asine)) return 1;
          return 0;
        };
      }
    };
    RingOut = class _RingOut {
      static {
        __name(this, "_RingOut");
      }
      events;
      poly;
      _isExteriorRing;
      _enclosingRing;
      /* Given the segments from the sweep line pass, compute & return a series
       * of closed rings from all the segments marked to be part of the result */
      static factory(allSegments) {
        const ringsOut = [];
        for (let i = 0, iMax = allSegments.length; i < iMax; i++) {
          const segment = allSegments[i];
          if (!segment.isInResult() || segment.ringOut) continue;
          let prevEvent = null;
          let event = segment.leftSE;
          let nextEvent = segment.rightSE;
          const events = [event];
          const startingPoint = event.point;
          const intersectionLEs = [];
          while (true) {
            prevEvent = event;
            event = nextEvent;
            events.push(event);
            if (event.point === startingPoint) break;
            while (true) {
              const availableLEs = event.getAvailableLinkedEvents();
              if (availableLEs.length === 0) {
                const firstPt = events[0].point;
                const lastPt = events[events.length - 1].point;
                throw new Error(
                  `Unable to complete output ring starting at [${firstPt.x}, ${firstPt.y}]. Last matching segment found ends at [${lastPt.x}, ${lastPt.y}].`
                );
              }
              if (availableLEs.length === 1) {
                nextEvent = availableLEs[0].otherSE;
                break;
              }
              let indexLE = null;
              for (let j = 0, jMax = intersectionLEs.length; j < jMax; j++) {
                if (intersectionLEs[j].point === event.point) {
                  indexLE = j;
                  break;
                }
              }
              if (indexLE !== null) {
                const intersectionLE = intersectionLEs.splice(indexLE)[0];
                const ringEvents = events.splice(intersectionLE.index);
                ringEvents.unshift(ringEvents[0].otherSE);
                ringsOut.push(new _RingOut(ringEvents.reverse()));
                continue;
              }
              intersectionLEs.push({
                index: events.length,
                point: event.point
              });
              const comparator = event.getLeftmostComparator(prevEvent);
              nextEvent = availableLEs.sort(comparator)[0].otherSE;
              break;
            }
          }
          ringsOut.push(new _RingOut(events));
        }
        return ringsOut;
      }
      constructor(events) {
        this.events = events;
        for (let i = 0, iMax = events.length; i < iMax; i++) {
          events[i].segment.ringOut = this;
        }
        this.poly = null;
      }
      getGeom() {
        let prevPt = this.events[0].point;
        const points = [prevPt];
        for (let i = 1, iMax = this.events.length - 1; i < iMax; i++) {
          const pt2 = this.events[i].point;
          const nextPt2 = this.events[i + 1].point;
          if (precision.orient(pt2, prevPt, nextPt2) === 0) continue;
          points.push(pt2);
          prevPt = pt2;
        }
        if (points.length === 1) return null;
        const pt = points[0];
        const nextPt = points[1];
        if (precision.orient(pt, prevPt, nextPt) === 0) points.shift();
        points.push(points[0]);
        const step = this.isExteriorRing() ? 1 : -1;
        const iStart = this.isExteriorRing() ? 0 : points.length - 1;
        const iEnd = this.isExteriorRing() ? points.length : -1;
        const orderedPoints = [];
        for (let i = iStart; i != iEnd; i += step)
          orderedPoints.push([points[i].x.toNumber(), points[i].y.toNumber()]);
        return orderedPoints;
      }
      isExteriorRing() {
        if (this._isExteriorRing === void 0) {
          const enclosing = this.enclosingRing();
          this._isExteriorRing = enclosing ? !enclosing.isExteriorRing() : true;
        }
        return this._isExteriorRing;
      }
      enclosingRing() {
        if (this._enclosingRing === void 0) {
          this._enclosingRing = this._calcEnclosingRing();
        }
        return this._enclosingRing;
      }
      /* Returns the ring that encloses this one, if any */
      _calcEnclosingRing() {
        let leftMostEvt = this.events[0];
        for (let i = 1, iMax = this.events.length; i < iMax; i++) {
          const evt = this.events[i];
          if (SweepEvent.compare(leftMostEvt, evt) > 0) leftMostEvt = evt;
        }
        let prevSeg = leftMostEvt.segment.prevInResult();
        let prevPrevSeg = prevSeg ? prevSeg.prevInResult() : null;
        while (true) {
          if (!prevSeg) return null;
          if (!prevPrevSeg) return prevSeg.ringOut;
          if (prevPrevSeg.ringOut !== prevSeg.ringOut) {
            if (prevPrevSeg.ringOut?.enclosingRing() !== prevSeg.ringOut) {
              return prevSeg.ringOut;
            } else return prevSeg.ringOut?.enclosingRing();
          }
          prevSeg = prevPrevSeg.prevInResult();
          prevPrevSeg = prevSeg ? prevSeg.prevInResult() : null;
        }
      }
    };
    PolyOut = class {
      static {
        __name(this, "PolyOut");
      }
      exteriorRing;
      interiorRings;
      constructor(exteriorRing) {
        this.exteriorRing = exteriorRing;
        exteriorRing.poly = this;
        this.interiorRings = [];
      }
      addInterior(ring) {
        this.interiorRings.push(ring);
        ring.poly = this;
      }
      getGeom() {
        const geom0 = this.exteriorRing.getGeom();
        if (geom0 === null) return null;
        const geom = [geom0];
        for (let i = 0, iMax = this.interiorRings.length; i < iMax; i++) {
          const ringGeom = this.interiorRings[i].getGeom();
          if (ringGeom === null) continue;
          geom.push(ringGeom);
        }
        return geom;
      }
    };
    MultiPolyOut = class {
      static {
        __name(this, "MultiPolyOut");
      }
      rings;
      polys;
      constructor(rings) {
        this.rings = rings;
        this.polys = this._composePolys(rings);
      }
      getGeom() {
        const geom = [];
        for (let i = 0, iMax = this.polys.length; i < iMax; i++) {
          const polyGeom = this.polys[i].getGeom();
          if (polyGeom === null) continue;
          geom.push(polyGeom);
        }
        return geom;
      }
      _composePolys(rings) {
        const polys = [];
        for (let i = 0, iMax = rings.length; i < iMax; i++) {
          const ring = rings[i];
          if (ring.poly) continue;
          if (ring.isExteriorRing()) polys.push(new PolyOut(ring));
          else {
            const enclosingRing = ring.enclosingRing();
            if (!enclosingRing?.poly) polys.push(new PolyOut(enclosingRing));
            enclosingRing?.poly?.addInterior(ring);
          }
        }
        return polys;
      }
    };
    SweepLine = class {
      static {
        __name(this, "SweepLine");
      }
      queue;
      tree;
      segments;
      constructor(queue, comparator = Segment.compare) {
        this.queue = queue;
        this.tree = new SplayTreeSet(comparator);
        this.segments = [];
      }
      process(event) {
        const segment = event.segment;
        const newEvents = [];
        if (event.consumedBy) {
          if (event.isLeft) this.queue.delete(event.otherSE);
          else this.tree.delete(segment);
          return newEvents;
        }
        if (event.isLeft) this.tree.add(segment);
        let prevSeg = segment;
        let nextSeg = segment;
        do {
          prevSeg = this.tree.lastBefore(prevSeg);
        } while (prevSeg != null && prevSeg.consumedBy != void 0);
        do {
          nextSeg = this.tree.firstAfter(nextSeg);
        } while (nextSeg != null && nextSeg.consumedBy != void 0);
        if (event.isLeft) {
          let prevMySplitter = null;
          if (prevSeg) {
            const prevInter = prevSeg.getIntersection(segment);
            if (prevInter !== null) {
              if (!segment.isAnEndpoint(prevInter)) prevMySplitter = prevInter;
              if (!prevSeg.isAnEndpoint(prevInter)) {
                const newEventsFromSplit = this._splitSafely(prevSeg, prevInter);
                for (let i = 0, iMax = newEventsFromSplit.length; i < iMax; i++) {
                  newEvents.push(newEventsFromSplit[i]);
                }
              }
            }
          }
          let nextMySplitter = null;
          if (nextSeg) {
            const nextInter = nextSeg.getIntersection(segment);
            if (nextInter !== null) {
              if (!segment.isAnEndpoint(nextInter)) nextMySplitter = nextInter;
              if (!nextSeg.isAnEndpoint(nextInter)) {
                const newEventsFromSplit = this._splitSafely(nextSeg, nextInter);
                for (let i = 0, iMax = newEventsFromSplit.length; i < iMax; i++) {
                  newEvents.push(newEventsFromSplit[i]);
                }
              }
            }
          }
          if (prevMySplitter !== null || nextMySplitter !== null) {
            let mySplitter = null;
            if (prevMySplitter === null) mySplitter = nextMySplitter;
            else if (nextMySplitter === null) mySplitter = prevMySplitter;
            else {
              const cmpSplitters = SweepEvent.comparePoints(
                prevMySplitter,
                nextMySplitter
              );
              mySplitter = cmpSplitters <= 0 ? prevMySplitter : nextMySplitter;
            }
            this.queue.delete(segment.rightSE);
            newEvents.push(segment.rightSE);
            const newEventsFromSplit = segment.split(mySplitter);
            for (let i = 0, iMax = newEventsFromSplit.length; i < iMax; i++) {
              newEvents.push(newEventsFromSplit[i]);
            }
          }
          if (newEvents.length > 0) {
            this.tree.delete(segment);
            newEvents.push(event);
          } else {
            this.segments.push(segment);
            segment.prev = prevSeg;
          }
        } else {
          if (prevSeg && nextSeg) {
            const inter = prevSeg.getIntersection(nextSeg);
            if (inter !== null) {
              if (!prevSeg.isAnEndpoint(inter)) {
                const newEventsFromSplit = this._splitSafely(prevSeg, inter);
                for (let i = 0, iMax = newEventsFromSplit.length; i < iMax; i++) {
                  newEvents.push(newEventsFromSplit[i]);
                }
              }
              if (!nextSeg.isAnEndpoint(inter)) {
                const newEventsFromSplit = this._splitSafely(nextSeg, inter);
                for (let i = 0, iMax = newEventsFromSplit.length; i < iMax; i++) {
                  newEvents.push(newEventsFromSplit[i]);
                }
              }
            }
          }
          this.tree.delete(segment);
        }
        return newEvents;
      }
      /* Safely split a segment that is currently in the datastructures
       * IE - a segment other than the one that is currently being processed. */
      _splitSafely(seg, pt) {
        this.tree.delete(seg);
        const rightSE = seg.rightSE;
        this.queue.delete(rightSE);
        const newEvents = seg.split(pt);
        newEvents.push(rightSE);
        if (seg.consumedBy === void 0) this.tree.add(seg);
        return newEvents;
      }
    };
    Operation = class {
      static {
        __name(this, "Operation");
      }
      type;
      numMultiPolys;
      run(type, geom, moreGeoms) {
        operation.type = type;
        const multipolys = [new MultiPolyIn(geom, true)];
        for (let i = 0, iMax = moreGeoms.length; i < iMax; i++) {
          multipolys.push(new MultiPolyIn(moreGeoms[i], false));
        }
        operation.numMultiPolys = multipolys.length;
        if (operation.type === "difference") {
          const subject = multipolys[0];
          let i = 1;
          while (i < multipolys.length) {
            if (getBboxOverlap(multipolys[i].bbox, subject.bbox) !== null) i++;
            else multipolys.splice(i, 1);
          }
        }
        if (operation.type === "intersection") {
          for (let i = 0, iMax = multipolys.length; i < iMax; i++) {
            const mpA = multipolys[i];
            for (let j = i + 1, jMax = multipolys.length; j < jMax; j++) {
              if (getBboxOverlap(mpA.bbox, multipolys[j].bbox) === null) return [];
            }
          }
        }
        const queue = new SplayTreeSet(SweepEvent.compare);
        for (let i = 0, iMax = multipolys.length; i < iMax; i++) {
          const sweepEvents = multipolys[i].getSweepEvents();
          for (let j = 0, jMax = sweepEvents.length; j < jMax; j++) {
            queue.add(sweepEvents[j]);
          }
        }
        const sweepLine = new SweepLine(queue);
        let evt = null;
        if (queue.size != 0) {
          evt = queue.first();
          queue.delete(evt);
        }
        while (evt) {
          const newEvents = sweepLine.process(evt);
          for (let i = 0, iMax = newEvents.length; i < iMax; i++) {
            const evt2 = newEvents[i];
            if (evt2.consumedBy === void 0) queue.add(evt2);
          }
          if (queue.size != 0) {
            evt = queue.first();
            queue.delete(evt);
          } else {
            evt = null;
          }
        }
        precision.reset();
        const ringsOut = RingOut.factory(sweepLine.segments);
        const result = new MultiPolyOut(ringsOut);
        return result.getGeom();
      }
    };
    operation = new Operation();
    operation_default = operation;
    segmentId = 0;
    Segment = class _Segment {
      static {
        __name(this, "_Segment");
      }
      id;
      leftSE;
      rightSE;
      rings;
      windings;
      ringOut;
      consumedBy;
      prev;
      _prevInResult;
      _beforeState;
      _afterState;
      _isInResult;
      /* This compare() function is for ordering segments in the sweep
       * line tree, and does so according to the following criteria:
       *
       * Consider the vertical line that lies an infinestimal step to the
       * right of the right-more of the two left endpoints of the input
       * segments. Imagine slowly moving a point up from negative infinity
       * in the increasing y direction. Which of the two segments will that
       * point intersect first? That segment comes 'before' the other one.
       *
       * If neither segment would be intersected by such a line, (if one
       * or more of the segments are vertical) then the line to be considered
       * is directly on the right-more of the two left inputs.
       */
      static compare(a, b) {
        const alx = a.leftSE.point.x;
        const blx = b.leftSE.point.x;
        const arx = a.rightSE.point.x;
        const brx = b.rightSE.point.x;
        if (brx.isLessThan(alx)) return 1;
        if (arx.isLessThan(blx)) return -1;
        const aly = a.leftSE.point.y;
        const bly = b.leftSE.point.y;
        const ary = a.rightSE.point.y;
        const bry = b.rightSE.point.y;
        if (alx.isLessThan(blx)) {
          if (bly.isLessThan(aly) && bly.isLessThan(ary)) return 1;
          if (bly.isGreaterThan(aly) && bly.isGreaterThan(ary)) return -1;
          const aCmpBLeft = a.comparePoint(b.leftSE.point);
          if (aCmpBLeft < 0) return 1;
          if (aCmpBLeft > 0) return -1;
          const bCmpARight = b.comparePoint(a.rightSE.point);
          if (bCmpARight !== 0) return bCmpARight;
          return -1;
        }
        if (alx.isGreaterThan(blx)) {
          if (aly.isLessThan(bly) && aly.isLessThan(bry)) return -1;
          if (aly.isGreaterThan(bly) && aly.isGreaterThan(bry)) return 1;
          const bCmpALeft = b.comparePoint(a.leftSE.point);
          if (bCmpALeft !== 0) return bCmpALeft;
          const aCmpBRight = a.comparePoint(b.rightSE.point);
          if (aCmpBRight < 0) return 1;
          if (aCmpBRight > 0) return -1;
          return 1;
        }
        if (aly.isLessThan(bly)) return -1;
        if (aly.isGreaterThan(bly)) return 1;
        if (arx.isLessThan(brx)) {
          const bCmpARight = b.comparePoint(a.rightSE.point);
          if (bCmpARight !== 0) return bCmpARight;
        }
        if (arx.isGreaterThan(brx)) {
          const aCmpBRight = a.comparePoint(b.rightSE.point);
          if (aCmpBRight < 0) return 1;
          if (aCmpBRight > 0) return -1;
        }
        if (!arx.eq(brx)) {
          const ay = ary.minus(aly);
          const ax = arx.minus(alx);
          const by = bry.minus(bly);
          const bx = brx.minus(blx);
          if (ay.isGreaterThan(ax) && by.isLessThan(bx)) return 1;
          if (ay.isLessThan(ax) && by.isGreaterThan(bx)) return -1;
        }
        if (arx.isGreaterThan(brx)) return 1;
        if (arx.isLessThan(brx)) return -1;
        if (ary.isLessThan(bry)) return -1;
        if (ary.isGreaterThan(bry)) return 1;
        if (a.id < b.id) return -1;
        if (a.id > b.id) return 1;
        return 0;
      }
      /* Warning: a reference to ringWindings input will be stored,
       *  and possibly will be later modified */
      constructor(leftSE, rightSE, rings, windings) {
        this.id = ++segmentId;
        this.leftSE = leftSE;
        leftSE.segment = this;
        leftSE.otherSE = rightSE;
        this.rightSE = rightSE;
        rightSE.segment = this;
        rightSE.otherSE = leftSE;
        this.rings = rings;
        this.windings = windings;
      }
      static fromRing(pt1, pt2, ring) {
        let leftPt, rightPt, winding;
        const cmpPts = SweepEvent.comparePoints(pt1, pt2);
        if (cmpPts < 0) {
          leftPt = pt1;
          rightPt = pt2;
          winding = 1;
        } else if (cmpPts > 0) {
          leftPt = pt2;
          rightPt = pt1;
          winding = -1;
        } else
          throw new Error(
            `Tried to create degenerate segment at [${pt1.x}, ${pt1.y}]`
          );
        const leftSE = new SweepEvent(leftPt, true);
        const rightSE = new SweepEvent(rightPt, false);
        return new _Segment(leftSE, rightSE, [ring], [winding]);
      }
      /* When a segment is split, the rightSE is replaced with a new sweep event */
      replaceRightSE(newRightSE) {
        this.rightSE = newRightSE;
        this.rightSE.segment = this;
        this.rightSE.otherSE = this.leftSE;
        this.leftSE.otherSE = this.rightSE;
      }
      bbox() {
        const y12 = this.leftSE.point.y;
        const y2 = this.rightSE.point.y;
        return {
          ll: { x: this.leftSE.point.x, y: y12.isLessThan(y2) ? y12 : y2 },
          ur: { x: this.rightSE.point.x, y: y12.isGreaterThan(y2) ? y12 : y2 }
        };
      }
      /* A vector from the left point to the right */
      vector() {
        return {
          x: this.rightSE.point.x.minus(this.leftSE.point.x),
          y: this.rightSE.point.y.minus(this.leftSE.point.y)
        };
      }
      isAnEndpoint(pt) {
        return pt.x.eq(this.leftSE.point.x) && pt.y.eq(this.leftSE.point.y) || pt.x.eq(this.rightSE.point.x) && pt.y.eq(this.rightSE.point.y);
      }
      /* Compare this segment with a point.
       *
       * A point P is considered to be colinear to a segment if there
       * exists a distance D such that if we travel along the segment
       * from one * endpoint towards the other a distance D, we find
       * ourselves at point P.
       *
       * Return value indicates:
       *
       *   1: point lies above the segment (to the left of vertical)
       *   0: point is colinear to segment
       *  -1: point lies below the segment (to the right of vertical)
       */
      comparePoint(point2) {
        return precision.orient(this.leftSE.point, point2, this.rightSE.point);
      }
      /**
       * Given another segment, returns the first non-trivial intersection
       * between the two segments (in terms of sweep line ordering), if it exists.
       *
       * A 'non-trivial' intersection is one that will cause one or both of the
       * segments to be split(). As such, 'trivial' vs. 'non-trivial' intersection:
       *
       *   * endpoint of segA with endpoint of segB --> trivial
       *   * endpoint of segA with point along segB --> non-trivial
       *   * endpoint of segB with point along segA --> non-trivial
       *   * point along segA with point along segB --> non-trivial
       *
       * If no non-trivial intersection exists, return null
       * Else, return null.
       */
      getIntersection(other) {
        const tBbox = this.bbox();
        const oBbox = other.bbox();
        const bboxOverlap = getBboxOverlap(tBbox, oBbox);
        if (bboxOverlap === null) return null;
        const tlp = this.leftSE.point;
        const trp = this.rightSE.point;
        const olp = other.leftSE.point;
        const orp = other.rightSE.point;
        const touchesOtherLSE = isInBbox(tBbox, olp) && this.comparePoint(olp) === 0;
        const touchesThisLSE = isInBbox(oBbox, tlp) && other.comparePoint(tlp) === 0;
        const touchesOtherRSE = isInBbox(tBbox, orp) && this.comparePoint(orp) === 0;
        const touchesThisRSE = isInBbox(oBbox, trp) && other.comparePoint(trp) === 0;
        if (touchesThisLSE && touchesOtherLSE) {
          if (touchesThisRSE && !touchesOtherRSE) return trp;
          if (!touchesThisRSE && touchesOtherRSE) return orp;
          return null;
        }
        if (touchesThisLSE) {
          if (touchesOtherRSE) {
            if (tlp.x.eq(orp.x) && tlp.y.eq(orp.y)) return null;
          }
          return tlp;
        }
        if (touchesOtherLSE) {
          if (touchesThisRSE) {
            if (trp.x.eq(olp.x) && trp.y.eq(olp.y)) return null;
          }
          return olp;
        }
        if (touchesThisRSE && touchesOtherRSE) return null;
        if (touchesThisRSE) return trp;
        if (touchesOtherRSE) return orp;
        const pt = intersection(tlp, this.vector(), olp, other.vector());
        if (pt === null) return null;
        if (!isInBbox(bboxOverlap, pt)) return null;
        return precision.snap(pt);
      }
      /**
       * Split the given segment into multiple segments on the given points.
       *  * Each existing segment will retain its leftSE and a new rightSE will be
       *    generated for it.
       *  * A new segment will be generated which will adopt the original segment's
       *    rightSE, and a new leftSE will be generated for it.
       *  * If there are more than two points given to split on, new segments
       *    in the middle will be generated with new leftSE and rightSE's.
       *  * An array of the newly generated SweepEvents will be returned.
       *
       * Warning: input array of points is modified
       */
      split(point2) {
        const newEvents = [];
        const alreadyLinked = point2.events !== void 0;
        const newLeftSE = new SweepEvent(point2, true);
        const newRightSE = new SweepEvent(point2, false);
        const oldRightSE = this.rightSE;
        this.replaceRightSE(newRightSE);
        newEvents.push(newRightSE);
        newEvents.push(newLeftSE);
        const newSeg = new _Segment(
          newLeftSE,
          oldRightSE,
          this.rings.slice(),
          this.windings.slice()
        );
        if (SweepEvent.comparePoints(newSeg.leftSE.point, newSeg.rightSE.point) > 0) {
          newSeg.swapEvents();
        }
        if (SweepEvent.comparePoints(this.leftSE.point, this.rightSE.point) > 0) {
          this.swapEvents();
        }
        if (alreadyLinked) {
          newLeftSE.checkForConsuming();
          newRightSE.checkForConsuming();
        }
        return newEvents;
      }
      /* Swap which event is left and right */
      swapEvents() {
        const tmpEvt = this.rightSE;
        this.rightSE = this.leftSE;
        this.leftSE = tmpEvt;
        this.leftSE.isLeft = true;
        this.rightSE.isLeft = false;
        for (let i = 0, iMax = this.windings.length; i < iMax; i++) {
          this.windings[i] *= -1;
        }
      }
      /* Consume another segment. We take their rings under our wing
       * and mark them as consumed. Use for perfectly overlapping segments */
      consume(other) {
        let consumer = this;
        let consumee = other;
        while (consumer.consumedBy) consumer = consumer.consumedBy;
        while (consumee.consumedBy) consumee = consumee.consumedBy;
        const cmp = _Segment.compare(consumer, consumee);
        if (cmp === 0) return;
        if (cmp > 0) {
          const tmp = consumer;
          consumer = consumee;
          consumee = tmp;
        }
        if (consumer.prev === consumee) {
          const tmp = consumer;
          consumer = consumee;
          consumee = tmp;
        }
        for (let i = 0, iMax = consumee.rings.length; i < iMax; i++) {
          const ring = consumee.rings[i];
          const winding = consumee.windings[i];
          const index = consumer.rings.indexOf(ring);
          if (index === -1) {
            consumer.rings.push(ring);
            consumer.windings.push(winding);
          } else consumer.windings[index] += winding;
        }
        consumee.rings = null;
        consumee.windings = null;
        consumee.consumedBy = consumer;
        consumee.leftSE.consumedBy = consumer.leftSE;
        consumee.rightSE.consumedBy = consumer.rightSE;
      }
      /* The first segment previous segment chain that is in the result */
      prevInResult() {
        if (this._prevInResult !== void 0) return this._prevInResult;
        if (!this.prev) this._prevInResult = null;
        else if (this.prev.isInResult()) this._prevInResult = this.prev;
        else this._prevInResult = this.prev.prevInResult();
        return this._prevInResult;
      }
      beforeState() {
        if (this._beforeState !== void 0) return this._beforeState;
        if (!this.prev)
          this._beforeState = {
            rings: [],
            windings: [],
            multiPolys: []
          };
        else {
          const seg = this.prev.consumedBy || this.prev;
          this._beforeState = seg.afterState();
        }
        return this._beforeState;
      }
      afterState() {
        if (this._afterState !== void 0) return this._afterState;
        const beforeState = this.beforeState();
        this._afterState = {
          rings: beforeState.rings.slice(0),
          windings: beforeState.windings.slice(0),
          multiPolys: []
        };
        const ringsAfter = this._afterState.rings;
        const windingsAfter = this._afterState.windings;
        const mpsAfter = this._afterState.multiPolys;
        for (let i = 0, iMax = this.rings.length; i < iMax; i++) {
          const ring = this.rings[i];
          const winding = this.windings[i];
          const index = ringsAfter.indexOf(ring);
          if (index === -1) {
            ringsAfter.push(ring);
            windingsAfter.push(winding);
          } else windingsAfter[index] += winding;
        }
        const polysAfter = [];
        const polysExclude = [];
        for (let i = 0, iMax = ringsAfter.length; i < iMax; i++) {
          if (windingsAfter[i] === 0) continue;
          const ring = ringsAfter[i];
          const poly = ring.poly;
          if (polysExclude.indexOf(poly) !== -1) continue;
          if (ring.isExterior) polysAfter.push(poly);
          else {
            if (polysExclude.indexOf(poly) === -1) polysExclude.push(poly);
            const index = polysAfter.indexOf(ring.poly);
            if (index !== -1) polysAfter.splice(index, 1);
          }
        }
        for (let i = 0, iMax = polysAfter.length; i < iMax; i++) {
          const mp = polysAfter[i].multiPoly;
          if (mpsAfter.indexOf(mp) === -1) mpsAfter.push(mp);
        }
        return this._afterState;
      }
      /* Is this segment part of the final result? */
      isInResult() {
        if (this.consumedBy) return false;
        if (this._isInResult !== void 0) return this._isInResult;
        const mpsBefore = this.beforeState().multiPolys;
        const mpsAfter = this.afterState().multiPolys;
        switch (operation_default.type) {
          case "union": {
            const noBefores = mpsBefore.length === 0;
            const noAfters = mpsAfter.length === 0;
            this._isInResult = noBefores !== noAfters;
            break;
          }
          case "intersection": {
            let least;
            let most;
            if (mpsBefore.length < mpsAfter.length) {
              least = mpsBefore.length;
              most = mpsAfter.length;
            } else {
              least = mpsAfter.length;
              most = mpsBefore.length;
            }
            this._isInResult = most === operation_default.numMultiPolys && least < most;
            break;
          }
          case "xor": {
            const diff = Math.abs(mpsBefore.length - mpsAfter.length);
            this._isInResult = diff % 2 === 1;
            break;
          }
          case "difference": {
            const isJustSubject = /* @__PURE__ */ __name((mps) => mps.length === 1 && mps[0].isSubject, "isJustSubject");
            this._isInResult = isJustSubject(mpsBefore) !== isJustSubject(mpsAfter);
            break;
          }
        }
        return this._isInResult;
      }
    };
    RingIn = class {
      static {
        __name(this, "RingIn");
      }
      poly;
      isExterior;
      segments;
      bbox;
      constructor(geomRing, poly, isExterior) {
        if (!Array.isArray(geomRing) || geomRing.length === 0) {
          throw new Error("Input geometry is not a valid Polygon or MultiPolygon");
        }
        this.poly = poly;
        this.isExterior = isExterior;
        this.segments = [];
        if (typeof geomRing[0][0] !== "number" || typeof geomRing[0][1] !== "number") {
          throw new Error("Input geometry is not a valid Polygon or MultiPolygon");
        }
        const firstPoint = precision.snap({ x: new bignumber_default(geomRing[0][0]), y: new bignumber_default(geomRing[0][1]) });
        this.bbox = {
          ll: { x: firstPoint.x, y: firstPoint.y },
          ur: { x: firstPoint.x, y: firstPoint.y }
        };
        let prevPoint = firstPoint;
        for (let i = 1, iMax = geomRing.length; i < iMax; i++) {
          if (typeof geomRing[i][0] !== "number" || typeof geomRing[i][1] !== "number") {
            throw new Error("Input geometry is not a valid Polygon or MultiPolygon");
          }
          const point2 = precision.snap({ x: new bignumber_default(geomRing[i][0]), y: new bignumber_default(geomRing[i][1]) });
          if (point2.x.eq(prevPoint.x) && point2.y.eq(prevPoint.y)) continue;
          this.segments.push(Segment.fromRing(prevPoint, point2, this));
          if (point2.x.isLessThan(this.bbox.ll.x)) this.bbox.ll.x = point2.x;
          if (point2.y.isLessThan(this.bbox.ll.y)) this.bbox.ll.y = point2.y;
          if (point2.x.isGreaterThan(this.bbox.ur.x)) this.bbox.ur.x = point2.x;
          if (point2.y.isGreaterThan(this.bbox.ur.y)) this.bbox.ur.y = point2.y;
          prevPoint = point2;
        }
        if (!firstPoint.x.eq(prevPoint.x) || !firstPoint.y.eq(prevPoint.y)) {
          this.segments.push(Segment.fromRing(prevPoint, firstPoint, this));
        }
      }
      getSweepEvents() {
        const sweepEvents = [];
        for (let i = 0, iMax = this.segments.length; i < iMax; i++) {
          const segment = this.segments[i];
          sweepEvents.push(segment.leftSE);
          sweepEvents.push(segment.rightSE);
        }
        return sweepEvents;
      }
    };
    PolyIn = class {
      static {
        __name(this, "PolyIn");
      }
      multiPoly;
      exteriorRing;
      interiorRings;
      bbox;
      constructor(geomPoly, multiPoly) {
        if (!Array.isArray(geomPoly)) {
          throw new Error("Input geometry is not a valid Polygon or MultiPolygon");
        }
        this.exteriorRing = new RingIn(geomPoly[0], this, true);
        this.bbox = {
          ll: { x: this.exteriorRing.bbox.ll.x, y: this.exteriorRing.bbox.ll.y },
          ur: { x: this.exteriorRing.bbox.ur.x, y: this.exteriorRing.bbox.ur.y }
        };
        this.interiorRings = [];
        for (let i = 1, iMax = geomPoly.length; i < iMax; i++) {
          const ring = new RingIn(geomPoly[i], this, false);
          if (ring.bbox.ll.x.isLessThan(this.bbox.ll.x)) this.bbox.ll.x = ring.bbox.ll.x;
          if (ring.bbox.ll.y.isLessThan(this.bbox.ll.y)) this.bbox.ll.y = ring.bbox.ll.y;
          if (ring.bbox.ur.x.isGreaterThan(this.bbox.ur.x)) this.bbox.ur.x = ring.bbox.ur.x;
          if (ring.bbox.ur.y.isGreaterThan(this.bbox.ur.y)) this.bbox.ur.y = ring.bbox.ur.y;
          this.interiorRings.push(ring);
        }
        this.multiPoly = multiPoly;
      }
      getSweepEvents() {
        const sweepEvents = this.exteriorRing.getSweepEvents();
        for (let i = 0, iMax = this.interiorRings.length; i < iMax; i++) {
          const ringSweepEvents = this.interiorRings[i].getSweepEvents();
          for (let j = 0, jMax = ringSweepEvents.length; j < jMax; j++) {
            sweepEvents.push(ringSweepEvents[j]);
          }
        }
        return sweepEvents;
      }
    };
    MultiPolyIn = class {
      static {
        __name(this, "MultiPolyIn");
      }
      isSubject;
      polys;
      bbox;
      constructor(geom, isSubject) {
        if (!Array.isArray(geom)) {
          throw new Error("Input geometry is not a valid Polygon or MultiPolygon");
        }
        try {
          if (typeof geom[0][0][0] === "number") geom = [geom];
        } catch (ex) {
        }
        this.polys = [];
        this.bbox = {
          ll: { x: new bignumber_default(Number.POSITIVE_INFINITY), y: new bignumber_default(Number.POSITIVE_INFINITY) },
          ur: { x: new bignumber_default(Number.NEGATIVE_INFINITY), y: new bignumber_default(Number.NEGATIVE_INFINITY) }
        };
        for (let i = 0, iMax = geom.length; i < iMax; i++) {
          const poly = new PolyIn(geom[i], this);
          if (poly.bbox.ll.x.isLessThan(this.bbox.ll.x)) this.bbox.ll.x = poly.bbox.ll.x;
          if (poly.bbox.ll.y.isLessThan(this.bbox.ll.y)) this.bbox.ll.y = poly.bbox.ll.y;
          if (poly.bbox.ur.x.isGreaterThan(this.bbox.ur.x)) this.bbox.ur.x = poly.bbox.ur.x;
          if (poly.bbox.ur.y.isGreaterThan(this.bbox.ur.y)) this.bbox.ur.y = poly.bbox.ur.y;
          this.polys.push(poly);
        }
        this.isSubject = isSubject;
      }
      getSweepEvents() {
        const sweepEvents = [];
        for (let i = 0, iMax = this.polys.length; i < iMax; i++) {
          const polySweepEvents = this.polys[i].getSweepEvents();
          for (let j = 0, jMax = polySweepEvents.length; j < jMax; j++) {
            sweepEvents.push(polySweepEvents[j]);
          }
        }
        return sweepEvents;
      }
    };
    union3 = /* @__PURE__ */ __name((geom, ...moreGeoms) => operation_default.run("union", geom, moreGeoms), "union");
    intersection2 = /* @__PURE__ */ __name((geom, ...moreGeoms) => operation_default.run("intersection", geom, moreGeoms), "intersection2");
    difference = /* @__PURE__ */ __name((geom, ...moreGeoms) => operation_default.run("difference", geom, moreGeoms), "difference");
    setPrecision = precision.set;
  }
});

// worker/node_modules/@turf/difference/dist/esm/index.js
function difference2(features) {
  const geoms = [];
  geomEach(features, (geom) => {
    geoms.push(geom.coordinates);
  });
  if (geoms.length < 2) {
    throw new Error("Must have at least two features");
  }
  const properties = features.features[0].properties || {};
  const differenced = difference(geoms[0], ...geoms.slice(1));
  if (differenced.length === 0) return null;
  if (differenced.length === 1) return polygon(differenced[0], properties);
  return multiPolygon(differenced, properties);
}
var init_esm14 = __esm({
  "worker/node_modules/@turf/difference/dist/esm/index.js"() {
    init_esm13();
    init_esm();
    init_esm4();
    __name(difference2, "difference2");
  }
});

// worker/node_modules/@turf/explode/dist/esm/index.js
function explode(geojson) {
  const points = [];
  if (geojson.type === "FeatureCollection") {
    featureEach(geojson, function(feature2) {
      coordEach(feature2, function(coord) {
        points.push(point(coord, feature2.properties));
      });
    });
  } else if (geojson.type === "Feature") {
    coordEach(geojson, function(coord) {
      points.push(point(coord, geojson.properties));
    });
  } else {
    coordEach(geojson, function(coord) {
      points.push(point(coord));
    });
  }
  return featureCollection(points);
}
var init_esm15 = __esm({
  "worker/node_modules/@turf/explode/dist/esm/index.js"() {
    init_esm4();
    init_esm();
    __name(explode, "explode");
  }
});

// worker/node_modules/@turf/intersect/dist/esm/index.js
function intersect3(features, options = {}) {
  const geoms = [];
  geomEach(features, (geom) => {
    geoms.push(geom.coordinates);
  });
  if (geoms.length < 2) {
    throw new Error("Must specify at least 2 geometries");
  }
  const intersection22 = intersection2(geoms[0], ...geoms.slice(1));
  if (intersection22.length === 0) return null;
  if (intersection22.length === 1)
    return polygon(intersection22[0], options.properties);
  return multiPolygon(intersection22, options.properties);
}
var init_esm16 = __esm({
  "worker/node_modules/@turf/intersect/dist/esm/index.js"() {
    init_esm();
    init_esm4();
    init_esm13();
    __name(intersect3, "intersect");
  }
});

// worker/node_modules/@turf/length/dist/esm/index.js
function length2(geojson, options = {}) {
  return segmentReduce(
    geojson,
    (previousValue, segment) => {
      const coords = segment.geometry.coordinates;
      return previousValue + distance(coords[0], coords[1], options);
    },
    0
  );
}
var init_esm17 = __esm({
  "worker/node_modules/@turf/length/dist/esm/index.js"() {
    init_esm3();
    init_esm4();
    __name(length2, "length");
  }
});

// worker/node_modules/@turf/nearest-neighbor-analysis/dist/esm/index.js
var init_esm18 = __esm({
  "worker/node_modules/@turf/nearest-neighbor-analysis/dist/esm/index.js"() {
  }
});

// worker/node_modules/@turf/projection/dist/esm/index.js
var init_esm19 = __esm({
  "worker/node_modules/@turf/projection/dist/esm/index.js"() {
  }
});

// worker/node_modules/@turf/quadrat-analysis/dist/esm/index.js
var init_esm20 = __esm({
  "worker/node_modules/@turf/quadrat-analysis/dist/esm/index.js"() {
  }
});

// worker/node_modules/@turf/random/dist/esm/index.js
var init_esm21 = __esm({
  "worker/node_modules/@turf/random/dist/esm/index.js"() {
  }
});

// worker/node_modules/@turf/union/dist/esm/index.js
function union22(features, options = {}) {
  const geoms = [];
  geomEach(features, (geom) => {
    geoms.push(geom.coordinates);
  });
  if (geoms.length < 2) {
    throw new Error("Must have at least 2 geometries");
  }
  const unioned = union3(geoms[0], ...geoms.slice(1));
  if (unioned.length === 0) return null;
  if (unioned.length === 1) return polygon(unioned[0], options.properties);
  else return multiPolygon(unioned, options.properties);
}
var init_esm22 = __esm({
  "worker/node_modules/@turf/union/dist/esm/index.js"() {
    init_esm13();
    init_esm();
    init_esm4();
    __name(union22, "union2");
  }
});

// worker/node_modules/@turf/turf/dist/esm/index.js
var init_esm23 = __esm({
  "worker/node_modules/@turf/turf/dist/esm/index.js"() {
    init_esm5();
    init_esm6();
    init_esm8();
    init_esm10();
    init_esm11();
    init_esm9();
    init_esm14();
    init_esm3();
    init_esm15();
    init_esm();
    init_esm16();
    init_esm2();
    init_esm17();
    init_esm4();
    init_esm18();
    init_esm19();
    init_esm20();
    init_esm21();
    init_esm22();
  }
});

// worker/node_modules/drizzle-orm/expressions.js
var init_expressions4 = __esm({
  "worker/node_modules/drizzle-orm/expressions.js"() {
    init_expressions();
  }
});

// worker/node_modules/drizzle-orm/operations.js
var init_operations2 = __esm({
  "worker/node_modules/drizzle-orm/operations.js"() {
  }
});

// worker/node_modules/drizzle-orm/index.js
var init_drizzle_orm2 = __esm({
  "worker/node_modules/drizzle-orm/index.js"() {
    init_alias();
    init_column_builder();
    init_column();
    init_entity();
    init_errors2();
    init_expressions4();
    init_logger();
    init_operations2();
    init_query_promise();
    init_relations();
    init_sql3();
    init_subquery();
    init_table();
    init_utils();
    init_view_common();
  }
});

// worker/src/storage.ts
function geometryToFeature(geometry) {
  if (geometry.type === "MultiPolygon") {
    return multiPolygon(geometry.coordinates);
  }
  return polygon(geometry.coordinates);
}
function safeParseGeometry(geometry) {
  if (typeof geometry === "string") {
    try {
      return JSON.parse(geometry);
    } catch {
      return null;
    }
  }
  return geometry;
}
function safeUnion(geomA, geomB) {
  try {
    const featureA = geometryToFeature(geomA);
    const featureB = geometryToFeature(geomB);
    const result = union22(featureCollection([featureA, featureB]));
    return result ? result.geometry : null;
  } catch (err) {
    console.error("[TERRITORY] safeUnion failed:", err);
    return null;
  }
}
var WorkerStorage;
var init_storage = __esm({
  "worker/src/storage.ts"() {
    "use strict";
    init_esm23();
    init_schema();
    init_drizzle_orm2();
    __name(geometryToFeature, "geometryToFeature");
    __name(safeParseGeometry, "safeParseGeometry");
    __name(safeUnion, "safeUnion");
    WorkerStorage = class _WorkerStorage {
      constructor(db) {
        this.db = db;
      }
      static {
        __name(this, "WorkerStorage");
      }
      _competitionTablesEnsured = false;
      _feedTablesEnsured = false;
      async getUser(id) {
        const [user] = await this.db.select().from(users).where(eq(users.id, id));
        return user || void 0;
      }
      async getUserByUsername(username) {
        const [user] = await this.db.select().from(users).where(eq(users.username, username));
        return user || void 0;
      }
      async getUserByEmail(email) {
        const [user] = await this.db.select().from(users).where(eq(users.email, email));
        return user || void 0;
      }
      async createUser(insertUser) {
        const [user] = await this.db.insert(users).values(insertUser).returning();
        return user;
      }
      // Ensure a "system" user exists for system-generated feed events (treasure spawns, etc.)
      static SYSTEM_USER_ID = "__system_runna__";
      async ensureSystemUser() {
        const existing = await this.db.select().from(users).where(eq(users.id, _WorkerStorage.SYSTEM_USER_ID)).limit(1);
        if (existing.length > 0) return _WorkerStorage.SYSTEM_USER_ID;
        try {
          await this.db.run(sql`INSERT OR IGNORE INTO users (id, username, email, name, password, color) VALUES (${_WorkerStorage.SYSTEM_USER_ID}, '_runna_system', 'system@runna.io', 'Runna', '', '#16a34a')`);
        } catch (_) {
        }
        return _WorkerStorage.SYSTEM_USER_ID;
      }
      async deleteUser(userId) {
        await this.db.delete(feedComments).where(eq(feedComments.userId, userId));
        await this.db.delete(feedEvents).where(eq(feedEvents.userId, userId));
        await this.db.delete(ephemeralPhotos).where(eq(ephemeralPhotos.senderId, userId));
        await this.db.delete(inactivityReminders).where(eq(inactivityReminders.userId, userId));
        await this.db.delete(territoryLossNotifications).where(eq(territoryLossNotifications.userId, userId));
        await this.db.delete(pushSubscriptions).where(eq(pushSubscriptions.userId, userId));
        await this.db.delete(emailPreferences).where(eq(emailPreferences.userId, userId));
        await this.db.delete(emailNotifications).where(eq(emailNotifications.userId, userId));
        await this.db.delete(conquestMetrics).where(eq(conquestMetrics.attackerId, userId));
        await this.db.delete(conquestMetrics).where(eq(conquestMetrics.defenderId, userId));
        await this.db.delete(polarActivities).where(eq(polarActivities.userId, userId));
        await this.db.delete(polarAccounts).where(eq(polarAccounts.userId, userId));
        await this.db.delete(stravaActivities).where(eq(stravaActivities.userId, userId));
        await this.db.delete(stravaAccounts).where(eq(stravaAccounts.userId, userId));
        await this.db.delete(friendRequests).where(eq(friendRequests.senderId, userId));
        await this.db.delete(friendRequests).where(eq(friendRequests.recipientId, userId));
        await this.db.delete(friendInvites).where(eq(friendInvites.userId, userId));
        await this.db.delete(friendships).where(eq(friendships.userId, userId));
        await this.db.delete(friendships).where(eq(friendships.friendId, userId));
        await this.db.delete(territories).where(eq(territories.userId, userId));
        await this.db.delete(routes).where(eq(routes.userId, userId));
        await this.db.delete(users).where(eq(users.id, userId));
      }
      async getAllUsersWithStats() {
        const allUsers = await this.db.select({
          id: users.id,
          username: users.username,
          name: users.name,
          color: users.color,
          avatar: users.avatar,
          totalArea: users.totalArea,
          createdAt: users.createdAt
        }).from(users).orderBy(desc(users.totalArea));
        const usersWithStats = await Promise.all(
          allUsers.map(async (user, index) => {
            const friendCount = await this.db.select({ count: sql`count(*)` }).from(friendships).where(eq(friendships.userId, user.id));
            return {
              ...user,
              rank: index + 1,
              friendCount: Number(friendCount[0]?.count || 0)
            };
          })
        );
        return usersWithStats;
      }
      async updateUser(userId, data) {
        const [updatedUser] = await this.db.update(users).set(data).where(eq(users.id, userId)).returning();
        return updatedUser;
      }
      async updateUserTotalArea(userId, area2) {
        await this.db.update(users).set({ totalArea: area2 }).where(eq(users.id, userId));
      }
      async createRoute(insertRoute) {
        const coordsValue = typeof insertRoute.coordinates === "string" ? insertRoute.coordinates : JSON.stringify(insertRoute.coordinates);
        const routeValues = {
          ...insertRoute,
          coordinates: coordsValue
        };
        const [route] = await this.db.insert(routes).values(routeValues).returning();
        return route;
      }
      // Get all routes from all users, sorted oldest first (for chronological reprocessing)
      async getAllRoutesChronological() {
        return await this.db.select().from(routes).orderBy(routes.completedAt);
      }
      // Get routes for a single user, sorted oldest first (chronological order for territory rebuilding)
      async getRoutesByUserIdChronological(userId) {
        return await this.db.select().from(routes).where(eq(routes.userId, userId)).orderBy(routes.completedAt);
      }
      async getRoutesByUserIdChronologicalPaginated(userId, limit, offset) {
        return await this.db.select().from(routes).where(eq(routes.userId, userId)).orderBy(routes.completedAt).limit(limit).offset(offset);
      }
      async getRouteCountByUserId(userId) {
        const result = await this.db.select({ count: sql`COUNT(*)` }).from(routes).where(eq(routes.userId, userId));
        return Number(result[0]?.count || 0);
      }
      // Get routes for a set of users, sorted oldest first
      async getRoutesForUsersChronological(userIds) {
        if (userIds.length === 0) return [];
        const placeholders = userIds.map((id) => `'${id.replace(/'/g, "''")}'`).join(",");
        return await this.db.select().from(routes).where(sql`${routes.userId} IN (${sql.raw(placeholders)})`).orderBy(routes.completedAt);
      }
      // Get routes from specific users that started within a time window of a given timestamp
      async getRoutesInTimeWindow(userIds, centerTime, windowMs = 15 * 60 * 1e3) {
        if (userIds.length === 0) return [];
        const center2 = new Date(centerTime).getTime();
        const minTime = new Date(center2 - windowMs).toISOString();
        const maxTime = new Date(center2 + windowMs).toISOString();
        const placeholders = userIds.map((id) => `'${id.replace(/'/g, "''")}'`).join(",");
        return await this.db.select().from(routes).where(sql`${routes.userId} IN (${sql.raw(placeholders)}) AND ${routes.startedAt} >= ${minTime} AND ${routes.startedAt} <= ${maxTime}`);
      }
      async getRoutesByUserId(userId) {
        const userRoutes = await this.db.select().from(routes).where(eq(routes.userId, userId)).orderBy(desc(routes.completedAt));
        const routesWithTerritories = await Promise.all(
          userRoutes.map(async (route) => {
            const [territory] = await this.db.select().from(territories).where(eq(territories.routeId, route.id));
            let parsedCoordinates = [];
            if (Array.isArray(route.coordinates)) {
              parsedCoordinates = route.coordinates;
            } else if (typeof route.coordinates === "string") {
              try {
                let parsed = JSON.parse(route.coordinates);
                if (typeof parsed === "string") {
                  try {
                    parsed = JSON.parse(parsed);
                  } catch (_) {
                  }
                }
                parsedCoordinates = Array.isArray(parsed) ? parsed : [];
              } catch (e) {
                console.error("Error parsing route coordinates:", e);
                parsedCoordinates = [];
              }
            }
            let ranTogetherWithUsers = [];
            if (route.ranTogetherWith) {
              try {
                const userIds = typeof route.ranTogetherWith === "string" ? JSON.parse(route.ranTogetherWith) : route.ranTogetherWith;
                if (Array.isArray(userIds) && userIds.length > 0) {
                  for (const userId2 of userIds) {
                    const [user] = await this.db.select({ id: users.id, name: users.name }).from(users).where(eq(users.id, userId2));
                    if (user) {
                      ranTogetherWithUsers.push(user);
                    }
                  }
                }
              } catch (e) {
                console.error("Error parsing ranTogetherWith:", e);
              }
            }
            return {
              ...route,
              coordinates: parsedCoordinates,
              territory: territory || void 0,
              ranTogetherWithUsers
            };
          })
        );
        return routesWithTerritories;
      }
      async getRoute(id) {
        const [route] = await this.db.select().from(routes).where(eq(routes.id, id));
        if (!route) return void 0;
        const parsedCoordinates = typeof route.coordinates === "string" ? JSON.parse(route.coordinates) : route.coordinates;
        return {
          ...route,
          coordinates: parsedCoordinates
        };
      }
      // Batch-load routes by IDs (only startedAt/completedAt needed for ran-together check)
      async getRouteTimesById(ids) {
        const result = /* @__PURE__ */ new Map();
        if (ids.length === 0) return result;
        const rows = await this.db.select({ id: routes.id, startedAt: routes.startedAt, completedAt: routes.completedAt }).from(routes).where(inArray(routes.id, ids));
        for (const row of rows) {
          result.set(row.id, { startedAt: row.startedAt, completedAt: row.completedAt });
        }
        return result;
      }
      async updateRouteRanTogether(routeId, ranTogetherWith) {
        await this.db.update(routes).set({ ranTogetherWith: JSON.stringify(ranTogetherWith) }).where(eq(routes.id, routeId));
      }
      async createTerritory(insertTerritory) {
        const [territory] = await this.db.insert(territories).values(insertTerritory).returning();
        return territory;
      }
      async getAllTerritories() {
        const allTerritories = await this.db.select({
          id: territories.id,
          userId: territories.userId,
          routeId: territories.routeId,
          geometry: territories.geometry,
          area: territories.area,
          conqueredAt: territories.conqueredAt,
          userName: users.name,
          userUsername: users.username,
          userColor: users.color,
          ranTogetherWith: routes.ranTogetherWith
        }).from(territories).leftJoin(users, eq(territories.userId, users.id)).leftJoin(routes, eq(territories.routeId, routes.id)).orderBy(desc(territories.conqueredAt));
        return this._enrichTerritoriesWithCoRunnerColors(allTerritories);
      }
      async getTerritoriesByUserId(userId) {
        return await this.db.select().from(territories).where(eq(territories.userId, userId)).orderBy(desc(territories.conqueredAt));
      }
      // Get total area for a user directly from DB (no geometry transfer)
      async getUserTotalAreaFromTerritories(userId) {
        const result = await this.db.select({ total: sql`COALESCE(SUM(${territories.area}), 0)` }).from(territories).where(eq(territories.userId, userId));
        return result[0]?.total || 0;
      }
      // Get territories only for specific users (much faster than getAllTerritories)
      async getTerritoriesForUsers(userIds) {
        if (userIds.length === 0) return [];
        const results = await this.db.select({
          id: territories.id,
          userId: territories.userId,
          routeId: territories.routeId,
          geometry: territories.geometry,
          area: territories.area,
          conqueredAt: territories.conqueredAt,
          userName: users.name,
          userUsername: users.username,
          userColor: users.color,
          ranTogetherWith: routes.ranTogetherWith
        }).from(territories).leftJoin(users, eq(territories.userId, users.id)).leftJoin(routes, eq(territories.routeId, routes.id)).where(inArray(territories.userId, userIds)).orderBy(desc(territories.conqueredAt));
        return this._enrichTerritoriesWithCoRunnerColors(results);
      }
      // Helper: resolve co-runner user colors for territories that have ranTogetherWith
      async _enrichTerritoriesWithCoRunnerColors(rows) {
        const coRunnerIds = /* @__PURE__ */ new Set();
        const parsedRanTogether = /* @__PURE__ */ new Map();
        for (const t of rows) {
          if (t.ranTogetherWith) {
            try {
              const ids = JSON.parse(t.ranTogetherWith);
              if (Array.isArray(ids) && ids.length > 0) {
                parsedRanTogether.set(t.id, ids);
                ids.forEach((id) => coRunnerIds.add(id));
              }
            } catch {
            }
          }
        }
        let coRunnerColorMap = /* @__PURE__ */ new Map();
        if (coRunnerIds.size > 0) {
          const coRunnerUsers = await this.db.select({ id: users.id, color: users.color }).from(users).where(inArray(users.id, [...coRunnerIds]));
          coRunnerColorMap = new Map(coRunnerUsers.map((u) => [u.id, u.color]));
        }
        return rows.map((t) => {
          const coRunnerUserIds = parsedRanTogether.get(t.id);
          const ranTogetherWithColors = coRunnerUserIds ? coRunnerUserIds.map((uid) => ({ id: uid, color: coRunnerColorMap.get(uid) || "#888888" })).filter((u) => u.id !== t.userId) : void 0;
          return {
            id: t.id,
            userId: t.userId,
            routeId: t.routeId,
            geometry: t.geometry,
            area: t.area,
            conqueredAt: t.conqueredAt,
            user: {
              id: t.userId,
              username: t.userUsername || "",
              name: t.userName || "",
              color: t.userColor || "#000000"
            },
            ranTogetherWithColors: ranTogetherWithColors && ranTogetherWithColors.length > 0 ? ranTogetherWithColors : void 0
          };
        });
      }
      async deleteTerritoryById(id) {
        await this.db.delete(territories).where(eq(territories.id, id));
      }
      async detachTerritoriesFromRoute(routeId) {
        await this.db.update(territories).set({ routeId: null }).where(eq(territories.routeId, routeId));
      }
      async cleanStaleRouteIds() {
        await this.db.run(sql`UPDATE territories SET route_id = NULL WHERE route_id IS NOT NULL AND route_id NOT IN (SELECT id FROM routes)`);
      }
      async subtractFromTerritory(territoryId, subtractionGeometry) {
        const [territory] = await this.db.select().from(territories).where(eq(territories.id, territoryId));
        if (!territory) {
          throw new Error("Territory not found");
        }
        return this.subtractFromTerritoryDirect(territory, subtractionGeometry);
      }
      // Like subtractFromTerritory but accepts a pre-loaded territory (avoids redundant DB read)
      async subtractFromTerritoryDirect(territory, subtractionGeometry) {
        const parsedGeometry = safeParseGeometry(territory.geometry);
        if (!parsedGeometry) {
          console.error(`[TERRITORY] Could not parse geometry for territory ${territory.id}`);
          return { updatedTerritory: territory, stolenArea: 0 };
        }
        let originalFeature;
        let subtractionFeature;
        try {
          originalFeature = geometryToFeature(parsedGeometry);
          subtractionFeature = geometryToFeature(subtractionGeometry);
        } catch (err) {
          console.error(`[TERRITORY] Could not create features for territory ${territory.id}:`, err);
          return { updatedTerritory: territory, stolenArea: 0 };
        }
        const intersection3 = intersect3(
          featureCollection([originalFeature, subtractionFeature])
        );
        if (!intersection3) {
          return { updatedTerritory: territory, stolenArea: 0 };
        }
        const stolenArea = area(intersection3);
        const difference3 = difference2(
          featureCollection([originalFeature, subtractionFeature])
        );
        if (!difference3) {
          await this.db.delete(territories).where(eq(territories.id, territory.id));
          return { updatedTerritory: null, stolenArea };
        }
        const newArea = area(difference3);
        const [updatedTerritory] = await this.db.update(territories).set({
          geometry: JSON.stringify(difference3.geometry),
          area: newArea
        }).where(eq(territories.id, territory.id)).returning();
        return { updatedTerritory, stolenArea };
      }
      async addOrMergeTerritory(userId, routeId, newGeometry, userTerritories, fortificationMultiplier = 1) {
        const newFeature = geometryToFeature(newGeometry);
        let newArea = area(newGeometry);
        let existingAreaInBuffer = 0;
        const overlapGeometries = [];
        for (const userTerritory of userTerritories) {
          try {
            const parsedGeometry = safeParseGeometry(userTerritory.geometry);
            if (!parsedGeometry) continue;
            const userFeature = geometryToFeature(parsedGeometry);
            const overlap = intersect3(
              featureCollection([newFeature, userFeature])
            );
            if (overlap) {
              existingAreaInBuffer += area(overlap);
              overlapGeometries.push(overlap.geometry);
            }
          } catch (err) {
            console.error("[TERRITORY] Error calculating overlap with existing territory:", err);
          }
        }
        let fortificationLayers = 0;
        for (const overlapGeom of overlapGeometries) {
          try {
            const overlapArea = area(overlapGeom);
            if (overlapArea < 10) continue;
            const bbox2 = bbox(overlapGeom);
            const recordsToCreate = fortificationMultiplier;
            for (let i = 0; i < recordsToCreate; i++) {
              await this.createFortificationRecord({
                userId,
                routeId,
                geometry: JSON.stringify(overlapGeom),
                area: overlapArea,
                bboxMinLng: bbox2[0],
                bboxMinLat: bbox2[1],
                bboxMaxLng: bbox2[2],
                bboxMaxLat: bbox2[3]
              });
              fortificationLayers++;
            }
          } catch (err) {
            console.error("[TERRITORY] Error saving fortification record:", err);
          }
        }
        const actualNewArea = newArea - existingAreaInBuffer;
        let finalGeometry = newGeometry;
        let mergeSucceeded = true;
        for (const userTerritory of userTerritories) {
          try {
            const parsedGeometry = safeParseGeometry(userTerritory.geometry);
            if (!parsedGeometry) continue;
            const merged = safeUnion(finalGeometry, parsedGeometry);
            if (merged) {
              finalGeometry = merged;
            } else {
              console.error(`[TERRITORY] Union failed for territory ${userTerritory.id}, geometry may be corrupt`);
              mergeSucceeded = false;
            }
          } catch (err) {
            console.error("[TERRITORY] Error merging territories:", err);
            mergeSucceeded = false;
          }
        }
        let finalArea;
        try {
          finalArea = area(finalGeometry);
          if (finalArea <= 0 || !isFinite(finalArea)) {
            throw new Error(`Invalid final area: ${finalArea}`);
          }
        } catch (err) {
          console.error("[TERRITORY] Final geometry validation failed, keeping existing territories:", err);
          const [territory2] = await this.db.insert(territories).values({
            userId,
            routeId,
            geometry: JSON.stringify(newGeometry),
            area: newArea,
            conqueredAt: /* @__PURE__ */ new Date()
          }).returning();
          const totalArea = await this.getUserTotalAreaFromTerritories(userId);
          return {
            territory: territory2,
            totalArea,
            newArea: actualNewArea,
            existingArea: existingAreaInBuffer,
            fortificationLayers
          };
        }
        const existingTotalArea = userTerritories.reduce((sum2, t) => sum2 + (t.area || 0), 0);
        if (existingTotalArea > 0 && finalArea < existingTotalArea * 0.5) {
          console.error(`[TERRITORY] SAFETY: Merged area (${finalArea}) is much less than existing (${existingTotalArea}). Aborting merge to prevent data loss.`);
          const [territory2] = await this.db.insert(territories).values({
            userId,
            routeId,
            geometry: JSON.stringify(newGeometry),
            area: newArea,
            conqueredAt: /* @__PURE__ */ new Date()
          }).returning();
          const totalArea = await this.getUserTotalAreaFromTerritories(userId);
          return {
            territory: territory2,
            totalArea,
            newArea: actualNewArea,
            existingArea: existingAreaInBuffer,
            fortificationLayers
          };
        }
        await this.db.delete(territories).where(eq(territories.userId, userId));
        const [territory] = await this.db.insert(territories).values({
          userId,
          routeId,
          geometry: JSON.stringify(finalGeometry),
          area: finalArea,
          conqueredAt: /* @__PURE__ */ new Date()
        }).returning();
        return {
          territory,
          totalArea: finalArea,
          newArea: actualNewArea,
          existingArea: existingAreaInBuffer,
          fortificationLayers
        };
      }
      async updateTerritoryGeometry(userId, routeId, geometry, area2) {
        await this.db.delete(territories).where(eq(territories.userId, userId));
        const [territory] = await this.db.insert(territories).values({
          userId,
          routeId,
          geometry: JSON.stringify(geometry),
          area: area2,
          conqueredAt: /* @__PURE__ */ new Date()
        }).returning();
        return territory;
      }
      async createFriendship(insertFriendship) {
        const [friendship] = await this.db.insert(friendships).values(insertFriendship).returning();
        return friendship;
      }
      async getFriendsByUserId(userId) {
        const userFriendships = await this.db.select({
          friendId: friendships.friendId
        }).from(friendships).where(eq(friendships.userId, userId));
        if (userFriendships.length === 0) {
          return [];
        }
        const friendIds = userFriendships.map((f) => f.friendId);
        const friendsLeaderboard = await this.getLeaderboardFriends(userId);
        return friendsLeaderboard.filter((user) => friendIds.includes(user.id));
      }
      async checkFriendship(userId, friendId) {
        const [friendship] = await this.db.select().from(friendships).where(
          sql`${friendships.userId} = ${userId} AND ${friendships.friendId} = ${friendId}`
        );
        return !!friendship;
      }
      // ==================== FRIEND REQUESTS ====================
      async createFriendRequest(data) {
        const [existing] = await this.db.select().from(friendRequests).where(
          sql`${friendRequests.senderId} = ${data.senderId} AND ${friendRequests.recipientId} = ${data.recipientId} AND ${friendRequests.status} = 'pending'`
        );
        if (existing) {
          return existing;
        }
        const [request] = await this.db.insert(friendRequests).values(data).returning();
        return request;
      }
      async getFriendRequestsByRecipient(recipientId) {
        return await this.db.select().from(friendRequests).where(
          sql`${friendRequests.recipientId} = ${recipientId} AND ${friendRequests.status} = 'pending'`
        );
      }
      async getFriendRequestsBySender(senderId) {
        return await this.db.select().from(friendRequests).where(
          sql`${friendRequests.senderId} = ${senderId} AND ${friendRequests.status} = 'pending'`
        );
      }
      async getFriendRequestById(id) {
        const [request] = await this.db.select().from(friendRequests).where(eq(friendRequests.id, id));
        return request || void 0;
      }
      async updateFriendRequestStatus(id, status) {
        await this.db.update(friendRequests).set({ status }).where(eq(friendRequests.id, id));
      }
      async deleteFriendRequest(id) {
        await this.db.delete(friendRequests).where(eq(friendRequests.id, id));
      }
      // ==================== PUSH SUBSCRIPTIONS ====================
      async createPushSubscription(data) {
        try {
          await this.db.delete(pushSubscriptions).where(eq(pushSubscriptions.endpoint, data.endpoint));
        } catch (e) {
        }
        const [subscription] = await this.db.insert(pushSubscriptions).values(data).returning();
        return subscription;
      }
      async getPushSubscriptionsByUserId(userId) {
        return await this.db.select().from(pushSubscriptions).where(eq(pushSubscriptions.userId, userId));
      }
      async deletePushSubscriptionsByUserId(userId) {
        await this.db.delete(pushSubscriptions).where(eq(pushSubscriptions.userId, userId));
      }
      // ==================== STRAVA ====================
      async getStravaAccountByUserId(userId) {
        const [account] = await this.db.select().from(stravaAccounts).where(eq(stravaAccounts.userId, userId));
        return account || void 0;
      }
      async getStravaAccountByAthleteId(athleteId) {
        const [account] = await this.db.select().from(stravaAccounts).where(eq(stravaAccounts.stravaAthleteId, athleteId));
        return account || void 0;
      }
      async createStravaAccount(data) {
        const [account] = await this.db.insert(stravaAccounts).values(data).returning();
        return account;
      }
      async updateStravaAccount(userId, data) {
        const [account] = await this.db.update(stravaAccounts).set(data).where(eq(stravaAccounts.userId, userId)).returning();
        return account;
      }
      async deleteStravaAccount(userId) {
        await this.db.delete(stravaAccounts).where(eq(stravaAccounts.userId, userId));
      }
      async getStravaActivityByStravaId(stravaActivityId) {
        const [activity] = await this.db.select().from(stravaActivities).where(eq(stravaActivities.stravaActivityId, stravaActivityId));
        return activity || void 0;
      }
      async createStravaActivity(data) {
        const [activity] = await this.db.insert(stravaActivities).values(data).returning();
        return activity;
      }
      async updateStravaActivity(id, data) {
        const [activity] = await this.db.update(stravaActivities).set(data).where(eq(stravaActivities.id, id)).returning();
        return activity;
      }
      async getUnprocessedStravaActivities(userId) {
        return await this.db.select().from(stravaActivities).where(sql`${stravaActivities.userId} = ${userId} AND ${stravaActivities.processed} = 0`);
      }
      // Get activities that failed processing (route_id is NULL, regardless of processed flag)
      async getFailedStravaActivities(userId) {
        return await this.db.select().from(stravaActivities).where(sql`${stravaActivities.userId} = ${userId} AND ${stravaActivities.routeId} IS NULL`).orderBy(desc(stravaActivities.startDate));
      }
      // Mark activity for retry by resetting processed flag
      async resetStravaActivityForRetry(id) {
        const [activity] = await this.db.update(stravaActivities).set({
          processed: false,
          processedAt: null,
          routeId: null,
          territoryId: null
        }).where(eq(stravaActivities.id, id)).returning();
        return activity;
      }
      async getStravaActivitiesByUserId(userId) {
        return await this.db.select().from(stravaActivities).where(eq(stravaActivities.userId, userId)).orderBy(desc(stravaActivities.startDate));
      }
      async deleteStravaActivityByRouteId(routeId) {
        await this.db.delete(stravaActivities).where(eq(stravaActivities.routeId, routeId));
      }
      // ==================== POLAR ====================
      async getPolarAccountByUserId(userId) {
        const [account] = await this.db.select().from(polarAccounts).where(eq(polarAccounts.userId, userId));
        return account || void 0;
      }
      async getPolarAccountByPolarUserId(polarUserId) {
        const [account] = await this.db.select().from(polarAccounts).where(eq(polarAccounts.polarUserId, polarUserId));
        return account || void 0;
      }
      async createPolarAccount(data) {
        const [account] = await this.db.insert(polarAccounts).values(data).returning();
        return account;
      }
      async updatePolarAccount(userId, data) {
        const [account] = await this.db.update(polarAccounts).set(data).where(eq(polarAccounts.userId, userId)).returning();
        return account;
      }
      async deletePolarAccount(userId) {
        await this.db.delete(polarAccounts).where(eq(polarAccounts.userId, userId));
      }
      async getPolarActivityByPolarId(polarExerciseId) {
        const [activity] = await this.db.select().from(polarActivities).where(eq(polarActivities.polarExerciseId, polarExerciseId));
        return activity || void 0;
      }
      async createPolarActivity(data) {
        const [activity] = await this.db.insert(polarActivities).values(data).returning();
        return activity;
      }
      async updatePolarActivity(id, data) {
        const [activity] = await this.db.update(polarActivities).set(data).where(eq(polarActivities.id, id)).returning();
        return activity;
      }
      async deletePolarActivityByRouteId(routeId) {
        await this.db.delete(polarActivities).where(eq(polarActivities.routeId, routeId));
      }
      async getUnprocessedPolarActivities(userId) {
        return await this.db.select().from(polarActivities).where(sql`${polarActivities.userId} = ${userId} AND ${polarActivities.processed} = 0 AND ${polarActivities.skipReason} IS NULL`).orderBy(desc(polarActivities.startDate));
      }
      // Get activities that failed processing (route_id is NULL, regardless of processed flag)
      async getFailedPolarActivities(userId) {
        return await this.db.select().from(polarActivities).where(sql`${polarActivities.userId} = ${userId} AND ${polarActivities.routeId} IS NULL`).orderBy(desc(polarActivities.startDate));
      }
      // Get ALL failed activities across ALL users (route_id IS NULL)
      async getAllFailedPolarActivities() {
        return await this.db.select().from(polarActivities).where(sql`${polarActivities.routeId} IS NULL`).orderBy(desc(polarActivities.startDate));
      }
      // Mark activity for retry by resetting processed flag and clearing skip reason
      async resetPolarActivityForRetry(id) {
        const [activity] = await this.db.update(polarActivities).set({
          processed: false,
          processedAt: null,
          skipReason: null,
          routeId: null,
          territoryId: null
        }).where(eq(polarActivities.id, id)).returning();
        return activity;
      }
      async getPolarActivitiesByUserId(userId) {
        return await this.db.select().from(polarActivities).where(eq(polarActivities.userId, userId)).orderBy(desc(polarActivities.startDate));
      }
      async findPolarActivityByAttributes(userId, distance2, startDate) {
        const activities = await this.db.select().from(polarActivities).where(and(eq(polarActivities.userId, userId), eq(polarActivities.startDate, startDate)));
        for (const act of activities) {
          const distDiff = Math.abs(act.distance - distance2);
          const threshold = Math.max(distance2 * 0.02, 10);
          if (distDiff <= threshold) {
            return act;
          }
        }
        return void 0;
      }
      async getPolarActivityById(id) {
        const [activity] = await this.db.select().from(polarActivities).where(eq(polarActivities.id, id));
        return activity || void 0;
      }
      async deletePolarActivityById(id) {
        await this.db.delete(polarActivities).where(eq(polarActivities.id, id));
      }
      // Delete all polar activities that were saved as "skipped" (have a skipReason).
      // These block reimport because getPolarActivityByPolarId finds them and treats them as "already imported".
      // Optionally filter by userId.
      async deleteSkippedPolarActivities(userId) {
        const condition = userId ? sql`${polarActivities.skipReason} IS NOT NULL AND ${polarActivities.userId} = ${userId}` : sql`${polarActivities.skipReason} IS NOT NULL`;
        const result = await this.db.delete(polarActivities).where(condition).returning();
        return result.length;
      }
      async deleteConquestMetricsByRouteId(routeId) {
        await this.db.delete(conquestMetrics).where(eq(conquestMetrics.routeId, routeId));
      }
      async deleteAllConquestMetrics() {
        await this.db.delete(conquestMetrics);
      }
      async deleteConquestMetricsByUserId(userId) {
        await this.db.delete(conquestMetrics).where(
          sql`${conquestMetrics.attackerId} = ${userId} OR ${conquestMetrics.defenderId} = ${userId}`
        );
      }
      // Check if a user has any conquest interactions (as attacker or defender)
      async hasConquestInteractions(userId) {
        const result = await this.db.select({ count: sql`COUNT(*)` }).from(conquestMetrics).where(sql`${conquestMetrics.attackerId} = ${userId} OR ${conquestMetrics.defenderId} = ${userId}`);
        return (result[0]?.count || 0) > 0;
      }
      async deleteRouteById(id) {
        await this.db.delete(routes).where(eq(routes.id, id));
      }
      async updateRouteName(id, name) {
        await this.db.update(routes).set({ name }).where(eq(routes.id, id));
      }
      async getRouteById(id) {
        const result = await this.db.select().from(routes).where(eq(routes.id, id)).limit(1);
        return result[0] || null;
      }
      async recalculateUserArea(userId) {
        const result = await this.db.select({ total: sql`COALESCE(SUM(${territories.area}), 0)` }).from(territories).where(eq(territories.userId, userId));
        const totalArea = result[0]?.total || 0;
        await this.db.update(users).set({ totalArea }).where(eq(users.id, userId));
      }
      async findRouteByAttributes(userId, name, distance2) {
        const userRoutes = await this.db.select().from(routes).where(and(eq(routes.userId, userId), eq(routes.name, name)));
        for (const route of userRoutes) {
          const distDiff = Math.abs(route.distance - distance2);
          const threshold = Math.max(distance2 * 0.01, 1);
          if (distDiff <= threshold) {
            return route;
          }
        }
        return null;
      }
      async deleteTerritoriesByUserId(userId) {
        await this.db.delete(territories).where(eq(territories.userId, userId));
      }
      // Batch delete territories for multiple users (single query)
      async deleteTerritoriesForUsers(userIds) {
        if (userIds.length === 0) return;
        await this.db.delete(territories).where(inArray(territories.userId, userIds));
      }
      // Batch reset total area for multiple users (single query)
      async resetTotalAreaForUsers(userIds) {
        if (userIds.length === 0) return;
        await this.db.update(users).set({ totalArea: 0 }).where(inArray(users.id, userIds));
      }
      // Batch delete conquest metrics for multiple users (single query)
      async deleteConquestMetricsForUsers(userIds) {
        if (userIds.length === 0) return;
        await this.db.delete(conquestMetrics).where(
          sql`${conquestMetrics.attackerId} IN (${sql.join(userIds.map((id) => sql`${id}`), sql`, `)}) OR ${conquestMetrics.defenderId} IN (${sql.join(userIds.map((id) => sql`${id}`), sql`, `)})`
        );
      }
      async deleteTerritoriesByRouteId(routeId) {
        await this.db.delete(territories).where(eq(territories.routeId, routeId));
      }
      async deleteAllPolarActivitiesByUserId(userId) {
        await this.db.delete(polarActivities).where(eq(polarActivities.userId, userId));
      }
      async deleteAllRoutesByUserId(userId) {
        await this.db.delete(routes).where(eq(routes.userId, userId));
      }
      async deleteAllStravaActivitiesByUserId(userId) {
        await this.db.delete(stravaActivities).where(eq(stravaActivities.userId, userId));
      }
      // Reset all Polar activities for all users (force reimport)
      async resetAllPolarActivitiesForReprocessing() {
        const result = await this.db.update(polarActivities).set({
          processed: false,
          processedAt: null,
          routeId: null,
          territoryId: null
        }).returning();
        return result.length;
      }
      // Get all distinct user IDs that have Polar activities
      async getUsersWithPolarActivities() {
        const results = await this.db.selectDistinct({ userId: polarActivities.userId }).from(polarActivities);
        return results.map((r) => r.userId);
      }
      // Get all Polar activities (for admin purposes)
      async getAllPolarActivities() {
        return await this.db.select().from(polarActivities).orderBy(desc(polarActivities.startDate));
      }
      // Get count of territories by userId
      async getTerritoryCountByUserId(userId) {
        const [result] = await this.db.select({ count: sql`count(*)` }).from(territories).where(eq(territories.userId, userId));
        return Number(result?.count || 0);
      }
      // Find duplicate routes: same user, same startedAt (prefix match), same distance (~2% tolerance)
      async findDuplicateRoutes(userId) {
        const userRoutes = await this.db.select().from(routes).where(eq(routes.userId, userId)).orderBy(routes.startedAt);
        const groups = /* @__PURE__ */ new Map();
        for (const route of userRoutes) {
          const dateKey = (route.startedAt || "").substring(0, 16);
          if (!groups.has(dateKey)) groups.set(dateKey, []);
          groups.get(dateKey).push(route);
        }
        const duplicates = [];
        for (const [, group] of groups) {
          if (group.length <= 1) continue;
          const distGroups = [];
          for (const route of group) {
            let found = false;
            for (const dg of distGroups) {
              const ref = dg[0];
              const distDiff = Math.abs(route.distance - ref.distance);
              const threshold = Math.max(ref.distance * 0.02, 10);
              if (distDiff <= threshold) {
                dg.push(route);
                found = true;
                break;
              }
            }
            if (!found) distGroups.push([route]);
          }
          for (const dg of distGroups) {
            if (dg.length <= 1) continue;
            const withPolar = dg.filter((r) => {
              return false;
            });
            const keepId = dg[0].id;
            const deleteIds = dg.slice(1).map((r) => r.id);
            duplicates.push({ keepId, deleteIds });
          }
        }
        return duplicates;
      }
      // Delete a specific route by ID (no cascade - handled manually)
      async deleteRouteByIdDirect(routeId) {
        await this.db.delete(routes).where(eq(routes.id, routeId));
      }
      // Get routes that have no linked polar activity (orphaned from reimport)
      async getOrphanedRoutes(userId) {
        const userRoutes = await this.db.select().from(routes).where(eq(routes.userId, userId));
        const polarActs = await this.db.select({ routeId: polarActivities.routeId }).from(polarActivities).where(and(eq(polarActivities.userId, userId), sql`${polarActivities.routeId} IS NOT NULL`));
        const stravaActs = await this.db.select({ routeId: stravaActivities.routeId }).from(stravaActivities).where(and(eq(stravaActivities.userId, userId), sql`${stravaActivities.routeId} IS NOT NULL`));
        const linkedRouteIds = /* @__PURE__ */ new Set([
          ...polarActs.map((a) => a.routeId).filter(Boolean),
          ...stravaActs.map((a) => a.routeId).filter(Boolean)
        ]);
        return userRoutes.filter((r) => !linkedRouteIds.has(r.id));
      }
      async findRouteByDateAndDistance(userId, startDate, distance2) {
        const datePrefix = startDate.substring(0, 16);
        const userRoutes = await this.db.select().from(routes).where(
          and(
            eq(routes.userId, userId),
            sql`${routes.startedAt} LIKE ${datePrefix + "%"}`
          )
        );
        for (const route of userRoutes) {
          const distDiff = Math.abs(route.distance - distance2);
          const threshold = Math.max(distance2 * 0.01, 1);
          if (distDiff <= threshold) {
            return route;
          }
        }
        return null;
      }
      async getPolarActivityStats(userId) {
        const [aggregate] = await this.db.select({
          total: sql`count(*)`,
          unprocessed: sql`sum(case when ${polarActivities.processed} = false then 1 else 0 end)`,
          lastStartDate: sql`max(${polarActivities.startDate})`
        }).from(polarActivities).where(eq(polarActivities.userId, userId));
        return {
          total: Number(aggregate?.total || 0),
          unprocessed: Number(aggregate?.unprocessed || 0),
          lastStartDate: aggregate?.lastStartDate || null
        };
      }
      // ==================== COROS ====================
      async getCorosAccountByUserId(userId) {
        const [account] = await this.db.select().from(corosAccounts).where(eq(corosAccounts.userId, userId));
        return account || void 0;
      }
      async getCorosAccountByOpenId(openId) {
        const [account] = await this.db.select().from(corosAccounts).where(eq(corosAccounts.corosOpenId, openId));
        return account || void 0;
      }
      async createCorosAccount(data) {
        const [account] = await this.db.insert(corosAccounts).values(data).returning();
        return account;
      }
      async updateCorosAccount(userId, data) {
        const [account] = await this.db.update(corosAccounts).set(data).where(eq(corosAccounts.userId, userId)).returning();
        return account;
      }
      async deleteCorosAccount(userId) {
        await this.db.delete(corosAccounts).where(eq(corosAccounts.userId, userId));
      }
      async getCorosActivityByWorkoutId(workoutId) {
        const [activity] = await this.db.select().from(corosActivities).where(eq(corosActivities.corosWorkoutId, workoutId));
        return activity || void 0;
      }
      async createCorosActivity(data) {
        const [activity] = await this.db.insert(corosActivities).values(data).returning();
        return activity;
      }
      async updateCorosActivity(id, data) {
        const [activity] = await this.db.update(corosActivities).set(data).where(eq(corosActivities.id, id)).returning();
        return activity;
      }
      async getUnprocessedCorosActivities(userId) {
        return await this.db.select().from(corosActivities).where(sql`${corosActivities.userId} = ${userId} AND ${corosActivities.processed} = 0 AND ${corosActivities.skipReason} IS NULL`).orderBy(desc(corosActivities.startDate));
      }
      async getCorosActivitiesByUserId(userId) {
        return await this.db.select().from(corosActivities).where(eq(corosActivities.userId, userId)).orderBy(desc(corosActivities.startDate));
      }
      async getCorosActivityById(id) {
        const [activity] = await this.db.select().from(corosActivities).where(eq(corosActivities.id, id));
        return activity || void 0;
      }
      async deleteCorosActivity(id) {
        await this.db.delete(corosActivities).where(eq(corosActivities.id, id));
      }
      async markCorosActivityProcessed(id, routeId, territoryId, skipReason) {
        const [activity] = await this.db.update(corosActivities).set({
          processed: true,
          processedAt: (/* @__PURE__ */ new Date()).toISOString(),
          routeId,
          territoryId,
          skipReason
        }).where(eq(corosActivities.id, id)).returning();
        return activity;
      }
      async getCorosActivityStats(userId) {
        const [aggregate] = await this.db.select({
          total: sql`count(*)`,
          unprocessed: sql`sum(case when ${corosActivities.processed} = false then 1 else 0 end)`,
          lastStartDate: sql`max(${corosActivities.startDate})`
        }).from(corosActivities).where(eq(corosActivities.userId, userId));
        return {
          total: Number(aggregate?.total || 0),
          unprocessed: Number(aggregate?.unprocessed || 0),
          lastStartDate: aggregate?.lastStartDate || null
        };
      }
      // New friendship methods for friend-only competition
      async getLeaderboardFriends(userId) {
        const userFriendships = await this.db.select({ friendId: friendships.friendId }).from(friendships).where(eq(friendships.userId, userId));
        const friendIds = userFriendships.map((f) => f.friendId);
        const allIds = [userId, ...friendIds];
        const friends = await this.db.select().from(users).where(sql`${users.id} IN (${sql.join(allIds.map((id) => sql`${id}`), sql`, `)})`).orderBy(desc(users.totalArea));
        const friendsWithStats = [];
        for (let i = 0; i < friends.length; i++) {
          const friend = friends[i];
          const [{ count }] = await this.db.select({ count: sql`count(*)` }).from(friendships).where(eq(friendships.userId, friend.id));
          friendsWithStats.push({
            ...friend,
            rank: i + 1,
            friendCount: Number(count) || 0
          });
        }
        return friendsWithStats;
      }
      async getTerritoriesWithUsersByFriends(userId) {
        const userFriendships = await this.db.select({ friendId: friendships.friendId }).from(friendships).where(eq(friendships.userId, userId));
        const friendIds = userFriendships.map((f) => f.friendId);
        const allIds = [userId, ...friendIds];
        const territoryData = await this.db.select({
          id: territories.id,
          userId: territories.userId,
          routeId: territories.routeId,
          geometry: territories.geometry,
          area: territories.area,
          conqueredAt: territories.conqueredAt,
          userName: users.name,
          userUsername: users.username,
          userColor: users.color,
          ranTogetherWith: routes.ranTogetherWith
        }).from(territories).leftJoin(users, eq(territories.userId, users.id)).leftJoin(routes, eq(territories.routeId, routes.id)).where(sql`${territories.userId} IN (${sql.join(allIds.map((id) => sql`${id}`), sql`, `)})`);
        return this._enrichTerritoriesWithCoRunnerColors(territoryData);
      }
      async searchUsers(query, currentUserId, limit = 20) {
        const lowerQuery = query.toLowerCase();
        return await this.db.select().from(users).where(
          sql`(LOWER(${users.username}) LIKE ${`%${lowerQuery}%`} OR LOWER(${users.name}) LIKE ${`%${lowerQuery}%`}) AND ${users.id} != ${currentUserId}`
        ).limit(limit);
      }
      async createBidirectionalFriendship(userId, friendId) {
        const existing = await this.db.select().from(friendships).where(
          sql`(${friendships.userId} = ${userId} AND ${friendships.friendId} = ${friendId}) OR (${friendships.userId} = ${friendId} AND ${friendships.friendId} = ${userId})`
        );
        if (existing.length > 0) {
          return;
        }
        await this.db.insert(friendships).values([
          { userId, friendId },
          { userId: friendId, friendId: userId }
        ]);
      }
      async deleteBidirectionalFriendship(userId, friendId) {
        await this.db.delete(friendships).where(
          sql`(${friendships.userId} = ${userId} AND ${friendships.friendId} = ${friendId}) OR (${friendships.userId} = ${friendId} AND ${friendships.friendId} = ${userId})`
        );
      }
      async createFriendInvite(userId) {
        const token = crypto.randomUUID();
        const expiresAt = new Date(Date.now() + 7 * 24 * 60 * 60 * 1e3);
        const [invite] = await this.db.insert(friendInvites).values({ userId, token, expiresAt }).returning();
        return invite;
      }
      async getFriendInviteByToken(token) {
        const [invite] = await this.db.select().from(friendInvites).where(eq(friendInvites.token, token));
        return invite || void 0;
      }
      async deleteFriendInvite(id) {
        await this.db.delete(friendInvites).where(eq(friendInvites.id, id));
      }
      async getFriendIds(userId) {
        const userFriendships = await this.db.select({ friendId: friendships.friendId }).from(friendships).where(eq(friendships.userId, userId));
        return userFriendships.map((f) => f.friendId);
      }
      // Get all friendship pairs for a set of users (single query)
      async getFriendshipMap(userIds) {
        if (userIds.length === 0) return /* @__PURE__ */ new Map();
        const result = await this.db.select({ userId: friendships.userId, friendId: friendships.friendId }).from(friendships).where(inArray(friendships.userId, userIds));
        const map2 = /* @__PURE__ */ new Map();
        for (const uid of userIds) {
          map2.set(uid, /* @__PURE__ */ new Set());
        }
        for (const row of result) {
          const set2 = map2.get(row.userId);
          if (set2) set2.add(row.friendId);
        }
        return map2;
      }
      async recordConquestMetric(attackerId, defenderId, areaStolen, routeId) {
        const [metric] = await this.db.insert(conquestMetrics).values({
          attackerId,
          defenderId,
          areaStolen,
          routeId
        }).returning();
        return metric;
      }
      async getConquestMetricsBetweenUsers(userId1, userId2) {
        const fromUser1ToUser2 = await this.db.select({ total: sql`SUM(${conquestMetrics.areaStolen})` }).from(conquestMetrics).where(
          sql`${conquestMetrics.attackerId} = ${userId1} AND ${conquestMetrics.defenderId} = ${userId2}`
        );
        const fromUser2ToUser1 = await this.db.select({ total: sql`SUM(${conquestMetrics.areaStolen})` }).from(conquestMetrics).where(
          sql`${conquestMetrics.attackerId} = ${userId2} AND ${conquestMetrics.defenderId} = ${userId1}`
        );
        return {
          totalFromFirstToSecond: fromUser1ToUser2[0]?.total || 0,
          totalFromSecondToFirst: fromUser2ToUser1[0]?.total || 0
        };
      }
      async getUserConquestStats(userId) {
        const stolenResult = await this.db.select({ total: sql`COALESCE(SUM(${conquestMetrics.areaStolen}), 0)` }).from(conquestMetrics).where(eq(conquestMetrics.attackerId, userId));
        const lostResult = await this.db.select({ total: sql`COALESCE(SUM(${conquestMetrics.areaStolen}), 0)` }).from(conquestMetrics).where(eq(conquestMetrics.defenderId, userId));
        const stolenByUserResult = await this.db.select({
          defenderId: conquestMetrics.defenderId,
          total: sql`SUM(${conquestMetrics.areaStolen})`
        }).from(conquestMetrics).where(eq(conquestMetrics.attackerId, userId)).groupBy(conquestMetrics.defenderId);
        const lostToUserResult = await this.db.select({
          attackerId: conquestMetrics.attackerId,
          total: sql`SUM(${conquestMetrics.areaStolen})`
        }).from(conquestMetrics).where(eq(conquestMetrics.defenderId, userId)).groupBy(conquestMetrics.attackerId);
        const stolenByUser = await Promise.all(
          stolenByUserResult.map(async (row) => {
            const [user] = await this.db.select({ id: users.id, name: users.name, color: users.color }).from(users).where(eq(users.id, row.defenderId));
            return {
              userId: row.defenderId,
              userName: user?.name || "Usuario desconocido",
              userColor: user?.color || "#888888",
              amount: row.total || 0
            };
          })
        );
        const lostToUser = await Promise.all(
          lostToUserResult.map(async (row) => {
            const [user] = await this.db.select({ id: users.id, name: users.name, color: users.color }).from(users).where(eq(users.id, row.attackerId));
            return {
              userId: row.attackerId,
              userName: user?.name || "Usuario desconocido",
              userColor: user?.color || "#888888",
              amount: row.total || 0
            };
          })
        );
        return {
          totalStolen: stolenResult[0]?.total || 0,
          totalLost: lostResult[0]?.total || 0,
          stolenByUser: stolenByUser.sort((a, b) => b.amount - a.amount),
          lostToUser: lostToUser.sort((a, b) => b.amount - a.amount)
        };
      }
      // Email Notifications
      async recordEmailNotification(data) {
        const [notification] = await this.db.insert(emailNotifications).values(data).returning();
        return notification;
      }
      async getEmailPreferences(userId) {
        const [prefs] = await this.db.select().from(emailPreferences).where(eq(emailPreferences.userId, userId));
        return prefs || void 0;
      }
      async createEmailPreferences(userId) {
        const [prefs] = await this.db.insert(emailPreferences).values({ userId }).returning();
        return prefs;
      }
      async updateEmailPreferences(userId, data) {
        const [updated] = await this.db.update(emailPreferences).set(data).where(eq(emailPreferences.userId, userId)).returning();
        return updated;
      }
      // --- Ephemeral Photos ---
      async ensureEphemeralPhotosTable() {
        try {
          await this.db.run(sql`CREATE TABLE IF NOT EXISTS ephemeral_photos (
        id TEXT PRIMARY KEY DEFAULT (lower(hex(randomblob(16)))),
        sender_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        recipient_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        photo_data TEXT NOT NULL,
        message TEXT,
        area_stolen REAL,
        viewed INTEGER NOT NULL DEFAULT 0,
        created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
        expires_at TEXT NOT NULL
      )`);
        } catch (_) {
        }
      }
      async createEphemeralPhoto(data) {
        await this.ensureEphemeralPhotosTable();
        const [photo] = await this.db.insert(ephemeralPhotos).values(data).returning();
        return photo;
      }
      async getPendingPhotosForUser(userId) {
        await this.ensureEphemeralPhotosTable();
        const results = await this.db.select({
          id: ephemeralPhotos.id,
          senderId: ephemeralPhotos.senderId,
          recipientId: ephemeralPhotos.recipientId,
          photoData: ephemeralPhotos.photoData,
          message: ephemeralPhotos.message,
          areaStolen: ephemeralPhotos.areaStolen,
          viewed: ephemeralPhotos.viewed,
          createdAt: ephemeralPhotos.createdAt,
          expiresAt: ephemeralPhotos.expiresAt,
          senderName: users.name,
          senderUsername: users.username,
          senderAvatar: users.avatar
        }).from(ephemeralPhotos).innerJoin(users, eq(ephemeralPhotos.senderId, users.id)).where(
          and(
            eq(ephemeralPhotos.recipientId, userId),
            eq(ephemeralPhotos.viewed, false)
          )
        );
        return results;
      }
      async viewAndDeleteEphemeralPhoto(photoId, userId) {
        await this.ensureEphemeralPhotosTable();
        const [photo] = await this.db.select().from(ephemeralPhotos).where(
          and(
            eq(ephemeralPhotos.id, photoId),
            eq(ephemeralPhotos.recipientId, userId)
          )
        );
        if (!photo) return null;
        await this.db.delete(ephemeralPhotos).where(eq(ephemeralPhotos.id, photoId));
        return photo;
      }
      async cleanupExpiredPhotos() {
        await this.ensureEphemeralPhotosTable();
        const now = (/* @__PURE__ */ new Date()).toISOString();
        const result = await this.db.delete(ephemeralPhotos).where(sql`${ephemeralPhotos.expiresAt} < ${now}`);
        return result.rowsAffected || 0;
      }
      // --- Social Feed ---
      async ensureFeedTables() {
        if (this._feedTablesEnsured) return;
        try {
          await this.db.run(sql`CREATE TABLE IF NOT EXISTS feed_events (
        id TEXT PRIMARY KEY DEFAULT (lower(hex(randomblob(16)))),
        user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        event_type TEXT NOT NULL,
        route_id TEXT REFERENCES routes(id) ON DELETE SET NULL,
        victim_id TEXT REFERENCES users(id) ON DELETE SET NULL,
        area_stolen REAL,
        distance REAL,
        duration INTEGER,
        new_area REAL,
        ran_together_with TEXT,
        record_type TEXT,
        record_value REAL,
        metadata TEXT,
        created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`);
          try {
            await this.db.run(sql`ALTER TABLE feed_events ADD COLUMN metadata TEXT`);
          } catch (_) {
          }
          try {
            await this.db.run(sql`ALTER TABLE polar_activities ADD COLUMN skip_reason TEXT`);
          } catch (_) {
          }
          await this.db.run(sql`CREATE TABLE IF NOT EXISTS feed_comments (
        id TEXT PRIMARY KEY DEFAULT (lower(hex(randomblob(16)))),
        feed_event_id TEXT NOT NULL REFERENCES feed_events(id) ON DELETE CASCADE,
        user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        parent_id TEXT,
        content TEXT NOT NULL,
        created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`);
          await this.db.run(sql`CREATE TABLE IF NOT EXISTS feed_reactions (
        id TEXT PRIMARY KEY DEFAULT (lower(hex(randomblob(16)))),
        user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        target_type TEXT NOT NULL,
        target_id TEXT NOT NULL,
        reaction_type TEXT NOT NULL,
        created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`);
          await this.db.run(sql`CREATE UNIQUE INDEX IF NOT EXISTS idx_feed_reactions_unique ON feed_reactions(user_id, target_type, target_id)`);
          this._feedTablesEnsured = true;
        } catch (e) {
          console.error("[FEED] ensureFeedTables error:", e);
        }
      }
      async createFeedEvent(data) {
        await this.ensureFeedTables();
        const [event] = await this.db.insert(feedEvents).values(data).returning();
        return event;
      }
      async updateFeedEvent(eventId, data) {
        await this.ensureFeedTables();
        const updateData = {};
        if (data.distance !== void 0) updateData.distance = data.distance;
        if (data.duration !== void 0) updateData.duration = data.duration;
        if (data.newArea !== void 0) updateData.newArea = data.newArea;
        if (data.metadata !== void 0) updateData.metadata = data.metadata;
        if (Object.keys(updateData).length === 0) return;
        await this.db.update(feedEvents).set(updateData).where(eq(feedEvents.id, eventId));
      }
      async getMaxConquestArea(userId) {
        await this.ensureFeedTables();
        const result = await this.db.select({ max: sql`COALESCE(MAX(${feedEvents.newArea}), 0)` }).from(feedEvents).where(and(eq(feedEvents.userId, userId), eq(feedEvents.eventType, "activity")));
        return result[0]?.max || 0;
      }
      async deleteFeedEventsByRouteId(routeId) {
        await this.ensureFeedTables();
        await this.db.run(sql`DELETE FROM feed_comments WHERE feed_event_id IN (SELECT id FROM feed_events WHERE route_id = ${routeId})`);
        await this.db.run(sql`DELETE FROM feed_events WHERE route_id = ${routeId}`);
      }
      async getFeedEventByRouteId(routeId) {
        await this.ensureFeedTables();
        const [event] = await this.db.select().from(feedEvents).where(eq(feedEvents.routeId, routeId));
        return event || void 0;
      }
      async getFeedForUser(userId, limit = 30, offset = 0) {
        await this.ensureFeedTables();
        let isCompetitionMode = false;
        try {
          const comp = await this.getActiveCompetition();
          if (comp) {
            const now = Date.now();
            const start = new Date(comp.startsAt).getTime();
            const end = new Date(comp.endsAt).getTime();
            if (comp.status === "active" || now >= start && now <= end) {
              isCompetitionMode = true;
            }
          }
        } catch (_) {
        }
        let events;
        if (isCompetitionMode) {
          events = await this.db.select().from(feedEvents).orderBy(desc(feedEvents.createdAt)).limit(limit).offset(offset);
        } else {
          const friendIds = await this.getFriendIds(userId);
          const allUserIds = [userId, ...friendIds];
          events = await this.db.select().from(feedEvents).where(inArray(feedEvents.userId, allUserIds)).orderBy(desc(feedEvents.createdAt)).limit(limit).offset(offset);
        }
        if (events.length === 0) return [];
        const userIdsNeeded = /* @__PURE__ */ new Set();
        const routeIdsNeeded = /* @__PURE__ */ new Set();
        const eventIds = [];
        for (const event of events) {
          userIdsNeeded.add(event.userId);
          if (event.victimId) userIdsNeeded.add(event.victimId);
          if (event.routeId) routeIdsNeeded.add(event.routeId);
          eventIds.push(event.id);
        }
        const usersList = userIdsNeeded.size > 0 ? await this.db.select().from(users).where(inArray(users.id, [...userIdsNeeded])) : [];
        const userMap = new Map(usersList.map((u) => [u.id, u]));
        const routesList = routeIdsNeeded.size > 0 ? await this.db.select({
          id: routes.id,
          name: routes.name,
          startedAt: routes.startedAt,
          coordinates: routes.coordinates
        }).from(routes).where(inArray(routes.id, [...routeIdsNeeded])) : [];
        const routeMap = new Map(routesList.map((r) => [r.id, r]));
        const commentCounts = eventIds.length > 0 ? await this.db.select({ feedEventId: feedComments.feedEventId, count: sql`COUNT(*)` }).from(feedComments).where(inArray(feedComments.feedEventId, eventIds)).groupBy(feedComments.feedEventId) : [];
        const commentCountMap = new Map(commentCounts.map((c) => [c.feedEventId, c.count]));
        const reactionCounts = eventIds.length > 0 ? await this.db.select({
          targetId: feedReactions.targetId,
          reactionType: feedReactions.reactionType,
          count: sql`COUNT(*)`
        }).from(feedReactions).where(and(
          eq(feedReactions.targetType, "event"),
          inArray(feedReactions.targetId, eventIds)
        )).groupBy(feedReactions.targetId, feedReactions.reactionType) : [];
        const likeCountMap = /* @__PURE__ */ new Map();
        const dislikeCountMap = /* @__PURE__ */ new Map();
        for (const r of reactionCounts) {
          if (r.reactionType === "like") likeCountMap.set(r.targetId, r.count);
          else if (r.reactionType === "dislike") dislikeCountMap.set(r.targetId, r.count);
        }
        const userReactions = eventIds.length > 0 ? await this.db.select({ targetId: feedReactions.targetId, reactionType: feedReactions.reactionType }).from(feedReactions).where(and(
          eq(feedReactions.userId, userId),
          eq(feedReactions.targetType, "event"),
          inArray(feedReactions.targetId, eventIds)
        )) : [];
        const userReactionMap = new Map(userReactions.map((r) => [r.targetId, r.reactionType]));
        let nicknameMap = /* @__PURE__ */ new Map();
        try {
          nicknameMap = await this.getActiveNicknamesForUsers([...userIdsNeeded]);
        } catch (_) {
        }
        const result = [];
        for (const event of events) {
          const eventUser = userMap.get(event.userId);
          if (!eventUser) continue;
          let victim = null;
          if (event.victimId) {
            const v = userMap.get(event.victimId);
            if (v) {
              const vnn = nicknameMap.get(v.id);
              victim = { id: v.id, username: v.username, name: v.name, color: v.color, avatar: v.avatar, nickname: vnn?.nickname || null, nicknameExpiresAt: vnn?.expiresAt || null };
            }
          }
          let routeName = null;
          let activityDate = null;
          let routeCoordinates = null;
          if (event.routeId) {
            const route = routeMap.get(event.routeId);
            if (route) {
              routeName = route.name;
              activityDate = route.startedAt;
              try {
                const raw2 = typeof route.coordinates === "string" ? JSON.parse(route.coordinates) : route.coordinates || [];
                if (raw2.length > 0) {
                  if (raw2.length <= 80) {
                    routeCoordinates = raw2;
                  } else {
                    const step = raw2.length / 80;
                    const sampled = [];
                    for (let i = 0; i < 80; i++) {
                      sampled.push(raw2[Math.min(Math.floor(i * step), raw2.length - 1)]);
                    }
                    sampled.push(raw2[raw2.length - 1]);
                    routeCoordinates = sampled;
                  }
                }
              } catch {
              }
            }
          }
          const commentCount = commentCountMap.get(event.id) || 0;
          const likeCount = likeCountMap.get(event.id) || 0;
          const dislikeCount = dislikeCountMap.get(event.id) || 0;
          const userReaction = userReactionMap.get(event.id) || null;
          const userNn = nicknameMap.get(eventUser.id);
          result.push({
            ...event,
            user: { id: eventUser.id, username: eventUser.username, name: eventUser.name, color: eventUser.color, avatar: eventUser.avatar, nickname: userNn?.nickname || null, nicknameExpiresAt: userNn?.expiresAt || null },
            victim,
            routeName,
            activityDate,
            routeCoordinates,
            commentCount,
            likeCount,
            dislikeCount,
            userReaction
          });
        }
        return result;
      }
      async getFeedEventComments(eventId, viewerUserId) {
        await this.ensureFeedTables();
        const rawComments = await this.db.select({
          id: feedComments.id,
          feedEventId: feedComments.feedEventId,
          userId: feedComments.userId,
          parentId: feedComments.parentId,
          content: feedComments.content,
          createdAt: feedComments.createdAt,
          userName: users.name,
          userUsername: users.username,
          userAvatar: users.avatar,
          userColor: users.color
        }).from(feedComments).innerJoin(users, eq(feedComments.userId, users.id)).where(eq(feedComments.feedEventId, eventId)).orderBy(feedComments.createdAt);
        if (rawComments.length === 0) return [];
        const commentIds = rawComments.map((c) => c.id);
        const reactionCounts = commentIds.length > 0 ? await this.db.select({
          targetId: feedReactions.targetId,
          reactionType: feedReactions.reactionType,
          count: sql`COUNT(*)`
        }).from(feedReactions).where(and(
          eq(feedReactions.targetType, "comment"),
          inArray(feedReactions.targetId, commentIds)
        )).groupBy(feedReactions.targetId, feedReactions.reactionType) : [];
        const likeCounts = /* @__PURE__ */ new Map();
        const dislikeCounts = /* @__PURE__ */ new Map();
        for (const r of reactionCounts) {
          if (r.reactionType === "like") likeCounts.set(r.targetId, r.count);
          else dislikeCounts.set(r.targetId, r.count);
        }
        const viewerReactions = viewerUserId && commentIds.length > 0 ? await this.db.select({ targetId: feedReactions.targetId, reactionType: feedReactions.reactionType }).from(feedReactions).where(and(
          eq(feedReactions.userId, viewerUserId),
          eq(feedReactions.targetType, "comment"),
          inArray(feedReactions.targetId, commentIds)
        )) : [];
        const viewerReactionMap = new Map(viewerReactions.map((r) => [r.targetId, r.reactionType]));
        const allComments = rawComments.map((row) => ({
          id: row.id,
          feedEventId: row.feedEventId,
          userId: row.userId,
          parentId: row.parentId,
          content: row.content,
          createdAt: row.createdAt,
          user: {
            id: row.userId,
            username: row.userUsername,
            name: row.userName,
            avatar: row.userAvatar,
            color: row.userColor
          },
          replies: [],
          likeCount: likeCounts.get(row.id) || 0,
          dislikeCount: dislikeCounts.get(row.id) || 0,
          userReaction: viewerReactionMap.get(row.id) || null
        }));
        const topLevel = allComments.filter((c) => !c.parentId);
        const replies = allComments.filter((c) => c.parentId);
        for (const reply of replies) {
          const parent = topLevel.find((c) => c.id === reply.parentId);
          if (parent) {
            parent.replies = parent.replies || [];
            parent.replies.push(reply);
          }
        }
        return topLevel;
      }
      /** Get top preview comments for a feed event — prioritizes comments with replies and most likes */
      async getPreviewComments(eventId, viewerUserId, limit = 3) {
        const allComments = await this.getFeedEventComments(eventId, viewerUserId);
        if (allComments.length === 0) return [];
        const sorted = [...allComments].sort((a, b) => {
          const aReplies = (a.replies?.length || 0) > 0 ? 1 : 0;
          const bReplies = (b.replies?.length || 0) > 0 ? 1 : 0;
          if (bReplies !== aReplies) return bReplies - aReplies;
          if (b.likeCount !== a.likeCount) return b.likeCount - a.likeCount;
          return new Date(a.createdAt).getTime() - new Date(b.createdAt).getTime();
        });
        const preview = sorted.slice(0, limit).map((c) => ({
          ...c,
          replies: c.replies && c.replies.length > 0 ? [c.replies[0]] : []
        }));
        return preview;
      }
      /** Toggle a reaction (like/dislike). Returns updated counts. */
      async toggleReaction(userId, targetType, targetId, reactionType) {
        await this.ensureFeedTables();
        const [existing] = await this.db.select().from(feedReactions).where(and(
          eq(feedReactions.userId, userId),
          eq(feedReactions.targetType, targetType),
          eq(feedReactions.targetId, targetId)
        ));
        if (existing) {
          if (existing.reactionType === reactionType) {
            await this.db.delete(feedReactions).where(eq(feedReactions.id, existing.id));
          } else {
            await this.db.update(feedReactions).set({ reactionType }).where(eq(feedReactions.id, existing.id));
          }
        } else {
          await this.db.insert(feedReactions).values({
            userId,
            targetType,
            targetId,
            reactionType
          });
        }
        const counts = await this.db.select({
          reactionType: feedReactions.reactionType,
          count: sql`COUNT(*)`
        }).from(feedReactions).where(and(
          eq(feedReactions.targetType, targetType),
          eq(feedReactions.targetId, targetId)
        )).groupBy(feedReactions.reactionType);
        let likeCount = 0;
        let dislikeCount = 0;
        for (const c of counts) {
          if (c.reactionType === "like") likeCount = c.count;
          else if (c.reactionType === "dislike") dislikeCount = c.count;
        }
        const [current] = await this.db.select({ reactionType: feedReactions.reactionType }).from(feedReactions).where(and(
          eq(feedReactions.userId, userId),
          eq(feedReactions.targetType, targetType),
          eq(feedReactions.targetId, targetId)
        ));
        return { likeCount, dislikeCount, userReaction: current?.reactionType || null };
      }
      async addFeedComment(data) {
        await this.ensureFeedTables();
        const [comment] = await this.db.insert(feedComments).values(data).returning();
        return comment;
      }
      async deleteFeedComment(commentId, userId) {
        await this.ensureFeedTables();
        const result = await this.db.delete(feedComments).where(and(eq(feedComments.id, commentId), eq(feedComments.userId, userId)));
        return (result.rowsAffected || 0) > 0;
      }
      async getFeedEvent(eventId) {
        await this.ensureFeedTables();
        const [event] = await this.db.select().from(feedEvents).where(eq(feedEvents.id, eventId));
        return event || void 0;
      }
      async getFeedCommentById(commentId) {
        await this.ensureFeedTables();
        const [comment] = await this.db.select().from(feedComments).where(eq(feedComments.id, commentId));
        return comment || void 0;
      }
      // ===== Inactivity Reminders =====
      /**
       * Ensure the inactivity_reminders table exists (auto-create if missing)
       */
      async ensureInactivityRemindersTable() {
        try {
          await this.db.run(sql`
        CREATE TABLE IF NOT EXISTS inactivity_reminders (
          id TEXT PRIMARY KEY DEFAULT (lower(hex(randomblob(16)))),
          user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
          message_index INTEGER NOT NULL,
          sent_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
        )
      `);
        } catch (e) {
        }
      }
      /**
       * Get the date of the user's last activity (most recent route completedAt).
       * Returns null if user has never uploaded an activity.
       */
      async getLastActivityDate(userId) {
        const result = await this.db.select({ completedAt: routes.completedAt }).from(routes).where(eq(routes.userId, userId)).orderBy(desc(routes.completedAt)).limit(1);
        return result.length > 0 ? result[0].completedAt : null;
      }
      /**
       * Get the last inactivity reminder sent to a user.
       */
      async getLastInactivityReminder(userId) {
        await this.ensureInactivityRemindersTable();
        const result = await this.db.select().from(inactivityReminders).where(eq(inactivityReminders.userId, userId)).orderBy(desc(inactivityReminders.sentAt)).limit(1);
        return result.length > 0 ? result[0] : null;
      }
      /**
       * Save a record of sending an inactivity reminder.
       */
      async saveInactivityReminder(userId, messageIndex) {
        await this.ensureInactivityRemindersTable();
        await this.db.insert(inactivityReminders).values({
          userId,
          messageIndex
        });
      }
      // ===== Territory Loss Notifications =====
      /**
       * Ensure the territory_loss_notifications table exists (auto-create if missing)
       */
      async ensureTerritoryLossNotificationsTable() {
        try {
          await this.db.run(sql`
        CREATE TABLE IF NOT EXISTS territory_loss_notifications (
          id TEXT PRIMARY KEY DEFAULT (lower(hex(randomblob(16)))),
          user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
          message_index INTEGER NOT NULL,
          sent_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
        )
      `);
        } catch (e) {
        }
      }
      /**
       * Get the last territory loss notification sent to a user.
       */
      async getLastTerritoryLossNotification(userId) {
        await this.ensureTerritoryLossNotificationsTable();
        const result = await this.db.select().from(territoryLossNotifications).where(eq(territoryLossNotifications.userId, userId)).orderBy(desc(territoryLossNotifications.sentAt)).limit(1);
        return result.length > 0 ? result[0] : null;
      }
      /**
       * Save a record of sending a territory loss notification.
       */
      async saveTerritoryLossNotification(userId, messageIndex) {
        await this.ensureTerritoryLossNotificationsTable();
        await this.db.insert(territoryLossNotifications).values({
          userId,
          messageIndex
        });
      }
      // ============ COMPETITION SYSTEM ============
      async ensureCompetitionTables() {
        if (this._competitionTablesEnsured) return;
        try {
          await this.db.run(sql`CREATE TABLE IF NOT EXISTS competitions (
        id TEXT PRIMARY KEY DEFAULT (lower(hex(randomblob(16)))),
        name TEXT NOT NULL,
        slug TEXT NOT NULL UNIQUE,
        starts_at TEXT NOT NULL,
        ends_at TEXT NOT NULL,
        status TEXT NOT NULL DEFAULT 'upcoming',
        config TEXT,
        created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`);
          await this.db.run(sql`CREATE TABLE IF NOT EXISTS treasures (
        id TEXT PRIMARY KEY DEFAULT (lower(hex(randomblob(16)))),
        competition_id TEXT NOT NULL,
        name TEXT NOT NULL,
        power_type TEXT NOT NULL,
        rarity TEXT NOT NULL,
        lat REAL NOT NULL,
        lng REAL NOT NULL,
        collected_by TEXT REFERENCES users(id) ON DELETE SET NULL,
        collected_at TEXT,
        spawned_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
        expires_at TEXT NOT NULL,
        active INTEGER NOT NULL DEFAULT 1
      )`);
          await this.db.run(sql`CREATE TABLE IF NOT EXISTS user_powers (
        id TEXT PRIMARY KEY DEFAULT (lower(hex(randomblob(16)))),
        user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        competition_id TEXT NOT NULL,
        power_type TEXT NOT NULL,
        treasure_id TEXT NOT NULL,
        status TEXT NOT NULL DEFAULT 'available',
        activated_at TEXT,
        used_at TEXT,
        expires_at TEXT,
        metadata TEXT,
        created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`);
          await this.db.run(sql`CREATE TABLE IF NOT EXISTS competition_stats (
        id TEXT PRIMARY KEY DEFAULT (lower(hex(randomblob(16)))),
        competition_id TEXT NOT NULL,
        user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        total_area REAL NOT NULL DEFAULT 0,
        total_distance REAL NOT NULL DEFAULT 0,
        total_duration INTEGER NOT NULL DEFAULT 0,
        activities_count INTEGER NOT NULL DEFAULT 0,
        treasures_collected INTEGER NOT NULL DEFAULT 0,
        area_stolen REAL NOT NULL DEFAULT 0,
        unique_victims INTEGER NOT NULL DEFAULT 0,
        ran_together_count INTEGER NOT NULL DEFAULT 0,
        updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`);
          await this.db.run(sql`CREATE UNIQUE INDEX IF NOT EXISTS idx_competition_stats_unique ON competition_stats(competition_id, user_id)`);
          await this.db.run(sql`CREATE TABLE IF NOT EXISTS weekly_summaries (
        id TEXT PRIMARY KEY DEFAULT (lower(hex(randomblob(16)))),
        competition_id TEXT NOT NULL,
        week_number INTEGER NOT NULL,
        data TEXT NOT NULL,
        generated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`);
          await this.db.run(sql`CREATE TABLE IF NOT EXISTS user_nicknames (
        id TEXT PRIMARY KEY DEFAULT (lower(hex(randomblob(16)))),
        target_user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        set_by_user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        nickname TEXT NOT NULL,
        expires_at TEXT NOT NULL,
        created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`);
          await this.db.run(sql`CREATE TABLE IF NOT EXISTS territory_fortifications (
        id TEXT PRIMARY KEY DEFAULT (lower(hex(randomblob(16)))),
        user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        route_id TEXT REFERENCES routes(id) ON DELETE CASCADE,
        geometry TEXT NOT NULL,
        area REAL NOT NULL DEFAULT 0,
        bbox_min_lng REAL,
        bbox_min_lat REAL,
        bbox_max_lng REAL,
        bbox_max_lat REAL,
        created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`);
          await this.db.run(sql`CREATE INDEX IF NOT EXISTS idx_fortifications_user ON territory_fortifications(user_id)`);
          await this.db.run(sql`CREATE INDEX IF NOT EXISTS idx_fortifications_bbox ON territory_fortifications(bbox_min_lng, bbox_min_lat, bbox_max_lng, bbox_max_lat)`);
          try {
            await this.db.run(sql`ALTER TABLE treasures ADD COLUMN zone TEXT`);
          } catch (_) {
          }
          this._competitionTablesEnsured = true;
        } catch (_) {
        }
      }
      // --- Competition CRUD ---
      async getActiveCompetition() {
        await this.ensureCompetitionTables();
        const result = await this.db.select().from(competitions).where(sql`${competitions.status} IN ('upcoming', 'active')`).orderBy(desc(competitions.createdAt)).limit(1);
        return result.length > 0 ? result[0] : null;
      }
      async getCompetitionById(id) {
        await this.ensureCompetitionTables();
        const result = await this.db.select().from(competitions).where(eq(competitions.id, id)).limit(1);
        return result.length > 0 ? result[0] : null;
      }
      async createCompetition(data) {
        await this.ensureCompetitionTables();
        const [comp] = await this.db.insert(competitions).values(data).returning();
        return comp;
      }
      async updateCompetitionStatus(id, status) {
        await this.ensureCompetitionTables();
        await this.db.update(competitions).set({ status }).where(eq(competitions.id, id));
      }
      // --- Treasure methods ---
      async getActiveTreasures(competitionId) {
        await this.ensureCompetitionTables();
        const now = (/* @__PURE__ */ new Date()).toISOString();
        return await this.db.select().from(treasures).where(and(
          eq(treasures.competitionId, competitionId),
          eq(treasures.active, true),
          sql`${treasures.collectedBy} IS NULL`,
          sql`${treasures.expiresAt} > ${now}`
        ));
      }
      async getTreasureById(id) {
        await this.ensureCompetitionTables();
        const result = await this.db.select().from(treasures).where(eq(treasures.id, id)).limit(1);
        return result.length > 0 ? result[0] : null;
      }
      async createTreasure(data) {
        await this.ensureCompetitionTables();
        const [t] = await this.db.insert(treasures).values(data).returning();
        return t;
      }
      async collectTreasure(treasureId, userId) {
        await this.ensureCompetitionTables();
        const now = (/* @__PURE__ */ new Date()).toISOString();
        await this.db.update(treasures).set({ collectedBy: userId, collectedAt: now, active: false }).where(and(
          eq(treasures.id, treasureId),
          sql`${treasures.collectedBy} IS NULL`,
          eq(treasures.active, true)
        ));
        return this.getTreasureById(treasureId);
      }
      async getAllTreasuresForCompetition(competitionId) {
        await this.ensureCompetitionTables();
        return await this.db.select().from(treasures).where(eq(treasures.competitionId, competitionId));
      }
      // --- User Powers ---
      async getUserPowers(userId, competitionId) {
        await this.ensureCompetitionTables();
        return await this.db.select().from(userPowers).where(and(
          eq(userPowers.userId, userId),
          eq(userPowers.competitionId, competitionId)
        )).orderBy(desc(userPowers.createdAt));
      }
      async getAvailablePowers(userId, competitionId) {
        await this.ensureCompetitionTables();
        return await this.db.select().from(userPowers).where(and(
          eq(userPowers.userId, userId),
          eq(userPowers.competitionId, competitionId),
          eq(userPowers.status, "available")
        ));
      }
      async getActivePowersForUser(userId, competitionId) {
        await this.ensureCompetitionTables();
        const now = (/* @__PURE__ */ new Date()).toISOString();
        return await this.db.select().from(userPowers).where(and(
          eq(userPowers.userId, userId),
          eq(userPowers.competitionId, competitionId),
          eq(userPowers.status, "active"),
          sql`(${userPowers.expiresAt} IS NULL OR ${userPowers.expiresAt} > ${now})`
        ));
      }
      async createUserPower(data) {
        await this.ensureCompetitionTables();
        const [p] = await this.db.insert(userPowers).values(data).returning();
        return p;
      }
      async activatePower(powerId) {
        await this.ensureCompetitionTables();
        const now = (/* @__PURE__ */ new Date()).toISOString();
        await this.db.update(userPowers).set({ status: "active", activatedAt: now }).where(and(eq(userPowers.id, powerId), eq(userPowers.status, "available")));
      }
      async setPowerExpiration(powerId, expiresAt) {
        await this.ensureCompetitionTables();
        await this.db.update(userPowers).set({ expiresAt }).where(eq(userPowers.id, powerId));
      }
      async getAllPushSubscriptions() {
        return await this.db.select().from(pushSubscriptions);
      }
      async usePower(powerId) {
        await this.ensureCompetitionTables();
        const now = (/* @__PURE__ */ new Date()).toISOString();
        await this.db.update(userPowers).set({ status: "used", usedAt: now }).where(and(
          eq(userPowers.id, powerId),
          sql`${userPowers.status} IN ('available', 'active')`
        ));
      }
      async getPowerById(powerId) {
        await this.ensureCompetitionTables();
        const result = await this.db.select().from(userPowers).where(eq(userPowers.id, powerId)).limit(1);
        return result.length > 0 ? result[0] : null;
      }
      /** Check if user has an active (not expired) shield power */
      async hasActiveShield(userId, competitionId) {
        const now = (/* @__PURE__ */ new Date()).toISOString();
        const result = await this.db.select().from(userPowers).where(and(
          eq(userPowers.userId, userId),
          eq(userPowers.competitionId, competitionId),
          eq(userPowers.powerType, "shield"),
          eq(userPowers.status, "active"),
          sql`(${userPowers.expiresAt} IS NULL OR ${userPowers.expiresAt} > ${now})`
        )).limit(1);
        return result.length > 0;
      }
      /** Check if user has an active time_bomb */
      async hasActiveTimeBomb(userId, competitionId) {
        const now = (/* @__PURE__ */ new Date()).toISOString();
        const result = await this.db.select().from(userPowers).where(and(
          eq(userPowers.userId, userId),
          eq(userPowers.competitionId, competitionId),
          eq(userPowers.powerType, "time_bomb"),
          eq(userPowers.status, "active"),
          sql`(${userPowers.expiresAt} IS NULL OR ${userPowers.expiresAt} > ${now})`
        )).limit(1);
        return result.length > 0;
      }
      /** Check if user has invisible territory (capa de sombras) */
      async hasInvisibility(userId, competitionId) {
        const now = (/* @__PURE__ */ new Date()).toISOString();
        const result = await this.db.select().from(userPowers).where(and(
          eq(userPowers.userId, userId),
          eq(userPowers.competitionId, competitionId),
          eq(userPowers.powerType, "invisibility"),
          eq(userPowers.status, "active"),
          sql`(${userPowers.expiresAt} IS NULL OR ${userPowers.expiresAt} > ${now})`
        )).limit(1);
        return result.length > 0;
      }
      /** Generic check for any active power type */
      async hasActivePowerOfType(userId, competitionId, powerType) {
        const now = (/* @__PURE__ */ new Date()).toISOString();
        const result = await this.db.select().from(userPowers).where(and(
          eq(userPowers.userId, userId),
          eq(userPowers.competitionId, competitionId),
          eq(userPowers.powerType, powerType),
          eq(userPowers.status, "active"),
          sql`(${userPowers.expiresAt} IS NULL OR ${userPowers.expiresAt} > ${now})`
        )).limit(1);
        return result.length > 0;
      }
      /** Batch-load all active defensive powers for multiple users in ONE query (saves N*4 subrequests) */
      async getActiveDefensivePowersForUsers(userIds, competitionId) {
        if (userIds.length === 0) return [];
        await this.ensureCompetitionTables();
        const now = (/* @__PURE__ */ new Date()).toISOString();
        const defensiveTypes = ["shield", "invisibility", "time_bomb", "sentinel"];
        const result = await this.db.select({
          userId: userPowers.userId,
          powerType: userPowers.powerType
        }).from(userPowers).where(and(
          inArray(userPowers.userId, userIds),
          eq(userPowers.competitionId, competitionId),
          inArray(userPowers.powerType, defensiveTypes),
          eq(userPowers.status, "active"),
          sql`(${userPowers.expiresAt} IS NULL OR ${userPowers.expiresAt} > ${now})`
        ));
        return result;
      }
      // --- Competition Stats ---
      async getOrCreateCompetitionStats(competitionId, userId) {
        await this.ensureCompetitionTables();
        const existing = await this.db.select().from(competitionStats).where(and(
          eq(competitionStats.competitionId, competitionId),
          eq(competitionStats.userId, userId)
        )).limit(1);
        if (existing.length > 0) return existing[0];
        const [created] = await this.db.insert(competitionStats).values({
          competitionId,
          userId,
          totalArea: 0,
          totalDistance: 0,
          totalDuration: 0,
          activitiesCount: 0,
          treasuresCollected: 0,
          areaStolen: 0,
          uniqueVictims: 0,
          ranTogetherCount: 0
        }).returning();
        return created;
      }
      async updateCompetitionStats(competitionId, userId, updates) {
        await this.ensureCompetitionTables();
        const now = (/* @__PURE__ */ new Date()).toISOString();
        await this.db.update(competitionStats).set({ ...updates, updatedAt: now }).where(and(
          eq(competitionStats.competitionId, competitionId),
          eq(competitionStats.userId, userId)
        ));
      }
      async incrementCompetitionStats(competitionId, userId, increments) {
        await this.ensureCompetitionTables();
        await this.getOrCreateCompetitionStats(competitionId, userId);
        const now = (/* @__PURE__ */ new Date()).toISOString();
        const setParts = [`updated_at = '${now}'`];
        if (increments.distance != null && increments.distance > 0) {
          setParts.push(`total_distance = total_distance + ${increments.distance}`);
        }
        if (increments.duration != null && increments.duration > 0) {
          setParts.push(`total_duration = total_duration + ${increments.duration}`);
        }
        if (increments.activities != null && increments.activities > 0) {
          setParts.push(`activities_count = activities_count + ${increments.activities}`);
        }
        if (increments.treasures != null && increments.treasures > 0) {
          setParts.push(`treasures_collected = treasures_collected + ${increments.treasures}`);
        }
        if (increments.areaStolen != null && increments.areaStolen > 0) {
          setParts.push(`area_stolen = area_stolen + ${increments.areaStolen}`);
        }
        if (increments.ranTogether != null && increments.ranTogether > 0) {
          setParts.push(`ran_together_count = ran_together_count + ${increments.ranTogether}`);
        }
        if (setParts.length > 1) {
          await this.db.run(sql.raw(`UPDATE competition_stats SET ${setParts.join(", ")} WHERE competition_id = '${competitionId}' AND user_id = '${userId}'`));
        }
      }
      /** Count distinct victims (defenders) this user has conquered, from conquest_metrics */
      async getDistinctVictimsCount(attackerId) {
        const result = await this.db.select({ count: sql`COUNT(DISTINCT ${conquestMetrics.defenderId})` }).from(conquestMetrics).where(eq(conquestMetrics.attackerId, attackerId));
        return result[0]?.count || 0;
      }
      async getCompetitionLeaderboard(competitionId) {
        await this.ensureCompetitionTables();
        const stats = await this.db.select().from(competitionStats).where(eq(competitionStats.competitionId, competitionId)).orderBy(desc(competitionStats.totalArea));
        const userIds = stats.map((s) => s.userId);
        if (userIds.length === 0) return [];
        const usersData = await this.db.select().from(users).where(inArray(users.id, userIds));
        const userMap = new Map(usersData.map((u) => [u.id, u]));
        return stats.map((s) => ({
          ...s,
          user: (() => {
            const u = userMap.get(s.userId);
            return { id: u?.id || s.userId, username: u?.username || "", name: u?.name || "", color: u?.color || "#888", avatar: u?.avatar || null };
          })()
        }));
      }
      // --- Weekly Summaries ---
      async getWeeklySummary(competitionId, weekNumber) {
        await this.ensureCompetitionTables();
        const result = await this.db.select().from(weeklySummaries).where(and(
          eq(weeklySummaries.competitionId, competitionId),
          eq(weeklySummaries.weekNumber, weekNumber)
        )).limit(1);
        return result.length > 0 ? result[0] : null;
      }
      async createWeeklySummary(data) {
        await this.ensureCompetitionTables();
        const [ws] = await this.db.insert(weeklySummaries).values(data).returning();
        return ws;
      }
      // --- User Nicknames ---
      async getActiveNickname(targetUserId) {
        await this.ensureCompetitionTables();
        const now = (/* @__PURE__ */ new Date()).toISOString();
        const result = await this.db.select().from(userNicknames).where(and(
          eq(userNicknames.targetUserId, targetUserId),
          sql`${userNicknames.expiresAt} > ${now}`
        )).orderBy(desc(userNicknames.createdAt)).limit(1);
        return result.length > 0 ? result[0] : null;
      }
      async getActiveNicknamesForUsers(userIds) {
        await this.ensureCompetitionTables();
        const now = (/* @__PURE__ */ new Date()).toISOString();
        const result = await this.db.select().from(userNicknames).where(and(
          inArray(userNicknames.targetUserId, userIds),
          sql`${userNicknames.expiresAt} > ${now}`
        ));
        const map2 = /* @__PURE__ */ new Map();
        for (const nn of result) {
          const existing = map2.get(nn.targetUserId);
          if (!existing || nn.createdAt > existing.createdAt) {
            map2.set(nn.targetUserId, { nickname: nn.nickname, expiresAt: nn.expiresAt });
          }
        }
        return map2;
      }
      async createNickname(data) {
        await this.ensureCompetitionTables();
        const [nn] = await this.db.insert(userNicknames).values(data).returning();
        return nn;
      }
      // --- Territory Fortifications ---
      async createFortificationRecord(data) {
        await this.ensureCompetitionTables();
        await this.db.run(sql`INSERT INTO territory_fortifications (id, user_id, route_id, geometry, area, bbox_min_lng, bbox_min_lat, bbox_max_lng, bbox_max_lat)
      VALUES (lower(hex(randomblob(16))), ${data.userId}, ${data.routeId}, ${data.geometry}, ${data.area}, ${data.bboxMinLng}, ${data.bboxMinLat}, ${data.bboxMaxLng}, ${data.bboxMaxLat})`);
      }
      async getFortificationsInBbox(userId, minLng, minLat, maxLng, maxLat) {
        await this.ensureCompetitionTables();
        const results = await this.db.select({
          id: territoryFortifications.id,
          geometry: territoryFortifications.geometry,
          area: territoryFortifications.area
        }).from(territoryFortifications).where(and(
          eq(territoryFortifications.userId, userId),
          sql`${territoryFortifications.bboxMaxLng} >= ${minLng}`,
          sql`${territoryFortifications.bboxMinLng} <= ${maxLng}`,
          sql`${territoryFortifications.bboxMaxLat} >= ${minLat}`,
          sql`${territoryFortifications.bboxMinLat} <= ${maxLat}`
        ));
        return results;
      }
      async getFortificationsByUserId(userId) {
        await this.ensureCompetitionTables();
        return await this.db.select({
          id: territoryFortifications.id,
          geometry: territoryFortifications.geometry,
          area: territoryFortifications.area,
          routeId: territoryFortifications.routeId
        }).from(territoryFortifications).where(eq(territoryFortifications.userId, userId));
      }
      async getAllFortifications() {
        await this.ensureCompetitionTables();
        return await this.db.select({
          id: territoryFortifications.id,
          userId: territoryFortifications.userId,
          geometry: territoryFortifications.geometry,
          area: territoryFortifications.area
        }).from(territoryFortifications);
      }
      async deleteFortificationsByRouteId(routeId) {
        await this.ensureCompetitionTables();
        await this.db.run(sql`DELETE FROM territory_fortifications WHERE route_id = ${routeId}`);
      }
      async deleteFortificationRecord(id) {
        await this.ensureCompetitionTables();
        await this.db.run(sql`DELETE FROM territory_fortifications WHERE id = ${id}`);
      }
      /** Delete multiple fortification records in a single query */
      async deleteFortificationRecordsBatch(ids) {
        if (ids.length === 0) return;
        await this.ensureCompetitionTables();
        const placeholders = ids.map((id) => `'${id.replace(/'/g, "''")}'`).join(",");
        await this.db.run(sql.raw(`DELETE FROM territory_fortifications WHERE id IN (${placeholders})`));
      }
      /** Batch insert conquest metrics in a single query (saves N subrequests → 1) */
      async recordConquestMetricsBatch(metrics) {
        if (metrics.length === 0) return;
        const values = metrics.map((m) => {
          const id = crypto.randomUUID();
          const rid = m.routeId ? `'${m.routeId.replace(/'/g, "''")}'` : "NULL";
          return `('${id}', '${m.attackerId.replace(/'/g, "''")}', '${m.defenderId.replace(/'/g, "''")}', ${m.areaStolen}, ${rid}, datetime('now'))`;
        }).join(",");
        await this.db.run(sql.raw(`INSERT INTO conquest_metrics (id, attacker_id, defender_id, area_stolen, route_id, conquered_at) VALUES ${values}`));
      }
      /** Batch insert feed events in a single query (saves N subrequests → 1) */
      async createFeedEventsBatch(events) {
        if (events.length === 0) return;
        const values = events.map((e) => {
          const id = crypto.randomUUID();
          return `('${id}', '${e.userId.replace(/'/g, "''")}', '${e.eventType.replace(/'/g, "''")}', '${e.metadata.replace(/'/g, "''")}', datetime('now'))`;
        }).join(",");
        await this.db.run(sql.raw(`INSERT INTO feed_events (id, user_id, event_type, metadata, created_at) VALUES ${values}`));
      }
      /** Batch update victim areas using subquery — single DB call for all victims */
      async updateVictimAreasBatch(victimIds) {
        if (victimIds.length === 0) return;
        const placeholders = victimIds.map((id) => `'${id.replace(/'/g, "''")}'`).join(",");
        await this.db.run(sql.raw(`
      UPDATE users SET total_area = COALESCE(
        (SELECT SUM(area) FROM territories WHERE territories.user_id = users.id), 0
      ) WHERE id IN (${placeholders})
    `));
      }
      // --- DB Reset for competition ---
      async resetForCompetition() {
        await this.db.run(sql`DELETE FROM feed_reactions`);
        await this.db.run(sql`DELETE FROM feed_comments`);
        await this.db.run(sql`DELETE FROM feed_events`);
        await this.db.run(sql`DELETE FROM conquest_metrics`);
        await this.db.run(sql`DELETE FROM ephemeral_photos`);
        await this.db.run(sql`DELETE FROM email_notifications`);
        await this.db.run(sql`DELETE FROM inactivity_reminders`);
        try {
          await this.db.run(sql`DELETE FROM territory_loss_notifications`);
        } catch (_) {
        }
        try {
          await this.db.run(sql`DELETE FROM user_nicknames`);
        } catch (_) {
        }
        try {
          await this.db.run(sql`DELETE FROM user_powers`);
        } catch (_) {
        }
        try {
          await this.db.run(sql`DELETE FROM treasures`);
        } catch (_) {
        }
        try {
          await this.db.run(sql`DELETE FROM competition_stats`);
        } catch (_) {
        }
        try {
          await this.db.run(sql`DELETE FROM weekly_summaries`);
        } catch (_) {
        }
        try {
          await this.db.run(sql`DELETE FROM territory_fortifications`);
        } catch (_) {
        }
        const routeCount = await this.db.select({ count: sql`count(*)` }).from(routes);
        const terrCount = await this.db.select({ count: sql`count(*)` }).from(territories);
        await this.db.run(sql`DELETE FROM territories`);
        await this.db.run(sql`DELETE FROM routes`);
        await this.db.run(sql`UPDATE users SET total_area = 0`);
        await this.db.run(sql`UPDATE polar_activities SET processed = 0, route_id = NULL, territory_id = NULL`);
        await this.db.run(sql`UPDATE strava_activities SET processed = 0, route_id = NULL, territory_id = NULL`);
        return {
          deletedRoutes: routeCount[0]?.count || 0,
          deletedTerritories: terrCount[0]?.count || 0
        };
      }
    };
  }
});

// node_modules/svix/dist/models/applicationIn.js
var require_applicationIn = __commonJS({
  "node_modules/svix/dist/models/applicationIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ApplicationInSerializer = void 0;
    exports.ApplicationInSerializer = {
      _fromJsonObject(object2) {
        return {
          metadata: object2["metadata"],
          name: object2["name"],
          rateLimit: object2["rateLimit"],
          uid: object2["uid"]
        };
      },
      _toJsonObject(self2) {
        return {
          metadata: self2.metadata,
          name: self2.name,
          rateLimit: self2.rateLimit,
          uid: self2.uid
        };
      }
    };
  }
});

// node_modules/svix/dist/models/applicationOut.js
var require_applicationOut = __commonJS({
  "node_modules/svix/dist/models/applicationOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ApplicationOutSerializer = void 0;
    exports.ApplicationOutSerializer = {
      _fromJsonObject(object2) {
        return {
          createdAt: new Date(object2["createdAt"]),
          id: object2["id"],
          metadata: object2["metadata"],
          name: object2["name"],
          rateLimit: object2["rateLimit"],
          uid: object2["uid"],
          updatedAt: new Date(object2["updatedAt"])
        };
      },
      _toJsonObject(self2) {
        return {
          createdAt: self2.createdAt,
          id: self2.id,
          metadata: self2.metadata,
          name: self2.name,
          rateLimit: self2.rateLimit,
          uid: self2.uid,
          updatedAt: self2.updatedAt
        };
      }
    };
  }
});

// node_modules/svix/dist/models/applicationPatch.js
var require_applicationPatch = __commonJS({
  "node_modules/svix/dist/models/applicationPatch.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ApplicationPatchSerializer = void 0;
    exports.ApplicationPatchSerializer = {
      _fromJsonObject(object2) {
        return {
          metadata: object2["metadata"],
          name: object2["name"],
          rateLimit: object2["rateLimit"],
          uid: object2["uid"]
        };
      },
      _toJsonObject(self2) {
        return {
          metadata: self2.metadata,
          name: self2.name,
          rateLimit: self2.rateLimit,
          uid: self2.uid
        };
      }
    };
  }
});

// node_modules/svix/dist/models/listResponseApplicationOut.js
var require_listResponseApplicationOut = __commonJS({
  "node_modules/svix/dist/models/listResponseApplicationOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ListResponseApplicationOutSerializer = void 0;
    var applicationOut_1 = require_applicationOut();
    exports.ListResponseApplicationOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => applicationOut_1.ApplicationOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"],
          prevIterator: object2["prevIterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => applicationOut_1.ApplicationOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator,
          prevIterator: self2.prevIterator
        };
      }
    };
  }
});

// node_modules/svix/dist/util.js
var require_util = __commonJS({
  "node_modules/svix/dist/util.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ApiException = void 0;
    var ApiException = class extends Error {
      static {
        __name(this, "ApiException");
      }
      constructor(code, body, headers) {
        super(`HTTP-Code: ${code}
Headers: ${JSON.stringify(headers)}`);
        this.code = code;
        this.body = body;
        this.headers = {};
        headers.forEach((value, name) => {
          this.headers[name] = value;
        });
      }
    };
    exports.ApiException = ApiException;
  }
});

// node_modules/uuid/dist/commonjs-browser/max.js
var require_max = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/max.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _default = exports.default = "ffffffff-ffff-ffff-ffff-ffffffffffff";
  }
});

// node_modules/uuid/dist/commonjs-browser/nil.js
var require_nil = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/nil.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _default = exports.default = "00000000-0000-0000-0000-000000000000";
  }
});

// node_modules/uuid/dist/commonjs-browser/regex.js
var require_regex = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/regex.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _default = exports.default = /^(?:[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$/i;
  }
});

// node_modules/uuid/dist/commonjs-browser/validate.js
var require_validate = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/validate.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _regex = _interopRequireDefault(require_regex());
    function _interopRequireDefault(e) {
      return e && e.__esModule ? e : { default: e };
    }
    __name(_interopRequireDefault, "_interopRequireDefault");
    function validate(uuid) {
      return typeof uuid === "string" && _regex.default.test(uuid);
    }
    __name(validate, "validate");
    var _default = exports.default = validate;
  }
});

// node_modules/uuid/dist/commonjs-browser/parse.js
var require_parse = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/parse.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _validate = _interopRequireDefault(require_validate());
    function _interopRequireDefault(e) {
      return e && e.__esModule ? e : { default: e };
    }
    __name(_interopRequireDefault, "_interopRequireDefault");
    function parse(uuid) {
      if (!(0, _validate.default)(uuid)) {
        throw TypeError("Invalid UUID");
      }
      var v;
      var arr = new Uint8Array(16);
      arr[0] = (v = parseInt(uuid.slice(0, 8), 16)) >>> 24;
      arr[1] = v >>> 16 & 255;
      arr[2] = v >>> 8 & 255;
      arr[3] = v & 255;
      arr[4] = (v = parseInt(uuid.slice(9, 13), 16)) >>> 8;
      arr[5] = v & 255;
      arr[6] = (v = parseInt(uuid.slice(14, 18), 16)) >>> 8;
      arr[7] = v & 255;
      arr[8] = (v = parseInt(uuid.slice(19, 23), 16)) >>> 8;
      arr[9] = v & 255;
      arr[10] = (v = parseInt(uuid.slice(24, 36), 16)) / 1099511627776 & 255;
      arr[11] = v / 4294967296 & 255;
      arr[12] = v >>> 24 & 255;
      arr[13] = v >>> 16 & 255;
      arr[14] = v >>> 8 & 255;
      arr[15] = v & 255;
      return arr;
    }
    __name(parse, "parse");
    var _default = exports.default = parse;
  }
});

// node_modules/uuid/dist/commonjs-browser/stringify.js
var require_stringify = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/stringify.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    exports.unsafeStringify = unsafeStringify;
    var _validate = _interopRequireDefault(require_validate());
    function _interopRequireDefault(e) {
      return e && e.__esModule ? e : { default: e };
    }
    __name(_interopRequireDefault, "_interopRequireDefault");
    var byteToHex = [];
    for (i = 0; i < 256; ++i) {
      byteToHex.push((i + 256).toString(16).slice(1));
    }
    var i;
    function unsafeStringify(arr, offset = 0) {
      return (byteToHex[arr[offset + 0]] + byteToHex[arr[offset + 1]] + byteToHex[arr[offset + 2]] + byteToHex[arr[offset + 3]] + "-" + byteToHex[arr[offset + 4]] + byteToHex[arr[offset + 5]] + "-" + byteToHex[arr[offset + 6]] + byteToHex[arr[offset + 7]] + "-" + byteToHex[arr[offset + 8]] + byteToHex[arr[offset + 9]] + "-" + byteToHex[arr[offset + 10]] + byteToHex[arr[offset + 11]] + byteToHex[arr[offset + 12]] + byteToHex[arr[offset + 13]] + byteToHex[arr[offset + 14]] + byteToHex[arr[offset + 15]]).toLowerCase();
    }
    __name(unsafeStringify, "unsafeStringify");
    function stringify(arr, offset = 0) {
      var uuid = unsafeStringify(arr, offset);
      if (!(0, _validate.default)(uuid)) {
        throw TypeError("Stringified UUID is invalid");
      }
      return uuid;
    }
    __name(stringify, "stringify");
    var _default = exports.default = stringify;
  }
});

// node_modules/uuid/dist/commonjs-browser/rng.js
var require_rng = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/rng.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = rng;
    var getRandomValues;
    var rnds8 = new Uint8Array(16);
    function rng() {
      if (!getRandomValues) {
        getRandomValues = typeof crypto !== "undefined" && crypto.getRandomValues && crypto.getRandomValues.bind(crypto);
        if (!getRandomValues) {
          throw new Error("crypto.getRandomValues() not supported. See https://github.com/uuidjs/uuid#getrandomvalues-not-supported");
        }
      }
      return getRandomValues(rnds8);
    }
    __name(rng, "rng");
  }
});

// node_modules/uuid/dist/commonjs-browser/v1.js
var require_v1 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/v1.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _rng = _interopRequireDefault(require_rng());
    var _stringify = require_stringify();
    function _interopRequireDefault(e) {
      return e && e.__esModule ? e : { default: e };
    }
    __name(_interopRequireDefault, "_interopRequireDefault");
    var _nodeId;
    var _clockseq;
    var _lastMSecs = 0;
    var _lastNSecs = 0;
    function v1(options, buf, offset) {
      var i = buf && offset || 0;
      var b = buf || new Array(16);
      options = options || {};
      var node = options.node;
      var clockseq = options.clockseq;
      if (!options._v6) {
        if (!node) {
          node = _nodeId;
        }
        if (clockseq == null) {
          clockseq = _clockseq;
        }
      }
      if (node == null || clockseq == null) {
        var seedBytes = options.random || (options.rng || _rng.default)();
        if (node == null) {
          node = [seedBytes[0], seedBytes[1], seedBytes[2], seedBytes[3], seedBytes[4], seedBytes[5]];
          if (!_nodeId && !options._v6) {
            node[0] |= 1;
            _nodeId = node;
          }
        }
        if (clockseq == null) {
          clockseq = (seedBytes[6] << 8 | seedBytes[7]) & 16383;
          if (_clockseq === void 0 && !options._v6) {
            _clockseq = clockseq;
          }
        }
      }
      var msecs = options.msecs !== void 0 ? options.msecs : Date.now();
      var nsecs = options.nsecs !== void 0 ? options.nsecs : _lastNSecs + 1;
      var dt = msecs - _lastMSecs + (nsecs - _lastNSecs) / 1e4;
      if (dt < 0 && options.clockseq === void 0) {
        clockseq = clockseq + 1 & 16383;
      }
      if ((dt < 0 || msecs > _lastMSecs) && options.nsecs === void 0) {
        nsecs = 0;
      }
      if (nsecs >= 1e4) {
        throw new Error("uuid.v1(): Can't create more than 10M uuids/sec");
      }
      _lastMSecs = msecs;
      _lastNSecs = nsecs;
      _clockseq = clockseq;
      msecs += 122192928e5;
      var tl = ((msecs & 268435455) * 1e4 + nsecs) % 4294967296;
      b[i++] = tl >>> 24 & 255;
      b[i++] = tl >>> 16 & 255;
      b[i++] = tl >>> 8 & 255;
      b[i++] = tl & 255;
      var tmh = msecs / 4294967296 * 1e4 & 268435455;
      b[i++] = tmh >>> 8 & 255;
      b[i++] = tmh & 255;
      b[i++] = tmh >>> 24 & 15 | 16;
      b[i++] = tmh >>> 16 & 255;
      b[i++] = clockseq >>> 8 | 128;
      b[i++] = clockseq & 255;
      for (var n = 0; n < 6; ++n) {
        b[i + n] = node[n];
      }
      return buf || (0, _stringify.unsafeStringify)(b);
    }
    __name(v1, "v1");
    var _default = exports.default = v1;
  }
});

// node_modules/uuid/dist/commonjs-browser/v1ToV6.js
var require_v1ToV6 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/v1ToV6.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = v1ToV6;
    var _parse = _interopRequireDefault(require_parse());
    var _stringify = require_stringify();
    function _interopRequireDefault(e) {
      return e && e.__esModule ? e : { default: e };
    }
    __name(_interopRequireDefault, "_interopRequireDefault");
    function v1ToV6(uuid) {
      var v1Bytes = typeof uuid === "string" ? (0, _parse.default)(uuid) : uuid;
      var v6Bytes = _v1ToV6(v1Bytes);
      return typeof uuid === "string" ? (0, _stringify.unsafeStringify)(v6Bytes) : v6Bytes;
    }
    __name(v1ToV6, "v1ToV6");
    function _v1ToV6(v1Bytes, randomize = false) {
      return Uint8Array.of((v1Bytes[6] & 15) << 4 | v1Bytes[7] >> 4 & 15, (v1Bytes[7] & 15) << 4 | (v1Bytes[4] & 240) >> 4, (v1Bytes[4] & 15) << 4 | (v1Bytes[5] & 240) >> 4, (v1Bytes[5] & 15) << 4 | (v1Bytes[0] & 240) >> 4, (v1Bytes[0] & 15) << 4 | (v1Bytes[1] & 240) >> 4, (v1Bytes[1] & 15) << 4 | (v1Bytes[2] & 240) >> 4, 96 | v1Bytes[2] & 15, v1Bytes[3], v1Bytes[8], v1Bytes[9], v1Bytes[10], v1Bytes[11], v1Bytes[12], v1Bytes[13], v1Bytes[14], v1Bytes[15]);
    }
    __name(_v1ToV6, "_v1ToV6");
  }
});

// node_modules/uuid/dist/commonjs-browser/v35.js
var require_v35 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/v35.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.URL = exports.DNS = void 0;
    exports.default = v35;
    var _stringify = require_stringify();
    var _parse = _interopRequireDefault(require_parse());
    function _interopRequireDefault(e) {
      return e && e.__esModule ? e : { default: e };
    }
    __name(_interopRequireDefault, "_interopRequireDefault");
    function stringToBytes(str) {
      str = unescape(encodeURIComponent(str));
      var bytes = [];
      for (var i = 0; i < str.length; ++i) {
        bytes.push(str.charCodeAt(i));
      }
      return bytes;
    }
    __name(stringToBytes, "stringToBytes");
    var DNS = exports.DNS = "6ba7b810-9dad-11d1-80b4-00c04fd430c8";
    var URL2 = exports.URL = "6ba7b811-9dad-11d1-80b4-00c04fd430c8";
    function v35(name, version5, hashfunc) {
      function generateUUID(value, namespace, buf, offset) {
        var _namespace;
        if (typeof value === "string") {
          value = stringToBytes(value);
        }
        if (typeof namespace === "string") {
          namespace = (0, _parse.default)(namespace);
        }
        if (((_namespace = namespace) === null || _namespace === void 0 ? void 0 : _namespace.length) !== 16) {
          throw TypeError("Namespace must be array-like (16 iterable integer values, 0-255)");
        }
        var bytes = new Uint8Array(16 + value.length);
        bytes.set(namespace);
        bytes.set(value, namespace.length);
        bytes = hashfunc(bytes);
        bytes[6] = bytes[6] & 15 | version5;
        bytes[8] = bytes[8] & 63 | 128;
        if (buf) {
          offset = offset || 0;
          for (var i = 0; i < 16; ++i) {
            buf[offset + i] = bytes[i];
          }
          return buf;
        }
        return (0, _stringify.unsafeStringify)(bytes);
      }
      __name(generateUUID, "generateUUID");
      try {
        generateUUID.name = name;
      } catch (err) {
      }
      generateUUID.DNS = DNS;
      generateUUID.URL = URL2;
      return generateUUID;
    }
    __name(v35, "v35");
  }
});

// node_modules/uuid/dist/commonjs-browser/md5.js
var require_md5 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/md5.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    function md5(bytes) {
      if (typeof bytes === "string") {
        var msg = unescape(encodeURIComponent(bytes));
        bytes = new Uint8Array(msg.length);
        for (var i = 0; i < msg.length; ++i) {
          bytes[i] = msg.charCodeAt(i);
        }
      }
      return md5ToHexEncodedArray(wordsToMd5(bytesToWords(bytes), bytes.length * 8));
    }
    __name(md5, "md5");
    function md5ToHexEncodedArray(input) {
      var output = [];
      var length32 = input.length * 32;
      var hexTab = "0123456789abcdef";
      for (var i = 0; i < length32; i += 8) {
        var x = input[i >> 5] >>> i % 32 & 255;
        var hex = parseInt(hexTab.charAt(x >>> 4 & 15) + hexTab.charAt(x & 15), 16);
        output.push(hex);
      }
      return output;
    }
    __name(md5ToHexEncodedArray, "md5ToHexEncodedArray");
    function getOutputLength(inputLength8) {
      return (inputLength8 + 64 >>> 9 << 4) + 14 + 1;
    }
    __name(getOutputLength, "getOutputLength");
    function wordsToMd5(x, len) {
      x[len >> 5] |= 128 << len % 32;
      x[getOutputLength(len) - 1] = len;
      var a = 1732584193;
      var b = -271733879;
      var c = -1732584194;
      var d = 271733878;
      for (var i = 0; i < x.length; i += 16) {
        var olda = a;
        var oldb = b;
        var oldc = c;
        var oldd = d;
        a = md5ff(a, b, c, d, x[i], 7, -680876936);
        d = md5ff(d, a, b, c, x[i + 1], 12, -389564586);
        c = md5ff(c, d, a, b, x[i + 2], 17, 606105819);
        b = md5ff(b, c, d, a, x[i + 3], 22, -1044525330);
        a = md5ff(a, b, c, d, x[i + 4], 7, -176418897);
        d = md5ff(d, a, b, c, x[i + 5], 12, 1200080426);
        c = md5ff(c, d, a, b, x[i + 6], 17, -1473231341);
        b = md5ff(b, c, d, a, x[i + 7], 22, -45705983);
        a = md5ff(a, b, c, d, x[i + 8], 7, 1770035416);
        d = md5ff(d, a, b, c, x[i + 9], 12, -1958414417);
        c = md5ff(c, d, a, b, x[i + 10], 17, -42063);
        b = md5ff(b, c, d, a, x[i + 11], 22, -1990404162);
        a = md5ff(a, b, c, d, x[i + 12], 7, 1804603682);
        d = md5ff(d, a, b, c, x[i + 13], 12, -40341101);
        c = md5ff(c, d, a, b, x[i + 14], 17, -1502002290);
        b = md5ff(b, c, d, a, x[i + 15], 22, 1236535329);
        a = md5gg(a, b, c, d, x[i + 1], 5, -165796510);
        d = md5gg(d, a, b, c, x[i + 6], 9, -1069501632);
        c = md5gg(c, d, a, b, x[i + 11], 14, 643717713);
        b = md5gg(b, c, d, a, x[i], 20, -373897302);
        a = md5gg(a, b, c, d, x[i + 5], 5, -701558691);
        d = md5gg(d, a, b, c, x[i + 10], 9, 38016083);
        c = md5gg(c, d, a, b, x[i + 15], 14, -660478335);
        b = md5gg(b, c, d, a, x[i + 4], 20, -405537848);
        a = md5gg(a, b, c, d, x[i + 9], 5, 568446438);
        d = md5gg(d, a, b, c, x[i + 14], 9, -1019803690);
        c = md5gg(c, d, a, b, x[i + 3], 14, -187363961);
        b = md5gg(b, c, d, a, x[i + 8], 20, 1163531501);
        a = md5gg(a, b, c, d, x[i + 13], 5, -1444681467);
        d = md5gg(d, a, b, c, x[i + 2], 9, -51403784);
        c = md5gg(c, d, a, b, x[i + 7], 14, 1735328473);
        b = md5gg(b, c, d, a, x[i + 12], 20, -1926607734);
        a = md5hh(a, b, c, d, x[i + 5], 4, -378558);
        d = md5hh(d, a, b, c, x[i + 8], 11, -2022574463);
        c = md5hh(c, d, a, b, x[i + 11], 16, 1839030562);
        b = md5hh(b, c, d, a, x[i + 14], 23, -35309556);
        a = md5hh(a, b, c, d, x[i + 1], 4, -1530992060);
        d = md5hh(d, a, b, c, x[i + 4], 11, 1272893353);
        c = md5hh(c, d, a, b, x[i + 7], 16, -155497632);
        b = md5hh(b, c, d, a, x[i + 10], 23, -1094730640);
        a = md5hh(a, b, c, d, x[i + 13], 4, 681279174);
        d = md5hh(d, a, b, c, x[i], 11, -358537222);
        c = md5hh(c, d, a, b, x[i + 3], 16, -722521979);
        b = md5hh(b, c, d, a, x[i + 6], 23, 76029189);
        a = md5hh(a, b, c, d, x[i + 9], 4, -640364487);
        d = md5hh(d, a, b, c, x[i + 12], 11, -421815835);
        c = md5hh(c, d, a, b, x[i + 15], 16, 530742520);
        b = md5hh(b, c, d, a, x[i + 2], 23, -995338651);
        a = md5ii(a, b, c, d, x[i], 6, -198630844);
        d = md5ii(d, a, b, c, x[i + 7], 10, 1126891415);
        c = md5ii(c, d, a, b, x[i + 14], 15, -1416354905);
        b = md5ii(b, c, d, a, x[i + 5], 21, -57434055);
        a = md5ii(a, b, c, d, x[i + 12], 6, 1700485571);
        d = md5ii(d, a, b, c, x[i + 3], 10, -1894986606);
        c = md5ii(c, d, a, b, x[i + 10], 15, -1051523);
        b = md5ii(b, c, d, a, x[i + 1], 21, -2054922799);
        a = md5ii(a, b, c, d, x[i + 8], 6, 1873313359);
        d = md5ii(d, a, b, c, x[i + 15], 10, -30611744);
        c = md5ii(c, d, a, b, x[i + 6], 15, -1560198380);
        b = md5ii(b, c, d, a, x[i + 13], 21, 1309151649);
        a = md5ii(a, b, c, d, x[i + 4], 6, -145523070);
        d = md5ii(d, a, b, c, x[i + 11], 10, -1120210379);
        c = md5ii(c, d, a, b, x[i + 2], 15, 718787259);
        b = md5ii(b, c, d, a, x[i + 9], 21, -343485551);
        a = safeAdd(a, olda);
        b = safeAdd(b, oldb);
        c = safeAdd(c, oldc);
        d = safeAdd(d, oldd);
      }
      return [a, b, c, d];
    }
    __name(wordsToMd5, "wordsToMd5");
    function bytesToWords(input) {
      if (input.length === 0) {
        return [];
      }
      var length8 = input.length * 8;
      var output = new Uint32Array(getOutputLength(length8));
      for (var i = 0; i < length8; i += 8) {
        output[i >> 5] |= (input[i / 8] & 255) << i % 32;
      }
      return output;
    }
    __name(bytesToWords, "bytesToWords");
    function safeAdd(x, y) {
      var lsw = (x & 65535) + (y & 65535);
      var msw = (x >> 16) + (y >> 16) + (lsw >> 16);
      return msw << 16 | lsw & 65535;
    }
    __name(safeAdd, "safeAdd");
    function bitRotateLeft(num, cnt) {
      return num << cnt | num >>> 32 - cnt;
    }
    __name(bitRotateLeft, "bitRotateLeft");
    function md5cmn(q, a, b, x, s, t) {
      return safeAdd(bitRotateLeft(safeAdd(safeAdd(a, q), safeAdd(x, t)), s), b);
    }
    __name(md5cmn, "md5cmn");
    function md5ff(a, b, c, d, x, s, t) {
      return md5cmn(b & c | ~b & d, a, b, x, s, t);
    }
    __name(md5ff, "md5ff");
    function md5gg(a, b, c, d, x, s, t) {
      return md5cmn(b & d | c & ~d, a, b, x, s, t);
    }
    __name(md5gg, "md5gg");
    function md5hh(a, b, c, d, x, s, t) {
      return md5cmn(b ^ c ^ d, a, b, x, s, t);
    }
    __name(md5hh, "md5hh");
    function md5ii(a, b, c, d, x, s, t) {
      return md5cmn(c ^ (b | ~d), a, b, x, s, t);
    }
    __name(md5ii, "md5ii");
    var _default = exports.default = md5;
  }
});

// node_modules/uuid/dist/commonjs-browser/v3.js
var require_v3 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/v3.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _v = _interopRequireDefault(require_v35());
    var _md = _interopRequireDefault(require_md5());
    function _interopRequireDefault(e) {
      return e && e.__esModule ? e : { default: e };
    }
    __name(_interopRequireDefault, "_interopRequireDefault");
    var v3 = (0, _v.default)("v3", 48, _md.default);
    var _default = exports.default = v3;
  }
});

// node_modules/uuid/dist/commonjs-browser/native.js
var require_native = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/native.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var randomUUID = typeof crypto !== "undefined" && crypto.randomUUID && crypto.randomUUID.bind(crypto);
    var _default = exports.default = {
      randomUUID
    };
  }
});

// node_modules/uuid/dist/commonjs-browser/v4.js
var require_v4 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/v4.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _native = _interopRequireDefault(require_native());
    var _rng = _interopRequireDefault(require_rng());
    var _stringify = require_stringify();
    function _interopRequireDefault(e) {
      return e && e.__esModule ? e : { default: e };
    }
    __name(_interopRequireDefault, "_interopRequireDefault");
    function v4(options, buf, offset) {
      if (_native.default.randomUUID && !buf && !options) {
        return _native.default.randomUUID();
      }
      options = options || {};
      var rnds = options.random || (options.rng || _rng.default)();
      rnds[6] = rnds[6] & 15 | 64;
      rnds[8] = rnds[8] & 63 | 128;
      if (buf) {
        offset = offset || 0;
        for (var i = 0; i < 16; ++i) {
          buf[offset + i] = rnds[i];
        }
        return buf;
      }
      return (0, _stringify.unsafeStringify)(rnds);
    }
    __name(v4, "v4");
    var _default = exports.default = v4;
  }
});

// node_modules/uuid/dist/commonjs-browser/sha1.js
var require_sha1 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/sha1.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    function f(s, x, y, z2) {
      switch (s) {
        case 0:
          return x & y ^ ~x & z2;
        case 1:
          return x ^ y ^ z2;
        case 2:
          return x & y ^ x & z2 ^ y & z2;
        case 3:
          return x ^ y ^ z2;
      }
    }
    __name(f, "f");
    function ROTL(x, n) {
      return x << n | x >>> 32 - n;
    }
    __name(ROTL, "ROTL");
    function sha1(bytes) {
      var K = [1518500249, 1859775393, 2400959708, 3395469782];
      var H = [1732584193, 4023233417, 2562383102, 271733878, 3285377520];
      if (typeof bytes === "string") {
        var msg = unescape(encodeURIComponent(bytes));
        bytes = [];
        for (var i = 0; i < msg.length; ++i) {
          bytes.push(msg.charCodeAt(i));
        }
      } else if (!Array.isArray(bytes)) {
        bytes = Array.prototype.slice.call(bytes);
      }
      bytes.push(128);
      var l = bytes.length / 4 + 2;
      var N = Math.ceil(l / 16);
      var M = new Array(N);
      for (var _i = 0; _i < N; ++_i) {
        var arr = new Uint32Array(16);
        for (var j = 0; j < 16; ++j) {
          arr[j] = bytes[_i * 64 + j * 4] << 24 | bytes[_i * 64 + j * 4 + 1] << 16 | bytes[_i * 64 + j * 4 + 2] << 8 | bytes[_i * 64 + j * 4 + 3];
        }
        M[_i] = arr;
      }
      M[N - 1][14] = (bytes.length - 1) * 8 / Math.pow(2, 32);
      M[N - 1][14] = Math.floor(M[N - 1][14]);
      M[N - 1][15] = (bytes.length - 1) * 8 & 4294967295;
      for (var _i2 = 0; _i2 < N; ++_i2) {
        var W = new Uint32Array(80);
        for (var t = 0; t < 16; ++t) {
          W[t] = M[_i2][t];
        }
        for (var _t = 16; _t < 80; ++_t) {
          W[_t] = ROTL(W[_t - 3] ^ W[_t - 8] ^ W[_t - 14] ^ W[_t - 16], 1);
        }
        var a = H[0];
        var b = H[1];
        var c = H[2];
        var d = H[3];
        var e = H[4];
        for (var _t2 = 0; _t2 < 80; ++_t2) {
          var s = Math.floor(_t2 / 20);
          var T = ROTL(a, 5) + f(s, b, c, d) + e + K[s] + W[_t2] >>> 0;
          e = d;
          d = c;
          c = ROTL(b, 30) >>> 0;
          b = a;
          a = T;
        }
        H[0] = H[0] + a >>> 0;
        H[1] = H[1] + b >>> 0;
        H[2] = H[2] + c >>> 0;
        H[3] = H[3] + d >>> 0;
        H[4] = H[4] + e >>> 0;
      }
      return [H[0] >> 24 & 255, H[0] >> 16 & 255, H[0] >> 8 & 255, H[0] & 255, H[1] >> 24 & 255, H[1] >> 16 & 255, H[1] >> 8 & 255, H[1] & 255, H[2] >> 24 & 255, H[2] >> 16 & 255, H[2] >> 8 & 255, H[2] & 255, H[3] >> 24 & 255, H[3] >> 16 & 255, H[3] >> 8 & 255, H[3] & 255, H[4] >> 24 & 255, H[4] >> 16 & 255, H[4] >> 8 & 255, H[4] & 255];
    }
    __name(sha1, "sha1");
    var _default = exports.default = sha1;
  }
});

// node_modules/uuid/dist/commonjs-browser/v5.js
var require_v5 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/v5.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _v = _interopRequireDefault(require_v35());
    var _sha = _interopRequireDefault(require_sha1());
    function _interopRequireDefault(e) {
      return e && e.__esModule ? e : { default: e };
    }
    __name(_interopRequireDefault, "_interopRequireDefault");
    var v5 = (0, _v.default)("v5", 80, _sha.default);
    var _default = exports.default = v5;
  }
});

// node_modules/uuid/dist/commonjs-browser/v6.js
var require_v6 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/v6.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = v6;
    var _stringify = require_stringify();
    var _v = _interopRequireDefault(require_v1());
    var _v1ToV = _interopRequireDefault(require_v1ToV6());
    function _interopRequireDefault(e) {
      return e && e.__esModule ? e : { default: e };
    }
    __name(_interopRequireDefault, "_interopRequireDefault");
    function ownKeys(e, r) {
      var t = Object.keys(e);
      if (Object.getOwnPropertySymbols) {
        var o = Object.getOwnPropertySymbols(e);
        r && (o = o.filter(function(r2) {
          return Object.getOwnPropertyDescriptor(e, r2).enumerable;
        })), t.push.apply(t, o);
      }
      return t;
    }
    __name(ownKeys, "ownKeys");
    function _objectSpread(e) {
      for (var r = 1; r < arguments.length; r++) {
        var t = null != arguments[r] ? arguments[r] : {};
        r % 2 ? ownKeys(Object(t), true).forEach(function(r2) {
          _defineProperty(e, r2, t[r2]);
        }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function(r2) {
          Object.defineProperty(e, r2, Object.getOwnPropertyDescriptor(t, r2));
        });
      }
      return e;
    }
    __name(_objectSpread, "_objectSpread");
    function _defineProperty(e, r, t) {
      return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: true, configurable: true, writable: true }) : e[r] = t, e;
    }
    __name(_defineProperty, "_defineProperty");
    function _toPropertyKey(t) {
      var i = _toPrimitive(t, "string");
      return "symbol" == typeof i ? i : i + "";
    }
    __name(_toPropertyKey, "_toPropertyKey");
    function _toPrimitive(t, r) {
      if ("object" != typeof t || !t) return t;
      var e = t[Symbol.toPrimitive];
      if (void 0 !== e) {
        var i = e.call(t, r || "default");
        if ("object" != typeof i) return i;
        throw new TypeError("@@toPrimitive must return a primitive value.");
      }
      return ("string" === r ? String : Number)(t);
    }
    __name(_toPrimitive, "_toPrimitive");
    function v6(options = {}, buf, offset = 0) {
      var bytes = (0, _v.default)(_objectSpread(_objectSpread({}, options), {}, {
        _v6: true
      }), new Uint8Array(16));
      bytes = (0, _v1ToV.default)(bytes);
      if (buf) {
        for (var i = 0; i < 16; i++) {
          buf[offset + i] = bytes[i];
        }
        return buf;
      }
      return (0, _stringify.unsafeStringify)(bytes);
    }
    __name(v6, "v6");
  }
});

// node_modules/uuid/dist/commonjs-browser/v6ToV1.js
var require_v6ToV1 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/v6ToV1.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = v6ToV1;
    var _parse = _interopRequireDefault(require_parse());
    var _stringify = require_stringify();
    function _interopRequireDefault(e) {
      return e && e.__esModule ? e : { default: e };
    }
    __name(_interopRequireDefault, "_interopRequireDefault");
    function v6ToV1(uuid) {
      var v6Bytes = typeof uuid === "string" ? (0, _parse.default)(uuid) : uuid;
      var v1Bytes = _v6ToV1(v6Bytes);
      return typeof uuid === "string" ? (0, _stringify.unsafeStringify)(v1Bytes) : v1Bytes;
    }
    __name(v6ToV1, "v6ToV1");
    function _v6ToV1(v6Bytes) {
      return Uint8Array.of((v6Bytes[3] & 15) << 4 | v6Bytes[4] >> 4 & 15, (v6Bytes[4] & 15) << 4 | (v6Bytes[5] & 240) >> 4, (v6Bytes[5] & 15) << 4 | v6Bytes[6] & 15, v6Bytes[7], (v6Bytes[1] & 15) << 4 | (v6Bytes[2] & 240) >> 4, (v6Bytes[2] & 15) << 4 | (v6Bytes[3] & 240) >> 4, 16 | (v6Bytes[0] & 240) >> 4, (v6Bytes[0] & 15) << 4 | (v6Bytes[1] & 240) >> 4, v6Bytes[8], v6Bytes[9], v6Bytes[10], v6Bytes[11], v6Bytes[12], v6Bytes[13], v6Bytes[14], v6Bytes[15]);
    }
    __name(_v6ToV1, "_v6ToV1");
  }
});

// node_modules/uuid/dist/commonjs-browser/v7.js
var require_v7 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/v7.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _rng = _interopRequireDefault(require_rng());
    var _stringify = require_stringify();
    function _interopRequireDefault(e) {
      return e && e.__esModule ? e : { default: e };
    }
    __name(_interopRequireDefault, "_interopRequireDefault");
    var _seqLow = null;
    var _seqHigh = null;
    var _msecs = 0;
    function v7(options, buf, offset) {
      options = options || {};
      var i = buf && offset || 0;
      var b = buf || new Uint8Array(16);
      var rnds = options.random || (options.rng || _rng.default)();
      var msecs = options.msecs !== void 0 ? options.msecs : Date.now();
      var seq = options.seq !== void 0 ? options.seq : null;
      var seqHigh = _seqHigh;
      var seqLow = _seqLow;
      if (msecs > _msecs && options.msecs === void 0) {
        _msecs = msecs;
        if (seq !== null) {
          seqHigh = null;
          seqLow = null;
        }
      }
      if (seq !== null) {
        if (seq > 2147483647) {
          seq = 2147483647;
        }
        seqHigh = seq >>> 19 & 4095;
        seqLow = seq & 524287;
      }
      if (seqHigh === null || seqLow === null) {
        seqHigh = rnds[6] & 127;
        seqHigh = seqHigh << 8 | rnds[7];
        seqLow = rnds[8] & 63;
        seqLow = seqLow << 8 | rnds[9];
        seqLow = seqLow << 5 | rnds[10] >>> 3;
      }
      if (msecs + 1e4 > _msecs && seq === null) {
        if (++seqLow > 524287) {
          seqLow = 0;
          if (++seqHigh > 4095) {
            seqHigh = 0;
            _msecs++;
          }
        }
      } else {
        _msecs = msecs;
      }
      _seqHigh = seqHigh;
      _seqLow = seqLow;
      b[i++] = _msecs / 1099511627776 & 255;
      b[i++] = _msecs / 4294967296 & 255;
      b[i++] = _msecs / 16777216 & 255;
      b[i++] = _msecs / 65536 & 255;
      b[i++] = _msecs / 256 & 255;
      b[i++] = _msecs & 255;
      b[i++] = seqHigh >>> 4 & 15 | 112;
      b[i++] = seqHigh & 255;
      b[i++] = seqLow >>> 13 & 63 | 128;
      b[i++] = seqLow >>> 5 & 255;
      b[i++] = seqLow << 3 & 255 | rnds[10] & 7;
      b[i++] = rnds[11];
      b[i++] = rnds[12];
      b[i++] = rnds[13];
      b[i++] = rnds[14];
      b[i++] = rnds[15];
      return buf || (0, _stringify.unsafeStringify)(b);
    }
    __name(v7, "v7");
    var _default = exports.default = v7;
  }
});

// node_modules/uuid/dist/commonjs-browser/version.js
var require_version = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/version.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _validate = _interopRequireDefault(require_validate());
    function _interopRequireDefault(e) {
      return e && e.__esModule ? e : { default: e };
    }
    __name(_interopRequireDefault, "_interopRequireDefault");
    function version5(uuid) {
      if (!(0, _validate.default)(uuid)) {
        throw TypeError("Invalid UUID");
      }
      return parseInt(uuid.slice(14, 15), 16);
    }
    __name(version5, "version");
    var _default = exports.default = version5;
  }
});

// node_modules/uuid/dist/commonjs-browser/index.js
var require_commonjs_browser = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    Object.defineProperty(exports, "MAX", {
      enumerable: true,
      get: /* @__PURE__ */ __name(function get() {
        return _max.default;
      }, "get")
    });
    Object.defineProperty(exports, "NIL", {
      enumerable: true,
      get: /* @__PURE__ */ __name(function get() {
        return _nil.default;
      }, "get")
    });
    Object.defineProperty(exports, "parse", {
      enumerable: true,
      get: /* @__PURE__ */ __name(function get() {
        return _parse.default;
      }, "get")
    });
    Object.defineProperty(exports, "stringify", {
      enumerable: true,
      get: /* @__PURE__ */ __name(function get() {
        return _stringify.default;
      }, "get")
    });
    Object.defineProperty(exports, "v1", {
      enumerable: true,
      get: /* @__PURE__ */ __name(function get() {
        return _v.default;
      }, "get")
    });
    Object.defineProperty(exports, "v1ToV6", {
      enumerable: true,
      get: /* @__PURE__ */ __name(function get() {
        return _v1ToV.default;
      }, "get")
    });
    Object.defineProperty(exports, "v3", {
      enumerable: true,
      get: /* @__PURE__ */ __name(function get() {
        return _v2.default;
      }, "get")
    });
    Object.defineProperty(exports, "v4", {
      enumerable: true,
      get: /* @__PURE__ */ __name(function get() {
        return _v3.default;
      }, "get")
    });
    Object.defineProperty(exports, "v5", {
      enumerable: true,
      get: /* @__PURE__ */ __name(function get() {
        return _v4.default;
      }, "get")
    });
    Object.defineProperty(exports, "v6", {
      enumerable: true,
      get: /* @__PURE__ */ __name(function get() {
        return _v5.default;
      }, "get")
    });
    Object.defineProperty(exports, "v6ToV1", {
      enumerable: true,
      get: /* @__PURE__ */ __name(function get() {
        return _v6ToV.default;
      }, "get")
    });
    Object.defineProperty(exports, "v7", {
      enumerable: true,
      get: /* @__PURE__ */ __name(function get() {
        return _v6.default;
      }, "get")
    });
    Object.defineProperty(exports, "validate", {
      enumerable: true,
      get: /* @__PURE__ */ __name(function get() {
        return _validate.default;
      }, "get")
    });
    Object.defineProperty(exports, "version", {
      enumerable: true,
      get: /* @__PURE__ */ __name(function get() {
        return _version.default;
      }, "get")
    });
    var _max = _interopRequireDefault(require_max());
    var _nil = _interopRequireDefault(require_nil());
    var _parse = _interopRequireDefault(require_parse());
    var _stringify = _interopRequireDefault(require_stringify());
    var _v = _interopRequireDefault(require_v1());
    var _v1ToV = _interopRequireDefault(require_v1ToV6());
    var _v2 = _interopRequireDefault(require_v3());
    var _v3 = _interopRequireDefault(require_v4());
    var _v4 = _interopRequireDefault(require_v5());
    var _v5 = _interopRequireDefault(require_v6());
    var _v6ToV = _interopRequireDefault(require_v6ToV1());
    var _v6 = _interopRequireDefault(require_v7());
    var _validate = _interopRequireDefault(require_validate());
    var _version = _interopRequireDefault(require_version());
    function _interopRequireDefault(e) {
      return e && e.__esModule ? e : { default: e };
    }
    __name(_interopRequireDefault, "_interopRequireDefault");
  }
});

// node_modules/svix/dist/request.js
var require_request = __commonJS({
  "node_modules/svix/dist/request.js"(exports) {
    "use strict";
    var __awaiter = exports && exports.__awaiter || function(thisArg, _arguments, P, generator) {
      function adopt(value) {
        return value instanceof P ? value : new P(function(resolve) {
          resolve(value);
        });
      }
      __name(adopt, "adopt");
      return new (P || (P = Promise))(function(resolve, reject) {
        function fulfilled(value) {
          try {
            step(generator.next(value));
          } catch (e) {
            reject(e);
          }
        }
        __name(fulfilled, "fulfilled");
        function rejected(value) {
          try {
            step(generator["throw"](value));
          } catch (e) {
            reject(e);
          }
        }
        __name(rejected, "rejected");
        function step(result) {
          result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
        }
        __name(step, "step");
        step((generator = generator.apply(thisArg, _arguments || [])).next());
      });
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SvixRequest = exports.HttpMethod = exports.LIB_VERSION = void 0;
    var util_1 = require_util();
    var uuid_1 = require_commonjs_browser();
    exports.LIB_VERSION = "1.84.1";
    var USER_AGENT = `svix-libs/${exports.LIB_VERSION}/javascript`;
    var HttpMethod;
    (function(HttpMethod2) {
      HttpMethod2["GET"] = "GET";
      HttpMethod2["HEAD"] = "HEAD";
      HttpMethod2["POST"] = "POST";
      HttpMethod2["PUT"] = "PUT";
      HttpMethod2["DELETE"] = "DELETE";
      HttpMethod2["CONNECT"] = "CONNECT";
      HttpMethod2["OPTIONS"] = "OPTIONS";
      HttpMethod2["TRACE"] = "TRACE";
      HttpMethod2["PATCH"] = "PATCH";
    })(HttpMethod = exports.HttpMethod || (exports.HttpMethod = {}));
    var SvixRequest = class {
      static {
        __name(this, "SvixRequest");
      }
      constructor(method, path) {
        this.method = method;
        this.path = path;
        this.queryParams = {};
        this.headerParams = {};
      }
      setPathParam(name, value) {
        const newPath = this.path.replace(`{${name}}`, encodeURIComponent(value));
        if (this.path === newPath) {
          throw new Error(`path parameter ${name} not found`);
        }
        this.path = newPath;
      }
      setQueryParams(params) {
        for (const [name, value] of Object.entries(params)) {
          this.setQueryParam(name, value);
        }
      }
      setQueryParam(name, value) {
        if (value === void 0 || value === null) {
          return;
        }
        if (typeof value === "string") {
          this.queryParams[name] = value;
        } else if (typeof value === "boolean" || typeof value === "number") {
          this.queryParams[name] = value.toString();
        } else if (value instanceof Date) {
          this.queryParams[name] = value.toISOString();
        } else if (Array.isArray(value)) {
          if (value.length > 0) {
            this.queryParams[name] = value.join(",");
          }
        } else {
          const _assert_unreachable = value;
          throw new Error(`query parameter ${name} has unsupported type`);
        }
      }
      setHeaderParam(name, value) {
        if (value === void 0) {
          return;
        }
        this.headerParams[name] = value;
      }
      setBody(value) {
        this.body = JSON.stringify(value);
      }
      send(ctx, parseResponseBody) {
        return __awaiter(this, void 0, void 0, function* () {
          const response = yield this.sendInner(ctx);
          if (response.status === 204) {
            return null;
          }
          const responseBody = yield response.text();
          return parseResponseBody(JSON.parse(responseBody));
        });
      }
      sendNoResponseBody(ctx) {
        return __awaiter(this, void 0, void 0, function* () {
          yield this.sendInner(ctx);
        });
      }
      sendInner(ctx) {
        var _a, _b;
        return __awaiter(this, void 0, void 0, function* () {
          const url = new URL(ctx.baseUrl + this.path);
          for (const [name, value] of Object.entries(this.queryParams)) {
            url.searchParams.set(name, value);
          }
          if (this.headerParams["idempotency-key"] === void 0 && this.method.toUpperCase() === "POST") {
            this.headerParams["idempotency-key"] = `auto_${(0, uuid_1.v4)()}`;
          }
          const randomId = Math.floor(Math.random() * Number.MAX_SAFE_INTEGER);
          if (this.body != null) {
            this.headerParams["content-type"] = "application/json";
          }
          const isCredentialsSupported = "credentials" in Request.prototype;
          const response = yield sendWithRetry(url, {
            method: this.method.toString(),
            body: this.body,
            headers: Object.assign({ accept: "application/json, */*;q=0.8", authorization: `Bearer ${ctx.token}`, "user-agent": USER_AGENT, "svix-req-id": randomId.toString() }, this.headerParams),
            credentials: isCredentialsSupported ? "same-origin" : void 0,
            signal: ctx.timeout !== void 0 ? AbortSignal.timeout(ctx.timeout) : void 0
          }, ctx.retryScheduleInMs, (_a = ctx.retryScheduleInMs) === null || _a === void 0 ? void 0 : _a[0], ((_b = ctx.retryScheduleInMs) === null || _b === void 0 ? void 0 : _b.length) || ctx.numRetries, ctx.fetch);
          return filterResponseForErrors(response);
        });
      }
    };
    exports.SvixRequest = SvixRequest;
    function filterResponseForErrors(response) {
      return __awaiter(this, void 0, void 0, function* () {
        if (response.status < 300) {
          return response;
        }
        const responseBody = yield response.text();
        if (response.status === 422) {
          throw new util_1.ApiException(response.status, JSON.parse(responseBody), response.headers);
        }
        if (response.status >= 400 && response.status <= 499) {
          throw new util_1.ApiException(response.status, JSON.parse(responseBody), response.headers);
        }
        throw new util_1.ApiException(response.status, responseBody, response.headers);
      });
    }
    __name(filterResponseForErrors, "filterResponseForErrors");
    function sendWithRetry(url, init, retryScheduleInMs, nextInterval = 50, triesLeft = 2, fetchImpl = fetch, retryCount = 1) {
      return __awaiter(this, void 0, void 0, function* () {
        const sleep = /* @__PURE__ */ __name((interval) => new Promise((resolve) => setTimeout(resolve, interval)), "sleep");
        try {
          const response = yield fetchImpl(url, init);
          if (triesLeft <= 0 || response.status < 500) {
            return response;
          }
        } catch (e) {
          if (triesLeft <= 0) {
            throw e;
          }
        }
        yield sleep(nextInterval);
        init.headers["svix-retry-count"] = retryCount.toString();
        nextInterval = (retryScheduleInMs === null || retryScheduleInMs === void 0 ? void 0 : retryScheduleInMs[retryCount]) || nextInterval * 2;
        return yield sendWithRetry(url, init, retryScheduleInMs, nextInterval, --triesLeft, fetchImpl, ++retryCount);
      });
    }
    __name(sendWithRetry, "sendWithRetry");
  }
});

// node_modules/svix/dist/api/application.js
var require_application = __commonJS({
  "node_modules/svix/dist/api/application.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Application = void 0;
    var applicationIn_1 = require_applicationIn();
    var applicationOut_1 = require_applicationOut();
    var applicationPatch_1 = require_applicationPatch();
    var listResponseApplicationOut_1 = require_listResponseApplicationOut();
    var request_1 = require_request();
    var Application = class {
      static {
        __name(this, "Application");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      list(options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app");
        request.setQueryParams({
          exclude_apps_with_no_endpoints: options === null || options === void 0 ? void 0 : options.excludeAppsWithNoEndpoints,
          exclude_apps_with_disabled_endpoints: options === null || options === void 0 ? void 0 : options.excludeAppsWithDisabledEndpoints,
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          order: options === null || options === void 0 ? void 0 : options.order
        });
        return request.send(this.requestCtx, listResponseApplicationOut_1.ListResponseApplicationOutSerializer._fromJsonObject);
      }
      create(applicationIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/app");
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(applicationIn_1.ApplicationInSerializer._toJsonObject(applicationIn));
        return request.send(this.requestCtx, applicationOut_1.ApplicationOutSerializer._fromJsonObject);
      }
      getOrCreate(applicationIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/app");
        request.setQueryParam("get_if_exists", true);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(applicationIn_1.ApplicationInSerializer._toJsonObject(applicationIn));
        return request.send(this.requestCtx, applicationOut_1.ApplicationOutSerializer._fromJsonObject);
      }
      get(appId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}");
        request.setPathParam("app_id", appId);
        return request.send(this.requestCtx, applicationOut_1.ApplicationOutSerializer._fromJsonObject);
      }
      update(appId, applicationIn) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PUT, "/api/v1/app/{app_id}");
        request.setPathParam("app_id", appId);
        request.setBody(applicationIn_1.ApplicationInSerializer._toJsonObject(applicationIn));
        return request.send(this.requestCtx, applicationOut_1.ApplicationOutSerializer._fromJsonObject);
      }
      delete(appId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.DELETE, "/api/v1/app/{app_id}");
        request.setPathParam("app_id", appId);
        return request.sendNoResponseBody(this.requestCtx);
      }
      patch(appId, applicationPatch) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PATCH, "/api/v1/app/{app_id}");
        request.setPathParam("app_id", appId);
        request.setBody(applicationPatch_1.ApplicationPatchSerializer._toJsonObject(applicationPatch));
        return request.send(this.requestCtx, applicationOut_1.ApplicationOutSerializer._fromJsonObject);
      }
    };
    exports.Application = Application;
  }
});

// node_modules/svix/dist/models/apiTokenOut.js
var require_apiTokenOut = __commonJS({
  "node_modules/svix/dist/models/apiTokenOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ApiTokenOutSerializer = void 0;
    exports.ApiTokenOutSerializer = {
      _fromJsonObject(object2) {
        return {
          createdAt: new Date(object2["createdAt"]),
          expiresAt: object2["expiresAt"] ? new Date(object2["expiresAt"]) : null,
          id: object2["id"],
          name: object2["name"],
          scopes: object2["scopes"],
          token: object2["token"]
        };
      },
      _toJsonObject(self2) {
        return {
          createdAt: self2.createdAt,
          expiresAt: self2.expiresAt,
          id: self2.id,
          name: self2.name,
          scopes: self2.scopes,
          token: self2.token
        };
      }
    };
  }
});

// node_modules/svix/dist/models/appPortalCapability.js
var require_appPortalCapability = __commonJS({
  "node_modules/svix/dist/models/appPortalCapability.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AppPortalCapabilitySerializer = exports.AppPortalCapability = void 0;
    var AppPortalCapability;
    (function(AppPortalCapability2) {
      AppPortalCapability2["ViewBase"] = "ViewBase";
      AppPortalCapability2["ViewEndpointSecret"] = "ViewEndpointSecret";
      AppPortalCapability2["ManageEndpointSecret"] = "ManageEndpointSecret";
      AppPortalCapability2["ManageTransformations"] = "ManageTransformations";
      AppPortalCapability2["CreateAttempts"] = "CreateAttempts";
      AppPortalCapability2["ManageEndpoint"] = "ManageEndpoint";
    })(AppPortalCapability = exports.AppPortalCapability || (exports.AppPortalCapability = {}));
    exports.AppPortalCapabilitySerializer = {
      _fromJsonObject(object2) {
        return object2;
      },
      _toJsonObject(self2) {
        return self2;
      }
    };
  }
});

// node_modules/svix/dist/models/appPortalAccessIn.js
var require_appPortalAccessIn = __commonJS({
  "node_modules/svix/dist/models/appPortalAccessIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AppPortalAccessInSerializer = void 0;
    var appPortalCapability_1 = require_appPortalCapability();
    var applicationIn_1 = require_applicationIn();
    exports.AppPortalAccessInSerializer = {
      _fromJsonObject(object2) {
        var _a;
        return {
          application: object2["application"] ? applicationIn_1.ApplicationInSerializer._fromJsonObject(object2["application"]) : void 0,
          capabilities: (_a = object2["capabilities"]) === null || _a === void 0 ? void 0 : _a.map((item) => appPortalCapability_1.AppPortalCapabilitySerializer._fromJsonObject(item)),
          expiry: object2["expiry"],
          featureFlags: object2["featureFlags"],
          readOnly: object2["readOnly"],
          sessionId: object2["sessionId"]
        };
      },
      _toJsonObject(self2) {
        var _a;
        return {
          application: self2.application ? applicationIn_1.ApplicationInSerializer._toJsonObject(self2.application) : void 0,
          capabilities: (_a = self2.capabilities) === null || _a === void 0 ? void 0 : _a.map((item) => appPortalCapability_1.AppPortalCapabilitySerializer._toJsonObject(item)),
          expiry: self2.expiry,
          featureFlags: self2.featureFlags,
          readOnly: self2.readOnly,
          sessionId: self2.sessionId
        };
      }
    };
  }
});

// node_modules/svix/dist/models/appPortalAccessOut.js
var require_appPortalAccessOut = __commonJS({
  "node_modules/svix/dist/models/appPortalAccessOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AppPortalAccessOutSerializer = void 0;
    exports.AppPortalAccessOutSerializer = {
      _fromJsonObject(object2) {
        return {
          token: object2["token"],
          url: object2["url"]
        };
      },
      _toJsonObject(self2) {
        return {
          token: self2.token,
          url: self2.url
        };
      }
    };
  }
});

// node_modules/svix/dist/models/applicationTokenExpireIn.js
var require_applicationTokenExpireIn = __commonJS({
  "node_modules/svix/dist/models/applicationTokenExpireIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ApplicationTokenExpireInSerializer = void 0;
    exports.ApplicationTokenExpireInSerializer = {
      _fromJsonObject(object2) {
        return {
          expiry: object2["expiry"],
          sessionIds: object2["sessionIds"]
        };
      },
      _toJsonObject(self2) {
        return {
          expiry: self2.expiry,
          sessionIds: self2.sessionIds
        };
      }
    };
  }
});

// node_modules/svix/dist/models/rotatePollerTokenIn.js
var require_rotatePollerTokenIn = __commonJS({
  "node_modules/svix/dist/models/rotatePollerTokenIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.RotatePollerTokenInSerializer = void 0;
    exports.RotatePollerTokenInSerializer = {
      _fromJsonObject(object2) {
        return {
          expiry: object2["expiry"],
          oldTokenExpiry: object2["oldTokenExpiry"]
        };
      },
      _toJsonObject(self2) {
        return {
          expiry: self2.expiry,
          oldTokenExpiry: self2.oldTokenExpiry
        };
      }
    };
  }
});

// node_modules/svix/dist/models/streamPortalAccessIn.js
var require_streamPortalAccessIn = __commonJS({
  "node_modules/svix/dist/models/streamPortalAccessIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StreamPortalAccessInSerializer = void 0;
    exports.StreamPortalAccessInSerializer = {
      _fromJsonObject(object2) {
        return {
          expiry: object2["expiry"],
          featureFlags: object2["featureFlags"],
          sessionId: object2["sessionId"]
        };
      },
      _toJsonObject(self2) {
        return {
          expiry: self2.expiry,
          featureFlags: self2.featureFlags,
          sessionId: self2.sessionId
        };
      }
    };
  }
});

// node_modules/svix/dist/models/dashboardAccessOut.js
var require_dashboardAccessOut = __commonJS({
  "node_modules/svix/dist/models/dashboardAccessOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.DashboardAccessOutSerializer = void 0;
    exports.DashboardAccessOutSerializer = {
      _fromJsonObject(object2) {
        return {
          token: object2["token"],
          url: object2["url"]
        };
      },
      _toJsonObject(self2) {
        return {
          token: self2.token,
          url: self2.url
        };
      }
    };
  }
});

// node_modules/svix/dist/api/authentication.js
var require_authentication = __commonJS({
  "node_modules/svix/dist/api/authentication.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Authentication = void 0;
    var apiTokenOut_1 = require_apiTokenOut();
    var appPortalAccessIn_1 = require_appPortalAccessIn();
    var appPortalAccessOut_1 = require_appPortalAccessOut();
    var applicationTokenExpireIn_1 = require_applicationTokenExpireIn();
    var rotatePollerTokenIn_1 = require_rotatePollerTokenIn();
    var streamPortalAccessIn_1 = require_streamPortalAccessIn();
    var dashboardAccessOut_1 = require_dashboardAccessOut();
    var request_1 = require_request();
    var Authentication = class {
      static {
        __name(this, "Authentication");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      appPortalAccess(appId, appPortalAccessIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/auth/app-portal-access/{app_id}");
        request.setPathParam("app_id", appId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(appPortalAccessIn_1.AppPortalAccessInSerializer._toJsonObject(appPortalAccessIn));
        return request.send(this.requestCtx, appPortalAccessOut_1.AppPortalAccessOutSerializer._fromJsonObject);
      }
      expireAll(appId, applicationTokenExpireIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/auth/app/{app_id}/expire-all");
        request.setPathParam("app_id", appId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(applicationTokenExpireIn_1.ApplicationTokenExpireInSerializer._toJsonObject(applicationTokenExpireIn));
        return request.sendNoResponseBody(this.requestCtx);
      }
      dashboardAccess(appId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/auth/dashboard-access/{app_id}");
        request.setPathParam("app_id", appId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        return request.send(this.requestCtx, dashboardAccessOut_1.DashboardAccessOutSerializer._fromJsonObject);
      }
      logout(options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/auth/logout");
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        return request.sendNoResponseBody(this.requestCtx);
      }
      streamPortalAccess(streamId, streamPortalAccessIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/auth/stream-portal-access/{stream_id}");
        request.setPathParam("stream_id", streamId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(streamPortalAccessIn_1.StreamPortalAccessInSerializer._toJsonObject(streamPortalAccessIn));
        return request.send(this.requestCtx, appPortalAccessOut_1.AppPortalAccessOutSerializer._fromJsonObject);
      }
      getStreamPollerToken(streamId, sinkId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/auth/stream/{stream_id}/sink/{sink_id}/poller/token");
        request.setPathParam("stream_id", streamId);
        request.setPathParam("sink_id", sinkId);
        return request.send(this.requestCtx, apiTokenOut_1.ApiTokenOutSerializer._fromJsonObject);
      }
      rotateStreamPollerToken(streamId, sinkId, rotatePollerTokenIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/auth/stream/{stream_id}/sink/{sink_id}/poller/token/rotate");
        request.setPathParam("stream_id", streamId);
        request.setPathParam("sink_id", sinkId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(rotatePollerTokenIn_1.RotatePollerTokenInSerializer._toJsonObject(rotatePollerTokenIn));
        return request.send(this.requestCtx, apiTokenOut_1.ApiTokenOutSerializer._fromJsonObject);
      }
    };
    exports.Authentication = Authentication;
  }
});

// node_modules/svix/dist/models/backgroundTaskStatus.js
var require_backgroundTaskStatus = __commonJS({
  "node_modules/svix/dist/models/backgroundTaskStatus.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BackgroundTaskStatusSerializer = exports.BackgroundTaskStatus = void 0;
    var BackgroundTaskStatus;
    (function(BackgroundTaskStatus2) {
      BackgroundTaskStatus2["Running"] = "running";
      BackgroundTaskStatus2["Finished"] = "finished";
      BackgroundTaskStatus2["Failed"] = "failed";
    })(BackgroundTaskStatus = exports.BackgroundTaskStatus || (exports.BackgroundTaskStatus = {}));
    exports.BackgroundTaskStatusSerializer = {
      _fromJsonObject(object2) {
        return object2;
      },
      _toJsonObject(self2) {
        return self2;
      }
    };
  }
});

// node_modules/svix/dist/models/backgroundTaskType.js
var require_backgroundTaskType = __commonJS({
  "node_modules/svix/dist/models/backgroundTaskType.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BackgroundTaskTypeSerializer = exports.BackgroundTaskType = void 0;
    var BackgroundTaskType;
    (function(BackgroundTaskType2) {
      BackgroundTaskType2["EndpointReplay"] = "endpoint.replay";
      BackgroundTaskType2["EndpointRecover"] = "endpoint.recover";
      BackgroundTaskType2["ApplicationStats"] = "application.stats";
      BackgroundTaskType2["MessageBroadcast"] = "message.broadcast";
      BackgroundTaskType2["SdkGenerate"] = "sdk.generate";
      BackgroundTaskType2["EventTypeAggregate"] = "event-type.aggregate";
      BackgroundTaskType2["ApplicationPurgeContent"] = "application.purge_content";
      BackgroundTaskType2["EndpointBulkReplay"] = "endpoint.bulk_replay";
    })(BackgroundTaskType = exports.BackgroundTaskType || (exports.BackgroundTaskType = {}));
    exports.BackgroundTaskTypeSerializer = {
      _fromJsonObject(object2) {
        return object2;
      },
      _toJsonObject(self2) {
        return self2;
      }
    };
  }
});

// node_modules/svix/dist/models/backgroundTaskOut.js
var require_backgroundTaskOut = __commonJS({
  "node_modules/svix/dist/models/backgroundTaskOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BackgroundTaskOutSerializer = void 0;
    var backgroundTaskStatus_1 = require_backgroundTaskStatus();
    var backgroundTaskType_1 = require_backgroundTaskType();
    exports.BackgroundTaskOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"],
          id: object2["id"],
          status: backgroundTaskStatus_1.BackgroundTaskStatusSerializer._fromJsonObject(object2["status"]),
          task: backgroundTaskType_1.BackgroundTaskTypeSerializer._fromJsonObject(object2["task"])
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data,
          id: self2.id,
          status: backgroundTaskStatus_1.BackgroundTaskStatusSerializer._toJsonObject(self2.status),
          task: backgroundTaskType_1.BackgroundTaskTypeSerializer._toJsonObject(self2.task)
        };
      }
    };
  }
});

// node_modules/svix/dist/models/listResponseBackgroundTaskOut.js
var require_listResponseBackgroundTaskOut = __commonJS({
  "node_modules/svix/dist/models/listResponseBackgroundTaskOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ListResponseBackgroundTaskOutSerializer = void 0;
    var backgroundTaskOut_1 = require_backgroundTaskOut();
    exports.ListResponseBackgroundTaskOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => backgroundTaskOut_1.BackgroundTaskOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"],
          prevIterator: object2["prevIterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => backgroundTaskOut_1.BackgroundTaskOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator,
          prevIterator: self2.prevIterator
        };
      }
    };
  }
});

// node_modules/svix/dist/api/backgroundTask.js
var require_backgroundTask = __commonJS({
  "node_modules/svix/dist/api/backgroundTask.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BackgroundTask = void 0;
    var backgroundTaskOut_1 = require_backgroundTaskOut();
    var listResponseBackgroundTaskOut_1 = require_listResponseBackgroundTaskOut();
    var request_1 = require_request();
    var BackgroundTask = class {
      static {
        __name(this, "BackgroundTask");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      list(options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/background-task");
        request.setQueryParams({
          status: options === null || options === void 0 ? void 0 : options.status,
          task: options === null || options === void 0 ? void 0 : options.task,
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          order: options === null || options === void 0 ? void 0 : options.order
        });
        return request.send(this.requestCtx, listResponseBackgroundTaskOut_1.ListResponseBackgroundTaskOutSerializer._fromJsonObject);
      }
      listByEndpoint(options) {
        return this.list(options);
      }
      get(taskId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/background-task/{task_id}");
        request.setPathParam("task_id", taskId);
        return request.send(this.requestCtx, backgroundTaskOut_1.BackgroundTaskOutSerializer._fromJsonObject);
      }
    };
    exports.BackgroundTask = BackgroundTask;
  }
});

// node_modules/svix/dist/models/connectorKind.js
var require_connectorKind = __commonJS({
  "node_modules/svix/dist/models/connectorKind.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ConnectorKindSerializer = exports.ConnectorKind = void 0;
    var ConnectorKind;
    (function(ConnectorKind2) {
      ConnectorKind2["Custom"] = "Custom";
      ConnectorKind2["AgenticCommerceProtocol"] = "AgenticCommerceProtocol";
      ConnectorKind2["CloseCrm"] = "CloseCRM";
      ConnectorKind2["CustomerIo"] = "CustomerIO";
      ConnectorKind2["Discord"] = "Discord";
      ConnectorKind2["Hubspot"] = "Hubspot";
      ConnectorKind2["Inngest"] = "Inngest";
      ConnectorKind2["Loops"] = "Loops";
      ConnectorKind2["Otel"] = "Otel";
      ConnectorKind2["Resend"] = "Resend";
      ConnectorKind2["Salesforce"] = "Salesforce";
      ConnectorKind2["Segment"] = "Segment";
      ConnectorKind2["Sendgrid"] = "Sendgrid";
      ConnectorKind2["Slack"] = "Slack";
      ConnectorKind2["Teams"] = "Teams";
      ConnectorKind2["TriggerDev"] = "TriggerDev";
      ConnectorKind2["Windmill"] = "Windmill";
      ConnectorKind2["Zapier"] = "Zapier";
    })(ConnectorKind = exports.ConnectorKind || (exports.ConnectorKind = {}));
    exports.ConnectorKindSerializer = {
      _fromJsonObject(object2) {
        return object2;
      },
      _toJsonObject(self2) {
        return self2;
      }
    };
  }
});

// node_modules/svix/dist/models/connectorProduct.js
var require_connectorProduct = __commonJS({
  "node_modules/svix/dist/models/connectorProduct.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ConnectorProductSerializer = exports.ConnectorProduct = void 0;
    var ConnectorProduct;
    (function(ConnectorProduct2) {
      ConnectorProduct2["Dispatch"] = "Dispatch";
      ConnectorProduct2["Stream"] = "Stream";
    })(ConnectorProduct = exports.ConnectorProduct || (exports.ConnectorProduct = {}));
    exports.ConnectorProductSerializer = {
      _fromJsonObject(object2) {
        return object2;
      },
      _toJsonObject(self2) {
        return self2;
      }
    };
  }
});

// node_modules/svix/dist/models/connectorIn.js
var require_connectorIn = __commonJS({
  "node_modules/svix/dist/models/connectorIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ConnectorInSerializer = void 0;
    var connectorKind_1 = require_connectorKind();
    var connectorProduct_1 = require_connectorProduct();
    exports.ConnectorInSerializer = {
      _fromJsonObject(object2) {
        return {
          allowedEventTypes: object2["allowedEventTypes"],
          description: object2["description"],
          featureFlags: object2["featureFlags"],
          instructions: object2["instructions"],
          kind: object2["kind"] ? connectorKind_1.ConnectorKindSerializer._fromJsonObject(object2["kind"]) : void 0,
          logo: object2["logo"],
          name: object2["name"],
          productType: object2["productType"] ? connectorProduct_1.ConnectorProductSerializer._fromJsonObject(object2["productType"]) : void 0,
          transformation: object2["transformation"],
          uid: object2["uid"]
        };
      },
      _toJsonObject(self2) {
        return {
          allowedEventTypes: self2.allowedEventTypes,
          description: self2.description,
          featureFlags: self2.featureFlags,
          instructions: self2.instructions,
          kind: self2.kind ? connectorKind_1.ConnectorKindSerializer._toJsonObject(self2.kind) : void 0,
          logo: self2.logo,
          name: self2.name,
          productType: self2.productType ? connectorProduct_1.ConnectorProductSerializer._toJsonObject(self2.productType) : void 0,
          transformation: self2.transformation,
          uid: self2.uid
        };
      }
    };
  }
});

// node_modules/svix/dist/models/connectorOut.js
var require_connectorOut = __commonJS({
  "node_modules/svix/dist/models/connectorOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ConnectorOutSerializer = void 0;
    var connectorKind_1 = require_connectorKind();
    var connectorProduct_1 = require_connectorProduct();
    exports.ConnectorOutSerializer = {
      _fromJsonObject(object2) {
        return {
          allowedEventTypes: object2["allowedEventTypes"],
          createdAt: new Date(object2["createdAt"]),
          description: object2["description"],
          featureFlags: object2["featureFlags"],
          id: object2["id"],
          instructions: object2["instructions"],
          kind: connectorKind_1.ConnectorKindSerializer._fromJsonObject(object2["kind"]),
          logo: object2["logo"],
          name: object2["name"],
          orgId: object2["orgId"],
          productType: connectorProduct_1.ConnectorProductSerializer._fromJsonObject(object2["productType"]),
          transformation: object2["transformation"],
          transformationUpdatedAt: new Date(object2["transformationUpdatedAt"]),
          uid: object2["uid"],
          updatedAt: new Date(object2["updatedAt"])
        };
      },
      _toJsonObject(self2) {
        return {
          allowedEventTypes: self2.allowedEventTypes,
          createdAt: self2.createdAt,
          description: self2.description,
          featureFlags: self2.featureFlags,
          id: self2.id,
          instructions: self2.instructions,
          kind: connectorKind_1.ConnectorKindSerializer._toJsonObject(self2.kind),
          logo: self2.logo,
          name: self2.name,
          orgId: self2.orgId,
          productType: connectorProduct_1.ConnectorProductSerializer._toJsonObject(self2.productType),
          transformation: self2.transformation,
          transformationUpdatedAt: self2.transformationUpdatedAt,
          uid: self2.uid,
          updatedAt: self2.updatedAt
        };
      }
    };
  }
});

// node_modules/svix/dist/models/connectorPatch.js
var require_connectorPatch = __commonJS({
  "node_modules/svix/dist/models/connectorPatch.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ConnectorPatchSerializer = void 0;
    var connectorKind_1 = require_connectorKind();
    exports.ConnectorPatchSerializer = {
      _fromJsonObject(object2) {
        return {
          allowedEventTypes: object2["allowedEventTypes"],
          description: object2["description"],
          featureFlags: object2["featureFlags"],
          instructions: object2["instructions"],
          kind: object2["kind"] ? connectorKind_1.ConnectorKindSerializer._fromJsonObject(object2["kind"]) : void 0,
          logo: object2["logo"],
          name: object2["name"],
          transformation: object2["transformation"]
        };
      },
      _toJsonObject(self2) {
        return {
          allowedEventTypes: self2.allowedEventTypes,
          description: self2.description,
          featureFlags: self2.featureFlags,
          instructions: self2.instructions,
          kind: self2.kind ? connectorKind_1.ConnectorKindSerializer._toJsonObject(self2.kind) : void 0,
          logo: self2.logo,
          name: self2.name,
          transformation: self2.transformation
        };
      }
    };
  }
});

// node_modules/svix/dist/models/connectorUpdate.js
var require_connectorUpdate = __commonJS({
  "node_modules/svix/dist/models/connectorUpdate.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ConnectorUpdateSerializer = void 0;
    var connectorKind_1 = require_connectorKind();
    exports.ConnectorUpdateSerializer = {
      _fromJsonObject(object2) {
        return {
          allowedEventTypes: object2["allowedEventTypes"],
          description: object2["description"],
          featureFlags: object2["featureFlags"],
          instructions: object2["instructions"],
          kind: object2["kind"] ? connectorKind_1.ConnectorKindSerializer._fromJsonObject(object2["kind"]) : void 0,
          logo: object2["logo"],
          name: object2["name"],
          transformation: object2["transformation"]
        };
      },
      _toJsonObject(self2) {
        return {
          allowedEventTypes: self2.allowedEventTypes,
          description: self2.description,
          featureFlags: self2.featureFlags,
          instructions: self2.instructions,
          kind: self2.kind ? connectorKind_1.ConnectorKindSerializer._toJsonObject(self2.kind) : void 0,
          logo: self2.logo,
          name: self2.name,
          transformation: self2.transformation
        };
      }
    };
  }
});

// node_modules/svix/dist/models/listResponseConnectorOut.js
var require_listResponseConnectorOut = __commonJS({
  "node_modules/svix/dist/models/listResponseConnectorOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ListResponseConnectorOutSerializer = void 0;
    var connectorOut_1 = require_connectorOut();
    exports.ListResponseConnectorOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => connectorOut_1.ConnectorOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"],
          prevIterator: object2["prevIterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => connectorOut_1.ConnectorOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator,
          prevIterator: self2.prevIterator
        };
      }
    };
  }
});

// node_modules/svix/dist/api/connector.js
var require_connector = __commonJS({
  "node_modules/svix/dist/api/connector.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Connector = void 0;
    var connectorIn_1 = require_connectorIn();
    var connectorOut_1 = require_connectorOut();
    var connectorPatch_1 = require_connectorPatch();
    var connectorUpdate_1 = require_connectorUpdate();
    var listResponseConnectorOut_1 = require_listResponseConnectorOut();
    var request_1 = require_request();
    var Connector = class {
      static {
        __name(this, "Connector");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      list(options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/connector");
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          order: options === null || options === void 0 ? void 0 : options.order,
          product_type: options === null || options === void 0 ? void 0 : options.productType
        });
        return request.send(this.requestCtx, listResponseConnectorOut_1.ListResponseConnectorOutSerializer._fromJsonObject);
      }
      create(connectorIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/connector");
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(connectorIn_1.ConnectorInSerializer._toJsonObject(connectorIn));
        return request.send(this.requestCtx, connectorOut_1.ConnectorOutSerializer._fromJsonObject);
      }
      get(connectorId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/connector/{connector_id}");
        request.setPathParam("connector_id", connectorId);
        return request.send(this.requestCtx, connectorOut_1.ConnectorOutSerializer._fromJsonObject);
      }
      update(connectorId, connectorUpdate) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PUT, "/api/v1/connector/{connector_id}");
        request.setPathParam("connector_id", connectorId);
        request.setBody(connectorUpdate_1.ConnectorUpdateSerializer._toJsonObject(connectorUpdate));
        return request.send(this.requestCtx, connectorOut_1.ConnectorOutSerializer._fromJsonObject);
      }
      delete(connectorId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.DELETE, "/api/v1/connector/{connector_id}");
        request.setPathParam("connector_id", connectorId);
        return request.sendNoResponseBody(this.requestCtx);
      }
      patch(connectorId, connectorPatch) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PATCH, "/api/v1/connector/{connector_id}");
        request.setPathParam("connector_id", connectorId);
        request.setBody(connectorPatch_1.ConnectorPatchSerializer._toJsonObject(connectorPatch));
        return request.send(this.requestCtx, connectorOut_1.ConnectorOutSerializer._fromJsonObject);
      }
    };
    exports.Connector = Connector;
  }
});

// node_modules/svix/dist/models/endpointHeadersIn.js
var require_endpointHeadersIn = __commonJS({
  "node_modules/svix/dist/models/endpointHeadersIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EndpointHeadersInSerializer = void 0;
    exports.EndpointHeadersInSerializer = {
      _fromJsonObject(object2) {
        return {
          headers: object2["headers"]
        };
      },
      _toJsonObject(self2) {
        return {
          headers: self2.headers
        };
      }
    };
  }
});

// node_modules/svix/dist/models/endpointHeadersOut.js
var require_endpointHeadersOut = __commonJS({
  "node_modules/svix/dist/models/endpointHeadersOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EndpointHeadersOutSerializer = void 0;
    exports.EndpointHeadersOutSerializer = {
      _fromJsonObject(object2) {
        return {
          headers: object2["headers"],
          sensitive: object2["sensitive"]
        };
      },
      _toJsonObject(self2) {
        return {
          headers: self2.headers,
          sensitive: self2.sensitive
        };
      }
    };
  }
});

// node_modules/svix/dist/models/endpointHeadersPatchIn.js
var require_endpointHeadersPatchIn = __commonJS({
  "node_modules/svix/dist/models/endpointHeadersPatchIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EndpointHeadersPatchInSerializer = void 0;
    exports.EndpointHeadersPatchInSerializer = {
      _fromJsonObject(object2) {
        return {
          deleteHeaders: object2["deleteHeaders"],
          headers: object2["headers"]
        };
      },
      _toJsonObject(self2) {
        return {
          deleteHeaders: self2.deleteHeaders,
          headers: self2.headers
        };
      }
    };
  }
});

// node_modules/svix/dist/models/endpointIn.js
var require_endpointIn = __commonJS({
  "node_modules/svix/dist/models/endpointIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EndpointInSerializer = void 0;
    exports.EndpointInSerializer = {
      _fromJsonObject(object2) {
        return {
          channels: object2["channels"],
          description: object2["description"],
          disabled: object2["disabled"],
          filterTypes: object2["filterTypes"],
          headers: object2["headers"],
          metadata: object2["metadata"],
          rateLimit: object2["rateLimit"],
          secret: object2["secret"],
          uid: object2["uid"],
          url: object2["url"],
          version: object2["version"]
        };
      },
      _toJsonObject(self2) {
        return {
          channels: self2.channels,
          description: self2.description,
          disabled: self2.disabled,
          filterTypes: self2.filterTypes,
          headers: self2.headers,
          metadata: self2.metadata,
          rateLimit: self2.rateLimit,
          secret: self2.secret,
          uid: self2.uid,
          url: self2.url,
          version: self2.version
        };
      }
    };
  }
});

// node_modules/svix/dist/models/endpointOut.js
var require_endpointOut = __commonJS({
  "node_modules/svix/dist/models/endpointOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EndpointOutSerializer = void 0;
    exports.EndpointOutSerializer = {
      _fromJsonObject(object2) {
        return {
          channels: object2["channels"],
          createdAt: new Date(object2["createdAt"]),
          description: object2["description"],
          disabled: object2["disabled"],
          filterTypes: object2["filterTypes"],
          id: object2["id"],
          metadata: object2["metadata"],
          rateLimit: object2["rateLimit"],
          uid: object2["uid"],
          updatedAt: new Date(object2["updatedAt"]),
          url: object2["url"],
          version: object2["version"]
        };
      },
      _toJsonObject(self2) {
        return {
          channels: self2.channels,
          createdAt: self2.createdAt,
          description: self2.description,
          disabled: self2.disabled,
          filterTypes: self2.filterTypes,
          id: self2.id,
          metadata: self2.metadata,
          rateLimit: self2.rateLimit,
          uid: self2.uid,
          updatedAt: self2.updatedAt,
          url: self2.url,
          version: self2.version
        };
      }
    };
  }
});

// node_modules/svix/dist/models/endpointPatch.js
var require_endpointPatch = __commonJS({
  "node_modules/svix/dist/models/endpointPatch.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EndpointPatchSerializer = void 0;
    exports.EndpointPatchSerializer = {
      _fromJsonObject(object2) {
        return {
          channels: object2["channels"],
          description: object2["description"],
          disabled: object2["disabled"],
          filterTypes: object2["filterTypes"],
          metadata: object2["metadata"],
          rateLimit: object2["rateLimit"],
          secret: object2["secret"],
          uid: object2["uid"],
          url: object2["url"],
          version: object2["version"]
        };
      },
      _toJsonObject(self2) {
        return {
          channels: self2.channels,
          description: self2.description,
          disabled: self2.disabled,
          filterTypes: self2.filterTypes,
          metadata: self2.metadata,
          rateLimit: self2.rateLimit,
          secret: self2.secret,
          uid: self2.uid,
          url: self2.url,
          version: self2.version
        };
      }
    };
  }
});

// node_modules/svix/dist/models/endpointSecretOut.js
var require_endpointSecretOut = __commonJS({
  "node_modules/svix/dist/models/endpointSecretOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EndpointSecretOutSerializer = void 0;
    exports.EndpointSecretOutSerializer = {
      _fromJsonObject(object2) {
        return {
          key: object2["key"]
        };
      },
      _toJsonObject(self2) {
        return {
          key: self2.key
        };
      }
    };
  }
});

// node_modules/svix/dist/models/endpointSecretRotateIn.js
var require_endpointSecretRotateIn = __commonJS({
  "node_modules/svix/dist/models/endpointSecretRotateIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EndpointSecretRotateInSerializer = void 0;
    exports.EndpointSecretRotateInSerializer = {
      _fromJsonObject(object2) {
        return {
          key: object2["key"]
        };
      },
      _toJsonObject(self2) {
        return {
          key: self2.key
        };
      }
    };
  }
});

// node_modules/svix/dist/models/endpointStats.js
var require_endpointStats = __commonJS({
  "node_modules/svix/dist/models/endpointStats.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EndpointStatsSerializer = void 0;
    exports.EndpointStatsSerializer = {
      _fromJsonObject(object2) {
        return {
          fail: object2["fail"],
          pending: object2["pending"],
          sending: object2["sending"],
          success: object2["success"]
        };
      },
      _toJsonObject(self2) {
        return {
          fail: self2.fail,
          pending: self2.pending,
          sending: self2.sending,
          success: self2.success
        };
      }
    };
  }
});

// node_modules/svix/dist/models/endpointTransformationIn.js
var require_endpointTransformationIn = __commonJS({
  "node_modules/svix/dist/models/endpointTransformationIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EndpointTransformationInSerializer = void 0;
    exports.EndpointTransformationInSerializer = {
      _fromJsonObject(object2) {
        return {
          code: object2["code"],
          enabled: object2["enabled"]
        };
      },
      _toJsonObject(self2) {
        return {
          code: self2.code,
          enabled: self2.enabled
        };
      }
    };
  }
});

// node_modules/svix/dist/models/endpointTransformationOut.js
var require_endpointTransformationOut = __commonJS({
  "node_modules/svix/dist/models/endpointTransformationOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EndpointTransformationOutSerializer = void 0;
    exports.EndpointTransformationOutSerializer = {
      _fromJsonObject(object2) {
        return {
          code: object2["code"],
          enabled: object2["enabled"],
          updatedAt: object2["updatedAt"] ? new Date(object2["updatedAt"]) : null
        };
      },
      _toJsonObject(self2) {
        return {
          code: self2.code,
          enabled: self2.enabled,
          updatedAt: self2.updatedAt
        };
      }
    };
  }
});

// node_modules/svix/dist/models/endpointTransformationPatch.js
var require_endpointTransformationPatch = __commonJS({
  "node_modules/svix/dist/models/endpointTransformationPatch.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EndpointTransformationPatchSerializer = void 0;
    exports.EndpointTransformationPatchSerializer = {
      _fromJsonObject(object2) {
        return {
          code: object2["code"],
          enabled: object2["enabled"]
        };
      },
      _toJsonObject(self2) {
        return {
          code: self2.code,
          enabled: self2.enabled
        };
      }
    };
  }
});

// node_modules/svix/dist/models/endpointUpdate.js
var require_endpointUpdate = __commonJS({
  "node_modules/svix/dist/models/endpointUpdate.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EndpointUpdateSerializer = void 0;
    exports.EndpointUpdateSerializer = {
      _fromJsonObject(object2) {
        return {
          channels: object2["channels"],
          description: object2["description"],
          disabled: object2["disabled"],
          filterTypes: object2["filterTypes"],
          metadata: object2["metadata"],
          rateLimit: object2["rateLimit"],
          uid: object2["uid"],
          url: object2["url"],
          version: object2["version"]
        };
      },
      _toJsonObject(self2) {
        return {
          channels: self2.channels,
          description: self2.description,
          disabled: self2.disabled,
          filterTypes: self2.filterTypes,
          metadata: self2.metadata,
          rateLimit: self2.rateLimit,
          uid: self2.uid,
          url: self2.url,
          version: self2.version
        };
      }
    };
  }
});

// node_modules/svix/dist/models/eventExampleIn.js
var require_eventExampleIn = __commonJS({
  "node_modules/svix/dist/models/eventExampleIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EventExampleInSerializer = void 0;
    exports.EventExampleInSerializer = {
      _fromJsonObject(object2) {
        return {
          eventType: object2["eventType"],
          exampleIndex: object2["exampleIndex"]
        };
      },
      _toJsonObject(self2) {
        return {
          eventType: self2.eventType,
          exampleIndex: self2.exampleIndex
        };
      }
    };
  }
});

// node_modules/svix/dist/models/listResponseEndpointOut.js
var require_listResponseEndpointOut = __commonJS({
  "node_modules/svix/dist/models/listResponseEndpointOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ListResponseEndpointOutSerializer = void 0;
    var endpointOut_1 = require_endpointOut();
    exports.ListResponseEndpointOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => endpointOut_1.EndpointOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"],
          prevIterator: object2["prevIterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => endpointOut_1.EndpointOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator,
          prevIterator: self2.prevIterator
        };
      }
    };
  }
});

// node_modules/svix/dist/models/messageOut.js
var require_messageOut = __commonJS({
  "node_modules/svix/dist/models/messageOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MessageOutSerializer = void 0;
    exports.MessageOutSerializer = {
      _fromJsonObject(object2) {
        return {
          channels: object2["channels"],
          deliverAt: object2["deliverAt"] ? new Date(object2["deliverAt"]) : null,
          eventId: object2["eventId"],
          eventType: object2["eventType"],
          id: object2["id"],
          payload: object2["payload"],
          tags: object2["tags"],
          timestamp: new Date(object2["timestamp"])
        };
      },
      _toJsonObject(self2) {
        return {
          channels: self2.channels,
          deliverAt: self2.deliverAt,
          eventId: self2.eventId,
          eventType: self2.eventType,
          id: self2.id,
          payload: self2.payload,
          tags: self2.tags,
          timestamp: self2.timestamp
        };
      }
    };
  }
});

// node_modules/svix/dist/models/recoverIn.js
var require_recoverIn = __commonJS({
  "node_modules/svix/dist/models/recoverIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.RecoverInSerializer = void 0;
    exports.RecoverInSerializer = {
      _fromJsonObject(object2) {
        return {
          since: new Date(object2["since"]),
          until: object2["until"] ? new Date(object2["until"]) : null
        };
      },
      _toJsonObject(self2) {
        return {
          since: self2.since,
          until: self2.until
        };
      }
    };
  }
});

// node_modules/svix/dist/models/recoverOut.js
var require_recoverOut = __commonJS({
  "node_modules/svix/dist/models/recoverOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.RecoverOutSerializer = void 0;
    var backgroundTaskStatus_1 = require_backgroundTaskStatus();
    var backgroundTaskType_1 = require_backgroundTaskType();
    exports.RecoverOutSerializer = {
      _fromJsonObject(object2) {
        return {
          id: object2["id"],
          status: backgroundTaskStatus_1.BackgroundTaskStatusSerializer._fromJsonObject(object2["status"]),
          task: backgroundTaskType_1.BackgroundTaskTypeSerializer._fromJsonObject(object2["task"])
        };
      },
      _toJsonObject(self2) {
        return {
          id: self2.id,
          status: backgroundTaskStatus_1.BackgroundTaskStatusSerializer._toJsonObject(self2.status),
          task: backgroundTaskType_1.BackgroundTaskTypeSerializer._toJsonObject(self2.task)
        };
      }
    };
  }
});

// node_modules/svix/dist/models/replayIn.js
var require_replayIn = __commonJS({
  "node_modules/svix/dist/models/replayIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ReplayInSerializer = void 0;
    exports.ReplayInSerializer = {
      _fromJsonObject(object2) {
        return {
          since: new Date(object2["since"]),
          until: object2["until"] ? new Date(object2["until"]) : null
        };
      },
      _toJsonObject(self2) {
        return {
          since: self2.since,
          until: self2.until
        };
      }
    };
  }
});

// node_modules/svix/dist/models/replayOut.js
var require_replayOut = __commonJS({
  "node_modules/svix/dist/models/replayOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ReplayOutSerializer = void 0;
    var backgroundTaskStatus_1 = require_backgroundTaskStatus();
    var backgroundTaskType_1 = require_backgroundTaskType();
    exports.ReplayOutSerializer = {
      _fromJsonObject(object2) {
        return {
          id: object2["id"],
          status: backgroundTaskStatus_1.BackgroundTaskStatusSerializer._fromJsonObject(object2["status"]),
          task: backgroundTaskType_1.BackgroundTaskTypeSerializer._fromJsonObject(object2["task"])
        };
      },
      _toJsonObject(self2) {
        return {
          id: self2.id,
          status: backgroundTaskStatus_1.BackgroundTaskStatusSerializer._toJsonObject(self2.status),
          task: backgroundTaskType_1.BackgroundTaskTypeSerializer._toJsonObject(self2.task)
        };
      }
    };
  }
});

// node_modules/svix/dist/api/endpoint.js
var require_endpoint = __commonJS({
  "node_modules/svix/dist/api/endpoint.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Endpoint = void 0;
    var endpointHeadersIn_1 = require_endpointHeadersIn();
    var endpointHeadersOut_1 = require_endpointHeadersOut();
    var endpointHeadersPatchIn_1 = require_endpointHeadersPatchIn();
    var endpointIn_1 = require_endpointIn();
    var endpointOut_1 = require_endpointOut();
    var endpointPatch_1 = require_endpointPatch();
    var endpointSecretOut_1 = require_endpointSecretOut();
    var endpointSecretRotateIn_1 = require_endpointSecretRotateIn();
    var endpointStats_1 = require_endpointStats();
    var endpointTransformationIn_1 = require_endpointTransformationIn();
    var endpointTransformationOut_1 = require_endpointTransformationOut();
    var endpointTransformationPatch_1 = require_endpointTransformationPatch();
    var endpointUpdate_1 = require_endpointUpdate();
    var eventExampleIn_1 = require_eventExampleIn();
    var listResponseEndpointOut_1 = require_listResponseEndpointOut();
    var messageOut_1 = require_messageOut();
    var recoverIn_1 = require_recoverIn();
    var recoverOut_1 = require_recoverOut();
    var replayIn_1 = require_replayIn();
    var replayOut_1 = require_replayOut();
    var request_1 = require_request();
    var Endpoint = class {
      static {
        __name(this, "Endpoint");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      list(appId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/endpoint");
        request.setPathParam("app_id", appId);
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          order: options === null || options === void 0 ? void 0 : options.order
        });
        return request.send(this.requestCtx, listResponseEndpointOut_1.ListResponseEndpointOutSerializer._fromJsonObject);
      }
      create(appId, endpointIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/app/{app_id}/endpoint");
        request.setPathParam("app_id", appId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(endpointIn_1.EndpointInSerializer._toJsonObject(endpointIn));
        return request.send(this.requestCtx, endpointOut_1.EndpointOutSerializer._fromJsonObject);
      }
      get(appId, endpointId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/endpoint/{endpoint_id}");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        return request.send(this.requestCtx, endpointOut_1.EndpointOutSerializer._fromJsonObject);
      }
      update(appId, endpointId, endpointUpdate) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PUT, "/api/v1/app/{app_id}/endpoint/{endpoint_id}");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        request.setBody(endpointUpdate_1.EndpointUpdateSerializer._toJsonObject(endpointUpdate));
        return request.send(this.requestCtx, endpointOut_1.EndpointOutSerializer._fromJsonObject);
      }
      delete(appId, endpointId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.DELETE, "/api/v1/app/{app_id}/endpoint/{endpoint_id}");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        return request.sendNoResponseBody(this.requestCtx);
      }
      patch(appId, endpointId, endpointPatch) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PATCH, "/api/v1/app/{app_id}/endpoint/{endpoint_id}");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        request.setBody(endpointPatch_1.EndpointPatchSerializer._toJsonObject(endpointPatch));
        return request.send(this.requestCtx, endpointOut_1.EndpointOutSerializer._fromJsonObject);
      }
      getHeaders(appId, endpointId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/endpoint/{endpoint_id}/headers");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        return request.send(this.requestCtx, endpointHeadersOut_1.EndpointHeadersOutSerializer._fromJsonObject);
      }
      updateHeaders(appId, endpointId, endpointHeadersIn) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PUT, "/api/v1/app/{app_id}/endpoint/{endpoint_id}/headers");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        request.setBody(endpointHeadersIn_1.EndpointHeadersInSerializer._toJsonObject(endpointHeadersIn));
        return request.sendNoResponseBody(this.requestCtx);
      }
      headersUpdate(appId, endpointId, endpointHeadersIn) {
        return this.updateHeaders(appId, endpointId, endpointHeadersIn);
      }
      patchHeaders(appId, endpointId, endpointHeadersPatchIn) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PATCH, "/api/v1/app/{app_id}/endpoint/{endpoint_id}/headers");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        request.setBody(endpointHeadersPatchIn_1.EndpointHeadersPatchInSerializer._toJsonObject(endpointHeadersPatchIn));
        return request.sendNoResponseBody(this.requestCtx);
      }
      headersPatch(appId, endpointId, endpointHeadersPatchIn) {
        return this.patchHeaders(appId, endpointId, endpointHeadersPatchIn);
      }
      recover(appId, endpointId, recoverIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/app/{app_id}/endpoint/{endpoint_id}/recover");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(recoverIn_1.RecoverInSerializer._toJsonObject(recoverIn));
        return request.send(this.requestCtx, recoverOut_1.RecoverOutSerializer._fromJsonObject);
      }
      replayMissing(appId, endpointId, replayIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/app/{app_id}/endpoint/{endpoint_id}/replay-missing");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(replayIn_1.ReplayInSerializer._toJsonObject(replayIn));
        return request.send(this.requestCtx, replayOut_1.ReplayOutSerializer._fromJsonObject);
      }
      getSecret(appId, endpointId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/endpoint/{endpoint_id}/secret");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        return request.send(this.requestCtx, endpointSecretOut_1.EndpointSecretOutSerializer._fromJsonObject);
      }
      rotateSecret(appId, endpointId, endpointSecretRotateIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/app/{app_id}/endpoint/{endpoint_id}/secret/rotate");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(endpointSecretRotateIn_1.EndpointSecretRotateInSerializer._toJsonObject(endpointSecretRotateIn));
        return request.sendNoResponseBody(this.requestCtx);
      }
      sendExample(appId, endpointId, eventExampleIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/app/{app_id}/endpoint/{endpoint_id}/send-example");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(eventExampleIn_1.EventExampleInSerializer._toJsonObject(eventExampleIn));
        return request.send(this.requestCtx, messageOut_1.MessageOutSerializer._fromJsonObject);
      }
      getStats(appId, endpointId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/endpoint/{endpoint_id}/stats");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        request.setQueryParams({
          since: options === null || options === void 0 ? void 0 : options.since,
          until: options === null || options === void 0 ? void 0 : options.until
        });
        return request.send(this.requestCtx, endpointStats_1.EndpointStatsSerializer._fromJsonObject);
      }
      transformationGet(appId, endpointId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/endpoint/{endpoint_id}/transformation");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        return request.send(this.requestCtx, endpointTransformationOut_1.EndpointTransformationOutSerializer._fromJsonObject);
      }
      patchTransformation(appId, endpointId, endpointTransformationPatch) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PATCH, "/api/v1/app/{app_id}/endpoint/{endpoint_id}/transformation");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        request.setBody(endpointTransformationPatch_1.EndpointTransformationPatchSerializer._toJsonObject(endpointTransformationPatch));
        return request.sendNoResponseBody(this.requestCtx);
      }
      transformationPartialUpdate(appId, endpointId, endpointTransformationIn) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PATCH, "/api/v1/app/{app_id}/endpoint/{endpoint_id}/transformation");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        request.setBody(endpointTransformationIn_1.EndpointTransformationInSerializer._toJsonObject(endpointTransformationIn));
        return request.sendNoResponseBody(this.requestCtx);
      }
    };
    exports.Endpoint = Endpoint;
  }
});

// node_modules/svix/dist/models/eventTypeIn.js
var require_eventTypeIn = __commonJS({
  "node_modules/svix/dist/models/eventTypeIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EventTypeInSerializer = void 0;
    exports.EventTypeInSerializer = {
      _fromJsonObject(object2) {
        return {
          archived: object2["archived"],
          deprecated: object2["deprecated"],
          description: object2["description"],
          featureFlag: object2["featureFlag"],
          featureFlags: object2["featureFlags"],
          groupName: object2["groupName"],
          name: object2["name"],
          schemas: object2["schemas"]
        };
      },
      _toJsonObject(self2) {
        return {
          archived: self2.archived,
          deprecated: self2.deprecated,
          description: self2.description,
          featureFlag: self2.featureFlag,
          featureFlags: self2.featureFlags,
          groupName: self2.groupName,
          name: self2.name,
          schemas: self2.schemas
        };
      }
    };
  }
});

// node_modules/svix/dist/models/environmentIn.js
var require_environmentIn = __commonJS({
  "node_modules/svix/dist/models/environmentIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EnvironmentInSerializer = void 0;
    var connectorIn_1 = require_connectorIn();
    var eventTypeIn_1 = require_eventTypeIn();
    exports.EnvironmentInSerializer = {
      _fromJsonObject(object2) {
        var _a, _b;
        return {
          connectors: (_a = object2["connectors"]) === null || _a === void 0 ? void 0 : _a.map((item) => connectorIn_1.ConnectorInSerializer._fromJsonObject(item)),
          eventTypes: (_b = object2["eventTypes"]) === null || _b === void 0 ? void 0 : _b.map((item) => eventTypeIn_1.EventTypeInSerializer._fromJsonObject(item)),
          settings: object2["settings"]
        };
      },
      _toJsonObject(self2) {
        var _a, _b;
        return {
          connectors: (_a = self2.connectors) === null || _a === void 0 ? void 0 : _a.map((item) => connectorIn_1.ConnectorInSerializer._toJsonObject(item)),
          eventTypes: (_b = self2.eventTypes) === null || _b === void 0 ? void 0 : _b.map((item) => eventTypeIn_1.EventTypeInSerializer._toJsonObject(item)),
          settings: self2.settings
        };
      }
    };
  }
});

// node_modules/svix/dist/models/eventTypeOut.js
var require_eventTypeOut = __commonJS({
  "node_modules/svix/dist/models/eventTypeOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EventTypeOutSerializer = void 0;
    exports.EventTypeOutSerializer = {
      _fromJsonObject(object2) {
        return {
          archived: object2["archived"],
          createdAt: new Date(object2["createdAt"]),
          deprecated: object2["deprecated"],
          description: object2["description"],
          featureFlag: object2["featureFlag"],
          featureFlags: object2["featureFlags"],
          groupName: object2["groupName"],
          name: object2["name"],
          schemas: object2["schemas"],
          updatedAt: new Date(object2["updatedAt"])
        };
      },
      _toJsonObject(self2) {
        return {
          archived: self2.archived,
          createdAt: self2.createdAt,
          deprecated: self2.deprecated,
          description: self2.description,
          featureFlag: self2.featureFlag,
          featureFlags: self2.featureFlags,
          groupName: self2.groupName,
          name: self2.name,
          schemas: self2.schemas,
          updatedAt: self2.updatedAt
        };
      }
    };
  }
});

// node_modules/svix/dist/models/environmentOut.js
var require_environmentOut = __commonJS({
  "node_modules/svix/dist/models/environmentOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EnvironmentOutSerializer = void 0;
    var connectorOut_1 = require_connectorOut();
    var eventTypeOut_1 = require_eventTypeOut();
    exports.EnvironmentOutSerializer = {
      _fromJsonObject(object2) {
        return {
          connectors: object2["connectors"].map((item) => connectorOut_1.ConnectorOutSerializer._fromJsonObject(item)),
          createdAt: new Date(object2["createdAt"]),
          eventTypes: object2["eventTypes"].map((item) => eventTypeOut_1.EventTypeOutSerializer._fromJsonObject(item)),
          settings: object2["settings"],
          version: object2["version"]
        };
      },
      _toJsonObject(self2) {
        return {
          connectors: self2.connectors.map((item) => connectorOut_1.ConnectorOutSerializer._toJsonObject(item)),
          createdAt: self2.createdAt,
          eventTypes: self2.eventTypes.map((item) => eventTypeOut_1.EventTypeOutSerializer._toJsonObject(item)),
          settings: self2.settings,
          version: self2.version
        };
      }
    };
  }
});

// node_modules/svix/dist/api/environment.js
var require_environment = __commonJS({
  "node_modules/svix/dist/api/environment.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Environment = void 0;
    var environmentIn_1 = require_environmentIn();
    var environmentOut_1 = require_environmentOut();
    var request_1 = require_request();
    var Environment = class {
      static {
        __name(this, "Environment");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      export(options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/environment/export");
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        return request.send(this.requestCtx, environmentOut_1.EnvironmentOutSerializer._fromJsonObject);
      }
      import(environmentIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/environment/import");
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(environmentIn_1.EnvironmentInSerializer._toJsonObject(environmentIn));
        return request.sendNoResponseBody(this.requestCtx);
      }
    };
    exports.Environment = Environment;
  }
});

// node_modules/svix/dist/models/eventTypeImportOpenApiIn.js
var require_eventTypeImportOpenApiIn = __commonJS({
  "node_modules/svix/dist/models/eventTypeImportOpenApiIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EventTypeImportOpenApiInSerializer = void 0;
    exports.EventTypeImportOpenApiInSerializer = {
      _fromJsonObject(object2) {
        return {
          dryRun: object2["dryRun"],
          replaceAll: object2["replaceAll"],
          spec: object2["spec"],
          specRaw: object2["specRaw"]
        };
      },
      _toJsonObject(self2) {
        return {
          dryRun: self2.dryRun,
          replaceAll: self2.replaceAll,
          spec: self2.spec,
          specRaw: self2.specRaw
        };
      }
    };
  }
});

// node_modules/svix/dist/models/eventTypeFromOpenApi.js
var require_eventTypeFromOpenApi = __commonJS({
  "node_modules/svix/dist/models/eventTypeFromOpenApi.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EventTypeFromOpenApiSerializer = void 0;
    exports.EventTypeFromOpenApiSerializer = {
      _fromJsonObject(object2) {
        return {
          deprecated: object2["deprecated"],
          description: object2["description"],
          featureFlag: object2["featureFlag"],
          featureFlags: object2["featureFlags"],
          groupName: object2["groupName"],
          name: object2["name"],
          schemas: object2["schemas"]
        };
      },
      _toJsonObject(self2) {
        return {
          deprecated: self2.deprecated,
          description: self2.description,
          featureFlag: self2.featureFlag,
          featureFlags: self2.featureFlags,
          groupName: self2.groupName,
          name: self2.name,
          schemas: self2.schemas
        };
      }
    };
  }
});

// node_modules/svix/dist/models/eventTypeImportOpenApiOutData.js
var require_eventTypeImportOpenApiOutData = __commonJS({
  "node_modules/svix/dist/models/eventTypeImportOpenApiOutData.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EventTypeImportOpenApiOutDataSerializer = void 0;
    var eventTypeFromOpenApi_1 = require_eventTypeFromOpenApi();
    exports.EventTypeImportOpenApiOutDataSerializer = {
      _fromJsonObject(object2) {
        var _a;
        return {
          modified: object2["modified"],
          toModify: (_a = object2["to_modify"]) === null || _a === void 0 ? void 0 : _a.map((item) => eventTypeFromOpenApi_1.EventTypeFromOpenApiSerializer._fromJsonObject(item))
        };
      },
      _toJsonObject(self2) {
        var _a;
        return {
          modified: self2.modified,
          to_modify: (_a = self2.toModify) === null || _a === void 0 ? void 0 : _a.map((item) => eventTypeFromOpenApi_1.EventTypeFromOpenApiSerializer._toJsonObject(item))
        };
      }
    };
  }
});

// node_modules/svix/dist/models/eventTypeImportOpenApiOut.js
var require_eventTypeImportOpenApiOut = __commonJS({
  "node_modules/svix/dist/models/eventTypeImportOpenApiOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EventTypeImportOpenApiOutSerializer = void 0;
    var eventTypeImportOpenApiOutData_1 = require_eventTypeImportOpenApiOutData();
    exports.EventTypeImportOpenApiOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: eventTypeImportOpenApiOutData_1.EventTypeImportOpenApiOutDataSerializer._fromJsonObject(object2["data"])
        };
      },
      _toJsonObject(self2) {
        return {
          data: eventTypeImportOpenApiOutData_1.EventTypeImportOpenApiOutDataSerializer._toJsonObject(self2.data)
        };
      }
    };
  }
});

// node_modules/svix/dist/models/eventTypePatch.js
var require_eventTypePatch = __commonJS({
  "node_modules/svix/dist/models/eventTypePatch.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EventTypePatchSerializer = void 0;
    exports.EventTypePatchSerializer = {
      _fromJsonObject(object2) {
        return {
          archived: object2["archived"],
          deprecated: object2["deprecated"],
          description: object2["description"],
          featureFlag: object2["featureFlag"],
          featureFlags: object2["featureFlags"],
          groupName: object2["groupName"],
          schemas: object2["schemas"]
        };
      },
      _toJsonObject(self2) {
        return {
          archived: self2.archived,
          deprecated: self2.deprecated,
          description: self2.description,
          featureFlag: self2.featureFlag,
          featureFlags: self2.featureFlags,
          groupName: self2.groupName,
          schemas: self2.schemas
        };
      }
    };
  }
});

// node_modules/svix/dist/models/eventTypeUpdate.js
var require_eventTypeUpdate = __commonJS({
  "node_modules/svix/dist/models/eventTypeUpdate.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EventTypeUpdateSerializer = void 0;
    exports.EventTypeUpdateSerializer = {
      _fromJsonObject(object2) {
        return {
          archived: object2["archived"],
          deprecated: object2["deprecated"],
          description: object2["description"],
          featureFlag: object2["featureFlag"],
          featureFlags: object2["featureFlags"],
          groupName: object2["groupName"],
          schemas: object2["schemas"]
        };
      },
      _toJsonObject(self2) {
        return {
          archived: self2.archived,
          deprecated: self2.deprecated,
          description: self2.description,
          featureFlag: self2.featureFlag,
          featureFlags: self2.featureFlags,
          groupName: self2.groupName,
          schemas: self2.schemas
        };
      }
    };
  }
});

// node_modules/svix/dist/models/listResponseEventTypeOut.js
var require_listResponseEventTypeOut = __commonJS({
  "node_modules/svix/dist/models/listResponseEventTypeOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ListResponseEventTypeOutSerializer = void 0;
    var eventTypeOut_1 = require_eventTypeOut();
    exports.ListResponseEventTypeOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => eventTypeOut_1.EventTypeOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"],
          prevIterator: object2["prevIterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => eventTypeOut_1.EventTypeOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator,
          prevIterator: self2.prevIterator
        };
      }
    };
  }
});

// node_modules/svix/dist/api/eventType.js
var require_eventType = __commonJS({
  "node_modules/svix/dist/api/eventType.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EventType = void 0;
    var eventTypeImportOpenApiIn_1 = require_eventTypeImportOpenApiIn();
    var eventTypeImportOpenApiOut_1 = require_eventTypeImportOpenApiOut();
    var eventTypeIn_1 = require_eventTypeIn();
    var eventTypeOut_1 = require_eventTypeOut();
    var eventTypePatch_1 = require_eventTypePatch();
    var eventTypeUpdate_1 = require_eventTypeUpdate();
    var listResponseEventTypeOut_1 = require_listResponseEventTypeOut();
    var request_1 = require_request();
    var EventType = class {
      static {
        __name(this, "EventType");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      list(options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/event-type");
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          order: options === null || options === void 0 ? void 0 : options.order,
          include_archived: options === null || options === void 0 ? void 0 : options.includeArchived,
          with_content: options === null || options === void 0 ? void 0 : options.withContent
        });
        return request.send(this.requestCtx, listResponseEventTypeOut_1.ListResponseEventTypeOutSerializer._fromJsonObject);
      }
      create(eventTypeIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/event-type");
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(eventTypeIn_1.EventTypeInSerializer._toJsonObject(eventTypeIn));
        return request.send(this.requestCtx, eventTypeOut_1.EventTypeOutSerializer._fromJsonObject);
      }
      importOpenapi(eventTypeImportOpenApiIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/event-type/import/openapi");
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(eventTypeImportOpenApiIn_1.EventTypeImportOpenApiInSerializer._toJsonObject(eventTypeImportOpenApiIn));
        return request.send(this.requestCtx, eventTypeImportOpenApiOut_1.EventTypeImportOpenApiOutSerializer._fromJsonObject);
      }
      get(eventTypeName) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/event-type/{event_type_name}");
        request.setPathParam("event_type_name", eventTypeName);
        return request.send(this.requestCtx, eventTypeOut_1.EventTypeOutSerializer._fromJsonObject);
      }
      update(eventTypeName, eventTypeUpdate) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PUT, "/api/v1/event-type/{event_type_name}");
        request.setPathParam("event_type_name", eventTypeName);
        request.setBody(eventTypeUpdate_1.EventTypeUpdateSerializer._toJsonObject(eventTypeUpdate));
        return request.send(this.requestCtx, eventTypeOut_1.EventTypeOutSerializer._fromJsonObject);
      }
      delete(eventTypeName, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.DELETE, "/api/v1/event-type/{event_type_name}");
        request.setPathParam("event_type_name", eventTypeName);
        request.setQueryParams({
          expunge: options === null || options === void 0 ? void 0 : options.expunge
        });
        return request.sendNoResponseBody(this.requestCtx);
      }
      patch(eventTypeName, eventTypePatch) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PATCH, "/api/v1/event-type/{event_type_name}");
        request.setPathParam("event_type_name", eventTypeName);
        request.setBody(eventTypePatch_1.EventTypePatchSerializer._toJsonObject(eventTypePatch));
        return request.send(this.requestCtx, eventTypeOut_1.EventTypeOutSerializer._fromJsonObject);
      }
    };
    exports.EventType = EventType;
  }
});

// node_modules/svix/dist/api/health.js
var require_health = __commonJS({
  "node_modules/svix/dist/api/health.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Health = void 0;
    var request_1 = require_request();
    var Health = class {
      static {
        __name(this, "Health");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      get() {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/health");
        return request.sendNoResponseBody(this.requestCtx);
      }
    };
    exports.Health = Health;
  }
});

// node_modules/svix/dist/models/ingestSourceConsumerPortalAccessIn.js
var require_ingestSourceConsumerPortalAccessIn = __commonJS({
  "node_modules/svix/dist/models/ingestSourceConsumerPortalAccessIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IngestSourceConsumerPortalAccessInSerializer = void 0;
    exports.IngestSourceConsumerPortalAccessInSerializer = {
      _fromJsonObject(object2) {
        return {
          expiry: object2["expiry"],
          readOnly: object2["readOnly"]
        };
      },
      _toJsonObject(self2) {
        return {
          expiry: self2.expiry,
          readOnly: self2.readOnly
        };
      }
    };
  }
});

// node_modules/svix/dist/models/ingestEndpointHeadersIn.js
var require_ingestEndpointHeadersIn = __commonJS({
  "node_modules/svix/dist/models/ingestEndpointHeadersIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IngestEndpointHeadersInSerializer = void 0;
    exports.IngestEndpointHeadersInSerializer = {
      _fromJsonObject(object2) {
        return {
          headers: object2["headers"]
        };
      },
      _toJsonObject(self2) {
        return {
          headers: self2.headers
        };
      }
    };
  }
});

// node_modules/svix/dist/models/ingestEndpointHeadersOut.js
var require_ingestEndpointHeadersOut = __commonJS({
  "node_modules/svix/dist/models/ingestEndpointHeadersOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IngestEndpointHeadersOutSerializer = void 0;
    exports.IngestEndpointHeadersOutSerializer = {
      _fromJsonObject(object2) {
        return {
          headers: object2["headers"],
          sensitive: object2["sensitive"]
        };
      },
      _toJsonObject(self2) {
        return {
          headers: self2.headers,
          sensitive: self2.sensitive
        };
      }
    };
  }
});

// node_modules/svix/dist/models/ingestEndpointIn.js
var require_ingestEndpointIn = __commonJS({
  "node_modules/svix/dist/models/ingestEndpointIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IngestEndpointInSerializer = void 0;
    exports.IngestEndpointInSerializer = {
      _fromJsonObject(object2) {
        return {
          description: object2["description"],
          disabled: object2["disabled"],
          metadata: object2["metadata"],
          rateLimit: object2["rateLimit"],
          secret: object2["secret"],
          uid: object2["uid"],
          url: object2["url"]
        };
      },
      _toJsonObject(self2) {
        return {
          description: self2.description,
          disabled: self2.disabled,
          metadata: self2.metadata,
          rateLimit: self2.rateLimit,
          secret: self2.secret,
          uid: self2.uid,
          url: self2.url
        };
      }
    };
  }
});

// node_modules/svix/dist/models/ingestEndpointOut.js
var require_ingestEndpointOut = __commonJS({
  "node_modules/svix/dist/models/ingestEndpointOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IngestEndpointOutSerializer = void 0;
    exports.IngestEndpointOutSerializer = {
      _fromJsonObject(object2) {
        return {
          createdAt: new Date(object2["createdAt"]),
          description: object2["description"],
          disabled: object2["disabled"],
          id: object2["id"],
          metadata: object2["metadata"],
          rateLimit: object2["rateLimit"],
          uid: object2["uid"],
          updatedAt: new Date(object2["updatedAt"]),
          url: object2["url"]
        };
      },
      _toJsonObject(self2) {
        return {
          createdAt: self2.createdAt,
          description: self2.description,
          disabled: self2.disabled,
          id: self2.id,
          metadata: self2.metadata,
          rateLimit: self2.rateLimit,
          uid: self2.uid,
          updatedAt: self2.updatedAt,
          url: self2.url
        };
      }
    };
  }
});

// node_modules/svix/dist/models/ingestEndpointSecretIn.js
var require_ingestEndpointSecretIn = __commonJS({
  "node_modules/svix/dist/models/ingestEndpointSecretIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IngestEndpointSecretInSerializer = void 0;
    exports.IngestEndpointSecretInSerializer = {
      _fromJsonObject(object2) {
        return {
          key: object2["key"]
        };
      },
      _toJsonObject(self2) {
        return {
          key: self2.key
        };
      }
    };
  }
});

// node_modules/svix/dist/models/ingestEndpointSecretOut.js
var require_ingestEndpointSecretOut = __commonJS({
  "node_modules/svix/dist/models/ingestEndpointSecretOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IngestEndpointSecretOutSerializer = void 0;
    exports.IngestEndpointSecretOutSerializer = {
      _fromJsonObject(object2) {
        return {
          key: object2["key"]
        };
      },
      _toJsonObject(self2) {
        return {
          key: self2.key
        };
      }
    };
  }
});

// node_modules/svix/dist/models/ingestEndpointTransformationOut.js
var require_ingestEndpointTransformationOut = __commonJS({
  "node_modules/svix/dist/models/ingestEndpointTransformationOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IngestEndpointTransformationOutSerializer = void 0;
    exports.IngestEndpointTransformationOutSerializer = {
      _fromJsonObject(object2) {
        return {
          code: object2["code"],
          enabled: object2["enabled"]
        };
      },
      _toJsonObject(self2) {
        return {
          code: self2.code,
          enabled: self2.enabled
        };
      }
    };
  }
});

// node_modules/svix/dist/models/ingestEndpointTransformationPatch.js
var require_ingestEndpointTransformationPatch = __commonJS({
  "node_modules/svix/dist/models/ingestEndpointTransformationPatch.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IngestEndpointTransformationPatchSerializer = void 0;
    exports.IngestEndpointTransformationPatchSerializer = {
      _fromJsonObject(object2) {
        return {
          code: object2["code"],
          enabled: object2["enabled"]
        };
      },
      _toJsonObject(self2) {
        return {
          code: self2.code,
          enabled: self2.enabled
        };
      }
    };
  }
});

// node_modules/svix/dist/models/ingestEndpointUpdate.js
var require_ingestEndpointUpdate = __commonJS({
  "node_modules/svix/dist/models/ingestEndpointUpdate.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IngestEndpointUpdateSerializer = void 0;
    exports.IngestEndpointUpdateSerializer = {
      _fromJsonObject(object2) {
        return {
          description: object2["description"],
          disabled: object2["disabled"],
          metadata: object2["metadata"],
          rateLimit: object2["rateLimit"],
          uid: object2["uid"],
          url: object2["url"]
        };
      },
      _toJsonObject(self2) {
        return {
          description: self2.description,
          disabled: self2.disabled,
          metadata: self2.metadata,
          rateLimit: self2.rateLimit,
          uid: self2.uid,
          url: self2.url
        };
      }
    };
  }
});

// node_modules/svix/dist/models/listResponseIngestEndpointOut.js
var require_listResponseIngestEndpointOut = __commonJS({
  "node_modules/svix/dist/models/listResponseIngestEndpointOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ListResponseIngestEndpointOutSerializer = void 0;
    var ingestEndpointOut_1 = require_ingestEndpointOut();
    exports.ListResponseIngestEndpointOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => ingestEndpointOut_1.IngestEndpointOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"],
          prevIterator: object2["prevIterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => ingestEndpointOut_1.IngestEndpointOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator,
          prevIterator: self2.prevIterator
        };
      }
    };
  }
});

// node_modules/svix/dist/api/ingestEndpoint.js
var require_ingestEndpoint = __commonJS({
  "node_modules/svix/dist/api/ingestEndpoint.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IngestEndpoint = void 0;
    var ingestEndpointHeadersIn_1 = require_ingestEndpointHeadersIn();
    var ingestEndpointHeadersOut_1 = require_ingestEndpointHeadersOut();
    var ingestEndpointIn_1 = require_ingestEndpointIn();
    var ingestEndpointOut_1 = require_ingestEndpointOut();
    var ingestEndpointSecretIn_1 = require_ingestEndpointSecretIn();
    var ingestEndpointSecretOut_1 = require_ingestEndpointSecretOut();
    var ingestEndpointTransformationOut_1 = require_ingestEndpointTransformationOut();
    var ingestEndpointTransformationPatch_1 = require_ingestEndpointTransformationPatch();
    var ingestEndpointUpdate_1 = require_ingestEndpointUpdate();
    var listResponseIngestEndpointOut_1 = require_listResponseIngestEndpointOut();
    var request_1 = require_request();
    var IngestEndpoint = class {
      static {
        __name(this, "IngestEndpoint");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      list(sourceId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/ingest/api/v1/source/{source_id}/endpoint");
        request.setPathParam("source_id", sourceId);
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          order: options === null || options === void 0 ? void 0 : options.order
        });
        return request.send(this.requestCtx, listResponseIngestEndpointOut_1.ListResponseIngestEndpointOutSerializer._fromJsonObject);
      }
      create(sourceId, ingestEndpointIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/ingest/api/v1/source/{source_id}/endpoint");
        request.setPathParam("source_id", sourceId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(ingestEndpointIn_1.IngestEndpointInSerializer._toJsonObject(ingestEndpointIn));
        return request.send(this.requestCtx, ingestEndpointOut_1.IngestEndpointOutSerializer._fromJsonObject);
      }
      get(sourceId, endpointId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/ingest/api/v1/source/{source_id}/endpoint/{endpoint_id}");
        request.setPathParam("source_id", sourceId);
        request.setPathParam("endpoint_id", endpointId);
        return request.send(this.requestCtx, ingestEndpointOut_1.IngestEndpointOutSerializer._fromJsonObject);
      }
      update(sourceId, endpointId, ingestEndpointUpdate) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PUT, "/ingest/api/v1/source/{source_id}/endpoint/{endpoint_id}");
        request.setPathParam("source_id", sourceId);
        request.setPathParam("endpoint_id", endpointId);
        request.setBody(ingestEndpointUpdate_1.IngestEndpointUpdateSerializer._toJsonObject(ingestEndpointUpdate));
        return request.send(this.requestCtx, ingestEndpointOut_1.IngestEndpointOutSerializer._fromJsonObject);
      }
      delete(sourceId, endpointId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.DELETE, "/ingest/api/v1/source/{source_id}/endpoint/{endpoint_id}");
        request.setPathParam("source_id", sourceId);
        request.setPathParam("endpoint_id", endpointId);
        return request.sendNoResponseBody(this.requestCtx);
      }
      getHeaders(sourceId, endpointId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/ingest/api/v1/source/{source_id}/endpoint/{endpoint_id}/headers");
        request.setPathParam("source_id", sourceId);
        request.setPathParam("endpoint_id", endpointId);
        return request.send(this.requestCtx, ingestEndpointHeadersOut_1.IngestEndpointHeadersOutSerializer._fromJsonObject);
      }
      updateHeaders(sourceId, endpointId, ingestEndpointHeadersIn) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PUT, "/ingest/api/v1/source/{source_id}/endpoint/{endpoint_id}/headers");
        request.setPathParam("source_id", sourceId);
        request.setPathParam("endpoint_id", endpointId);
        request.setBody(ingestEndpointHeadersIn_1.IngestEndpointHeadersInSerializer._toJsonObject(ingestEndpointHeadersIn));
        return request.sendNoResponseBody(this.requestCtx);
      }
      getSecret(sourceId, endpointId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/ingest/api/v1/source/{source_id}/endpoint/{endpoint_id}/secret");
        request.setPathParam("source_id", sourceId);
        request.setPathParam("endpoint_id", endpointId);
        return request.send(this.requestCtx, ingestEndpointSecretOut_1.IngestEndpointSecretOutSerializer._fromJsonObject);
      }
      rotateSecret(sourceId, endpointId, ingestEndpointSecretIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/ingest/api/v1/source/{source_id}/endpoint/{endpoint_id}/secret/rotate");
        request.setPathParam("source_id", sourceId);
        request.setPathParam("endpoint_id", endpointId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(ingestEndpointSecretIn_1.IngestEndpointSecretInSerializer._toJsonObject(ingestEndpointSecretIn));
        return request.sendNoResponseBody(this.requestCtx);
      }
      getTransformation(sourceId, endpointId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/ingest/api/v1/source/{source_id}/endpoint/{endpoint_id}/transformation");
        request.setPathParam("source_id", sourceId);
        request.setPathParam("endpoint_id", endpointId);
        return request.send(this.requestCtx, ingestEndpointTransformationOut_1.IngestEndpointTransformationOutSerializer._fromJsonObject);
      }
      setTransformation(sourceId, endpointId, ingestEndpointTransformationPatch) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PATCH, "/ingest/api/v1/source/{source_id}/endpoint/{endpoint_id}/transformation");
        request.setPathParam("source_id", sourceId);
        request.setPathParam("endpoint_id", endpointId);
        request.setBody(ingestEndpointTransformationPatch_1.IngestEndpointTransformationPatchSerializer._toJsonObject(ingestEndpointTransformationPatch));
        return request.sendNoResponseBody(this.requestCtx);
      }
    };
    exports.IngestEndpoint = IngestEndpoint;
  }
});

// node_modules/svix/dist/models/adobeSignConfig.js
var require_adobeSignConfig = __commonJS({
  "node_modules/svix/dist/models/adobeSignConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AdobeSignConfigSerializer = void 0;
    exports.AdobeSignConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          clientId: object2["clientId"]
        };
      },
      _toJsonObject(self2) {
        return {
          clientId: self2.clientId
        };
      }
    };
  }
});

// node_modules/svix/dist/models/airwallexConfig.js
var require_airwallexConfig = __commonJS({
  "node_modules/svix/dist/models/airwallexConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AirwallexConfigSerializer = void 0;
    exports.AirwallexConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          secret: object2["secret"]
        };
      },
      _toJsonObject(self2) {
        return {
          secret: self2.secret
        };
      }
    };
  }
});

// node_modules/svix/dist/models/checkbookConfig.js
var require_checkbookConfig = __commonJS({
  "node_modules/svix/dist/models/checkbookConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.CheckbookConfigSerializer = void 0;
    exports.CheckbookConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          secret: object2["secret"]
        };
      },
      _toJsonObject(self2) {
        return {
          secret: self2.secret
        };
      }
    };
  }
});

// node_modules/svix/dist/models/cronConfig.js
var require_cronConfig = __commonJS({
  "node_modules/svix/dist/models/cronConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.CronConfigSerializer = void 0;
    exports.CronConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          contentType: object2["contentType"],
          payload: object2["payload"],
          schedule: object2["schedule"]
        };
      },
      _toJsonObject(self2) {
        return {
          contentType: self2.contentType,
          payload: self2.payload,
          schedule: self2.schedule
        };
      }
    };
  }
});

// node_modules/svix/dist/models/docusignConfig.js
var require_docusignConfig = __commonJS({
  "node_modules/svix/dist/models/docusignConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.DocusignConfigSerializer = void 0;
    exports.DocusignConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          secret: object2["secret"]
        };
      },
      _toJsonObject(self2) {
        return {
          secret: self2.secret
        };
      }
    };
  }
});

// node_modules/svix/dist/models/easypostConfig.js
var require_easypostConfig = __commonJS({
  "node_modules/svix/dist/models/easypostConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EasypostConfigSerializer = void 0;
    exports.EasypostConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          secret: object2["secret"]
        };
      },
      _toJsonObject(self2) {
        return {
          secret: self2.secret
        };
      }
    };
  }
});

// node_modules/svix/dist/models/githubConfig.js
var require_githubConfig = __commonJS({
  "node_modules/svix/dist/models/githubConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.GithubConfigSerializer = void 0;
    exports.GithubConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          secret: object2["secret"]
        };
      },
      _toJsonObject(self2) {
        return {
          secret: self2.secret
        };
      }
    };
  }
});

// node_modules/svix/dist/models/hubspotConfig.js
var require_hubspotConfig = __commonJS({
  "node_modules/svix/dist/models/hubspotConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.HubspotConfigSerializer = void 0;
    exports.HubspotConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          secret: object2["secret"]
        };
      },
      _toJsonObject(self2) {
        return {
          secret: self2.secret
        };
      }
    };
  }
});

// node_modules/svix/dist/models/orumIoConfig.js
var require_orumIoConfig = __commonJS({
  "node_modules/svix/dist/models/orumIoConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OrumIoConfigSerializer = void 0;
    exports.OrumIoConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          publicKey: object2["publicKey"]
        };
      },
      _toJsonObject(self2) {
        return {
          publicKey: self2.publicKey
        };
      }
    };
  }
});

// node_modules/svix/dist/models/pandaDocConfig.js
var require_pandaDocConfig = __commonJS({
  "node_modules/svix/dist/models/pandaDocConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PandaDocConfigSerializer = void 0;
    exports.PandaDocConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          secret: object2["secret"]
        };
      },
      _toJsonObject(self2) {
        return {
          secret: self2.secret
        };
      }
    };
  }
});

// node_modules/svix/dist/models/portIoConfig.js
var require_portIoConfig = __commonJS({
  "node_modules/svix/dist/models/portIoConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PortIoConfigSerializer = void 0;
    exports.PortIoConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          secret: object2["secret"]
        };
      },
      _toJsonObject(self2) {
        return {
          secret: self2.secret
        };
      }
    };
  }
});

// node_modules/svix/dist/models/rutterConfig.js
var require_rutterConfig = __commonJS({
  "node_modules/svix/dist/models/rutterConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.RutterConfigSerializer = void 0;
    exports.RutterConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          secret: object2["secret"]
        };
      },
      _toJsonObject(self2) {
        return {
          secret: self2.secret
        };
      }
    };
  }
});

// node_modules/svix/dist/models/segmentConfig.js
var require_segmentConfig = __commonJS({
  "node_modules/svix/dist/models/segmentConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SegmentConfigSerializer = void 0;
    exports.SegmentConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          secret: object2["secret"]
        };
      },
      _toJsonObject(self2) {
        return {
          secret: self2.secret
        };
      }
    };
  }
});

// node_modules/svix/dist/models/shopifyConfig.js
var require_shopifyConfig = __commonJS({
  "node_modules/svix/dist/models/shopifyConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ShopifyConfigSerializer = void 0;
    exports.ShopifyConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          secret: object2["secret"]
        };
      },
      _toJsonObject(self2) {
        return {
          secret: self2.secret
        };
      }
    };
  }
});

// node_modules/svix/dist/models/slackConfig.js
var require_slackConfig = __commonJS({
  "node_modules/svix/dist/models/slackConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SlackConfigSerializer = void 0;
    exports.SlackConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          secret: object2["secret"]
        };
      },
      _toJsonObject(self2) {
        return {
          secret: self2.secret
        };
      }
    };
  }
});

// node_modules/svix/dist/models/stripeConfig.js
var require_stripeConfig = __commonJS({
  "node_modules/svix/dist/models/stripeConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StripeConfigSerializer = void 0;
    exports.StripeConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          secret: object2["secret"]
        };
      },
      _toJsonObject(self2) {
        return {
          secret: self2.secret
        };
      }
    };
  }
});

// node_modules/svix/dist/models/svixConfig.js
var require_svixConfig = __commonJS({
  "node_modules/svix/dist/models/svixConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SvixConfigSerializer = void 0;
    exports.SvixConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          secret: object2["secret"]
        };
      },
      _toJsonObject(self2) {
        return {
          secret: self2.secret
        };
      }
    };
  }
});

// node_modules/svix/dist/models/telnyxConfig.js
var require_telnyxConfig = __commonJS({
  "node_modules/svix/dist/models/telnyxConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.TelnyxConfigSerializer = void 0;
    exports.TelnyxConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          publicKey: object2["publicKey"]
        };
      },
      _toJsonObject(self2) {
        return {
          publicKey: self2.publicKey
        };
      }
    };
  }
});

// node_modules/svix/dist/models/vapiConfig.js
var require_vapiConfig = __commonJS({
  "node_modules/svix/dist/models/vapiConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.VapiConfigSerializer = void 0;
    exports.VapiConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          secret: object2["secret"]
        };
      },
      _toJsonObject(self2) {
        return {
          secret: self2.secret
        };
      }
    };
  }
});

// node_modules/svix/dist/models/veriffConfig.js
var require_veriffConfig = __commonJS({
  "node_modules/svix/dist/models/veriffConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.VeriffConfigSerializer = void 0;
    exports.VeriffConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          secret: object2["secret"]
        };
      },
      _toJsonObject(self2) {
        return {
          secret: self2.secret
        };
      }
    };
  }
});

// node_modules/svix/dist/models/zoomConfig.js
var require_zoomConfig = __commonJS({
  "node_modules/svix/dist/models/zoomConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ZoomConfigSerializer = void 0;
    exports.ZoomConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          secret: object2["secret"]
        };
      },
      _toJsonObject(self2) {
        return {
          secret: self2.secret
        };
      }
    };
  }
});

// node_modules/svix/dist/models/ingestSourceIn.js
var require_ingestSourceIn = __commonJS({
  "node_modules/svix/dist/models/ingestSourceIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IngestSourceInSerializer = void 0;
    var adobeSignConfig_1 = require_adobeSignConfig();
    var airwallexConfig_1 = require_airwallexConfig();
    var checkbookConfig_1 = require_checkbookConfig();
    var cronConfig_1 = require_cronConfig();
    var docusignConfig_1 = require_docusignConfig();
    var easypostConfig_1 = require_easypostConfig();
    var githubConfig_1 = require_githubConfig();
    var hubspotConfig_1 = require_hubspotConfig();
    var orumIoConfig_1 = require_orumIoConfig();
    var pandaDocConfig_1 = require_pandaDocConfig();
    var portIoConfig_1 = require_portIoConfig();
    var rutterConfig_1 = require_rutterConfig();
    var segmentConfig_1 = require_segmentConfig();
    var shopifyConfig_1 = require_shopifyConfig();
    var slackConfig_1 = require_slackConfig();
    var stripeConfig_1 = require_stripeConfig();
    var svixConfig_1 = require_svixConfig();
    var telnyxConfig_1 = require_telnyxConfig();
    var vapiConfig_1 = require_vapiConfig();
    var veriffConfig_1 = require_veriffConfig();
    var zoomConfig_1 = require_zoomConfig();
    exports.IngestSourceInSerializer = {
      _fromJsonObject(object2) {
        const type = object2["type"];
        function getConfig(type2) {
          switch (type2) {
            case "generic-webhook":
              return {};
            case "cron":
              return cronConfig_1.CronConfigSerializer._fromJsonObject(object2["config"]);
            case "adobe-sign":
              return adobeSignConfig_1.AdobeSignConfigSerializer._fromJsonObject(object2["config"]);
            case "beehiiv":
              return svixConfig_1.SvixConfigSerializer._fromJsonObject(object2["config"]);
            case "brex":
              return svixConfig_1.SvixConfigSerializer._fromJsonObject(object2["config"]);
            case "checkbook":
              return checkbookConfig_1.CheckbookConfigSerializer._fromJsonObject(object2["config"]);
            case "clerk":
              return svixConfig_1.SvixConfigSerializer._fromJsonObject(object2["config"]);
            case "docusign":
              return docusignConfig_1.DocusignConfigSerializer._fromJsonObject(object2["config"]);
            case "easypost":
              return easypostConfig_1.EasypostConfigSerializer._fromJsonObject(object2["config"]);
            case "github":
              return githubConfig_1.GithubConfigSerializer._fromJsonObject(object2["config"]);
            case "guesty":
              return svixConfig_1.SvixConfigSerializer._fromJsonObject(object2["config"]);
            case "hubspot":
              return hubspotConfig_1.HubspotConfigSerializer._fromJsonObject(object2["config"]);
            case "incident-io":
              return svixConfig_1.SvixConfigSerializer._fromJsonObject(object2["config"]);
            case "lithic":
              return svixConfig_1.SvixConfigSerializer._fromJsonObject(object2["config"]);
            case "nash":
              return svixConfig_1.SvixConfigSerializer._fromJsonObject(object2["config"]);
            case "orum-io":
              return orumIoConfig_1.OrumIoConfigSerializer._fromJsonObject(object2["config"]);
            case "panda-doc":
              return pandaDocConfig_1.PandaDocConfigSerializer._fromJsonObject(object2["config"]);
            case "port-io":
              return portIoConfig_1.PortIoConfigSerializer._fromJsonObject(object2["config"]);
            case "pleo":
              return svixConfig_1.SvixConfigSerializer._fromJsonObject(object2["config"]);
            case "replicate":
              return svixConfig_1.SvixConfigSerializer._fromJsonObject(object2["config"]);
            case "resend":
              return svixConfig_1.SvixConfigSerializer._fromJsonObject(object2["config"]);
            case "rutter":
              return rutterConfig_1.RutterConfigSerializer._fromJsonObject(object2["config"]);
            case "safebase":
              return svixConfig_1.SvixConfigSerializer._fromJsonObject(object2["config"]);
            case "sardine":
              return svixConfig_1.SvixConfigSerializer._fromJsonObject(object2["config"]);
            case "segment":
              return segmentConfig_1.SegmentConfigSerializer._fromJsonObject(object2["config"]);
            case "shopify":
              return shopifyConfig_1.ShopifyConfigSerializer._fromJsonObject(object2["config"]);
            case "slack":
              return slackConfig_1.SlackConfigSerializer._fromJsonObject(object2["config"]);
            case "stripe":
              return stripeConfig_1.StripeConfigSerializer._fromJsonObject(object2["config"]);
            case "stych":
              return svixConfig_1.SvixConfigSerializer._fromJsonObject(object2["config"]);
            case "svix":
              return svixConfig_1.SvixConfigSerializer._fromJsonObject(object2["config"]);
            case "zoom":
              return zoomConfig_1.ZoomConfigSerializer._fromJsonObject(object2["config"]);
            case "telnyx":
              return telnyxConfig_1.TelnyxConfigSerializer._fromJsonObject(object2["config"]);
            case "vapi":
              return vapiConfig_1.VapiConfigSerializer._fromJsonObject(object2["config"]);
            case "open-ai":
              return svixConfig_1.SvixConfigSerializer._fromJsonObject(object2["config"]);
            case "render":
              return svixConfig_1.SvixConfigSerializer._fromJsonObject(object2["config"]);
            case "veriff":
              return veriffConfig_1.VeriffConfigSerializer._fromJsonObject(object2["config"]);
            case "airwallex":
              return airwallexConfig_1.AirwallexConfigSerializer._fromJsonObject(object2["config"]);
            default:
              throw new Error(`Unexpected type: ${type2}`);
          }
        }
        __name(getConfig, "getConfig");
        return {
          type,
          config: getConfig(type),
          metadata: object2["metadata"],
          name: object2["name"],
          uid: object2["uid"]
        };
      },
      _toJsonObject(self2) {
        let config;
        switch (self2.type) {
          case "generic-webhook":
            config = {};
            break;
          case "cron":
            config = cronConfig_1.CronConfigSerializer._toJsonObject(self2.config);
            break;
          case "adobe-sign":
            config = adobeSignConfig_1.AdobeSignConfigSerializer._toJsonObject(self2.config);
            break;
          case "beehiiv":
            config = svixConfig_1.SvixConfigSerializer._toJsonObject(self2.config);
            break;
          case "brex":
            config = svixConfig_1.SvixConfigSerializer._toJsonObject(self2.config);
            break;
          case "checkbook":
            config = checkbookConfig_1.CheckbookConfigSerializer._toJsonObject(self2.config);
            break;
          case "clerk":
            config = svixConfig_1.SvixConfigSerializer._toJsonObject(self2.config);
            break;
          case "docusign":
            config = docusignConfig_1.DocusignConfigSerializer._toJsonObject(self2.config);
            break;
          case "easypost":
            config = easypostConfig_1.EasypostConfigSerializer._toJsonObject(self2.config);
            break;
          case "github":
            config = githubConfig_1.GithubConfigSerializer._toJsonObject(self2.config);
            break;
          case "guesty":
            config = svixConfig_1.SvixConfigSerializer._toJsonObject(self2.config);
            break;
          case "hubspot":
            config = hubspotConfig_1.HubspotConfigSerializer._toJsonObject(self2.config);
            break;
          case "incident-io":
            config = svixConfig_1.SvixConfigSerializer._toJsonObject(self2.config);
            break;
          case "lithic":
            config = svixConfig_1.SvixConfigSerializer._toJsonObject(self2.config);
            break;
          case "nash":
            config = svixConfig_1.SvixConfigSerializer._toJsonObject(self2.config);
            break;
          case "orum-io":
            config = orumIoConfig_1.OrumIoConfigSerializer._toJsonObject(self2.config);
            break;
          case "panda-doc":
            config = pandaDocConfig_1.PandaDocConfigSerializer._toJsonObject(self2.config);
            break;
          case "port-io":
            config = portIoConfig_1.PortIoConfigSerializer._toJsonObject(self2.config);
            break;
          case "pleo":
            config = svixConfig_1.SvixConfigSerializer._toJsonObject(self2.config);
            break;
          case "replicate":
            config = svixConfig_1.SvixConfigSerializer._toJsonObject(self2.config);
            break;
          case "resend":
            config = svixConfig_1.SvixConfigSerializer._toJsonObject(self2.config);
            break;
          case "rutter":
            config = rutterConfig_1.RutterConfigSerializer._toJsonObject(self2.config);
            break;
          case "safebase":
            config = svixConfig_1.SvixConfigSerializer._toJsonObject(self2.config);
            break;
          case "sardine":
            config = svixConfig_1.SvixConfigSerializer._toJsonObject(self2.config);
            break;
          case "segment":
            config = segmentConfig_1.SegmentConfigSerializer._toJsonObject(self2.config);
            break;
          case "shopify":
            config = shopifyConfig_1.ShopifyConfigSerializer._toJsonObject(self2.config);
            break;
          case "slack":
            config = slackConfig_1.SlackConfigSerializer._toJsonObject(self2.config);
            break;
          case "stripe":
            config = stripeConfig_1.StripeConfigSerializer._toJsonObject(self2.config);
            break;
          case "stych":
            config = svixConfig_1.SvixConfigSerializer._toJsonObject(self2.config);
            break;
          case "svix":
            config = svixConfig_1.SvixConfigSerializer._toJsonObject(self2.config);
            break;
          case "zoom":
            config = zoomConfig_1.ZoomConfigSerializer._toJsonObject(self2.config);
            break;
          case "telnyx":
            config = telnyxConfig_1.TelnyxConfigSerializer._toJsonObject(self2.config);
            break;
          case "vapi":
            config = vapiConfig_1.VapiConfigSerializer._toJsonObject(self2.config);
            break;
          case "open-ai":
            config = svixConfig_1.SvixConfigSerializer._toJsonObject(self2.config);
            break;
          case "render":
            config = svixConfig_1.SvixConfigSerializer._toJsonObject(self2.config);
            break;
          case "veriff":
            config = veriffConfig_1.VeriffConfigSerializer._toJsonObject(self2.config);
            break;
          case "airwallex":
            config = airwallexConfig_1.AirwallexConfigSerializer._toJsonObject(self2.config);
            break;
        }
        return {
          type: self2.type,
          config,
          metadata: self2.metadata,
          name: self2.name,
          uid: self2.uid
        };
      }
    };
  }
});

// node_modules/svix/dist/models/adobeSignConfigOut.js
var require_adobeSignConfigOut = __commonJS({
  "node_modules/svix/dist/models/adobeSignConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AdobeSignConfigOutSerializer = void 0;
    exports.AdobeSignConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/airwallexConfigOut.js
var require_airwallexConfigOut = __commonJS({
  "node_modules/svix/dist/models/airwallexConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AirwallexConfigOutSerializer = void 0;
    exports.AirwallexConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/checkbookConfigOut.js
var require_checkbookConfigOut = __commonJS({
  "node_modules/svix/dist/models/checkbookConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.CheckbookConfigOutSerializer = void 0;
    exports.CheckbookConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/docusignConfigOut.js
var require_docusignConfigOut = __commonJS({
  "node_modules/svix/dist/models/docusignConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.DocusignConfigOutSerializer = void 0;
    exports.DocusignConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/easypostConfigOut.js
var require_easypostConfigOut = __commonJS({
  "node_modules/svix/dist/models/easypostConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EasypostConfigOutSerializer = void 0;
    exports.EasypostConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/githubConfigOut.js
var require_githubConfigOut = __commonJS({
  "node_modules/svix/dist/models/githubConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.GithubConfigOutSerializer = void 0;
    exports.GithubConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/hubspotConfigOut.js
var require_hubspotConfigOut = __commonJS({
  "node_modules/svix/dist/models/hubspotConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.HubspotConfigOutSerializer = void 0;
    exports.HubspotConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/orumIoConfigOut.js
var require_orumIoConfigOut = __commonJS({
  "node_modules/svix/dist/models/orumIoConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OrumIoConfigOutSerializer = void 0;
    exports.OrumIoConfigOutSerializer = {
      _fromJsonObject(object2) {
        return {
          publicKey: object2["publicKey"]
        };
      },
      _toJsonObject(self2) {
        return {
          publicKey: self2.publicKey
        };
      }
    };
  }
});

// node_modules/svix/dist/models/pandaDocConfigOut.js
var require_pandaDocConfigOut = __commonJS({
  "node_modules/svix/dist/models/pandaDocConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PandaDocConfigOutSerializer = void 0;
    exports.PandaDocConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/portIoConfigOut.js
var require_portIoConfigOut = __commonJS({
  "node_modules/svix/dist/models/portIoConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PortIoConfigOutSerializer = void 0;
    exports.PortIoConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/rutterConfigOut.js
var require_rutterConfigOut = __commonJS({
  "node_modules/svix/dist/models/rutterConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.RutterConfigOutSerializer = void 0;
    exports.RutterConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/segmentConfigOut.js
var require_segmentConfigOut = __commonJS({
  "node_modules/svix/dist/models/segmentConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SegmentConfigOutSerializer = void 0;
    exports.SegmentConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/shopifyConfigOut.js
var require_shopifyConfigOut = __commonJS({
  "node_modules/svix/dist/models/shopifyConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ShopifyConfigOutSerializer = void 0;
    exports.ShopifyConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/slackConfigOut.js
var require_slackConfigOut = __commonJS({
  "node_modules/svix/dist/models/slackConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SlackConfigOutSerializer = void 0;
    exports.SlackConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/stripeConfigOut.js
var require_stripeConfigOut = __commonJS({
  "node_modules/svix/dist/models/stripeConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StripeConfigOutSerializer = void 0;
    exports.StripeConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/svixConfigOut.js
var require_svixConfigOut = __commonJS({
  "node_modules/svix/dist/models/svixConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SvixConfigOutSerializer = void 0;
    exports.SvixConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/telnyxConfigOut.js
var require_telnyxConfigOut = __commonJS({
  "node_modules/svix/dist/models/telnyxConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.TelnyxConfigOutSerializer = void 0;
    exports.TelnyxConfigOutSerializer = {
      _fromJsonObject(object2) {
        return {
          publicKey: object2["publicKey"]
        };
      },
      _toJsonObject(self2) {
        return {
          publicKey: self2.publicKey
        };
      }
    };
  }
});

// node_modules/svix/dist/models/vapiConfigOut.js
var require_vapiConfigOut = __commonJS({
  "node_modules/svix/dist/models/vapiConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.VapiConfigOutSerializer = void 0;
    exports.VapiConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/veriffConfigOut.js
var require_veriffConfigOut = __commonJS({
  "node_modules/svix/dist/models/veriffConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.VeriffConfigOutSerializer = void 0;
    exports.VeriffConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/zoomConfigOut.js
var require_zoomConfigOut = __commonJS({
  "node_modules/svix/dist/models/zoomConfigOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ZoomConfigOutSerializer = void 0;
    exports.ZoomConfigOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/ingestSourceOut.js
var require_ingestSourceOut = __commonJS({
  "node_modules/svix/dist/models/ingestSourceOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IngestSourceOutSerializer = void 0;
    var adobeSignConfigOut_1 = require_adobeSignConfigOut();
    var airwallexConfigOut_1 = require_airwallexConfigOut();
    var checkbookConfigOut_1 = require_checkbookConfigOut();
    var cronConfig_1 = require_cronConfig();
    var docusignConfigOut_1 = require_docusignConfigOut();
    var easypostConfigOut_1 = require_easypostConfigOut();
    var githubConfigOut_1 = require_githubConfigOut();
    var hubspotConfigOut_1 = require_hubspotConfigOut();
    var orumIoConfigOut_1 = require_orumIoConfigOut();
    var pandaDocConfigOut_1 = require_pandaDocConfigOut();
    var portIoConfigOut_1 = require_portIoConfigOut();
    var rutterConfigOut_1 = require_rutterConfigOut();
    var segmentConfigOut_1 = require_segmentConfigOut();
    var shopifyConfigOut_1 = require_shopifyConfigOut();
    var slackConfigOut_1 = require_slackConfigOut();
    var stripeConfigOut_1 = require_stripeConfigOut();
    var svixConfigOut_1 = require_svixConfigOut();
    var telnyxConfigOut_1 = require_telnyxConfigOut();
    var vapiConfigOut_1 = require_vapiConfigOut();
    var veriffConfigOut_1 = require_veriffConfigOut();
    var zoomConfigOut_1 = require_zoomConfigOut();
    exports.IngestSourceOutSerializer = {
      _fromJsonObject(object2) {
        const type = object2["type"];
        function getConfig(type2) {
          switch (type2) {
            case "generic-webhook":
              return {};
            case "cron":
              return cronConfig_1.CronConfigSerializer._fromJsonObject(object2["config"]);
            case "adobe-sign":
              return adobeSignConfigOut_1.AdobeSignConfigOutSerializer._fromJsonObject(object2["config"]);
            case "beehiiv":
              return svixConfigOut_1.SvixConfigOutSerializer._fromJsonObject(object2["config"]);
            case "brex":
              return svixConfigOut_1.SvixConfigOutSerializer._fromJsonObject(object2["config"]);
            case "checkbook":
              return checkbookConfigOut_1.CheckbookConfigOutSerializer._fromJsonObject(object2["config"]);
            case "clerk":
              return svixConfigOut_1.SvixConfigOutSerializer._fromJsonObject(object2["config"]);
            case "docusign":
              return docusignConfigOut_1.DocusignConfigOutSerializer._fromJsonObject(object2["config"]);
            case "easypost":
              return easypostConfigOut_1.EasypostConfigOutSerializer._fromJsonObject(object2["config"]);
            case "github":
              return githubConfigOut_1.GithubConfigOutSerializer._fromJsonObject(object2["config"]);
            case "guesty":
              return svixConfigOut_1.SvixConfigOutSerializer._fromJsonObject(object2["config"]);
            case "hubspot":
              return hubspotConfigOut_1.HubspotConfigOutSerializer._fromJsonObject(object2["config"]);
            case "incident-io":
              return svixConfigOut_1.SvixConfigOutSerializer._fromJsonObject(object2["config"]);
            case "lithic":
              return svixConfigOut_1.SvixConfigOutSerializer._fromJsonObject(object2["config"]);
            case "nash":
              return svixConfigOut_1.SvixConfigOutSerializer._fromJsonObject(object2["config"]);
            case "orum-io":
              return orumIoConfigOut_1.OrumIoConfigOutSerializer._fromJsonObject(object2["config"]);
            case "panda-doc":
              return pandaDocConfigOut_1.PandaDocConfigOutSerializer._fromJsonObject(object2["config"]);
            case "port-io":
              return portIoConfigOut_1.PortIoConfigOutSerializer._fromJsonObject(object2["config"]);
            case "pleo":
              return svixConfigOut_1.SvixConfigOutSerializer._fromJsonObject(object2["config"]);
            case "replicate":
              return svixConfigOut_1.SvixConfigOutSerializer._fromJsonObject(object2["config"]);
            case "resend":
              return svixConfigOut_1.SvixConfigOutSerializer._fromJsonObject(object2["config"]);
            case "rutter":
              return rutterConfigOut_1.RutterConfigOutSerializer._fromJsonObject(object2["config"]);
            case "safebase":
              return svixConfigOut_1.SvixConfigOutSerializer._fromJsonObject(object2["config"]);
            case "sardine":
              return svixConfigOut_1.SvixConfigOutSerializer._fromJsonObject(object2["config"]);
            case "segment":
              return segmentConfigOut_1.SegmentConfigOutSerializer._fromJsonObject(object2["config"]);
            case "shopify":
              return shopifyConfigOut_1.ShopifyConfigOutSerializer._fromJsonObject(object2["config"]);
            case "slack":
              return slackConfigOut_1.SlackConfigOutSerializer._fromJsonObject(object2["config"]);
            case "stripe":
              return stripeConfigOut_1.StripeConfigOutSerializer._fromJsonObject(object2["config"]);
            case "stych":
              return svixConfigOut_1.SvixConfigOutSerializer._fromJsonObject(object2["config"]);
            case "svix":
              return svixConfigOut_1.SvixConfigOutSerializer._fromJsonObject(object2["config"]);
            case "zoom":
              return zoomConfigOut_1.ZoomConfigOutSerializer._fromJsonObject(object2["config"]);
            case "telnyx":
              return telnyxConfigOut_1.TelnyxConfigOutSerializer._fromJsonObject(object2["config"]);
            case "vapi":
              return vapiConfigOut_1.VapiConfigOutSerializer._fromJsonObject(object2["config"]);
            case "open-ai":
              return svixConfigOut_1.SvixConfigOutSerializer._fromJsonObject(object2["config"]);
            case "render":
              return svixConfigOut_1.SvixConfigOutSerializer._fromJsonObject(object2["config"]);
            case "veriff":
              return veriffConfigOut_1.VeriffConfigOutSerializer._fromJsonObject(object2["config"]);
            case "airwallex":
              return airwallexConfigOut_1.AirwallexConfigOutSerializer._fromJsonObject(object2["config"]);
            default:
              throw new Error(`Unexpected type: ${type2}`);
          }
        }
        __name(getConfig, "getConfig");
        return {
          type,
          config: getConfig(type),
          createdAt: new Date(object2["createdAt"]),
          id: object2["id"],
          ingestUrl: object2["ingestUrl"],
          metadata: object2["metadata"],
          name: object2["name"],
          uid: object2["uid"],
          updatedAt: new Date(object2["updatedAt"])
        };
      },
      _toJsonObject(self2) {
        let config;
        switch (self2.type) {
          case "generic-webhook":
            config = {};
            break;
          case "cron":
            config = cronConfig_1.CronConfigSerializer._toJsonObject(self2.config);
            break;
          case "adobe-sign":
            config = adobeSignConfigOut_1.AdobeSignConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "beehiiv":
            config = svixConfigOut_1.SvixConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "brex":
            config = svixConfigOut_1.SvixConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "checkbook":
            config = checkbookConfigOut_1.CheckbookConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "clerk":
            config = svixConfigOut_1.SvixConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "docusign":
            config = docusignConfigOut_1.DocusignConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "easypost":
            config = easypostConfigOut_1.EasypostConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "github":
            config = githubConfigOut_1.GithubConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "guesty":
            config = svixConfigOut_1.SvixConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "hubspot":
            config = hubspotConfigOut_1.HubspotConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "incident-io":
            config = svixConfigOut_1.SvixConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "lithic":
            config = svixConfigOut_1.SvixConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "nash":
            config = svixConfigOut_1.SvixConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "orum-io":
            config = orumIoConfigOut_1.OrumIoConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "panda-doc":
            config = pandaDocConfigOut_1.PandaDocConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "port-io":
            config = portIoConfigOut_1.PortIoConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "pleo":
            config = svixConfigOut_1.SvixConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "replicate":
            config = svixConfigOut_1.SvixConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "resend":
            config = svixConfigOut_1.SvixConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "rutter":
            config = rutterConfigOut_1.RutterConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "safebase":
            config = svixConfigOut_1.SvixConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "sardine":
            config = svixConfigOut_1.SvixConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "segment":
            config = segmentConfigOut_1.SegmentConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "shopify":
            config = shopifyConfigOut_1.ShopifyConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "slack":
            config = slackConfigOut_1.SlackConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "stripe":
            config = stripeConfigOut_1.StripeConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "stych":
            config = svixConfigOut_1.SvixConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "svix":
            config = svixConfigOut_1.SvixConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "zoom":
            config = zoomConfigOut_1.ZoomConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "telnyx":
            config = telnyxConfigOut_1.TelnyxConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "vapi":
            config = vapiConfigOut_1.VapiConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "open-ai":
            config = svixConfigOut_1.SvixConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "render":
            config = svixConfigOut_1.SvixConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "veriff":
            config = veriffConfigOut_1.VeriffConfigOutSerializer._toJsonObject(self2.config);
            break;
          case "airwallex":
            config = airwallexConfigOut_1.AirwallexConfigOutSerializer._toJsonObject(self2.config);
            break;
        }
        return {
          type: self2.type,
          config,
          createdAt: self2.createdAt,
          id: self2.id,
          ingestUrl: self2.ingestUrl,
          metadata: self2.metadata,
          name: self2.name,
          uid: self2.uid,
          updatedAt: self2.updatedAt
        };
      }
    };
  }
});

// node_modules/svix/dist/models/listResponseIngestSourceOut.js
var require_listResponseIngestSourceOut = __commonJS({
  "node_modules/svix/dist/models/listResponseIngestSourceOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ListResponseIngestSourceOutSerializer = void 0;
    var ingestSourceOut_1 = require_ingestSourceOut();
    exports.ListResponseIngestSourceOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => ingestSourceOut_1.IngestSourceOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"],
          prevIterator: object2["prevIterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => ingestSourceOut_1.IngestSourceOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator,
          prevIterator: self2.prevIterator
        };
      }
    };
  }
});

// node_modules/svix/dist/models/rotateTokenOut.js
var require_rotateTokenOut = __commonJS({
  "node_modules/svix/dist/models/rotateTokenOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.RotateTokenOutSerializer = void 0;
    exports.RotateTokenOutSerializer = {
      _fromJsonObject(object2) {
        return {
          ingestUrl: object2["ingestUrl"]
        };
      },
      _toJsonObject(self2) {
        return {
          ingestUrl: self2.ingestUrl
        };
      }
    };
  }
});

// node_modules/svix/dist/api/ingestSource.js
var require_ingestSource = __commonJS({
  "node_modules/svix/dist/api/ingestSource.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IngestSource = void 0;
    var ingestSourceIn_1 = require_ingestSourceIn();
    var ingestSourceOut_1 = require_ingestSourceOut();
    var listResponseIngestSourceOut_1 = require_listResponseIngestSourceOut();
    var rotateTokenOut_1 = require_rotateTokenOut();
    var request_1 = require_request();
    var IngestSource = class {
      static {
        __name(this, "IngestSource");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      list(options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/ingest/api/v1/source");
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          order: options === null || options === void 0 ? void 0 : options.order
        });
        return request.send(this.requestCtx, listResponseIngestSourceOut_1.ListResponseIngestSourceOutSerializer._fromJsonObject);
      }
      create(ingestSourceIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/ingest/api/v1/source");
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(ingestSourceIn_1.IngestSourceInSerializer._toJsonObject(ingestSourceIn));
        return request.send(this.requestCtx, ingestSourceOut_1.IngestSourceOutSerializer._fromJsonObject);
      }
      get(sourceId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/ingest/api/v1/source/{source_id}");
        request.setPathParam("source_id", sourceId);
        return request.send(this.requestCtx, ingestSourceOut_1.IngestSourceOutSerializer._fromJsonObject);
      }
      update(sourceId, ingestSourceIn) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PUT, "/ingest/api/v1/source/{source_id}");
        request.setPathParam("source_id", sourceId);
        request.setBody(ingestSourceIn_1.IngestSourceInSerializer._toJsonObject(ingestSourceIn));
        return request.send(this.requestCtx, ingestSourceOut_1.IngestSourceOutSerializer._fromJsonObject);
      }
      delete(sourceId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.DELETE, "/ingest/api/v1/source/{source_id}");
        request.setPathParam("source_id", sourceId);
        return request.sendNoResponseBody(this.requestCtx);
      }
      rotateToken(sourceId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/ingest/api/v1/source/{source_id}/token/rotate");
        request.setPathParam("source_id", sourceId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        return request.send(this.requestCtx, rotateTokenOut_1.RotateTokenOutSerializer._fromJsonObject);
      }
    };
    exports.IngestSource = IngestSource;
  }
});

// node_modules/svix/dist/api/ingest.js
var require_ingest = __commonJS({
  "node_modules/svix/dist/api/ingest.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Ingest = void 0;
    var dashboardAccessOut_1 = require_dashboardAccessOut();
    var ingestSourceConsumerPortalAccessIn_1 = require_ingestSourceConsumerPortalAccessIn();
    var ingestEndpoint_1 = require_ingestEndpoint();
    var ingestSource_1 = require_ingestSource();
    var request_1 = require_request();
    var Ingest = class {
      static {
        __name(this, "Ingest");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      get endpoint() {
        return new ingestEndpoint_1.IngestEndpoint(this.requestCtx);
      }
      get source() {
        return new ingestSource_1.IngestSource(this.requestCtx);
      }
      dashboard(sourceId, ingestSourceConsumerPortalAccessIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/ingest/api/v1/source/{source_id}/dashboard");
        request.setPathParam("source_id", sourceId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(ingestSourceConsumerPortalAccessIn_1.IngestSourceConsumerPortalAccessInSerializer._toJsonObject(ingestSourceConsumerPortalAccessIn));
        return request.send(this.requestCtx, dashboardAccessOut_1.DashboardAccessOutSerializer._fromJsonObject);
      }
    };
    exports.Ingest = Ingest;
  }
});

// node_modules/svix/dist/models/integrationIn.js
var require_integrationIn = __commonJS({
  "node_modules/svix/dist/models/integrationIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IntegrationInSerializer = void 0;
    exports.IntegrationInSerializer = {
      _fromJsonObject(object2) {
        return {
          featureFlags: object2["featureFlags"],
          name: object2["name"]
        };
      },
      _toJsonObject(self2) {
        return {
          featureFlags: self2.featureFlags,
          name: self2.name
        };
      }
    };
  }
});

// node_modules/svix/dist/models/integrationKeyOut.js
var require_integrationKeyOut = __commonJS({
  "node_modules/svix/dist/models/integrationKeyOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IntegrationKeyOutSerializer = void 0;
    exports.IntegrationKeyOutSerializer = {
      _fromJsonObject(object2) {
        return {
          key: object2["key"]
        };
      },
      _toJsonObject(self2) {
        return {
          key: self2.key
        };
      }
    };
  }
});

// node_modules/svix/dist/models/integrationOut.js
var require_integrationOut = __commonJS({
  "node_modules/svix/dist/models/integrationOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IntegrationOutSerializer = void 0;
    exports.IntegrationOutSerializer = {
      _fromJsonObject(object2) {
        return {
          createdAt: new Date(object2["createdAt"]),
          featureFlags: object2["featureFlags"],
          id: object2["id"],
          name: object2["name"],
          updatedAt: new Date(object2["updatedAt"])
        };
      },
      _toJsonObject(self2) {
        return {
          createdAt: self2.createdAt,
          featureFlags: self2.featureFlags,
          id: self2.id,
          name: self2.name,
          updatedAt: self2.updatedAt
        };
      }
    };
  }
});

// node_modules/svix/dist/models/integrationUpdate.js
var require_integrationUpdate = __commonJS({
  "node_modules/svix/dist/models/integrationUpdate.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IntegrationUpdateSerializer = void 0;
    exports.IntegrationUpdateSerializer = {
      _fromJsonObject(object2) {
        return {
          featureFlags: object2["featureFlags"],
          name: object2["name"]
        };
      },
      _toJsonObject(self2) {
        return {
          featureFlags: self2.featureFlags,
          name: self2.name
        };
      }
    };
  }
});

// node_modules/svix/dist/models/listResponseIntegrationOut.js
var require_listResponseIntegrationOut = __commonJS({
  "node_modules/svix/dist/models/listResponseIntegrationOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ListResponseIntegrationOutSerializer = void 0;
    var integrationOut_1 = require_integrationOut();
    exports.ListResponseIntegrationOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => integrationOut_1.IntegrationOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"],
          prevIterator: object2["prevIterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => integrationOut_1.IntegrationOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator,
          prevIterator: self2.prevIterator
        };
      }
    };
  }
});

// node_modules/svix/dist/api/integration.js
var require_integration = __commonJS({
  "node_modules/svix/dist/api/integration.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Integration = void 0;
    var integrationIn_1 = require_integrationIn();
    var integrationKeyOut_1 = require_integrationKeyOut();
    var integrationOut_1 = require_integrationOut();
    var integrationUpdate_1 = require_integrationUpdate();
    var listResponseIntegrationOut_1 = require_listResponseIntegrationOut();
    var request_1 = require_request();
    var Integration = class {
      static {
        __name(this, "Integration");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      list(appId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/integration");
        request.setPathParam("app_id", appId);
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          order: options === null || options === void 0 ? void 0 : options.order
        });
        return request.send(this.requestCtx, listResponseIntegrationOut_1.ListResponseIntegrationOutSerializer._fromJsonObject);
      }
      create(appId, integrationIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/app/{app_id}/integration");
        request.setPathParam("app_id", appId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(integrationIn_1.IntegrationInSerializer._toJsonObject(integrationIn));
        return request.send(this.requestCtx, integrationOut_1.IntegrationOutSerializer._fromJsonObject);
      }
      get(appId, integId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/integration/{integ_id}");
        request.setPathParam("app_id", appId);
        request.setPathParam("integ_id", integId);
        return request.send(this.requestCtx, integrationOut_1.IntegrationOutSerializer._fromJsonObject);
      }
      update(appId, integId, integrationUpdate) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PUT, "/api/v1/app/{app_id}/integration/{integ_id}");
        request.setPathParam("app_id", appId);
        request.setPathParam("integ_id", integId);
        request.setBody(integrationUpdate_1.IntegrationUpdateSerializer._toJsonObject(integrationUpdate));
        return request.send(this.requestCtx, integrationOut_1.IntegrationOutSerializer._fromJsonObject);
      }
      delete(appId, integId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.DELETE, "/api/v1/app/{app_id}/integration/{integ_id}");
        request.setPathParam("app_id", appId);
        request.setPathParam("integ_id", integId);
        return request.sendNoResponseBody(this.requestCtx);
      }
      getKey(appId, integId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/integration/{integ_id}/key");
        request.setPathParam("app_id", appId);
        request.setPathParam("integ_id", integId);
        return request.send(this.requestCtx, integrationKeyOut_1.IntegrationKeyOutSerializer._fromJsonObject);
      }
      rotateKey(appId, integId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/app/{app_id}/integration/{integ_id}/key/rotate");
        request.setPathParam("app_id", appId);
        request.setPathParam("integ_id", integId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        return request.send(this.requestCtx, integrationKeyOut_1.IntegrationKeyOutSerializer._fromJsonObject);
      }
    };
    exports.Integration = Integration;
  }
});

// node_modules/svix/dist/models/expungeAllContentsOut.js
var require_expungeAllContentsOut = __commonJS({
  "node_modules/svix/dist/models/expungeAllContentsOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ExpungeAllContentsOutSerializer = void 0;
    var backgroundTaskStatus_1 = require_backgroundTaskStatus();
    var backgroundTaskType_1 = require_backgroundTaskType();
    exports.ExpungeAllContentsOutSerializer = {
      _fromJsonObject(object2) {
        return {
          id: object2["id"],
          status: backgroundTaskStatus_1.BackgroundTaskStatusSerializer._fromJsonObject(object2["status"]),
          task: backgroundTaskType_1.BackgroundTaskTypeSerializer._fromJsonObject(object2["task"])
        };
      },
      _toJsonObject(self2) {
        return {
          id: self2.id,
          status: backgroundTaskStatus_1.BackgroundTaskStatusSerializer._toJsonObject(self2.status),
          task: backgroundTaskType_1.BackgroundTaskTypeSerializer._toJsonObject(self2.task)
        };
      }
    };
  }
});

// node_modules/svix/dist/models/listResponseMessageOut.js
var require_listResponseMessageOut = __commonJS({
  "node_modules/svix/dist/models/listResponseMessageOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ListResponseMessageOutSerializer = void 0;
    var messageOut_1 = require_messageOut();
    exports.ListResponseMessageOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => messageOut_1.MessageOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"],
          prevIterator: object2["prevIterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => messageOut_1.MessageOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator,
          prevIterator: self2.prevIterator
        };
      }
    };
  }
});

// node_modules/svix/dist/models/pollingEndpointConsumerSeekIn.js
var require_pollingEndpointConsumerSeekIn = __commonJS({
  "node_modules/svix/dist/models/pollingEndpointConsumerSeekIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PollingEndpointConsumerSeekInSerializer = void 0;
    exports.PollingEndpointConsumerSeekInSerializer = {
      _fromJsonObject(object2) {
        return {
          after: new Date(object2["after"])
        };
      },
      _toJsonObject(self2) {
        return {
          after: self2.after
        };
      }
    };
  }
});

// node_modules/svix/dist/models/pollingEndpointConsumerSeekOut.js
var require_pollingEndpointConsumerSeekOut = __commonJS({
  "node_modules/svix/dist/models/pollingEndpointConsumerSeekOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PollingEndpointConsumerSeekOutSerializer = void 0;
    exports.PollingEndpointConsumerSeekOutSerializer = {
      _fromJsonObject(object2) {
        return {
          iterator: object2["iterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          iterator: self2.iterator
        };
      }
    };
  }
});

// node_modules/svix/dist/models/pollingEndpointMessageOut.js
var require_pollingEndpointMessageOut = __commonJS({
  "node_modules/svix/dist/models/pollingEndpointMessageOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PollingEndpointMessageOutSerializer = void 0;
    exports.PollingEndpointMessageOutSerializer = {
      _fromJsonObject(object2) {
        return {
          channels: object2["channels"],
          deliverAt: object2["deliverAt"] ? new Date(object2["deliverAt"]) : null,
          eventId: object2["eventId"],
          eventType: object2["eventType"],
          headers: object2["headers"],
          id: object2["id"],
          payload: object2["payload"],
          tags: object2["tags"],
          timestamp: new Date(object2["timestamp"])
        };
      },
      _toJsonObject(self2) {
        return {
          channels: self2.channels,
          deliverAt: self2.deliverAt,
          eventId: self2.eventId,
          eventType: self2.eventType,
          headers: self2.headers,
          id: self2.id,
          payload: self2.payload,
          tags: self2.tags,
          timestamp: self2.timestamp
        };
      }
    };
  }
});

// node_modules/svix/dist/models/pollingEndpointOut.js
var require_pollingEndpointOut = __commonJS({
  "node_modules/svix/dist/models/pollingEndpointOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PollingEndpointOutSerializer = void 0;
    var pollingEndpointMessageOut_1 = require_pollingEndpointMessageOut();
    exports.PollingEndpointOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => pollingEndpointMessageOut_1.PollingEndpointMessageOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => pollingEndpointMessageOut_1.PollingEndpointMessageOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator
        };
      }
    };
  }
});

// node_modules/svix/dist/api/messagePoller.js
var require_messagePoller = __commonJS({
  "node_modules/svix/dist/api/messagePoller.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MessagePoller = void 0;
    var pollingEndpointConsumerSeekIn_1 = require_pollingEndpointConsumerSeekIn();
    var pollingEndpointConsumerSeekOut_1 = require_pollingEndpointConsumerSeekOut();
    var pollingEndpointOut_1 = require_pollingEndpointOut();
    var request_1 = require_request();
    var MessagePoller = class {
      static {
        __name(this, "MessagePoller");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      poll(appId, sinkId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/poller/{sink_id}");
        request.setPathParam("app_id", appId);
        request.setPathParam("sink_id", sinkId);
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          event_type: options === null || options === void 0 ? void 0 : options.eventType,
          channel: options === null || options === void 0 ? void 0 : options.channel,
          after: options === null || options === void 0 ? void 0 : options.after
        });
        return request.send(this.requestCtx, pollingEndpointOut_1.PollingEndpointOutSerializer._fromJsonObject);
      }
      consumerPoll(appId, sinkId, consumerId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/poller/{sink_id}/consumer/{consumer_id}");
        request.setPathParam("app_id", appId);
        request.setPathParam("sink_id", sinkId);
        request.setPathParam("consumer_id", consumerId);
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator
        });
        return request.send(this.requestCtx, pollingEndpointOut_1.PollingEndpointOutSerializer._fromJsonObject);
      }
      consumerSeek(appId, sinkId, consumerId, pollingEndpointConsumerSeekIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/app/{app_id}/poller/{sink_id}/consumer/{consumer_id}/seek");
        request.setPathParam("app_id", appId);
        request.setPathParam("sink_id", sinkId);
        request.setPathParam("consumer_id", consumerId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(pollingEndpointConsumerSeekIn_1.PollingEndpointConsumerSeekInSerializer._toJsonObject(pollingEndpointConsumerSeekIn));
        return request.send(this.requestCtx, pollingEndpointConsumerSeekOut_1.PollingEndpointConsumerSeekOutSerializer._fromJsonObject);
      }
    };
    exports.MessagePoller = MessagePoller;
  }
});

// node_modules/svix/dist/models/messageIn.js
var require_messageIn = __commonJS({
  "node_modules/svix/dist/models/messageIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MessageInSerializer = void 0;
    var applicationIn_1 = require_applicationIn();
    exports.MessageInSerializer = {
      _fromJsonObject(object2) {
        return {
          application: object2["application"] ? applicationIn_1.ApplicationInSerializer._fromJsonObject(object2["application"]) : void 0,
          channels: object2["channels"],
          deliverAt: object2["deliverAt"] ? new Date(object2["deliverAt"]) : null,
          eventId: object2["eventId"],
          eventType: object2["eventType"],
          payload: object2["payload"],
          payloadRetentionHours: object2["payloadRetentionHours"],
          payloadRetentionPeriod: object2["payloadRetentionPeriod"],
          tags: object2["tags"],
          transformationsParams: object2["transformationsParams"]
        };
      },
      _toJsonObject(self2) {
        return {
          application: self2.application ? applicationIn_1.ApplicationInSerializer._toJsonObject(self2.application) : void 0,
          channels: self2.channels,
          deliverAt: self2.deliverAt,
          eventId: self2.eventId,
          eventType: self2.eventType,
          payload: self2.payload,
          payloadRetentionHours: self2.payloadRetentionHours,
          payloadRetentionPeriod: self2.payloadRetentionPeriod,
          tags: self2.tags,
          transformationsParams: self2.transformationsParams
        };
      }
    };
  }
});

// node_modules/svix/dist/api/message.js
var require_message = __commonJS({
  "node_modules/svix/dist/api/message.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.messageInRaw = exports.Message = void 0;
    var expungeAllContentsOut_1 = require_expungeAllContentsOut();
    var listResponseMessageOut_1 = require_listResponseMessageOut();
    var messageOut_1 = require_messageOut();
    var messagePoller_1 = require_messagePoller();
    var request_1 = require_request();
    var messageIn_1 = require_messageIn();
    var Message = class {
      static {
        __name(this, "Message");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      get poller() {
        return new messagePoller_1.MessagePoller(this.requestCtx);
      }
      list(appId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/msg");
        request.setPathParam("app_id", appId);
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          channel: options === null || options === void 0 ? void 0 : options.channel,
          before: options === null || options === void 0 ? void 0 : options.before,
          after: options === null || options === void 0 ? void 0 : options.after,
          with_content: options === null || options === void 0 ? void 0 : options.withContent,
          tag: options === null || options === void 0 ? void 0 : options.tag,
          event_types: options === null || options === void 0 ? void 0 : options.eventTypes
        });
        return request.send(this.requestCtx, listResponseMessageOut_1.ListResponseMessageOutSerializer._fromJsonObject);
      }
      create(appId, messageIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/app/{app_id}/msg");
        request.setPathParam("app_id", appId);
        request.setQueryParams({
          with_content: options === null || options === void 0 ? void 0 : options.withContent
        });
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(messageIn_1.MessageInSerializer._toJsonObject(messageIn));
        return request.send(this.requestCtx, messageOut_1.MessageOutSerializer._fromJsonObject);
      }
      expungeAllContents(appId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/app/{app_id}/msg/expunge-all-contents");
        request.setPathParam("app_id", appId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        return request.send(this.requestCtx, expungeAllContentsOut_1.ExpungeAllContentsOutSerializer._fromJsonObject);
      }
      get(appId, msgId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/msg/{msg_id}");
        request.setPathParam("app_id", appId);
        request.setPathParam("msg_id", msgId);
        request.setQueryParams({
          with_content: options === null || options === void 0 ? void 0 : options.withContent
        });
        return request.send(this.requestCtx, messageOut_1.MessageOutSerializer._fromJsonObject);
      }
      expungeContent(appId, msgId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.DELETE, "/api/v1/app/{app_id}/msg/{msg_id}/content");
        request.setPathParam("app_id", appId);
        request.setPathParam("msg_id", msgId);
        return request.sendNoResponseBody(this.requestCtx);
      }
    };
    exports.Message = Message;
    function messageInRaw(eventType, payload, contentType) {
      const headers = contentType ? { "content-type": contentType } : void 0;
      return {
        eventType,
        payload: {},
        transformationsParams: {
          rawPayload: payload,
          headers
        }
      };
    }
    __name(messageInRaw, "messageInRaw");
    exports.messageInRaw = messageInRaw;
  }
});

// node_modules/svix/dist/models/emptyResponse.js
var require_emptyResponse = __commonJS({
  "node_modules/svix/dist/models/emptyResponse.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EmptyResponseSerializer = void 0;
    exports.EmptyResponseSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/messageStatus.js
var require_messageStatus = __commonJS({
  "node_modules/svix/dist/models/messageStatus.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MessageStatusSerializer = exports.MessageStatus = void 0;
    var MessageStatus;
    (function(MessageStatus2) {
      MessageStatus2[MessageStatus2["Success"] = 0] = "Success";
      MessageStatus2[MessageStatus2["Pending"] = 1] = "Pending";
      MessageStatus2[MessageStatus2["Fail"] = 2] = "Fail";
      MessageStatus2[MessageStatus2["Sending"] = 3] = "Sending";
    })(MessageStatus = exports.MessageStatus || (exports.MessageStatus = {}));
    exports.MessageStatusSerializer = {
      _fromJsonObject(object2) {
        return object2;
      },
      _toJsonObject(self2) {
        return self2;
      }
    };
  }
});

// node_modules/svix/dist/models/messageStatusText.js
var require_messageStatusText = __commonJS({
  "node_modules/svix/dist/models/messageStatusText.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MessageStatusTextSerializer = exports.MessageStatusText = void 0;
    var MessageStatusText;
    (function(MessageStatusText2) {
      MessageStatusText2["Success"] = "success";
      MessageStatusText2["Pending"] = "pending";
      MessageStatusText2["Fail"] = "fail";
      MessageStatusText2["Sending"] = "sending";
    })(MessageStatusText = exports.MessageStatusText || (exports.MessageStatusText = {}));
    exports.MessageStatusTextSerializer = {
      _fromJsonObject(object2) {
        return object2;
      },
      _toJsonObject(self2) {
        return self2;
      }
    };
  }
});

// node_modules/svix/dist/models/endpointMessageOut.js
var require_endpointMessageOut = __commonJS({
  "node_modules/svix/dist/models/endpointMessageOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EndpointMessageOutSerializer = void 0;
    var messageStatus_1 = require_messageStatus();
    var messageStatusText_1 = require_messageStatusText();
    exports.EndpointMessageOutSerializer = {
      _fromJsonObject(object2) {
        return {
          channels: object2["channels"],
          deliverAt: object2["deliverAt"] ? new Date(object2["deliverAt"]) : null,
          eventId: object2["eventId"],
          eventType: object2["eventType"],
          id: object2["id"],
          nextAttempt: object2["nextAttempt"] ? new Date(object2["nextAttempt"]) : null,
          payload: object2["payload"],
          status: messageStatus_1.MessageStatusSerializer._fromJsonObject(object2["status"]),
          statusText: messageStatusText_1.MessageStatusTextSerializer._fromJsonObject(object2["statusText"]),
          tags: object2["tags"],
          timestamp: new Date(object2["timestamp"])
        };
      },
      _toJsonObject(self2) {
        return {
          channels: self2.channels,
          deliverAt: self2.deliverAt,
          eventId: self2.eventId,
          eventType: self2.eventType,
          id: self2.id,
          nextAttempt: self2.nextAttempt,
          payload: self2.payload,
          status: messageStatus_1.MessageStatusSerializer._toJsonObject(self2.status),
          statusText: messageStatusText_1.MessageStatusTextSerializer._toJsonObject(self2.statusText),
          tags: self2.tags,
          timestamp: self2.timestamp
        };
      }
    };
  }
});

// node_modules/svix/dist/models/listResponseEndpointMessageOut.js
var require_listResponseEndpointMessageOut = __commonJS({
  "node_modules/svix/dist/models/listResponseEndpointMessageOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ListResponseEndpointMessageOutSerializer = void 0;
    var endpointMessageOut_1 = require_endpointMessageOut();
    exports.ListResponseEndpointMessageOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => endpointMessageOut_1.EndpointMessageOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"],
          prevIterator: object2["prevIterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => endpointMessageOut_1.EndpointMessageOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator,
          prevIterator: self2.prevIterator
        };
      }
    };
  }
});

// node_modules/svix/dist/models/messageAttemptTriggerType.js
var require_messageAttemptTriggerType = __commonJS({
  "node_modules/svix/dist/models/messageAttemptTriggerType.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MessageAttemptTriggerTypeSerializer = exports.MessageAttemptTriggerType = void 0;
    var MessageAttemptTriggerType;
    (function(MessageAttemptTriggerType2) {
      MessageAttemptTriggerType2[MessageAttemptTriggerType2["Scheduled"] = 0] = "Scheduled";
      MessageAttemptTriggerType2[MessageAttemptTriggerType2["Manual"] = 1] = "Manual";
    })(MessageAttemptTriggerType = exports.MessageAttemptTriggerType || (exports.MessageAttemptTriggerType = {}));
    exports.MessageAttemptTriggerTypeSerializer = {
      _fromJsonObject(object2) {
        return object2;
      },
      _toJsonObject(self2) {
        return self2;
      }
    };
  }
});

// node_modules/svix/dist/models/messageAttemptOut.js
var require_messageAttemptOut = __commonJS({
  "node_modules/svix/dist/models/messageAttemptOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MessageAttemptOutSerializer = void 0;
    var messageAttemptTriggerType_1 = require_messageAttemptTriggerType();
    var messageOut_1 = require_messageOut();
    var messageStatus_1 = require_messageStatus();
    var messageStatusText_1 = require_messageStatusText();
    exports.MessageAttemptOutSerializer = {
      _fromJsonObject(object2) {
        return {
          endpointId: object2["endpointId"],
          id: object2["id"],
          msg: object2["msg"] ? messageOut_1.MessageOutSerializer._fromJsonObject(object2["msg"]) : void 0,
          msgId: object2["msgId"],
          response: object2["response"],
          responseDurationMs: object2["responseDurationMs"],
          responseStatusCode: object2["responseStatusCode"],
          status: messageStatus_1.MessageStatusSerializer._fromJsonObject(object2["status"]),
          statusText: messageStatusText_1.MessageStatusTextSerializer._fromJsonObject(object2["statusText"]),
          timestamp: new Date(object2["timestamp"]),
          triggerType: messageAttemptTriggerType_1.MessageAttemptTriggerTypeSerializer._fromJsonObject(object2["triggerType"]),
          url: object2["url"]
        };
      },
      _toJsonObject(self2) {
        return {
          endpointId: self2.endpointId,
          id: self2.id,
          msg: self2.msg ? messageOut_1.MessageOutSerializer._toJsonObject(self2.msg) : void 0,
          msgId: self2.msgId,
          response: self2.response,
          responseDurationMs: self2.responseDurationMs,
          responseStatusCode: self2.responseStatusCode,
          status: messageStatus_1.MessageStatusSerializer._toJsonObject(self2.status),
          statusText: messageStatusText_1.MessageStatusTextSerializer._toJsonObject(self2.statusText),
          timestamp: self2.timestamp,
          triggerType: messageAttemptTriggerType_1.MessageAttemptTriggerTypeSerializer._toJsonObject(self2.triggerType),
          url: self2.url
        };
      }
    };
  }
});

// node_modules/svix/dist/models/listResponseMessageAttemptOut.js
var require_listResponseMessageAttemptOut = __commonJS({
  "node_modules/svix/dist/models/listResponseMessageAttemptOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ListResponseMessageAttemptOutSerializer = void 0;
    var messageAttemptOut_1 = require_messageAttemptOut();
    exports.ListResponseMessageAttemptOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => messageAttemptOut_1.MessageAttemptOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"],
          prevIterator: object2["prevIterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => messageAttemptOut_1.MessageAttemptOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator,
          prevIterator: self2.prevIterator
        };
      }
    };
  }
});

// node_modules/svix/dist/models/messageEndpointOut.js
var require_messageEndpointOut = __commonJS({
  "node_modules/svix/dist/models/messageEndpointOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MessageEndpointOutSerializer = void 0;
    var messageStatus_1 = require_messageStatus();
    var messageStatusText_1 = require_messageStatusText();
    exports.MessageEndpointOutSerializer = {
      _fromJsonObject(object2) {
        return {
          channels: object2["channels"],
          createdAt: new Date(object2["createdAt"]),
          description: object2["description"],
          disabled: object2["disabled"],
          filterTypes: object2["filterTypes"],
          id: object2["id"],
          nextAttempt: object2["nextAttempt"] ? new Date(object2["nextAttempt"]) : null,
          rateLimit: object2["rateLimit"],
          status: messageStatus_1.MessageStatusSerializer._fromJsonObject(object2["status"]),
          statusText: messageStatusText_1.MessageStatusTextSerializer._fromJsonObject(object2["statusText"]),
          uid: object2["uid"],
          updatedAt: new Date(object2["updatedAt"]),
          url: object2["url"],
          version: object2["version"]
        };
      },
      _toJsonObject(self2) {
        return {
          channels: self2.channels,
          createdAt: self2.createdAt,
          description: self2.description,
          disabled: self2.disabled,
          filterTypes: self2.filterTypes,
          id: self2.id,
          nextAttempt: self2.nextAttempt,
          rateLimit: self2.rateLimit,
          status: messageStatus_1.MessageStatusSerializer._toJsonObject(self2.status),
          statusText: messageStatusText_1.MessageStatusTextSerializer._toJsonObject(self2.statusText),
          uid: self2.uid,
          updatedAt: self2.updatedAt,
          url: self2.url,
          version: self2.version
        };
      }
    };
  }
});

// node_modules/svix/dist/models/listResponseMessageEndpointOut.js
var require_listResponseMessageEndpointOut = __commonJS({
  "node_modules/svix/dist/models/listResponseMessageEndpointOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ListResponseMessageEndpointOutSerializer = void 0;
    var messageEndpointOut_1 = require_messageEndpointOut();
    exports.ListResponseMessageEndpointOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => messageEndpointOut_1.MessageEndpointOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"],
          prevIterator: object2["prevIterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => messageEndpointOut_1.MessageEndpointOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator,
          prevIterator: self2.prevIterator
        };
      }
    };
  }
});

// node_modules/svix/dist/api/messageAttempt.js
var require_messageAttempt = __commonJS({
  "node_modules/svix/dist/api/messageAttempt.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MessageAttempt = void 0;
    var emptyResponse_1 = require_emptyResponse();
    var listResponseEndpointMessageOut_1 = require_listResponseEndpointMessageOut();
    var listResponseMessageAttemptOut_1 = require_listResponseMessageAttemptOut();
    var listResponseMessageEndpointOut_1 = require_listResponseMessageEndpointOut();
    var messageAttemptOut_1 = require_messageAttemptOut();
    var request_1 = require_request();
    var MessageAttempt = class {
      static {
        __name(this, "MessageAttempt");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      listByEndpoint(appId, endpointId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/attempt/endpoint/{endpoint_id}");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          status: options === null || options === void 0 ? void 0 : options.status,
          status_code_class: options === null || options === void 0 ? void 0 : options.statusCodeClass,
          channel: options === null || options === void 0 ? void 0 : options.channel,
          tag: options === null || options === void 0 ? void 0 : options.tag,
          before: options === null || options === void 0 ? void 0 : options.before,
          after: options === null || options === void 0 ? void 0 : options.after,
          with_content: options === null || options === void 0 ? void 0 : options.withContent,
          with_msg: options === null || options === void 0 ? void 0 : options.withMsg,
          event_types: options === null || options === void 0 ? void 0 : options.eventTypes
        });
        return request.send(this.requestCtx, listResponseMessageAttemptOut_1.ListResponseMessageAttemptOutSerializer._fromJsonObject);
      }
      listByMsg(appId, msgId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/attempt/msg/{msg_id}");
        request.setPathParam("app_id", appId);
        request.setPathParam("msg_id", msgId);
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          status: options === null || options === void 0 ? void 0 : options.status,
          status_code_class: options === null || options === void 0 ? void 0 : options.statusCodeClass,
          channel: options === null || options === void 0 ? void 0 : options.channel,
          tag: options === null || options === void 0 ? void 0 : options.tag,
          endpoint_id: options === null || options === void 0 ? void 0 : options.endpointId,
          before: options === null || options === void 0 ? void 0 : options.before,
          after: options === null || options === void 0 ? void 0 : options.after,
          with_content: options === null || options === void 0 ? void 0 : options.withContent,
          event_types: options === null || options === void 0 ? void 0 : options.eventTypes
        });
        return request.send(this.requestCtx, listResponseMessageAttemptOut_1.ListResponseMessageAttemptOutSerializer._fromJsonObject);
      }
      listAttemptedMessages(appId, endpointId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/endpoint/{endpoint_id}/msg");
        request.setPathParam("app_id", appId);
        request.setPathParam("endpoint_id", endpointId);
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          channel: options === null || options === void 0 ? void 0 : options.channel,
          tag: options === null || options === void 0 ? void 0 : options.tag,
          status: options === null || options === void 0 ? void 0 : options.status,
          before: options === null || options === void 0 ? void 0 : options.before,
          after: options === null || options === void 0 ? void 0 : options.after,
          with_content: options === null || options === void 0 ? void 0 : options.withContent,
          event_types: options === null || options === void 0 ? void 0 : options.eventTypes
        });
        return request.send(this.requestCtx, listResponseEndpointMessageOut_1.ListResponseEndpointMessageOutSerializer._fromJsonObject);
      }
      get(appId, msgId, attemptId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/msg/{msg_id}/attempt/{attempt_id}");
        request.setPathParam("app_id", appId);
        request.setPathParam("msg_id", msgId);
        request.setPathParam("attempt_id", attemptId);
        return request.send(this.requestCtx, messageAttemptOut_1.MessageAttemptOutSerializer._fromJsonObject);
      }
      expungeContent(appId, msgId, attemptId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.DELETE, "/api/v1/app/{app_id}/msg/{msg_id}/attempt/{attempt_id}/content");
        request.setPathParam("app_id", appId);
        request.setPathParam("msg_id", msgId);
        request.setPathParam("attempt_id", attemptId);
        return request.sendNoResponseBody(this.requestCtx);
      }
      listAttemptedDestinations(appId, msgId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/app/{app_id}/msg/{msg_id}/endpoint");
        request.setPathParam("app_id", appId);
        request.setPathParam("msg_id", msgId);
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator
        });
        return request.send(this.requestCtx, listResponseMessageEndpointOut_1.ListResponseMessageEndpointOutSerializer._fromJsonObject);
      }
      resend(appId, msgId, endpointId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/app/{app_id}/msg/{msg_id}/endpoint/{endpoint_id}/resend");
        request.setPathParam("app_id", appId);
        request.setPathParam("msg_id", msgId);
        request.setPathParam("endpoint_id", endpointId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        return request.send(this.requestCtx, emptyResponse_1.EmptyResponseSerializer._fromJsonObject);
      }
    };
    exports.MessageAttempt = MessageAttempt;
  }
});

// node_modules/svix/dist/models/operationalWebhookEndpointOut.js
var require_operationalWebhookEndpointOut = __commonJS({
  "node_modules/svix/dist/models/operationalWebhookEndpointOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OperationalWebhookEndpointOutSerializer = void 0;
    exports.OperationalWebhookEndpointOutSerializer = {
      _fromJsonObject(object2) {
        return {
          createdAt: new Date(object2["createdAt"]),
          description: object2["description"],
          disabled: object2["disabled"],
          filterTypes: object2["filterTypes"],
          id: object2["id"],
          metadata: object2["metadata"],
          rateLimit: object2["rateLimit"],
          uid: object2["uid"],
          updatedAt: new Date(object2["updatedAt"]),
          url: object2["url"]
        };
      },
      _toJsonObject(self2) {
        return {
          createdAt: self2.createdAt,
          description: self2.description,
          disabled: self2.disabled,
          filterTypes: self2.filterTypes,
          id: self2.id,
          metadata: self2.metadata,
          rateLimit: self2.rateLimit,
          uid: self2.uid,
          updatedAt: self2.updatedAt,
          url: self2.url
        };
      }
    };
  }
});

// node_modules/svix/dist/models/listResponseOperationalWebhookEndpointOut.js
var require_listResponseOperationalWebhookEndpointOut = __commonJS({
  "node_modules/svix/dist/models/listResponseOperationalWebhookEndpointOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ListResponseOperationalWebhookEndpointOutSerializer = void 0;
    var operationalWebhookEndpointOut_1 = require_operationalWebhookEndpointOut();
    exports.ListResponseOperationalWebhookEndpointOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => operationalWebhookEndpointOut_1.OperationalWebhookEndpointOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"],
          prevIterator: object2["prevIterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => operationalWebhookEndpointOut_1.OperationalWebhookEndpointOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator,
          prevIterator: self2.prevIterator
        };
      }
    };
  }
});

// node_modules/svix/dist/models/operationalWebhookEndpointHeadersIn.js
var require_operationalWebhookEndpointHeadersIn = __commonJS({
  "node_modules/svix/dist/models/operationalWebhookEndpointHeadersIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OperationalWebhookEndpointHeadersInSerializer = void 0;
    exports.OperationalWebhookEndpointHeadersInSerializer = {
      _fromJsonObject(object2) {
        return {
          headers: object2["headers"]
        };
      },
      _toJsonObject(self2) {
        return {
          headers: self2.headers
        };
      }
    };
  }
});

// node_modules/svix/dist/models/operationalWebhookEndpointHeadersOut.js
var require_operationalWebhookEndpointHeadersOut = __commonJS({
  "node_modules/svix/dist/models/operationalWebhookEndpointHeadersOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OperationalWebhookEndpointHeadersOutSerializer = void 0;
    exports.OperationalWebhookEndpointHeadersOutSerializer = {
      _fromJsonObject(object2) {
        return {
          headers: object2["headers"],
          sensitive: object2["sensitive"]
        };
      },
      _toJsonObject(self2) {
        return {
          headers: self2.headers,
          sensitive: self2.sensitive
        };
      }
    };
  }
});

// node_modules/svix/dist/models/operationalWebhookEndpointIn.js
var require_operationalWebhookEndpointIn = __commonJS({
  "node_modules/svix/dist/models/operationalWebhookEndpointIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OperationalWebhookEndpointInSerializer = void 0;
    exports.OperationalWebhookEndpointInSerializer = {
      _fromJsonObject(object2) {
        return {
          description: object2["description"],
          disabled: object2["disabled"],
          filterTypes: object2["filterTypes"],
          metadata: object2["metadata"],
          rateLimit: object2["rateLimit"],
          secret: object2["secret"],
          uid: object2["uid"],
          url: object2["url"]
        };
      },
      _toJsonObject(self2) {
        return {
          description: self2.description,
          disabled: self2.disabled,
          filterTypes: self2.filterTypes,
          metadata: self2.metadata,
          rateLimit: self2.rateLimit,
          secret: self2.secret,
          uid: self2.uid,
          url: self2.url
        };
      }
    };
  }
});

// node_modules/svix/dist/models/operationalWebhookEndpointSecretIn.js
var require_operationalWebhookEndpointSecretIn = __commonJS({
  "node_modules/svix/dist/models/operationalWebhookEndpointSecretIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OperationalWebhookEndpointSecretInSerializer = void 0;
    exports.OperationalWebhookEndpointSecretInSerializer = {
      _fromJsonObject(object2) {
        return {
          key: object2["key"]
        };
      },
      _toJsonObject(self2) {
        return {
          key: self2.key
        };
      }
    };
  }
});

// node_modules/svix/dist/models/operationalWebhookEndpointSecretOut.js
var require_operationalWebhookEndpointSecretOut = __commonJS({
  "node_modules/svix/dist/models/operationalWebhookEndpointSecretOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OperationalWebhookEndpointSecretOutSerializer = void 0;
    exports.OperationalWebhookEndpointSecretOutSerializer = {
      _fromJsonObject(object2) {
        return {
          key: object2["key"]
        };
      },
      _toJsonObject(self2) {
        return {
          key: self2.key
        };
      }
    };
  }
});

// node_modules/svix/dist/models/operationalWebhookEndpointUpdate.js
var require_operationalWebhookEndpointUpdate = __commonJS({
  "node_modules/svix/dist/models/operationalWebhookEndpointUpdate.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OperationalWebhookEndpointUpdateSerializer = void 0;
    exports.OperationalWebhookEndpointUpdateSerializer = {
      _fromJsonObject(object2) {
        return {
          description: object2["description"],
          disabled: object2["disabled"],
          filterTypes: object2["filterTypes"],
          metadata: object2["metadata"],
          rateLimit: object2["rateLimit"],
          uid: object2["uid"],
          url: object2["url"]
        };
      },
      _toJsonObject(self2) {
        return {
          description: self2.description,
          disabled: self2.disabled,
          filterTypes: self2.filterTypes,
          metadata: self2.metadata,
          rateLimit: self2.rateLimit,
          uid: self2.uid,
          url: self2.url
        };
      }
    };
  }
});

// node_modules/svix/dist/api/operationalWebhookEndpoint.js
var require_operationalWebhookEndpoint = __commonJS({
  "node_modules/svix/dist/api/operationalWebhookEndpoint.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OperationalWebhookEndpoint = void 0;
    var listResponseOperationalWebhookEndpointOut_1 = require_listResponseOperationalWebhookEndpointOut();
    var operationalWebhookEndpointHeadersIn_1 = require_operationalWebhookEndpointHeadersIn();
    var operationalWebhookEndpointHeadersOut_1 = require_operationalWebhookEndpointHeadersOut();
    var operationalWebhookEndpointIn_1 = require_operationalWebhookEndpointIn();
    var operationalWebhookEndpointOut_1 = require_operationalWebhookEndpointOut();
    var operationalWebhookEndpointSecretIn_1 = require_operationalWebhookEndpointSecretIn();
    var operationalWebhookEndpointSecretOut_1 = require_operationalWebhookEndpointSecretOut();
    var operationalWebhookEndpointUpdate_1 = require_operationalWebhookEndpointUpdate();
    var request_1 = require_request();
    var OperationalWebhookEndpoint = class {
      static {
        __name(this, "OperationalWebhookEndpoint");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      list(options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/operational-webhook/endpoint");
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          order: options === null || options === void 0 ? void 0 : options.order
        });
        return request.send(this.requestCtx, listResponseOperationalWebhookEndpointOut_1.ListResponseOperationalWebhookEndpointOutSerializer._fromJsonObject);
      }
      create(operationalWebhookEndpointIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/operational-webhook/endpoint");
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(operationalWebhookEndpointIn_1.OperationalWebhookEndpointInSerializer._toJsonObject(operationalWebhookEndpointIn));
        return request.send(this.requestCtx, operationalWebhookEndpointOut_1.OperationalWebhookEndpointOutSerializer._fromJsonObject);
      }
      get(endpointId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/operational-webhook/endpoint/{endpoint_id}");
        request.setPathParam("endpoint_id", endpointId);
        return request.send(this.requestCtx, operationalWebhookEndpointOut_1.OperationalWebhookEndpointOutSerializer._fromJsonObject);
      }
      update(endpointId, operationalWebhookEndpointUpdate) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PUT, "/api/v1/operational-webhook/endpoint/{endpoint_id}");
        request.setPathParam("endpoint_id", endpointId);
        request.setBody(operationalWebhookEndpointUpdate_1.OperationalWebhookEndpointUpdateSerializer._toJsonObject(operationalWebhookEndpointUpdate));
        return request.send(this.requestCtx, operationalWebhookEndpointOut_1.OperationalWebhookEndpointOutSerializer._fromJsonObject);
      }
      delete(endpointId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.DELETE, "/api/v1/operational-webhook/endpoint/{endpoint_id}");
        request.setPathParam("endpoint_id", endpointId);
        return request.sendNoResponseBody(this.requestCtx);
      }
      getHeaders(endpointId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/operational-webhook/endpoint/{endpoint_id}/headers");
        request.setPathParam("endpoint_id", endpointId);
        return request.send(this.requestCtx, operationalWebhookEndpointHeadersOut_1.OperationalWebhookEndpointHeadersOutSerializer._fromJsonObject);
      }
      updateHeaders(endpointId, operationalWebhookEndpointHeadersIn) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PUT, "/api/v1/operational-webhook/endpoint/{endpoint_id}/headers");
        request.setPathParam("endpoint_id", endpointId);
        request.setBody(operationalWebhookEndpointHeadersIn_1.OperationalWebhookEndpointHeadersInSerializer._toJsonObject(operationalWebhookEndpointHeadersIn));
        return request.sendNoResponseBody(this.requestCtx);
      }
      getSecret(endpointId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/operational-webhook/endpoint/{endpoint_id}/secret");
        request.setPathParam("endpoint_id", endpointId);
        return request.send(this.requestCtx, operationalWebhookEndpointSecretOut_1.OperationalWebhookEndpointSecretOutSerializer._fromJsonObject);
      }
      rotateSecret(endpointId, operationalWebhookEndpointSecretIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/operational-webhook/endpoint/{endpoint_id}/secret/rotate");
        request.setPathParam("endpoint_id", endpointId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(operationalWebhookEndpointSecretIn_1.OperationalWebhookEndpointSecretInSerializer._toJsonObject(operationalWebhookEndpointSecretIn));
        return request.sendNoResponseBody(this.requestCtx);
      }
    };
    exports.OperationalWebhookEndpoint = OperationalWebhookEndpoint;
  }
});

// node_modules/svix/dist/api/operationalWebhook.js
var require_operationalWebhook = __commonJS({
  "node_modules/svix/dist/api/operationalWebhook.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OperationalWebhook = void 0;
    var operationalWebhookEndpoint_1 = require_operationalWebhookEndpoint();
    var OperationalWebhook = class {
      static {
        __name(this, "OperationalWebhook");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      get endpoint() {
        return new operationalWebhookEndpoint_1.OperationalWebhookEndpoint(this.requestCtx);
      }
    };
    exports.OperationalWebhook = OperationalWebhook;
  }
});

// node_modules/svix/dist/models/aggregateEventTypesOut.js
var require_aggregateEventTypesOut = __commonJS({
  "node_modules/svix/dist/models/aggregateEventTypesOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AggregateEventTypesOutSerializer = void 0;
    var backgroundTaskStatus_1 = require_backgroundTaskStatus();
    var backgroundTaskType_1 = require_backgroundTaskType();
    exports.AggregateEventTypesOutSerializer = {
      _fromJsonObject(object2) {
        return {
          id: object2["id"],
          status: backgroundTaskStatus_1.BackgroundTaskStatusSerializer._fromJsonObject(object2["status"]),
          task: backgroundTaskType_1.BackgroundTaskTypeSerializer._fromJsonObject(object2["task"])
        };
      },
      _toJsonObject(self2) {
        return {
          id: self2.id,
          status: backgroundTaskStatus_1.BackgroundTaskStatusSerializer._toJsonObject(self2.status),
          task: backgroundTaskType_1.BackgroundTaskTypeSerializer._toJsonObject(self2.task)
        };
      }
    };
  }
});

// node_modules/svix/dist/models/appUsageStatsIn.js
var require_appUsageStatsIn = __commonJS({
  "node_modules/svix/dist/models/appUsageStatsIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AppUsageStatsInSerializer = void 0;
    exports.AppUsageStatsInSerializer = {
      _fromJsonObject(object2) {
        return {
          appIds: object2["appIds"],
          since: new Date(object2["since"]),
          until: new Date(object2["until"])
        };
      },
      _toJsonObject(self2) {
        return {
          appIds: self2.appIds,
          since: self2.since,
          until: self2.until
        };
      }
    };
  }
});

// node_modules/svix/dist/models/appUsageStatsOut.js
var require_appUsageStatsOut = __commonJS({
  "node_modules/svix/dist/models/appUsageStatsOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AppUsageStatsOutSerializer = void 0;
    var backgroundTaskStatus_1 = require_backgroundTaskStatus();
    var backgroundTaskType_1 = require_backgroundTaskType();
    exports.AppUsageStatsOutSerializer = {
      _fromJsonObject(object2) {
        return {
          id: object2["id"],
          status: backgroundTaskStatus_1.BackgroundTaskStatusSerializer._fromJsonObject(object2["status"]),
          task: backgroundTaskType_1.BackgroundTaskTypeSerializer._fromJsonObject(object2["task"]),
          unresolvedAppIds: object2["unresolvedAppIds"]
        };
      },
      _toJsonObject(self2) {
        return {
          id: self2.id,
          status: backgroundTaskStatus_1.BackgroundTaskStatusSerializer._toJsonObject(self2.status),
          task: backgroundTaskType_1.BackgroundTaskTypeSerializer._toJsonObject(self2.task),
          unresolvedAppIds: self2.unresolvedAppIds
        };
      }
    };
  }
});

// node_modules/svix/dist/api/statistics.js
var require_statistics = __commonJS({
  "node_modules/svix/dist/api/statistics.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Statistics = void 0;
    var aggregateEventTypesOut_1 = require_aggregateEventTypesOut();
    var appUsageStatsIn_1 = require_appUsageStatsIn();
    var appUsageStatsOut_1 = require_appUsageStatsOut();
    var request_1 = require_request();
    var Statistics = class {
      static {
        __name(this, "Statistics");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      aggregateAppStats(appUsageStatsIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/stats/usage/app");
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(appUsageStatsIn_1.AppUsageStatsInSerializer._toJsonObject(appUsageStatsIn));
        return request.send(this.requestCtx, appUsageStatsOut_1.AppUsageStatsOutSerializer._fromJsonObject);
      }
      aggregateEventTypes() {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PUT, "/api/v1/stats/usage/event-types");
        return request.send(this.requestCtx, aggregateEventTypesOut_1.AggregateEventTypesOutSerializer._fromJsonObject);
      }
    };
    exports.Statistics = Statistics;
  }
});

// node_modules/svix/dist/models/httpSinkHeadersPatchIn.js
var require_httpSinkHeadersPatchIn = __commonJS({
  "node_modules/svix/dist/models/httpSinkHeadersPatchIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.HttpSinkHeadersPatchInSerializer = void 0;
    exports.HttpSinkHeadersPatchInSerializer = {
      _fromJsonObject(object2) {
        return {
          headers: object2["headers"]
        };
      },
      _toJsonObject(self2) {
        return {
          headers: self2.headers
        };
      }
    };
  }
});

// node_modules/svix/dist/models/sinkTransformationOut.js
var require_sinkTransformationOut = __commonJS({
  "node_modules/svix/dist/models/sinkTransformationOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SinkTransformationOutSerializer = void 0;
    exports.SinkTransformationOutSerializer = {
      _fromJsonObject(object2) {
        return {
          code: object2["code"],
          enabled: object2["enabled"]
        };
      },
      _toJsonObject(self2) {
        return {
          code: self2.code,
          enabled: self2.enabled
        };
      }
    };
  }
});

// node_modules/svix/dist/models/streamEventTypeOut.js
var require_streamEventTypeOut = __commonJS({
  "node_modules/svix/dist/models/streamEventTypeOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StreamEventTypeOutSerializer = void 0;
    exports.StreamEventTypeOutSerializer = {
      _fromJsonObject(object2) {
        return {
          archived: object2["archived"],
          createdAt: new Date(object2["createdAt"]),
          deprecated: object2["deprecated"],
          description: object2["description"],
          featureFlags: object2["featureFlags"],
          name: object2["name"],
          updatedAt: new Date(object2["updatedAt"])
        };
      },
      _toJsonObject(self2) {
        return {
          archived: self2.archived,
          createdAt: self2.createdAt,
          deprecated: self2.deprecated,
          description: self2.description,
          featureFlags: self2.featureFlags,
          name: self2.name,
          updatedAt: self2.updatedAt
        };
      }
    };
  }
});

// node_modules/svix/dist/models/listResponseStreamEventTypeOut.js
var require_listResponseStreamEventTypeOut = __commonJS({
  "node_modules/svix/dist/models/listResponseStreamEventTypeOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ListResponseStreamEventTypeOutSerializer = void 0;
    var streamEventTypeOut_1 = require_streamEventTypeOut();
    exports.ListResponseStreamEventTypeOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => streamEventTypeOut_1.StreamEventTypeOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"],
          prevIterator: object2["prevIterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => streamEventTypeOut_1.StreamEventTypeOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator,
          prevIterator: self2.prevIterator
        };
      }
    };
  }
});

// node_modules/svix/dist/models/streamEventTypeIn.js
var require_streamEventTypeIn = __commonJS({
  "node_modules/svix/dist/models/streamEventTypeIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StreamEventTypeInSerializer = void 0;
    exports.StreamEventTypeInSerializer = {
      _fromJsonObject(object2) {
        return {
          archived: object2["archived"],
          deprecated: object2["deprecated"],
          description: object2["description"],
          featureFlags: object2["featureFlags"],
          name: object2["name"]
        };
      },
      _toJsonObject(self2) {
        return {
          archived: self2.archived,
          deprecated: self2.deprecated,
          description: self2.description,
          featureFlags: self2.featureFlags,
          name: self2.name
        };
      }
    };
  }
});

// node_modules/svix/dist/models/streamEventTypePatch.js
var require_streamEventTypePatch = __commonJS({
  "node_modules/svix/dist/models/streamEventTypePatch.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StreamEventTypePatchSerializer = void 0;
    exports.StreamEventTypePatchSerializer = {
      _fromJsonObject(object2) {
        return {
          archived: object2["archived"],
          deprecated: object2["deprecated"],
          description: object2["description"],
          featureFlags: object2["featureFlags"],
          name: object2["name"]
        };
      },
      _toJsonObject(self2) {
        return {
          archived: self2.archived,
          deprecated: self2.deprecated,
          description: self2.description,
          featureFlags: self2.featureFlags,
          name: self2.name
        };
      }
    };
  }
});

// node_modules/svix/dist/api/streamingEventType.js
var require_streamingEventType = __commonJS({
  "node_modules/svix/dist/api/streamingEventType.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StreamingEventType = void 0;
    var listResponseStreamEventTypeOut_1 = require_listResponseStreamEventTypeOut();
    var streamEventTypeIn_1 = require_streamEventTypeIn();
    var streamEventTypeOut_1 = require_streamEventTypeOut();
    var streamEventTypePatch_1 = require_streamEventTypePatch();
    var request_1 = require_request();
    var StreamingEventType = class {
      static {
        __name(this, "StreamingEventType");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      list(options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/stream/event-type");
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          order: options === null || options === void 0 ? void 0 : options.order,
          include_archived: options === null || options === void 0 ? void 0 : options.includeArchived
        });
        return request.send(this.requestCtx, listResponseStreamEventTypeOut_1.ListResponseStreamEventTypeOutSerializer._fromJsonObject);
      }
      create(streamEventTypeIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/stream/event-type");
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(streamEventTypeIn_1.StreamEventTypeInSerializer._toJsonObject(streamEventTypeIn));
        return request.send(this.requestCtx, streamEventTypeOut_1.StreamEventTypeOutSerializer._fromJsonObject);
      }
      get(name) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/stream/event-type/{name}");
        request.setPathParam("name", name);
        return request.send(this.requestCtx, streamEventTypeOut_1.StreamEventTypeOutSerializer._fromJsonObject);
      }
      update(name, streamEventTypeIn) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PUT, "/api/v1/stream/event-type/{name}");
        request.setPathParam("name", name);
        request.setBody(streamEventTypeIn_1.StreamEventTypeInSerializer._toJsonObject(streamEventTypeIn));
        return request.send(this.requestCtx, streamEventTypeOut_1.StreamEventTypeOutSerializer._fromJsonObject);
      }
      delete(name, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.DELETE, "/api/v1/stream/event-type/{name}");
        request.setPathParam("name", name);
        request.setQueryParams({
          expunge: options === null || options === void 0 ? void 0 : options.expunge
        });
        return request.sendNoResponseBody(this.requestCtx);
      }
      patch(name, streamEventTypePatch) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PATCH, "/api/v1/stream/event-type/{name}");
        request.setPathParam("name", name);
        request.setBody(streamEventTypePatch_1.StreamEventTypePatchSerializer._toJsonObject(streamEventTypePatch));
        return request.send(this.requestCtx, streamEventTypeOut_1.StreamEventTypeOutSerializer._fromJsonObject);
      }
    };
    exports.StreamingEventType = StreamingEventType;
  }
});

// node_modules/svix/dist/models/eventIn.js
var require_eventIn = __commonJS({
  "node_modules/svix/dist/models/eventIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EventInSerializer = void 0;
    exports.EventInSerializer = {
      _fromJsonObject(object2) {
        return {
          eventType: object2["eventType"],
          payload: object2["payload"]
        };
      },
      _toJsonObject(self2) {
        return {
          eventType: self2.eventType,
          payload: self2.payload
        };
      }
    };
  }
});

// node_modules/svix/dist/models/streamIn.js
var require_streamIn = __commonJS({
  "node_modules/svix/dist/models/streamIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StreamInSerializer = void 0;
    exports.StreamInSerializer = {
      _fromJsonObject(object2) {
        return {
          metadata: object2["metadata"],
          name: object2["name"],
          uid: object2["uid"]
        };
      },
      _toJsonObject(self2) {
        return {
          metadata: self2.metadata,
          name: self2.name,
          uid: self2.uid
        };
      }
    };
  }
});

// node_modules/svix/dist/models/createStreamEventsIn.js
var require_createStreamEventsIn = __commonJS({
  "node_modules/svix/dist/models/createStreamEventsIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.CreateStreamEventsInSerializer = void 0;
    var eventIn_1 = require_eventIn();
    var streamIn_1 = require_streamIn();
    exports.CreateStreamEventsInSerializer = {
      _fromJsonObject(object2) {
        return {
          events: object2["events"].map((item) => eventIn_1.EventInSerializer._fromJsonObject(item)),
          stream: object2["stream"] ? streamIn_1.StreamInSerializer._fromJsonObject(object2["stream"]) : void 0
        };
      },
      _toJsonObject(self2) {
        return {
          events: self2.events.map((item) => eventIn_1.EventInSerializer._toJsonObject(item)),
          stream: self2.stream ? streamIn_1.StreamInSerializer._toJsonObject(self2.stream) : void 0
        };
      }
    };
  }
});

// node_modules/svix/dist/models/createStreamEventsOut.js
var require_createStreamEventsOut = __commonJS({
  "node_modules/svix/dist/models/createStreamEventsOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.CreateStreamEventsOutSerializer = void 0;
    exports.CreateStreamEventsOutSerializer = {
      _fromJsonObject(_object) {
        return {};
      },
      _toJsonObject(_self) {
        return {};
      }
    };
  }
});

// node_modules/svix/dist/models/eventOut.js
var require_eventOut = __commonJS({
  "node_modules/svix/dist/models/eventOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EventOutSerializer = void 0;
    exports.EventOutSerializer = {
      _fromJsonObject(object2) {
        return {
          eventType: object2["eventType"],
          payload: object2["payload"],
          timestamp: new Date(object2["timestamp"])
        };
      },
      _toJsonObject(self2) {
        return {
          eventType: self2.eventType,
          payload: self2.payload,
          timestamp: self2.timestamp
        };
      }
    };
  }
});

// node_modules/svix/dist/models/eventStreamOut.js
var require_eventStreamOut = __commonJS({
  "node_modules/svix/dist/models/eventStreamOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EventStreamOutSerializer = void 0;
    var eventOut_1 = require_eventOut();
    exports.EventStreamOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => eventOut_1.EventOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => eventOut_1.EventOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator
        };
      }
    };
  }
});

// node_modules/svix/dist/api/streamingEvents.js
var require_streamingEvents = __commonJS({
  "node_modules/svix/dist/api/streamingEvents.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StreamingEvents = void 0;
    var createStreamEventsIn_1 = require_createStreamEventsIn();
    var createStreamEventsOut_1 = require_createStreamEventsOut();
    var eventStreamOut_1 = require_eventStreamOut();
    var request_1 = require_request();
    var StreamingEvents = class {
      static {
        __name(this, "StreamingEvents");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      create(streamId, createStreamEventsIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/stream/{stream_id}/events");
        request.setPathParam("stream_id", streamId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(createStreamEventsIn_1.CreateStreamEventsInSerializer._toJsonObject(createStreamEventsIn));
        return request.send(this.requestCtx, createStreamEventsOut_1.CreateStreamEventsOutSerializer._fromJsonObject);
      }
      get(streamId, sinkId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/stream/{stream_id}/sink/{sink_id}/events");
        request.setPathParam("stream_id", streamId);
        request.setPathParam("sink_id", sinkId);
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          after: options === null || options === void 0 ? void 0 : options.after
        });
        return request.send(this.requestCtx, eventStreamOut_1.EventStreamOutSerializer._fromJsonObject);
      }
    };
    exports.StreamingEvents = StreamingEvents;
  }
});

// node_modules/svix/dist/models/azureBlobStorageConfig.js
var require_azureBlobStorageConfig = __commonJS({
  "node_modules/svix/dist/models/azureBlobStorageConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AzureBlobStorageConfigSerializer = void 0;
    exports.AzureBlobStorageConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          accessKey: object2["accessKey"],
          account: object2["account"],
          container: object2["container"]
        };
      },
      _toJsonObject(self2) {
        return {
          accessKey: self2.accessKey,
          account: self2.account,
          container: self2.container
        };
      }
    };
  }
});

// node_modules/svix/dist/models/googleCloudStorageConfig.js
var require_googleCloudStorageConfig = __commonJS({
  "node_modules/svix/dist/models/googleCloudStorageConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.GoogleCloudStorageConfigSerializer = void 0;
    exports.GoogleCloudStorageConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          bucket: object2["bucket"],
          credentials: object2["credentials"]
        };
      },
      _toJsonObject(self2) {
        return {
          bucket: self2.bucket,
          credentials: self2.credentials
        };
      }
    };
  }
});

// node_modules/svix/dist/models/s3Config.js
var require_s3Config = __commonJS({
  "node_modules/svix/dist/models/s3Config.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.S3ConfigSerializer = void 0;
    exports.S3ConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          accessKeyId: object2["accessKeyId"],
          bucket: object2["bucket"],
          region: object2["region"],
          secretAccessKey: object2["secretAccessKey"]
        };
      },
      _toJsonObject(self2) {
        return {
          accessKeyId: self2.accessKeyId,
          bucket: self2.bucket,
          region: self2.region,
          secretAccessKey: self2.secretAccessKey
        };
      }
    };
  }
});

// node_modules/svix/dist/models/sinkHttpConfig.js
var require_sinkHttpConfig = __commonJS({
  "node_modules/svix/dist/models/sinkHttpConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SinkHttpConfigSerializer = void 0;
    exports.SinkHttpConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          headers: object2["headers"],
          key: object2["key"],
          url: object2["url"]
        };
      },
      _toJsonObject(self2) {
        return {
          headers: self2.headers,
          key: self2.key,
          url: self2.url
        };
      }
    };
  }
});

// node_modules/svix/dist/models/sinkOtelV1Config.js
var require_sinkOtelV1Config = __commonJS({
  "node_modules/svix/dist/models/sinkOtelV1Config.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SinkOtelV1ConfigSerializer = void 0;
    exports.SinkOtelV1ConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          headers: object2["headers"],
          url: object2["url"]
        };
      },
      _toJsonObject(self2) {
        return {
          headers: self2.headers,
          url: self2.url
        };
      }
    };
  }
});

// node_modules/svix/dist/models/sinkStatus.js
var require_sinkStatus = __commonJS({
  "node_modules/svix/dist/models/sinkStatus.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SinkStatusSerializer = exports.SinkStatus = void 0;
    var SinkStatus;
    (function(SinkStatus2) {
      SinkStatus2["Enabled"] = "enabled";
      SinkStatus2["Paused"] = "paused";
      SinkStatus2["Disabled"] = "disabled";
      SinkStatus2["Retrying"] = "retrying";
    })(SinkStatus = exports.SinkStatus || (exports.SinkStatus = {}));
    exports.SinkStatusSerializer = {
      _fromJsonObject(object2) {
        return object2;
      },
      _toJsonObject(self2) {
        return self2;
      }
    };
  }
});

// node_modules/svix/dist/models/streamSinkOut.js
var require_streamSinkOut = __commonJS({
  "node_modules/svix/dist/models/streamSinkOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StreamSinkOutSerializer = void 0;
    var azureBlobStorageConfig_1 = require_azureBlobStorageConfig();
    var googleCloudStorageConfig_1 = require_googleCloudStorageConfig();
    var s3Config_1 = require_s3Config();
    var sinkHttpConfig_1 = require_sinkHttpConfig();
    var sinkOtelV1Config_1 = require_sinkOtelV1Config();
    var sinkStatus_1 = require_sinkStatus();
    exports.StreamSinkOutSerializer = {
      _fromJsonObject(object2) {
        const type = object2["type"];
        function getConfig(type2) {
          switch (type2) {
            case "poller":
              return {};
            case "azureBlobStorage":
              return azureBlobStorageConfig_1.AzureBlobStorageConfigSerializer._fromJsonObject(object2["config"]);
            case "otelTracing":
              return sinkOtelV1Config_1.SinkOtelV1ConfigSerializer._fromJsonObject(object2["config"]);
            case "http":
              return sinkHttpConfig_1.SinkHttpConfigSerializer._fromJsonObject(object2["config"]);
            case "amazonS3":
              return s3Config_1.S3ConfigSerializer._fromJsonObject(object2["config"]);
            case "googleCloudStorage":
              return googleCloudStorageConfig_1.GoogleCloudStorageConfigSerializer._fromJsonObject(object2["config"]);
            default:
              throw new Error(`Unexpected type: ${type2}`);
          }
        }
        __name(getConfig, "getConfig");
        return {
          type,
          config: getConfig(type),
          batchSize: object2["batchSize"],
          createdAt: new Date(object2["createdAt"]),
          currentIterator: object2["currentIterator"],
          eventTypes: object2["eventTypes"],
          failureReason: object2["failureReason"],
          id: object2["id"],
          maxWaitSecs: object2["maxWaitSecs"],
          metadata: object2["metadata"],
          nextRetryAt: object2["nextRetryAt"] ? new Date(object2["nextRetryAt"]) : null,
          status: sinkStatus_1.SinkStatusSerializer._fromJsonObject(object2["status"]),
          uid: object2["uid"],
          updatedAt: new Date(object2["updatedAt"])
        };
      },
      _toJsonObject(self2) {
        let config;
        switch (self2.type) {
          case "poller":
            config = {};
            break;
          case "azureBlobStorage":
            config = azureBlobStorageConfig_1.AzureBlobStorageConfigSerializer._toJsonObject(self2.config);
            break;
          case "otelTracing":
            config = sinkOtelV1Config_1.SinkOtelV1ConfigSerializer._toJsonObject(self2.config);
            break;
          case "http":
            config = sinkHttpConfig_1.SinkHttpConfigSerializer._toJsonObject(self2.config);
            break;
          case "amazonS3":
            config = s3Config_1.S3ConfigSerializer._toJsonObject(self2.config);
            break;
          case "googleCloudStorage":
            config = googleCloudStorageConfig_1.GoogleCloudStorageConfigSerializer._toJsonObject(self2.config);
            break;
        }
        return {
          type: self2.type,
          config,
          batchSize: self2.batchSize,
          createdAt: self2.createdAt,
          currentIterator: self2.currentIterator,
          eventTypes: self2.eventTypes,
          failureReason: self2.failureReason,
          id: self2.id,
          maxWaitSecs: self2.maxWaitSecs,
          metadata: self2.metadata,
          nextRetryAt: self2.nextRetryAt,
          status: sinkStatus_1.SinkStatusSerializer._toJsonObject(self2.status),
          uid: self2.uid,
          updatedAt: self2.updatedAt
        };
      }
    };
  }
});

// node_modules/svix/dist/models/listResponseStreamSinkOut.js
var require_listResponseStreamSinkOut = __commonJS({
  "node_modules/svix/dist/models/listResponseStreamSinkOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ListResponseStreamSinkOutSerializer = void 0;
    var streamSinkOut_1 = require_streamSinkOut();
    exports.ListResponseStreamSinkOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => streamSinkOut_1.StreamSinkOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"],
          prevIterator: object2["prevIterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => streamSinkOut_1.StreamSinkOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator,
          prevIterator: self2.prevIterator
        };
      }
    };
  }
});

// node_modules/svix/dist/models/sinkSecretOut.js
var require_sinkSecretOut = __commonJS({
  "node_modules/svix/dist/models/sinkSecretOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SinkSecretOutSerializer = void 0;
    exports.SinkSecretOutSerializer = {
      _fromJsonObject(object2) {
        return {
          key: object2["key"]
        };
      },
      _toJsonObject(self2) {
        return {
          key: self2.key
        };
      }
    };
  }
});

// node_modules/svix/dist/models/sinkTransformIn.js
var require_sinkTransformIn = __commonJS({
  "node_modules/svix/dist/models/sinkTransformIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SinkTransformInSerializer = void 0;
    exports.SinkTransformInSerializer = {
      _fromJsonObject(object2) {
        return {
          code: object2["code"]
        };
      },
      _toJsonObject(self2) {
        return {
          code: self2.code
        };
      }
    };
  }
});

// node_modules/svix/dist/models/sinkStatusIn.js
var require_sinkStatusIn = __commonJS({
  "node_modules/svix/dist/models/sinkStatusIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SinkStatusInSerializer = exports.SinkStatusIn = void 0;
    var SinkStatusIn;
    (function(SinkStatusIn2) {
      SinkStatusIn2["Enabled"] = "enabled";
      SinkStatusIn2["Disabled"] = "disabled";
    })(SinkStatusIn = exports.SinkStatusIn || (exports.SinkStatusIn = {}));
    exports.SinkStatusInSerializer = {
      _fromJsonObject(object2) {
        return object2;
      },
      _toJsonObject(self2) {
        return self2;
      }
    };
  }
});

// node_modules/svix/dist/models/streamSinkIn.js
var require_streamSinkIn = __commonJS({
  "node_modules/svix/dist/models/streamSinkIn.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StreamSinkInSerializer = void 0;
    var azureBlobStorageConfig_1 = require_azureBlobStorageConfig();
    var googleCloudStorageConfig_1 = require_googleCloudStorageConfig();
    var s3Config_1 = require_s3Config();
    var sinkHttpConfig_1 = require_sinkHttpConfig();
    var sinkOtelV1Config_1 = require_sinkOtelV1Config();
    var sinkStatusIn_1 = require_sinkStatusIn();
    exports.StreamSinkInSerializer = {
      _fromJsonObject(object2) {
        const type = object2["type"];
        function getConfig(type2) {
          switch (type2) {
            case "poller":
              return {};
            case "azureBlobStorage":
              return azureBlobStorageConfig_1.AzureBlobStorageConfigSerializer._fromJsonObject(object2["config"]);
            case "otelTracing":
              return sinkOtelV1Config_1.SinkOtelV1ConfigSerializer._fromJsonObject(object2["config"]);
            case "http":
              return sinkHttpConfig_1.SinkHttpConfigSerializer._fromJsonObject(object2["config"]);
            case "amazonS3":
              return s3Config_1.S3ConfigSerializer._fromJsonObject(object2["config"]);
            case "googleCloudStorage":
              return googleCloudStorageConfig_1.GoogleCloudStorageConfigSerializer._fromJsonObject(object2["config"]);
            default:
              throw new Error(`Unexpected type: ${type2}`);
          }
        }
        __name(getConfig, "getConfig");
        return {
          type,
          config: getConfig(type),
          batchSize: object2["batchSize"],
          eventTypes: object2["eventTypes"],
          maxWaitSecs: object2["maxWaitSecs"],
          metadata: object2["metadata"],
          status: object2["status"] ? sinkStatusIn_1.SinkStatusInSerializer._fromJsonObject(object2["status"]) : void 0,
          uid: object2["uid"]
        };
      },
      _toJsonObject(self2) {
        let config;
        switch (self2.type) {
          case "poller":
            config = {};
            break;
          case "azureBlobStorage":
            config = azureBlobStorageConfig_1.AzureBlobStorageConfigSerializer._toJsonObject(self2.config);
            break;
          case "otelTracing":
            config = sinkOtelV1Config_1.SinkOtelV1ConfigSerializer._toJsonObject(self2.config);
            break;
          case "http":
            config = sinkHttpConfig_1.SinkHttpConfigSerializer._toJsonObject(self2.config);
            break;
          case "amazonS3":
            config = s3Config_1.S3ConfigSerializer._toJsonObject(self2.config);
            break;
          case "googleCloudStorage":
            config = googleCloudStorageConfig_1.GoogleCloudStorageConfigSerializer._toJsonObject(self2.config);
            break;
        }
        return {
          type: self2.type,
          config,
          batchSize: self2.batchSize,
          eventTypes: self2.eventTypes,
          maxWaitSecs: self2.maxWaitSecs,
          metadata: self2.metadata,
          status: self2.status ? sinkStatusIn_1.SinkStatusInSerializer._toJsonObject(self2.status) : void 0,
          uid: self2.uid
        };
      }
    };
  }
});

// node_modules/svix/dist/models/amazonS3PatchConfig.js
var require_amazonS3PatchConfig = __commonJS({
  "node_modules/svix/dist/models/amazonS3PatchConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AmazonS3PatchConfigSerializer = void 0;
    exports.AmazonS3PatchConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          accessKeyId: object2["accessKeyId"],
          bucket: object2["bucket"],
          region: object2["region"],
          secretAccessKey: object2["secretAccessKey"]
        };
      },
      _toJsonObject(self2) {
        return {
          accessKeyId: self2.accessKeyId,
          bucket: self2.bucket,
          region: self2.region,
          secretAccessKey: self2.secretAccessKey
        };
      }
    };
  }
});

// node_modules/svix/dist/models/azureBlobStoragePatchConfig.js
var require_azureBlobStoragePatchConfig = __commonJS({
  "node_modules/svix/dist/models/azureBlobStoragePatchConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AzureBlobStoragePatchConfigSerializer = void 0;
    exports.AzureBlobStoragePatchConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          accessKey: object2["accessKey"],
          account: object2["account"],
          container: object2["container"]
        };
      },
      _toJsonObject(self2) {
        return {
          accessKey: self2.accessKey,
          account: self2.account,
          container: self2.container
        };
      }
    };
  }
});

// node_modules/svix/dist/models/googleCloudStoragePatchConfig.js
var require_googleCloudStoragePatchConfig = __commonJS({
  "node_modules/svix/dist/models/googleCloudStoragePatchConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.GoogleCloudStoragePatchConfigSerializer = void 0;
    exports.GoogleCloudStoragePatchConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          bucket: object2["bucket"],
          credentials: object2["credentials"]
        };
      },
      _toJsonObject(self2) {
        return {
          bucket: self2.bucket,
          credentials: self2.credentials
        };
      }
    };
  }
});

// node_modules/svix/dist/models/httpPatchConfig.js
var require_httpPatchConfig = __commonJS({
  "node_modules/svix/dist/models/httpPatchConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.HttpPatchConfigSerializer = void 0;
    exports.HttpPatchConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          url: object2["url"]
        };
      },
      _toJsonObject(self2) {
        return {
          url: self2.url
        };
      }
    };
  }
});

// node_modules/svix/dist/models/otelTracingPatchConfig.js
var require_otelTracingPatchConfig = __commonJS({
  "node_modules/svix/dist/models/otelTracingPatchConfig.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OtelTracingPatchConfigSerializer = void 0;
    exports.OtelTracingPatchConfigSerializer = {
      _fromJsonObject(object2) {
        return {
          url: object2["url"]
        };
      },
      _toJsonObject(self2) {
        return {
          url: self2.url
        };
      }
    };
  }
});

// node_modules/svix/dist/models/streamSinkPatch.js
var require_streamSinkPatch = __commonJS({
  "node_modules/svix/dist/models/streamSinkPatch.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StreamSinkPatchSerializer = void 0;
    var amazonS3PatchConfig_1 = require_amazonS3PatchConfig();
    var azureBlobStoragePatchConfig_1 = require_azureBlobStoragePatchConfig();
    var googleCloudStoragePatchConfig_1 = require_googleCloudStoragePatchConfig();
    var httpPatchConfig_1 = require_httpPatchConfig();
    var otelTracingPatchConfig_1 = require_otelTracingPatchConfig();
    var sinkStatusIn_1 = require_sinkStatusIn();
    exports.StreamSinkPatchSerializer = {
      _fromJsonObject(object2) {
        const type = object2["type"];
        function getConfig(type2) {
          switch (type2) {
            case "poller":
              return {};
            case "azureBlobStorage":
              return azureBlobStoragePatchConfig_1.AzureBlobStoragePatchConfigSerializer._fromJsonObject(object2["config"]);
            case "otelTracing":
              return otelTracingPatchConfig_1.OtelTracingPatchConfigSerializer._fromJsonObject(object2["config"]);
            case "http":
              return httpPatchConfig_1.HttpPatchConfigSerializer._fromJsonObject(object2["config"]);
            case "amazonS3":
              return amazonS3PatchConfig_1.AmazonS3PatchConfigSerializer._fromJsonObject(object2["config"]);
            case "googleCloudStorage":
              return googleCloudStoragePatchConfig_1.GoogleCloudStoragePatchConfigSerializer._fromJsonObject(object2["config"]);
            default:
              throw new Error(`Unexpected type: ${type2}`);
          }
        }
        __name(getConfig, "getConfig");
        return {
          type,
          config: getConfig(type),
          batchSize: object2["batchSize"],
          eventTypes: object2["eventTypes"],
          maxWaitSecs: object2["maxWaitSecs"],
          metadata: object2["metadata"],
          status: object2["status"] ? sinkStatusIn_1.SinkStatusInSerializer._fromJsonObject(object2["status"]) : void 0,
          uid: object2["uid"]
        };
      },
      _toJsonObject(self2) {
        let config;
        switch (self2.type) {
          case "poller":
            config = {};
            break;
          case "azureBlobStorage":
            config = azureBlobStoragePatchConfig_1.AzureBlobStoragePatchConfigSerializer._toJsonObject(self2.config);
            break;
          case "otelTracing":
            config = otelTracingPatchConfig_1.OtelTracingPatchConfigSerializer._toJsonObject(self2.config);
            break;
          case "http":
            config = httpPatchConfig_1.HttpPatchConfigSerializer._toJsonObject(self2.config);
            break;
          case "amazonS3":
            config = amazonS3PatchConfig_1.AmazonS3PatchConfigSerializer._toJsonObject(self2.config);
            break;
          case "googleCloudStorage":
            config = googleCloudStoragePatchConfig_1.GoogleCloudStoragePatchConfigSerializer._toJsonObject(self2.config);
            break;
        }
        return {
          type: self2.type,
          config,
          batchSize: self2.batchSize,
          eventTypes: self2.eventTypes,
          maxWaitSecs: self2.maxWaitSecs,
          metadata: self2.metadata,
          status: self2.status ? sinkStatusIn_1.SinkStatusInSerializer._toJsonObject(self2.status) : void 0,
          uid: self2.uid
        };
      }
    };
  }
});

// node_modules/svix/dist/api/streamingSink.js
var require_streamingSink = __commonJS({
  "node_modules/svix/dist/api/streamingSink.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StreamingSink = void 0;
    var emptyResponse_1 = require_emptyResponse();
    var endpointSecretRotateIn_1 = require_endpointSecretRotateIn();
    var listResponseStreamSinkOut_1 = require_listResponseStreamSinkOut();
    var sinkSecretOut_1 = require_sinkSecretOut();
    var sinkTransformIn_1 = require_sinkTransformIn();
    var streamSinkIn_1 = require_streamSinkIn();
    var streamSinkOut_1 = require_streamSinkOut();
    var streamSinkPatch_1 = require_streamSinkPatch();
    var request_1 = require_request();
    var StreamingSink = class {
      static {
        __name(this, "StreamingSink");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      list(streamId, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/stream/{stream_id}/sink");
        request.setPathParam("stream_id", streamId);
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          order: options === null || options === void 0 ? void 0 : options.order
        });
        return request.send(this.requestCtx, listResponseStreamSinkOut_1.ListResponseStreamSinkOutSerializer._fromJsonObject);
      }
      create(streamId, streamSinkIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/stream/{stream_id}/sink");
        request.setPathParam("stream_id", streamId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(streamSinkIn_1.StreamSinkInSerializer._toJsonObject(streamSinkIn));
        return request.send(this.requestCtx, streamSinkOut_1.StreamSinkOutSerializer._fromJsonObject);
      }
      get(streamId, sinkId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/stream/{stream_id}/sink/{sink_id}");
        request.setPathParam("stream_id", streamId);
        request.setPathParam("sink_id", sinkId);
        return request.send(this.requestCtx, streamSinkOut_1.StreamSinkOutSerializer._fromJsonObject);
      }
      update(streamId, sinkId, streamSinkIn) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PUT, "/api/v1/stream/{stream_id}/sink/{sink_id}");
        request.setPathParam("stream_id", streamId);
        request.setPathParam("sink_id", sinkId);
        request.setBody(streamSinkIn_1.StreamSinkInSerializer._toJsonObject(streamSinkIn));
        return request.send(this.requestCtx, streamSinkOut_1.StreamSinkOutSerializer._fromJsonObject);
      }
      delete(streamId, sinkId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.DELETE, "/api/v1/stream/{stream_id}/sink/{sink_id}");
        request.setPathParam("stream_id", streamId);
        request.setPathParam("sink_id", sinkId);
        return request.sendNoResponseBody(this.requestCtx);
      }
      patch(streamId, sinkId, streamSinkPatch) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PATCH, "/api/v1/stream/{stream_id}/sink/{sink_id}");
        request.setPathParam("stream_id", streamId);
        request.setPathParam("sink_id", sinkId);
        request.setBody(streamSinkPatch_1.StreamSinkPatchSerializer._toJsonObject(streamSinkPatch));
        return request.send(this.requestCtx, streamSinkOut_1.StreamSinkOutSerializer._fromJsonObject);
      }
      getSecret(streamId, sinkId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/stream/{stream_id}/sink/{sink_id}/secret");
        request.setPathParam("stream_id", streamId);
        request.setPathParam("sink_id", sinkId);
        return request.send(this.requestCtx, sinkSecretOut_1.SinkSecretOutSerializer._fromJsonObject);
      }
      rotateSecret(streamId, sinkId, endpointSecretRotateIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/stream/{stream_id}/sink/{sink_id}/secret/rotate");
        request.setPathParam("stream_id", streamId);
        request.setPathParam("sink_id", sinkId);
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(endpointSecretRotateIn_1.EndpointSecretRotateInSerializer._toJsonObject(endpointSecretRotateIn));
        return request.send(this.requestCtx, emptyResponse_1.EmptyResponseSerializer._fromJsonObject);
      }
      transformationPartialUpdate(streamId, sinkId, sinkTransformIn) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PATCH, "/api/v1/stream/{stream_id}/sink/{sink_id}/transformation");
        request.setPathParam("stream_id", streamId);
        request.setPathParam("sink_id", sinkId);
        request.setBody(sinkTransformIn_1.SinkTransformInSerializer._toJsonObject(sinkTransformIn));
        return request.send(this.requestCtx, emptyResponse_1.EmptyResponseSerializer._fromJsonObject);
      }
    };
    exports.StreamingSink = StreamingSink;
  }
});

// node_modules/svix/dist/models/streamOut.js
var require_streamOut = __commonJS({
  "node_modules/svix/dist/models/streamOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StreamOutSerializer = void 0;
    exports.StreamOutSerializer = {
      _fromJsonObject(object2) {
        return {
          createdAt: new Date(object2["createdAt"]),
          id: object2["id"],
          metadata: object2["metadata"],
          name: object2["name"],
          uid: object2["uid"],
          updatedAt: new Date(object2["updatedAt"])
        };
      },
      _toJsonObject(self2) {
        return {
          createdAt: self2.createdAt,
          id: self2.id,
          metadata: self2.metadata,
          name: self2.name,
          uid: self2.uid,
          updatedAt: self2.updatedAt
        };
      }
    };
  }
});

// node_modules/svix/dist/models/listResponseStreamOut.js
var require_listResponseStreamOut = __commonJS({
  "node_modules/svix/dist/models/listResponseStreamOut.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ListResponseStreamOutSerializer = void 0;
    var streamOut_1 = require_streamOut();
    exports.ListResponseStreamOutSerializer = {
      _fromJsonObject(object2) {
        return {
          data: object2["data"].map((item) => streamOut_1.StreamOutSerializer._fromJsonObject(item)),
          done: object2["done"],
          iterator: object2["iterator"],
          prevIterator: object2["prevIterator"]
        };
      },
      _toJsonObject(self2) {
        return {
          data: self2.data.map((item) => streamOut_1.StreamOutSerializer._toJsonObject(item)),
          done: self2.done,
          iterator: self2.iterator,
          prevIterator: self2.prevIterator
        };
      }
    };
  }
});

// node_modules/svix/dist/models/streamPatch.js
var require_streamPatch = __commonJS({
  "node_modules/svix/dist/models/streamPatch.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StreamPatchSerializer = void 0;
    exports.StreamPatchSerializer = {
      _fromJsonObject(object2) {
        return {
          description: object2["description"],
          metadata: object2["metadata"],
          uid: object2["uid"]
        };
      },
      _toJsonObject(self2) {
        return {
          description: self2.description,
          metadata: self2.metadata,
          uid: self2.uid
        };
      }
    };
  }
});

// node_modules/svix/dist/api/streamingStream.js
var require_streamingStream = __commonJS({
  "node_modules/svix/dist/api/streamingStream.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StreamingStream = void 0;
    var listResponseStreamOut_1 = require_listResponseStreamOut();
    var streamIn_1 = require_streamIn();
    var streamOut_1 = require_streamOut();
    var streamPatch_1 = require_streamPatch();
    var request_1 = require_request();
    var StreamingStream = class {
      static {
        __name(this, "StreamingStream");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      list(options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/stream");
        request.setQueryParams({
          limit: options === null || options === void 0 ? void 0 : options.limit,
          iterator: options === null || options === void 0 ? void 0 : options.iterator,
          order: options === null || options === void 0 ? void 0 : options.order
        });
        return request.send(this.requestCtx, listResponseStreamOut_1.ListResponseStreamOutSerializer._fromJsonObject);
      }
      create(streamIn, options) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/stream");
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(streamIn_1.StreamInSerializer._toJsonObject(streamIn));
        return request.send(this.requestCtx, streamOut_1.StreamOutSerializer._fromJsonObject);
      }
      get(streamId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/stream/{stream_id}");
        request.setPathParam("stream_id", streamId);
        return request.send(this.requestCtx, streamOut_1.StreamOutSerializer._fromJsonObject);
      }
      update(streamId, streamIn) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PUT, "/api/v1/stream/{stream_id}");
        request.setPathParam("stream_id", streamId);
        request.setBody(streamIn_1.StreamInSerializer._toJsonObject(streamIn));
        return request.send(this.requestCtx, streamOut_1.StreamOutSerializer._fromJsonObject);
      }
      delete(streamId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.DELETE, "/api/v1/stream/{stream_id}");
        request.setPathParam("stream_id", streamId);
        return request.sendNoResponseBody(this.requestCtx);
      }
      patch(streamId, streamPatch) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PATCH, "/api/v1/stream/{stream_id}");
        request.setPathParam("stream_id", streamId);
        request.setBody(streamPatch_1.StreamPatchSerializer._toJsonObject(streamPatch));
        return request.send(this.requestCtx, streamOut_1.StreamOutSerializer._fromJsonObject);
      }
    };
    exports.StreamingStream = StreamingStream;
  }
});

// node_modules/svix/dist/api/streaming.js
var require_streaming = __commonJS({
  "node_modules/svix/dist/api/streaming.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Streaming = void 0;
    var endpointHeadersOut_1 = require_endpointHeadersOut();
    var httpSinkHeadersPatchIn_1 = require_httpSinkHeadersPatchIn();
    var sinkTransformationOut_1 = require_sinkTransformationOut();
    var streamingEventType_1 = require_streamingEventType();
    var streamingEvents_1 = require_streamingEvents();
    var streamingSink_1 = require_streamingSink();
    var streamingStream_1 = require_streamingStream();
    var request_1 = require_request();
    var Streaming = class {
      static {
        __name(this, "Streaming");
      }
      constructor(requestCtx) {
        this.requestCtx = requestCtx;
      }
      get event_type() {
        return new streamingEventType_1.StreamingEventType(this.requestCtx);
      }
      get events() {
        return new streamingEvents_1.StreamingEvents(this.requestCtx);
      }
      get sink() {
        return new streamingSink_1.StreamingSink(this.requestCtx);
      }
      get stream() {
        return new streamingStream_1.StreamingStream(this.requestCtx);
      }
      sinkHeadersGet(streamId, sinkId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/stream/{stream_id}/sink/{sink_id}/headers");
        request.setPathParam("stream_id", streamId);
        request.setPathParam("sink_id", sinkId);
        return request.send(this.requestCtx, endpointHeadersOut_1.EndpointHeadersOutSerializer._fromJsonObject);
      }
      sinkHeadersPatch(streamId, sinkId, httpSinkHeadersPatchIn) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.PATCH, "/api/v1/stream/{stream_id}/sink/{sink_id}/headers");
        request.setPathParam("stream_id", streamId);
        request.setPathParam("sink_id", sinkId);
        request.setBody(httpSinkHeadersPatchIn_1.HttpSinkHeadersPatchInSerializer._toJsonObject(httpSinkHeadersPatchIn));
        return request.send(this.requestCtx, endpointHeadersOut_1.EndpointHeadersOutSerializer._fromJsonObject);
      }
      sinkTransformationGet(streamId, sinkId) {
        const request = new request_1.SvixRequest(request_1.HttpMethod.GET, "/api/v1/stream/{stream_id}/sink/{sink_id}/transformation");
        request.setPathParam("stream_id", streamId);
        request.setPathParam("sink_id", sinkId);
        return request.send(this.requestCtx, sinkTransformationOut_1.SinkTransformationOutSerializer._fromJsonObject);
      }
    };
    exports.Streaming = Streaming;
  }
});

// node_modules/svix/dist/HttpErrors.js
var require_HttpErrors = __commonJS({
  "node_modules/svix/dist/HttpErrors.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.HTTPValidationError = exports.ValidationError = exports.HttpErrorOut = void 0;
    var HttpErrorOut = class _HttpErrorOut {
      static {
        __name(this, "HttpErrorOut");
      }
      static getAttributeTypeMap() {
        return _HttpErrorOut.attributeTypeMap;
      }
    };
    exports.HttpErrorOut = HttpErrorOut;
    HttpErrorOut.discriminator = void 0;
    HttpErrorOut.mapping = void 0;
    HttpErrorOut.attributeTypeMap = [
      {
        name: "code",
        baseName: "code",
        type: "string",
        format: ""
      },
      {
        name: "detail",
        baseName: "detail",
        type: "string",
        format: ""
      }
    ];
    var ValidationError = class _ValidationError {
      static {
        __name(this, "ValidationError");
      }
      static getAttributeTypeMap() {
        return _ValidationError.attributeTypeMap;
      }
    };
    exports.ValidationError = ValidationError;
    ValidationError.discriminator = void 0;
    ValidationError.mapping = void 0;
    ValidationError.attributeTypeMap = [
      {
        name: "loc",
        baseName: "loc",
        type: "Array<string>",
        format: ""
      },
      {
        name: "msg",
        baseName: "msg",
        type: "string",
        format: ""
      },
      {
        name: "type",
        baseName: "type",
        type: "string",
        format: ""
      }
    ];
    var HTTPValidationError = class _HTTPValidationError {
      static {
        __name(this, "HTTPValidationError");
      }
      static getAttributeTypeMap() {
        return _HTTPValidationError.attributeTypeMap;
      }
    };
    exports.HTTPValidationError = HTTPValidationError;
    HTTPValidationError.discriminator = void 0;
    HTTPValidationError.mapping = void 0;
    HTTPValidationError.attributeTypeMap = [
      {
        name: "detail",
        baseName: "detail",
        type: "Array<ValidationError>",
        format: ""
      }
    ];
  }
});

// node_modules/standardwebhooks/dist/timing_safe_equal.js
var require_timing_safe_equal = __commonJS({
  "node_modules/standardwebhooks/dist/timing_safe_equal.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.timingSafeEqual = void 0;
    function assert(expr, msg = "") {
      if (!expr) {
        throw new Error(msg);
      }
    }
    __name(assert, "assert");
    function timingSafeEqual(a, b) {
      if (a.byteLength !== b.byteLength) {
        return false;
      }
      if (!(a instanceof DataView)) {
        a = new DataView(ArrayBuffer.isView(a) ? a.buffer : a);
      }
      if (!(b instanceof DataView)) {
        b = new DataView(ArrayBuffer.isView(b) ? b.buffer : b);
      }
      assert(a instanceof DataView);
      assert(b instanceof DataView);
      const length3 = a.byteLength;
      let out = 0;
      let i = -1;
      while (++i < length3) {
        out |= a.getUint8(i) ^ b.getUint8(i);
      }
      return out === 0;
    }
    __name(timingSafeEqual, "timingSafeEqual");
    exports.timingSafeEqual = timingSafeEqual;
  }
});

// node_modules/@stablelib/base64/lib/base64.js
var require_base64 = __commonJS({
  "node_modules/@stablelib/base64/lib/base64.js"(exports) {
    "use strict";
    var __extends = exports && exports.__extends || /* @__PURE__ */ (function() {
      var extendStatics = /* @__PURE__ */ __name(function(d, b) {
        extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
          d2.__proto__ = b2;
        } || function(d2, b2) {
          for (var p in b2) if (b2.hasOwnProperty(p)) d2[p] = b2[p];
        };
        return extendStatics(d, b);
      }, "extendStatics");
      return function(d, b) {
        extendStatics(d, b);
        function __() {
          this.constructor = d;
        }
        __name(__, "__");
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
      };
    })();
    Object.defineProperty(exports, "__esModule", { value: true });
    var INVALID_BYTE = 256;
    var Coder = (
      /** @class */
      (function() {
        function Coder2(_paddingCharacter) {
          if (_paddingCharacter === void 0) {
            _paddingCharacter = "=";
          }
          this._paddingCharacter = _paddingCharacter;
        }
        __name(Coder2, "Coder");
        Coder2.prototype.encodedLength = function(length3) {
          if (!this._paddingCharacter) {
            return (length3 * 8 + 5) / 6 | 0;
          }
          return (length3 + 2) / 3 * 4 | 0;
        };
        Coder2.prototype.encode = function(data) {
          var out = "";
          var i = 0;
          for (; i < data.length - 2; i += 3) {
            var c = data[i] << 16 | data[i + 1] << 8 | data[i + 2];
            out += this._encodeByte(c >>> 3 * 6 & 63);
            out += this._encodeByte(c >>> 2 * 6 & 63);
            out += this._encodeByte(c >>> 1 * 6 & 63);
            out += this._encodeByte(c >>> 0 * 6 & 63);
          }
          var left = data.length - i;
          if (left > 0) {
            var c = data[i] << 16 | (left === 2 ? data[i + 1] << 8 : 0);
            out += this._encodeByte(c >>> 3 * 6 & 63);
            out += this._encodeByte(c >>> 2 * 6 & 63);
            if (left === 2) {
              out += this._encodeByte(c >>> 1 * 6 & 63);
            } else {
              out += this._paddingCharacter || "";
            }
            out += this._paddingCharacter || "";
          }
          return out;
        };
        Coder2.prototype.maxDecodedLength = function(length3) {
          if (!this._paddingCharacter) {
            return (length3 * 6 + 7) / 8 | 0;
          }
          return length3 / 4 * 3 | 0;
        };
        Coder2.prototype.decodedLength = function(s) {
          return this.maxDecodedLength(s.length - this._getPaddingLength(s));
        };
        Coder2.prototype.decode = function(s) {
          if (s.length === 0) {
            return new Uint8Array(0);
          }
          var paddingLength = this._getPaddingLength(s);
          var length3 = s.length - paddingLength;
          var out = new Uint8Array(this.maxDecodedLength(length3));
          var op = 0;
          var i = 0;
          var haveBad = 0;
          var v0 = 0, v1 = 0, v2 = 0, v3 = 0;
          for (; i < length3 - 4; i += 4) {
            v0 = this._decodeChar(s.charCodeAt(i + 0));
            v1 = this._decodeChar(s.charCodeAt(i + 1));
            v2 = this._decodeChar(s.charCodeAt(i + 2));
            v3 = this._decodeChar(s.charCodeAt(i + 3));
            out[op++] = v0 << 2 | v1 >>> 4;
            out[op++] = v1 << 4 | v2 >>> 2;
            out[op++] = v2 << 6 | v3;
            haveBad |= v0 & INVALID_BYTE;
            haveBad |= v1 & INVALID_BYTE;
            haveBad |= v2 & INVALID_BYTE;
            haveBad |= v3 & INVALID_BYTE;
          }
          if (i < length3 - 1) {
            v0 = this._decodeChar(s.charCodeAt(i));
            v1 = this._decodeChar(s.charCodeAt(i + 1));
            out[op++] = v0 << 2 | v1 >>> 4;
            haveBad |= v0 & INVALID_BYTE;
            haveBad |= v1 & INVALID_BYTE;
          }
          if (i < length3 - 2) {
            v2 = this._decodeChar(s.charCodeAt(i + 2));
            out[op++] = v1 << 4 | v2 >>> 2;
            haveBad |= v2 & INVALID_BYTE;
          }
          if (i < length3 - 3) {
            v3 = this._decodeChar(s.charCodeAt(i + 3));
            out[op++] = v2 << 6 | v3;
            haveBad |= v3 & INVALID_BYTE;
          }
          if (haveBad !== 0) {
            throw new Error("Base64Coder: incorrect characters for decoding");
          }
          return out;
        };
        Coder2.prototype._encodeByte = function(b) {
          var result = b;
          result += 65;
          result += 25 - b >>> 8 & 0 - 65 - 26 + 97;
          result += 51 - b >>> 8 & 26 - 97 - 52 + 48;
          result += 61 - b >>> 8 & 52 - 48 - 62 + 43;
          result += 62 - b >>> 8 & 62 - 43 - 63 + 47;
          return String.fromCharCode(result);
        };
        Coder2.prototype._decodeChar = function(c) {
          var result = INVALID_BYTE;
          result += (42 - c & c - 44) >>> 8 & -INVALID_BYTE + c - 43 + 62;
          result += (46 - c & c - 48) >>> 8 & -INVALID_BYTE + c - 47 + 63;
          result += (47 - c & c - 58) >>> 8 & -INVALID_BYTE + c - 48 + 52;
          result += (64 - c & c - 91) >>> 8 & -INVALID_BYTE + c - 65 + 0;
          result += (96 - c & c - 123) >>> 8 & -INVALID_BYTE + c - 97 + 26;
          return result;
        };
        Coder2.prototype._getPaddingLength = function(s) {
          var paddingLength = 0;
          if (this._paddingCharacter) {
            for (var i = s.length - 1; i >= 0; i--) {
              if (s[i] !== this._paddingCharacter) {
                break;
              }
              paddingLength++;
            }
            if (s.length < 4 || paddingLength > 2) {
              throw new Error("Base64Coder: incorrect padding");
            }
          }
          return paddingLength;
        };
        return Coder2;
      })()
    );
    exports.Coder = Coder;
    var stdCoder = new Coder();
    function encode2(data) {
      return stdCoder.encode(data);
    }
    __name(encode2, "encode");
    exports.encode = encode2;
    function decode2(s) {
      return stdCoder.decode(s);
    }
    __name(decode2, "decode");
    exports.decode = decode2;
    var URLSafeCoder = (
      /** @class */
      (function(_super) {
        __extends(URLSafeCoder2, _super);
        function URLSafeCoder2() {
          return _super !== null && _super.apply(this, arguments) || this;
        }
        __name(URLSafeCoder2, "URLSafeCoder");
        URLSafeCoder2.prototype._encodeByte = function(b) {
          var result = b;
          result += 65;
          result += 25 - b >>> 8 & 0 - 65 - 26 + 97;
          result += 51 - b >>> 8 & 26 - 97 - 52 + 48;
          result += 61 - b >>> 8 & 52 - 48 - 62 + 45;
          result += 62 - b >>> 8 & 62 - 45 - 63 + 95;
          return String.fromCharCode(result);
        };
        URLSafeCoder2.prototype._decodeChar = function(c) {
          var result = INVALID_BYTE;
          result += (44 - c & c - 46) >>> 8 & -INVALID_BYTE + c - 45 + 62;
          result += (94 - c & c - 96) >>> 8 & -INVALID_BYTE + c - 95 + 63;
          result += (47 - c & c - 58) >>> 8 & -INVALID_BYTE + c - 48 + 52;
          result += (64 - c & c - 91) >>> 8 & -INVALID_BYTE + c - 65 + 0;
          result += (96 - c & c - 123) >>> 8 & -INVALID_BYTE + c - 97 + 26;
          return result;
        };
        return URLSafeCoder2;
      })(Coder)
    );
    exports.URLSafeCoder = URLSafeCoder;
    var urlSafeCoder = new URLSafeCoder();
    function encodeURLSafe(data) {
      return urlSafeCoder.encode(data);
    }
    __name(encodeURLSafe, "encodeURLSafe");
    exports.encodeURLSafe = encodeURLSafe;
    function decodeURLSafe(s) {
      return urlSafeCoder.decode(s);
    }
    __name(decodeURLSafe, "decodeURLSafe");
    exports.decodeURLSafe = decodeURLSafe;
    exports.encodedLength = function(length3) {
      return stdCoder.encodedLength(length3);
    };
    exports.maxDecodedLength = function(length3) {
      return stdCoder.maxDecodedLength(length3);
    };
    exports.decodedLength = function(s) {
      return stdCoder.decodedLength(s);
    };
  }
});

// node_modules/fast-sha256/sha256.js
var require_sha256 = __commonJS({
  "node_modules/fast-sha256/sha256.js"(exports, module) {
    (function(root, factory) {
      var exports2 = {};
      factory(exports2);
      var sha256 = exports2["default"];
      for (var k in exports2) {
        sha256[k] = exports2[k];
      }
      if (typeof module === "object" && typeof module.exports === "object") {
        module.exports = sha256;
      } else if (typeof define === "function" && define.amd) {
        define(function() {
          return sha256;
        });
      } else {
        root.sha256 = sha256;
      }
    })(exports, function(exports2) {
      "use strict";
      exports2.__esModule = true;
      exports2.digestLength = 32;
      exports2.blockSize = 64;
      var K = new Uint32Array([
        1116352408,
        1899447441,
        3049323471,
        3921009573,
        961987163,
        1508970993,
        2453635748,
        2870763221,
        3624381080,
        310598401,
        607225278,
        1426881987,
        1925078388,
        2162078206,
        2614888103,
        3248222580,
        3835390401,
        4022224774,
        264347078,
        604807628,
        770255983,
        1249150122,
        1555081692,
        1996064986,
        2554220882,
        2821834349,
        2952996808,
        3210313671,
        3336571891,
        3584528711,
        113926993,
        338241895,
        666307205,
        773529912,
        1294757372,
        1396182291,
        1695183700,
        1986661051,
        2177026350,
        2456956037,
        2730485921,
        2820302411,
        3259730800,
        3345764771,
        3516065817,
        3600352804,
        4094571909,
        275423344,
        430227734,
        506948616,
        659060556,
        883997877,
        958139571,
        1322822218,
        1537002063,
        1747873779,
        1955562222,
        2024104815,
        2227730452,
        2361852424,
        2428436474,
        2756734187,
        3204031479,
        3329325298
      ]);
      function hashBlocks(w, v, p, pos, len) {
        var a, b, c, d, e, f, g, h, u, i, j, t1, t2;
        while (len >= 64) {
          a = v[0];
          b = v[1];
          c = v[2];
          d = v[3];
          e = v[4];
          f = v[5];
          g = v[6];
          h = v[7];
          for (i = 0; i < 16; i++) {
            j = pos + i * 4;
            w[i] = (p[j] & 255) << 24 | (p[j + 1] & 255) << 16 | (p[j + 2] & 255) << 8 | p[j + 3] & 255;
          }
          for (i = 16; i < 64; i++) {
            u = w[i - 2];
            t1 = (u >>> 17 | u << 32 - 17) ^ (u >>> 19 | u << 32 - 19) ^ u >>> 10;
            u = w[i - 15];
            t2 = (u >>> 7 | u << 32 - 7) ^ (u >>> 18 | u << 32 - 18) ^ u >>> 3;
            w[i] = (t1 + w[i - 7] | 0) + (t2 + w[i - 16] | 0);
          }
          for (i = 0; i < 64; i++) {
            t1 = (((e >>> 6 | e << 32 - 6) ^ (e >>> 11 | e << 32 - 11) ^ (e >>> 25 | e << 32 - 25)) + (e & f ^ ~e & g) | 0) + (h + (K[i] + w[i] | 0) | 0) | 0;
            t2 = ((a >>> 2 | a << 32 - 2) ^ (a >>> 13 | a << 32 - 13) ^ (a >>> 22 | a << 32 - 22)) + (a & b ^ a & c ^ b & c) | 0;
            h = g;
            g = f;
            f = e;
            e = d + t1 | 0;
            d = c;
            c = b;
            b = a;
            a = t1 + t2 | 0;
          }
          v[0] += a;
          v[1] += b;
          v[2] += c;
          v[3] += d;
          v[4] += e;
          v[5] += f;
          v[6] += g;
          v[7] += h;
          pos += 64;
          len -= 64;
        }
        return pos;
      }
      __name(hashBlocks, "hashBlocks");
      var Hash = (
        /** @class */
        (function() {
          function Hash2() {
            this.digestLength = exports2.digestLength;
            this.blockSize = exports2.blockSize;
            this.state = new Int32Array(8);
            this.temp = new Int32Array(64);
            this.buffer = new Uint8Array(128);
            this.bufferLength = 0;
            this.bytesHashed = 0;
            this.finished = false;
            this.reset();
          }
          __name(Hash2, "Hash");
          Hash2.prototype.reset = function() {
            this.state[0] = 1779033703;
            this.state[1] = 3144134277;
            this.state[2] = 1013904242;
            this.state[3] = 2773480762;
            this.state[4] = 1359893119;
            this.state[5] = 2600822924;
            this.state[6] = 528734635;
            this.state[7] = 1541459225;
            this.bufferLength = 0;
            this.bytesHashed = 0;
            this.finished = false;
            return this;
          };
          Hash2.prototype.clean = function() {
            for (var i = 0; i < this.buffer.length; i++) {
              this.buffer[i] = 0;
            }
            for (var i = 0; i < this.temp.length; i++) {
              this.temp[i] = 0;
            }
            this.reset();
          };
          Hash2.prototype.update = function(data, dataLength) {
            if (dataLength === void 0) {
              dataLength = data.length;
            }
            if (this.finished) {
              throw new Error("SHA256: can't update because hash was finished.");
            }
            var dataPos = 0;
            this.bytesHashed += dataLength;
            if (this.bufferLength > 0) {
              while (this.bufferLength < 64 && dataLength > 0) {
                this.buffer[this.bufferLength++] = data[dataPos++];
                dataLength--;
              }
              if (this.bufferLength === 64) {
                hashBlocks(this.temp, this.state, this.buffer, 0, 64);
                this.bufferLength = 0;
              }
            }
            if (dataLength >= 64) {
              dataPos = hashBlocks(this.temp, this.state, data, dataPos, dataLength);
              dataLength %= 64;
            }
            while (dataLength > 0) {
              this.buffer[this.bufferLength++] = data[dataPos++];
              dataLength--;
            }
            return this;
          };
          Hash2.prototype.finish = function(out) {
            if (!this.finished) {
              var bytesHashed = this.bytesHashed;
              var left = this.bufferLength;
              var bitLenHi = bytesHashed / 536870912 | 0;
              var bitLenLo = bytesHashed << 3;
              var padLength = bytesHashed % 64 < 56 ? 64 : 128;
              this.buffer[left] = 128;
              for (var i = left + 1; i < padLength - 8; i++) {
                this.buffer[i] = 0;
              }
              this.buffer[padLength - 8] = bitLenHi >>> 24 & 255;
              this.buffer[padLength - 7] = bitLenHi >>> 16 & 255;
              this.buffer[padLength - 6] = bitLenHi >>> 8 & 255;
              this.buffer[padLength - 5] = bitLenHi >>> 0 & 255;
              this.buffer[padLength - 4] = bitLenLo >>> 24 & 255;
              this.buffer[padLength - 3] = bitLenLo >>> 16 & 255;
              this.buffer[padLength - 2] = bitLenLo >>> 8 & 255;
              this.buffer[padLength - 1] = bitLenLo >>> 0 & 255;
              hashBlocks(this.temp, this.state, this.buffer, 0, padLength);
              this.finished = true;
            }
            for (var i = 0; i < 8; i++) {
              out[i * 4 + 0] = this.state[i] >>> 24 & 255;
              out[i * 4 + 1] = this.state[i] >>> 16 & 255;
              out[i * 4 + 2] = this.state[i] >>> 8 & 255;
              out[i * 4 + 3] = this.state[i] >>> 0 & 255;
            }
            return this;
          };
          Hash2.prototype.digest = function() {
            var out = new Uint8Array(this.digestLength);
            this.finish(out);
            return out;
          };
          Hash2.prototype._saveState = function(out) {
            for (var i = 0; i < this.state.length; i++) {
              out[i] = this.state[i];
            }
          };
          Hash2.prototype._restoreState = function(from, bytesHashed) {
            for (var i = 0; i < this.state.length; i++) {
              this.state[i] = from[i];
            }
            this.bytesHashed = bytesHashed;
            this.finished = false;
            this.bufferLength = 0;
          };
          return Hash2;
        })()
      );
      exports2.Hash = Hash;
      var HMAC = (
        /** @class */
        (function() {
          function HMAC2(key) {
            this.inner = new Hash();
            this.outer = new Hash();
            this.blockSize = this.inner.blockSize;
            this.digestLength = this.inner.digestLength;
            var pad = new Uint8Array(this.blockSize);
            if (key.length > this.blockSize) {
              new Hash().update(key).finish(pad).clean();
            } else {
              for (var i = 0; i < key.length; i++) {
                pad[i] = key[i];
              }
            }
            for (var i = 0; i < pad.length; i++) {
              pad[i] ^= 54;
            }
            this.inner.update(pad);
            for (var i = 0; i < pad.length; i++) {
              pad[i] ^= 54 ^ 92;
            }
            this.outer.update(pad);
            this.istate = new Uint32Array(8);
            this.ostate = new Uint32Array(8);
            this.inner._saveState(this.istate);
            this.outer._saveState(this.ostate);
            for (var i = 0; i < pad.length; i++) {
              pad[i] = 0;
            }
          }
          __name(HMAC2, "HMAC");
          HMAC2.prototype.reset = function() {
            this.inner._restoreState(this.istate, this.inner.blockSize);
            this.outer._restoreState(this.ostate, this.outer.blockSize);
            return this;
          };
          HMAC2.prototype.clean = function() {
            for (var i = 0; i < this.istate.length; i++) {
              this.ostate[i] = this.istate[i] = 0;
            }
            this.inner.clean();
            this.outer.clean();
          };
          HMAC2.prototype.update = function(data) {
            this.inner.update(data);
            return this;
          };
          HMAC2.prototype.finish = function(out) {
            if (this.outer.finished) {
              this.outer.finish(out);
            } else {
              this.inner.finish(out);
              this.outer.update(out, this.digestLength).finish(out);
            }
            return this;
          };
          HMAC2.prototype.digest = function() {
            var out = new Uint8Array(this.digestLength);
            this.finish(out);
            return out;
          };
          return HMAC2;
        })()
      );
      exports2.HMAC = HMAC;
      function hash(data) {
        var h = new Hash().update(data);
        var digest = h.digest();
        h.clean();
        return digest;
      }
      __name(hash, "hash");
      exports2.hash = hash;
      exports2["default"] = hash;
      function hmac(key, data) {
        var h = new HMAC(key).update(data);
        var digest = h.digest();
        h.clean();
        return digest;
      }
      __name(hmac, "hmac");
      exports2.hmac = hmac;
      function fillBuffer(buffer2, hmac2, info, counter) {
        var num = counter[0];
        if (num === 0) {
          throw new Error("hkdf: cannot expand more");
        }
        hmac2.reset();
        if (num > 1) {
          hmac2.update(buffer2);
        }
        if (info) {
          hmac2.update(info);
        }
        hmac2.update(counter);
        hmac2.finish(buffer2);
        counter[0]++;
      }
      __name(fillBuffer, "fillBuffer");
      var hkdfSalt = new Uint8Array(exports2.digestLength);
      function hkdf2(key, salt, info, length3) {
        if (salt === void 0) {
          salt = hkdfSalt;
        }
        if (length3 === void 0) {
          length3 = 32;
        }
        var counter = new Uint8Array([1]);
        var okm = hmac(salt, key);
        var hmac_ = new HMAC(okm);
        var buffer2 = new Uint8Array(hmac_.digestLength);
        var bufpos = buffer2.length;
        var out = new Uint8Array(length3);
        for (var i = 0; i < length3; i++) {
          if (bufpos === buffer2.length) {
            fillBuffer(buffer2, hmac_, info, counter);
            bufpos = 0;
          }
          out[i] = buffer2[bufpos++];
        }
        hmac_.clean();
        buffer2.fill(0);
        counter.fill(0);
        return out;
      }
      __name(hkdf2, "hkdf");
      exports2.hkdf = hkdf2;
      function pbkdf2(password, salt, iterations, dkLen) {
        var prf = new HMAC(password);
        var len = prf.digestLength;
        var ctr = new Uint8Array(4);
        var t = new Uint8Array(len);
        var u = new Uint8Array(len);
        var dk = new Uint8Array(dkLen);
        for (var i = 0; i * len < dkLen; i++) {
          var c = i + 1;
          ctr[0] = c >>> 24 & 255;
          ctr[1] = c >>> 16 & 255;
          ctr[2] = c >>> 8 & 255;
          ctr[3] = c >>> 0 & 255;
          prf.reset();
          prf.update(salt);
          prf.update(ctr);
          prf.finish(u);
          for (var j = 0; j < len; j++) {
            t[j] = u[j];
          }
          for (var j = 2; j <= iterations; j++) {
            prf.reset();
            prf.update(u).finish(u);
            for (var k = 0; k < len; k++) {
              t[k] ^= u[k];
            }
          }
          for (var j = 0; j < len && i * len + j < dkLen; j++) {
            dk[i * len + j] = t[j];
          }
        }
        for (var i = 0; i < len; i++) {
          t[i] = u[i] = 0;
        }
        for (var i = 0; i < 4; i++) {
          ctr[i] = 0;
        }
        prf.clean();
        return dk;
      }
      __name(pbkdf2, "pbkdf2");
      exports2.pbkdf2 = pbkdf2;
    });
  }
});

// node_modules/standardwebhooks/dist/index.js
var require_dist = __commonJS({
  "node_modules/standardwebhooks/dist/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Webhook = exports.WebhookVerificationError = void 0;
    var timing_safe_equal_1 = require_timing_safe_equal();
    var base64 = require_base64();
    var sha256 = require_sha256();
    var WEBHOOK_TOLERANCE_IN_SECONDS = 5 * 60;
    var ExtendableError = class _ExtendableError extends Error {
      static {
        __name(this, "ExtendableError");
      }
      constructor(message) {
        super(message);
        Object.setPrototypeOf(this, _ExtendableError.prototype);
        this.name = "ExtendableError";
        this.stack = new Error(message).stack;
      }
    };
    var WebhookVerificationError = class _WebhookVerificationError extends ExtendableError {
      static {
        __name(this, "WebhookVerificationError");
      }
      constructor(message) {
        super(message);
        Object.setPrototypeOf(this, _WebhookVerificationError.prototype);
        this.name = "WebhookVerificationError";
      }
    };
    exports.WebhookVerificationError = WebhookVerificationError;
    var Webhook2 = class _Webhook {
      static {
        __name(this, "Webhook");
      }
      constructor(secret, options) {
        if (!secret) {
          throw new Error("Secret can't be empty.");
        }
        if ((options === null || options === void 0 ? void 0 : options.format) === "raw") {
          if (secret instanceof Uint8Array) {
            this.key = secret;
          } else {
            this.key = Uint8Array.from(secret, (c) => c.charCodeAt(0));
          }
        } else {
          if (typeof secret !== "string") {
            throw new Error("Expected secret to be of type string");
          }
          if (secret.startsWith(_Webhook.prefix)) {
            secret = secret.substring(_Webhook.prefix.length);
          }
          this.key = base64.decode(secret);
        }
      }
      verify(payload, headers_) {
        const headers = {};
        for (const key of Object.keys(headers_)) {
          headers[key.toLowerCase()] = headers_[key];
        }
        const msgId = headers["webhook-id"];
        const msgSignature = headers["webhook-signature"];
        const msgTimestamp = headers["webhook-timestamp"];
        if (!msgSignature || !msgId || !msgTimestamp) {
          throw new WebhookVerificationError("Missing required headers");
        }
        const timestamp = this.verifyTimestamp(msgTimestamp);
        const computedSignature = this.sign(msgId, timestamp, payload);
        const expectedSignature = computedSignature.split(",")[1];
        const passedSignatures = msgSignature.split(" ");
        const encoder = new globalThis.TextEncoder();
        for (const versionedSignature of passedSignatures) {
          const [version5, signature] = versionedSignature.split(",");
          if (version5 !== "v1") {
            continue;
          }
          if ((0, timing_safe_equal_1.timingSafeEqual)(encoder.encode(signature), encoder.encode(expectedSignature))) {
            return JSON.parse(payload.toString());
          }
        }
        throw new WebhookVerificationError("No matching signature found");
      }
      sign(msgId, timestamp, payload) {
        if (typeof payload === "string") {
        } else if (payload.constructor.name === "Buffer") {
          payload = payload.toString();
        } else {
          throw new Error("Expected payload to be of type string or Buffer.");
        }
        const encoder = new TextEncoder();
        const timestampNumber = Math.floor(timestamp.getTime() / 1e3);
        const toSign = encoder.encode(`${msgId}.${timestampNumber}.${payload}`);
        const expectedSignature = base64.encode(sha256.hmac(this.key, toSign));
        return `v1,${expectedSignature}`;
      }
      verifyTimestamp(timestampHeader) {
        const now = Math.floor(Date.now() / 1e3);
        const timestamp = parseInt(timestampHeader, 10);
        if (isNaN(timestamp)) {
          throw new WebhookVerificationError("Invalid Signature Headers");
        }
        if (now - timestamp > WEBHOOK_TOLERANCE_IN_SECONDS) {
          throw new WebhookVerificationError("Message timestamp too old");
        }
        if (timestamp > now + WEBHOOK_TOLERANCE_IN_SECONDS) {
          throw new WebhookVerificationError("Message timestamp too new");
        }
        return new Date(timestamp * 1e3);
      }
    };
    exports.Webhook = Webhook2;
    Webhook2.prefix = "whsec_";
  }
});

// node_modules/svix/dist/webhook.js
var require_webhook = __commonJS({
  "node_modules/svix/dist/webhook.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Webhook = exports.WebhookVerificationError = void 0;
    var standardwebhooks_1 = require_dist();
    var standardwebhooks_2 = require_dist();
    Object.defineProperty(exports, "WebhookVerificationError", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return standardwebhooks_2.WebhookVerificationError;
    }, "get") });
    var Webhook2 = class {
      static {
        __name(this, "Webhook");
      }
      constructor(secret, options) {
        this.inner = new standardwebhooks_1.Webhook(secret, options);
      }
      verify(payload, headers_) {
        var _a, _b, _c, _d, _e, _f;
        const headers = {};
        for (const key of Object.keys(headers_)) {
          headers[key.toLowerCase()] = headers_[key];
        }
        headers["webhook-id"] = (_b = (_a = headers["svix-id"]) !== null && _a !== void 0 ? _a : headers["webhook-id"]) !== null && _b !== void 0 ? _b : "";
        headers["webhook-signature"] = (_d = (_c = headers["svix-signature"]) !== null && _c !== void 0 ? _c : headers["webhook-signature"]) !== null && _d !== void 0 ? _d : "";
        headers["webhook-timestamp"] = (_f = (_e = headers["svix-timestamp"]) !== null && _e !== void 0 ? _e : headers["webhook-timestamp"]) !== null && _f !== void 0 ? _f : "";
        return this.inner.verify(payload, headers);
      }
      sign(msgId, timestamp, payload) {
        return this.inner.sign(msgId, timestamp, payload);
      }
    };
    exports.Webhook = Webhook2;
  }
});

// node_modules/svix/dist/models/endpointDisabledTrigger.js
var require_endpointDisabledTrigger = __commonJS({
  "node_modules/svix/dist/models/endpointDisabledTrigger.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EndpointDisabledTriggerSerializer = exports.EndpointDisabledTrigger = void 0;
    var EndpointDisabledTrigger;
    (function(EndpointDisabledTrigger2) {
      EndpointDisabledTrigger2["Manual"] = "manual";
      EndpointDisabledTrigger2["Automatic"] = "automatic";
    })(EndpointDisabledTrigger = exports.EndpointDisabledTrigger || (exports.EndpointDisabledTrigger = {}));
    exports.EndpointDisabledTriggerSerializer = {
      _fromJsonObject(object2) {
        return object2;
      },
      _toJsonObject(self2) {
        return self2;
      }
    };
  }
});

// node_modules/svix/dist/models/ordering.js
var require_ordering = __commonJS({
  "node_modules/svix/dist/models/ordering.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OrderingSerializer = exports.Ordering = void 0;
    var Ordering;
    (function(Ordering2) {
      Ordering2["Ascending"] = "ascending";
      Ordering2["Descending"] = "descending";
    })(Ordering = exports.Ordering || (exports.Ordering = {}));
    exports.OrderingSerializer = {
      _fromJsonObject(object2) {
        return object2;
      },
      _toJsonObject(self2) {
        return self2;
      }
    };
  }
});

// node_modules/svix/dist/models/statusCodeClass.js
var require_statusCodeClass = __commonJS({
  "node_modules/svix/dist/models/statusCodeClass.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StatusCodeClassSerializer = exports.StatusCodeClass = void 0;
    var StatusCodeClass;
    (function(StatusCodeClass2) {
      StatusCodeClass2[StatusCodeClass2["CodeNone"] = 0] = "CodeNone";
      StatusCodeClass2[StatusCodeClass2["Code1xx"] = 100] = "Code1xx";
      StatusCodeClass2[StatusCodeClass2["Code2xx"] = 200] = "Code2xx";
      StatusCodeClass2[StatusCodeClass2["Code3xx"] = 300] = "Code3xx";
      StatusCodeClass2[StatusCodeClass2["Code4xx"] = 400] = "Code4xx";
      StatusCodeClass2[StatusCodeClass2["Code5xx"] = 500] = "Code5xx";
    })(StatusCodeClass = exports.StatusCodeClass || (exports.StatusCodeClass = {}));
    exports.StatusCodeClassSerializer = {
      _fromJsonObject(object2) {
        return object2;
      },
      _toJsonObject(self2) {
        return self2;
      }
    };
  }
});

// node_modules/svix/dist/models/index.js
var require_models = __commonJS({
  "node_modules/svix/dist/models/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StatusCodeClass = exports.SinkStatusIn = exports.SinkStatus = exports.Ordering = exports.MessageStatusText = exports.MessageStatus = exports.MessageAttemptTriggerType = exports.EndpointDisabledTrigger = exports.ConnectorProduct = exports.ConnectorKind = exports.BackgroundTaskType = exports.BackgroundTaskStatus = exports.AppPortalCapability = void 0;
    var appPortalCapability_1 = require_appPortalCapability();
    Object.defineProperty(exports, "AppPortalCapability", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return appPortalCapability_1.AppPortalCapability;
    }, "get") });
    var backgroundTaskStatus_1 = require_backgroundTaskStatus();
    Object.defineProperty(exports, "BackgroundTaskStatus", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return backgroundTaskStatus_1.BackgroundTaskStatus;
    }, "get") });
    var backgroundTaskType_1 = require_backgroundTaskType();
    Object.defineProperty(exports, "BackgroundTaskType", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return backgroundTaskType_1.BackgroundTaskType;
    }, "get") });
    var connectorKind_1 = require_connectorKind();
    Object.defineProperty(exports, "ConnectorKind", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return connectorKind_1.ConnectorKind;
    }, "get") });
    var connectorProduct_1 = require_connectorProduct();
    Object.defineProperty(exports, "ConnectorProduct", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return connectorProduct_1.ConnectorProduct;
    }, "get") });
    var endpointDisabledTrigger_1 = require_endpointDisabledTrigger();
    Object.defineProperty(exports, "EndpointDisabledTrigger", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return endpointDisabledTrigger_1.EndpointDisabledTrigger;
    }, "get") });
    var messageAttemptTriggerType_1 = require_messageAttemptTriggerType();
    Object.defineProperty(exports, "MessageAttemptTriggerType", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return messageAttemptTriggerType_1.MessageAttemptTriggerType;
    }, "get") });
    var messageStatus_1 = require_messageStatus();
    Object.defineProperty(exports, "MessageStatus", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return messageStatus_1.MessageStatus;
    }, "get") });
    var messageStatusText_1 = require_messageStatusText();
    Object.defineProperty(exports, "MessageStatusText", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return messageStatusText_1.MessageStatusText;
    }, "get") });
    var ordering_1 = require_ordering();
    Object.defineProperty(exports, "Ordering", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return ordering_1.Ordering;
    }, "get") });
    var sinkStatus_1 = require_sinkStatus();
    Object.defineProperty(exports, "SinkStatus", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return sinkStatus_1.SinkStatus;
    }, "get") });
    var sinkStatusIn_1 = require_sinkStatusIn();
    Object.defineProperty(exports, "SinkStatusIn", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return sinkStatusIn_1.SinkStatusIn;
    }, "get") });
    var statusCodeClass_1 = require_statusCodeClass();
    Object.defineProperty(exports, "StatusCodeClass", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return statusCodeClass_1.StatusCodeClass;
    }, "get") });
  }
});

// node_modules/svix/dist/index.js
var require_dist2 = __commonJS({
  "node_modules/svix/dist/index.js"(exports) {
    "use strict";
    var __createBinding = exports && exports.__createBinding || (Object.create ? (function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc3 = Object.getOwnPropertyDescriptor(m, k);
      if (!desc3 || ("get" in desc3 ? !m.__esModule : desc3.writable || desc3.configurable)) {
        desc3 = { enumerable: true, get: /* @__PURE__ */ __name(function() {
          return m[k];
        }, "get") };
      }
      Object.defineProperty(o, k2, desc3);
    }) : (function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    }));
    var __exportStar = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Svix = exports.messageInRaw = exports.ValidationError = exports.HttpErrorOut = exports.HTTPValidationError = exports.ApiException = void 0;
    var application_1 = require_application();
    var authentication_1 = require_authentication();
    var backgroundTask_1 = require_backgroundTask();
    var connector_1 = require_connector();
    var endpoint_1 = require_endpoint();
    var environment_1 = require_environment();
    var eventType_1 = require_eventType();
    var health_1 = require_health();
    var ingest_1 = require_ingest();
    var integration_1 = require_integration();
    var message_1 = require_message();
    var messageAttempt_1 = require_messageAttempt();
    var operationalWebhook_1 = require_operationalWebhook();
    var statistics_1 = require_statistics();
    var streaming_1 = require_streaming();
    var operationalWebhookEndpoint_1 = require_operationalWebhookEndpoint();
    var util_1 = require_util();
    Object.defineProperty(exports, "ApiException", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return util_1.ApiException;
    }, "get") });
    var HttpErrors_1 = require_HttpErrors();
    Object.defineProperty(exports, "HTTPValidationError", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return HttpErrors_1.HTTPValidationError;
    }, "get") });
    Object.defineProperty(exports, "HttpErrorOut", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return HttpErrors_1.HttpErrorOut;
    }, "get") });
    Object.defineProperty(exports, "ValidationError", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return HttpErrors_1.ValidationError;
    }, "get") });
    __exportStar(require_webhook(), exports);
    __exportStar(require_models(), exports);
    var message_2 = require_message();
    Object.defineProperty(exports, "messageInRaw", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return message_2.messageInRaw;
    }, "get") });
    var REGIONS = [
      { region: "us", url: "https://api.us.svix.com" },
      { region: "eu", url: "https://api.eu.svix.com" },
      { region: "in", url: "https://api.in.svix.com" },
      { region: "ca", url: "https://api.ca.svix.com" },
      { region: "au", url: "https://api.au.svix.com" }
    ];
    var Svix = class {
      static {
        __name(this, "Svix");
      }
      constructor(token, options = {}) {
        var _a, _b, _c;
        const regionalUrl = (_a = REGIONS.find((x) => x.region === token.split(".")[1])) === null || _a === void 0 ? void 0 : _a.url;
        const baseUrl2 = (_c = (_b = options.serverUrl) !== null && _b !== void 0 ? _b : regionalUrl) !== null && _c !== void 0 ? _c : "https://api.svix.com";
        if (options.retryScheduleInMs) {
          this.requestCtx = {
            baseUrl: baseUrl2,
            token,
            timeout: options.requestTimeout,
            retryScheduleInMs: options.retryScheduleInMs,
            fetch: options.fetch
          };
          return;
        }
        if (options.numRetries) {
          this.requestCtx = {
            baseUrl: baseUrl2,
            token,
            timeout: options.requestTimeout,
            numRetries: options.numRetries,
            fetch: options.fetch
          };
          return;
        }
        this.requestCtx = {
          baseUrl: baseUrl2,
          token,
          timeout: options.requestTimeout,
          fetch: options.fetch
        };
      }
      get application() {
        return new application_1.Application(this.requestCtx);
      }
      get authentication() {
        return new authentication_1.Authentication(this.requestCtx);
      }
      get backgroundTask() {
        return new backgroundTask_1.BackgroundTask(this.requestCtx);
      }
      get connector() {
        return new connector_1.Connector(this.requestCtx);
      }
      get endpoint() {
        return new endpoint_1.Endpoint(this.requestCtx);
      }
      get environment() {
        return new environment_1.Environment(this.requestCtx);
      }
      get eventType() {
        return new eventType_1.EventType(this.requestCtx);
      }
      get health() {
        return new health_1.Health(this.requestCtx);
      }
      get ingest() {
        return new ingest_1.Ingest(this.requestCtx);
      }
      get integration() {
        return new integration_1.Integration(this.requestCtx);
      }
      get message() {
        return new message_1.Message(this.requestCtx);
      }
      get messageAttempt() {
        return new messageAttempt_1.MessageAttempt(this.requestCtx);
      }
      get operationalWebhook() {
        return new operationalWebhook_1.OperationalWebhook(this.requestCtx);
      }
      get statistics() {
        return new statistics_1.Statistics(this.requestCtx);
      }
      get streaming() {
        return new streaming_1.Streaming(this.requestCtx);
      }
      get operationalWebhookEndpoint() {
        return new operationalWebhookEndpoint_1.OperationalWebhookEndpoint(this.requestCtx);
      }
    };
    exports.Svix = Svix;
  }
});

// node_modules/resend/dist/index.mjs
function buildPaginationQuery(options) {
  const searchParams = new URLSearchParams();
  if (options.limit !== void 0) searchParams.set("limit", options.limit.toString());
  if ("after" in options && options.after !== void 0) searchParams.set("after", options.after);
  if ("before" in options && options.before !== void 0) searchParams.set("before", options.before);
  return searchParams.toString();
}
function parseAttachments(attachments) {
  return attachments?.map((attachment) => ({
    content: attachment.content,
    filename: attachment.filename,
    path: attachment.path,
    content_type: attachment.contentType,
    content_id: attachment.contentId
  }));
}
function parseEmailToApiOptions(email) {
  return {
    attachments: parseAttachments(email.attachments),
    bcc: email.bcc,
    cc: email.cc,
    from: email.from,
    headers: email.headers,
    html: email.html,
    reply_to: email.replyTo,
    scheduled_at: email.scheduledAt,
    subject: email.subject,
    tags: email.tags,
    text: email.text,
    to: email.to,
    template: email.template ? {
      id: email.template.id,
      variables: email.template.variables
    } : void 0,
    topic_id: email.topicId
  };
}
async function render(node) {
  let render$1;
  try {
    ({ render: render$1 } = await import("@react-email/render"));
  } catch {
    throw new Error("Failed to render React component. Make sure to install `@react-email/render` or `@react-email/components`.");
  }
  return render$1(node);
}
function parseContactPropertyFromApi(contactProperty) {
  return {
    id: contactProperty.id,
    key: contactProperty.key,
    createdAt: contactProperty.created_at,
    type: contactProperty.type,
    fallbackValue: contactProperty.fallback_value
  };
}
function parseContactPropertyToApiOptions(contactProperty) {
  if ("key" in contactProperty) return {
    key: contactProperty.key,
    type: contactProperty.type,
    fallback_value: contactProperty.fallbackValue
  };
  return { fallback_value: contactProperty.fallbackValue };
}
function parseDomainToApiOptions(domain) {
  return {
    name: domain.name,
    region: domain.region,
    custom_return_path: domain.customReturnPath,
    capabilities: domain.capabilities,
    open_tracking: domain.openTracking,
    click_tracking: domain.clickTracking,
    tls: domain.tls
  };
}
function getPaginationQueryProperties(options = {}) {
  const query = new URLSearchParams();
  if (options.before) query.set("before", options.before);
  if (options.after) query.set("after", options.after);
  if (options.limit) query.set("limit", options.limit.toString());
  return query.size > 0 ? `?${query.toString()}` : "";
}
function parseVariables(variables) {
  return variables?.map((variable) => ({
    key: variable.key,
    type: variable.type,
    fallback_value: variable.fallbackValue
  }));
}
function parseTemplateToApiOptions(template) {
  return {
    name: "name" in template ? template.name : void 0,
    subject: template.subject,
    html: template.html,
    text: template.text,
    alias: template.alias,
    from: template.from,
    reply_to: template.replyTo,
    variables: parseVariables(template.variables)
  };
}
var import_svix, version4, ApiKeys, Batch4, Broadcasts, ContactProperties, ContactSegments, ContactTopics, Contacts, Domains, Attachments$1, Attachments, Receiving, Emails, Segments, ChainableTemplateResult, Templates, Topics, Webhooks, defaultBaseUrl, defaultUserAgent, baseUrl, userAgent, Resend;
var init_dist = __esm({
  "node_modules/resend/dist/index.mjs"() {
    import_svix = __toESM(require_dist2(), 1);
    version4 = "6.8.0";
    __name(buildPaginationQuery, "buildPaginationQuery");
    ApiKeys = class {
      static {
        __name(this, "ApiKeys");
      }
      constructor(resend) {
        this.resend = resend;
      }
      async create(payload, options = {}) {
        return await this.resend.post("/api-keys", payload, options);
      }
      async list(options = {}) {
        const queryString = buildPaginationQuery(options);
        const url = queryString ? `/api-keys?${queryString}` : "/api-keys";
        return await this.resend.get(url);
      }
      async remove(id) {
        return await this.resend.delete(`/api-keys/${id}`);
      }
    };
    __name(parseAttachments, "parseAttachments");
    __name(parseEmailToApiOptions, "parseEmailToApiOptions");
    __name(render, "render");
    Batch4 = class {
      static {
        __name(this, "Batch");
      }
      constructor(resend) {
        this.resend = resend;
      }
      async send(payload, options) {
        return this.create(payload, options);
      }
      async create(payload, options) {
        const emails = [];
        for (const email of payload) {
          if (email.react) {
            email.html = await render(email.react);
            email.react = void 0;
          }
          emails.push(parseEmailToApiOptions(email));
        }
        return await this.resend.post("/emails/batch", emails, {
          ...options,
          headers: {
            "x-batch-validation": options?.batchValidation ?? "strict",
            ...options?.headers
          }
        });
      }
    };
    Broadcasts = class {
      static {
        __name(this, "Broadcasts");
      }
      constructor(resend) {
        this.resend = resend;
      }
      async create(payload, options = {}) {
        if (payload.react) payload.html = await render(payload.react);
        return await this.resend.post("/broadcasts", {
          name: payload.name,
          segment_id: payload.segmentId,
          audience_id: payload.audienceId,
          preview_text: payload.previewText,
          from: payload.from,
          html: payload.html,
          reply_to: payload.replyTo,
          subject: payload.subject,
          text: payload.text,
          topic_id: payload.topicId,
          send: payload.send,
          scheduled_at: payload.scheduledAt
        }, options);
      }
      async send(id, payload) {
        return await this.resend.post(`/broadcasts/${id}/send`, { scheduled_at: payload?.scheduledAt });
      }
      async list(options = {}) {
        const queryString = buildPaginationQuery(options);
        const url = queryString ? `/broadcasts?${queryString}` : "/broadcasts";
        return await this.resend.get(url);
      }
      async get(id) {
        return await this.resend.get(`/broadcasts/${id}`);
      }
      async remove(id) {
        return await this.resend.delete(`/broadcasts/${id}`);
      }
      async update(id, payload) {
        if (payload.react) payload.html = await render(payload.react);
        return await this.resend.patch(`/broadcasts/${id}`, {
          name: payload.name,
          segment_id: payload.segmentId,
          audience_id: payload.audienceId,
          from: payload.from,
          html: payload.html,
          text: payload.text,
          subject: payload.subject,
          reply_to: payload.replyTo,
          preview_text: payload.previewText,
          topic_id: payload.topicId
        });
      }
    };
    __name(parseContactPropertyFromApi, "parseContactPropertyFromApi");
    __name(parseContactPropertyToApiOptions, "parseContactPropertyToApiOptions");
    ContactProperties = class {
      static {
        __name(this, "ContactProperties");
      }
      constructor(resend) {
        this.resend = resend;
      }
      async create(options) {
        const apiOptions = parseContactPropertyToApiOptions(options);
        return await this.resend.post("/contact-properties", apiOptions);
      }
      async list(options = {}) {
        const queryString = buildPaginationQuery(options);
        const url = queryString ? `/contact-properties?${queryString}` : "/contact-properties";
        const response = await this.resend.get(url);
        if (response.data) return {
          data: {
            ...response.data,
            data: response.data.data.map((apiContactProperty) => parseContactPropertyFromApi(apiContactProperty))
          },
          headers: response.headers,
          error: null
        };
        return response;
      }
      async get(id) {
        if (!id) return {
          data: null,
          headers: null,
          error: {
            message: "Missing `id` field.",
            statusCode: null,
            name: "missing_required_field"
          }
        };
        const response = await this.resend.get(`/contact-properties/${id}`);
        if (response.data) return {
          data: {
            object: "contact_property",
            ...parseContactPropertyFromApi(response.data)
          },
          headers: response.headers,
          error: null
        };
        return response;
      }
      async update(payload) {
        if (!payload.id) return {
          data: null,
          headers: null,
          error: {
            message: "Missing `id` field.",
            statusCode: null,
            name: "missing_required_field"
          }
        };
        const apiOptions = parseContactPropertyToApiOptions(payload);
        return await this.resend.patch(`/contact-properties/${payload.id}`, apiOptions);
      }
      async remove(id) {
        if (!id) return {
          data: null,
          headers: null,
          error: {
            message: "Missing `id` field.",
            statusCode: null,
            name: "missing_required_field"
          }
        };
        return await this.resend.delete(`/contact-properties/${id}`);
      }
    };
    ContactSegments = class {
      static {
        __name(this, "ContactSegments");
      }
      constructor(resend) {
        this.resend = resend;
      }
      async list(options) {
        if (!options.contactId && !options.email) return {
          data: null,
          headers: null,
          error: {
            message: "Missing `id` or `email` field.",
            statusCode: null,
            name: "missing_required_field"
          }
        };
        const identifier = options.email ? options.email : options.contactId;
        const queryString = buildPaginationQuery(options);
        const url = queryString ? `/contacts/${identifier}/segments?${queryString}` : `/contacts/${identifier}/segments`;
        return await this.resend.get(url);
      }
      async add(options) {
        if (!options.contactId && !options.email) return {
          data: null,
          headers: null,
          error: {
            message: "Missing `id` or `email` field.",
            statusCode: null,
            name: "missing_required_field"
          }
        };
        const identifier = options.email ? options.email : options.contactId;
        return this.resend.post(`/contacts/${identifier}/segments/${options.segmentId}`);
      }
      async remove(options) {
        if (!options.contactId && !options.email) return {
          data: null,
          headers: null,
          error: {
            message: "Missing `id` or `email` field.",
            statusCode: null,
            name: "missing_required_field"
          }
        };
        const identifier = options.email ? options.email : options.contactId;
        return this.resend.delete(`/contacts/${identifier}/segments/${options.segmentId}`);
      }
    };
    ContactTopics = class {
      static {
        __name(this, "ContactTopics");
      }
      constructor(resend) {
        this.resend = resend;
      }
      async update(payload) {
        if (!payload.id && !payload.email) return {
          data: null,
          headers: null,
          error: {
            message: "Missing `id` or `email` field.",
            statusCode: null,
            name: "missing_required_field"
          }
        };
        const identifier = payload.email ? payload.email : payload.id;
        return this.resend.patch(`/contacts/${identifier}/topics`, payload.topics);
      }
      async list(options) {
        if (!options.id && !options.email) return {
          data: null,
          headers: null,
          error: {
            message: "Missing `id` or `email` field.",
            statusCode: null,
            name: "missing_required_field"
          }
        };
        const identifier = options.email ? options.email : options.id;
        const queryString = buildPaginationQuery(options);
        const url = queryString ? `/contacts/${identifier}/topics?${queryString}` : `/contacts/${identifier}/topics`;
        return this.resend.get(url);
      }
    };
    Contacts = class {
      static {
        __name(this, "Contacts");
      }
      constructor(resend) {
        this.resend = resend;
        this.topics = new ContactTopics(this.resend);
        this.segments = new ContactSegments(this.resend);
      }
      async create(payload, options = {}) {
        if (!payload.audienceId) return await this.resend.post("/contacts", {
          unsubscribed: payload.unsubscribed,
          email: payload.email,
          first_name: payload.firstName,
          last_name: payload.lastName,
          properties: payload.properties
        }, options);
        return await this.resend.post(`/audiences/${payload.audienceId}/contacts`, {
          unsubscribed: payload.unsubscribed,
          email: payload.email,
          first_name: payload.firstName,
          last_name: payload.lastName,
          properties: payload.properties
        }, options);
      }
      async list(options = {}) {
        const segmentId2 = options.segmentId ?? options.audienceId;
        if (!segmentId2) {
          const queryString$1 = buildPaginationQuery(options);
          const url$1 = queryString$1 ? `/contacts?${queryString$1}` : "/contacts";
          return await this.resend.get(url$1);
        }
        const queryString = buildPaginationQuery(options);
        const url = queryString ? `/segments/${segmentId2}/contacts?${queryString}` : `/segments/${segmentId2}/contacts`;
        return await this.resend.get(url);
      }
      async get(options) {
        if (typeof options === "string") return this.resend.get(`/contacts/${options}`);
        if (!options.id && !options.email) return {
          data: null,
          headers: null,
          error: {
            message: "Missing `id` or `email` field.",
            statusCode: null,
            name: "missing_required_field"
          }
        };
        if (!options.audienceId) return this.resend.get(`/contacts/${options?.email ? options?.email : options?.id}`);
        return this.resend.get(`/audiences/${options.audienceId}/contacts/${options?.email ? options?.email : options?.id}`);
      }
      async update(options) {
        if (!options.id && !options.email) return {
          data: null,
          headers: null,
          error: {
            message: "Missing `id` or `email` field.",
            statusCode: null,
            name: "missing_required_field"
          }
        };
        if (!options.audienceId) return await this.resend.patch(`/contacts/${options?.email ? options?.email : options?.id}`, {
          unsubscribed: options.unsubscribed,
          first_name: options.firstName,
          last_name: options.lastName,
          properties: options.properties
        });
        return await this.resend.patch(`/audiences/${options.audienceId}/contacts/${options?.email ? options?.email : options?.id}`, {
          unsubscribed: options.unsubscribed,
          first_name: options.firstName,
          last_name: options.lastName,
          properties: options.properties
        });
      }
      async remove(payload) {
        if (typeof payload === "string") return this.resend.delete(`/contacts/${payload}`);
        if (!payload.id && !payload.email) return {
          data: null,
          headers: null,
          error: {
            message: "Missing `id` or `email` field.",
            statusCode: null,
            name: "missing_required_field"
          }
        };
        if (!payload.audienceId) return this.resend.delete(`/contacts/${payload?.email ? payload?.email : payload?.id}`);
        return this.resend.delete(`/audiences/${payload.audienceId}/contacts/${payload?.email ? payload?.email : payload?.id}`);
      }
    };
    __name(parseDomainToApiOptions, "parseDomainToApiOptions");
    Domains = class {
      static {
        __name(this, "Domains");
      }
      constructor(resend) {
        this.resend = resend;
      }
      async create(payload, options = {}) {
        return await this.resend.post("/domains", parseDomainToApiOptions(payload), options);
      }
      async list(options = {}) {
        const queryString = buildPaginationQuery(options);
        const url = queryString ? `/domains?${queryString}` : "/domains";
        return await this.resend.get(url);
      }
      async get(id) {
        return await this.resend.get(`/domains/${id}`);
      }
      async update(payload) {
        return await this.resend.patch(`/domains/${payload.id}`, {
          click_tracking: payload.clickTracking,
          open_tracking: payload.openTracking,
          tls: payload.tls,
          capabilities: payload.capabilities
        });
      }
      async remove(id) {
        return await this.resend.delete(`/domains/${id}`);
      }
      async verify(id) {
        return await this.resend.post(`/domains/${id}/verify`);
      }
    };
    Attachments$1 = class {
      static {
        __name(this, "Attachments$1");
      }
      constructor(resend) {
        this.resend = resend;
      }
      async get(options) {
        const { emailId, id } = options;
        return await this.resend.get(`/emails/${emailId}/attachments/${id}`);
      }
      async list(options) {
        const { emailId } = options;
        const queryString = buildPaginationQuery(options);
        const url = queryString ? `/emails/${emailId}/attachments?${queryString}` : `/emails/${emailId}/attachments`;
        return await this.resend.get(url);
      }
    };
    Attachments = class {
      static {
        __name(this, "Attachments");
      }
      constructor(resend) {
        this.resend = resend;
      }
      async get(options) {
        const { emailId, id } = options;
        return await this.resend.get(`/emails/receiving/${emailId}/attachments/${id}`);
      }
      async list(options) {
        const { emailId } = options;
        const queryString = buildPaginationQuery(options);
        const url = queryString ? `/emails/receiving/${emailId}/attachments?${queryString}` : `/emails/receiving/${emailId}/attachments`;
        return await this.resend.get(url);
      }
    };
    Receiving = class {
      static {
        __name(this, "Receiving");
      }
      constructor(resend) {
        this.resend = resend;
        this.attachments = new Attachments(resend);
      }
      async get(id) {
        return await this.resend.get(`/emails/receiving/${id}`);
      }
      async list(options = {}) {
        const queryString = buildPaginationQuery(options);
        const url = queryString ? `/emails/receiving?${queryString}` : "/emails/receiving";
        return await this.resend.get(url);
      }
    };
    Emails = class {
      static {
        __name(this, "Emails");
      }
      constructor(resend) {
        this.resend = resend;
        this.attachments = new Attachments$1(resend);
        this.receiving = new Receiving(resend);
      }
      async send(payload, options = {}) {
        return this.create(payload, options);
      }
      async create(payload, options = {}) {
        if (payload.react) payload.html = await render(payload.react);
        return await this.resend.post("/emails", parseEmailToApiOptions(payload), options);
      }
      async get(id) {
        return await this.resend.get(`/emails/${id}`);
      }
      async list(options = {}) {
        const queryString = buildPaginationQuery(options);
        const url = queryString ? `/emails?${queryString}` : "/emails";
        return await this.resend.get(url);
      }
      async update(payload) {
        return await this.resend.patch(`/emails/${payload.id}`, { scheduled_at: payload.scheduledAt });
      }
      async cancel(id) {
        return await this.resend.post(`/emails/${id}/cancel`);
      }
    };
    Segments = class {
      static {
        __name(this, "Segments");
      }
      constructor(resend) {
        this.resend = resend;
      }
      async create(payload, options = {}) {
        return await this.resend.post("/segments", payload, options);
      }
      async list(options = {}) {
        const queryString = buildPaginationQuery(options);
        const url = queryString ? `/segments?${queryString}` : "/segments";
        return await this.resend.get(url);
      }
      async get(id) {
        return await this.resend.get(`/segments/${id}`);
      }
      async remove(id) {
        return await this.resend.delete(`/segments/${id}`);
      }
    };
    __name(getPaginationQueryProperties, "getPaginationQueryProperties");
    __name(parseVariables, "parseVariables");
    __name(parseTemplateToApiOptions, "parseTemplateToApiOptions");
    ChainableTemplateResult = class {
      static {
        __name(this, "ChainableTemplateResult");
      }
      constructor(promise, publishFn) {
        this.promise = promise;
        this.publishFn = publishFn;
      }
      then(onfulfilled, onrejected) {
        return this.promise.then(onfulfilled, onrejected);
      }
      async publish() {
        const { data, error } = await this.promise;
        if (error) return {
          data: null,
          headers: null,
          error
        };
        return this.publishFn(data.id);
      }
    };
    Templates = class {
      static {
        __name(this, "Templates");
      }
      constructor(resend) {
        this.resend = resend;
      }
      create(payload) {
        return new ChainableTemplateResult(this.performCreate(payload), this.publish.bind(this));
      }
      async performCreate(payload) {
        if (payload.react) {
          if (!this.renderAsync) try {
            const { renderAsync } = await import("@react-email/render");
            this.renderAsync = renderAsync;
          } catch {
            throw new Error("Failed to render React component. Make sure to install `@react-email/render`");
          }
          payload.html = await this.renderAsync(payload.react);
        }
        return this.resend.post("/templates", parseTemplateToApiOptions(payload));
      }
      async remove(identifier) {
        return await this.resend.delete(`/templates/${identifier}`);
      }
      async get(identifier) {
        return await this.resend.get(`/templates/${identifier}`);
      }
      async list(options = {}) {
        return this.resend.get(`/templates${getPaginationQueryProperties(options)}`);
      }
      duplicate(identifier) {
        return new ChainableTemplateResult(this.resend.post(`/templates/${identifier}/duplicate`), this.publish.bind(this));
      }
      async publish(identifier) {
        return await this.resend.post(`/templates/${identifier}/publish`);
      }
      async update(identifier, payload) {
        return await this.resend.patch(`/templates/${identifier}`, parseTemplateToApiOptions(payload));
      }
    };
    Topics = class {
      static {
        __name(this, "Topics");
      }
      constructor(resend) {
        this.resend = resend;
      }
      async create(payload) {
        const { defaultSubscription, ...body } = payload;
        return await this.resend.post("/topics", {
          ...body,
          default_subscription: defaultSubscription
        });
      }
      async list() {
        return await this.resend.get("/topics");
      }
      async get(id) {
        if (!id) return {
          data: null,
          headers: null,
          error: {
            message: "Missing `id` field.",
            statusCode: null,
            name: "missing_required_field"
          }
        };
        return await this.resend.get(`/topics/${id}`);
      }
      async update(payload) {
        if (!payload.id) return {
          data: null,
          headers: null,
          error: {
            message: "Missing `id` field.",
            statusCode: null,
            name: "missing_required_field"
          }
        };
        return await this.resend.patch(`/topics/${payload.id}`, payload);
      }
      async remove(id) {
        if (!id) return {
          data: null,
          headers: null,
          error: {
            message: "Missing `id` field.",
            statusCode: null,
            name: "missing_required_field"
          }
        };
        return await this.resend.delete(`/topics/${id}`);
      }
    };
    Webhooks = class {
      static {
        __name(this, "Webhooks");
      }
      constructor(resend) {
        this.resend = resend;
      }
      async create(payload, options = {}) {
        return await this.resend.post("/webhooks", payload, options);
      }
      async get(id) {
        return await this.resend.get(`/webhooks/${id}`);
      }
      async list(options = {}) {
        const queryString = buildPaginationQuery(options);
        const url = queryString ? `/webhooks?${queryString}` : "/webhooks";
        return await this.resend.get(url);
      }
      async update(id, payload) {
        return await this.resend.patch(`/webhooks/${id}`, payload);
      }
      async remove(id) {
        return await this.resend.delete(`/webhooks/${id}`);
      }
      verify(payload) {
        return new import_svix.Webhook(payload.webhookSecret).verify(payload.payload, {
          "svix-id": payload.headers.id,
          "svix-timestamp": payload.headers.timestamp,
          "svix-signature": payload.headers.signature
        });
      }
    };
    defaultBaseUrl = "https://api.resend.com";
    defaultUserAgent = `resend-node:${version4}`;
    baseUrl = typeof process !== "undefined" && process.env ? process.env.RESEND_BASE_URL || defaultBaseUrl : defaultBaseUrl;
    userAgent = typeof process !== "undefined" && process.env ? process.env.RESEND_USER_AGENT || defaultUserAgent : defaultUserAgent;
    Resend = class {
      static {
        __name(this, "Resend");
      }
      constructor(key) {
        this.key = key;
        this.apiKeys = new ApiKeys(this);
        this.segments = new Segments(this);
        this.audiences = this.segments;
        this.batch = new Batch4(this);
        this.broadcasts = new Broadcasts(this);
        this.contacts = new Contacts(this);
        this.contactProperties = new ContactProperties(this);
        this.domains = new Domains(this);
        this.emails = new Emails(this);
        this.webhooks = new Webhooks(this);
        this.templates = new Templates(this);
        this.topics = new Topics(this);
        if (!key) {
          if (typeof process !== "undefined" && process.env) this.key = process.env.RESEND_API_KEY;
          if (!this.key) throw new Error('Missing API key. Pass it to the constructor `new Resend("re_123")`');
        }
        this.headers = new Headers({
          Authorization: `Bearer ${this.key}`,
          "User-Agent": userAgent,
          "Content-Type": "application/json"
        });
      }
      async fetchRequest(path, options = {}) {
        try {
          const response = await fetch(`${baseUrl}${path}`, options);
          if (!response.ok) try {
            const rawError = await response.text();
            return {
              data: null,
              error: JSON.parse(rawError),
              headers: Object.fromEntries(response.headers.entries())
            };
          } catch (err) {
            if (err instanceof SyntaxError) return {
              data: null,
              error: {
                name: "application_error",
                statusCode: response.status,
                message: "Internal server error. We are unable to process your request right now, please try again later."
              },
              headers: Object.fromEntries(response.headers.entries())
            };
            const error = {
              message: response.statusText,
              statusCode: response.status,
              name: "application_error"
            };
            if (err instanceof Error) return {
              data: null,
              error: {
                ...error,
                message: err.message
              },
              headers: Object.fromEntries(response.headers.entries())
            };
            return {
              data: null,
              error,
              headers: Object.fromEntries(response.headers.entries())
            };
          }
          return {
            data: await response.json(),
            error: null,
            headers: Object.fromEntries(response.headers.entries())
          };
        } catch {
          return {
            data: null,
            error: {
              name: "application_error",
              statusCode: null,
              message: "Unable to fetch data. The request could not be resolved."
            },
            headers: null
          };
        }
      }
      async post(path, entity, options = {}) {
        const headers = new Headers(this.headers);
        if (options.headers) for (const [key, value] of new Headers(options.headers).entries()) headers.set(key, value);
        if (options.idempotencyKey) headers.set("Idempotency-Key", options.idempotencyKey);
        const requestOptions = {
          method: "POST",
          body: JSON.stringify(entity),
          ...options,
          headers
        };
        return this.fetchRequest(path, requestOptions);
      }
      async get(path, options = {}) {
        const headers = new Headers(this.headers);
        if (options.headers) for (const [key, value] of new Headers(options.headers).entries()) headers.set(key, value);
        const requestOptions = {
          method: "GET",
          ...options,
          headers
        };
        return this.fetchRequest(path, requestOptions);
      }
      async put(path, entity, options = {}) {
        const headers = new Headers(this.headers);
        if (options.headers) for (const [key, value] of new Headers(options.headers).entries()) headers.set(key, value);
        const requestOptions = {
          method: "PUT",
          body: JSON.stringify(entity),
          ...options,
          headers
        };
        return this.fetchRequest(path, requestOptions);
      }
      async patch(path, entity, options = {}) {
        const headers = new Headers(this.headers);
        if (options.headers) for (const [key, value] of new Headers(options.headers).entries()) headers.set(key, value);
        const requestOptions = {
          method: "PATCH",
          body: JSON.stringify(entity),
          ...options,
          headers
        };
        return this.fetchRequest(path, requestOptions);
      }
      async delete(path, query) {
        const requestOptions = {
          method: "DELETE",
          body: JSON.stringify(query),
          headers: this.headers
        };
        return this.fetchRequest(path, requestOptions);
      }
    };
  }
});

// worker/src/email.ts
var APP_URL, EmailService;
var init_email = __esm({
  "worker/src/email.ts"() {
    "use strict";
    init_dist();
    APP_URL = "https://runna-io.pages.dev";
    EmailService = class {
      static {
        __name(this, "EmailService");
      }
      resend;
      provider = "resend";
      apiKey;
      fromEmail = "onboarding@resend.dev";
      fromName = "Runna.io";
      constructor(apiKey, fromEmail, provider) {
        this.apiKey = apiKey;
        this.provider = provider || "resend";
        if (this.provider === "resend") {
          this.resend = new Resend(apiKey);
        }
        if (fromEmail && fromEmail.trim().length > 0) {
          this.fromEmail = fromEmail.trim();
        }
      }
      async send(to, subject, html) {
        if (this.provider === "sendgrid") {
          const payload = {
            personalizations: [
              {
                to: [{ email: to }]
              }
            ],
            from: { email: this.fromEmail, name: this.fromName },
            subject,
            content: [{ type: "text/html", value: html }]
          };
          const resp = await fetch("https://api.sendgrid.com/v3/mail/send", {
            method: "POST",
            headers: {
              Authorization: `Bearer ${this.apiKey}`,
              "Content-Type": "application/json"
            },
            body: JSON.stringify(payload)
          });
          if (!resp.ok) {
            const text3 = await resp.text();
            console.error("[EMAIL] SendGrid send failed:", resp.status, text3);
            throw new Error(`SendGrid error ${resp.status}`);
          }
          return;
        }
        if (!this.resend) throw new Error("Resend client not initialized");
        await this.resend.emails.send({
          from: `${this.fromName} <${this.fromEmail}>`,
          to,
          subject,
          html
        });
      }
      async sendFriendRequestEmail(recipientEmail, senderName, senderUsername) {
        try {
          await this.send(
            recipientEmail,
            `\xA1${senderName} te envi\xF3 una solicitud de amistad en Runna.io!`,
            `
          <div style="font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif; max-width: 600px; margin: 0 auto; padding: 20px;">
            <div style="text-align: center; margin-bottom: 30px;">
              <h1 style="color: #1a1a1a; margin: 0;">\u{1F91D} Nueva Solicitud de Amistad</h1>
            </div>
            
            <div style="background: #f8f9fa; padding: 20px; border-radius: 8px; margin-bottom: 20px;">
              <p style="font-size: 16px; color: #555; margin: 0;">
                <strong>${senderName}</strong> (@${senderUsername}) te ha enviado una solicitud de amistad en Runna.io.
              </p>
            </div>

            <div style="text-align: center; margin: 30px 0;">
              <a href="${APP_URL}/friends" style="display: inline-block; background: #3b82f6; color: white; padding: 12px 24px; border-radius: 6px; text-decoration: none; font-weight: 600;">
                Ver solicitud
              </a>
            </div>

            <p style="color: #999; font-size: 12px; text-align: center; margin-top: 30px;">
              \xA9 2026 Runna.io. Todos los derechos reservados.
            </p>
          </div>
        `
          );
          console.log("[EMAIL] Friend request sent to", recipientEmail);
        } catch (error) {
          console.error("[EMAIL] Failed to send friend request email:", error);
        }
      }
      async sendFriendAcceptedEmail(recipientEmail, acceptorName, acceptorUsername) {
        try {
          await this.send(
            recipientEmail,
            `\xA1${acceptorName} acept\xF3 tu solicitud de amistad!`,
            `
          <div style="font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif; max-width: 600px; margin: 0 auto; padding: 20px;">
            <div style="text-align: center; margin-bottom: 30px;">
              <h1 style="color: #1a1a1a; margin: 0;">\u2705 \xA1Amistad Aceptada!</h1>
            </div>
            
            <div style="background: #f0fdf4; padding: 20px; border-radius: 8px; margin-bottom: 20px;">
              <p style="font-size: 16px; color: #555; margin: 0;">
                <strong>${acceptorName}</strong> (@${acceptorUsername}) acept\xF3 tu solicitud de amistad. \xA1Ahora son amigos en Runna.io!
              </p>
            </div>

            <div style="text-align: center; margin: 30px 0;">
              <a href="${APP_URL}" style="display: inline-block; background: #3b82f6; color: white; padding: 12px 24px; border-radius: 6px; text-decoration: none; font-weight: 600;">
                Ir a Runna.io
              </a>
            </div>

            <p style="color: #999; font-size: 12px; text-align: center; margin-top: 30px;">
              \xA9 2026 Runna.io. Todos los derechos reservados.
            </p>
          </div>
        `
          );
          console.log("[EMAIL] Friend accepted sent to", recipientEmail);
        } catch (error) {
          console.error("[EMAIL] Failed to send friend accepted email:", error);
        }
      }
      async sendTerritoryConqueredEmail(recipientEmail, conquererName, conquererUsername, areaStolen) {
        const kmStolen = (areaStolen / 1e6).toFixed(2);
        try {
          await this.send(
            recipientEmail,
            `\u26A0\uFE0F \xA1${conquererName} te conquist\xF3 ${kmStolen} km\xB2 en Runna.io!`,
            `
          <div style="font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif; max-width: 600px; margin: 0 auto; padding: 20px;">
            <div style="text-align: center; margin-bottom: 30px;">
              <h1 style="color: #dc2626; margin: 0;">\u2694\uFE0F Territorio Conquistado</h1>
            </div>
            
            <div style="background: #fef2f2; border: 2px solid #fca5a5; padding: 20px; border-radius: 8px; margin-bottom: 20px;">
              <p style="font-size: 16px; color: #555; margin: 0;">
                <strong>${conquererName}</strong> (@${conquererUsername}) ha conquistado <strong style="color: #dc2626;">${kmStolen} km\xB2</strong> de tu territorio en Runna.io.
              </p>
            </div>

            <div style="background: #f3f4f6; padding: 15px; border-radius: 6px; margin-bottom: 20px;">
              <p style="color: #666; font-size: 14px; margin: 0;">
                Sigue registrando actividades para reconquistar tu territorio y expandir tu dominio.
              </p>
            </div>

            <div style="text-align: center; margin: 30px 0;">
              <a href="${APP_URL}" style="display: inline-block; background: #3b82f6; color: white; padding: 12px 24px; border-radius: 6px; text-decoration: none; font-weight: 600;">
                Ver mapa
              </a>
            </div>

            <p style="color: #999; font-size: 12px; text-align: center; margin-top: 30px;">
              \xA9 2026 Runna.io. Todos los derechos reservados.
            </p>
          </div>
        `
          );
          console.log("[EMAIL] Territory conquered sent to", recipientEmail);
        } catch (error) {
          console.error("[EMAIL] Failed to send territory conquered email:", error);
        }
      }
      async sendWelcomeEmail(recipientEmail, recipientName) {
        try {
          await this.send(
            recipientEmail,
            "Bienvenido a Runna.io \u{1F680}",
            `
          <div style="font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif; max-width: 640px; margin: 0 auto; padding: 24px;">
            <div style="text-align: center; margin-bottom: 28px;">
              <h1 style="color: #0f172a; margin: 0; font-size: 26px;">\xA1Bienvenido a Runna.io, ${recipientName}!</h1>
              <p style="color: #475569; margin: 8px 0 0; font-size: 15px;">Empieza a conquistar territorio mientras corres.</p>
            </div>

            <div style="background: linear-gradient(135deg, #ecfeff 0%, #e0f2fe 100%); padding: 18px 20px; border-radius: 12px; border: 1px solid #bae6fd; margin-bottom: 18px;">
              <p style="margin: 0; color: #0f172a; font-size: 15px; line-height: 1.5;">
                \u{1F4CD} Corre, registra tus rutas y conquista el mapa. Compite con amigos y defiende tu territorio.
              </p>
            </div>

            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 12px; margin-bottom: 22px;">
              <div style="background: #f8fafc; border: 1px solid #e2e8f0; border-radius: 10px; padding: 14px;">
                <p style="margin: 0; color: #0f172a; font-weight: 600;">1) Crea tu primera ruta</p>
                <p style="margin: 4px 0 0; color: #475569; font-size: 14px;">Activa el tracking y conquista tu primera zona.</p>
              </div>
              <div style="background: #f8fafc; border: 1px solid #e2e8f0; border-radius: 10px; padding: 14px;">
                <p style="margin: 0; color: #0f172a; font-weight: 600;">2) Invita amigos</p>
                <p style="margin: 4px 0 0; color: #475569; font-size: 14px;">Reta a tus amigos para ver qui\xE9n domina la ciudad.</p>
              </div>
              <div style="background: #f8fafc; border: 1px solid #e2e8f0; border-radius: 10px; padding: 14px;">
                <p style="margin: 0; color: #0f172a; font-weight: 600;">3) Sube al ranking</p>
                <p style="margin: 4px 0 0; color: #475569; font-size: 14px;">Gana territorio y escala posiciones.</p>
              </div>
            </div>

            <div style="text-align: center; margin: 24px 0;">
              <a href="${APP_URL}" style="display: inline-block; background: #2563eb; color: white; padding: 14px 22px; border-radius: 10px; text-decoration: none; font-weight: 700; box-shadow: 0 10px 30px rgba(37, 99, 235, 0.25);">
                Abrir Runna.io
              </a>
            </div>

            <p style="color: #94a3b8; font-size: 12px; text-align: center; margin-top: 28px;">
              \xA9 2026 Runna.io. Todo listo para conquistar el mapa.
            </p>
          </div>
        `
          );
          console.log("[EMAIL] Welcome sent to", recipientEmail);
        } catch (error) {
          console.error("[EMAIL] Failed to send welcome email:", JSON.stringify({
            message: error?.message,
            status: error?.status,
            name: error?.name,
            stack: error?.stack
          }, null, 2));
        }
      }
      async recordNotification(storage, userId, notificationType, relatedUserId, subject, body, areaStolen) {
        try {
          await storage.recordEmailNotification({
            userId,
            notificationType,
            relatedUserId,
            subject,
            body,
            areaStolen
          });
        } catch (error) {
          console.error("[EMAIL] Failed to record notification:", error);
        }
      }
      async sendVerificationCode(recipientEmail, recipientName, code) {
        try {
          await this.send(
            recipientEmail,
            `Tu c\xF3digo de verificaci\xF3n: ${code}`,
            `
          <div style="font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif; max-width: 600px; margin: 0 auto; padding: 20px;">
            <div style="text-align: center; margin-bottom: 30px;">
              <h1 style="color: #1a1a1a; margin: 0;">\u2728 Verifica tu cuenta</h1>
            </div>
            
            <div style="background: #f8f9fa; padding: 20px; border-radius: 8px; margin-bottom: 20px; text-align: center;">
              <p style="font-size: 16px; color: #555; margin: 0 0 15px 0;">
                Hola <strong>${recipientName}</strong>, usa este c\xF3digo para verificar tu email:
              </p>
              <div style="background: #1a1a1a; color: white; font-size: 32px; font-weight: bold; letter-spacing: 8px; padding: 20px 30px; border-radius: 12px; display: inline-block;">
                ${code}
              </div>
              <p style="font-size: 14px; color: #888; margin: 15px 0 0 0;">
                El c\xF3digo expira en 10 minutos
              </p>
            </div>

            <p style="color: #666; font-size: 14px; text-align: center;">
              Si no solicitaste este c\xF3digo, ignora este email.
            </p>

            <p style="color: #999; font-size: 12px; text-align: center; margin-top: 30px;">
              \xA9 2026 Runna.io. Todos los derechos reservados.
            </p>
          </div>
        `
          );
          console.log("[EMAIL] Verification code sent to", recipientEmail);
          return true;
        } catch (error) {
          console.error("[EMAIL] Failed to send verification code:", error);
          return false;
        }
      }
    };
  }
});

// shared/colors.ts
var USER_COLORS;
var init_colors = __esm({
  "shared/colors.ts"() {
    "use strict";
    USER_COLORS = [
      "#E41A1C",
      // Rojo
      "#377EB8",
      // Azul
      "#4DAF4A",
      // Verde
      "#984EA3",
      // Morado
      "#FF7F00",
      // Naranja
      "#A65628",
      // Marrón
      "#F781BF",
      // Rosa
      "#999999",
      // Gris
      "#A0E550",
      // Verde Pistacho Chillón
      "#FC8D62",
      // Coral
      "#8DA0CB",
      // Lavanda
      "#E78AC3"
      // Rosa magenta
    ];
  }
});

// worker/src/territory.ts
function simplifyCoordinates(coords, maxPoints = 200) {
  if (coords.length <= maxPoints) return coords;
  const step = Math.ceil(coords.length / maxPoints);
  const simplified = [];
  for (let i = 0; i < coords.length; i += step) {
    simplified.push(coords[i]);
  }
  if (simplified[simplified.length - 1] !== coords[coords.length - 1]) {
    simplified.push(coords[coords.length - 1]);
  }
  return simplified;
}
function routeToEnclosedPolygon(coords, maxSimplifyPoints = 150) {
  if (coords.length < 10) return null;
  const simplified = simplifyCoordinates(coords, maxSimplifyPoints);
  const ring = simplified.map((c) => [c[1], c[0]]);
  const first = ring[0];
  const last = ring[ring.length - 1];
  if (first[0] !== last[0] || first[1] !== last[1]) {
    ring.push([first[0], first[1]]);
  }
  try {
    const poly = polygon([ring]);
    const area2 = area(poly);
    if (!area2 || area2 <= 0 || !isFinite(area2)) return null;
    return poly;
  } catch (e) {
    try {
      const line = lineString(ring.slice(0, -1));
      const hull = convex(explode(line));
      if (hull && area(hull) > 0) return hull;
    } catch (_) {
    }
    return null;
  }
}
function activitiesOverlapInTime(activity1Start, activity1End, activity2Start, activity2End, toleranceMs = 15 * 60 * 1e3) {
  const a1Start = new Date(activity1Start).getTime();
  const a2Start = new Date(activity2Start).getTime();
  const startDiff = Math.abs(a1Start - a2Start);
  return startDiff <= toleranceMs;
}
function geometriesOverlapByPercentage(geometry1, geometry2, minOverlapPercent = 0.9) {
  try {
    const feature1 = geometry1.type === "Feature" ? geometry1 : feature(geometry1);
    const feature2 = geometry2.type === "Feature" ? geometry2 : feature(geometry2);
    const intersection3 = intersect3(featureCollection([feature1, feature2]));
    if (!intersection3) return false;
    const area1 = area(feature1);
    const area2 = area(feature2);
    const intersectionArea = area(intersection3);
    const smallerArea = Math.min(area1, area2);
    const overlapPercent = smallerArea > 0 ? intersectionArea / smallerArea : 0;
    return overlapPercent >= minOverlapPercent;
  } catch (err) {
    console.error("[TERRITORY] Error calculating geometry overlap:", err);
    return false;
  }
}
function isCompetitionActive(comp) {
  if (!comp) return false;
  if (comp.status === "active") return true;
  const now = Date.now();
  const start = new Date(comp.startsAt).getTime();
  const end = new Date(comp.endsAt).getTime();
  return now >= start && now <= end;
}
function encodePolyline(coordinates) {
  let encoded = "";
  let prevLat = 0;
  let prevLng = 0;
  for (const [lat, lng] of coordinates) {
    const dlat = Math.round((lat - prevLat) * 1e5);
    const dlng = Math.round((lng - prevLng) * 1e5);
    encoded += encodeValue(dlat);
    encoded += encodeValue(dlng);
    prevLat = lat;
    prevLng = lng;
  }
  return encoded;
}
function encodeValue(val) {
  val = val << 1;
  if (val < 0) val = ~val;
  let encoded = "";
  while (val >= 32) {
    encoded += String.fromCharCode((32 | val & 31) + 63);
    val >>= 5;
  }
  encoded += String.fromCharCode(val + 63);
  return encoded;
}
function decodePolyline(encoded) {
  const points = [];
  let index = 0;
  let lat = 0;
  let lng = 0;
  while (index < encoded.length) {
    let shift = 0;
    let result = 0;
    let byte;
    do {
      byte = encoded.charCodeAt(index++) - 63;
      result |= (byte & 31) << shift;
      shift += 5;
    } while (byte >= 32);
    const deltaLat = result & 1 ? ~(result >> 1) : result >> 1;
    lat += deltaLat;
    shift = 0;
    result = 0;
    do {
      byte = encoded.charCodeAt(index++) - 63;
      result |= (byte & 31) << shift;
      shift += 5;
    } while (byte >= 32);
    const deltaLng = result & 1 ? ~(result >> 1) : result >> 1;
    lng += deltaLng;
    points.push([lat / 1e5, lng / 1e5]);
  }
  return points;
}
function formatAreaNotification(sqMeters) {
  const abs2 = Math.abs(sqMeters);
  const km2 = abs2 / 1e6;
  const sign2 = sqMeters < 0 ? "-" : "";
  if (km2 >= 0.01) return `${sign2}${km2.toFixed(2)} km\xB2`;
  return `${sign2}${Math.round(abs2)} m\xB2`;
}
var init_territory = __esm({
  "worker/src/territory.ts"() {
    "use strict";
    init_esm23();
    __name(simplifyCoordinates, "simplifyCoordinates");
    __name(routeToEnclosedPolygon, "routeToEnclosedPolygon");
    __name(activitiesOverlapInTime, "activitiesOverlapInTime");
    __name(geometriesOverlapByPercentage, "geometriesOverlapByPercentage");
    __name(isCompetitionActive, "isCompetitionActive");
    __name(encodePolyline, "encodePolyline");
    __name(encodeValue, "encodeValue");
    __name(decodePolyline, "decodePolyline");
    __name(formatAreaNotification, "formatAreaNotification");
  }
});

// worker/src/pushHelper.ts
var pushHelper_exports = {};
__export(pushHelper_exports, {
  sendPushNotification: () => sendPushNotification,
  sendPushToUser: () => sendPushToUser
});
function base64UrlToUint8Array(base64Url) {
  const padding = "=".repeat((4 - base64Url.length % 4) % 4);
  const base64 = (base64Url + padding).replace(/-/g, "+").replace(/_/g, "/");
  const raw2 = atob(base64);
  const arr = new Uint8Array(raw2.length);
  for (let i = 0; i < raw2.length; i++) arr[i] = raw2.charCodeAt(i);
  return arr;
}
function uint8ArrayToBase64Url(arr) {
  let binary = "";
  for (let i = 0; i < arr.length; i++) binary += String.fromCharCode(arr[i]);
  return btoa(binary).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
}
function concatBuffers(...buffers) {
  const total = buffers.reduce((sum2, b) => sum2 + b.byteLength, 0);
  const result = new Uint8Array(total);
  let offset = 0;
  for (const buf of buffers) {
    result.set(new Uint8Array(buf), offset);
    offset += buf.byteLength;
  }
  return result.buffer;
}
async function hkdfExtract(salt, ikm) {
  const key = await crypto.subtle.importKey("raw", salt, { name: "HMAC", hash: "SHA-256" }, false, ["sign"]);
  return crypto.subtle.sign("HMAC", key, ikm);
}
async function hkdfExpand(prk, info, length3) {
  const key = await crypto.subtle.importKey("raw", prk, { name: "HMAC", hash: "SHA-256" }, false, ["sign"]);
  let t = new ArrayBuffer(0);
  let okm = new Uint8Array(0);
  for (let i = 0; okm.length < length3; i++) {
    const input = concatBuffers(t, info, new Uint8Array([i + 1]).buffer);
    t = await crypto.subtle.sign("HMAC", key, input);
    const newOkm = new Uint8Array(okm.length + t.byteLength);
    newOkm.set(okm);
    newOkm.set(new Uint8Array(t), okm.length);
    okm = newOkm;
  }
  return okm.slice(0, length3).buffer;
}
async function hkdf(salt, ikm, info, length3) {
  const prk = await hkdfExtract(salt, ikm);
  return hkdfExpand(prk, info, length3);
}
function createInfo(type, clientPublicKey, serverPublicKey) {
  const encoder = new TextEncoder();
  const typeBytes = encoder.encode(type);
  const ceStr = encoder.encode("Content-Encoding: ");
  const p256 = encoder.encode("P-256");
  const result = new Uint8Array(
    ceStr.length + typeBytes.length + 1 + p256.length + 1 + 2 + clientPublicKey.length + 2 + serverPublicKey.length
  );
  let offset = 0;
  result.set(ceStr, offset);
  offset += ceStr.length;
  result.set(typeBytes, offset);
  offset += typeBytes.length;
  result[offset++] = 0;
  result.set(p256, offset);
  offset += p256.length;
  result[offset++] = 0;
  result[offset++] = 0;
  result[offset++] = clientPublicKey.length;
  result.set(clientPublicKey, offset);
  offset += clientPublicKey.length;
  result[offset++] = 0;
  result[offset++] = serverPublicKey.length;
  result.set(serverPublicKey, offset);
  return result.buffer;
}
async function encryptPayload(payload, clientPublicKeyB64, clientAuthB64) {
  const encoder = new TextEncoder();
  const payloadBytes = encoder.encode(payload);
  const clientPublicKeyBytes = base64UrlToUint8Array(clientPublicKeyB64);
  const clientAuth = base64UrlToUint8Array(clientAuthB64);
  const salt = crypto.getRandomValues(new Uint8Array(16));
  const serverKeyPair = await crypto.subtle.generateKey(
    { name: "ECDH", namedCurve: "P-256" },
    true,
    ["deriveBits"]
  );
  const serverPublicKeyRaw = new Uint8Array(
    await crypto.subtle.exportKey("raw", serverKeyPair.publicKey)
  );
  const clientPublicKey = await crypto.subtle.importKey(
    "raw",
    clientPublicKeyBytes,
    { name: "ECDH", namedCurve: "P-256" },
    false,
    []
  );
  const sharedSecret = await crypto.subtle.deriveBits(
    { name: "ECDH", public: clientPublicKey },
    serverKeyPair.privateKey,
    256
  );
  const authInfo = encoder.encode("Content-Encoding: auth\0");
  const ikm = await hkdf(clientAuth.buffer, sharedSecret, authInfo.buffer, 32);
  const cekInfo = createInfo("aesgcm", clientPublicKeyBytes, serverPublicKeyRaw);
  const contentEncryptionKey = await hkdf(salt.buffer, ikm, cekInfo, 16);
  const nonceInfo = createInfo("nonce", clientPublicKeyBytes, serverPublicKeyRaw);
  const nonce = await hkdf(salt.buffer, ikm, nonceInfo, 12);
  const padded = new Uint8Array(2 + payloadBytes.length);
  padded[0] = 0;
  padded[1] = 0;
  padded.set(payloadBytes, 2);
  const cryptoKey = await crypto.subtle.importKey(
    "raw",
    contentEncryptionKey,
    { name: "AES-GCM" },
    false,
    ["encrypt"]
  );
  const encrypted = await crypto.subtle.encrypt(
    { name: "AES-GCM", iv: nonce, tagLength: 128 },
    cryptoKey,
    padded
  );
  return { body: encrypted, serverPublicKey: serverPublicKeyRaw, salt };
}
async function importVapidPrivateKey(base64UrlKey) {
  const rawKey = base64UrlToUint8Array(base64UrlKey);
  const pkcs8Header = new Uint8Array([
    48,
    65,
    2,
    1,
    0,
    48,
    19,
    6,
    7,
    42,
    134,
    72,
    206,
    61,
    2,
    1,
    6,
    8,
    42,
    134,
    72,
    206,
    61,
    3,
    1,
    7,
    4,
    39,
    48,
    37,
    2,
    1,
    1,
    4,
    32
  ]);
  const pkcs8 = concatBuffers(pkcs8Header.buffer, rawKey.buffer);
  return crypto.subtle.importKey(
    "pkcs8",
    pkcs8,
    { name: "ECDSA", namedCurve: "P-256" },
    false,
    ["sign"]
  );
}
function derToRaw(der) {
  if (der.length === 64) return der;
  const raw2 = new Uint8Array(64);
  let offset = 2;
  offset++;
  const rLen = der[offset++];
  const rBytes = der.slice(offset, offset + rLen);
  offset += rLen;
  offset++;
  const sLen = der[offset++];
  const sBytes = der.slice(offset, offset + sLen);
  if (rLen <= 32) {
    raw2.set(rBytes, 32 - rLen);
  } else {
    raw2.set(rBytes.slice(rLen - 32), 0);
  }
  if (sLen <= 32) {
    raw2.set(sBytes, 64 - sLen);
  } else {
    raw2.set(sBytes.slice(sLen - 32), 32);
  }
  return raw2;
}
async function createVapidJwt(audience, subject, privateKeyB64) {
  const header = { typ: "JWT", alg: "ES256" };
  const now = Math.floor(Date.now() / 1e3);
  const payload = { aud: audience, exp: now + 12 * 60 * 60, sub: subject };
  const headerB64 = uint8ArrayToBase64Url(new TextEncoder().encode(JSON.stringify(header)));
  const payloadB64 = uint8ArrayToBase64Url(new TextEncoder().encode(JSON.stringify(payload)));
  const unsignedToken = `${headerB64}.${payloadB64}`;
  const privateKey = await importVapidPrivateKey(privateKeyB64);
  const signatureBuffer = await crypto.subtle.sign(
    { name: "ECDSA", hash: "SHA-256" },
    privateKey,
    new TextEncoder().encode(unsignedToken)
  );
  const signatureRaw = derToRaw(new Uint8Array(signatureBuffer));
  const signatureB64 = uint8ArrayToBase64Url(signatureRaw);
  return `${unsignedToken}.${signatureB64}`;
}
async function sendPushNotification(subscription, payload, vapidPublicKey, vapidPrivateKey, vapidSubject) {
  try {
    console.log(`[PUSH] Sending to endpoint: ${subscription.endpoint.substring(0, 80)}...`);
    const payloadString = JSON.stringify(payload);
    const { body: encryptedBody, serverPublicKey, salt } = await encryptPayload(
      payloadString,
      subscription.keys.p256dh,
      subscription.keys.auth
    );
    const url = new URL(subscription.endpoint);
    const audience = `${url.protocol}//${url.host}`;
    const jwt = await createVapidJwt(audience, vapidSubject, vapidPrivateKey);
    const response = await fetch(subscription.endpoint, {
      method: "POST",
      headers: {
        "Content-Type": "application/octet-stream",
        "Content-Encoding": "aesgcm",
        "Encryption": `salt=${uint8ArrayToBase64Url(salt)}`,
        "Crypto-Key": `dh=${uint8ArrayToBase64Url(serverPublicKey)};p256ecdsa=${vapidPublicKey}`,
        "Authorization": `WebPush ${jwt}`,
        "TTL": "86400",
        "Urgency": "high"
      },
      body: encryptedBody
    });
    if (response.status === 201 || response.status === 200) {
      console.log(`[PUSH] \u2705 Success (${response.status})`);
      return true;
    }
    const responseText = await response.text();
    console.error(`[PUSH] \u274C Failed: ${response.status} - ${responseText}`);
    return false;
  } catch (error) {
    console.error("[PUSH] Error sending notification:", error);
    return false;
  }
}
async function sendPushToUser(subscriptions, payload, vapidPublicKey, vapidPrivateKey, vapidSubject) {
  const results = await Promise.allSettled(
    subscriptions.map(
      (sub) => sendPushNotification(sub, payload, vapidPublicKey, vapidPrivateKey, vapidSubject)
    )
  );
  const successes = results.filter((r) => r.status === "fulfilled" && r.value).length;
  console.log(`[PUSH] Sent ${successes}/${subscriptions.length} notifications`);
}
var init_pushHelper = __esm({
  "worker/src/pushHelper.ts"() {
    "use strict";
    __name(base64UrlToUint8Array, "base64UrlToUint8Array");
    __name(uint8ArrayToBase64Url, "uint8ArrayToBase64Url");
    __name(concatBuffers, "concatBuffers");
    __name(hkdfExtract, "hkdfExtract");
    __name(hkdfExpand, "hkdfExpand");
    __name(hkdf, "hkdf");
    __name(createInfo, "createInfo");
    __name(encryptPayload, "encryptPayload");
    __name(importVapidPrivateKey, "importVapidPrivateKey");
    __name(derToRaw, "derToRaw");
    __name(createVapidJwt, "createVapidJwt");
    __name(sendPushNotification, "sendPushNotification");
    __name(sendPushToUser, "sendPushToUser");
  }
});

// worker/src/notifications.ts
var notifications_exports = {};
__export(notifications_exports, {
  TERRITORY_LOSS_MESSAGES: () => TERRITORY_LOSS_MESSAGES,
  notifyAreaOvertake: () => notifyAreaOvertake,
  notifyComment: () => notifyComment,
  notifyFriendNewActivity: () => notifyFriendNewActivity,
  notifyFriendRequest: () => notifyFriendRequest,
  notifyFriendRequestAccepted: () => notifyFriendRequestAccepted,
  notifyReaction: () => notifyReaction,
  notifyTerritoryLoss: () => notifyTerritoryLoss
});
function formatAreaNotification2(sqMeters) {
  const abs2 = Math.abs(sqMeters);
  const km2 = abs2 / 1e6;
  const sign2 = sqMeters < 0 ? "-" : "";
  if (km2 >= 0.01) return `${sign2}${km2.toFixed(2)} km\xB2`;
  return `${sign2}${Math.round(abs2)} m\xB2`;
}
function formatAreaFromKm2(km2) {
  const abs2 = Math.abs(km2);
  const sign2 = km2 < 0 ? "-" : "";
  if (abs2 >= 0.01) return `${sign2}${abs2.toFixed(2)} km\xB2`;
  return `${sign2}${Math.round(abs2 * 1e6)} m\xB2`;
}
function getRandomTerritoryLossMessage(conquerorHandle, areaText, lastMessageIndex) {
  let index;
  do {
    index = Math.floor(Math.random() * TERRITORY_LOSS_MESSAGES.length);
  } while (index === lastMessageIndex && TERRITORY_LOSS_MESSAGES.length > 1);
  return {
    message: TERRITORY_LOSS_MESSAGES[index].replace("{user}", conquerorHandle).replace("{area}", areaText),
    index
  };
}
async function notifyTerritoryLoss(storage, victimUserId, conquerorUserId, env, stolenAreaM2) {
  try {
    const subscriptions = await storage.getPushSubscriptionsByUserId(victimUserId);
    if (subscriptions.length === 0) {
      return;
    }
    const conqueror = await storage.getUser(conquerorUserId);
    const conquerorHandle = conqueror?.username ? `@${conqueror.username}` : conqueror?.name ? `@${conqueror.name}` : "@alguien";
    const areaText = stolenAreaM2 ? formatAreaNotification2(stolenAreaM2) : "algo de";
    const lastNotification = await storage.getLastTerritoryLossNotification(victimUserId);
    const lastIndex = lastNotification?.messageIndex ?? null;
    const { message, index } = getRandomTerritoryLossMessage(conquerorHandle, areaText, lastIndex);
    const payload = {
      title: "\u{1F6A8} \xA1Te han robado territorio!",
      body: message,
      tag: "territory-loss",
      data: {
        url: "/",
        type: "territory_loss",
        conquerorId: conquerorUserId
      }
    };
    const pushSubs = subscriptions.map((sub) => ({
      endpoint: sub.endpoint,
      keys: {
        p256dh: sub.p256dh,
        auth: sub.auth
      }
    }));
    await sendPushToUser(
      pushSubs,
      payload,
      env.VAPID_PUBLIC_KEY || "",
      env.VAPID_PRIVATE_KEY || "",
      env.VAPID_SUBJECT || "mailto:notifications@runna.io"
    );
    await storage.saveTerritoryLossNotification(victimUserId, index);
    console.log(`\u2705 Sent territory loss notification to user ${victimUserId}`);
  } catch (error) {
    console.error("Error sending territory loss notification:", error);
  }
}
async function notifyFriendRequest(storage, recipientUserId, senderUserId, env) {
  try {
    const subscriptions = await storage.getPushSubscriptionsByUserId(recipientUserId);
    if (subscriptions.length === 0) {
      return;
    }
    const sender = await storage.getUser(senderUserId);
    const senderName = sender?.name || sender?.username || "Alguien";
    const payload = {
      title: "\u{1F44B} Nueva solicitud de amistad",
      body: `${senderName} quiere conectar contigo en Runna`,
      tag: "friend-request",
      data: {
        url: "/friends",
        type: "friend_request",
        senderId: senderUserId
      }
    };
    const pushSubs = subscriptions.map((sub) => ({
      endpoint: sub.endpoint,
      keys: {
        p256dh: sub.p256dh,
        auth: sub.auth
      }
    }));
    await sendPushToUser(
      pushSubs,
      payload,
      env.VAPID_PUBLIC_KEY || "",
      env.VAPID_PRIVATE_KEY || "",
      env.VAPID_SUBJECT || "mailto:notifications@runna.io"
    );
    console.log(`\u2705 Sent friend request notification to user ${recipientUserId}`);
  } catch (error) {
    console.error("Error sending friend request notification:", error);
  }
}
async function notifyFriendRequestAccepted(storage, originalSenderUserId, accepterUserId, env) {
  try {
    const subscriptions = await storage.getPushSubscriptionsByUserId(originalSenderUserId);
    if (subscriptions.length === 0) {
      return;
    }
    const accepter = await storage.getUser(accepterUserId);
    const accepterName = accepter?.name || accepter?.username || "Alguien";
    const payload = {
      title: "\u2705 Solicitud aceptada",
      body: `${accepterName} acept\xF3 tu solicitud de amistad`,
      tag: "friend-accepted",
      data: {
        url: "/friends",
        type: "friend_accepted",
        friendId: accepterUserId
      }
    };
    const pushSubs = subscriptions.map((sub) => ({
      endpoint: sub.endpoint,
      keys: {
        p256dh: sub.p256dh,
        auth: sub.auth
      }
    }));
    await sendPushToUser(
      pushSubs,
      payload,
      env.VAPID_PUBLIC_KEY || "",
      env.VAPID_PRIVATE_KEY || "",
      env.VAPID_SUBJECT || "mailto:notifications@runna.io"
    );
    console.log(`\u2705 Sent friend accepted notification to user ${originalSenderUserId}`);
  } catch (error) {
    console.error("Error sending friend accepted notification:", error);
  }
}
async function notifyFriendNewActivity(storage, activityUserId, distanceKm, newAreaKm2, env) {
  try {
    const user = await storage.getUser(activityUserId);
    const userName = user?.name || user?.username || "Un amigo";
    const friendIds = await storage.getFriendIds(activityUserId);
    if (friendIds.length === 0) return;
    const distanceText = distanceKm.toFixed(1);
    const areaText = formatAreaFromKm2(newAreaKm2);
    const payload = {
      title: `\u{1F3C3} ${userName} ha salido a correr`,
      body: `${distanceText} km recorridos \u2014 ${areaText} de \xE1rea nueva`,
      tag: `friend-activity-${activityUserId}`,
      data: {
        url: "/",
        type: "friend_activity",
        userId: activityUserId
      }
    };
    for (const friendId of friendIds) {
      try {
        const subscriptions = await storage.getPushSubscriptionsByUserId(friendId);
        if (subscriptions.length === 0) continue;
        const pushSubs = subscriptions.map((sub) => ({
          endpoint: sub.endpoint,
          keys: { p256dh: sub.p256dh, auth: sub.auth }
        }));
        await sendPushToUser(
          pushSubs,
          payload,
          env.VAPID_PUBLIC_KEY || "",
          env.VAPID_PRIVATE_KEY || "",
          env.VAPID_SUBJECT || "mailto:notifications@runna.io"
        );
      } catch (friendErr) {
        console.error(`Error sending activity notification to friend ${friendId}:`, friendErr);
      }
    }
    console.log(`\u2705 Sent activity notification to ${friendIds.length} friends of ${activityUserId}`);
  } catch (error) {
    console.error("Error sending friend activity notification:", error);
  }
}
async function notifyAreaOvertake(storage, overtakerUserId, victimUserId, overtakerAreaKm2, env) {
  try {
    const subscriptions = await storage.getPushSubscriptionsByUserId(victimUserId);
    if (subscriptions.length === 0) return;
    const overtaker = await storage.getUser(overtakerUserId);
    const overtakerName = overtaker?.name || overtaker?.username || "Alguien";
    const payload = {
      title: "\u{1F4CA} \xA1Te han superado en \xE1rea!",
      body: `${overtakerName} ahora tiene ${formatAreaFromKm2(overtakerAreaKm2)} y te ha adelantado`,
      tag: `area-overtake-${overtakerUserId}`,
      data: {
        url: "/",
        type: "area_overtake",
        userId: overtakerUserId
      }
    };
    const pushSubs = subscriptions.map((sub) => ({
      endpoint: sub.endpoint,
      keys: { p256dh: sub.p256dh, auth: sub.auth }
    }));
    await sendPushToUser(
      pushSubs,
      payload,
      env.VAPID_PUBLIC_KEY || "",
      env.VAPID_PRIVATE_KEY || "",
      env.VAPID_SUBJECT || "mailto:notifications@runna.io"
    );
    console.log(`\u2705 Sent area overtake notification to ${victimUserId}`);
  } catch (error) {
    console.error("Error sending area overtake notification:", error);
  }
}
async function notifyReaction(storage, reactorUserId, targetOwnerId, reactionType, targetType, env) {
  try {
    if (reactorUserId === targetOwnerId) return;
    const subscriptions = await storage.getPushSubscriptionsByUserId(targetOwnerId);
    if (subscriptions.length === 0) return;
    const reactor = await storage.getUser(reactorUserId);
    const reactorName = reactor?.name || "Alguien";
    const emoji = reactionType === "like" ? "\u{1F44D}" : "\u{1F44E}";
    const actionText = reactionType === "like" ? "le gust\xF3" : "no le gust\xF3";
    const targetText = targetType === "event" ? "tu publicaci\xF3n" : "tu comentario";
    const payload = {
      title: `${emoji} ${reactorName} reaccion\xF3`,
      body: `A ${reactorName} ${actionText} ${targetText}`,
      tag: `reaction-${targetType}-${reactionType}`,
      data: {
        url: "/",
        type: "reaction",
        reactorId: reactorUserId
      }
    };
    const pushSubs = subscriptions.map((sub) => ({
      endpoint: sub.endpoint,
      keys: { p256dh: sub.p256dh, auth: sub.auth }
    }));
    await sendPushToUser(
      pushSubs,
      payload,
      env.VAPID_PUBLIC_KEY || "",
      env.VAPID_PRIVATE_KEY || "",
      env.VAPID_SUBJECT || "mailto:notifications@runna.io"
    );
    console.log(`\u2705 Sent reaction notification to ${targetOwnerId} from ${reactorName}`);
  } catch (error) {
    console.error("Error sending reaction notification:", error);
  }
}
async function notifyComment(storage, commenterUserId, postOwnerId, commentText, env) {
  try {
    if (commenterUserId === postOwnerId) return;
    const subscriptions = await storage.getPushSubscriptionsByUserId(postOwnerId);
    if (subscriptions.length === 0) return;
    const commenter = await storage.getUser(commenterUserId);
    const commenterName = commenter?.name || "Alguien";
    const preview = commentText.length > 60 ? commentText.substring(0, 60) + "..." : commentText;
    const payload = {
      title: `\u{1F4AC} ${commenterName} coment\xF3`,
      body: preview,
      tag: "comment",
      data: {
        url: "/",
        type: "comment",
        commenterId: commenterUserId
      }
    };
    const pushSubs = subscriptions.map((sub) => ({
      endpoint: sub.endpoint,
      keys: { p256dh: sub.p256dh, auth: sub.auth }
    }));
    await sendPushToUser(
      pushSubs,
      payload,
      env.VAPID_PUBLIC_KEY || "",
      env.VAPID_PRIVATE_KEY || "",
      env.VAPID_SUBJECT || "mailto:notifications@runna.io"
    );
    console.log(`\u2705 Sent comment notification to ${postOwnerId} from ${commenterName}`);
  } catch (error) {
    console.error("Error sending comment notification:", error);
  }
}
var TERRITORY_LOSS_MESSAGES;
var init_notifications = __esm({
  "worker/src/notifications.ts"() {
    "use strict";
    init_pushHelper();
    __name(formatAreaNotification2, "formatAreaNotification");
    __name(formatAreaFromKm2, "formatAreaFromKm2");
    TERRITORY_LOSS_MESSAGES = [
      "{user} se llevo {area} de tu reino. Dejalo pasar? Ni de chiste.",
      "{user} te recorto el mapa con {area}. Hora de la tijera inversa.",
      "{user} se quedo con {area} de tu territorio. Tu orgullo esta en modo venganza.",
      "{user} piso tu patio y se llevo {area}. Sal a poner orden.",
      "{user} se llevo {area} de tu mapa. Hoy no se duerme.",
      "{user} te robo {area}. Tus zapatillas piden justicia.",
      "{user} se llevo {area}. Tu mapa llora, tus piernas no.",
      "{user} marco {area} en tu zona. Responde con kilometros.",
      "{user} se llevo {area} de tu imperio. A correr o a llorar.",
      "{user} te recorto {area}. Tu revancha esta en modo sprint.",
      "{user} se llevo {area} de tu reino. Hoy se corre con rabia fina.",
      "{user} te saco {area} del mapa. Que empiece la persecucion.",
      "{user} se llevo {area}. Tu GPS pide represalias.",
      "{user} te bajo {area} de territorio. Haz que se arrepienta.",
      "{user} se quedo con {area}. Tu ego ya calento.",
      "{user} se llevo {area} de tu zona. Devuelvelo con estilo.",
      "{user} te robo {area}. No eres tu, es el. Corrige eso.",
      "{user} se llevo {area}. Tu cardio tiene asuntos pendientes.",
      "{user} te quito {area} de tu patio. Hoy no hay excusas.",
      "{user} se llevo {area}. La remontada empieza ahora.",
      "{user} te saco {area}. Ponte serio, pero con gracia.",
      "{user} se llevo {area}. Hora de marcar territorio otra vez.",
      "{user} te quito {area}. Tu proxima carrera tiene nombre y apellido.",
      "{user} se llevo {area}. Que tu siguiente run sea venganza elegante.",
      "{user} te robo {area}. Hoy el mapa se recupera a pulso.",
      "{user} se llevo {area}. Dejalo? Ni en tus suenos.",
      "{user} te quito {area}. Te vas a quedar mirando? Corre.",
      "{user} se llevo {area}. A ver si tus piernas tambien saben hablar.",
      "{user} te robo {area}. Tu respuesta no cabe en excusas.",
      "{user} se llevo {area}. Dale una clase de territorio 101.",
      "{user} te recorto {area}. Tu turno de devolver el favor.",
      "{user} se llevo {area}. Que no te lo cuenten, recuperalo.",
      "{user} te quito {area}. Te pico? Entonces sal ya.",
      "{user} se llevo {area}. Tiempo de callar y correr.",
      "{user} te robo {area}. Hoy toca revancha con estilo."
    ];
    __name(getRandomTerritoryLossMessage, "getRandomTerritoryLossMessage");
    __name(notifyTerritoryLoss, "notifyTerritoryLoss");
    __name(notifyFriendRequest, "notifyFriendRequest");
    __name(notifyFriendRequestAccepted, "notifyFriendRequestAccepted");
    __name(notifyFriendNewActivity, "notifyFriendNewActivity");
    __name(notifyAreaOvertake, "notifyAreaOvertake");
    __name(notifyReaction, "notifyReaction");
    __name(notifyComment, "notifyComment");
  }
});

// worker/src/inactivityReminders.ts
var inactivityReminders_exports = {};
__export(inactivityReminders_exports, {
  INACTIVITY_REMINDER_MESSAGES: () => INACTIVITY_REMINDER_MESSAGES,
  checkAndSendInactivityReminders: () => checkAndSendInactivityReminders,
  getRandomMessage: () => getRandomMessage
});
function getRandomMessage(lastMessageIndex) {
  let index;
  do {
    index = Math.floor(Math.random() * INACTIVITY_REMINDER_MESSAGES.length);
  } while (index === lastMessageIndex && INACTIVITY_REMINDER_MESSAGES.length > 1);
  return {
    message: INACTIVITY_REMINDER_MESSAGES[index],
    index
  };
}
async function checkAndSendInactivityReminders(storage, env) {
  const stats = { sent: 0, skipped: 0, errors: 0 };
  try {
    const allUsers = await storage.getAllUsersWithStats();
    console.log(`[INACTIVITY] Checking ${allUsers.length} users for inactivity...`);
    const now = /* @__PURE__ */ new Date();
    const twoDaysAgo = new Date(now.getTime() - 2 * 24 * 60 * 60 * 1e3);
    for (const user of allUsers) {
      try {
        const subscriptions = await storage.getPushSubscriptionsByUserId(user.id);
        if (subscriptions.length === 0) {
          stats.skipped++;
          continue;
        }
        const lastActivity = await storage.getLastActivityDate(user.id);
        if (lastActivity && new Date(lastActivity) > twoDaysAgo) {
          stats.skipped++;
          continue;
        }
        const lastReminder = await storage.getLastInactivityReminder(user.id);
        if (lastReminder) {
          const lastSentAt = new Date(lastReminder.sentAt);
          const twoDaysSinceReminder = new Date(now.getTime() - 2 * 24 * 60 * 60 * 1e3);
          if (lastSentAt > twoDaysSinceReminder) {
            stats.skipped++;
            continue;
          }
        }
        const lastIndex = lastReminder?.messageIndex ?? null;
        const { message, index } = getRandomMessage(lastIndex);
        const payload = {
          title: "\u{1F3C3} \xA1Te echamos de menos!",
          body: message,
          tag: "inactivity-reminder",
          data: {
            url: "/",
            type: "inactivity_reminder"
          }
        };
        const pushSubs = subscriptions.map((sub) => ({
          endpoint: sub.endpoint,
          keys: {
            p256dh: sub.p256dh,
            auth: sub.auth
          }
        }));
        await sendPushToUser(
          pushSubs,
          payload,
          env.VAPID_PUBLIC_KEY || "",
          env.VAPID_PRIVATE_KEY || "",
          env.VAPID_SUBJECT || "mailto:notifications@runna.io"
        );
        await storage.saveInactivityReminder(user.id, index);
        console.log(`[INACTIVITY] \u2705 Sent reminder to ${user.username} (message #${index})`);
        stats.sent++;
      } catch (userErr) {
        console.error(`[INACTIVITY] Error processing user ${user.id}:`, userErr);
        stats.errors++;
      }
    }
    console.log(`[INACTIVITY] Done. Sent: ${stats.sent}, Skipped: ${stats.skipped}, Errors: ${stats.errors}`);
  } catch (error) {
    console.error("[INACTIVITY] Fatal error in checkAndSendInactivityReminders:", error);
    throw error;
  }
  return stats;
}
var INACTIVITY_REMINDER_MESSAGES;
var init_inactivityReminders = __esm({
  "worker/src/inactivityReminders.ts"() {
    "use strict";
    init_pushHelper();
    INACTIVITY_REMINDER_MESSAGES = [
      // 1-15: Originales
      "\xA1Oye! Tu sof\xE1 se est\xE1 quejando. Vamos, sal a correr \u{1F3C3}",
      "2 d\xEDas sin subir actividad... \xBFD\xF3nde est\xE1 tu competidor interno? \u{1F4AA}",
      "\xA1Es hora de conquistar nuevos territorios! \xBFListo para salir?",
      "Tus pulmones: 'Me extra\xF1as, \xBFverdad?' Sal a trotar ya \u{1F602}",
      "Recuerdo cuando pod\xEDas subir una actividad cada d\xEDa... \xA1Vuelve! \u{1F45F}",
      "\xBFSab\xEDas que alguien m\xE1s est\xE1 conquistando territorio AHORA? No te quedes atr\xE1s \u{1F5FA}\uFE0F",
      "Tu \xFAltimo rival: 'Pens\xE9 que te hab\xEDas retirado' \u{1F60F} \xA1Demu\xE9strale que no!",
      "Cada paso cuenta. Cada carrera suma. \xA1Vamos a hacer historia hoy! \u{1F3C6}",
      "\xA1Eh! Hace 2 d\xEDas no te vemos. \xBFNecesitas un empuj\xF3n? Aqu\xED est\xE1 \u{1F680}",
      "Mientras t\xFA descansas, otros est\xE1n ganando territorio. \xBFQu\xE9 esperas? \u{1F3C3}\u200D\u2640\uFE0F",
      "\xA1Vamos! Sabemos que tienes energ\xEDa. \xA1Sal y demu\xE9strale a todos de qu\xE9 est\xE1s hecho!",
      "Nuestros mapas te extra\xF1an. T\xFA sabes, esos territorios no van a conquistarse solos \u{1F5FA}\uFE0F",
      "Es mi\xE9rcoles (o el d\xEDa que sea). Es hora de una buena carrera. \xA1T\xFA puedes! \u{1F4A8}",
      "\xA1Alerta! Se detect\xF3 inactividad. Prescrip... digo, \xA1act\xEDvate ya! \u{1F514}",
      "La leyenda dice que apareces cada 2 d\xEDas... \xBFEs hora de escribir tu siguiente cap\xEDtulo? \u2728",
      // 16-25: Nuevos bromistas
      "Tu colch\xF3n est\xE1 pidiendo un descanso de ti. \xA1Vete a correr! \u{1F634}\u{1F3C3}",
      "Advertencia: Est\xE1s a punto de convertirte en legendario (perezoso) \u{1F9A5}",
      "Los p\xEDxeles del mapa extra\xF1an tus coordenadas. \xA1Vuelve! \u{1F4CD}",
      "Spoiler: S\xED, puedes. Ahora corre y demu\xE9stramelo \u{1F3AC}",
      "Tu cardio: 'Eyyy, \xBFme olvidaste?' No seas malo \u{1F494}",
      "Ranking de inactividad: #1 (en mi lista de preocupaciones) \u{1F4CA}",
      "Cuando corriste por \xFAltima vez, la IA a\xFAn no sab\xEDa qu\xE9 era un 'vago'... ahora ya lo sabe \u{1F916}",
      "Noticia de \xFAltimo momento: Usuario desaparece sin dejar rastro \u{1F6A8}",
      "Tu cuerpo te da 2 d\xEDas para descansar, no 2 semanas. \xA1A mover! \u{1F525}",
      "Diccionario: Runna - sustantivo. Sin\xF3nimo de aventura. T\xFA: no has tenido una en 2 d\xEDas \u{1F4D6}",
      // 26-27: Extras
      "Hace 2 d\xEDas que no corres. En ese tiempo surgieron 47 nuevas modas de TikTok que ya se fueron \u{1F4F1}",
      "Dicen que correr alarga la vida... t\xFA ya estar\xEDas en los 200 a\xF1os \u{1F9D3}"
    ];
    __name(getRandomMessage, "getRandomMessage");
    __name(checkAndSendInactivityReminders, "checkAndSendInactivityReminders");
  }
});

// worker/src/routes.ts
var routes_exports = {};
__export(routes_exports, {
  autoCollectTreasuresAlongRoute: () => autoCollectTreasuresAlongRoute,
  generateFeedEvents: () => generateFeedEvents,
  processTerritoryConquest: () => processTerritoryConquest,
  registerRoutes: () => registerRoutes,
  reprocessFriendGroupTerritoriesChronologically: () => reprocessFriendGroupTerritoriesChronologically
});
function getDb(env) {
  return createDb(env.DATABASE_URL, env.TURSO_AUTH_TOKEN);
}
async function getFriendGroupColors(storage, userId) {
  const friendIds = await storage.getFriendIds(userId);
  const colors = /* @__PURE__ */ new Set();
  for (const fid of friendIds) {
    const friend = await storage.getUser(fid);
    if (friend) colors.add(friend.color.toUpperCase());
  }
  return colors;
}
function findAvailableColor(usedColors) {
  for (const color of USER_COLORS) {
    if (!usedColors.has(color.toUpperCase())) {
      return color;
    }
  }
  return null;
}
async function resolveColorConflictOnFriendship(storage, user1Id, user2Id) {
  const user1 = await storage.getUser(user1Id);
  const user2 = await storage.getUser(user2Id);
  if (!user1 || !user2) return { changed: false };
  const user1FriendColors = await getFriendGroupColors(storage, user1Id);
  const user2FriendColors = await getFriendGroupColors(storage, user2Id);
  user1FriendColors.add(user1.color.toUpperCase());
  user2FriendColors.add(user2.color.toUpperCase());
  if (user1FriendColors.has(user2.color.toUpperCase())) {
    const allUsed = /* @__PURE__ */ new Set([...user1FriendColors, ...user2FriendColors]);
    const newColor = findAvailableColor(allUsed);
    if (newColor) {
      await storage.updateUser(user2Id, { color: newColor });
      console.log(`[COLOR CONFLICT] Auto-changed ${user2.name}'s color from ${user2.color} to ${newColor}`);
      return { changed: true, newColor, userId: user2Id };
    }
  }
  if (user2FriendColors.has(user1.color.toUpperCase())) {
    const allUsed = /* @__PURE__ */ new Set([...user1FriendColors, ...user2FriendColors]);
    const newColor = findAvailableColor(allUsed);
    if (newColor) {
      await storage.updateUser(user1Id, { color: newColor });
      console.log(`[COLOR CONFLICT] Auto-changed ${user1.name}'s color from ${user1.color} to ${newColor}`);
      return { changed: true, newColor, userId: user1Id };
    }
  }
  return { changed: false };
}
async function hashPassword(password) {
  const encoder = new TextEncoder();
  const data = encoder.encode(password + "runna_salt_2024");
  const hash = await crypto.subtle.digest("SHA-256", data);
  return Array.from(new Uint8Array(hash)).map((b) => b.toString(16).padStart(2, "0")).join("");
}
async function verifyPassword(password, hash) {
  const computedHash = await hashPassword(password);
  return computedHash === hash;
}
async function generateFeedEvents(storage, userId, routeId, distance2, duration, conquestResult, skipRecordsCheck = false, existingFeedEventId) {
  try {
    const metadata = {};
    if (conquestResult.victims && conquestResult.victims.length > 0) {
      metadata.victims = conquestResult.victims.map((v) => ({
        userId: v.userId,
        userName: v.userName,
        userColor: v.userColor,
        stolenArea: v.stolenArea
      }));
    }
    if (conquestResult.ranTogetherWith && conquestResult.ranTogetherWith.length > 0) {
      const ranNames = await Promise.all(
        conquestResult.ranTogetherWith.map(async (uid) => {
          const u = await storage.getUser(uid);
          return { id: uid, name: u?.name || uid };
        })
      );
      metadata.ranTogetherWith = ranNames;
    }
    if (conquestResult.treasuresCollected && conquestResult.treasuresCollected.length > 0) {
      metadata.treasures = conquestResult.treasuresCollected.map((t) => ({
        treasureId: t.treasureId || t.id,
        treasureName: t.treasureName || t.name,
        powerType: t.powerType,
        rarity: t.rarity
      }));
    }
    if (conquestResult.fortressesDestroyed && conquestResult.fortressesDestroyed > 0) {
      metadata.fortressesDestroyed = conquestResult.fortressesDestroyed;
    }
    const records = [];
    if (!skipRecordsCheck) {
      try {
        if (distance2 && distance2 >= 500) {
          const userRoutes = await storage.getRoutesByUserIdChronological(userId);
          const previousRoutes = userRoutes.filter((r) => r.id !== routeId);
          if (previousRoutes.length > 0 && distance2 > 1e3) {
            const maxPrevDistance = Math.max(...previousRoutes.map((r) => r.distance || 0));
            if (distance2 > maxPrevDistance) {
              records.push({ type: "longest_run", value: distance2 });
            }
          }
          if (distance2 >= 1e3 && duration && duration > 0) {
            const pace = duration / 60 / (distance2 / 1e3);
            const prevPaces = previousRoutes.filter((r) => r.distance && r.distance >= 1e3 && r.duration && r.duration > 0).map((r) => r.duration / 60 / (r.distance / 1e3));
            if (prevPaces.length > 0) {
              const bestPrevPace = Math.min(...prevPaces);
              if (pace < bestPrevPace) {
                records.push({ type: "fastest_pace", value: pace });
              }
            }
          }
          if (conquestResult.newAreaConquered > 0 && previousRoutes.length > 0) {
            try {
              const prevConquests = await storage.getMaxConquestArea(userId);
              if (conquestResult.newAreaConquered > prevConquests) {
                records.push({ type: "biggest_conquest", value: conquestResult.newAreaConquered });
              }
            } catch (_) {
            }
          }
        }
      } catch (prErr) {
        console.warn("[FEED] Personal record check failed (non-critical):", prErr);
      }
    }
    if (records.length > 0) {
      metadata.records = records;
    }
    if (existingFeedEventId) {
      await storage.updateFeedEvent(existingFeedEventId, {
        distance: distance2,
        duration,
        newArea: conquestResult.newAreaConquered,
        metadata: Object.keys(metadata).length > 0 ? JSON.stringify(metadata) : null
      });
      console.log(`[FEED] \u2705 Updated existing feed event ${existingFeedEventId} with conquest data`);
    } else {
      const existing = await storage.getFeedEventByRouteId(routeId);
      if (existing) {
        await storage.updateFeedEvent(existing.id, {
          distance: distance2,
          duration,
          newArea: conquestResult.newAreaConquered,
          metadata: Object.keys(metadata).length > 0 ? JSON.stringify(metadata) : null
        });
        console.log(`[FEED] \u2705 Updated feed event ${existing.id} (found by routeId) with conquest data`);
      } else {
        await storage.createFeedEvent({
          userId,
          eventType: "activity",
          routeId,
          distance: distance2,
          duration,
          newArea: conquestResult.newAreaConquered,
          metadata: Object.keys(metadata).length > 0 ? JSON.stringify(metadata) : null
        });
        console.log(`[FEED] \u2705 Created new feed event for route ${routeId}`);
      }
    }
  } catch (e) {
    console.error("[FEED] Error creating/updating feed event:", e);
  }
}
async function autoCollectTreasuresAlongRoute(storage, userId, competitionId, routeCoordinates) {
  const treasuresCollected = [];
  if (!routeCoordinates || routeCoordinates.length === 0) return treasuresCollected;
  try {
    const activeTreasures = await storage.getActiveTreasures(competitionId);
    if (activeTreasures.length === 0) return treasuresCollected;
    const sampledCoords = routeCoordinates.filter((_, i) => i % 5 === 0 || i === routeCoordinates.length - 1);
    for (const treasure of activeTreasures) {
      for (const coord of sampledCoords) {
        const dist = distance(
          point([coord[1], coord[0]]),
          // [lng, lat]
          point([treasure.lng, treasure.lat]),
          { units: "meters" }
        );
        if (dist <= 100) {
          const result = await storage.collectTreasure(treasure.id, userId);
          if (result && result.collectedBy === userId) {
            await storage.createUserPower({
              id: crypto.randomUUID(),
              userId,
              competitionId,
              powerType: treasure.powerType,
              treasureId: treasure.id,
              status: "available"
            });
            treasuresCollected.push({
              treasureId: treasure.id,
              treasureName: treasure.name,
              powerType: treasure.powerType,
              rarity: treasure.rarity,
              zone: treasure.zone || null
            });
            console.log(`[TREASURE] Auto-collected: ${treasure.name} (${treasure.powerType}) by ${userId}`);
            try {
              await storage.createFeedEvent({
                userId,
                eventType: "treasure_found",
                routeId: null,
                distance: 0,
                duration: 0,
                newArea: 0,
                metadata: JSON.stringify({
                  treasureId: treasure.id,
                  treasureName: treasure.name,
                  powerType: treasure.powerType,
                  rarity: treasure.rarity
                })
              });
            } catch (_) {
            }
            try {
              await storage.incrementCompetitionStats(competitionId, userId, { treasures: 1 });
            } catch (_) {
            }
          }
          break;
        }
      }
    }
  } catch (err) {
    console.error("[TREASURE] Error in standalone auto-collect:", err);
  }
  return treasuresCollected;
}
async function processTerritoryConquest(storage, userId, routeId, bufferedGeometry, env, activityStartedAt, activityCompletedAt, routeCoordinates) {
  const competition = await storage.getActiveCompetition();
  const isCompMode = !!(competition && isCompetitionActive(competition));
  const friendIds = await storage.getFriendIds(userId);
  let allTerritories;
  if (isCompMode) {
    console.log("[COMPETITION] All-vs-all mode active \u2014 loading ALL territories");
    allTerritories = await storage.getAllTerritories();
  } else {
    const relevantUserIds = [userId, ...friendIds];
    allTerritories = await storage.getTerritoriesForUsers(relevantUserIds);
  }
  const userTerritories = allTerritories.filter((t) => t.userId === userId);
  const enemyTerritories = isCompMode ? allTerritories.filter((t) => t.userId !== userId) : allTerritories.filter((t) => t.userId !== userId && friendIds.includes(t.userId));
  let hasDoubleAreaPower = false;
  let hasStealBoostPower = false;
  let hasMagnetPower = false;
  let hasBulldozerPower = false;
  let hasBatteringRamPower = false;
  let hasWallPower = false;
  let doubleAreaPowerId = null;
  let stealBoostPowerId = null;
  let magnetPowerId = null;
  let bulldozerPowerId = null;
  let batteringRamPowerId = null;
  let fortressesDestroyed = 0;
  if (isCompMode && competition) {
    try {
      const activePowers = await storage.getActivePowersForUser(userId, competition.id);
      for (const power of activePowers) {
        if (power.powerType === "double_area") {
          hasDoubleAreaPower = true;
          doubleAreaPowerId = power.id;
        }
        if (power.powerType === "steal_boost") {
          hasStealBoostPower = true;
          stealBoostPowerId = power.id;
        }
        if (power.powerType === "magnet") {
          hasMagnetPower = true;
          magnetPowerId = power.id;
        }
        if (power.powerType === "bulldozer") {
          hasBulldozerPower = true;
          bulldozerPowerId = power.id;
        }
        if (power.powerType === "battering_ram") {
          hasBatteringRamPower = true;
          batteringRamPowerId = power.id;
        }
        if (power.powerType === "wall") {
          hasWallPower = true;
        }
      }
      if (hasDoubleAreaPower || hasStealBoostPower || hasMagnetPower || hasBulldozerPower || hasBatteringRamPower || hasWallPower) {
        console.log(`[COMPETITION] Active powers for ${userId}: double_area=${hasDoubleAreaPower} steal_boost=${hasStealBoostPower} magnet=${hasMagnetPower} bulldozer=${hasBulldozerPower} battering_ram=${hasBatteringRamPower} wall=${hasWallPower}`);
      }
    } catch (err) {
      console.error("[COMPETITION] Error checking active powers:", err);
    }
  }
  let attackGeometry = bufferedGeometry;
  if (isCompMode && (hasDoubleAreaPower || hasMagnetPower)) {
    try {
      let currentGeom = attackGeometry;
      if (hasDoubleAreaPower) {
        const area2 = area(feature(currentGeom));
        const approxRadius = Math.sqrt(area2 / Math.PI);
        const bufferDist = approxRadius * (Math.SQRT2 - 1);
        const expanded = buffer(feature(currentGeom), bufferDist / 1e3, { units: "kilometers" });
        if (expanded) {
          currentGeom = expanded.geometry;
          console.log(`[COMPETITION] DOUBLE AREA applied: ${(area2 / 1e6).toFixed(4)} \u2192 ${(area(expanded) / 1e6).toFixed(4)} km\xB2`);
        }
      }
      if (hasMagnetPower) {
        const area2 = area(feature(currentGeom));
        const approxRadius = Math.sqrt(area2 / Math.PI);
        const bufferDist = approxRadius * (Math.sqrt(1.25) - 1);
        const expanded = buffer(feature(currentGeom), bufferDist / 1e3, { units: "kilometers" });
        if (expanded) {
          currentGeom = expanded.geometry;
          console.log(`[COMPETITION] MAGNET applied: +25% area`);
        }
      }
      attackGeometry = currentGeom;
    } catch (err) {
      console.error("[COMPETITION] Error applying power geometry expansion:", err);
    }
  }
  let totalStolenArea = 0;
  const victimsNotified = [];
  const ranTogetherWith = [];
  const victimMap = /* @__PURE__ */ new Map();
  const defenderCache = /* @__PURE__ */ new Map();
  const enemyUserIds = [...new Set(enemyTerritories.map((t) => t.userId))];
  let ranTogetherUserIds = /* @__PURE__ */ new Set();
  if (activityStartedAt && activityCompletedAt && enemyUserIds.length > 0) {
    try {
      const nearbyRoutes = await storage.getRoutesInTimeWindow(enemyUserIds, activityStartedAt);
      for (const enemyRoute of nearbyRoutes) {
        if (!enemyRoute.startedAt || !enemyRoute.completedAt) continue;
        const timeOverlap = activitiesOverlapInTime(
          activityStartedAt,
          activityCompletedAt,
          enemyRoute.startedAt,
          enemyRoute.completedAt
        );
        if (!timeOverlap) continue;
        const enemyCoords = Array.isArray(enemyRoute.coordinates) ? enemyRoute.coordinates : typeof enemyRoute.coordinates === "string" ? JSON.parse(enemyRoute.coordinates) : [];
        if (enemyCoords.length < 10) continue;
        const enemyPoly = routeToEnclosedPolygon(enemyCoords, 150);
        if (!enemyPoly) continue;
        const areaOverlap = geometriesOverlapByPercentage(
          bufferedGeometry,
          enemyPoly.geometry,
          0.9
        );
        if (areaOverlap) {
          console.log(`[TERRITORY] Ran together with user ${enemyRoute.userId} (route ${enemyRoute.id}) - will skip stealing from this user`);
          ranTogetherUserIds.add(enemyRoute.userId);
          if (!ranTogetherWith.includes(enemyRoute.userId)) {
            ranTogetherWith.push(enemyRoute.userId);
          }
        }
      }
    } catch (err) {
      console.error("[TERRITORY] Error checking ran-together routes:", err);
    }
  }
  console.log(`[TERRITORY] Processing ${enemyTerritories.length} enemy territories...`);
  const enemyDefensivePowers = /* @__PURE__ */ new Map();
  if (isCompMode && competition && enemyUserIds.length > 0) {
    try {
      const allDefensivePowers = await storage.getActiveDefensivePowersForUsers(enemyUserIds, competition.id);
      for (const power of allDefensivePowers) {
        if (!enemyDefensivePowers.has(power.userId)) {
          enemyDefensivePowers.set(power.userId, /* @__PURE__ */ new Set());
        }
        enemyDefensivePowers.get(power.userId).add(power.powerType);
      }
      if (enemyDefensivePowers.size > 0) {
        console.log(`[COMPETITION] Pre-loaded defensive powers for ${enemyDefensivePowers.size} enemy users`);
      }
    } catch (err) {
      console.error("[COMPETITION] Error pre-loading defensive powers:", err);
    }
  }
  const deferredNotifications = [];
  const victimsToUpdate = /* @__PURE__ */ new Set();
  let timeBombPenalty = 0;
  const pendingConquestMetrics = [];
  const pendingSentinelAlerts = [];
  const pendingFortificationDeletes = [];
  const MAX_ENEMY_TERRITORIES = 25;
  const territoriesToProcess = enemyTerritories.slice(0, MAX_ENEMY_TERRITORIES);
  if (enemyTerritories.length > MAX_ENEMY_TERRITORIES) {
    console.warn(`[TERRITORY] Capping enemy territories: processing ${MAX_ENEMY_TERRITORIES} of ${enemyTerritories.length} (rest will be handled on next import)`);
  }
  let attackBbox = null;
  try {
    attackBbox = bbox(attackGeometry);
  } catch (_) {
  }
  for (const enemyTerritory of territoriesToProcess) {
    try {
      if (ranTogetherUserIds.has(enemyTerritory.userId)) {
        console.log(`[TERRITORY] Skipping territory ${enemyTerritory.id} from user ${enemyTerritory.userId} - ran together`);
        continue;
      }
      if (isCompMode && competition) {
        const defenderPowers = enemyDefensivePowers.get(enemyTerritory.userId);
        if (defenderPowers?.has("shield")) {
          continue;
        }
        if (defenderPowers?.has("invisibility")) {
          continue;
        }
        if (!hasBulldozerPower && attackBbox) {
          try {
            const defenderGeomRaw = typeof enemyTerritory.geometry === "string" ? JSON.parse(enemyTerritory.geometry) : enemyTerritory.geometry;
            if (defenderGeomRaw) {
              const fortRecords = await storage.getFortificationsInBbox(
                enemyTerritory.userId,
                attackBbox[0],
                attackBbox[1],
                attackBbox[2],
                attackBbox[3]
              );
              if (fortRecords.length > 0) {
                const attackFeature = feature(attackGeometry);
                let overlappingLayers = 0;
                const layersToRemove = [];
                for (const fort of fortRecords) {
                  try {
                    const fortGeom = JSON.parse(fort.geometry);
                    const fortFeature = feature(fortGeom);
                    const overlap = intersect3(featureCollection([attackFeature, fortFeature]));
                    if (overlap && area(overlap) > 10) {
                      overlappingLayers++;
                      layersToRemove.push(fort.id);
                    }
                  } catch (_) {
                  }
                }
                const fortLevel = overlappingLayers * 0.5;
                if (fortLevel >= 1) {
                  const layersToBreak = hasBatteringRamPower ? Math.min(overlappingLayers, 3) : 1;
                  console.log(`[COMPETITION] FORTIFICATION level ${fortLevel} \u2014 breaking ${layersToBreak} layer(s) from ${enemyTerritory.userId}`);
                  for (let i = 0; i < layersToBreak && i < layersToRemove.length; i++) {
                    pendingFortificationDeletes.push(layersToRemove[i]);
                  }
                  fortressesDestroyed += layersToBreak;
                  continue;
                }
              }
            }
          } catch (err) {
            console.error("[TERRITORY] Error checking fortification:", err);
          }
        } else if (hasBulldozerPower) {
        }
      }
      const result2 = await storage.subtractFromTerritoryDirect(
        enemyTerritory,
        attackGeometry
      );
      if (result2.stolenArea > 0) {
        if (isCompMode && competition) {
          const defenderPowers = enemyDefensivePowers.get(enemyTerritory.userId);
          if (defenderPowers?.has("time_bomb")) {
            console.log(`[COMPETITION] TIME BOMB triggered by ${enemyTerritory.userId}!`);
            timeBombPenalty += result2.stolenArea;
            victimsToUpdate.add(enemyTerritory.userId);
            continue;
          }
        }
        const effectiveStolenArea = hasStealBoostPower ? result2.stolenArea * 1.5 : result2.stolenArea;
        totalStolenArea += effectiveStolenArea;
        victimsToUpdate.add(enemyTerritory.userId);
        console.log(
          `[TERRITORY] Stole ${(result2.stolenArea / 1e6).toFixed(4)} km\xB2 from user ${enemyTerritory.userId}`
        );
        pendingConquestMetrics.push({
          attackerId: userId,
          defenderId: enemyTerritory.userId,
          areaStolen: result2.stolenArea,
          routeId
        });
        if (isCompMode && competition) {
          const defenderPowers = enemyDefensivePowers.get(enemyTerritory.userId);
          if (defenderPowers?.has("sentinel")) {
            if (!defenderCache.has(userId)) {
              const attacker2 = await storage.getUser(userId);
              if (attacker2) defenderCache.set(userId, attacker2);
            }
            const attacker = defenderCache.get(userId);
            pendingSentinelAlerts.push({
              userId: enemyTerritory.userId,
              eventType: "sentinel_alert",
              metadata: JSON.stringify({
                alertMessage: `\u{1F514} \xA1ALERTA CENTINELA! ${attacker?.name || "Alguien"} est\xE1 robando tu territorio (${formatAreaNotification(result2.stolenArea)})`,
                attackerId: userId,
                attackerName: attacker?.name || "Unknown",
                stolenArea: result2.stolenArea,
                timestamp: (/* @__PURE__ */ new Date()).toISOString()
              })
            });
          }
        }
        let defender = defenderCache.get(enemyTerritory.userId);
        if (!defender) {
          defender = await storage.getUser(enemyTerritory.userId);
          defenderCache.set(enemyTerritory.userId, defender);
        }
        if (!victimMap.has(enemyTerritory.userId)) {
          victimMap.set(enemyTerritory.userId, {
            userId: enemyTerritory.userId,
            userName: defender?.name || defender?.username || "Usuario",
            userColor: defender?.color || "#9ca3af",
            stolenArea: 0
          });
        }
        const currentVictim = victimMap.get(enemyTerritory.userId);
        if (currentVictim) {
          currentVictim.stolenArea += result2.stolenArea;
        }
        if (defender?.email) {
          deferredNotifications.push({
            victimId: enemyTerritory.userId,
            attackerName: "",
            attackerUsername: "",
            victimEmail: defender.email,
            stolenArea: result2.stolenArea
          });
        }
      }
    } catch (err) {
      console.error("[TERRITORY] Error processing enemy territory:", err);
    }
  }
  try {
    if (pendingFortificationDeletes.length > 0) {
      await storage.deleteFortificationRecordsBatch(pendingFortificationDeletes);
      console.log(`[TERRITORY] Batch deleted ${pendingFortificationDeletes.length} fortification records`);
    }
  } catch (err) {
    console.error("[TERRITORY] Batch fortification delete failed:", err);
  }
  try {
    if (pendingConquestMetrics.length > 0) {
      await storage.recordConquestMetricsBatch(pendingConquestMetrics);
      console.log(`[TERRITORY] Batch inserted ${pendingConquestMetrics.length} conquest metrics`);
    }
  } catch (err) {
    console.error("[TERRITORY] Batch conquest metrics failed, falling back:", err);
  }
  try {
    if (pendingSentinelAlerts.length > 0) {
      await storage.createFeedEventsBatch(pendingSentinelAlerts);
      console.log(`[TERRITORY] Batch inserted ${pendingSentinelAlerts.length} sentinel alerts`);
    }
  } catch (err) {
    console.error("[TERRITORY] Batch sentinel alerts failed:", err);
  }
  try {
    if (victimsToUpdate.size > 0) {
      await storage.updateVictimAreasBatch(Array.from(victimsToUpdate));
      console.log(`[TERRITORY] Batch updated areas for ${victimsToUpdate.size} victims`);
    }
  } catch (err) {
    console.error("[TERRITORY] Batch victim area update failed:", err);
  }
  const fortificationMultiplier = isCompMode && hasWallPower ? 2 : 1;
  console.log("[TERRITORY] Merging with existing user territories...");
  const result = await storage.addOrMergeTerritory(
    userId,
    routeId,
    attackGeometry,
    // Use power-expanded geometry if applicable
    userTerritories,
    fortificationMultiplier
  );
  const effectiveNewArea = isCompMode ? result.newArea * 1.5 : result.newArea;
  let finalTotalArea = result.totalArea;
  if (isCompMode && result.newArea > 0) {
    const newTerritoryBonus = result.newArea * 0.5;
    finalTotalArea += newTerritoryBonus;
    console.log(`[COMPETITION] New territory bonus: +${(newTerritoryBonus / 1e6).toFixed(4)} km\xB2 (1.5x on ${(result.newArea / 1e6).toFixed(4)} km\xB2 new area)`);
  }
  if (result.fortificationLayers > 0) {
    console.log(`[COMPETITION] Fortification: ${result.fortificationLayers} layer(s) added (multiplier: ${fortificationMultiplier}x)`);
  }
  if (timeBombPenalty > 0) {
    finalTotalArea = Math.max(0, finalTotalArea - timeBombPenalty);
    console.log(`[COMPETITION] Time bomb penalty applied: -${(timeBombPenalty / 1e6).toFixed(4)} km\xB2`);
  }
  await storage.updateUserTotalArea(userId, finalTotalArea);
  const MAX_NOTIFICATIONS = 5;
  if (deferredNotifications.length > 0 && env) {
    const attacker = await storage.getUser(userId);
    if (attacker) {
      let notifyFn = null;
      try {
        const mod = await Promise.resolve().then(() => (init_notifications(), notifications_exports));
        notifyFn = mod.notifyTerritoryLoss;
      } catch (_e) {
      }
      const sortedNotifs = [...deferredNotifications].sort((a, b) => b.stolenArea - a.stolenArea);
      const cappedNotifs = sortedNotifs.slice(0, MAX_NOTIFICATIONS);
      if (deferredNotifications.length > MAX_NOTIFICATIONS) {
        console.log(`[TERRITORY] Capping notifications: ${deferredNotifications.length} victims, sending to top ${MAX_NOTIFICATIONS}`);
      }
      for (const notif of cappedNotifs) {
        try {
          const provider = env.EMAIL_PROVIDER || (env.SENDGRID_API_KEY ? "sendgrid" : "resend");
          const apiKey = provider === "sendgrid" ? env.SENDGRID_API_KEY : env.RESEND_API_KEY;
          const fromEmail = provider === "sendgrid" ? env.SENDGRID_FROM || env.RESEND_FROM : env.RESEND_FROM;
          const emailService = new EmailService(apiKey, fromEmail, provider);
          await emailService.sendTerritoryConqueredEmail(
            notif.victimEmail,
            attacker.name,
            attacker.username,
            notif.stolenArea
          );
          await emailService.recordNotification(
            storage,
            notif.victimId,
            "territory_conquered",
            userId,
            `\xA1${attacker.name} te conquist\xF3 ${formatAreaNotification(notif.stolenArea)}!`,
            `${attacker.name} (@${attacker.username}) ha conquistado ${formatAreaNotification(notif.stolenArea)} de tu territorio.`,
            notif.stolenArea
          );
        } catch (emailErr) {
          console.error("[TERRITORY] Failed to send conquest email:", emailErr);
        }
        if (notifyFn && !victimsNotified.includes(notif.victimId)) {
          try {
            await notifyFn(storage, notif.victimId, userId, env, notif.stolenArea);
            victimsNotified.push(notif.victimId);
          } catch (notifErr) {
            console.error("[TERRITORY] Failed to send notification:", notifErr);
          }
        }
      }
    }
  }
  console.log(`[TERRITORY] Conquest complete:
    - Total area: ${(finalTotalArea / 1e6).toFixed(4)} km\xB2
    - New area: ${(result.newArea / 1e6).toFixed(4)} km\xB2
    - Area stolen: ${(totalStolenArea / 1e6).toFixed(4)} km\xB2
    - Existing area in route: ${(result.existingArea / 1e6).toFixed(4)} km\xB2
    - Ran together with: ${ranTogetherWith.length > 0 ? ranTogetherWith.join(", ") : "none"}
    - Competition mode: ${isCompMode}
  `);
  const treasuresCollected = [];
  const powersUsed = [];
  if (isCompMode && competition) {
    if (routeCoordinates && routeCoordinates.length > 0) {
      try {
        const activeTreasures = await storage.getActiveTreasures(competition.id);
        const sampledCoords = routeCoordinates.filter((_, i) => i % 5 === 0);
        for (const treasure of activeTreasures) {
          let collected = false;
          for (const coord of sampledCoords) {
            const dist = distance(
              point([coord[1], coord[0]]),
              // [lng, lat]
              point([treasure.lng, treasure.lat]),
              { units: "meters" }
            );
            if (dist <= 100) {
              const result2 = await storage.collectTreasure(treasure.id, userId);
              if (result2 && result2.collectedBy === userId) {
                await storage.createUserPower({
                  id: crypto.randomUUID(),
                  userId,
                  competitionId: competition.id,
                  powerType: treasure.powerType,
                  treasureId: treasure.id,
                  status: "available"
                });
                treasuresCollected.push({
                  treasureId: treasure.id,
                  treasureName: treasure.name,
                  powerType: treasure.powerType,
                  rarity: treasure.rarity,
                  zone: treasure.zone || null
                });
                console.log(`[COMPETITION] Treasure auto-collected: ${treasure.name} (${treasure.powerType}) by ${userId}`);
              }
              collected = true;
              break;
            }
          }
        }
      } catch (err) {
        console.error("[COMPETITION] Error auto-collecting treasures:", err);
      }
    }
    try {
      if (doubleAreaPowerId) {
        await storage.usePower(doubleAreaPowerId);
        powersUsed.push("double_area");
        console.log(`[COMPETITION] Power consumed: double_area (${doubleAreaPowerId})`);
      }
      if (stealBoostPowerId) {
        await storage.usePower(stealBoostPowerId);
        powersUsed.push("steal_boost");
        console.log(`[COMPETITION] Power consumed: steal_boost (${stealBoostPowerId})`);
      }
      if (magnetPowerId) {
        await storage.usePower(magnetPowerId);
        powersUsed.push("magnet");
        console.log(`[COMPETITION] Power consumed: magnet (${magnetPowerId})`);
      }
      if (bulldozerPowerId) {
        await storage.usePower(bulldozerPowerId);
        powersUsed.push("bulldozer");
        console.log(`[COMPETITION] Power consumed: bulldozer (${bulldozerPowerId})`);
      }
      if (batteringRamPowerId) {
        await storage.usePower(batteringRamPowerId);
        powersUsed.push("battering_ram");
        console.log(`[COMPETITION] Power consumed: battering_ram (${batteringRamPowerId})`);
      }
    } catch (err) {
      console.error("[COMPETITION] Error consuming powers:", err);
    }
    try {
      let routeDistance = 0;
      let routeDuration = 0;
      if (routeCoordinates && routeCoordinates.length >= 2) {
        try {
          const line = lineString(routeCoordinates.map((c) => [c[1], c[0]]));
          routeDistance = length2(line, { units: "meters" });
        } catch (_) {
        }
      }
      if (activityStartedAt && activityCompletedAt) {
        routeDuration = Math.round((new Date(activityCompletedAt).getTime() - new Date(activityStartedAt).getTime()) / 1e3);
      }
      try {
        await storage.updateCompetitionStats(competition.id, userId, { totalArea: finalTotalArea });
      } catch (_) {
      }
      await storage.incrementCompetitionStats(competition.id, userId, {
        activities: 1,
        distance: routeDistance,
        duration: routeDuration,
        areaStolen: totalStolenArea,
        treasures: treasuresCollected.length,
        ranTogether: ranTogetherWith.length
      });
      try {
        const distinctVictims = await storage.getDistinctVictimsCount(userId);
        await storage.updateCompetitionStats(competition.id, userId, { uniqueVictims: distinctVictims });
      } catch (_) {
      }
    } catch (err) {
      console.error("[COMPETITION] Error updating competition stats:", err);
    }
  }
  return {
    territory: result.territory,
    totalArea: finalTotalArea,
    newAreaConquered: effectiveNewArea,
    areaStolen: totalStolenArea,
    victimsNotified,
    victims: Array.from(victimMap.values()).sort((a, b) => b.stolenArea - a.stolenArea),
    ranTogetherWith,
    treasuresCollected,
    powersUsed,
    fortressesDestroyed
  };
}
function safeUnion2(geomA, geomB) {
  try {
    const toFeature = /* @__PURE__ */ __name((geom) => geom.type === "MultiPolygon" ? multiPolygon(geom.coordinates) : polygon(geom.coordinates), "toFeature");
    const result = union22(featureCollection([toFeature(geomA), toFeature(geomB)]));
    return result ? result.geometry : null;
  } catch (err) {
    console.error("[TERRITORY] safeUnion failed:", err);
    return null;
  }
}
async function reprocessUserTerritories(storage, userId) {
  const userRoutes = await storage.getRoutesByUserIdChronological(userId);
  let currentGeometry = null;
  let processedCount = 0;
  for (const route of userRoutes) {
    try {
      const coords = Array.isArray(route.coordinates) ? route.coordinates : typeof route.coordinates === "string" ? JSON.parse(route.coordinates) : [];
      if (coords.length < 10) continue;
      const enclosedPoly = routeToEnclosedPolygon(coords, 150);
      if (!enclosedPoly) continue;
      if (currentGeometry) {
        const merged = safeUnion2(currentGeometry, enclosedPoly.geometry);
        if (merged) {
          currentGeometry = merged;
        } else {
          console.error(`[REPROCESS] Error merging route ${route.id}, skipping`);
        }
      } else {
        currentGeometry = enclosedPoly.geometry;
      }
      processedCount++;
    } catch (err) {
      console.error(`[REPROCESS] Error processing route ${route.id}:`, err);
    }
  }
  if (currentGeometry) {
    const totalArea = area(currentGeometry);
    if (totalArea > 0 && isFinite(totalArea)) {
      await storage.deleteTerritoriesByUserId(userId);
      await storage.updateTerritoryGeometry(userId, null, currentGeometry, totalArea);
      await storage.updateUserTotalArea(userId, totalArea);
      console.log(`[REPROCESS] Rebuilt territory for user ${userId}: ${processedCount} routes, ${(totalArea / 1e6).toFixed(4)} km\xB2`);
    } else {
      console.error(`[REPROCESS] Invalid rebuilt area (${totalArea}), not replacing territories`);
    }
  } else {
    await storage.deleteTerritoriesByUserId(userId);
    await storage.updateUserTotalArea(userId, 0);
  }
}
async function reprocessFriendGroupTerritoriesChronologically(storage, triggerUserId) {
  console.log(`[FRIEND REPROCESS] Starting for user ${triggerUserId} + friends...`);
  const friendIds = await storage.getFriendIds(triggerUserId);
  const groupUserIds = [triggerUserId, ...friendIds];
  console.log(`[FRIEND REPROCESS] Friend group: ${groupUserIds.length} users`);
  await storage.deleteTerritoriesForUsers(groupUserIds);
  await storage.resetTotalAreaForUsers(groupUserIds);
  await storage.deleteConquestMetricsForUsers(groupUserIds);
  const allRoutes = await storage.getRoutesForUsersChronological(groupUserIds);
  console.log(`[FRIEND REPROCESS] ${allRoutes.length} routes to process`);
  const userGeometries = /* @__PURE__ */ new Map();
  const friendshipMap = await storage.getFriendshipMap(groupUserIds);
  const routesByUser = /* @__PURE__ */ new Map();
  for (const r of allRoutes) {
    if (!routesByUser.has(r.userId)) routesByUser.set(r.userId, []);
    routesByUser.get(r.userId).push(r);
  }
  const routePolygonCache = /* @__PURE__ */ new Map();
  const toFeature = /* @__PURE__ */ __name((geom) => geom.type === "MultiPolygon" ? multiPolygon(geom.coordinates) : polygon(geom.coordinates), "toFeature");
  for (const route of allRoutes) {
    try {
      const coords = Array.isArray(route.coordinates) ? route.coordinates : typeof route.coordinates === "string" ? JSON.parse(route.coordinates) : [];
      if (coords.length < 10) continue;
      const enclosedPoly = routeToEnclosedPolygon(coords, 150);
      if (!enclosedPoly) continue;
      const routeGeometry = enclosedPoly.geometry;
      routePolygonCache.set(route.id, routeGeometry);
      const routeOwnerFriends = friendshipMap.get(route.userId) || /* @__PURE__ */ new Set();
      for (const [otherUserId, otherGeometry] of userGeometries) {
        if (otherUserId === route.userId) continue;
        if (!routeOwnerFriends.has(otherUserId)) continue;
        try {
          const otherFeature = toFeature(otherGeometry);
          const routeFeature = toFeature(routeGeometry);
          const intersection3 = intersect3(featureCollection([otherFeature, routeFeature]));
          if (intersection3) {
            if (route.startedAt && route.completedAt) {
              const otherRoutes = routesByUser.get(otherUserId) || [];
              let ranTogether = false;
              for (const otherRoute of otherRoutes) {
                if (!otherRoute.startedAt || !otherRoute.completedAt) continue;
                const timeOverlap = activitiesOverlapInTime(
                  route.startedAt,
                  route.completedAt,
                  otherRoute.startedAt,
                  otherRoute.completedAt
                );
                if (timeOverlap) {
                  const otherRoutePolygon = routePolygonCache.get(otherRoute.id);
                  if (otherRoutePolygon) {
                    const areaOverlap = geometriesOverlapByPercentage(
                      routeGeometry,
                      otherRoutePolygon,
                      0.9
                    );
                    if (areaOverlap) {
                      console.log(`[FRIEND REPROCESS] Ran together: route ${route.id} (${route.userId}) and route ${otherRoute.id} (${otherUserId}) - skipping steal`);
                      ranTogether = true;
                      break;
                    }
                  }
                }
              }
              if (ranTogether) continue;
            }
            const stolenArea = area(intersection3);
            if (stolenArea > 0) {
              const remaining = difference2(featureCollection([otherFeature, routeFeature]));
              if (remaining) {
                userGeometries.set(otherUserId, remaining.geometry);
              } else {
                userGeometries.delete(otherUserId);
              }
              try {
                await storage.recordConquestMetric(
                  route.userId,
                  otherUserId,
                  stolenArea,
                  route.id
                );
              } catch (_e) {
              }
            }
          }
        } catch (_e) {
        }
      }
      const existing = userGeometries.get(route.userId);
      if (existing) {
        try {
          const existingFeature = toFeature(existing);
          const newFeature = toFeature(routeGeometry);
          const union4 = union22(featureCollection([existingFeature, newFeature]));
          if (union4) {
            userGeometries.set(route.userId, union4.geometry);
          }
        } catch (_e) {
        }
      } else {
        userGeometries.set(route.userId, routeGeometry);
      }
    } catch (err) {
      console.error(`[FRIEND REPROCESS] Error on route ${route.id}:`, err);
    }
  }
  for (const [uid, geometry] of userGeometries) {
    const totalArea = area(geometry);
    await storage.updateTerritoryGeometry(uid, null, geometry, totalArea);
    await storage.updateUserTotalArea(uid, totalArea);
  }
  for (const uid of groupUserIds) {
    if (!userGeometries.has(uid)) {
      await storage.updateUserTotalArea(uid, 0);
    }
  }
  console.log("[FRIEND REPROCESS] Chronological reprocessing complete");
}
function coerceDate(value) {
  const date = value instanceof Date ? value : typeof value === "number" ? new Date(value) : new Date(Number(value) || value);
  return Number.isNaN(date.getTime()) ? null : date;
}
async function getValidStravaToken(stravaAccount, storage, env) {
  const now = /* @__PURE__ */ new Date();
  const expiresAt = new Date(stravaAccount.expiresAt);
  const bufferMs = 5 * 60 * 1e3;
  if (expiresAt.getTime() - bufferMs > now.getTime()) {
    return stravaAccount.accessToken;
  }
  try {
    const params = new URLSearchParams({
      client_id: env.STRAVA_CLIENT_ID,
      client_secret: env.STRAVA_CLIENT_SECRET,
      grant_type: "refresh_token",
      refresh_token: stravaAccount.refreshToken
    });
    const response = await fetch("https://www.strava.com/oauth/token", {
      method: "POST",
      headers: { "Content-Type": "application/x-www-form-urlencoded" },
      body: params.toString()
    });
    if (!response.ok) {
      console.error("Failed to refresh Strava token:", await response.text());
      return null;
    }
    const data = await response.json();
    const { access_token, refresh_token, expires_at } = data;
    await storage.updateStravaAccount(stravaAccount.userId, {
      accessToken: access_token,
      refreshToken: refresh_token,
      expiresAt: new Date(expires_at * 1e3)
    });
    return access_token;
  } catch (error) {
    console.error("Error refreshing Strava token:", error);
    return null;
  }
}
function registerRoutes(app2) {
  app2.post("/api/seed", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const existingUsers = await storage.getAllUsersWithStats();
      if (existingUsers.length > 0) {
        return c.json({ message: "Database already has users", defaultUser: existingUsers[0] });
      }
      const hashedPassword = await hashPassword("demo123");
      const user = await storage.createUser({
        username: "demo_runner",
        name: "Demo Runner",
        password: hashedPassword,
        color: "#3B82F6",
        avatar: null
      });
      return c.json({ message: "Database seeded successfully", defaultUser: user });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/health", (c) => {
    return c.json({
      status: "ok",
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      service: "runna-io-api"
    });
  });
  app2.get("/api/current-user/:userId", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const userId = c.req.param("userId");
      const user = await storage.getUser(userId);
      if (!user) {
        return c.json({ error: "User not found" }, 404);
      }
      const friendsLeaderboard = await storage.getLeaderboardFriends(userId);
      const userWithStats = friendsLeaderboard.find((u) => u.id === userId);
      if (!userWithStats) {
        const allUsers = await storage.getAllUsersWithStats();
        const globalUser = allUsers.find((u) => u.id === userId);
        const { password: _2, ...userWithoutPassword2 } = globalUser || user;
        return c.json(userWithoutPassword2);
      }
      const { password: _, ...userWithoutPassword } = userWithStats;
      return c.json(userWithoutPassword);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/auth/login", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const body = await c.req.json();
      const { username, email, password } = body;
      const identifier = username || email;
      if (!identifier || !password) {
        return c.json({ error: "Usuario o correo y password son requeridos" }, 400);
      }
      const user = identifier.includes("@") ? await storage.getUserByEmail(identifier) : await storage.getUserByUsername(identifier);
      if (!user) {
        return c.json({ error: "Usuario no encontrado" }, 401);
      }
      const isValid3 = await verifyPassword(password, user.password);
      if (!isValid3) {
        return c.json({ error: "Contrase\xF1a incorrecta" }, 401);
      }
      const { password: _, ...userWithoutPassword } = user;
      return c.json(userWithoutPassword);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/users", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const body = await c.req.json();
      const { password, email, ...userData } = body;
      if (!email || !email.includes("@")) {
        return c.json({ error: "Email v\xE1lido requerido" }, 400);
      }
      if (!password || password.length < 4) {
        return c.json({ error: "La contrase\xF1a debe tener al menos 4 caracteres" }, 400);
      }
      const verificationCode = Math.floor(1e5 + Math.random() * 9e5).toString();
      const expiresAt = new Date(Date.now() + 10 * 60 * 1e3).toISOString();
      const hashedPassword = await hashPassword(password);
      const validatedData = insertUserSchema.parse({
        ...userData,
        email,
        password: hashedPassword
      });
      const user = await storage.createUser({
        ...validatedData,
        emailVerified: false,
        verificationCode,
        verificationCodeExpiresAt: expiresAt
      });
      await storage.createEmailPreferences(user.id);
      try {
        const provider1 = c.env.EMAIL_PROVIDER || (c.env.SENDGRID_API_KEY ? "sendgrid" : "resend");
        const apiKey1 = provider1 === "sendgrid" ? c.env.SENDGRID_API_KEY : c.env.RESEND_API_KEY;
        const fromEmail1 = provider1 === "sendgrid" ? c.env.SENDGRID_FROM || c.env.RESEND_FROM : c.env.RESEND_FROM;
        const emailService = new EmailService(apiKey1, fromEmail1, provider1);
        await emailService.sendVerificationCode(user.email, user.name, verificationCode);
      } catch (err) {
        console.error("[EMAIL] Failed to send verification code:", err);
      }
      const { password: _, verificationCode: __, ...userWithoutSensitive } = user;
      return c.json({ ...userWithoutSensitive, requiresVerification: true });
    } catch (error) {
      return c.json({ error: error.message }, 400);
    }
  });
  app2.post("/api/auth/verify-email", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const { userId, code } = await c.req.json();
      if (!userId || !code) {
        return c.json({ error: "userId y code son requeridos" }, 400);
      }
      const user = await storage.getUser(userId);
      if (!user) {
        return c.json({ error: "Usuario no encontrado" }, 404);
      }
      const userAny = user;
      if (userAny.emailVerified) {
        return c.json({ success: true, message: "Email ya verificado" });
      }
      if (!userAny.verificationCode || !userAny.verificationCodeExpiresAt) {
        return c.json({ error: "No hay c\xF3digo de verificaci\xF3n pendiente" }, 400);
      }
      const now = /* @__PURE__ */ new Date();
      const expiresAt = new Date(userAny.verificationCodeExpiresAt);
      if (now > expiresAt) {
        return c.json({ error: "El c\xF3digo ha expirado. Solicita uno nuevo." }, 400);
      }
      if (userAny.verificationCode !== code) {
        return c.json({ error: "C\xF3digo incorrecto" }, 400);
      }
      await storage.updateUser(userId, {
        emailVerified: true,
        verificationCode: null,
        verificationCodeExpiresAt: null
      });
      try {
        const provider1 = c.env.EMAIL_PROVIDER || (c.env.SENDGRID_API_KEY ? "sendgrid" : "resend");
        const apiKey1 = provider1 === "sendgrid" ? c.env.SENDGRID_API_KEY : c.env.RESEND_API_KEY;
        const fromEmail1 = provider1 === "sendgrid" ? c.env.SENDGRID_FROM || c.env.RESEND_FROM : c.env.RESEND_FROM;
        const emailService = new EmailService(apiKey1, fromEmail1, provider1);
        await emailService.sendWelcomeEmail(user.email, user.name);
      } catch (err) {
        console.error("[EMAIL] Failed to send welcome email:", err);
      }
      return c.json({ success: true, message: "Email verificado correctamente" });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/auth/resend-verification", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const { userId } = await c.req.json();
      if (!userId) {
        return c.json({ error: "userId es requerido" }, 400);
      }
      const user = await storage.getUser(userId);
      if (!user) {
        return c.json({ error: "Usuario no encontrado" }, 404);
      }
      const userAny = user;
      if (userAny.emailVerified) {
        return c.json({ success: true, message: "Email ya verificado" });
      }
      const verificationCode = Math.floor(1e5 + Math.random() * 9e5).toString();
      const expiresAt = new Date(Date.now() + 10 * 60 * 1e3).toISOString();
      await storage.updateUser(userId, {
        verificationCode,
        verificationCodeExpiresAt: expiresAt
      });
      try {
        const provider1 = c.env.EMAIL_PROVIDER || (c.env.SENDGRID_API_KEY ? "sendgrid" : "resend");
        const apiKey1 = provider1 === "sendgrid" ? c.env.SENDGRID_API_KEY : c.env.RESEND_API_KEY;
        const fromEmail1 = provider1 === "sendgrid" ? c.env.SENDGRID_FROM || c.env.RESEND_FROM : c.env.RESEND_FROM;
        const emailService = new EmailService(apiKey1, fromEmail1, provider1);
        await emailService.sendVerificationCode(user.email, user.name, verificationCode);
      } catch (err) {
        console.error("[EMAIL] Failed to resend verification code:", err);
        return c.json({ error: "No se pudo enviar el c\xF3digo" }, 500);
      }
      return c.json({ success: true, message: "C\xF3digo reenviado" });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/user/:id", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const id = c.req.param("id");
      const user = await storage.getUser(id);
      if (!user) {
        return c.json({ error: "User not found" }, 404);
      }
      const allUsers = await storage.getAllUsersWithStats();
      const userWithStats = allUsers.find((u) => u.id === id);
      return c.json(userWithStats || user);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.patch("/api/users/:id", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const id = c.req.param("id");
      const body = await c.req.json();
      const { name, color, avatar } = body;
      const updateData = {};
      if (name !== void 0) updateData.name = name;
      if (color !== void 0) {
        const friendColors = await getFriendGroupColors(storage, id);
        if (friendColors.has(color.toUpperCase())) {
          return c.json({ error: "Este color ya lo usa uno de tus amigos. Elige otro." }, 400);
        }
        updateData.color = color;
      }
      if (avatar !== void 0) updateData.avatar = avatar;
      const updatedUser = await storage.updateUser(id, updateData);
      return c.json(updatedUser);
    } catch (error) {
      return c.json({ error: error.message }, 400);
    }
  });
  app2.delete("/api/users/:id", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const id = c.req.param("id");
      const user = await storage.getUser(id);
      if (!user) {
        return c.json({ error: "User not found" }, 404);
      }
      await storage.deleteUser(id);
      return c.json({ success: true });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/user/avatar", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const formData = await c.req.formData();
      const userId = formData.get("userId");
      const file = formData.get("avatar");
      if (!file || !userId) {
        return c.json({ error: "Missing file or userId" }, 400);
      }
      if (file.size > 5 * 1024 * 1024) {
        return c.json({ error: "File size must be less than 5MB" }, 400);
      }
      const arrayBuffer = await file.arrayBuffer();
      const bytes = new Uint8Array(arrayBuffer);
      try {
        console.log("Uploading avatar:", { name: file.name, type: file.type, size: file.size });
      } catch (e) {
      }
      const base64Encode = /* @__PURE__ */ __name((input) => {
        const lookup = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
        const output = [];
        const len = input.length;
        let i = 0;
        for (; i + 2 < len; i += 3) {
          const n = input[i] << 16 | input[i + 1] << 8 | input[i + 2];
          output.push(
            lookup[n >> 18 & 63],
            lookup[n >> 12 & 63],
            lookup[n >> 6 & 63],
            lookup[n & 63]
          );
          if (output.length > 16384) {
            output.push("");
          }
        }
        if (i < len) {
          const a = input[i];
          const b = i + 1 < len ? input[i + 1] : 0;
          const n = a << 16 | b << 8;
          output.push(lookup[n >> 18 & 63]);
          output.push(lookup[n >> 12 & 63]);
          output.push(i + 1 < len ? lookup[n >> 6 & 63] : "=");
          output.push("=");
        }
        return output.join("");
      }, "base64Encode");
      const base64 = base64Encode(bytes);
      const dataUrl = `data:${file.type};base64,${base64}`;
      const updatedUser = await storage.updateUser(userId, { avatar: dataUrl });
      return c.json({ success: true, avatar: dataUrl });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.delete("/api/user/avatar", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const body = await c.req.json();
      const { userId } = body;
      if (!userId) {
        return c.json({ error: "Missing userId" }, 400);
      }
      const updatedUser = await storage.updateUser(userId, { avatar: null });
      return c.json({ success: true });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/leaderboard", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const users2 = await storage.getAllUsersWithStats();
      return c.json(users2);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/routes", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const body = await c.req.json();
      if (body.coordinates && typeof body.coordinates !== "string") {
        body.coordinates = JSON.stringify(body.coordinates);
      }
      const routeData = insertRouteSchema.parse(body);
      const route = await storage.createRoute(routeData);
      let earlyFeedEventId;
      try {
        const earlyFeedEvent = await storage.createFeedEvent({
          userId: routeData.userId,
          eventType: "activity",
          routeId: route.id,
          distance: routeData.distance,
          duration: routeData.duration,
          newArea: 0,
          metadata: null
        });
        earlyFeedEventId = earlyFeedEvent.id;
        console.log(`[FEED] \u2705 Early feed event created: ${earlyFeedEventId} for route ${route.id}`);
      } catch (earlyFeedErr) {
        console.error("[FEED] \u274C Early feed event creation failed:", earlyFeedErr);
      }
      const coords = typeof routeData.coordinates === "string" ? JSON.parse(routeData.coordinates) : routeData.coordinates;
      try {
        await c.env.TERRITORY_QUEUE.send({
          type: "process_route_territory",
          userId: routeData.userId,
          routeId: route.id,
          coordinates: typeof routeData.coordinates === "string" ? routeData.coordinates : JSON.stringify(routeData.coordinates),
          startedAt: routeData.startedAt,
          completedAt: routeData.completedAt,
          distance: routeData.distance,
          duration: routeData.duration,
          earlyFeedEventId
        });
        console.log(`[QUEUE] \u2705 Enqueued territory processing for route ${route.id}`);
      } catch (queueErr) {
        console.error(`[QUEUE] \u274C Failed to enqueue territory processing:`, queueErr);
        c.executionCtx.waitUntil((async () => {
          try {
            const enclosedPoly = routeToEnclosedPolygon(coords, 150);
            if (enclosedPoly) {
              const conquestResult = await processTerritoryConquest(
                storage,
                routeData.userId,
                route.id,
                enclosedPoly.geometry,
                c.env,
                routeData.startedAt,
                routeData.completedAt,
                coords
              );
              if (conquestResult.ranTogetherWith.length > 0) {
                await storage.updateRouteRanTogether(route.id, conquestResult.ranTogetherWith);
              }
              await generateFeedEvents(storage, routeData.userId, route.id, routeData.distance, routeData.duration, conquestResult, false, earlyFeedEventId);
            }
          } catch (fallbackErr) {
            console.error("[QUEUE FALLBACK] Inline territory processing failed:", fallbackErr);
          }
        })());
      }
      const summaryPolyline = encodePolyline(coords);
      return c.json({
        route,
        summaryPolyline,
        // Territory data will be processed asynchronously via queue.
        // Client fetches fresh territory data from GET /api/territories/friends/:userId
        territoryProcessing: "queued"
      });
    } catch (error) {
      return c.json({ error: error.message }, 400);
    }
  });
  app2.patch("/api/routes/:routeId/name", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const routeId = c.req.param("routeId");
      const { userId, name } = await c.req.json();
      if (!userId || !name || !name.trim()) {
        return c.json({ error: "userId and name are required" }, 400);
      }
      const route = await storage.getRouteById(routeId);
      if (!route) {
        return c.json({ error: "Route not found" }, 404);
      }
      if (route.userId !== userId) {
        return c.json({ error: "Unauthorized" }, 403);
      }
      await storage.updateRouteName(routeId, name.trim());
      return c.json({ success: true });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.delete("/api/routes/:userId/:routeId", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const routeId = c.req.param("routeId");
      const userId = c.req.param("userId");
      const route = await storage.getRouteById(routeId);
      if (!route) {
        return c.json({ error: "Route not found" }, 404);
      }
      if (route.userId !== userId) {
        return c.json({ error: "Unauthorized" }, 403);
      }
      await storage.detachTerritoriesFromRoute(routeId);
      await storage.deleteFeedEventsByRouteId(routeId);
      await storage.deleteConquestMetricsByRouteId(routeId);
      await storage.deletePolarActivityByRouteId(routeId);
      await storage.deleteStravaActivityByRouteId(routeId);
      await storage.deleteRouteById(routeId);
      try {
        console.log("[DELETE ROUTE] Reprocessing user territories only (safe mode)");
        await reprocessUserTerritories(storage, userId);
      } catch (e) {
        console.error("[DELETE ROUTE] Error reprocessing territories:", e);
      }
      return c.json({ success: true });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/admin/reprocess-territories", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      try {
        await storage.cleanStaleRouteIds();
      } catch (e) {
        console.error("[REPROCESS] Could not clean stale route_ids:", e);
      }
      const allUsers = await storage.getAllUsersWithStats();
      const processedGroups = /* @__PURE__ */ new Set();
      for (const user of allUsers) {
        if (processedGroups.has(user.id)) continue;
        const friendIds = await storage.getFriendIds(user.id);
        processedGroups.add(user.id);
        for (const fid of friendIds) processedGroups.add(fid);
        await reprocessFriendGroupTerritoriesChronologically(storage, user.id);
      }
      const results = [];
      for (const user of allUsers) {
        const updatedUser = await storage.getUser(user.id);
        const userRoutes = await storage.getRoutesByUserIdChronological(user.id);
        results.push({
          userId: user.id,
          name: user.name,
          totalArea: updatedUser?.totalArea || 0,
          routeCount: userRoutes.length
        });
      }
      return c.json({ success: true, results });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/admin/reprocess-user/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const user = await storage.getUser(userId);
      if (!user) {
        return c.json({ error: `User '${userId}' not found` }, 404);
      }
      await reprocessUserTerritories(storage, userId);
      const updatedUser = await storage.getUser(userId);
      return c.json({
        success: true,
        userId,
        name: updatedUser?.name,
        totalArea: updatedUser?.totalArea || 0
      });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/admin/reprocess-user-batch/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const body = await c.req.json().catch(() => ({}));
      const batchSize = body.batchSize || 20;
      const offset = body.offset || 0;
      const user = await storage.getUser(userId);
      if (!user) {
        return c.json({ error: `User '${userId}' not found` }, 404);
      }
      const totalRoutes = await storage.getRouteCountByUserId(userId);
      if (offset === 0) {
        await storage.deleteTerritoriesByUserId(userId);
      }
      let currentGeometry = null;
      if (offset > 0) {
        const existingTerritories = await storage.getTerritoriesByUserId(userId);
        if (existingTerritories.length > 0) {
          currentGeometry = existingTerritories[0].geometry;
        }
      }
      const batchRoutes = await storage.getRoutesByUserIdChronologicalPaginated(userId, batchSize, offset);
      let processed = 0;
      for (const route of batchRoutes) {
        try {
          const coords = Array.isArray(route.coordinates) ? route.coordinates : typeof route.coordinates === "string" ? JSON.parse(route.coordinates) : [];
          if (coords.length < 10) continue;
          const enclosedPoly = routeToEnclosedPolygon(coords, 150);
          if (!enclosedPoly) continue;
          if (currentGeometry) {
            try {
              const toFeature = /* @__PURE__ */ __name((geom) => geom.type === "MultiPolygon" ? multiPolygon(geom.coordinates) : polygon(geom.coordinates), "toFeature");
              const currentFeature = toFeature(currentGeometry);
              const newFeature = toFeature(enclosedPoly.geometry);
              const union4 = union22(featureCollection([currentFeature, newFeature]));
              if (union4) currentGeometry = union4.geometry;
            } catch (err) {
              console.error("[BATCH REPROCESS] Error merging geometry:", err);
            }
          } else {
            currentGeometry = enclosedPoly.geometry;
          }
          processed++;
        } catch (err) {
          console.error(`[BATCH REPROCESS] Error processing route ${route.id}:`, err);
        }
      }
      if (currentGeometry) {
        const totalArea = area(currentGeometry);
        await storage.updateTerritoryGeometry(userId, null, currentGeometry, totalArea);
        await storage.updateUserTotalArea(userId, totalArea);
      }
      const nextOffset = offset + batchSize;
      const done = nextOffset >= totalRoutes;
      return c.json({
        success: true,
        userId,
        name: user.name,
        totalRoutes,
        processed,
        offset,
        nextOffset: done ? null : nextOffset,
        done,
        currentArea: currentGeometry ? area(currentGeometry) : 0
      });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/admin/reprocess-friend-group/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const user = await storage.getUser(userId);
      if (!user) {
        return c.json({ error: `User '${userId}' not found` }, 404);
      }
      const body = await c.req.json().catch(() => ({}));
      const batchSize = body.batchSize || 30;
      const offset = body.offset || 0;
      const friendIds = await storage.getFriendIds(userId);
      const groupUserIds = [userId, ...friendIds];
      if (offset === 0) {
        await storage.deleteTerritoriesForUsers(groupUserIds);
        await storage.resetTotalAreaForUsers(groupUserIds);
        await storage.deleteConquestMetricsForUsers(groupUserIds);
      }
      const allRoutes = await storage.getRoutesForUsersChronological(groupUserIds);
      const totalRoutes = allRoutes.length;
      const userGeometries = /* @__PURE__ */ new Map();
      if (offset > 0) {
        for (const uid of groupUserIds) {
          const territories2 = await storage.getTerritoriesByUserId(uid);
          if (territories2.length > 0) {
            try {
              const geom = typeof territories2[0].geometry === "string" ? JSON.parse(territories2[0].geometry) : territories2[0].geometry;
              userGeometries.set(uid, geom);
            } catch (_) {
            }
          }
        }
      }
      const friendshipMap = await storage.getFriendshipMap(groupUserIds);
      const routePolygonCache = /* @__PURE__ */ new Map();
      const routesByUser = /* @__PURE__ */ new Map();
      for (const r of allRoutes) {
        if (!routesByUser.has(r.userId)) routesByUser.set(r.userId, []);
        routesByUser.get(r.userId).push(r);
      }
      const toFeature = /* @__PURE__ */ __name((geom) => geom.type === "MultiPolygon" ? multiPolygon(geom.coordinates) : polygon(geom.coordinates), "toFeature");
      const batchEnd = Math.min(offset + batchSize, totalRoutes);
      let processed = 0;
      for (let i = offset; i < batchEnd; i++) {
        const route = allRoutes[i];
        try {
          const coords = Array.isArray(route.coordinates) ? route.coordinates : typeof route.coordinates === "string" ? JSON.parse(route.coordinates) : [];
          if (coords.length < 10) continue;
          const enclosedPoly = routeToEnclosedPolygon(coords, 150);
          if (!enclosedPoly) continue;
          const routeGeometry = enclosedPoly.geometry;
          routePolygonCache.set(route.id, routeGeometry);
          const routeOwnerFriends = friendshipMap.get(route.userId) || /* @__PURE__ */ new Set();
          for (const [otherUserId, otherGeometry] of userGeometries) {
            if (otherUserId === route.userId) continue;
            if (!routeOwnerFriends.has(otherUserId)) continue;
            try {
              const otherFeature = toFeature(otherGeometry);
              const routeFeature = toFeature(routeGeometry);
              const intersection3 = intersect3(featureCollection([otherFeature, routeFeature]));
              if (intersection3) {
                if (route.startedAt && route.completedAt) {
                  const otherRoutes = routesByUser.get(otherUserId) || [];
                  let ranTogether = false;
                  for (const otherRoute of otherRoutes) {
                    if (!otherRoute.startedAt || !otherRoute.completedAt) continue;
                    const timeOverlap = activitiesOverlapInTime(
                      route.startedAt,
                      route.completedAt,
                      otherRoute.startedAt,
                      otherRoute.completedAt
                    );
                    if (timeOverlap) {
                      let otherRoutePolygon = routePolygonCache.get(otherRoute.id);
                      if (!otherRoutePolygon) {
                        try {
                          const otherCoords = Array.isArray(otherRoute.coordinates) ? otherRoute.coordinates : typeof otherRoute.coordinates === "string" ? JSON.parse(otherRoute.coordinates) : [];
                          if (otherCoords.length >= 10) {
                            const otherPoly = routeToEnclosedPolygon(otherCoords, 150);
                            if (otherPoly) {
                              otherRoutePolygon = otherPoly.geometry;
                              routePolygonCache.set(otherRoute.id, otherRoutePolygon);
                            }
                          }
                        } catch (_) {
                        }
                      }
                      if (otherRoutePolygon) {
                        const areaOverlap = geometriesOverlapByPercentage(routeGeometry, otherRoutePolygon, 0.9);
                        if (areaOverlap) {
                          ranTogether = true;
                          break;
                        }
                      }
                    }
                  }
                  if (ranTogether) continue;
                }
                const stolenArea = area(intersection3);
                if (stolenArea > 0) {
                  const remaining = difference2(featureCollection([otherFeature, routeFeature]));
                  if (remaining) {
                    userGeometries.set(otherUserId, remaining.geometry);
                  } else {
                    userGeometries.delete(otherUserId);
                  }
                  try {
                    await storage.recordConquestMetric(route.userId, otherUserId, stolenArea, route.id);
                  } catch (_e) {
                  }
                }
              }
            } catch (_e) {
            }
          }
          const existing = userGeometries.get(route.userId);
          if (existing) {
            try {
              const merged = union22(featureCollection([toFeature(existing), toFeature(routeGeometry)]));
              if (merged) userGeometries.set(route.userId, merged.geometry);
            } catch (_e) {
            }
          } else {
            userGeometries.set(route.userId, routeGeometry);
          }
          processed++;
        } catch (err) {
          console.error(`[FRIEND GROUP REPROCESS] Error on route ${route.id}:`, err);
        }
      }
      for (const [uid, geometry] of userGeometries) {
        const totalArea = area(geometry);
        await storage.deleteTerritoriesByUserId(uid);
        await storage.updateTerritoryGeometry(uid, null, geometry, totalArea);
        await storage.updateUserTotalArea(uid, totalArea);
      }
      for (const uid of groupUserIds) {
        if (!userGeometries.has(uid)) {
          await storage.deleteTerritoriesByUserId(uid);
          await storage.updateUserTotalArea(uid, 0);
        }
      }
      const done = batchEnd >= totalRoutes;
      const nextOffset = done ? null : batchEnd;
      const summary = [];
      for (const uid of groupUserIds) {
        const u = await storage.getUser(uid);
        summary.push({
          userId: uid,
          name: u?.name,
          totalArea: u?.totalArea || 0
        });
      }
      return c.json({
        success: true,
        userId,
        friendGroupSize: groupUserIds.length,
        totalRoutes,
        processed,
        offset,
        batchEnd,
        nextOffset,
        done,
        summary
      });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/admin/cleanup-user/:username", async (c) => {
    try {
      const username = c.req.param("username");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const user = await storage.getUserByUsername(username);
      if (!user) {
        return c.json({ error: `User '${username}' not found` }, 404);
      }
      const userId = user.id;
      console.log(`[ADMIN CLEANUP] Starting full cleanup for user: ${username} (${userId})`);
      await storage.deleteConquestMetricsByUserId(userId);
      console.log(`[ADMIN CLEANUP] Deleted conquest metrics`);
      await storage.deleteTerritoriesByUserId(userId);
      console.log(`[ADMIN CLEANUP] Deleted territories`);
      await storage.deleteAllRoutesByUserId(userId);
      console.log(`[ADMIN CLEANUP] Deleted routes`);
      await storage.deleteAllPolarActivitiesByUserId(userId);
      console.log(`[ADMIN CLEANUP] Deleted polar activities`);
      await storage.deleteAllStravaActivitiesByUserId(userId);
      console.log(`[ADMIN CLEANUP] Deleted strava activities`);
      await storage.updateUser(userId, { totalArea: 0, totalDistance: 0 });
      console.log(`[ADMIN CLEANUP] Reset user stats`);
      try {
        const friendIds = await storage.getFriendIds(userId);
        for (const fid of friendIds) {
          try {
            await reprocessUserTerritories(storage, fid);
          } catch (e) {
            console.error(`[ADMIN CLEANUP] Error reprocessing friend ${fid}:`, e);
          }
        }
        console.log(`[ADMIN CLEANUP] Reprocessed friend territories individually`);
      } catch (e) {
        console.error("[ADMIN CLEANUP] Error reprocessing friends:", e);
      }
      return c.json({
        success: true,
        message: `User '${username}' data cleaned. Polar/Strava activities removed. User can now sync to reimport.`,
        userId
      });
    } catch (error) {
      console.error("[ADMIN CLEANUP] Error:", error);
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/tasks/inactivity-check", async (c) => {
    try {
      const authHeader = c.req.header("Authorization");
      const cronSecret = c.env.UPSTASH_CRON_SECRET;
      if (cronSecret && authHeader !== `Bearer ${cronSecret}`) {
        return c.json({ error: "Unauthorized" }, 401);
      }
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const { checkAndSendInactivityReminders: checkAndSendInactivityReminders2 } = await Promise.resolve().then(() => (init_inactivityReminders(), inactivityReminders_exports));
      const result = await checkAndSendInactivityReminders2(storage, c.env);
      return c.json({
        success: true,
        message: "Inactivity check completed",
        ...result
      });
    } catch (error) {
      console.error("[CRON INACTIVITY] Error:", error);
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/tasks/inactivity-check", async (c) => {
    try {
      const authHeader = c.req.header("Authorization");
      const cronSecret = c.env.UPSTASH_CRON_SECRET;
      if (cronSecret && authHeader !== `Bearer ${cronSecret}`) {
        return c.json({ error: "Unauthorized" }, 401);
      }
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const { checkAndSendInactivityReminders: checkAndSendInactivityReminders2 } = await Promise.resolve().then(() => (init_inactivityReminders(), inactivityReminders_exports));
      const result = await checkAndSendInactivityReminders2(storage, c.env);
      return c.json({
        success: true,
        message: "Inactivity check completed",
        ...result
      });
    } catch (error) {
      console.error("[CRON INACTIVITY] Error:", error);
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/admin/reimport-polar-activities", async (c) => {
    const phase = c.req.query("phase") || "process";
    const db = getDb(c.env);
    const storage = new WorkerStorage(db);
    if (phase === "diagnose") {
      try {
        const userIds = await storage.getUsersWithPolarActivities();
        const diagnostics = [];
        for (const userId of userIds) {
          const user = await storage.getUser(userId);
          const allActivities = await storage.getPolarActivitiesByUserId(userId);
          const unprocessed = allActivities.filter((a) => !a.processed);
          const withRoute = allActivities.filter((a) => a.routeId);
          const withoutRoute = allActivities.filter((a) => !a.routeId);
          const routeCount = await storage.getRouteCountByUserId(userId);
          const territoryCount = await storage.getTerritoryCountByUserId(userId);
          diagnostics.push({
            userId,
            userName: user?.name || userId,
            polarActivities: {
              total: allActivities.length,
              processed: allActivities.length - unprocessed.length,
              unprocessed: unprocessed.length,
              withRoute: withRoute.length,
              withoutRoute: withoutRoute.length
            },
            routes: routeCount,
            territories: territoryCount
          });
        }
        return c.json({ phase: "diagnose", diagnostics });
      } catch (error) {
        return c.json({ error: error.message }, 500);
      }
    }
    if (phase === "cleanup") {
      try {
        const userIds = await storage.getUsersWithPolarActivities();
        const cleanupResults = [];
        for (const userId of userIds) {
          const user = await storage.getUser(userId);
          let deletedRoutes = 0;
          let resetActivities = 0;
          const orphanedRoutes = await storage.getOrphanedRoutes(userId);
          for (const route of orphanedRoutes) {
            await storage.detachTerritoriesFromRoute(route.id);
            await storage.deleteConquestMetricsByRouteId(route.id);
            await storage.deleteRouteByIdDirect(route.id);
            deletedRoutes++;
          }
          const allActivities = await storage.getPolarActivitiesByUserId(userId);
          for (const activity of allActivities) {
            if (activity.routeId) {
              const route = await storage.getRouteById(activity.routeId);
              if (!route) {
                await storage.updatePolarActivity(activity.id, {
                  routeId: null,
                  territoryId: null,
                  processed: false,
                  processedAt: null
                });
                resetActivities++;
              }
            }
          }
          cleanupResults.push({
            userId,
            userName: user?.name || userId,
            orphanedRoutesDeleted: deletedRoutes,
            activitiesReset: resetActivities
          });
        }
        return c.json({ phase: "cleanup", results: cleanupResults });
      } catch (error) {
        return c.json({ error: error.message }, 500);
      }
    }
    if (phase === "process") {
      try {
        const userIds = await storage.getUsersWithPolarActivities();
        for (const userId of userIds) {
          const unprocessed = await storage.getUnprocessedPolarActivities(userId);
          if (unprocessed.length === 0) continue;
          const user = await storage.getUser(userId);
          const activity = unprocessed[0];
          console.log(`[REIMPORT] Processing activity ${activity.id} for user ${user?.name || userId}: "${activity.name}"`);
          if (!activity.summaryPolyline) {
            await storage.updatePolarActivity(activity.id, { processed: true, processedAt: /* @__PURE__ */ new Date() });
            return c.json({
              phase: "process",
              action: "skipped_no_gps",
              activity: { id: activity.id, name: activity.name },
              user: user?.name || userId,
              remaining: unprocessed.length - 1
            });
          }
          const startDate = coerceDate(activity.startDate);
          if (!startDate) {
            await storage.updatePolarActivity(activity.id, { processed: true, processedAt: /* @__PURE__ */ new Date(), skipReason: "bad_date" });
            return c.json({
              phase: "process",
              action: "skipped_bad_date",
              activity: { id: activity.id, name: activity.name },
              user: user?.name || userId,
              remaining: unprocessed.length - 1
            });
          }
          const COMP_MIN_DATE = /* @__PURE__ */ new Date("2026-03-02T00:00:00Z");
          if (startDate < COMP_MIN_DATE) {
            await storage.updatePolarActivity(activity.id, { processed: true, processedAt: /* @__PURE__ */ new Date(), skipReason: "before_competition" });
            console.log(`[REIMPORT] \u274C Activity ${activity.id} before competition start (${startDate.toISOString()})`);
            return c.json({
              phase: "process",
              action: "skipped_before_competition",
              activity: { id: activity.id, name: activity.name, date: startDate.toISOString() },
              user: user?.name || userId,
              remaining: unprocessed.length - 1
            });
          }
          const decoded = decodePolyline(activity.summaryPolyline);
          const coordinates = decoded.map((coord) => [coord[0], coord[1]]);
          if (coordinates.length < 3) {
            await storage.updatePolarActivity(activity.id, { processed: true, processedAt: /* @__PURE__ */ new Date() });
            return c.json({
              phase: "process",
              action: "skipped_few_coords",
              activity: { id: activity.id, name: activity.name },
              user: user?.name || userId,
              remaining: unprocessed.length - 1
            });
          }
          const startedAtStr = startDate.toISOString();
          const completedAtStr = new Date(startDate.getTime() + activity.duration * 1e3).toISOString();
          let route = await storage.findRouteByDateAndDistance(userId, startedAtStr, activity.distance);
          let routeAction = "reused_existing";
          if (!route) {
            route = await storage.createRoute({
              userId: activity.userId,
              name: activity.name,
              coordinates,
              distance: activity.distance,
              duration: activity.duration,
              startedAt: startedAtStr,
              completedAt: completedAtStr
            });
            routeAction = "created_new";
          }
          await storage.updatePolarActivity(activity.id, {
            routeId: route.id,
            processed: true,
            processedAt: /* @__PURE__ */ new Date()
          });
          return c.json({
            phase: "process",
            action: "processed",
            routeAction,
            activity: { id: activity.id, name: activity.name, distance: Math.round(activity.distance) },
            user: user?.name || userId,
            remaining: unprocessed.length - 1,
            note: "Ruta creada. Territorios se calculan con phase=reprocess al final."
          });
        }
        return c.json({
          phase: "process",
          action: "all_done",
          message: "Todas las actividades procesadas. Ahora ejecuta phase=reprocess para calcular territorios."
        });
      } catch (error) {
        console.error("[REIMPORT] Error:", error);
        return c.json({ error: error.message }, 500);
      }
    }
    if (phase === "reprocess") {
      try {
        const allUsers = await storage.getAllUsersWithStats();
        const processedGroupKey = /* @__PURE__ */ new Set();
        for (const user of allUsers) {
          const friendIds = await storage.getFriendIds(user.id);
          const groupIds = [user.id, ...friendIds].sort();
          const groupKey = groupIds.join(",");
          if (processedGroupKey.has(groupKey)) continue;
          processedGroupKey.add(groupKey);
          let needsReprocess = false;
          for (const uid of groupIds) {
            const routeCount = await storage.getRouteCountByUserId(uid);
            const territoryCount = await storage.getTerritoryCountByUserId(uid);
            if (routeCount > 0 && territoryCount === 0) {
              needsReprocess = true;
              break;
            }
          }
          if (!needsReprocess) continue;
          console.log(`[REPROCESS] Processing friend group: ${groupIds.length} users, trigger: ${user.name}`);
          await reprocessFriendGroupTerritoriesChronologically(storage, user.id);
          const groupResults = [];
          for (const uid of groupIds) {
            const u = await storage.getUser(uid);
            groupResults.push({
              userId: uid,
              name: u?.name || uid,
              totalArea: u?.totalArea || 0
            });
          }
          return c.json({
            phase: "reprocess",
            action: "processed_group",
            group: groupResults,
            message: "Un grupo de amigos procesado. Ejecuta de nuevo si hay m\xE1s."
          });
        }
        const results = [];
        for (const user of allUsers) {
          results.push({
            userId: user.id,
            name: user.name,
            totalArea: user.totalArea || 0
          });
        }
        return c.json({ phase: "reprocess", action: "all_done", results });
      } catch (error) {
        return c.json({ error: error.message }, 500);
      }
    }
    if (phase === "reset") {
      try {
        const userIds = await storage.getUsersWithPolarActivities();
        const resetResults = [];
        for (const userId of userIds) {
          const user = await storage.getUser(userId);
          const activities = await storage.getPolarActivitiesByUserId(userId);
          let resetCount = 0;
          for (const activity of activities) {
            await storage.updatePolarActivity(activity.id, {
              processed: false,
              processedAt: null,
              routeId: null,
              territoryId: null
            });
            resetCount++;
          }
          await storage.deleteTerritoriesByUserId(userId);
          await storage.deleteAllRoutesByUserId(userId);
          await storage.deleteConquestMetricsByUserId(userId);
          await storage.updateUser(userId, { totalArea: 0 });
          resetResults.push({
            userId,
            userName: user?.name || userId,
            activitiesReset: resetCount
          });
        }
        return c.json({ phase: "reset", results: resetResults });
      } catch (error) {
        return c.json({ error: error.message }, 500);
      }
    }
    return c.json({ error: `Fase desconocida: ${phase}. Usa: diagnose, cleanup, process, reprocess, reset` }, 400);
  });
  app2.get("/api/routes/:userId", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const userId = c.req.param("userId");
      const routes2 = await storage.getRoutesByUserId(userId);
      return c.json(routes2);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/feed/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const limit = parseInt(c.req.query("limit") || "30");
      const offset = parseInt(c.req.query("offset") || "0");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const events = await storage.getFeedForUser(userId, limit, offset);
      return c.json(events);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/feed/events/:eventId/comments", async (c) => {
    try {
      const eventId = c.req.param("eventId");
      const viewerUserId = c.req.query("userId") || void 0;
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const comments = await storage.getFeedEventComments(eventId, viewerUserId);
      return c.json(comments);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/feed/events/:eventId/comments", async (c) => {
    try {
      const eventId = c.req.param("eventId");
      const { userId, content, parentId } = await c.req.json();
      if (!userId || !content || !content.trim()) {
        return c.json({ error: "userId and content are required" }, 400);
      }
      if (content.length > 500) {
        return c.json({ error: "Comment too long (max 500 chars)" }, 400);
      }
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const comment = await storage.addFeedComment({
        feedEventId: eventId,
        userId,
        content: content.trim(),
        parentId: parentId || null
      });
      c.executionCtx.waitUntil((async () => {
        try {
          const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_pushHelper(), pushHelper_exports));
          const commenter = await storage.getUser(userId);
          const commenterName = commenter?.name || "Alguien";
          const notifiedUserIds = /* @__PURE__ */ new Set([userId]);
          const event = await storage.getFeedEvent(eventId);
          if (event && event.userId !== userId) {
            notifiedUserIds.add(event.userId);
            const subs = await storage.getPushSubscriptionsByUserId(event.userId);
            if (subs.length > 0) {
              await sendPushToUser2(
                subs.map((s) => ({ endpoint: s.endpoint, keys: { p256dh: s.p256dh, auth: s.auth } })),
                {
                  title: `\u{1F4AC} ${commenterName} coment\xF3 tu actividad`,
                  body: content.trim().substring(0, 100),
                  tag: "feed-comment",
                  data: { url: "/activity", type: "feed_comment" }
                },
                c.env.VAPID_PUBLIC_KEY || "",
                c.env.VAPID_PRIVATE_KEY || "",
                c.env.VAPID_SUBJECT || "mailto:notifications@runna.io"
              );
            }
          }
          if (parentId) {
            const parentComment = await storage.getFeedCommentById(parentId);
            if (parentComment && !notifiedUserIds.has(parentComment.userId)) {
              notifiedUserIds.add(parentComment.userId);
              const subs = await storage.getPushSubscriptionsByUserId(parentComment.userId);
              if (subs.length > 0) {
                await sendPushToUser2(
                  subs.map((s) => ({ endpoint: s.endpoint, keys: { p256dh: s.p256dh, auth: s.auth } })),
                  {
                    title: `\u{1F4AC} ${commenterName} respondi\xF3 tu comentario`,
                    body: content.trim().substring(0, 100),
                    tag: "feed-reply",
                    data: { url: "/activity", type: "feed_reply" }
                  },
                  c.env.VAPID_PUBLIC_KEY || "",
                  c.env.VAPID_PRIVATE_KEY || "",
                  c.env.VAPID_SUBJECT || "mailto:notifications@runna.io"
                );
              }
            }
          }
          const mentionRegex = /@([\w\u00C0-\u024F]+(?:\s[\w\u00C0-\u024F]+)?)/g;
          const mentions = [...content.matchAll(mentionRegex)].map((m) => m[1]);
          if (mentions.length > 0) {
            const friendIds = await storage.getFriendIds(userId);
            for (const friendId of friendIds) {
              const friend = await storage.getUser(friendId);
              if (!friend || notifiedUserIds.has(friend.id)) continue;
              const friendNameLower = friend.name?.toLowerCase() || "";
              const friendUsernameLower = friend.username?.toLowerCase() || "";
              const isMentioned = mentions.some((m) => {
                const ml = m.toLowerCase();
                return ml === friendNameLower || ml === friendUsernameLower;
              });
              if (isMentioned) {
                notifiedUserIds.add(friend.id);
                const subs = await storage.getPushSubscriptionsByUserId(friend.id);
                if (subs.length > 0) {
                  await sendPushToUser2(
                    subs.map((s) => ({ endpoint: s.endpoint, keys: { p256dh: s.p256dh, auth: s.auth } })),
                    {
                      title: `\u{1F4E3} ${commenterName} te mencion\xF3 en un comentario`,
                      body: content.trim().substring(0, 100),
                      tag: "feed-mention",
                      data: { url: "/activity", type: "feed_mention" }
                    },
                    c.env.VAPID_PUBLIC_KEY || "",
                    c.env.VAPID_PRIVATE_KEY || "",
                    c.env.VAPID_SUBJECT || "mailto:notifications@runna.io"
                  );
                }
              }
            }
          }
        } catch (e) {
          console.error("[FEED] Error sending comment notification:", e);
        }
      })());
      return c.json(comment);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.delete("/api/feed/comments/:commentId", async (c) => {
    try {
      const commentId = c.req.param("commentId");
      const { userId } = await c.req.json();
      if (!userId) {
        return c.json({ error: "userId is required" }, 400);
      }
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const deleted = await storage.deleteFeedComment(commentId, userId);
      if (!deleted) {
        return c.json({ error: "Comment not found or not yours" }, 404);
      }
      return c.json({ success: true });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/feed/reactions", async (c) => {
    try {
      const { userId, targetType, targetId, reactionType } = await c.req.json();
      if (!userId || !targetType || !targetId || !reactionType) {
        return c.json({ error: "userId, targetType, targetId, and reactionType are required" }, 400);
      }
      if (!["event", "comment"].includes(targetType)) {
        return c.json({ error: 'targetType must be "event" or "comment"' }, 400);
      }
      if (!["like", "dislike"].includes(reactionType)) {
        return c.json({ error: 'reactionType must be "like" or "dislike"' }, 400);
      }
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const result = await storage.toggleReaction(userId, targetType, targetId, reactionType);
      if (result.userReaction === reactionType) {
        try {
          let ownerId = null;
          if (targetType === "event") {
            const event = await storage.getFeedEvent(targetId);
            if (event) ownerId = event.userId;
          } else if (targetType === "comment") {
            const comment = await storage.getFeedCommentById(targetId);
            if (comment) ownerId = comment.userId;
          }
          if (ownerId && ownerId !== userId) {
            const { notifyReaction: notifyReaction2 } = await Promise.resolve().then(() => (init_notifications(), notifications_exports));
            await notifyReaction2(storage, userId, ownerId, reactionType, targetType, c.env);
          }
        } catch (notifErr) {
          console.error("[REACTION] Push notification error:", notifErr);
        }
      }
      return c.json(result);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/admin/init-feed-tables", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      await storage.ensureFeedTables();
      return c.json({ success: true });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/feed/events/:eventId/preview-comments", async (c) => {
    try {
      const eventId = c.req.param("eventId");
      const viewerUserId = c.req.query("userId") || "";
      const limit = parseInt(c.req.query("limit") || "3");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const comments = await storage.getPreviewComments(eventId, viewerUserId, limit);
      return c.json(comments);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/territories", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const territories2 = await storage.getAllTerritories();
      return c.json(territories2);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/conquest-stats/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const stats = await storage.getUserConquestStats(userId);
      return c.json(stats);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/friends", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const body = await c.req.json();
      const { userId, friendId } = body;
      if (!userId || !friendId) {
        return c.json({ error: "userId and friendId required" }, 400);
      }
      if (userId === friendId) {
        return c.json({ error: "Cannot add yourself as friend" }, 400);
      }
      const alreadyFriends = await storage.checkFriendship(userId, friendId);
      if (alreadyFriends) {
        return c.json({ error: "Already friends" }, 400);
      }
      const sender = await storage.getUser(userId);
      const recipient = await storage.getUser(friendId);
      let colorWarning = false;
      if (sender && recipient && sender.color.toUpperCase() === recipient.color.toUpperCase()) {
        colorWarning = true;
      }
      const request = await storage.createFriendRequest({
        senderId: userId,
        recipientId: friendId
      });
      const { notifyFriendRequest: notifyFriendRequest2 } = await Promise.resolve().then(() => (init_notifications(), notifications_exports));
      await notifyFriendRequest2(storage, friendId, userId, c.env);
      try {
        const sender2 = await storage.getUser(userId);
        const recipient2 = await storage.getUser(friendId);
        const provider2 = c.env.EMAIL_PROVIDER || (c.env.SENDGRID_API_KEY ? "sendgrid" : "resend");
        const apiKey2 = provider2 === "sendgrid" ? c.env.SENDGRID_API_KEY : c.env.RESEND_API_KEY;
        const fromEmail2 = provider2 === "sendgrid" ? c.env.SENDGRID_FROM || c.env.RESEND_FROM : c.env.RESEND_FROM;
        const emailService = new EmailService(apiKey2, fromEmail2, provider2);
        if (sender2 && recipient2 && recipient2.email) {
          const prefs = await storage.getEmailPreferences(friendId);
          if (!prefs || prefs.friendRequestNotifications) {
            await emailService.sendFriendRequestEmail(
              recipient2.email,
              sender2.name,
              sender2.username
            );
            await emailService.recordNotification(
              storage,
              friendId,
              "friend_request",
              userId,
              `\xA1${sender2.name} te envi\xF3 una solicitud de amistad!`,
              `${sender2.name} (@${sender2.username}) te ha enviado una solicitud de amistad en Runna.io.`
            );
          }
        }
      } catch (emailErr) {
        console.error("[EMAIL] Failed to send friend request email:", emailErr);
      }
      return c.json({ success: true, requestId: request.id, colorWarning });
    } catch (error) {
      return c.json({ error: error.message }, 400);
    }
  });
  app2.get("/api/friends/:userId", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const userId = c.req.param("userId");
      const friends = await storage.getFriendsByUserId(userId);
      return c.json(friends);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.delete("/api/friends/:friendId", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const friendId = c.req.param("friendId");
      const body = await c.req.json();
      const { userId } = body;
      if (!userId) {
        return c.json({ error: "userId required in body" }, 400);
      }
      await storage.deleteBidirectionalFriendship(userId, friendId);
      return c.json({ success: true });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/friends/requests/:userId", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const userId = c.req.param("userId");
      const requests = await storage.getFriendRequestsByRecipient(userId);
      const enrichedRequests = await Promise.all(
        requests.map(async (req) => {
          const sender = await storage.getUser(req.senderId);
          return {
            ...req,
            sender: sender ? {
              id: sender.id,
              name: sender.name,
              username: sender.username,
              avatar: sender.avatar,
              color: sender.color
            } : null
          };
        })
      );
      return c.json(enrichedRequests);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/friends/requests/sent/:userId", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const userId = c.req.param("userId");
      const requests = await storage.getFriendRequestsBySender(userId);
      const enrichedRequests = await Promise.all(
        requests.map(async (req) => {
          const recipient = await storage.getUser(req.recipientId);
          return {
            ...req,
            recipient: recipient ? {
              id: recipient.id,
              name: recipient.name,
              username: recipient.username,
              avatar: recipient.avatar,
              color: recipient.color
            } : null
          };
        })
      );
      return c.json(enrichedRequests);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/friends/requests/:requestId/accept", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const requestId = c.req.param("requestId");
      const body = await c.req.json();
      const { userId } = body;
      const request = await storage.getFriendRequestById(requestId);
      if (!request) {
        return c.json({ error: "Request not found" }, 404);
      }
      if (request.recipientId !== userId) {
        return c.json({ error: "Unauthorized" }, 403);
      }
      if (request.status !== "pending") {
        return c.json({ error: "Request already processed" }, 400);
      }
      const colorResult = await resolveColorConflictOnFriendship(storage, request.senderId, userId);
      await storage.createBidirectionalFriendship(request.senderId, request.recipientId);
      await storage.updateFriendRequestStatus(requestId, "accepted");
      const { notifyFriendRequestAccepted: notifyFriendRequestAccepted2 } = await Promise.resolve().then(() => (init_notifications(), notifications_exports));
      await notifyFriendRequestAccepted2(storage, request.senderId, userId, c.env);
      try {
        const sender = await storage.getUser(request.senderId);
        const recipient = await storage.getUser(userId);
        const provider3 = c.env.EMAIL_PROVIDER || (c.env.SENDGRID_API_KEY ? "sendgrid" : "resend");
        const apiKey3 = provider3 === "sendgrid" ? c.env.SENDGRID_API_KEY : c.env.RESEND_API_KEY;
        const fromEmail3 = provider3 === "sendgrid" ? c.env.SENDGRID_FROM || c.env.RESEND_FROM : c.env.RESEND_FROM;
        const emailService = new EmailService(apiKey3, fromEmail3, provider3);
        if (sender && sender.email && recipient) {
          const prefs = await storage.getEmailPreferences(request.senderId);
          if (!prefs || prefs.friendAcceptedNotifications) {
            await emailService.sendFriendAcceptedEmail(
              sender.email,
              recipient.name,
              recipient.username
            );
            await emailService.recordNotification(
              storage,
              request.senderId,
              "friend_accepted",
              userId,
              `\xA1${recipient.name} acept\xF3 tu solicitud de amistad!`,
              `${recipient.name} (@${recipient.username}) acept\xF3 tu solicitud de amistad. Ahora son amigos en Runna.io!`
            );
          }
        }
      } catch (emailErr) {
        console.error("[EMAIL] Failed to send friend accepted email:", emailErr);
      }
      try {
        console.log(`[FRIEND ACCEPT] Enqueuing territory reprocess for new friends: ${request.senderId} <-> ${userId}`);
        await c.env.TERRITORY_QUEUE.send({
          type: "reprocess_friend_group",
          userId: request.senderId,
          friendUserId: userId
        });
        console.log(`[FRIEND ACCEPT] Territory reprocess enqueued successfully`);
      } catch (queueErr) {
        console.error("[FRIEND ACCEPT] Failed to enqueue territory reprocess:", queueErr);
      }
      return c.json({ success: true, colorChanged: colorResult.changed, newColor: colorResult.newColor || null });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/friends/requests/:requestId/reject", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const requestId = c.req.param("requestId");
      const body = await c.req.json();
      const { userId } = body;
      const request = await storage.getFriendRequestById(requestId);
      if (!request) {
        return c.json({ error: "Request not found" }, 404);
      }
      if (request.recipientId !== userId) {
        return c.json({ error: "Unauthorized" }, 403);
      }
      await storage.updateFriendRequestStatus(requestId, "rejected");
      return c.json({ success: true });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.delete("/api/friends/requests/:requestId", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const requestId = c.req.param("requestId");
      const request = await storage.getFriendRequestById(requestId);
      if (!request) {
        return c.json({ error: "Request not found" }, 404);
      }
      await storage.deleteFriendRequest(requestId);
      return c.json({ success: true });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/users/search", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const query = c.req.query("query");
      const userId = c.req.query("userId");
      if (!query || !userId) {
        return c.json({ error: "query and userId required" }, 400);
      }
      const users2 = await storage.searchUsers(query, userId);
      return c.json(users2);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/users/:id/stats", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const userId = c.req.param("id");
      const user = await storage.getUser(userId);
      if (!user) {
        return c.json({ error: "User not found" }, 404);
      }
      const totalArea = user.totalArea || 0;
      let activitiesCount = 0;
      let lastActivity = null;
      try {
        const routes2 = await storage.getRoutesByUserId(userId);
        activitiesCount = routes2.length;
        const lastRouteDate = routes2.length > 0 ? new Date(routes2[0].completedAt) : null;
        try {
          const stravaActs = await storage.getStravaActivitiesByUserId(userId);
          const lastStravaDate = stravaActs.length > 0 ? new Date(stravaActs[0].startDate) : null;
          if (lastRouteDate && lastStravaDate) {
            lastActivity = (lastRouteDate > lastStravaDate ? lastRouteDate : lastStravaDate).toISOString();
          } else if (lastRouteDate) {
            lastActivity = lastRouteDate.toISOString();
          } else if (lastStravaDate) {
            lastActivity = lastStravaDate.toISOString();
          }
        } catch (e) {
          console.error("Error fetching strava activities for stats:", e);
          if (lastRouteDate) lastActivity = lastRouteDate.toISOString();
        }
      } catch (e) {
        console.error("Error fetching routes for user stats:", e);
      }
      let totalStolen = 0;
      let totalLost = 0;
      let stolenFromViewer = 0;
      let stolenByViewer = 0;
      try {
        const conquestStats = await storage.getUserConquestStats(userId);
        totalStolen = conquestStats.totalStolen;
        totalLost = conquestStats.totalLost;
      } catch (e) {
        console.error("Error fetching conquest stats:", e);
      }
      const viewerId = c.req.query("viewerId");
      if (viewerId && viewerId !== userId) {
        try {
          const between3 = await storage.getConquestMetricsBetweenUsers(userId, viewerId);
          stolenFromViewer = between3.totalFromFirstToSecond;
          stolenByViewer = between3.totalFromSecondToFirst;
        } catch (e) {
          console.error("Error fetching conquest metrics between users:", e);
        }
      }
      let nicknameData = { nickname: null, nicknameExpiresAt: null };
      try {
        const nn = await storage.getActiveNickname(userId);
        if (nn) nicknameData = { nickname: nn.nickname, nicknameExpiresAt: nn.expiresAt };
      } catch (_) {
      }
      const stats = {
        user: {
          id: user.id,
          name: user.name,
          username: user.username,
          avatar: user.avatar,
          color: user.color,
          nickname: nicknameData.nickname,
          nicknameExpiresAt: nicknameData.nicknameExpiresAt
        },
        totalArea,
        // in m²
        activitiesCount,
        lastActivity,
        // ISO string or null
        totalStolen,
        // total area this user has stolen globally (m²)
        totalLost,
        // total area stolen from this user globally (m²)
        stolenFromViewer,
        // area this user stole from the viewer (m²)
        stolenByViewer
        // area the viewer stole from this user (m²)
      };
      return c.json(stats);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/leaderboard/friends/:userId", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const userId = c.req.param("userId");
      const friends = await storage.getLeaderboardFriends(userId);
      const nicknameMap = await storage.getActiveNicknamesForUsers(friends.map((f) => f.id));
      const enriched = friends.map((f) => {
        const nn = nicknameMap.get(f.id);
        return { ...f, nickname: nn?.nickname || null, nicknameExpiresAt: nn?.expiresAt || null };
      });
      return c.json(enriched);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/territories/friends/:userId", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const userId = c.req.param("userId");
      const territories2 = await storage.getTerritoriesWithUsersByFriends(userId);
      return c.json(territories2);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/friends/invite", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const body = await c.req.json();
      const { userId } = body;
      if (!userId) {
        return c.json({ error: "userId required" }, 400);
      }
      const invite = await storage.createFriendInvite(userId);
      const inviteUrl = `${c.env.FRONTEND_URL || "https://runna-io.pages.dev"}/friends/accept/${invite.token}`;
      return c.json({ token: invite.token, url: inviteUrl });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/friends/accept/:token", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const token = c.req.param("token");
      const body = await c.req.json();
      const { userId } = body;
      if (!userId) {
        return c.json({ error: "userId required" }, 400);
      }
      const invite = await storage.getFriendInviteByToken(token);
      if (!invite) {
        return c.json({ error: "Invite not found or expired" }, 404);
      }
      if (/* @__PURE__ */ new Date() > invite.expiresAt) {
        await storage.deleteFriendInvite(invite.id);
        return c.json({ error: "Invite expired" }, 400);
      }
      if (invite.userId === userId) {
        return c.json({ error: "Cannot accept your own invite" }, 400);
      }
      const colorResult = await resolveColorConflictOnFriendship(storage, invite.userId, userId);
      await storage.createBidirectionalFriendship(invite.userId, userId);
      await storage.deleteFriendInvite(invite.id);
      const { notifyFriendRequestAccepted: notifyFriendRequestAccepted2 } = await Promise.resolve().then(() => (init_notifications(), notifications_exports));
      await notifyFriendRequestAccepted2(storage, invite.userId, userId, c.env);
      try {
        console.log(`[FRIEND INVITE ACCEPT] Enqueuing territory reprocess for: ${invite.userId} <-> ${userId}`);
        await c.env.TERRITORY_QUEUE.send({
          type: "reprocess_friend_group",
          userId: invite.userId,
          friendUserId: userId
        });
      } catch (queueErr) {
        console.error("[FRIEND INVITE ACCEPT] Failed to enqueue territory reprocess:", queueErr);
      }
      return c.json({ success: true, friendId: invite.userId, colorChanged: colorResult.changed, newColor: colorResult.newColor || null });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/push/subscribe", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const body = await c.req.json();
      const { userId, endpoint, keys } = body;
      if (!userId || !endpoint || !keys?.p256dh || !keys?.auth) {
        return c.json({ error: "Missing required fields" }, 400);
      }
      await storage.createPushSubscription({
        userId,
        endpoint,
        p256dh: keys.p256dh,
        auth: keys.auth
      });
      return c.json({ success: true });
    } catch (error) {
      console.error("Error subscribing to push:", error);
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/push/unsubscribe", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const body = await c.req.json();
      const { userId } = body;
      if (!userId) {
        return c.json({ error: "userId required" }, 400);
      }
      await storage.deletePushSubscriptionsByUserId(userId);
      return c.json({ success: true });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/push/test", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const body = await c.req.json();
      const { userId } = body;
      if (!userId) return c.json({ error: "userId required" }, 400);
      const subscriptions = await storage.getPushSubscriptionsByUserId(userId);
      if (subscriptions.length === 0) {
        return c.json({ error: "No push subscriptions found for this user", subscriptionCount: 0 }, 404);
      }
      const { sendPushNotification: sendPushNotification2 } = await Promise.resolve().then(() => (init_pushHelper(), pushHelper_exports));
      const pushSubs = subscriptions.map((sub) => ({
        endpoint: sub.endpoint,
        keys: { p256dh: sub.p256dh, auth: sub.auth }
      }));
      const payload = {
        title: "\u{1F3C3} Runna.io",
        body: "\xA1Las notificaciones funcionan correctamente!",
        tag: "test-notification",
        data: { url: "/", type: "test" }
      };
      const results = [];
      for (const sub of pushSubs) {
        try {
          const ok = await sendPushNotification2(
            sub,
            payload,
            c.env.VAPID_PUBLIC_KEY || "",
            c.env.VAPID_PRIVATE_KEY || "",
            c.env.VAPID_SUBJECT || "mailto:notifications@runna.io"
          );
          results.push({ endpoint: sub.endpoint.slice(0, 60), success: ok });
        } catch (err) {
          results.push({ endpoint: sub.endpoint.slice(0, 60), success: false, error: err.message });
        }
      }
      return c.json({ success: true, subscriptionCount: subscriptions.length, results });
    } catch (error) {
      console.error("Error sending test push:", error);
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/strava/status/:userId", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const userId = c.req.param("userId");
      const stravaAccount = await storage.getStravaAccountByUserId(userId);
      if (stravaAccount) {
        const failedActivities = await storage.getFailedStravaActivities(userId);
        return c.json({
          connected: true,
          athleteData: stravaAccount.athleteData,
          lastSyncAt: stravaAccount.lastSyncAt,
          failedActivities: failedActivities.length
        });
      } else {
        return c.json({ connected: false });
      }
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/strava/failed/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const failedActivities = await storage.getFailedStravaActivities(userId);
      return c.json(failedActivities);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/strava/retry/:activityId", async (c) => {
    try {
      const activityId = c.req.param("activityId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const activity = await storage.resetStravaActivityForRetry(activityId);
      console.log(`[STRAVA] Activity ${activityId} reset for retry`);
      return c.json({ message: "Activity reset for retry", activity });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/strava/retry-all/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const failedActivities = await storage.getFailedStravaActivities(userId);
      console.log(`[STRAVA] Retrying ${failedActivities.length} failed activities for user ${userId}`);
      let retryCount = 0;
      for (const activity of failedActivities) {
        try {
          await storage.resetStravaActivityForRetry(activity.id);
          retryCount++;
        } catch (err) {
          console.error(`[STRAVA] Failed to reset activity ${activity.id}:`, err);
        }
      }
      return c.json({
        message: `${retryCount}/${failedActivities.length} activities reset for retry`,
        retryCount,
        totalFailed: failedActivities.length
      });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/strava/connect", async (c) => {
    try {
      const userId = c.req.query("userId");
      const STRAVA_CLIENT_ID = c.env.STRAVA_CLIENT_ID;
      const STRAVA_REDIRECT_URI = `${c.env.WORKER_URL || "https://runna-io-api.runna-io-api.workers.dev"}/api/strava/callback`;
      if (!userId || !STRAVA_CLIENT_ID) {
        return c.json({ error: "userId required and Strava not configured" }, 400);
      }
      const state = btoa(JSON.stringify({ userId, ts: Date.now() }));
      const scopes = "read,activity:read_all";
      const authUrl = `https://www.strava.com/oauth/authorize?client_id=${STRAVA_CLIENT_ID}&redirect_uri=${encodeURIComponent(STRAVA_REDIRECT_URI)}&response_type=code&scope=${scopes}&state=${state}`;
      return c.json({ authUrl });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/strava/callback", async (c) => {
    const FRONTEND_URL = c.env.FRONTEND_URL || "https://runna-io.pages.dev";
    try {
      const code = c.req.query("code");
      const state = c.req.query("state");
      const authError = c.req.query("error");
      const STRAVA_CLIENT_ID = c.env.STRAVA_CLIENT_ID;
      const STRAVA_CLIENT_SECRET = c.env.STRAVA_CLIENT_SECRET;
      if (authError) {
        return c.redirect(`${FRONTEND_URL}/?strava_error=denied`);
      }
      if (!code || !state || !STRAVA_CLIENT_ID || !STRAVA_CLIENT_SECRET) {
        return c.redirect(`${FRONTEND_URL}/?strava_error=invalid`);
      }
      let userId;
      try {
        const decoded = JSON.parse(atob(state));
        userId = decoded.userId;
      } catch {
        return c.redirect(`${FRONTEND_URL}/?strava_error=invalid_state`);
      }
      const params = new URLSearchParams({
        client_id: STRAVA_CLIENT_ID,
        client_secret: STRAVA_CLIENT_SECRET,
        code,
        grant_type: "authorization_code"
      });
      const tokenResponse = await fetch("https://www.strava.com/api/v3/oauth/token", {
        method: "POST",
        headers: { "Content-Type": "application/x-www-form-urlencoded" },
        body: params.toString()
      });
      if (!tokenResponse.ok) {
        console.error("Strava token exchange failed:", await tokenResponse.text());
        return c.redirect(`${FRONTEND_URL}/?strava_error=token_exchange`);
      }
      const tokenData = await tokenResponse.json();
      const { access_token, refresh_token, expires_at, athlete } = tokenData;
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const existingAccount = await storage.getStravaAccountByAthleteId(athlete.id);
      if (existingAccount && existingAccount.userId !== userId) {
        return c.redirect(`${FRONTEND_URL}/?strava_error=already_linked`);
      }
      const expiresAtDate = new Date(expires_at * 1e3);
      const stravaAccountData = {
        userId,
        stravaAthleteId: athlete.id,
        accessToken: access_token,
        refreshToken: refresh_token,
        expiresAt: expiresAtDate,
        scope: "read,activity:read_all",
        athleteData: athlete,
        lastSyncAt: null
      };
      if (existingAccount) {
        await storage.updateStravaAccount(userId, stravaAccountData);
      } else {
        await storage.createStravaAccount(stravaAccountData);
      }
      return c.redirect(`${FRONTEND_URL}/?strava_connected=true`);
    } catch (error) {
      console.error("Strava callback error:", error);
      return c.redirect(`${FRONTEND_URL}/?strava_error=server`);
    }
  });
  app2.post("/api/strava/disconnect", async (c) => {
    try {
      const body = await c.req.json();
      const { userId } = body;
      if (!userId) {
        return c.json({ error: "userId required" }, 400);
      }
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const stravaAccount = await storage.getStravaAccountByUserId(userId);
      if (!stravaAccount) {
        return c.json({ error: "Strava account not connected" }, 404);
      }
      try {
        await fetch("https://www.strava.com/oauth/deauthorize", {
          method: "POST",
          headers: { "Content-Type": "application/x-www-form-urlencoded" },
          body: `access_token=${stravaAccount.accessToken}`
        });
      } catch (e) {
        console.error("Failed to revoke Strava token:", e);
      }
      await storage.deleteStravaAccount(userId);
      return c.json({ success: true });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/strava/webhook", (c) => {
    const mode = c.req.query("hub.mode");
    const token = c.req.query("hub.verify_token");
    const challenge = c.req.query("hub.challenge");
    if (mode === "subscribe" && token === c.env.STRAVA_WEBHOOK_VERIFY_TOKEN) {
      return c.json({ "hub.challenge": challenge });
    } else {
      return c.json({ error: "Forbidden" }, 403);
    }
  });
  app2.post("/api/strava/webhook", async (c) => {
    try {
      const body = await c.req.json();
      const { object_type, aspect_type, object_id, owner_id } = body;
      if (object_type === "activity" && aspect_type === "create") {
        const db = getDb(c.env);
        const storage = new WorkerStorage(db);
        const stravaAccount = await storage.getStravaAccountByAthleteId(owner_id);
        if (stravaAccount) {
          const existingActivity = await storage.getStravaActivityByStravaId(object_id);
          if (!existingActivity) {
            const validToken = await getValidStravaToken(stravaAccount, storage, c.env);
            if (!validToken) {
              console.error("Failed to get valid Strava token for athlete:", owner_id);
              return c.json({ received: true }, 200);
            }
            const activityResponse = await fetch(
              `https://www.strava.com/api/v3/activities/${object_id}?include_all_efforts=false`,
              {
                headers: { "Authorization": `Bearer ${validToken}` }
              }
            );
            if (activityResponse.ok) {
              const activity = await activityResponse.json();
              if (["Run", "Walk", "Hike", "Trail Run"].includes(activity.type)) {
                await storage.createStravaActivity({
                  stravaActivityId: activity.id,
                  userId: stravaAccount.userId,
                  routeId: null,
                  territoryId: null,
                  name: activity.name,
                  activityType: activity.type,
                  distance: activity.distance,
                  duration: activity.moving_time,
                  startDate: new Date(activity.start_date),
                  summaryPolyline: activity.map?.summary_polyline || null,
                  processed: false,
                  processedAt: null
                });
              }
            } else {
              console.error("Failed to fetch Strava activity:", object_id, await activityResponse.text());
            }
          }
        }
      }
      return c.json({ received: true }, 200);
    } catch (error) {
      console.error("Strava webhook error:", error);
      return c.json({ received: true }, 200);
    }
  });
  app2.post("/api/strava/process/:userId", async (c) => {
    const stravaProcessStart = Date.now();
    const STRAVA_MAX_PROCESSING_TIME = 25e3;
    const STRAVA_BATCH_SIZE = 1;
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const unprocessed = await storage.getUnprocessedStravaActivities(userId);
      const results = [];
      console.log(`[STRAVA PROCESS] Starting - ${unprocessed.length} unprocessed for user ${userId}`);
      if (unprocessed.length === 0) {
        return c.json({ processed: 0, results: [], remaining: 0, message: "No activities to process" });
      }
      const toBatch = unprocessed.slice(0, STRAVA_BATCH_SIZE);
      for (const activity of toBatch) {
        if (Date.now() - stravaProcessStart > STRAVA_MAX_PROCESSING_TIME) {
          console.warn(`[STRAVA PROCESS] Timeout approaching - stopping at ${results.length} processed`);
          break;
        }
        if (!activity.summaryPolyline) {
          await storage.updateStravaActivity(activity.id, { processed: true, processedAt: /* @__PURE__ */ new Date() });
          continue;
        }
        try {
          const startDate = coerceDate(activity.startDate);
          if (!startDate) {
            console.log(`[STRAVA PROCESS] Skipping activity ${activity.id} - invalid start date`);
            await storage.updateStravaActivity(activity.id, { processed: true, processedAt: /* @__PURE__ */ new Date() });
            continue;
          }
          const decoded = decodePolyline(activity.summaryPolyline);
          const coordinates = decoded.map((coord) => [coord[0], coord[1]]);
          if (coordinates.length >= 10) {
            const startedAtStr = startDate.toISOString();
            const completedAtStr = new Date(startDate.getTime() + activity.duration * 1e3).toISOString();
            let route = await storage.findRouteByDateAndDistance(userId, startedAtStr, activity.distance);
            if (route) {
              console.log(`[STRAVA PROCESS] Found existing route by date+distance: ${route.id} - reusing`);
            } else {
              route = await storage.createRoute({
                userId: activity.userId,
                name: activity.name,
                coordinates,
                distance: activity.distance,
                duration: activity.duration,
                startedAt: startedAtStr,
                completedAt: completedAtStr
              });
            }
            await storage.updateStravaActivity(activity.id, { routeId: route.id });
            const enclosedPoly = routeToEnclosedPolygon(coordinates, 150);
            if (enclosedPoly) {
              const conquestResult = await processTerritoryConquest(
                storage,
                userId,
                route.id,
                enclosedPoly.geometry,
                void 0,
                // skip notifications to reduce subrequests
                startedAtStr,
                completedAtStr,
                coordinates
              );
              if (conquestResult.ranTogetherWith.length > 0) {
                await storage.updateRouteRanTogether(route.id, conquestResult.ranTogetherWith);
              }
              await storage.updateStravaActivity(activity.id, {
                processed: true,
                processedAt: /* @__PURE__ */ new Date(),
                routeId: route.id,
                territoryId: conquestResult.territory.id
              });
              results.push({
                activityId: activity.stravaActivityId,
                routeId: route.id,
                territoryId: conquestResult.territory.id,
                metrics: {
                  totalArea: conquestResult.totalArea,
                  newAreaConquered: conquestResult.newAreaConquered,
                  areaStolen: conquestResult.areaStolen,
                  ranTogetherWith: conquestResult.ranTogetherWith,
                  victimsNotified: conquestResult.victimsNotified,
                  victims: conquestResult.victims
                }
              });
              try {
                await generateFeedEvents(storage, userId, route.id, activity.distance, activity.duration, conquestResult);
              } catch (feedErr) {
                console.warn(`[STRAVA PROCESS] Feed events failed (non-critical):`, feedErr);
              }
            } else {
              try {
                await generateFeedEvents(storage, userId, route.id, activity.distance, activity.duration, { newAreaConquered: 0, victims: [], ranTogetherWith: [] });
              } catch (feedErr) {
                console.warn("[STRAVA] Feed event failed for route without polygon:", feedErr);
              }
              await storage.updateStravaActivity(activity.id, {
                routeId: route.id,
                processed: true,
                processedAt: /* @__PURE__ */ new Date()
              });
            }
          } else {
            try {
              const startedAtShort = startDate.toISOString();
              const completedAtShort = new Date(startDate.getTime() + activity.duration * 1e3).toISOString();
              let shortRoute = await storage.findRouteByDateAndDistance(userId, startedAtShort, activity.distance);
              if (!shortRoute) {
                shortRoute = await storage.createRoute({
                  userId: activity.userId,
                  name: activity.name,
                  coordinates,
                  distance: activity.distance,
                  duration: activity.duration,
                  startedAt: startedAtShort,
                  completedAt: completedAtShort
                });
              }
              await generateFeedEvents(storage, userId, shortRoute.id, activity.distance, activity.duration, { newAreaConquered: 0, victims: [], ranTogetherWith: [] });
              await storage.updateStravaActivity(activity.id, { routeId: shortRoute.id, processed: true, processedAt: /* @__PURE__ */ new Date() });
            } catch (feedErr) {
              console.warn("[STRAVA] Short route feed event failed:", feedErr);
              await storage.updateStravaActivity(activity.id, { processed: true, processedAt: /* @__PURE__ */ new Date() });
            }
          }
        } catch (err) {
          console.error("Error processing Strava activity:", err);
          await storage.updateStravaActivity(activity.id, { processed: true, processedAt: /* @__PURE__ */ new Date() });
        }
      }
      const remaining = unprocessed.length - toBatch.length;
      const processingTime = Date.now() - stravaProcessStart;
      console.log(`[STRAVA PROCESS] Completed in ${processingTime}ms - ${results.length} processed, ${remaining} remaining`);
      return c.json({
        processed: results.length,
        results,
        remaining,
        processingTime,
        message: remaining > 0 ? `${results.length} procesadas, ${remaining} pendientes.` : `${results.length} procesadas correctamente`
      });
    } catch (error) {
      console.error("[STRAVA PROCESS] Critical error:", error);
      return c.json({ error: error.message, processed: 0, remaining: -1 }, 500);
    }
  });
  app2.get("/api/strava/activities/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const activities = await storage.getStravaActivitiesByUserId(userId);
      return c.json(activities);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/strava/sync/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const stravaAccount = await storage.getStravaAccountByUserId(userId);
      if (!stravaAccount) {
        return c.json({ error: "Strava account not connected" }, 404);
      }
      const validToken = await getValidStravaToken(stravaAccount, storage, c.env);
      if (!validToken) {
        return c.json({ error: "Failed to get valid Strava token" }, 401);
      }
      const after = Math.floor((Date.now() - 30 * 24 * 60 * 60 * 1e3) / 1e3);
      const STRAVA_MAX_PAGES = 3;
      const DETAIL_FETCH_BUDGET = 10;
      const STRAVA_SYNC_TIMEOUT = 2e4;
      const stravaSyncStart = Date.now();
      let detailFetchesUsed = 0;
      let imported = 0;
      let totalFetched = 0;
      let hasMore = false;
      for (let page = 1; page <= STRAVA_MAX_PAGES; page++) {
        if (Date.now() - stravaSyncStart > STRAVA_SYNC_TIMEOUT) {
          console.warn(`[STRAVA SYNC] Time limit reached \u2014 stopping at page ${page}`);
          hasMore = true;
          break;
        }
        const activitiesResponse = await fetch(
          `https://www.strava.com/api/v3/athlete/activities?after=${after}&per_page=50&page=${page}`,
          {
            headers: { "Authorization": `Bearer ${validToken}` }
          }
        );
        if (!activitiesResponse.ok) {
          console.error("Failed to fetch Strava activities:", await activitiesResponse.text());
          if (page === 1) {
            return c.json({ error: "Failed to fetch activities from Strava" }, 500);
          }
          break;
        }
        const stravaActivitiesList = await activitiesResponse.json();
        totalFetched += stravaActivitiesList.length;
        if (stravaActivitiesList.length === 0) break;
        for (const activity of stravaActivitiesList) {
          if (!["Run", "Walk", "Hike", "Trail Run"].includes(activity.type)) {
            continue;
          }
          const existing = await storage.getStravaActivityByStravaId(activity.id);
          if (existing) {
            continue;
          }
          if (detailFetchesUsed >= DETAIL_FETCH_BUDGET) {
            console.log(`[STRAVA SYNC] Detail fetch budget exhausted (${DETAIL_FETCH_BUDGET}), skipping remaining`);
            hasMore = true;
            break;
          }
          let summaryPolyline = null;
          try {
            detailFetchesUsed++;
            const detailedResponse = await fetch(
              `https://www.strava.com/api/v3/activities/${activity.id}?include_all_efforts=false`,
              {
                headers: { "Authorization": `Bearer ${validToken}` }
              }
            );
            if (detailedResponse.ok) {
              const detailedActivity = await detailedResponse.json();
              summaryPolyline = detailedActivity.map?.summary_polyline || null;
            } else {
              console.warn(`Failed to fetch detailed activity ${activity.id}:`, await detailedResponse.text());
            }
          } catch (err) {
            console.warn(`Error fetching detailed Strava activity ${activity.id}:`, err);
          }
          if (!summaryPolyline) {
            console.log(`Skipping activity ${activity.id} - no GPS data (polyline)`);
            continue;
          }
          await storage.createStravaActivity({
            stravaActivityId: activity.id,
            userId,
            routeId: null,
            territoryId: null,
            name: activity.name,
            activityType: activity.type,
            distance: activity.distance,
            duration: activity.moving_time,
            startDate: new Date(activity.start_date),
            summaryPolyline,
            processed: false,
            processedAt: null
          });
          imported++;
        }
        if (stravaActivitiesList.length < 50) break;
        hasMore = true;
      }
      await storage.updateStravaAccount(userId, { lastSyncAt: /* @__PURE__ */ new Date() });
      return c.json({ imported, total: totalFetched, hasMore });
    } catch (error) {
      console.error("Strava sync error:", error);
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/polar/status/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const polarAccount = await storage.getPolarAccountByUserId(userId);
      if (polarAccount) {
        const stats = await storage.getPolarActivityStats(userId);
        const failedActivities = await storage.getFailedPolarActivities(userId);
        return c.json({
          connected: true,
          polarUserId: polarAccount.polarUserId,
          lastSyncAt: polarAccount.lastSyncAt,
          totalActivities: stats.total,
          pendingActivities: stats.unprocessed,
          failedActivities: failedActivities.length,
          lastActivityStart: stats.lastStartDate
        });
      } else {
        return c.json({ connected: false });
      }
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/polar/failed/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const failedActivities = await storage.getFailedPolarActivities(userId);
      return c.json(failedActivities);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/polar/retry-all/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const failedActivities = await storage.getFailedPolarActivities(userId);
      console.log(`[POLAR] Retrying ${failedActivities.length} failed activities for user ${userId}`);
      let retryCount = 0;
      for (const activity of failedActivities) {
        try {
          await storage.resetPolarActivityForRetry(activity.id);
          retryCount++;
        } catch (err) {
          console.error(`[POLAR] Failed to reset activity ${activity.id}:`, err);
        }
      }
      return c.json({
        message: `${retryCount}/${failedActivities.length} activities reset for retry`,
        retryCount,
        totalFailed: failedActivities.length
      });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/polar/retry/:activityId", async (c) => {
    try {
      const activityId = c.req.param("activityId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const activity = await storage.resetPolarActivityForRetry(activityId);
      console.log(`[POLAR] Activity ${activityId} reset for retry (was skipReason: ${activity.skipReason})`);
      return c.json({ success: true, activity });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/polar/failed-all", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const failedActivities = await storage.getAllFailedPolarActivities();
      const byUser = {};
      for (const act of failedActivities) {
        byUser[act.userId] = (byUser[act.userId] || 0) + 1;
      }
      return c.json({
        totalFailed: failedActivities.length,
        byUser,
        activities: failedActivities.map((a) => ({
          id: a.id,
          userId: a.userId,
          name: a.name,
          processed: a.processed,
          routeId: a.routeId,
          territoryId: a.territoryId
        }))
      });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/polar/retry-all-global", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const failedActivities = await storage.getAllFailedPolarActivities();
      console.log(`[POLAR] Globally retrying ${failedActivities.length} failed activities`);
      let retryCount = 0;
      for (const activity of failedActivities) {
        try {
          await storage.resetPolarActivityForRetry(activity.id);
          retryCount++;
        } catch (err) {
          console.error(`[POLAR] Failed to reset activity ${activity.id}:`, err);
        }
      }
      return c.json({
        message: `${retryCount}/${failedActivities.length} activities reset for retry globally`,
        retryCount,
        totalFailed: failedActivities.length
      });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/polar/connect", async (c) => {
    try {
      const userId = c.req.query("userId");
      const POLAR_CLIENT_ID = c.env.POLAR_CLIENT_ID;
      console.log("Polar connect request - userId:", userId, "POLAR_CLIENT_ID:", POLAR_CLIENT_ID ? "configured" : "NOT configured");
      if (!userId || !POLAR_CLIENT_ID) {
        console.error("Missing userId or POLAR_CLIENT_ID");
        return c.json({ error: "userId required and Polar not configured" }, 400);
      }
      const state = btoa(JSON.stringify({ userId, ts: Date.now() }));
      const redirectUri = `${c.env.WORKER_URL || "https://runna-io-api.runna-io-api.workers.dev"}/api/polar/callback`;
      const authUrl = `https://flow.polar.com/oauth2/authorization?response_type=code&client_id=${POLAR_CLIENT_ID}&redirect_uri=${encodeURIComponent(redirectUri)}&state=${state}`;
      console.log("Generated authUrl:", authUrl);
      return c.json({ authUrl });
    } catch (error) {
      console.error("Polar connect error:", error);
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/polar/callback", async (c) => {
    try {
      const code = c.req.query("code");
      const state = c.req.query("state");
      const authError = c.req.query("error");
      const POLAR_CLIENT_ID = c.env.POLAR_CLIENT_ID;
      const POLAR_CLIENT_SECRET = c.env.POLAR_CLIENT_SECRET;
      const FRONTEND_URL = c.env.FRONTEND_URL || "https://runna-io.pages.dev";
      console.log("Polar callback started - code:", code ? "present" : "missing", "state:", state ? "present" : "missing", "error:", authError);
      if (authError) {
        console.log("Auth error detected:", authError);
        return c.redirect(`${FRONTEND_URL}/profile?polar_error=denied`);
      }
      if (!code || !state || !POLAR_CLIENT_ID || !POLAR_CLIENT_SECRET) {
        console.error("Missing required params - code:", !!code, "state:", !!state, "CLIENT_ID:", !!POLAR_CLIENT_ID, "CLIENT_SECRET:", !!POLAR_CLIENT_SECRET);
        return c.redirect(`${FRONTEND_URL}/profile?polar_error=invalid`);
      }
      let userId;
      try {
        const decoded = JSON.parse(atob(state));
        userId = decoded.userId;
        console.log("State decoded - userId:", userId);
      } catch (e) {
        console.error("State decode error:", e);
        return c.redirect(`${FRONTEND_URL}/profile?polar_error=invalid_state`);
      }
      const redirectUri = `${c.env.WORKER_URL || "https://runna-io-api.runna-io-api.workers.dev"}/api/polar/callback`;
      const authHeader = btoa(`${POLAR_CLIENT_ID}:${POLAR_CLIENT_SECRET}`);
      console.log("Exchanging code for token...");
      const tokenResponse = await fetch("https://polarremote.com/v2/oauth2/token", {
        method: "POST",
        headers: {
          "Content-Type": "application/x-www-form-urlencoded",
          "Authorization": `Basic ${authHeader}`,
          "Accept": "application/json"
        },
        body: `grant_type=authorization_code&code=${code}&redirect_uri=${encodeURIComponent(redirectUri)}`
      });
      if (!tokenResponse.ok) {
        const errorText = await tokenResponse.text();
        console.error("Polar token exchange failed:", errorText);
        return c.redirect(`${FRONTEND_URL}/profile?polar_error=token_exchange`);
      }
      const tokenData = await tokenResponse.json();
      const { access_token, x_user_id } = tokenData;
      const normalizedPolarUserId = Number(x_user_id);
      console.log("Token received - x_user_id:", x_user_id);
      if (!Number.isFinite(normalizedPolarUserId)) {
        console.error("Invalid x_user_id received:", x_user_id);
        return c.redirect(`${FRONTEND_URL}/profile?polar_error=invalid_user`);
      }
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      console.log("Checking for existing account...");
      const existingAccount = await storage.getPolarAccountByPolarUserId(normalizedPolarUserId);
      if (existingAccount && existingAccount.userId !== userId) {
        console.log("Account already linked to different user");
        return c.redirect(`${FRONTEND_URL}/profile?polar_error=already_linked`);
      }
      try {
        console.log("Registering user with Polar using x_user_id:", x_user_id);
        const registerResponse = await fetch("https://www.polaraccesslink.com/v3/users", {
          method: "POST",
          headers: {
            "Content-Type": "application/xml",
            "Authorization": `Bearer ${access_token}`,
            "Accept": "application/json"
          },
          body: `<?xml version="1.0" encoding="UTF-8"?><register><member-id>${x_user_id}</member-id></register>`
        });
        if (!registerResponse.ok && registerResponse.status !== 409) {
          const errorText = await registerResponse.text();
          console.error("Polar user registration failed:", errorText);
          return c.redirect(`${FRONTEND_URL}/profile?polar_error=registration`);
        }
        console.log("User registered or already exists");
      } catch (e) {
        console.error("Polar registration error:", e);
      }
      const polarAccountData = {
        userId,
        polarUserId: normalizedPolarUserId,
        accessToken: access_token,
        memberId: normalizedPolarUserId.toString(),
        lastSyncAt: null
      };
      console.log("Saving account to database...");
      if (existingAccount) {
        await storage.updatePolarAccount(userId, polarAccountData);
        console.log("Account updated");
      } else {
        await storage.createPolarAccount(polarAccountData);
        console.log("Account created");
      }
      console.log("[BACKFILL] Triggering initial Polar sync via waitUntil...");
      const baseUrl2 = c.env.WORKER_URL || "https://runna-io-api.runna-io-api.workers.dev";
      c.executionCtx.waitUntil(
        new Promise((resolve) => setTimeout(resolve, 2e3)).then(
          () => fetch(`${baseUrl2}/api/polar/sync/${userId}`, { method: "POST" }).then((res) => console.log(`[BACKFILL] Initial sync triggered: ${res.status}`)).catch((err) => console.error("[BACKFILL] Initial sync failed:", err))
        )
      );
      console.log("Polar callback success - redirecting to:", `${FRONTEND_URL}/profile?polar_connected=true`);
      return c.redirect(`${FRONTEND_URL}/profile?polar_connected=true`);
    } catch (error) {
      console.error("Polar callback error:", error);
      return c.redirect(`${c.env.FRONTEND_URL || "https://runna-io.pages.dev"}/profile?polar_error=server`);
    }
  });
  app2.post("/api/polar/disconnect", async (c) => {
    try {
      const body = await c.req.json();
      const { userId } = body;
      if (!userId) {
        return c.json({ error: "userId required" }, 400);
      }
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const polarAccount = await storage.getPolarAccountByUserId(userId);
      if (!polarAccount) {
        return c.json({ error: "Polar account not connected" }, 404);
      }
      try {
        await fetch(`https://www.polaraccesslink.com/v3/users/${polarAccount.polarUserId}`, {
          method: "DELETE",
          headers: { "Authorization": `Bearer ${polarAccount.accessToken}` }
        });
      } catch (e) {
        console.error("Failed to delete Polar user:", e);
      }
      await storage.deletePolarAccount(userId);
      return c.json({ success: true });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/polar/transactions/reset/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const polarAccount = await storage.getPolarAccountByUserId(userId);
      if (!polarAccount) {
        return c.json({ error: "Polar account not connected" }, 404);
      }
      const status = await resetPolarTransactions(polarAccount);
      return c.json({ resetStatus: status });
    } catch (error) {
      console.error("[RESET] error:", error);
      return c.json({ error: error.message }, 500);
    }
  });
  const resetPolarTransactions = /* @__PURE__ */ __name(async (polarAccount) => {
    try {
      const res = await fetch(`https://www.polaraccesslink.com/v3/users/${polarAccount.polarUserId}/exercise-transactions`, {
        method: "DELETE",
        headers: { "Authorization": `Bearer ${polarAccount.accessToken}` }
      });
      console.log("[SYNC] Reset transactions status:", res.status);
      return res.status;
    } catch (err) {
      console.error("[SYNC] Reset transactions failed:", err);
      return null;
    }
  }, "resetPolarTransactions");
  const syncPolarExercisesDirect = /* @__PURE__ */ __name(async (polarAccount, userId, storage) => {
    console.log("[DIRECT SYNC] Using non-transactional GET /v3/exercises endpoint...");
    const syncStartTime = Date.now();
    const MAX_SYNC_TIME = 2e4;
    const MAX_EXERCISES = 10;
    const GPX_FETCH_BUDGET = 5;
    let gpxFetchesUsed = 0;
    try {
      const listResponse = await fetch(
        "https://www.polaraccesslink.com/v3/exercises?samples=false&zones=false&route=true",
        {
          headers: {
            "Authorization": `Bearer ${polarAccount.accessToken}`,
            "Accept": "application/json"
          }
        }
      );
      if (listResponse.status === 401) {
        console.error("[DIRECT SYNC] Token expired");
        return { imported: 0, total: 0, message: "Polar token expired" };
      }
      if (!listResponse.ok) {
        const errorText = await listResponse.text();
        console.error("[DIRECT SYNC] Failed to list exercises:", listResponse.status, errorText);
        return { imported: 0, total: 0, message: `Failed to fetch exercises: ${listResponse.status}` };
      }
      const exercises = await listResponse.json();
      console.log(`[DIRECT SYNC] Found ${exercises.length} exercises from Polar`);
      let imported = 0;
      let skipped = 0;
      let errors = 0;
      let processed = 0;
      let stoppedEarly = false;
      for (const exercise of exercises) {
        if (Date.now() - syncStartTime > MAX_SYNC_TIME) {
          console.warn(`[DIRECT SYNC] \u23F1\uFE0F Time limit reached (${MAX_SYNC_TIME}ms) \u2014 stopping at ${processed} exercises`);
          stoppedEarly = true;
          break;
        }
        const exerciseId = exercise.id;
        if (!exerciseId) {
          console.warn("[DIRECT SYNC] Exercise without id, skipping");
          skipped++;
          continue;
        }
        const existing = await storage.getPolarActivityByPolarId(exerciseId.toString());
        if (existing) {
          if (!existing.skipReason) {
            console.log(`[DIRECT SYNC]    \u23ED\uFE0F  Already imported: ${exerciseId}`);
            skipped++;
            continue;
          }
          const permanentSkipReasons = ["before_competition", "excluded_type"];
          if (permanentSkipReasons.includes(existing.skipReason)) {
            skipped++;
            continue;
          }
        }
        if (processed >= MAX_EXERCISES) {
          console.warn(`[DIRECT SYNC] \u{1F4CA} Exercise limit reached (${MAX_EXERCISES}) \u2014 stopping`);
          stoppedEarly = true;
          break;
        }
        processed++;
        try {
          console.log(`
[DIRECT SYNC] \u{1F4CD} Exercise: ${exerciseId}`);
          if (existing && existing.skipReason) {
            console.log(`[DIRECT SYNC]    \u{1F504} Previously skipped (${existing.skipReason}), re-evaluating`);
            await storage.deletePolarActivityById(existing.id);
          }
          const startTimeRaw = exercise.start_time;
          if (!existing && startTimeRaw) {
            const dupCheck = await storage.findPolarActivityByAttributes(
              userId,
              Number(exercise.distance) || 0,
              new Date(startTimeRaw).toISOString()
            );
            if (dupCheck && !dupCheck.skipReason) {
              console.log(`[DIRECT SYNC]    \u23ED\uFE0F  Already imported (by attributes match: ${dupCheck.id})`);
              skipped++;
              continue;
            }
            if (dupCheck && dupCheck.skipReason) {
              const permanentSkipReasons = ["before_competition", "excluded_type"];
              if (permanentSkipReasons.includes(dupCheck.skipReason)) {
                console.log(`[DIRECT SYNC]    \u23ED\uFE0F  Permanently skipped by attributes (${dupCheck.skipReason})`);
                skipped++;
                continue;
              }
              console.log(`[DIRECT SYNC]    \u{1F504} Attribute match was skipped (${dupCheck.skipReason}), re-evaluating`);
              await storage.deletePolarActivityById(dupCheck.id);
            }
          }
          const sport = exercise.detailed_sport_info || exercise.sport || "";
          const activityType = String(sport).toLowerCase().trim();
          const distance2 = Number(exercise.distance) || 0;
          const duration = exercise.duration ? parseDuration(exercise.duration) : 0;
          const startTime = exercise.start_time;
          console.log(`[DIRECT SYNC]    Sport: "${sport}" | Distance: ${distance2}m | Duration: ${duration}s | Start: ${startTime}`);
          const saveSkipped = /* @__PURE__ */ __name(async (reason) => {
            const startDateISO2 = startTime ? new Date(startTime).toISOString() : (/* @__PURE__ */ new Date()).toISOString();
            await storage.createPolarActivity({
              polarExerciseId: exerciseId.toString(),
              userId,
              routeId: null,
              territoryId: null,
              name: `${sport || "Actividad"} (${(distance2 / 1e3).toFixed(2)}km)`,
              activityType: sport || "unknown",
              distance: distance2,
              duration,
              startDate: startDateISO2,
              summaryPolyline: null,
              processed: true,
              processedAt: (/* @__PURE__ */ new Date()).toISOString(),
              skipReason: reason
            });
            console.log(`[DIRECT SYNC]    \u{1F4BE} Saved as skipped: ${reason}`);
          }, "saveSkipped");
          const COMPETITION_MIN_DATE = "2026-03-02T00:00:00Z";
          if (startTime) {
            const activityDate = new Date(startTime);
            if (activityDate < new Date(COMPETITION_MIN_DATE)) {
              console.log(`[DIRECT SYNC]    \u274C Activity before competition start (${startTime})`);
              await saveSkipped("before_competition");
              skipped++;
              continue;
            }
          }
          const excludeTypes = ["sleep", "rest", "pause", "meditation", "breathing"];
          const isExcluded = excludeTypes.some((t) => activityType.includes(t));
          if (isExcluded) {
            console.log(`[DIRECT SYNC]    \u274C Excluded type (${sport})`);
            await saveSkipped("excluded_type");
            skipped++;
            continue;
          }
          if (distance2 < 100) {
            console.log(`[DIRECT SYNC]    \u274C Distance too short (${distance2}m)`);
            await saveSkipped("distance_too_short");
            skipped++;
            continue;
          }
          if (duration < 60) {
            console.log(`[DIRECT SYNC]    \u274C Duration too short (${duration}s)`);
            await saveSkipped("duration_too_short");
            skipped++;
            continue;
          }
          let summaryPolyline = null;
          if (exercise.route && Array.isArray(exercise.route) && exercise.route.length >= 2) {
            const coordinates = exercise.route.filter((pt) => pt.latitude && pt.longitude).map((pt) => [pt.latitude, pt.longitude]);
            if (coordinates.length >= 2) {
              summaryPolyline = encodePolyline(coordinates);
              console.log(`[DIRECT SYNC]    \u2705 Route encoded from inline data (${coordinates.length} points)`);
            }
          }
          if (!summaryPolyline && gpxFetchesUsed < GPX_FETCH_BUDGET) {
            try {
              gpxFetchesUsed++;
              const gpxResponse = await fetch(
                `https://www.polaraccesslink.com/v3/exercises/${exerciseId}/gpx`,
                {
                  headers: {
                    "Authorization": `Bearer ${polarAccount.accessToken}`,
                    "Accept": "application/gpx+xml"
                  }
                }
              );
              if (gpxResponse.ok) {
                const gpxText = await gpxResponse.text();
                if (gpxText && gpxText.length > 0) {
                  const coordinates = parseGpxToCoordinates(gpxText);
                  if (coordinates.length >= 2) {
                    summaryPolyline = encodePolyline(coordinates);
                    console.log(`[DIRECT SYNC]    \u2705 Polyline from GPX (${coordinates.length} points)`);
                  }
                }
              } else {
                console.log(`[DIRECT SYNC]    \u2139\uFE0F  No GPX (${gpxResponse.status})`);
              }
            } catch (e) {
              console.error(`[DIRECT SYNC]    \u274C GPX error: ${e}`);
            }
          } else if (!summaryPolyline && gpxFetchesUsed >= GPX_FETCH_BUDGET) {
            console.log(`[DIRECT SYNC]    \u23ED\uFE0F GPX budget exhausted (${GPX_FETCH_BUDGET}), skipping GPX fetch`);
          }
          const startDateISO = startTime ? new Date(startTime).toISOString() : (/* @__PURE__ */ new Date()).toISOString();
          await storage.createPolarActivity({
            polarExerciseId: exerciseId.toString(),
            userId,
            routeId: null,
            territoryId: null,
            name: `${sport} (${(distance2 / 1e3).toFixed(2)}km)`,
            activityType: sport,
            distance: distance2,
            duration,
            startDate: startDateISO,
            summaryPolyline,
            processed: summaryPolyline ? false : true,
            // Auto-mark no-GPS activities as processed (can't be displayed anyway)
            processedAt: summaryPolyline ? null : (/* @__PURE__ */ new Date()).toISOString()
          });
          console.log(`[DIRECT SYNC]    \u2705 IMPORTED!${!summaryPolyline ? " (no GPS, auto-processed)" : ""}`);
          imported++;
        } catch (e) {
          console.error(`[DIRECT SYNC]    \u274C Error: ${e}`);
          errors++;
        }
      }
      const remaining = stoppedEarly ? exercises.length - processed : 0;
      console.log(`
[DIRECT SYNC] \u{1F4CA} RESULT: ${imported} imported, ${skipped} skipped, ${errors} errors out of ${exercises.length} total (processed ${processed}, remaining ~${remaining})`);
      return {
        imported,
        total: exercises.length,
        skipped,
        errors,
        remaining,
        message: `${imported} importadas de ${exercises.length} (direct sync)${stoppedEarly ? ` \u2014 parado temprano, ~${remaining} pendientes` : ""}`
      };
    } catch (error) {
      console.error("[DIRECT SYNC] Error:", error);
      return { imported: 0, total: 0, message: `Direct sync error: ${error.message}` };
    }
  }, "syncPolarExercisesDirect");
  app2.post("/api/polar/sync/:userId", async (c) => {
    const userId = c.req.param("userId");
    const db = getDb(c.env);
    const storage = new WorkerStorage(db);
    const polarAccount = await storage.getPolarAccountByUserId(userId);
    try {
      if (!polarAccount) {
        console.error("[SYNC] No Polar account found for user:", userId);
        return c.json({ error: "Polar account not connected" }, 404);
      }
      console.log(`
\u{1F504} [SYNC] Starting Polar sync for user: ${userId}`);
      console.log("[SYNC] Using non-transactional GET /v3/exercises (recommended by Polar)");
      const result = await syncPolarExercisesDirect(polarAccount, userId, storage);
      await storage.updatePolarAccount(userId, { lastSyncAt: /* @__PURE__ */ new Date() });
      return c.json(result);
    } catch (error) {
      console.error("Polar sync error:", error);
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/polar/activities/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const activities = await storage.getPolarActivitiesByUserId(userId);
      return c.json(activities);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.delete("/api/polar/activities/:userId/:activityId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const activityId = c.req.param("activityId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const activity = await storage.getPolarActivityById(activityId);
      if (!activity) {
        return c.json({ error: "Actividad no encontrada" }, 404);
      }
      if (activity.userId !== userId) {
        return c.json({ error: "No autorizado" }, 403);
      }
      let routeId = activity.routeId;
      if (!routeId) {
        console.log(`[DELETE] routeId is null for activity ${activityId}, searching by attributes...`);
        let matchedRoute = await storage.findRouteByAttributes(userId, activity.name, activity.distance);
        if (!matchedRoute && activity.startDate) {
          matchedRoute = await storage.findRouteByDateAndDistance(userId, activity.startDate, activity.distance);
        }
        if (matchedRoute) {
          routeId = matchedRoute.id;
          console.log(`[DELETE] Found matching route by attributes or date: ${routeId}`);
        } else {
          console.log(`[DELETE] No matching route found for activity ${activityId}`);
        }
      }
      if (routeId) {
        try {
          await storage.detachTerritoriesFromRoute(routeId);
          await storage.deleteConquestMetricsByRouteId(routeId);
          await storage.deletePolarActivityByRouteId(routeId);
          await storage.deleteStravaActivityByRouteId(routeId);
          await storage.deleteRouteById(routeId);
          console.log(`[DELETE] Route ${routeId} and associated records cleaned up`);
        } catch (e) {
          console.error("Error cleaning up route-related data:", e);
        }
      } else {
        await storage.deletePolarActivityById(activityId);
        console.log(`[DELETE] Polar activity ${activityId} deleted (no route attached)`);
      }
      try {
        console.log("[DELETE] Reprocessing user territories only (safe mode)");
        await reprocessUserTerritories(storage, userId);
      } catch (e) {
        console.error("Error reprocessing territories after delete:", e);
      }
      return c.json({ success: true });
    } catch (error) {
      console.error("Error deleting Polar activity:", error);
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/polar/sync-full/:userId", async (c) => {
    const userId = c.req.param("userId");
    const db = getDb(c.env);
    const storage = new WorkerStorage(db);
    const polarAccount = await storage.getPolarAccountByUserId(userId);
    try {
      if (!polarAccount) {
        return c.json({ error: "Polar account not connected" }, 404);
      }
      console.log(`
\u{1F504} [FULL SYNC] Starting full sync for user: ${userId}`);
      console.log("[FULL SYNC] Using non-transactional GET /v3/exercises (recommended by Polar)");
      const result = await syncPolarExercisesDirect(polarAccount, userId, storage);
      await storage.updatePolarAccount(userId, { lastSyncAt: /* @__PURE__ */ new Date() });
      return c.json(result);
    } catch (error) {
      console.error("Polar full sync error:", error);
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/polar/process/:userId", async (c) => {
    const startTime = Date.now();
    const MAX_PROCESSING_TIME = 25e3;
    try {
      const userId = c.req.param("userId");
      const body = await c.req.json().catch(() => ({}));
      const requestedActivityId = body?.activityId || null;
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const unprocessed = await storage.getUnprocessedPolarActivities(userId);
      console.log(`[PROCESS] Starting - ${unprocessed.length} unprocessed Polar activities for user ${userId}${requestedActivityId ? ` (requested: ${requestedActivityId})` : ""}`);
      if (unprocessed.length === 0) {
        return c.json({ processed: 0, results: [], message: "No activities to process" });
      }
      const results = [];
      let toBatch;
      if (requestedActivityId) {
        const requested = unprocessed.find((a) => a.id === requestedActivityId);
        if (!requested) {
          console.log(`[PROCESS] Requested activity ${requestedActivityId} not in unprocessed list, may already be processed`);
          return c.json({ processed: 0, results: [], message: "Activity already processed or not found" });
        }
        toBatch = [requested];
      } else {
        const BATCH_SIZE = 1;
        toBatch = unprocessed.slice(0, BATCH_SIZE);
      }
      console.log(`[PROCESS] Processing batch of ${toBatch.length} activities (${unprocessed.length - toBatch.length} remaining)`);
      for (const activity of toBatch) {
        if (Date.now() - startTime > MAX_PROCESSING_TIME) {
          console.warn(`[PROCESS] Timeout approaching - stopping at ${results.length} processed`);
          break;
        }
        if (!activity.summaryPolyline) {
          console.log(`[PROCESS] Skipping activity ${activity.id} - no GPS data`);
          await storage.updatePolarActivity(activity.id, { processed: true, processedAt: /* @__PURE__ */ new Date(), skipReason: "no_gps" });
          continue;
        }
        try {
          console.log(`[PROCESS] Processing activity ${activity.id}: ${activity.name}`);
          const startDate = coerceDate(activity.startDate);
          if (!startDate) {
            console.log(`[PROCESS] Skipping activity ${activity.id} - invalid start date`);
            await storage.updatePolarActivity(activity.id, { processed: true, processedAt: /* @__PURE__ */ new Date(), skipReason: "bad_date" });
            continue;
          }
          const COMP_PROCESS_MIN_DATE = /* @__PURE__ */ new Date("2026-03-02T00:00:00Z");
          if (startDate < COMP_PROCESS_MIN_DATE) {
            console.log(`[PROCESS] Skipping activity ${activity.id} - before competition start (${startDate.toISOString()})`);
            await storage.updatePolarActivity(activity.id, { processed: true, processedAt: /* @__PURE__ */ new Date(), skipReason: "before_competition" });
            continue;
          }
          const decoded = decodePolyline(activity.summaryPolyline);
          const coordinates = decoded.map((coord) => [coord[0], coord[1]]);
          if (coordinates.length < 10) {
            console.log(`[PROCESS] Short Polar activity ${activity.id} (${coordinates.length} coords) \u2014 creating route + feed event without territory`);
            try {
              const startedAtShort = startDate.toISOString();
              const completedAtShort = new Date(startDate.getTime() + activity.duration * 1e3).toISOString();
              let shortRoute = await storage.findRouteByDateAndDistance(userId, startedAtShort, activity.distance);
              if (!shortRoute) {
                shortRoute = await storage.createRoute({
                  userId: activity.userId,
                  name: activity.name,
                  coordinates,
                  distance: activity.distance,
                  duration: activity.duration,
                  startedAt: startedAtShort,
                  completedAt: completedAtShort
                });
              }
              await generateFeedEvents(storage, userId, shortRoute.id, activity.distance, activity.duration, { newAreaConquered: 0, victims: [], ranTogetherWith: [] });
              await storage.updatePolarActivity(activity.id, { routeId: shortRoute.id, processed: true, processedAt: /* @__PURE__ */ new Date() });
            } catch (feedErr) {
              console.warn("[POLAR] Short route feed event failed:", feedErr);
              await storage.updatePolarActivity(activity.id, { processed: true, processedAt: /* @__PURE__ */ new Date() });
            }
            continue;
          }
          console.log(`[PROCESS] Creating route for activity ${activity.id}`);
          const startedAtStr = startDate.toISOString();
          const completedAtStr = new Date(startDate.getTime() + activity.duration * 1e3).toISOString();
          let route = null;
          if (activity.routeId) {
            route = await storage.getRouteById(activity.routeId);
            if (route) {
              console.log(`[PROCESS] Reusing existing route ${route.id} for activity ${activity.id}`);
            }
          }
          if (!route) {
            route = await storage.findRouteByDateAndDistance(userId, startedAtStr, activity.distance);
            if (route) {
              console.log(`[PROCESS] Found existing route by date+distance: ${route.id} - reusing to avoid duplicate`);
            }
          }
          if (!route) {
            route = await storage.createRoute({
              userId: activity.userId,
              name: activity.name,
              coordinates,
              distance: activity.distance,
              duration: activity.duration,
              startedAt: startedAtStr,
              completedAt: completedAtStr
            });
            console.log(`[PROCESS] Route created: ${route.id}`);
          }
          await storage.updatePolarActivity(activity.id, { routeId: route.id });
          const needsChronologicalReprocess = false;
          console.log(`[PROCESS] Calculating enclosed territory (${coordinates.length} coords)...`);
          const enclosedPoly = routeToEnclosedPolygon(coordinates, 150);
          console.log(`[PROCESS] Enclosed polygon calculated`);
          if (enclosedPoly) {
            console.log(`[PROCESS] Processing territory conquest...`);
            const conquestResult = await processTerritoryConquest(
              storage,
              userId,
              route.id,
              enclosedPoly.geometry,
              void 0,
              // skip notifications to reduce subrequests
              startedAtStr,
              completedAtStr,
              coordinates
            );
            if (conquestResult.ranTogetherWith.length > 0) {
              await storage.updateRouteRanTogether(route.id, conquestResult.ranTogetherWith);
            }
            console.log(`[PROCESS] Territory updated: ${conquestResult.territory.id}, area: ${conquestResult.territory.area}`);
            await storage.updatePolarActivity(activity.id, {
              routeId: route.id,
              processed: true,
              processedAt: /* @__PURE__ */ new Date()
            });
            results.push({
              activityId: activity.id,
              routeId: route.id,
              territoryId: conquestResult.territory.id,
              area: conquestResult.territory.area,
              needsChronologicalReprocess,
              metrics: {
                totalArea: conquestResult.totalArea,
                newAreaConquered: conquestResult.newAreaConquered,
                areaStolen: conquestResult.areaStolen,
                ranTogetherWith: conquestResult.ranTogetherWith,
                victimsNotified: conquestResult.victimsNotified,
                victims: conquestResult.victims,
                treasuresCollected: conquestResult.treasuresCollected || [],
                powersUsed: conquestResult.powersUsed || []
              }
            });
            try {
              await generateFeedEvents(storage, userId, route.id, activity.distance, activity.duration, conquestResult, true);
            } catch (feedErr) {
              console.warn(`[PROCESS] Feed events failed (non-critical):`, feedErr);
            }
          } else {
            try {
              await generateFeedEvents(storage, userId, route.id, activity.distance, activity.duration, { newAreaConquered: 0, victims: [], ranTogetherWith: [] }, true);
            } catch (feedErr) {
              console.warn("[POLAR] Feed event failed for route without polygon:", feedErr);
            }
            await storage.updatePolarActivity(activity.id, {
              routeId: route.id,
              processed: true,
              processedAt: /* @__PURE__ */ new Date()
            });
          }
          console.log(`[PROCESS] \u2705 Activity ${activity.id} processed successfully`);
        } catch (e) {
          console.error(`[PROCESS] \u274C Error processing activity ${activity.id}:`, e);
          await storage.updatePolarActivity(activity.id, { processed: true, processedAt: /* @__PURE__ */ new Date() });
        }
      }
      const remaining = unprocessed.length - toBatch.length;
      const processingTime = Date.now() - startTime;
      console.log(`[PROCESS] Completed in ${processingTime}ms - ${results.length} processed, ${remaining} remaining`);
      return c.json({
        processed: results.length,
        results,
        remaining,
        processingTime,
        message: remaining > 0 ? `${results.length} procesadas, ${remaining} pendientes. Ejecuta de nuevo para continuar.` : `${results.length} procesadas correctamente`
      });
    } catch (error) {
      const processingTime = Date.now() - startTime;
      console.error("[PROCESS] \u274C Critical error:", error);
      return c.json({
        error: error.message,
        processed: 0,
        processingTime,
        message: "Error al procesar actividades. Por favor intenta de nuevo."
      }, 500);
    }
  });
  app2.get("/api/polar/debug/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      console.log("\u{1F50D} [DEBUG] Starting Polar data check for user:", userId);
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const polarAccount = await storage.getPolarAccountByUserId(userId);
      if (!polarAccount) {
        return c.json({ error: "No Polar account" }, 404);
      }
      console.log("\u2705 Account found");
      console.log("  Token:", polarAccount.accessToken?.substring(0, 20) + "...");
      console.log("\n\u{1F50D} [DEBUG EXERCISES]");
      const exercisesResponse = await fetch(
        "https://www.polaraccesslink.com/v3/exercises",
        {
          method: "GET",
          headers: {
            "Authorization": `Bearer ${polarAccount.accessToken}`,
            "Accept": "application/json"
          }
        }
      );
      console.log("Status:", exercisesResponse.status);
      const exercisesText = await exercisesResponse.text();
      console.log("Response length:", exercisesText.length);
      console.log("Response preview:", exercisesText.substring(0, 500));
      let exercises = [];
      if (exercisesText.length > 0) {
        try {
          exercises = JSON.parse(exercisesText);
          console.log("Parsed exercises:", exercises.length);
          if (exercises.length > 0) {
            console.log("\nFirst exercise sample:");
            console.log(JSON.stringify(exercises[0], null, 2));
          }
        } catch (e) {
          console.error("\u274C Failed to parse exercises:", e);
        }
      }
      console.log("\n\u{1F50D} [DEBUG DAILY ACTIVITIES]");
      const to = /* @__PURE__ */ new Date();
      const from = new Date(to.getTime() - 30 * 24 * 60 * 60 * 1e3);
      const fromStr = from.toISOString().split("T")[0];
      const toStr = to.toISOString().split("T")[0];
      console.log("Date range:", fromStr, "to", toStr);
      const activitiesResponse = await fetch(
        `https://www.polaraccesslink.com/v3/users/activities?from=${fromStr}&to=${toStr}`,
        {
          method: "GET",
          headers: {
            "Authorization": `Bearer ${polarAccount.accessToken}`,
            "Accept": "application/json"
          }
        }
      );
      console.log("Status:", activitiesResponse.status);
      const activitiesText = await activitiesResponse.text();
      console.log("Response length:", activitiesText.length);
      console.log("Response preview:", activitiesText.substring(0, 500));
      let activities = [];
      if (activitiesText.length > 0) {
        try {
          const activitiesData = JSON.parse(activitiesText);
          activities = Array.isArray(activitiesData) ? activitiesData : activitiesData.activities || [];
          console.log("Parsed activities:", activities.length);
          if (activities.length > 0) {
            console.log("\nFirst activity sample:");
            console.log(JSON.stringify(activities[0], null, 2));
          }
        } catch (e) {
          console.error("\u274C Failed to parse activities:", e);
        }
      }
      console.log("\n\u{1F50D} [DEBUG DATABASE]");
      const dbActivities = await storage.getPolarActivitiesByUserId(userId);
      console.log("Activities in DB:", dbActivities.length);
      if (dbActivities.length > 0) {
        console.log("First DB activity:");
        console.log(JSON.stringify(dbActivities[0], null, 2).substring(0, 300));
      }
      return c.json({
        summary: {
          polarAccountFound: !!polarAccount,
          tokenValid: !!polarAccount.accessToken,
          exercisesFromAPI: {
            count: exercises.length,
            status: exercisesResponse.status,
            sample: exercises.length > 0 ? exercises[0] : null
          },
          activitiesFromAPI: {
            count: activities.length,
            status: activitiesResponse.status,
            dateRange: `${fromStr} to ${toStr}`,
            sample: activities.length > 0 ? activities[0] : null
          },
          databaseActivities: {
            count: dbActivities.length,
            all: dbActivities
          }
        },
        logs: "Check Cloudflare Real-time tail for full logs"
      });
    } catch (error) {
      console.error("\u274C [DEBUG ERROR]:", error.message);
      console.error(error.stack);
      return c.json({ error: error.message, stack: error.stack }, 500);
    }
  });
  app2.get("/api/coros/status/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const corosAccount = await storage.getCorosAccountByUserId(userId);
      if (corosAccount) {
        const stats = await storage.getCorosActivityStats(userId);
        return c.json({
          connected: true,
          corosOpenId: corosAccount.corosOpenId,
          lastSyncAt: corosAccount.lastSyncAt,
          totalActivities: stats.total,
          pendingActivities: stats.unprocessed,
          lastActivityStart: stats.lastStartDate
        });
      } else {
        return c.json({ connected: false });
      }
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/coros/connect", async (c) => {
    try {
      const userId = c.req.query("userId");
      const COROS_CLIENT_ID = c.env.COROS_CLIENT_ID;
      const COROS_REDIRECT_URI = `${c.env.WORKER_URL || "https://runna-io-api.runna-io-api.workers.dev"}/api/coros/callback`;
      if (!userId || !COROS_CLIENT_ID) {
        return c.json({ error: "userId required and COROS not configured" }, 400);
      }
      const state = btoa(JSON.stringify({ userId, ts: Date.now() }));
      const authUrl = `https://open.coros.com/oauth2/authorize?client_id=${COROS_CLIENT_ID}&redirect_uri=${encodeURIComponent(COROS_REDIRECT_URI)}&response_type=code&state=${state}`;
      return c.json({ authUrl });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/coros/callback", async (c) => {
    const FRONTEND_URL = c.env.FRONTEND_URL || "https://runna-io.pages.dev";
    try {
      const code = c.req.query("code");
      const state = c.req.query("state");
      const authError = c.req.query("error");
      const COROS_CLIENT_ID = c.env.COROS_CLIENT_ID;
      const COROS_CLIENT_SECRET = c.env.COROS_CLIENT_SECRET;
      if (authError) {
        return c.redirect(`${FRONTEND_URL}/?coros_error=denied`);
      }
      if (!code || !state || !COROS_CLIENT_ID || !COROS_CLIENT_SECRET) {
        return c.redirect(`${FRONTEND_URL}/?coros_error=invalid`);
      }
      let userId;
      try {
        const decoded = JSON.parse(atob(state));
        userId = decoded.userId;
      } catch {
        return c.redirect(`${FRONTEND_URL}/?coros_error=invalid_state`);
      }
      const tokenResponse = await fetch("https://open.coros.com/oauth2/token", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          client_id: COROS_CLIENT_ID,
          client_secret: COROS_CLIENT_SECRET,
          code,
          grant_type: "authorization_code"
        })
      });
      if (!tokenResponse.ok) {
        console.error("COROS token exchange failed:", await tokenResponse.text());
        return c.redirect(`${FRONTEND_URL}/?coros_error=token_exchange`);
      }
      const tokenData = await tokenResponse.json();
      const { access_token, refresh_token, expires_in, openId } = tokenData;
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const existingAccount = await storage.getCorosAccountByOpenId(openId);
      if (existingAccount && existingAccount.userId !== userId) {
        return c.redirect(`${FRONTEND_URL}/?coros_error=already_linked`);
      }
      const expiresAt = expires_in ? new Date(Date.now() + expires_in * 1e3) : null;
      const corosAccountData = {
        userId,
        corosOpenId: openId,
        accessToken: access_token,
        refreshToken: refresh_token,
        expiresAt,
        lastSyncAt: null
      };
      if (existingAccount) {
        await storage.updateCorosAccount(userId, corosAccountData);
      } else {
        await storage.createCorosAccount(corosAccountData);
      }
      return c.redirect(`${FRONTEND_URL}/?coros_connected=true`);
    } catch (error) {
      console.error("COROS callback error:", error);
      return c.redirect(`${FRONTEND_URL}/?coros_error=server`);
    }
  });
  app2.post("/api/coros/disconnect", async (c) => {
    try {
      const body = await c.req.json();
      const { userId } = body;
      if (!userId) {
        return c.json({ error: "userId required" }, 400);
      }
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const corosAccount = await storage.getCorosAccountByUserId(userId);
      if (!corosAccount) {
        return c.json({ error: "COROS account not connected" }, 404);
      }
      await storage.deleteCorosAccount(userId);
      return c.json({ success: true });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/coros/webhook", async (c) => {
    try {
      const body = await c.req.json();
      console.log("[COROS WEBHOOK] Received data:", JSON.stringify(body));
      return c.json({ received: true }, 200);
    } catch (error) {
      console.error("COROS webhook error:", error);
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/coros/activities/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const activities = await storage.getCorosActivitiesByUserId(userId);
      return c.json({ activities });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.delete("/api/coros/activities/:userId/:activityId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const activityId = c.req.param("activityId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const activity = await storage.getCorosActivityById(activityId);
      if (!activity || activity.userId !== userId) {
        return c.json({ error: "Activity not found or unauthorized" }, 404);
      }
      if (activity.routeId) {
        await storage.deleteRoute(activity.routeId);
      }
      if (activity.territoryId) {
        await storage.deleteTerritory(activity.territoryId);
      }
      await storage.deleteCorosActivity(activityId);
      return c.json({ success: true });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/coros/process/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const activities = await storage.getUnprocessedCorosActivities(userId);
      let processed = 0;
      let skipped = 0;
      const errors = [];
      for (const activity of activities) {
        try {
          if (!activity.summaryPolyline) {
            await storage.markCorosActivityProcessed(activity.id, null, null, "no_gps");
            skipped++;
            continue;
          }
          const route = await storage.createRoute({
            userId,
            name: activity.name,
            distance: activity.distance,
            duration: activity.duration,
            gpxData: activity.summaryPolyline,
            sourceType: "coros",
            sourceId: activity.id,
            createdAt: new Date(activity.startDate)
          });
          const routeCoords = JSON.parse(route.gpxData);
          const lineString2 = lineString(routeCoords);
          const buffered = buffer(lineString2, 0.05, { units: "kilometers" });
          const territory = await storage.createTerritory({
            userId,
            routeId: route.id,
            geometry: JSON.stringify(buffered.geometry),
            area: area(buffered),
            createdAt: new Date(activity.startDate)
          });
          await storage.handleTerritoryOverlaps(territory.id, userId);
          await storage.markCorosActivityProcessed(activity.id, route.id, territory.id, null);
          processed++;
        } catch (err) {
          console.error(`[COROS] Failed to process activity ${activity.id}:`, err);
          await storage.markCorosActivityProcessed(activity.id, null, null, err.message);
          errors.push(`${activity.name}: ${err.message}`);
        }
      }
      return c.json({ processed, skipped, errors, total: activities.length });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/ephemeral-photos", async (c) => {
    try {
      const { senderId, recipientId, photoData, message, areaStolen } = await c.req.json();
      if (!senderId || !recipientId || !photoData) {
        return c.json({ error: "senderId, recipientId, and photoData are required" }, 400);
      }
      if (photoData.length > 7e5) {
        return c.json({ error: "Photo too large. Max 500KB." }, 400);
      }
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const expiresAt = new Date(Date.now() + 24 * 60 * 60 * 1e3).toISOString();
      const photo = await storage.createEphemeralPhoto({
        senderId,
        recipientId,
        photoData,
        message: message || null,
        areaStolen: areaStolen || null,
        expiresAt
      });
      try {
        const sender = await storage.getUser(senderId);
        if (sender) {
          const subs = await storage.getPushSubscriptionsByUserId(recipientId);
          if (subs.length > 0) {
            const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_pushHelper(), pushHelper_exports));
            const pushSubs = subs.map((s) => ({ endpoint: s.endpoint, keys: { p256dh: s.p256dh, auth: s.auth } }));
            const areaText = areaStolen ? formatAreaNotification(areaStolen) : null;
            const bodyText = areaText ? `${sender.name} te ha robado ${areaText} y te ha enviado una foto` : `${sender.name} te ha enviado una foto de conquista`;
            await sendPushToUser2(
              pushSubs,
              {
                title: "\u{1F4F8} \xA1Foto de conquista!",
                body: bodyText,
                tag: "ephemeral-photo",
                data: { url: "/", type: "ephemeral-photo", photoId: photo.id }
              },
              c.env.VAPID_PUBLIC_KEY || "",
              c.env.VAPID_PRIVATE_KEY || "",
              c.env.VAPID_SUBJECT || "mailto:notifications@runna.io"
            );
          }
        }
      } catch (_) {
      }
      return c.json({ success: true, photoId: photo.id });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/ephemeral-photos/pending/:userId", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      await storage.cleanupExpiredPhotos();
      const photos = await storage.getPendingPhotosForUser(userId);
      const listing = photos.map((p) => ({
        id: p.id,
        senderId: p.senderId,
        senderName: p.senderName,
        senderUsername: p.senderUsername,
        senderAvatar: p.senderAvatar,
        message: p.message,
        areaStolen: p.areaStolen,
        createdAt: p.createdAt
      }));
      return c.json(listing);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/ephemeral-photos/:photoId/view", async (c) => {
    try {
      const photoId = c.req.param("photoId");
      const userId = c.req.query("userId");
      if (!userId) {
        return c.json({ error: "userId query param required" }, 400);
      }
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const photo = await storage.viewAndDeleteEphemeralPhoto(photoId, userId);
      if (!photo) {
        return c.json({ error: "Photo not found or already viewed" }, 404);
      }
      return c.json(photo);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  const COMPETITION_NAME2 = "La Primera Conquista del Ebro";
  const COMPETITION_SLUG2 = "la-primera-conquista-del-ebro";
  const COMPETITION_START2 = "2026-03-02T09:00:00+01:00";
  const COMPETITION_END2 = "2026-03-30T23:59:59+02:00";
  const ZARAGOZA_BOUNDS3 = {
    minLat: 41.62,
    maxLat: 41.69,
    minLng: -0.93,
    maxLng: -0.84
  };
  const ZARAGOZA_SPAWN_POINTS = [
    // Centro histórico
    { lat: 41.656, lng: -0.8773, zone: "Plaza del Pilar" },
    { lat: 41.6542, lng: -0.8817, zone: "Calle Alfonso I" },
    { lat: 41.653, lng: -0.877, zone: "El Tubo" },
    { lat: 41.6518, lng: -0.8839, zone: "Plaza Espa\xF1a" },
    { lat: 41.6575, lng: -0.8782, zone: "Paseo Echegaray" },
    // Parque Grande / Ribera
    { lat: 41.637, lng: -0.887, zone: "Parque Grande" },
    { lat: 41.64, lng: -0.883, zone: "Cabezo Buenavista" },
    { lat: 41.635, lng: -0.878, zone: "Jard\xEDn Bot\xE1nico" },
    { lat: 41.642, lng: -0.892, zone: "Paseo Sagasta" },
    // Expo / Ribera norte
    { lat: 41.662, lng: -0.908, zone: "Expo Zaragoza" },
    { lat: 41.663, lng: -0.897, zone: "Puente del Tercer Milenio" },
    { lat: 41.66, lng: -0.885, zone: "Puente de Piedra" },
    // Delicias / Romareda
    { lat: 41.648, lng: -0.907, zone: "Estaci\xF3n Delicias" },
    { lat: 41.643, lng: -0.9, zone: "Parque Delicias" },
    { lat: 41.64, lng: -0.895, zone: "Romareda" },
    // Actur / Universidad
    { lat: 41.67, lng: -0.876, zone: "Campus R\xEDo Ebro" },
    { lat: 41.668, lng: -0.888, zone: "Actur" },
    { lat: 41.665, lng: -0.865, zone: "Parque del Agua" },
    // La Almozara / Casablanca
    { lat: 41.655, lng: -0.91, zone: "La Almozara" },
    { lat: 41.632, lng: -0.891, zone: "Casablanca" },
    // San José / Las Fuentes
    { lat: 41.652, lng: -0.865, zone: "Las Fuentes" },
    { lat: 41.649, lng: -0.87, zone: "Parque Bruil" },
    // Valdespartera / Arcosur
    { lat: 41.625, lng: -0.905, zone: "Valdespartera" },
    { lat: 41.628, lng: -0.885, zone: "Pinares de Venecia" },
    // Arrabal / Jesús
    { lat: 41.661, lng: -0.873, zone: "Arrabal" },
    { lat: 41.646, lng: -0.866, zone: "Barrio Jes\xFAs" },
    // Torrero / Canal Imperial
    { lat: 41.634, lng: -0.873, zone: "Canal Imperial" },
    { lat: 41.631, lng: -0.88, zone: "Torrero" },
    // Montecanal / Rosales
    { lat: 41.626, lng: -0.897, zone: "Montecanal" },
    { lat: 41.629, lng: -0.91, zone: "Rosales del Canal" }
  ];
  const TREASURE_POWERS = {
    shield: { name: "Escudo de Acero", rarity: "rare", description: "Tu siguiente territorio es inmune a robos durante 24h", color: "#7C8CA1", emoji: "\u{1F6E1}\uFE0F" },
    double_area: { name: "Doble Conquista", rarity: "epic", description: "Los km\xB2 de tu siguiente ruta cuentan x2", color: "#F59E0B", emoji: "\u26A1" },
    nickname: { name: "Pluma del Troll", rarity: "common", description: "Pon un apodo p\xFAblico a otro usuario durante 48h", color: "#CD7F32", emoji: "\u270F\uFE0F" },
    steal_boost: { name: "Espada Voraz", rarity: "epic", description: "Robas un 50% m\xE1s de lo normal en tu siguiente ruta", color: "#F59E0B", emoji: "\u2694\uFE0F" },
    invisibility: { name: "Capa de Sombras", rarity: "legendary", description: "Tu territorio es invisible en el mapa para otros durante 24h", color: "#8B5CF6", emoji: "\u{1F47B}" },
    time_bomb: { name: "Bomba Temporal", rarity: "rare", description: "El que te robe en las pr\xF3ximas 24h pierde el doble", color: "#7C8CA1", emoji: "\u{1F4A3}" },
    magnet: { name: "Im\xE1n de Tierras", rarity: "legendary", description: "Tu siguiente ruta absorbe un 25% extra de territorios en su radio", color: "#8B5CF6", emoji: "\u{1F9F2}" },
    reveal: { name: "Ojo del Halc\xF3n", rarity: "common", description: "Revela la ubicaci\xF3n del siguiente tesoro 1h antes", color: "#CD7F32", emoji: "\u{1F985}" },
    bulldozer: { name: "El Arrasador", rarity: "epic", description: "Tu siguiente ruta ignora TODAS las fortalezas enemigas", color: "#F59E0B", emoji: "\u{1F69C}" },
    battering_ram: { name: "Ariete de Guerra", rarity: "legendary", description: "Tu siguiente ruta tiene fuerza \xD73: cada pasada rompe 3 capas de fortaleza", color: "#8B5CF6", emoji: "\u{1FA93}" },
    wall: { name: "Muralla Imparable", rarity: "rare", description: "Durante 24h, cada carrera fortalece el doble (+1.0 en vez de +0.5)", color: "#7C8CA1", emoji: "\u{1F9F1}" },
    sentinel: { name: "Centinela", rarity: "epic", description: "Durante 24h, recibes notificaci\xF3n instant\xE1nea cuando alguien intenta robar tu territorio", color: "#F59E0B", emoji: "\u{1F514}" }
  };
  const RARITY_WEIGHTS = [
    { rarity: "common", powers: ["nickname", "reveal"], weight: 13 },
    { rarity: "rare", powers: ["shield", "time_bomb", "wall"], weight: 12 },
    { rarity: "epic", powers: ["double_area", "steal_boost", "bulldozer", "sentinel"], weight: 11 },
    { rarity: "legendary", powers: ["invisibility", "magnet", "battering_ram"], weight: 10 }
  ];
  function pickRandomTreasure() {
    const totalWeight = RARITY_WEIGHTS.reduce((sum2, r) => sum2 + r.weight, 0);
    let roll = Math.random() * totalWeight;
    for (const tier of RARITY_WEIGHTS) {
      roll -= tier.weight;
      if (roll <= 0) {
        const pow2 = tier.powers[Math.floor(Math.random() * tier.powers.length)];
        const info = TREASURE_POWERS[pow2];
        return { powerType: pow2, rarity: tier.rarity, name: info.name };
      }
    }
    return { powerType: "nickname", rarity: "common", name: "Pluma del Troll" };
  }
  __name(pickRandomTreasure, "pickRandomTreasure");
  function pickRandomSpawnPoint() {
    const base = ZARAGOZA_SPAWN_POINTS[Math.floor(Math.random() * ZARAGOZA_SPAWN_POINTS.length)];
    const jitterLat = (Math.random() - 0.5) * 2e-3;
    const jitterLng = (Math.random() - 0.5) * 2e-3;
    return {
      lat: base.lat + jitterLat,
      lng: base.lng + jitterLng,
      zone: base.zone
    };
  }
  __name(pickRandomSpawnPoint, "pickRandomSpawnPoint");
  app2.get("/api/competition/active", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const comp = await storage.getActiveCompetition();
      if (!comp) {
        return c.json({ competition: null, status: "no_competition", state: "none" });
      }
      const now = Date.now();
      const start = new Date(comp.startsAt).getTime();
      const end = new Date(comp.endsAt).getTime();
      let status = "upcoming";
      if (now >= start && now <= end) status = "active";
      else if (now > end) status = "finished";
      if (status !== comp.status) {
        await storage.updateCompetitionStatus(comp.id, status);
      }
      const totalDays = Math.ceil((end - start) / (1e3 * 60 * 60 * 24));
      const dayOfCompetition = status === "active" ? Math.min(totalDays, Math.ceil((now - start) / (1e3 * 60 * 60 * 24))) : void 0;
      const timeUntilStart = status === "upcoming" ? Math.max(0, start - now) : void 0;
      return c.json({
        competition: {
          id: comp.id,
          name: comp.name,
          slug: comp.slug,
          startsAt: comp.startsAt,
          endsAt: comp.endsAt,
          status
        },
        status,
        state: status,
        // backward compat
        timeUntilStart,
        dayOfCompetition,
        totalDays,
        treasurePowers: TREASURE_POWERS
      });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  function requireAdminAuth(c) {
    const secret = c.env.UPSTASH_CRON_SECRET;
    if (!secret) return true;
    const authHeader = c.req.header("Authorization");
    return authHeader === `Bearer ${secret}`;
  }
  __name(requireAdminAuth, "requireAdminAuth");
  app2.post("/api/admin/competition", async (c) => {
    try {
      if (!requireAdminAuth(c)) return c.json({ error: "Unauthorized" }, 401);
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const body = await c.req.json().catch(() => ({}));
      const comp = await storage.createCompetition({
        name: body.name || COMPETITION_NAME2,
        slug: body.slug || COMPETITION_SLUG2,
        startsAt: body.startsAt || COMPETITION_START2,
        endsAt: body.endsAt || COMPETITION_END2,
        config: body.config || null
      });
      return c.json(comp);
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/admin/reset-for-competition", async (c) => {
    try {
      if (!requireAdminAuth(c)) return c.json({ error: "Unauthorized" }, 401);
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const result = await storage.resetForCompetition();
      return c.json({ success: true, ...result });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  function getTodaySpawnHour(competitionId) {
    const today = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
    const seed = today + competitionId;
    let hash = 0;
    for (let i = 0; i < seed.length; i++) {
      hash = (hash << 5) - hash + seed.charCodeAt(i) | 0;
    }
    return (hash % 24 + 24) % 24;
  }
  __name(getTodaySpawnHour, "getTodaySpawnHour");
  async function wasTreasureSpawnedToday(storage, competitionId) {
    const allTreasures = await storage.getAllTreasuresForCompetition(competitionId);
    const todayStart = (/* @__PURE__ */ new Date((/* @__PURE__ */ new Date()).toISOString().slice(0, 10) + "T00:00:00Z")).getTime();
    return allTreasures.some((t) => new Date(t.spawnedAt).getTime() >= todayStart);
  }
  __name(wasTreasureSpawnedToday, "wasTreasureSpawnedToday");
  async function spawnTreasureNow(storage, comp, env, source) {
    const pick = pickRandomTreasure();
    const spot = pickRandomSpawnPoint();
    const now = /* @__PURE__ */ new Date();
    const expiresAt = new Date(now.getTime() + 24 * 60 * 60 * 1e3).toISOString();
    const rarityLabel = pick.rarity === "legendary" ? "\u{1F48E} LEGENDARIO" : pick.rarity === "epic" ? "\u2728 \xC9PICO" : pick.rarity === "rare" ? "\u{1F535} RARO" : "\u{1F7E4} COM\xDAN";
    const treasure = await storage.createTreasure({
      competitionId: comp.id,
      name: pick.name,
      powerType: pick.powerType,
      rarity: pick.rarity,
      lat: spot.lat,
      lng: spot.lng,
      spawnedAt: now.toISOString(),
      expiresAt,
      zone: spot.zone
    });
    console.log(`[TREASURE ${source}] Spawned ${pick.name} (${pick.rarity}) at ${spot.zone}`);
    try {
      const systemUserId = await storage.ensureSystemUser();
      await storage.createFeedEvent({
        userId: systemUserId,
        eventType: "treasure_spawned",
        routeId: null,
        metadata: JSON.stringify({
          treasureId: treasure.id,
          treasureName: pick.name,
          powerType: pick.powerType,
          rarity: pick.rarity,
          zone: spot.zone,
          emoji: TREASURE_POWERS[pick.powerType]?.emoji || "\u{1F4E6}"
        })
      });
    } catch (e) {
      console.error(`[TREASURE ${source}] Feed event error:`, e);
    }
    try {
      const allSubs = await storage.getAllPushSubscriptions();
      const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_pushHelper(), pushHelper_exports));
      const payload = {
        title: `\u{1F5FA}\uFE0F \xA1Nuevo tesoro en ${spot.zone}!`,
        body: `Un ${pick.name} (${rarityLabel}) ha aparecido en ${spot.zone}. \xA1Corre a por \xE9l!`,
        tag: `treasure-${treasure.id}`,
        data: { type: "treasure_spawned", treasureId: treasure.id }
      };
      const subsByUser = /* @__PURE__ */ new Map();
      for (const sub of allSubs) {
        if (!subsByUser.has(sub.userId)) subsByUser.set(sub.userId, []);
        subsByUser.get(sub.userId).push({ endpoint: sub.endpoint, keys: { p256dh: sub.p256dh, auth: sub.auth } });
      }
      let sent = 0;
      for (const [_, userSubs] of subsByUser) {
        try {
          await sendPushToUser2(userSubs, payload, env.VAPID_PUBLIC_KEY, env.VAPID_PRIVATE_KEY, "mailto:runna.io.service@gmail.com");
          sent++;
        } catch (_2) {
        }
      }
      console.log(`[TREASURE ${source}] Notified ${sent} users`);
    } catch (e) {
      console.error(`[TREASURE ${source}] Push error:`, e);
    }
    return treasure;
  }
  __name(spawnTreasureNow, "spawnTreasureNow");
  function isUpstashAuthorized(c) {
    const authHeader = c.req.header("Authorization");
    const upstashSignature = c.req.header("Upstash-Signature");
    const cronSecret = c.env.UPSTASH_CRON_SECRET;
    return !cronSecret || authHeader === `Bearer ${cronSecret}` || !!upstashSignature;
  }
  __name(isUpstashAuthorized, "isUpstashAuthorized");
  app2.get("/api/treasures/active", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const comp = await storage.getActiveCompetition();
      if (!comp || !isCompetitionActive(comp)) {
        return c.json({ treasures: [] });
      }
      let active = await storage.getActiveTreasures(comp.id);
      const forceSpawn = c.req.query("force") === "true";
      const spawnHour = getTodaySpawnHour(comp.id);
      const currentHour = (/* @__PURE__ */ new Date()).getUTCHours();
      if (forceSpawn || currentHour >= spawnHour) {
        const alreadySpawned = await wasTreasureSpawnedToday(storage, comp.id);
        if (!alreadySpawned) {
          try {
            await spawnTreasureNow(storage, comp, c.env, "AUTO-SPAWN");
            active = await storage.getActiveTreasures(comp.id);
          } catch (spawnErr) {
            console.error("[TREASURE AUTO-SPAWN] Error:", spawnErr);
          }
        }
      }
      return c.json({
        treasures: active.map((t) => ({
          id: t.id,
          name: t.name,
          powerType: t.powerType,
          rarity: t.rarity,
          lat: t.lat,
          lng: t.lng,
          spawnedAt: t.spawnedAt,
          expiresAt: t.expiresAt,
          power: TREASURE_POWERS[t.powerType] || null
        }))
      });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/treasures/renotify", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const comp = await storage.getActiveCompetition();
      if (!comp) return c.json({ error: "No active competition" }, 404);
      const active = await storage.getActiveTreasures(comp.id);
      if (active.length === 0) return c.json({ error: "No active treasures" }, 404);
      const allSubs = await storage.getAllPushSubscriptions();
      if (allSubs.length === 0) return c.json({ error: "No subscriptions" }, 404);
      const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_pushHelper(), pushHelper_exports));
      const subsByUser = /* @__PURE__ */ new Map();
      for (const sub of allSubs) {
        if (!subsByUser.has(sub.userId)) subsByUser.set(sub.userId, []);
        subsByUser.get(sub.userId).push({ endpoint: sub.endpoint, keys: { p256dh: sub.p256dh, auth: sub.auth } });
      }
      let totalSent = 0;
      for (const t of active) {
        const power = TREASURE_POWERS[t.powerType];
        const payload = {
          title: `\u{1F5FA}\uFE0F \xA1Tesoro disponible!`,
          body: `${power?.name || t.name} (${t.rarity === "legendary" ? "\u{1F48E} LEGENDARIO" : t.rarity === "epic" ? "\u2728 \xC9PICO" : t.rarity === "rare" ? "\u{1F535} RARO" : "\u{1F7E4} COM\xDAN"}) est\xE1 en el mapa. \xA1Corre a por \xE9l!`,
          tag: `treasure-renotify-${t.id}`,
          data: { type: "treasure_spawned", treasureId: t.id }
        };
        for (const [_, userSubs] of subsByUser) {
          try {
            await sendPushToUser2(userSubs, payload, c.env.VAPID_PUBLIC_KEY || "", c.env.VAPID_PRIVATE_KEY || "", "mailto:runna.io.service@gmail.com");
            totalSent++;
          } catch (_2) {
          }
        }
      }
      return c.json({ success: true, sent: totalSent, treasures: active.length });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/push/debug/:username", async (c) => {
    try {
      const username = c.req.param("username");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const user = await storage.getUserByUsername(username);
      if (!user) return c.json({ error: `User '${username}' not found` }, 404);
      const subs = await storage.getPushSubscriptionsByUserId(user.id);
      const allSubs = await storage.getAllPushSubscriptions();
      const subsByUser = /* @__PURE__ */ new Map();
      for (const sub of allSubs) {
        subsByUser.set(sub.userId, (subsByUser.get(sub.userId) || 0) + 1);
      }
      return c.json({
        userId: user.id,
        username: user.username,
        name: user.name,
        subscriptionCount: subs.length,
        subscriptions: subs.map((s) => ({ endpoint: s.endpoint?.slice(0, 80) + "...", createdAt: s.createdAt })),
        totalUsersWithSubs: subsByUser.size,
        allSubCounts: Object.fromEntries(subsByUser)
      });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/treasures/collect", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const body = await c.req.json();
      const { userId, treasureId, lat, lng } = body;
      if (!userId || !treasureId) return c.json({ error: "Missing userId or treasureId" }, 400);
      const comp = await storage.getActiveCompetition();
      if (!comp || !isCompetitionActive(comp)) {
        return c.json({ error: "No active competition" }, 400);
      }
      const treasure = await storage.getTreasureById(treasureId);
      if (!treasure || treasure.collectedBy || !treasure.active) {
        return c.json({ error: "Treasure not available" }, 404);
      }
      if (lat && lng) {
        const dist = haversineDistance(lat, lng, treasure.lat, treasure.lng);
        if (dist > 100) {
          return c.json({ error: "Too far from treasure", distance: dist }, 400);
        }
      }
      const collected = await storage.collectTreasure(treasureId, userId);
      if (!collected || collected.collectedBy !== userId) {
        return c.json({ error: "Already collected by someone else" }, 409);
      }
      const powerDef = TREASURE_POWERS[treasure.powerType];
      await storage.createUserPower({
        userId,
        competitionId: comp.id,
        powerType: treasure.powerType,
        treasureId: treasure.id,
        expiresAt: null,
        metadata: null
      });
      await storage.incrementCompetitionStats(comp.id, userId, { treasures: 1 });
      await storage.createFeedEvent({
        userId,
        eventType: "treasure_found",
        routeId: null,
        metadata: JSON.stringify({
          treasureName: treasure.name,
          powerType: treasure.powerType,
          rarity: treasure.rarity,
          zone: treasure.zone || null,
          emoji: powerDef?.emoji || "\u{1F4E6}"
        })
      });
      return c.json({
        success: true,
        treasure: {
          ...treasure,
          collectedBy: userId,
          collectedAt: (/* @__PURE__ */ new Date()).toISOString()
        },
        power: {
          type: treasure.powerType,
          name: powerDef?.name || treasure.name,
          rarity: treasure.rarity,
          description: powerDef?.description || "",
          emoji: powerDef?.emoji || "\u{1F4E6}"
        }
      });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/users/:userId/powers", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const comp = await storage.getActiveCompetition();
      if (!comp || !isCompetitionActive(comp)) {
        return c.json({ powers: [] });
      }
      const powers = await storage.getUserPowers(userId, comp.id);
      return c.json({
        powers: powers.map((p) => ({
          ...p,
          definition: TREASURE_POWERS[p.powerType] || null
        }))
      });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/powers/:powerId/activate", async (c) => {
    try {
      const powerId = c.req.param("powerId");
      const body = await c.req.json().catch(() => ({}));
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const power = await storage.getPowerById(powerId);
      if (!power) return c.json({ error: "Power not found" }, 404);
      if (power.status !== "available") return c.json({ error: "Power already used or expired" }, 400);
      const now = (/* @__PURE__ */ new Date()).toISOString();
      const in24h = new Date(Date.now() + 24 * 60 * 60 * 1e3).toISOString();
      const in48h = new Date(Date.now() + 48 * 60 * 60 * 1e3).toISOString();
      switch (power.powerType) {
        case "shield":
        case "time_bomb":
        case "invisibility": {
          await storage.activatePower(powerId);
          await storage.setPowerExpiration(powerId, in24h);
          return c.json({ success: true, message: `Activado durante 24h`, expiresAt: in24h });
        }
        case "double_area":
        case "steal_boost":
        case "magnet":
        case "bulldozer":
        case "battering_ram": {
          await storage.activatePower(powerId);
          return c.json({ success: true, message: "Se aplicar\xE1 en tu siguiente ruta" });
        }
        case "wall":
        case "sentinel": {
          await storage.activatePower(powerId);
          await storage.setPowerExpiration(powerId, in24h);
          return c.json({ success: true, message: `Activado durante 24h`, expiresAt: in24h });
        }
        case "nickname": {
          const { targetUserId, nickname } = body;
          if (!targetUserId || !nickname) return c.json({ error: "Missing targetUserId or nickname" }, 400);
          if (nickname.length > 20) return c.json({ error: "Nickname too long (max 20 chars)" }, 400);
          await storage.createNickname({
            targetUserId,
            setByUserId: power.userId,
            nickname,
            expiresAt: in48h
          });
          await storage.usePower(powerId);
          const attacker = await storage.getUser(power.userId);
          const target = await storage.getUser(targetUserId);
          const attackerName = attacker?.name || "Alguien";
          const targetName = target?.name || "un jugador";
          try {
            await storage.createFeedEvent({
              userId: power.userId,
              eventType: "nickname_changed",
              victimId: targetUserId,
              metadata: JSON.stringify({
                nickname,
                targetName,
                attackerName,
                expiresAt: in48h
              })
            });
          } catch (e) {
            console.error("[NICKNAME] Feed event error:", e);
          }
          const env = c.env;
          c.executionCtx.waitUntil?.((async () => {
            try {
              const allSubs = await storage.getAllPushSubscriptions();
              const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_pushHelper(), pushHelper_exports));
              const payload = {
                title: `\u{1F3AD} \xA1Nuevo apodo en la competici\xF3n!`,
                body: `${attackerName} ha puesto el apodo "${nickname}" a ${targetName} durante 48h`,
                tag: `nickname-${targetUserId}`,
                data: { type: "nickname_changed", targetUserId }
              };
              const subsByUser = /* @__PURE__ */ new Map();
              for (const sub of allSubs) {
                if (!subsByUser.has(sub.userId)) subsByUser.set(sub.userId, []);
                subsByUser.get(sub.userId).push({ endpoint: sub.endpoint, keys: { p256dh: sub.p256dh, auth: sub.auth } });
              }
              let sent = 0;
              for (const [_, userSubs] of subsByUser) {
                try {
                  await sendPushToUser2(userSubs, payload, env.VAPID_PUBLIC_KEY, env.VAPID_PRIVATE_KEY, "mailto:runna.io.service@gmail.com");
                  sent++;
                } catch (_2) {
                }
              }
              console.log(`[NICKNAME] Notified ${sent} users about nickname "${nickname}" for ${targetName}`);
            } catch (e) {
              console.error("[NICKNAME] Push error:", e);
            }
          })());
          return c.json({ success: true, message: `Apodo "${nickname}" asignado durante 48h` });
        }
        case "reveal": {
          await storage.usePower(powerId);
          return c.json({ success: true, message: "Recibir\xE1s la ubicaci\xF3n del pr\xF3ximo tesoro 1h antes" });
        }
        default:
          return c.json({ error: "Unknown power type" }, 400);
      }
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/territories/fortifications", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const comp = await storage.getActiveCompetition();
      if (!comp || !isCompetitionActive(comp)) {
        return c.json({ fortifications: [] });
      }
      const allForts = await storage.getAllFortifications();
      const byUser = /* @__PURE__ */ new Map();
      for (const fort of allForts) {
        if (!byUser.has(fort.userId)) byUser.set(fort.userId, []);
        byUser.get(fort.userId).push({ geometry: fort.geometry, area: fort.area });
      }
      const fortifications = [];
      for (const [uid, records] of byUser) {
        fortifications.push({
          userId: uid,
          layers: records.length,
          records: records.map((r) => ({
            geometry: JSON.parse(r.geometry),
            area: r.area
          }))
        });
      }
      return c.json({ fortifications });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/competition/participants", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const allUsers = await storage.getAllUsersWithStats();
      return c.json({
        participants: allUsers.map((u) => ({
          id: u.id,
          username: u.username,
          name: u.name,
          color: u.color,
          avatar: u.avatar
        }))
      });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/competition/leaderboard", async (c) => {
    try {
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const comp = await storage.getActiveCompetition();
      if (!comp) return c.json({ leaderboard: [] });
      const leaderboard = await storage.getCompetitionLeaderboard(comp.id);
      const enriched = await Promise.all(leaderboard.map(async (entry) => {
        const nn = await storage.getActiveNickname(entry.user.id);
        return { ...entry, user: { ...entry.user, nickname: nn?.nickname || null, nicknameExpiresAt: nn?.expiresAt || null } };
      }));
      return c.json({ leaderboard: enriched });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/competition/weekly-summary/:weekNumber", async (c) => {
    try {
      const weekNumber = parseInt(c.req.param("weekNumber"));
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const comp = await storage.getActiveCompetition();
      if (!comp) return c.json({ summary: null });
      const summary = await storage.getWeeklySummary(comp.id, weekNumber);
      return c.json({ summary: summary ? JSON.parse(summary.data) : null, weekNumber });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/tasks/spawn-treasure", async (c) => {
    try {
      if (!isUpstashAuthorized(c)) {
        console.log("[TREASURE CRON] Auth failed. Headers:", JSON.stringify(Object.fromEntries([...new Map(c.req.raw.headers)])).slice(0, 200));
        return c.json({ error: "Unauthorized" }, 401);
      }
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const comp = await storage.getActiveCompetition();
      if (!comp || !isCompetitionActive(comp)) {
        return c.json({ skipped: true, reason: "No active competition" });
      }
      const spawnHour = getTodaySpawnHour(comp.id);
      const currentHour = (/* @__PURE__ */ new Date()).getUTCHours();
      if (currentHour !== spawnHour) {
        return c.json({ skipped: true, reason: `Not spawn hour (today=${spawnHour}h UTC, now=${currentHour}h UTC)` });
      }
      const alreadySpawned = await wasTreasureSpawnedToday(storage, comp.id);
      if (alreadySpawned) {
        return c.json({ skipped: true, reason: "Treasure already spawned today" });
      }
      const treasure = await spawnTreasureNow(storage, comp, c.env, "CRON");
      return c.json({ success: true, treasure });
    } catch (error) {
      console.error("[TREASURE CRON] Error:", error);
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/tasks/weekly-summary", async (c) => {
    try {
      if (!isUpstashAuthorized(c)) {
        return c.json({ error: "Unauthorized" }, 401);
      }
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const comp = await storage.getActiveCompetition();
      if (!comp || !isCompetitionActive(comp)) {
        return c.json({ skipped: true, reason: "No active competition" });
      }
      const startDate = new Date(comp.startsAt);
      const now = /* @__PURE__ */ new Date();
      const weekNumber = Math.ceil((now.getTime() - startDate.getTime()) / (7 * 24 * 60 * 60 * 1e3));
      const leaderboard = await storage.getCompetitionLeaderboard(comp.id);
      if (leaderboard.length === 0) {
        return c.json({ skipped: true, reason: "No participants yet" });
      }
      const byArea = [...leaderboard].sort((a, b) => b.totalArea - a.totalArea);
      const byStolen = [...leaderboard].sort((a, b) => b.areaStolen - a.areaStolen);
      const byDistance = [...leaderboard].sort((a, b) => b.totalDistance - a.totalDistance);
      const byActivities = [...leaderboard].sort((a, b) => b.activitiesCount - a.activitiesCount);
      const byTreasures = [...leaderboard].sort((a, b) => b.treasuresCollected - a.treasuresCollected);
      const byVictims = [...leaderboard].sort((a, b) => b.uniqueVictims - a.uniqueVictims);
      const byRanTogether = [...leaderboard].sort((a, b) => b.ranTogetherCount - a.ranTogetherCount);
      const maxArea = byArea[0]?.totalArea || 1;
      const maxStolen = byStolen[0]?.areaStolen || 1;
      const maxDist = byDistance[0]?.totalDistance || 1;
      const maxAct = byActivities[0]?.activitiesCount || 1;
      const mvpScores = leaderboard.map((s) => ({
        ...s,
        mvpScore: s.totalArea / maxArea * 0.3 + s.areaStolen / maxStolen * 0.25 + s.totalDistance / maxDist * 0.2 + s.activitiesCount / maxAct * 0.25
      })).sort((a, b) => b.mvpScore - a.mvpScore);
      const summaryData = {
        weekNumber,
        generatedAt: now.toISOString(),
        competitionName: comp.name,
        participantCount: leaderboard.length,
        awards: {
          territory_king: { title: "\u{1F3C6} Rey del Territorio", user: byArea[0]?.user, value: byArea[0]?.totalArea || 0, unit: "m\xB2" },
          conqueror: { title: "\u{1F5E1}\uFE0F El Conquistador", user: byStolen[0]?.user, value: byStolen[0]?.areaStolen || 0, unit: "m\xB2 robados" },
          marathon: { title: "\u{1F3C3} Maratonista", user: byDistance[0]?.user, value: byDistance[0]?.totalDistance || 0, unit: "m" },
          consistent: { title: "\u{1F4CA} El Constante", user: byActivities[0]?.user, value: byActivities[0]?.activitiesCount || 0, unit: "actividades" },
          treasure_hunter: { title: "\u{1F48E} Cazatesoros", user: byTreasures[0]?.user, value: byTreasures[0]?.treasuresCollected || 0, unit: "tesoros" },
          precise: { title: "\u{1F3AF} El Preciso", user: byVictims[0]?.user, value: byVictims[0]?.uniqueVictims || 0, unit: "v\xEDctimas" },
          social: { title: "\u{1F91D} Alma Social", user: byRanTogether[0]?.user, value: byRanTogether[0]?.ranTogetherCount || 0, unit: "carreras juntos" },
          mvp: { title: "\u26A1 MVP de la Semana", user: mvpScores[0]?.user, value: Math.round(mvpScores[0]?.mvpScore * 100) || 0, unit: "puntos" }
        },
        ranking: byArea.slice(0, 10).map((s, i) => ({
          rank: i + 1,
          user: s.user,
          totalArea: s.totalArea,
          activitiesCount: s.activitiesCount,
          areaStolen: s.areaStolen
        }))
      };
      await storage.createWeeklySummary({
        competitionId: comp.id,
        weekNumber,
        data: JSON.stringify(summaryData)
      });
      try {
        const allSubs = await storage.getAllPushSubscriptions();
        const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_pushHelper(), pushHelper_exports));
        const payload = {
          title: `\u{1F4CA} Resumen Semanal \u2014 Semana ${weekNumber}`,
          body: `\xA1El resumen de La Primera Conquista del Ebro est\xE1 listo! Abre la app para ver los premios.`,
          tag: `weekly-summary-${weekNumber}`,
          data: { type: "weekly_summary", weekNumber }
        };
        const subsByUser = /* @__PURE__ */ new Map();
        for (const sub of allSubs) {
          if (!subsByUser.has(sub.userId)) subsByUser.set(sub.userId, []);
          subsByUser.get(sub.userId).push({ endpoint: sub.endpoint, keys: { p256dh: sub.p256dh, auth: sub.auth } });
        }
        for (const [_, userSubs] of subsByUser) {
          try {
            await sendPushToUser2(userSubs, payload, c.env.VAPID_PUBLIC_KEY, c.env.VAPID_PRIVATE_KEY, "mailto:runna.io.service@gmail.com");
          } catch (_2) {
          }
        }
      } catch (_) {
      }
      return c.json({ success: true, weekNumber, summary: summaryData });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/admin/spawn-treasure", async (c) => {
    try {
      if (!requireAdminAuth(c)) return c.json({ error: "Unauthorized" }, 401);
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const body = await c.req.json().catch(() => ({}));
      const comp = await storage.getActiveCompetition();
      if (!comp) return c.json({ error: "No competition" }, 400);
      const pick = body.powerType ? { powerType: body.powerType, rarity: TREASURE_POWERS[body.powerType]?.rarity || "common", name: TREASURE_POWERS[body.powerType]?.name || body.powerType } : pickRandomTreasure();
      const spot = body.lat && body.lng ? { lat: body.lat, lng: body.lng, zone: body.zone || "Manual" } : pickRandomSpawnPoint();
      const now = /* @__PURE__ */ new Date();
      const expiresAt = new Date(now.getTime() + (body.durationHours || 24) * 60 * 60 * 1e3).toISOString();
      const treasure = await storage.createTreasure({
        competitionId: comp.id,
        name: pick.name,
        powerType: pick.powerType,
        rarity: pick.rarity,
        lat: spot.lat,
        lng: spot.lng,
        spawnedAt: now.toISOString(),
        expiresAt,
        zone: spot.zone
      });
      try {
        const systemUserId = await storage.ensureSystemUser();
        await storage.createFeedEvent({
          userId: systemUserId,
          eventType: "treasure_spawned",
          routeId: null,
          metadata: JSON.stringify({
            treasureId: treasure.id,
            treasureName: pick.name,
            powerType: pick.powerType,
            rarity: pick.rarity,
            zone: spot.zone,
            emoji: TREASURE_POWERS[pick.powerType]?.emoji || "\u{1F4E6}"
          })
        });
      } catch (_) {
      }
      try {
        const allSubs = await storage.getAllPushSubscriptions();
        const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_pushHelper(), pushHelper_exports));
        const payload = {
          title: `\u{1F5FA}\uFE0F \xA1Nuevo tesoro en ${spot.zone}!`,
          body: `Un ${pick.name} (${pick.rarity === "legendary" ? "\u{1F48E} LEGENDARIO" : pick.rarity === "epic" ? "\u2728 \xC9PICO" : pick.rarity === "rare" ? "\u{1F535} RARO" : "\u{1F7E4} COM\xDAN"}) ha aparecido. \xA1Corre a por \xE9l!`,
          tag: `treasure-${treasure.id}`,
          data: { type: "treasure_spawned", treasureId: treasure.id }
        };
        const subsByUser = /* @__PURE__ */ new Map();
        for (const sub of allSubs) {
          if (!subsByUser.has(sub.userId)) subsByUser.set(sub.userId, []);
          subsByUser.get(sub.userId).push({ endpoint: sub.endpoint, keys: { p256dh: sub.p256dh, auth: sub.auth } });
        }
        let sent = 0;
        for (const [_, userSubs] of subsByUser) {
          try {
            await sendPushToUser2(userSubs, payload, c.env.VAPID_PUBLIC_KEY, c.env.VAPID_PRIVATE_KEY, "mailto:runna.io.service@gmail.com");
            sent++;
          } catch (_2) {
          }
        }
        console.log(`[TREASURE-ADMIN] Spawned ${pick.name} at ${spot.zone}, notified ${sent} users`);
      } catch (e) {
        console.error("[TREASURE-ADMIN] Push error:", e);
      }
      return c.json({ success: true, treasure, zone: spot.zone });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/admin/reset-skipped-polar", async (c) => {
    try {
      if (!requireAdminAuth(c)) return c.json({ error: "Unauthorized" }, 401);
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const body = await c.req.json().catch(() => ({}));
      const targetUserId = body?.userId || null;
      const deleted = await storage.deleteSkippedPolarActivities(targetUserId);
      console.log(`[ADMIN] Reset ${deleted} skipped Polar activities${targetUserId ? ` for user ${targetUserId}` : " (all users)"}`);
      return c.json({ success: true, deleted, message: `${deleted} actividades skipped eliminadas. Ejecuta sync de nuevo para reimportarlas.` });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/admin/retrofix-route", async (c) => {
    try {
      if (!requireAdminAuth(c)) return c.json({ error: "Unauthorized" }, 401);
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const body = await c.req.json();
      const { routeId } = body;
      if (!routeId) return c.json({ error: "routeId required" }, 400);
      const route = await storage.getRouteById(routeId);
      if (!route) return c.json({ error: "Route not found" }, 404);
      const coords = typeof route.coordinates === "string" ? JSON.parse(route.coordinates) : route.coordinates;
      const existingFeed = await storage.getFeedEventByRouteId(route.id);
      let feedCreated = false;
      if (!existingFeed) {
        try {
          await generateFeedEvents(storage, route.userId, route.id, route.distance || 0, route.duration || 0, { newAreaConquered: 0, victims: [], ranTogetherWith: [] });
          feedCreated = true;
          console.log(`[ADMIN RETROFIX] Feed event created for route ${routeId}`);
        } catch (feedErr) {
          console.error(`[ADMIN RETROFIX] Feed event creation failed:`, feedErr);
        }
      } else {
        console.log(`[ADMIN RETROFIX] Feed event already exists for route ${routeId}`);
      }
      let treasuresCollected = [];
      if (coords && coords.length > 0) {
        try {
          const competition = await storage.getActiveCompetition();
          if (competition && isCompetitionActive(competition)) {
            treasuresCollected = await autoCollectTreasuresAlongRoute(storage, route.userId, competition.id, coords);
            console.log(`[ADMIN RETROFIX] Treasures collected: ${treasuresCollected.length}`);
          }
        } catch (tErr) {
          console.error(`[ADMIN RETROFIX] Treasure collection failed:`, tErr);
        }
      }
      return c.json({
        success: true,
        routeId,
        userId: route.userId,
        feedCreated,
        feedAlreadyExisted: !!existingFeed,
        treasuresCollected: treasuresCollected.length,
        treasures: treasuresCollected
      });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.post("/api/admin/broadcast", async (c) => {
    try {
      if (!requireAdminAuth(c)) return c.json({ error: "Unauthorized" }, 401);
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const body = await c.req.json();
      const { title, body: msgBody, tag, data } = body;
      if (!title || !msgBody) return c.json({ error: "title and body required" }, 400);
      const allSubs = await storage.getAllPushSubscriptions();
      if (allSubs.length === 0) return c.json({ error: "No subscriptions", sent: 0 }, 404);
      const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_pushHelper(), pushHelper_exports));
      const payload = { title, body: msgBody, tag: tag || "broadcast", data: data || {} };
      const subsByUser = /* @__PURE__ */ new Map();
      for (const sub of allSubs) {
        if (!subsByUser.has(sub.userId)) subsByUser.set(sub.userId, []);
        subsByUser.get(sub.userId).push({
          endpoint: sub.endpoint,
          keys: { p256dh: sub.p256dh, auth: sub.auth }
        });
      }
      let sent = 0;
      let failed = 0;
      for (const [_, userSubs] of subsByUser) {
        try {
          await sendPushToUser2(userSubs, payload, c.env.VAPID_PUBLIC_KEY || "", c.env.VAPID_PRIVATE_KEY || "", "mailto:runna.io.service@gmail.com");
          sent++;
        } catch (_2) {
          failed++;
        }
      }
      return c.json({ success: true, sent, failed, totalUsers: subsByUser.size, totalSubs: allSubs.length });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  app2.get("/api/users/:userId/nickname", async (c) => {
    try {
      const userId = c.req.param("userId");
      const db = getDb(c.env);
      const storage = new WorkerStorage(db);
      const nickname = await storage.getActiveNickname(userId);
      return c.json({ nickname: nickname?.nickname || null, expiresAt: nickname?.expiresAt || null });
    } catch (error) {
      return c.json({ error: error.message }, 500);
    }
  });
  function haversineDistance(lat1, lon1, lat2, lon2) {
    const R = 6371e3;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLon = (lon2 - lon1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) + Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) * Math.sin(dLon / 2) * Math.sin(dLon / 2);
    return R * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
  }
  __name(haversineDistance, "haversineDistance");
}
function parseGpxToCoordinates(gpxText) {
  const coordinates = [];
  const trkptRegex = /<trkpt[^>]*lat="([^"]+)"[^>]*lon="([^"]+)"/g;
  let match2;
  while ((match2 = trkptRegex.exec(gpxText)) !== null) {
    const lat = parseFloat(match2[1]);
    const lon = parseFloat(match2[2]);
    if (!isNaN(lat) && !isNaN(lon)) {
      coordinates.push([lat, lon]);
    }
  }
  return coordinates;
}
function parseDuration(duration) {
  if (!duration) return 0;
  const extMatch = duration.match(/P(?:(\d+)D)?T(?:(\d+)H)?(?:(\d+)M)?(?:(\d+(?:\.\d+)?)S)?/);
  if (extMatch) {
    const days = parseInt(extMatch[1] || "0", 10);
    const hours2 = parseInt(extMatch[2] || "0", 10);
    const minutes2 = parseInt(extMatch[3] || "0", 10);
    const seconds2 = parseFloat(extMatch[4] || "0");
    return days * 86400 + hours2 * 3600 + minutes2 * 60 + Math.round(seconds2);
  }
  const match2 = duration.match(/PT(?:(\d+)H)?(?:(\d+)M)?(?:(\d+(?:\.\d+)?)S)?/);
  if (!match2) {
    console.warn(`[PARSE] Unrecognized duration format: "${duration}"`);
    return 0;
  }
  const hours = parseInt(match2[1] || "0", 10);
  const minutes = parseInt(match2[2] || "0", 10);
  const seconds = parseFloat(match2[3] || "0");
  return hours * 3600 + minutes * 60 + Math.round(seconds);
}
var init_routes = __esm({
  "worker/src/routes.ts"() {
    "use strict";
    init_db3();
    init_storage();
    init_schema();
    init_esm23();
    init_email();
    init_colors();
    init_territory();
    __name(getDb, "getDb");
    __name(getFriendGroupColors, "getFriendGroupColors");
    __name(findAvailableColor, "findAvailableColor");
    __name(resolveColorConflictOnFriendship, "resolveColorConflictOnFriendship");
    __name(hashPassword, "hashPassword");
    __name(verifyPassword, "verifyPassword");
    __name(generateFeedEvents, "generateFeedEvents");
    __name(autoCollectTreasuresAlongRoute, "autoCollectTreasuresAlongRoute");
    __name(processTerritoryConquest, "processTerritoryConquest");
    __name(safeUnion2, "safeUnion");
    __name(reprocessUserTerritories, "reprocessUserTerritories");
    __name(reprocessFriendGroupTerritoriesChronologically, "reprocessFriendGroupTerritoriesChronologically");
    __name(coerceDate, "coerceDate");
    __name(getValidStravaToken, "getValidStravaToken");
    __name(registerRoutes, "registerRoutes");
    __name(parseGpxToCoordinates, "parseGpxToCoordinates");
    __name(parseDuration, "parseDuration");
  }
});

// worker/node_modules/hono/dist/compose.js
var compose = /* @__PURE__ */ __name((middleware, onError, onNotFound) => {
  return (context, next) => {
    let index = -1;
    return dispatch(0);
    async function dispatch(i) {
      if (i <= index) {
        throw new Error("next() called multiple times");
      }
      index = i;
      let res;
      let isError = false;
      let handler;
      if (middleware[i]) {
        handler = middleware[i][0][0];
        context.req.routeIndex = i;
      } else {
        handler = i === middleware.length && next || void 0;
      }
      if (handler) {
        try {
          res = await handler(context, () => dispatch(i + 1));
        } catch (err) {
          if (err instanceof Error && onError) {
            context.error = err;
            res = await onError(err, context);
            isError = true;
          } else {
            throw err;
          }
        }
      } else {
        if (context.finalized === false && onNotFound) {
          res = await onNotFound(context);
        }
      }
      if (res && (context.finalized === false || isError)) {
        context.res = res;
      }
      return context;
    }
    __name(dispatch, "dispatch");
  };
}, "compose");

// worker/node_modules/hono/dist/request/constants.js
var GET_MATCH_RESULT = /* @__PURE__ */ Symbol();

// worker/node_modules/hono/dist/utils/body.js
var parseBody = /* @__PURE__ */ __name(async (request, options = /* @__PURE__ */ Object.create(null)) => {
  const { all = false, dot = false } = options;
  const headers = request instanceof HonoRequest ? request.raw.headers : request.headers;
  const contentType = headers.get("Content-Type");
  if (contentType?.startsWith("multipart/form-data") || contentType?.startsWith("application/x-www-form-urlencoded")) {
    return parseFormData(request, { all, dot });
  }
  return {};
}, "parseBody");
async function parseFormData(request, options) {
  const formData = await request.formData();
  if (formData) {
    return convertFormDataToBodyData(formData, options);
  }
  return {};
}
__name(parseFormData, "parseFormData");
function convertFormDataToBodyData(formData, options) {
  const form = /* @__PURE__ */ Object.create(null);
  formData.forEach((value, key) => {
    const shouldParseAllValues = options.all || key.endsWith("[]");
    if (!shouldParseAllValues) {
      form[key] = value;
    } else {
      handleParsingAllValues(form, key, value);
    }
  });
  if (options.dot) {
    Object.entries(form).forEach(([key, value]) => {
      const shouldParseDotValues = key.includes(".");
      if (shouldParseDotValues) {
        handleParsingNestedValues(form, key, value);
        delete form[key];
      }
    });
  }
  return form;
}
__name(convertFormDataToBodyData, "convertFormDataToBodyData");
var handleParsingAllValues = /* @__PURE__ */ __name((form, key, value) => {
  if (form[key] !== void 0) {
    if (Array.isArray(form[key])) {
      ;
      form[key].push(value);
    } else {
      form[key] = [form[key], value];
    }
  } else {
    if (!key.endsWith("[]")) {
      form[key] = value;
    } else {
      form[key] = [value];
    }
  }
}, "handleParsingAllValues");
var handleParsingNestedValues = /* @__PURE__ */ __name((form, key, value) => {
  let nestedForm = form;
  const keys = key.split(".");
  keys.forEach((key2, index) => {
    if (index === keys.length - 1) {
      nestedForm[key2] = value;
    } else {
      if (!nestedForm[key2] || typeof nestedForm[key2] !== "object" || Array.isArray(nestedForm[key2]) || nestedForm[key2] instanceof File) {
        nestedForm[key2] = /* @__PURE__ */ Object.create(null);
      }
      nestedForm = nestedForm[key2];
    }
  });
}, "handleParsingNestedValues");

// worker/node_modules/hono/dist/utils/url.js
var splitPath = /* @__PURE__ */ __name((path) => {
  const paths = path.split("/");
  if (paths[0] === "") {
    paths.shift();
  }
  return paths;
}, "splitPath");
var splitRoutingPath = /* @__PURE__ */ __name((routePath) => {
  const { groups, path } = extractGroupsFromPath(routePath);
  const paths = splitPath(path);
  return replaceGroupMarks(paths, groups);
}, "splitRoutingPath");
var extractGroupsFromPath = /* @__PURE__ */ __name((path) => {
  const groups = [];
  path = path.replace(/\{[^}]+\}/g, (match2, index) => {
    const mark = `@${index}`;
    groups.push([mark, match2]);
    return mark;
  });
  return { groups, path };
}, "extractGroupsFromPath");
var replaceGroupMarks = /* @__PURE__ */ __name((paths, groups) => {
  for (let i = groups.length - 1; i >= 0; i--) {
    const [mark] = groups[i];
    for (let j = paths.length - 1; j >= 0; j--) {
      if (paths[j].includes(mark)) {
        paths[j] = paths[j].replace(mark, groups[i][1]);
        break;
      }
    }
  }
  return paths;
}, "replaceGroupMarks");
var patternCache = {};
var getPattern = /* @__PURE__ */ __name((label, next) => {
  if (label === "*") {
    return "*";
  }
  const match2 = label.match(/^\:([^\{\}]+)(?:\{(.+)\})?$/);
  if (match2) {
    const cacheKey = `${label}#${next}`;
    if (!patternCache[cacheKey]) {
      if (match2[2]) {
        patternCache[cacheKey] = next && next[0] !== ":" && next[0] !== "*" ? [cacheKey, match2[1], new RegExp(`^${match2[2]}(?=/${next})`)] : [label, match2[1], new RegExp(`^${match2[2]}$`)];
      } else {
        patternCache[cacheKey] = [label, match2[1], true];
      }
    }
    return patternCache[cacheKey];
  }
  return null;
}, "getPattern");
var tryDecode = /* @__PURE__ */ __name((str, decoder) => {
  try {
    return decoder(str);
  } catch {
    return str.replace(/(?:%[0-9A-Fa-f]{2})+/g, (match2) => {
      try {
        return decoder(match2);
      } catch {
        return match2;
      }
    });
  }
}, "tryDecode");
var tryDecodeURI = /* @__PURE__ */ __name((str) => tryDecode(str, decodeURI), "tryDecodeURI");
var getPath = /* @__PURE__ */ __name((request) => {
  const url = request.url;
  const start = url.indexOf("/", url.indexOf(":") + 4);
  let i = start;
  for (; i < url.length; i++) {
    const charCode = url.charCodeAt(i);
    if (charCode === 37) {
      const queryIndex = url.indexOf("?", i);
      const path = url.slice(start, queryIndex === -1 ? void 0 : queryIndex);
      return tryDecodeURI(path.includes("%25") ? path.replace(/%25/g, "%2525") : path);
    } else if (charCode === 63) {
      break;
    }
  }
  return url.slice(start, i);
}, "getPath");
var getPathNoStrict = /* @__PURE__ */ __name((request) => {
  const result = getPath(request);
  return result.length > 1 && result.at(-1) === "/" ? result.slice(0, -1) : result;
}, "getPathNoStrict");
var mergePath = /* @__PURE__ */ __name((base, sub, ...rest) => {
  if (rest.length) {
    sub = mergePath(sub, ...rest);
  }
  return `${base?.[0] === "/" ? "" : "/"}${base}${sub === "/" ? "" : `${base?.at(-1) === "/" ? "" : "/"}${sub?.[0] === "/" ? sub.slice(1) : sub}`}`;
}, "mergePath");
var checkOptionalParameter = /* @__PURE__ */ __name((path) => {
  if (path.charCodeAt(path.length - 1) !== 63 || !path.includes(":")) {
    return null;
  }
  const segments = path.split("/");
  const results = [];
  let basePath = "";
  segments.forEach((segment) => {
    if (segment !== "" && !/\:/.test(segment)) {
      basePath += "/" + segment;
    } else if (/\:/.test(segment)) {
      if (/\?/.test(segment)) {
        if (results.length === 0 && basePath === "") {
          results.push("/");
        } else {
          results.push(basePath);
        }
        const optionalSegment = segment.replace("?", "");
        basePath += "/" + optionalSegment;
        results.push(basePath);
      } else {
        basePath += "/" + segment;
      }
    }
  });
  return results.filter((v, i, a) => a.indexOf(v) === i);
}, "checkOptionalParameter");
var _decodeURI = /* @__PURE__ */ __name((value) => {
  if (!/[%+]/.test(value)) {
    return value;
  }
  if (value.indexOf("+") !== -1) {
    value = value.replace(/\+/g, " ");
  }
  return value.indexOf("%") !== -1 ? tryDecode(value, decodeURIComponent_) : value;
}, "_decodeURI");
var _getQueryParam = /* @__PURE__ */ __name((url, key, multiple) => {
  let encoded;
  if (!multiple && key && !/[%+]/.test(key)) {
    let keyIndex2 = url.indexOf("?", 8);
    if (keyIndex2 === -1) {
      return void 0;
    }
    if (!url.startsWith(key, keyIndex2 + 1)) {
      keyIndex2 = url.indexOf(`&${key}`, keyIndex2 + 1);
    }
    while (keyIndex2 !== -1) {
      const trailingKeyCode = url.charCodeAt(keyIndex2 + key.length + 1);
      if (trailingKeyCode === 61) {
        const valueIndex = keyIndex2 + key.length + 2;
        const endIndex = url.indexOf("&", valueIndex);
        return _decodeURI(url.slice(valueIndex, endIndex === -1 ? void 0 : endIndex));
      } else if (trailingKeyCode == 38 || isNaN(trailingKeyCode)) {
        return "";
      }
      keyIndex2 = url.indexOf(`&${key}`, keyIndex2 + 1);
    }
    encoded = /[%+]/.test(url);
    if (!encoded) {
      return void 0;
    }
  }
  const results = {};
  encoded ??= /[%+]/.test(url);
  let keyIndex = url.indexOf("?", 8);
  while (keyIndex !== -1) {
    const nextKeyIndex = url.indexOf("&", keyIndex + 1);
    let valueIndex = url.indexOf("=", keyIndex);
    if (valueIndex > nextKeyIndex && nextKeyIndex !== -1) {
      valueIndex = -1;
    }
    let name = url.slice(
      keyIndex + 1,
      valueIndex === -1 ? nextKeyIndex === -1 ? void 0 : nextKeyIndex : valueIndex
    );
    if (encoded) {
      name = _decodeURI(name);
    }
    keyIndex = nextKeyIndex;
    if (name === "") {
      continue;
    }
    let value;
    if (valueIndex === -1) {
      value = "";
    } else {
      value = url.slice(valueIndex + 1, nextKeyIndex === -1 ? void 0 : nextKeyIndex);
      if (encoded) {
        value = _decodeURI(value);
      }
    }
    if (multiple) {
      if (!(results[name] && Array.isArray(results[name]))) {
        results[name] = [];
      }
      ;
      results[name].push(value);
    } else {
      results[name] ??= value;
    }
  }
  return key ? results[key] : results;
}, "_getQueryParam");
var getQueryParam = _getQueryParam;
var getQueryParams = /* @__PURE__ */ __name((url, key) => {
  return _getQueryParam(url, key, true);
}, "getQueryParams");
var decodeURIComponent_ = decodeURIComponent;

// worker/node_modules/hono/dist/request.js
var tryDecodeURIComponent = /* @__PURE__ */ __name((str) => tryDecode(str, decodeURIComponent_), "tryDecodeURIComponent");
var HonoRequest = class {
  static {
    __name(this, "HonoRequest");
  }
  /**
   * `.raw` can get the raw Request object.
   *
   * @see {@link https://hono.dev/docs/api/request#raw}
   *
   * @example
   * ```ts
   * // For Cloudflare Workers
   * app.post('/', async (c) => {
   *   const metadata = c.req.raw.cf?.hostMetadata?
   *   ...
   * })
   * ```
   */
  raw;
  #validatedData;
  // Short name of validatedData
  #matchResult;
  routeIndex = 0;
  /**
   * `.path` can get the pathname of the request.
   *
   * @see {@link https://hono.dev/docs/api/request#path}
   *
   * @example
   * ```ts
   * app.get('/about/me', (c) => {
   *   const pathname = c.req.path // `/about/me`
   * })
   * ```
   */
  path;
  bodyCache = {};
  constructor(request, path = "/", matchResult = [[]]) {
    this.raw = request;
    this.path = path;
    this.#matchResult = matchResult;
    this.#validatedData = {};
  }
  param(key) {
    return key ? this.#getDecodedParam(key) : this.#getAllDecodedParams();
  }
  #getDecodedParam(key) {
    const paramKey = this.#matchResult[0][this.routeIndex][1][key];
    const param = this.#getParamValue(paramKey);
    return param && /\%/.test(param) ? tryDecodeURIComponent(param) : param;
  }
  #getAllDecodedParams() {
    const decoded = {};
    const keys = Object.keys(this.#matchResult[0][this.routeIndex][1]);
    for (const key of keys) {
      const value = this.#getParamValue(this.#matchResult[0][this.routeIndex][1][key]);
      if (value !== void 0) {
        decoded[key] = /\%/.test(value) ? tryDecodeURIComponent(value) : value;
      }
    }
    return decoded;
  }
  #getParamValue(paramKey) {
    return this.#matchResult[1] ? this.#matchResult[1][paramKey] : paramKey;
  }
  query(key) {
    return getQueryParam(this.url, key);
  }
  queries(key) {
    return getQueryParams(this.url, key);
  }
  header(name) {
    if (name) {
      return this.raw.headers.get(name) ?? void 0;
    }
    const headerData = {};
    this.raw.headers.forEach((value, key) => {
      headerData[key] = value;
    });
    return headerData;
  }
  async parseBody(options) {
    return this.bodyCache.parsedBody ??= await parseBody(this, options);
  }
  #cachedBody = /* @__PURE__ */ __name((key) => {
    const { bodyCache, raw: raw2 } = this;
    const cachedBody = bodyCache[key];
    if (cachedBody) {
      return cachedBody;
    }
    const anyCachedKey = Object.keys(bodyCache)[0];
    if (anyCachedKey) {
      return bodyCache[anyCachedKey].then((body) => {
        if (anyCachedKey === "json") {
          body = JSON.stringify(body);
        }
        return new Response(body)[key]();
      });
    }
    return bodyCache[key] = raw2[key]();
  }, "#cachedBody");
  /**
   * `.json()` can parse Request body of type `application/json`
   *
   * @see {@link https://hono.dev/docs/api/request#json}
   *
   * @example
   * ```ts
   * app.post('/entry', async (c) => {
   *   const body = await c.req.json()
   * })
   * ```
   */
  json() {
    return this.#cachedBody("text").then((text3) => JSON.parse(text3));
  }
  /**
   * `.text()` can parse Request body of type `text/plain`
   *
   * @see {@link https://hono.dev/docs/api/request#text}
   *
   * @example
   * ```ts
   * app.post('/entry', async (c) => {
   *   const body = await c.req.text()
   * })
   * ```
   */
  text() {
    return this.#cachedBody("text");
  }
  /**
   * `.arrayBuffer()` parse Request body as an `ArrayBuffer`
   *
   * @see {@link https://hono.dev/docs/api/request#arraybuffer}
   *
   * @example
   * ```ts
   * app.post('/entry', async (c) => {
   *   const body = await c.req.arrayBuffer()
   * })
   * ```
   */
  arrayBuffer() {
    return this.#cachedBody("arrayBuffer");
  }
  /**
   * Parses the request body as a `Blob`.
   * @example
   * ```ts
   * app.post('/entry', async (c) => {
   *   const body = await c.req.blob();
   * });
   * ```
   * @see https://hono.dev/docs/api/request#blob
   */
  blob() {
    return this.#cachedBody("blob");
  }
  /**
   * Parses the request body as `FormData`.
   * @example
   * ```ts
   * app.post('/entry', async (c) => {
   *   const body = await c.req.formData();
   * });
   * ```
   * @see https://hono.dev/docs/api/request#formdata
   */
  formData() {
    return this.#cachedBody("formData");
  }
  /**
   * Adds validated data to the request.
   *
   * @param target - The target of the validation.
   * @param data - The validated data to add.
   */
  addValidatedData(target, data) {
    this.#validatedData[target] = data;
  }
  valid(target) {
    return this.#validatedData[target];
  }
  /**
   * `.url()` can get the request url strings.
   *
   * @see {@link https://hono.dev/docs/api/request#url}
   *
   * @example
   * ```ts
   * app.get('/about/me', (c) => {
   *   const url = c.req.url // `http://localhost:8787/about/me`
   *   ...
   * })
   * ```
   */
  get url() {
    return this.raw.url;
  }
  /**
   * `.method()` can get the method name of the request.
   *
   * @see {@link https://hono.dev/docs/api/request#method}
   *
   * @example
   * ```ts
   * app.get('/about/me', (c) => {
   *   const method = c.req.method // `GET`
   * })
   * ```
   */
  get method() {
    return this.raw.method;
  }
  get [GET_MATCH_RESULT]() {
    return this.#matchResult;
  }
  /**
   * `.matchedRoutes()` can return a matched route in the handler
   *
   * @deprecated
   *
   * Use matchedRoutes helper defined in "hono/route" instead.
   *
   * @see {@link https://hono.dev/docs/api/request#matchedroutes}
   *
   * @example
   * ```ts
   * app.use('*', async function logger(c, next) {
   *   await next()
   *   c.req.matchedRoutes.forEach(({ handler, method, path }, i) => {
   *     const name = handler.name || (handler.length < 2 ? '[handler]' : '[middleware]')
   *     console.log(
   *       method,
   *       ' ',
   *       path,
   *       ' '.repeat(Math.max(10 - path.length, 0)),
   *       name,
   *       i === c.req.routeIndex ? '<- respond from here' : ''
   *     )
   *   })
   * })
   * ```
   */
  get matchedRoutes() {
    return this.#matchResult[0].map(([[, route]]) => route);
  }
  /**
   * `routePath()` can retrieve the path registered within the handler
   *
   * @deprecated
   *
   * Use routePath helper defined in "hono/route" instead.
   *
   * @see {@link https://hono.dev/docs/api/request#routepath}
   *
   * @example
   * ```ts
   * app.get('/posts/:id', (c) => {
   *   return c.json({ path: c.req.routePath })
   * })
   * ```
   */
  get routePath() {
    return this.#matchResult[0].map(([[, route]]) => route)[this.routeIndex].path;
  }
};

// worker/node_modules/hono/dist/utils/html.js
var HtmlEscapedCallbackPhase = {
  Stringify: 1,
  BeforeStream: 2,
  Stream: 3
};
var raw = /* @__PURE__ */ __name((value, callbacks) => {
  const escapedString = new String(value);
  escapedString.isEscaped = true;
  escapedString.callbacks = callbacks;
  return escapedString;
}, "raw");
var resolveCallback = /* @__PURE__ */ __name(async (str, phase, preserveCallbacks, context, buffer2) => {
  if (typeof str === "object" && !(str instanceof String)) {
    if (!(str instanceof Promise)) {
      str = str.toString();
    }
    if (str instanceof Promise) {
      str = await str;
    }
  }
  const callbacks = str.callbacks;
  if (!callbacks?.length) {
    return Promise.resolve(str);
  }
  if (buffer2) {
    buffer2[0] += str;
  } else {
    buffer2 = [str];
  }
  const resStr = Promise.all(callbacks.map((c) => c({ phase, buffer: buffer2, context }))).then(
    (res) => Promise.all(
      res.filter(Boolean).map((str2) => resolveCallback(str2, phase, false, context, buffer2))
    ).then(() => buffer2[0])
  );
  if (preserveCallbacks) {
    return raw(await resStr, callbacks);
  } else {
    return resStr;
  }
}, "resolveCallback");

// worker/node_modules/hono/dist/context.js
var TEXT_PLAIN = "text/plain; charset=UTF-8";
var setDefaultContentType = /* @__PURE__ */ __name((contentType, headers) => {
  return {
    "Content-Type": contentType,
    ...headers
  };
}, "setDefaultContentType");
var Context = class {
  static {
    __name(this, "Context");
  }
  #rawRequest;
  #req;
  /**
   * `.env` can get bindings (environment variables, secrets, KV namespaces, D1 database, R2 bucket etc.) in Cloudflare Workers.
   *
   * @see {@link https://hono.dev/docs/api/context#env}
   *
   * @example
   * ```ts
   * // Environment object for Cloudflare Workers
   * app.get('*', async c => {
   *   const counter = c.env.COUNTER
   * })
   * ```
   */
  env = {};
  #var;
  finalized = false;
  /**
   * `.error` can get the error object from the middleware if the Handler throws an error.
   *
   * @see {@link https://hono.dev/docs/api/context#error}
   *
   * @example
   * ```ts
   * app.use('*', async (c, next) => {
   *   await next()
   *   if (c.error) {
   *     // do something...
   *   }
   * })
   * ```
   */
  error;
  #status;
  #executionCtx;
  #res;
  #layout;
  #renderer;
  #notFoundHandler;
  #preparedHeaders;
  #matchResult;
  #path;
  /**
   * Creates an instance of the Context class.
   *
   * @param req - The Request object.
   * @param options - Optional configuration options for the context.
   */
  constructor(req, options) {
    this.#rawRequest = req;
    if (options) {
      this.#executionCtx = options.executionCtx;
      this.env = options.env;
      this.#notFoundHandler = options.notFoundHandler;
      this.#path = options.path;
      this.#matchResult = options.matchResult;
    }
  }
  /**
   * `.req` is the instance of {@link HonoRequest}.
   */
  get req() {
    this.#req ??= new HonoRequest(this.#rawRequest, this.#path, this.#matchResult);
    return this.#req;
  }
  /**
   * @see {@link https://hono.dev/docs/api/context#event}
   * The FetchEvent associated with the current request.
   *
   * @throws Will throw an error if the context does not have a FetchEvent.
   */
  get event() {
    if (this.#executionCtx && "respondWith" in this.#executionCtx) {
      return this.#executionCtx;
    } else {
      throw Error("This context has no FetchEvent");
    }
  }
  /**
   * @see {@link https://hono.dev/docs/api/context#executionctx}
   * The ExecutionContext associated with the current request.
   *
   * @throws Will throw an error if the context does not have an ExecutionContext.
   */
  get executionCtx() {
    if (this.#executionCtx) {
      return this.#executionCtx;
    } else {
      throw Error("This context has no ExecutionContext");
    }
  }
  /**
   * @see {@link https://hono.dev/docs/api/context#res}
   * The Response object for the current request.
   */
  get res() {
    return this.#res ||= new Response(null, {
      headers: this.#preparedHeaders ??= new Headers()
    });
  }
  /**
   * Sets the Response object for the current request.
   *
   * @param _res - The Response object to set.
   */
  set res(_res) {
    if (this.#res && _res) {
      _res = new Response(_res.body, _res);
      for (const [k, v] of this.#res.headers.entries()) {
        if (k === "content-type") {
          continue;
        }
        if (k === "set-cookie") {
          const cookies = this.#res.headers.getSetCookie();
          _res.headers.delete("set-cookie");
          for (const cookie of cookies) {
            _res.headers.append("set-cookie", cookie);
          }
        } else {
          _res.headers.set(k, v);
        }
      }
    }
    this.#res = _res;
    this.finalized = true;
  }
  /**
   * `.render()` can create a response within a layout.
   *
   * @see {@link https://hono.dev/docs/api/context#render-setrenderer}
   *
   * @example
   * ```ts
   * app.get('/', (c) => {
   *   return c.render('Hello!')
   * })
   * ```
   */
  render = /* @__PURE__ */ __name((...args) => {
    this.#renderer ??= (content) => this.html(content);
    return this.#renderer(...args);
  }, "render");
  /**
   * Sets the layout for the response.
   *
   * @param layout - The layout to set.
   * @returns The layout function.
   */
  setLayout = /* @__PURE__ */ __name((layout) => this.#layout = layout, "setLayout");
  /**
   * Gets the current layout for the response.
   *
   * @returns The current layout function.
   */
  getLayout = /* @__PURE__ */ __name(() => this.#layout, "getLayout");
  /**
   * `.setRenderer()` can set the layout in the custom middleware.
   *
   * @see {@link https://hono.dev/docs/api/context#render-setrenderer}
   *
   * @example
   * ```tsx
   * app.use('*', async (c, next) => {
   *   c.setRenderer((content) => {
   *     return c.html(
   *       <html>
   *         <body>
   *           <p>{content}</p>
   *         </body>
   *       </html>
   *     )
   *   })
   *   await next()
   * })
   * ```
   */
  setRenderer = /* @__PURE__ */ __name((renderer) => {
    this.#renderer = renderer;
  }, "setRenderer");
  /**
   * `.header()` can set headers.
   *
   * @see {@link https://hono.dev/docs/api/context#header}
   *
   * @example
   * ```ts
   * app.get('/welcome', (c) => {
   *   // Set headers
   *   c.header('X-Message', 'Hello!')
   *   c.header('Content-Type', 'text/plain')
   *
   *   return c.body('Thank you for coming')
   * })
   * ```
   */
  header = /* @__PURE__ */ __name((name, value, options) => {
    if (this.finalized) {
      this.#res = new Response(this.#res.body, this.#res);
    }
    const headers = this.#res ? this.#res.headers : this.#preparedHeaders ??= new Headers();
    if (value === void 0) {
      headers.delete(name);
    } else if (options?.append) {
      headers.append(name, value);
    } else {
      headers.set(name, value);
    }
  }, "header");
  status = /* @__PURE__ */ __name((status) => {
    this.#status = status;
  }, "status");
  /**
   * `.set()` can set the value specified by the key.
   *
   * @see {@link https://hono.dev/docs/api/context#set-get}
   *
   * @example
   * ```ts
   * app.use('*', async (c, next) => {
   *   c.set('message', 'Hono is hot!!')
   *   await next()
   * })
   * ```
   */
  set = /* @__PURE__ */ __name((key, value) => {
    this.#var ??= /* @__PURE__ */ new Map();
    this.#var.set(key, value);
  }, "set");
  /**
   * `.get()` can use the value specified by the key.
   *
   * @see {@link https://hono.dev/docs/api/context#set-get}
   *
   * @example
   * ```ts
   * app.get('/', (c) => {
   *   const message = c.get('message')
   *   return c.text(`The message is "${message}"`)
   * })
   * ```
   */
  get = /* @__PURE__ */ __name((key) => {
    return this.#var ? this.#var.get(key) : void 0;
  }, "get");
  /**
   * `.var` can access the value of a variable.
   *
   * @see {@link https://hono.dev/docs/api/context#var}
   *
   * @example
   * ```ts
   * const result = c.var.client.oneMethod()
   * ```
   */
  // c.var.propName is a read-only
  get var() {
    if (!this.#var) {
      return {};
    }
    return Object.fromEntries(this.#var);
  }
  #newResponse(data, arg, headers) {
    const responseHeaders = this.#res ? new Headers(this.#res.headers) : this.#preparedHeaders ?? new Headers();
    if (typeof arg === "object" && "headers" in arg) {
      const argHeaders = arg.headers instanceof Headers ? arg.headers : new Headers(arg.headers);
      for (const [key, value] of argHeaders) {
        if (key.toLowerCase() === "set-cookie") {
          responseHeaders.append(key, value);
        } else {
          responseHeaders.set(key, value);
        }
      }
    }
    if (headers) {
      for (const [k, v] of Object.entries(headers)) {
        if (typeof v === "string") {
          responseHeaders.set(k, v);
        } else {
          responseHeaders.delete(k);
          for (const v2 of v) {
            responseHeaders.append(k, v2);
          }
        }
      }
    }
    const status = typeof arg === "number" ? arg : arg?.status ?? this.#status;
    return new Response(data, { status, headers: responseHeaders });
  }
  newResponse = /* @__PURE__ */ __name((...args) => this.#newResponse(...args), "newResponse");
  /**
   * `.body()` can return the HTTP response.
   * You can set headers with `.header()` and set HTTP status code with `.status`.
   * This can also be set in `.text()`, `.json()` and so on.
   *
   * @see {@link https://hono.dev/docs/api/context#body}
   *
   * @example
   * ```ts
   * app.get('/welcome', (c) => {
   *   // Set headers
   *   c.header('X-Message', 'Hello!')
   *   c.header('Content-Type', 'text/plain')
   *   // Set HTTP status code
   *   c.status(201)
   *
   *   // Return the response body
   *   return c.body('Thank you for coming')
   * })
   * ```
   */
  body = /* @__PURE__ */ __name((data, arg, headers) => this.#newResponse(data, arg, headers), "body");
  /**
   * `.text()` can render text as `Content-Type:text/plain`.
   *
   * @see {@link https://hono.dev/docs/api/context#text}
   *
   * @example
   * ```ts
   * app.get('/say', (c) => {
   *   return c.text('Hello!')
   * })
   * ```
   */
  text = /* @__PURE__ */ __name((text3, arg, headers) => {
    return !this.#preparedHeaders && !this.#status && !arg && !headers && !this.finalized ? new Response(text3) : this.#newResponse(
      text3,
      arg,
      setDefaultContentType(TEXT_PLAIN, headers)
    );
  }, "text");
  /**
   * `.json()` can render JSON as `Content-Type:application/json`.
   *
   * @see {@link https://hono.dev/docs/api/context#json}
   *
   * @example
   * ```ts
   * app.get('/api', (c) => {
   *   return c.json({ message: 'Hello!' })
   * })
   * ```
   */
  json = /* @__PURE__ */ __name((object2, arg, headers) => {
    return this.#newResponse(
      JSON.stringify(object2),
      arg,
      setDefaultContentType("application/json", headers)
    );
  }, "json");
  html = /* @__PURE__ */ __name((html, arg, headers) => {
    const res = /* @__PURE__ */ __name((html2) => this.#newResponse(html2, arg, setDefaultContentType("text/html; charset=UTF-8", headers)), "res");
    return typeof html === "object" ? resolveCallback(html, HtmlEscapedCallbackPhase.Stringify, false, {}).then(res) : res(html);
  }, "html");
  /**
   * `.redirect()` can Redirect, default status code is 302.
   *
   * @see {@link https://hono.dev/docs/api/context#redirect}
   *
   * @example
   * ```ts
   * app.get('/redirect', (c) => {
   *   return c.redirect('/')
   * })
   * app.get('/redirect-permanently', (c) => {
   *   return c.redirect('/', 301)
   * })
   * ```
   */
  redirect = /* @__PURE__ */ __name((location, status) => {
    const locationString = String(location);
    this.header(
      "Location",
      // Multibyes should be encoded
      // eslint-disable-next-line no-control-regex
      !/[^\x00-\xFF]/.test(locationString) ? locationString : encodeURI(locationString)
    );
    return this.newResponse(null, status ?? 302);
  }, "redirect");
  /**
   * `.notFound()` can return the Not Found Response.
   *
   * @see {@link https://hono.dev/docs/api/context#notfound}
   *
   * @example
   * ```ts
   * app.get('/notfound', (c) => {
   *   return c.notFound()
   * })
   * ```
   */
  notFound = /* @__PURE__ */ __name(() => {
    this.#notFoundHandler ??= () => new Response();
    return this.#notFoundHandler(this);
  }, "notFound");
};

// worker/node_modules/hono/dist/router.js
var METHOD_NAME_ALL = "ALL";
var METHOD_NAME_ALL_LOWERCASE = "all";
var METHODS = ["get", "post", "put", "delete", "options", "patch"];
var MESSAGE_MATCHER_IS_ALREADY_BUILT = "Can not add a route since the matcher is already built.";
var UnsupportedPathError = class extends Error {
  static {
    __name(this, "UnsupportedPathError");
  }
};

// worker/node_modules/hono/dist/utils/constants.js
var COMPOSED_HANDLER = "__COMPOSED_HANDLER";

// worker/node_modules/hono/dist/hono-base.js
var notFoundHandler = /* @__PURE__ */ __name((c) => {
  return c.text("404 Not Found", 404);
}, "notFoundHandler");
var errorHandler = /* @__PURE__ */ __name((err, c) => {
  if ("getResponse" in err) {
    const res = err.getResponse();
    return c.newResponse(res.body, res);
  }
  console.error(err);
  return c.text("Internal Server Error", 500);
}, "errorHandler");
var Hono = class _Hono {
  static {
    __name(this, "_Hono");
  }
  get;
  post;
  put;
  delete;
  options;
  patch;
  all;
  on;
  use;
  /*
    This class is like an abstract class and does not have a router.
    To use it, inherit the class and implement router in the constructor.
  */
  router;
  getPath;
  // Cannot use `#` because it requires visibility at JavaScript runtime.
  _basePath = "/";
  #path = "/";
  routes = [];
  constructor(options = {}) {
    const allMethods = [...METHODS, METHOD_NAME_ALL_LOWERCASE];
    allMethods.forEach((method) => {
      this[method] = (args1, ...args) => {
        if (typeof args1 === "string") {
          this.#path = args1;
        } else {
          this.#addRoute(method, this.#path, args1);
        }
        args.forEach((handler) => {
          this.#addRoute(method, this.#path, handler);
        });
        return this;
      };
    });
    this.on = (method, path, ...handlers) => {
      for (const p of [path].flat()) {
        this.#path = p;
        for (const m of [method].flat()) {
          handlers.map((handler) => {
            this.#addRoute(m.toUpperCase(), this.#path, handler);
          });
        }
      }
      return this;
    };
    this.use = (arg1, ...handlers) => {
      if (typeof arg1 === "string") {
        this.#path = arg1;
      } else {
        this.#path = "*";
        handlers.unshift(arg1);
      }
      handlers.forEach((handler) => {
        this.#addRoute(METHOD_NAME_ALL, this.#path, handler);
      });
      return this;
    };
    const { strict, ...optionsWithoutStrict } = options;
    Object.assign(this, optionsWithoutStrict);
    this.getPath = strict ?? true ? options.getPath ?? getPath : getPathNoStrict;
  }
  #clone() {
    const clone2 = new _Hono({
      router: this.router,
      getPath: this.getPath
    });
    clone2.errorHandler = this.errorHandler;
    clone2.#notFoundHandler = this.#notFoundHandler;
    clone2.routes = this.routes;
    return clone2;
  }
  #notFoundHandler = notFoundHandler;
  // Cannot use `#` because it requires visibility at JavaScript runtime.
  errorHandler = errorHandler;
  /**
   * `.route()` allows grouping other Hono instance in routes.
   *
   * @see {@link https://hono.dev/docs/api/routing#grouping}
   *
   * @param {string} path - base Path
   * @param {Hono} app - other Hono instance
   * @returns {Hono} routed Hono instance
   *
   * @example
   * ```ts
   * const app = new Hono()
   * const app2 = new Hono()
   *
   * app2.get("/user", (c) => c.text("user"))
   * app.route("/api", app2) // GET /api/user
   * ```
   */
  route(path, app2) {
    const subApp = this.basePath(path);
    app2.routes.map((r) => {
      let handler;
      if (app2.errorHandler === errorHandler) {
        handler = r.handler;
      } else {
        handler = /* @__PURE__ */ __name(async (c, next) => (await compose([], app2.errorHandler)(c, () => r.handler(c, next))).res, "handler");
        handler[COMPOSED_HANDLER] = r.handler;
      }
      subApp.#addRoute(r.method, r.path, handler);
    });
    return this;
  }
  /**
   * `.basePath()` allows base paths to be specified.
   *
   * @see {@link https://hono.dev/docs/api/routing#base-path}
   *
   * @param {string} path - base Path
   * @returns {Hono} changed Hono instance
   *
   * @example
   * ```ts
   * const api = new Hono().basePath('/api')
   * ```
   */
  basePath(path) {
    const subApp = this.#clone();
    subApp._basePath = mergePath(this._basePath, path);
    return subApp;
  }
  /**
   * `.onError()` handles an error and returns a customized Response.
   *
   * @see {@link https://hono.dev/docs/api/hono#error-handling}
   *
   * @param {ErrorHandler} handler - request Handler for error
   * @returns {Hono} changed Hono instance
   *
   * @example
   * ```ts
   * app.onError((err, c) => {
   *   console.error(`${err}`)
   *   return c.text('Custom Error Message', 500)
   * })
   * ```
   */
  onError = /* @__PURE__ */ __name((handler) => {
    this.errorHandler = handler;
    return this;
  }, "onError");
  /**
   * `.notFound()` allows you to customize a Not Found Response.
   *
   * @see {@link https://hono.dev/docs/api/hono#not-found}
   *
   * @param {NotFoundHandler} handler - request handler for not-found
   * @returns {Hono} changed Hono instance
   *
   * @example
   * ```ts
   * app.notFound((c) => {
   *   return c.text('Custom 404 Message', 404)
   * })
   * ```
   */
  notFound = /* @__PURE__ */ __name((handler) => {
    this.#notFoundHandler = handler;
    return this;
  }, "notFound");
  /**
   * `.mount()` allows you to mount applications built with other frameworks into your Hono application.
   *
   * @see {@link https://hono.dev/docs/api/hono#mount}
   *
   * @param {string} path - base Path
   * @param {Function} applicationHandler - other Request Handler
   * @param {MountOptions} [options] - options of `.mount()`
   * @returns {Hono} mounted Hono instance
   *
   * @example
   * ```ts
   * import { Router as IttyRouter } from 'itty-router'
   * import { Hono } from 'hono'
   * // Create itty-router application
   * const ittyRouter = IttyRouter()
   * // GET /itty-router/hello
   * ittyRouter.get('/hello', () => new Response('Hello from itty-router'))
   *
   * const app = new Hono()
   * app.mount('/itty-router', ittyRouter.handle)
   * ```
   *
   * @example
   * ```ts
   * const app = new Hono()
   * // Send the request to another application without modification.
   * app.mount('/app', anotherApp, {
   *   replaceRequest: (req) => req,
   * })
   * ```
   */
  mount(path, applicationHandler, options) {
    let replaceRequest;
    let optionHandler;
    if (options) {
      if (typeof options === "function") {
        optionHandler = options;
      } else {
        optionHandler = options.optionHandler;
        if (options.replaceRequest === false) {
          replaceRequest = /* @__PURE__ */ __name((request) => request, "replaceRequest");
        } else {
          replaceRequest = options.replaceRequest;
        }
      }
    }
    const getOptions = optionHandler ? (c) => {
      const options2 = optionHandler(c);
      return Array.isArray(options2) ? options2 : [options2];
    } : (c) => {
      let executionContext = void 0;
      try {
        executionContext = c.executionCtx;
      } catch {
      }
      return [c.env, executionContext];
    };
    replaceRequest ||= (() => {
      const mergedPath = mergePath(this._basePath, path);
      const pathPrefixLength = mergedPath === "/" ? 0 : mergedPath.length;
      return (request) => {
        const url = new URL(request.url);
        url.pathname = url.pathname.slice(pathPrefixLength) || "/";
        return new Request(url, request);
      };
    })();
    const handler = /* @__PURE__ */ __name(async (c, next) => {
      const res = await applicationHandler(replaceRequest(c.req.raw), ...getOptions(c));
      if (res) {
        return res;
      }
      await next();
    }, "handler");
    this.#addRoute(METHOD_NAME_ALL, mergePath(path, "*"), handler);
    return this;
  }
  #addRoute(method, path, handler) {
    method = method.toUpperCase();
    path = mergePath(this._basePath, path);
    const r = { basePath: this._basePath, path, method, handler };
    this.router.add(method, path, [handler, r]);
    this.routes.push(r);
  }
  #handleError(err, c) {
    if (err instanceof Error) {
      return this.errorHandler(err, c);
    }
    throw err;
  }
  #dispatch(request, executionCtx, env, method) {
    if (method === "HEAD") {
      return (async () => new Response(null, await this.#dispatch(request, executionCtx, env, "GET")))();
    }
    const path = this.getPath(request, { env });
    const matchResult = this.router.match(method, path);
    const c = new Context(request, {
      path,
      matchResult,
      env,
      executionCtx,
      notFoundHandler: this.#notFoundHandler
    });
    if (matchResult[0].length === 1) {
      let res;
      try {
        res = matchResult[0][0][0][0](c, async () => {
          c.res = await this.#notFoundHandler(c);
        });
      } catch (err) {
        return this.#handleError(err, c);
      }
      return res instanceof Promise ? res.then(
        (resolved) => resolved || (c.finalized ? c.res : this.#notFoundHandler(c))
      ).catch((err) => this.#handleError(err, c)) : res ?? this.#notFoundHandler(c);
    }
    const composed = compose(matchResult[0], this.errorHandler, this.#notFoundHandler);
    return (async () => {
      try {
        const context = await composed(c);
        if (!context.finalized) {
          throw new Error(
            "Context is not finalized. Did you forget to return a Response object or `await next()`?"
          );
        }
        return context.res;
      } catch (err) {
        return this.#handleError(err, c);
      }
    })();
  }
  /**
   * `.fetch()` will be entry point of your app.
   *
   * @see {@link https://hono.dev/docs/api/hono#fetch}
   *
   * @param {Request} request - request Object of request
   * @param {Env} Env - env Object
   * @param {ExecutionContext} - context of execution
   * @returns {Response | Promise<Response>} response of request
   *
   */
  fetch = /* @__PURE__ */ __name((request, ...rest) => {
    return this.#dispatch(request, rest[1], rest[0], request.method);
  }, "fetch");
  /**
   * `.request()` is a useful method for testing.
   * You can pass a URL or pathname to send a GET request.
   * app will return a Response object.
   * ```ts
   * test('GET /hello is ok', async () => {
   *   const res = await app.request('/hello')
   *   expect(res.status).toBe(200)
   * })
   * ```
   * @see https://hono.dev/docs/api/hono#request
   */
  request = /* @__PURE__ */ __name((input, requestInit, Env, executionCtx) => {
    if (input instanceof Request) {
      return this.fetch(requestInit ? new Request(input, requestInit) : input, Env, executionCtx);
    }
    input = input.toString();
    return this.fetch(
      new Request(
        /^https?:\/\//.test(input) ? input : `http://localhost${mergePath("/", input)}`,
        requestInit
      ),
      Env,
      executionCtx
    );
  }, "request");
  /**
   * `.fire()` automatically adds a global fetch event listener.
   * This can be useful for environments that adhere to the Service Worker API, such as non-ES module Cloudflare Workers.
   * @deprecated
   * Use `fire` from `hono/service-worker` instead.
   * ```ts
   * import { Hono } from 'hono'
   * import { fire } from 'hono/service-worker'
   *
   * const app = new Hono()
   * // ...
   * fire(app)
   * ```
   * @see https://hono.dev/docs/api/hono#fire
   * @see https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API
   * @see https://developers.cloudflare.com/workers/reference/migrate-to-module-workers/
   */
  fire = /* @__PURE__ */ __name(() => {
    addEventListener("fetch", (event) => {
      event.respondWith(this.#dispatch(event.request, event, void 0, event.request.method));
    });
  }, "fire");
};

// worker/node_modules/hono/dist/router/reg-exp-router/matcher.js
var emptyParam = [];
function match(method, path) {
  const matchers = this.buildAllMatchers();
  const match2 = /* @__PURE__ */ __name(((method2, path2) => {
    const matcher = matchers[method2] || matchers[METHOD_NAME_ALL];
    const staticMatch = matcher[2][path2];
    if (staticMatch) {
      return staticMatch;
    }
    const match3 = path2.match(matcher[0]);
    if (!match3) {
      return [[], emptyParam];
    }
    const index = match3.indexOf("", 1);
    return [matcher[1][index], match3];
  }), "match2");
  this.match = match2;
  return match2(method, path);
}
__name(match, "match");

// worker/node_modules/hono/dist/router/reg-exp-router/node.js
var LABEL_REG_EXP_STR = "[^/]+";
var ONLY_WILDCARD_REG_EXP_STR = ".*";
var TAIL_WILDCARD_REG_EXP_STR = "(?:|/.*)";
var PATH_ERROR = /* @__PURE__ */ Symbol();
var regExpMetaChars = new Set(".\\+*[^]$()");
function compareKey(a, b) {
  if (a.length === 1) {
    return b.length === 1 ? a < b ? -1 : 1 : -1;
  }
  if (b.length === 1) {
    return 1;
  }
  if (a === ONLY_WILDCARD_REG_EXP_STR || a === TAIL_WILDCARD_REG_EXP_STR) {
    return 1;
  } else if (b === ONLY_WILDCARD_REG_EXP_STR || b === TAIL_WILDCARD_REG_EXP_STR) {
    return -1;
  }
  if (a === LABEL_REG_EXP_STR) {
    return 1;
  } else if (b === LABEL_REG_EXP_STR) {
    return -1;
  }
  return a.length === b.length ? a < b ? -1 : 1 : b.length - a.length;
}
__name(compareKey, "compareKey");
var Node = class _Node {
  static {
    __name(this, "_Node");
  }
  #index;
  #varIndex;
  #children = /* @__PURE__ */ Object.create(null);
  insert(tokens, index, paramMap, context, pathErrorCheckOnly) {
    if (tokens.length === 0) {
      if (this.#index !== void 0) {
        throw PATH_ERROR;
      }
      if (pathErrorCheckOnly) {
        return;
      }
      this.#index = index;
      return;
    }
    const [token, ...restTokens] = tokens;
    const pattern = token === "*" ? restTokens.length === 0 ? ["", "", ONLY_WILDCARD_REG_EXP_STR] : ["", "", LABEL_REG_EXP_STR] : token === "/*" ? ["", "", TAIL_WILDCARD_REG_EXP_STR] : token.match(/^\:([^\{\}]+)(?:\{(.+)\})?$/);
    let node;
    if (pattern) {
      const name = pattern[1];
      let regexpStr = pattern[2] || LABEL_REG_EXP_STR;
      if (name && pattern[2]) {
        if (regexpStr === ".*") {
          throw PATH_ERROR;
        }
        regexpStr = regexpStr.replace(/^\((?!\?:)(?=[^)]+\)$)/, "(?:");
        if (/\((?!\?:)/.test(regexpStr)) {
          throw PATH_ERROR;
        }
      }
      node = this.#children[regexpStr];
      if (!node) {
        if (Object.keys(this.#children).some(
          (k) => k !== ONLY_WILDCARD_REG_EXP_STR && k !== TAIL_WILDCARD_REG_EXP_STR
        )) {
          throw PATH_ERROR;
        }
        if (pathErrorCheckOnly) {
          return;
        }
        node = this.#children[regexpStr] = new _Node();
        if (name !== "") {
          node.#varIndex = context.varIndex++;
        }
      }
      if (!pathErrorCheckOnly && name !== "") {
        paramMap.push([name, node.#varIndex]);
      }
    } else {
      node = this.#children[token];
      if (!node) {
        if (Object.keys(this.#children).some(
          (k) => k.length > 1 && k !== ONLY_WILDCARD_REG_EXP_STR && k !== TAIL_WILDCARD_REG_EXP_STR
        )) {
          throw PATH_ERROR;
        }
        if (pathErrorCheckOnly) {
          return;
        }
        node = this.#children[token] = new _Node();
      }
    }
    node.insert(restTokens, index, paramMap, context, pathErrorCheckOnly);
  }
  buildRegExpStr() {
    const childKeys = Object.keys(this.#children).sort(compareKey);
    const strList = childKeys.map((k) => {
      const c = this.#children[k];
      return (typeof c.#varIndex === "number" ? `(${k})@${c.#varIndex}` : regExpMetaChars.has(k) ? `\\${k}` : k) + c.buildRegExpStr();
    });
    if (typeof this.#index === "number") {
      strList.unshift(`#${this.#index}`);
    }
    if (strList.length === 0) {
      return "";
    }
    if (strList.length === 1) {
      return strList[0];
    }
    return "(?:" + strList.join("|") + ")";
  }
};

// worker/node_modules/hono/dist/router/reg-exp-router/trie.js
var Trie = class {
  static {
    __name(this, "Trie");
  }
  #context = { varIndex: 0 };
  #root = new Node();
  insert(path, index, pathErrorCheckOnly) {
    const paramAssoc = [];
    const groups = [];
    for (let i = 0; ; ) {
      let replaced = false;
      path = path.replace(/\{[^}]+\}/g, (m) => {
        const mark = `@\\${i}`;
        groups[i] = [mark, m];
        i++;
        replaced = true;
        return mark;
      });
      if (!replaced) {
        break;
      }
    }
    const tokens = path.match(/(?::[^\/]+)|(?:\/\*$)|./g) || [];
    for (let i = groups.length - 1; i >= 0; i--) {
      const [mark] = groups[i];
      for (let j = tokens.length - 1; j >= 0; j--) {
        if (tokens[j].indexOf(mark) !== -1) {
          tokens[j] = tokens[j].replace(mark, groups[i][1]);
          break;
        }
      }
    }
    this.#root.insert(tokens, index, paramAssoc, this.#context, pathErrorCheckOnly);
    return paramAssoc;
  }
  buildRegExp() {
    let regexp = this.#root.buildRegExpStr();
    if (regexp === "") {
      return [/^$/, [], []];
    }
    let captureIndex = 0;
    const indexReplacementMap = [];
    const paramReplacementMap = [];
    regexp = regexp.replace(/#(\d+)|@(\d+)|\.\*\$/g, (_, handlerIndex, paramIndex) => {
      if (handlerIndex !== void 0) {
        indexReplacementMap[++captureIndex] = Number(handlerIndex);
        return "$()";
      }
      if (paramIndex !== void 0) {
        paramReplacementMap[Number(paramIndex)] = ++captureIndex;
        return "";
      }
      return "";
    });
    return [new RegExp(`^${regexp}`), indexReplacementMap, paramReplacementMap];
  }
};

// worker/node_modules/hono/dist/router/reg-exp-router/router.js
var nullMatcher = [/^$/, [], /* @__PURE__ */ Object.create(null)];
var wildcardRegExpCache = /* @__PURE__ */ Object.create(null);
function buildWildcardRegExp(path) {
  return wildcardRegExpCache[path] ??= new RegExp(
    path === "*" ? "" : `^${path.replace(
      /\/\*$|([.\\+*[^\]$()])/g,
      (_, metaChar) => metaChar ? `\\${metaChar}` : "(?:|/.*)"
    )}$`
  );
}
__name(buildWildcardRegExp, "buildWildcardRegExp");
function clearWildcardRegExpCache() {
  wildcardRegExpCache = /* @__PURE__ */ Object.create(null);
}
__name(clearWildcardRegExpCache, "clearWildcardRegExpCache");
function buildMatcherFromPreprocessedRoutes(routes2) {
  const trie = new Trie();
  const handlerData = [];
  if (routes2.length === 0) {
    return nullMatcher;
  }
  const routesWithStaticPathFlag = routes2.map(
    (route) => [!/\*|\/:/.test(route[0]), ...route]
  ).sort(
    ([isStaticA, pathA], [isStaticB, pathB]) => isStaticA ? 1 : isStaticB ? -1 : pathA.length - pathB.length
  );
  const staticMap = /* @__PURE__ */ Object.create(null);
  for (let i = 0, j = -1, len = routesWithStaticPathFlag.length; i < len; i++) {
    const [pathErrorCheckOnly, path, handlers] = routesWithStaticPathFlag[i];
    if (pathErrorCheckOnly) {
      staticMap[path] = [handlers.map(([h]) => [h, /* @__PURE__ */ Object.create(null)]), emptyParam];
    } else {
      j++;
    }
    let paramAssoc;
    try {
      paramAssoc = trie.insert(path, j, pathErrorCheckOnly);
    } catch (e) {
      throw e === PATH_ERROR ? new UnsupportedPathError(path) : e;
    }
    if (pathErrorCheckOnly) {
      continue;
    }
    handlerData[j] = handlers.map(([h, paramCount]) => {
      const paramIndexMap = /* @__PURE__ */ Object.create(null);
      paramCount -= 1;
      for (; paramCount >= 0; paramCount--) {
        const [key, value] = paramAssoc[paramCount];
        paramIndexMap[key] = value;
      }
      return [h, paramIndexMap];
    });
  }
  const [regexp, indexReplacementMap, paramReplacementMap] = trie.buildRegExp();
  for (let i = 0, len = handlerData.length; i < len; i++) {
    for (let j = 0, len2 = handlerData[i].length; j < len2; j++) {
      const map2 = handlerData[i][j]?.[1];
      if (!map2) {
        continue;
      }
      const keys = Object.keys(map2);
      for (let k = 0, len3 = keys.length; k < len3; k++) {
        map2[keys[k]] = paramReplacementMap[map2[keys[k]]];
      }
    }
  }
  const handlerMap = [];
  for (const i in indexReplacementMap) {
    handlerMap[i] = handlerData[indexReplacementMap[i]];
  }
  return [regexp, handlerMap, staticMap];
}
__name(buildMatcherFromPreprocessedRoutes, "buildMatcherFromPreprocessedRoutes");
function findMiddleware(middleware, path) {
  if (!middleware) {
    return void 0;
  }
  for (const k of Object.keys(middleware).sort((a, b) => b.length - a.length)) {
    if (buildWildcardRegExp(k).test(path)) {
      return [...middleware[k]];
    }
  }
  return void 0;
}
__name(findMiddleware, "findMiddleware");
var RegExpRouter = class {
  static {
    __name(this, "RegExpRouter");
  }
  name = "RegExpRouter";
  #middleware;
  #routes;
  constructor() {
    this.#middleware = { [METHOD_NAME_ALL]: /* @__PURE__ */ Object.create(null) };
    this.#routes = { [METHOD_NAME_ALL]: /* @__PURE__ */ Object.create(null) };
  }
  add(method, path, handler) {
    const middleware = this.#middleware;
    const routes2 = this.#routes;
    if (!middleware || !routes2) {
      throw new Error(MESSAGE_MATCHER_IS_ALREADY_BUILT);
    }
    if (!middleware[method]) {
      ;
      [middleware, routes2].forEach((handlerMap) => {
        handlerMap[method] = /* @__PURE__ */ Object.create(null);
        Object.keys(handlerMap[METHOD_NAME_ALL]).forEach((p) => {
          handlerMap[method][p] = [...handlerMap[METHOD_NAME_ALL][p]];
        });
      });
    }
    if (path === "/*") {
      path = "*";
    }
    const paramCount = (path.match(/\/:/g) || []).length;
    if (/\*$/.test(path)) {
      const re = buildWildcardRegExp(path);
      if (method === METHOD_NAME_ALL) {
        Object.keys(middleware).forEach((m) => {
          middleware[m][path] ||= findMiddleware(middleware[m], path) || findMiddleware(middleware[METHOD_NAME_ALL], path) || [];
        });
      } else {
        middleware[method][path] ||= findMiddleware(middleware[method], path) || findMiddleware(middleware[METHOD_NAME_ALL], path) || [];
      }
      Object.keys(middleware).forEach((m) => {
        if (method === METHOD_NAME_ALL || method === m) {
          Object.keys(middleware[m]).forEach((p) => {
            re.test(p) && middleware[m][p].push([handler, paramCount]);
          });
        }
      });
      Object.keys(routes2).forEach((m) => {
        if (method === METHOD_NAME_ALL || method === m) {
          Object.keys(routes2[m]).forEach(
            (p) => re.test(p) && routes2[m][p].push([handler, paramCount])
          );
        }
      });
      return;
    }
    const paths = checkOptionalParameter(path) || [path];
    for (let i = 0, len = paths.length; i < len; i++) {
      const path2 = paths[i];
      Object.keys(routes2).forEach((m) => {
        if (method === METHOD_NAME_ALL || method === m) {
          routes2[m][path2] ||= [
            ...findMiddleware(middleware[m], path2) || findMiddleware(middleware[METHOD_NAME_ALL], path2) || []
          ];
          routes2[m][path2].push([handler, paramCount - len + i + 1]);
        }
      });
    }
  }
  match = match;
  buildAllMatchers() {
    const matchers = /* @__PURE__ */ Object.create(null);
    Object.keys(this.#routes).concat(Object.keys(this.#middleware)).forEach((method) => {
      matchers[method] ||= this.#buildMatcher(method);
    });
    this.#middleware = this.#routes = void 0;
    clearWildcardRegExpCache();
    return matchers;
  }
  #buildMatcher(method) {
    const routes2 = [];
    let hasOwnRoute = method === METHOD_NAME_ALL;
    [this.#middleware, this.#routes].forEach((r) => {
      const ownRoute = r[method] ? Object.keys(r[method]).map((path) => [path, r[method][path]]) : [];
      if (ownRoute.length !== 0) {
        hasOwnRoute ||= true;
        routes2.push(...ownRoute);
      } else if (method !== METHOD_NAME_ALL) {
        routes2.push(
          ...Object.keys(r[METHOD_NAME_ALL]).map((path) => [path, r[METHOD_NAME_ALL][path]])
        );
      }
    });
    if (!hasOwnRoute) {
      return null;
    } else {
      return buildMatcherFromPreprocessedRoutes(routes2);
    }
  }
};

// worker/node_modules/hono/dist/router/smart-router/router.js
var SmartRouter = class {
  static {
    __name(this, "SmartRouter");
  }
  name = "SmartRouter";
  #routers = [];
  #routes = [];
  constructor(init) {
    this.#routers = init.routers;
  }
  add(method, path, handler) {
    if (!this.#routes) {
      throw new Error(MESSAGE_MATCHER_IS_ALREADY_BUILT);
    }
    this.#routes.push([method, path, handler]);
  }
  match(method, path) {
    if (!this.#routes) {
      throw new Error("Fatal error");
    }
    const routers = this.#routers;
    const routes2 = this.#routes;
    const len = routers.length;
    let i = 0;
    let res;
    for (; i < len; i++) {
      const router = routers[i];
      try {
        for (let i2 = 0, len2 = routes2.length; i2 < len2; i2++) {
          router.add(...routes2[i2]);
        }
        res = router.match(method, path);
      } catch (e) {
        if (e instanceof UnsupportedPathError) {
          continue;
        }
        throw e;
      }
      this.match = router.match.bind(router);
      this.#routers = [router];
      this.#routes = void 0;
      break;
    }
    if (i === len) {
      throw new Error("Fatal error");
    }
    this.name = `SmartRouter + ${this.activeRouter.name}`;
    return res;
  }
  get activeRouter() {
    if (this.#routes || this.#routers.length !== 1) {
      throw new Error("No active router has been determined yet.");
    }
    return this.#routers[0];
  }
};

// worker/node_modules/hono/dist/router/trie-router/node.js
var emptyParams = /* @__PURE__ */ Object.create(null);
var Node2 = class _Node2 {
  static {
    __name(this, "_Node");
  }
  #methods;
  #children;
  #patterns;
  #order = 0;
  #params = emptyParams;
  constructor(method, handler, children) {
    this.#children = children || /* @__PURE__ */ Object.create(null);
    this.#methods = [];
    if (method && handler) {
      const m = /* @__PURE__ */ Object.create(null);
      m[method] = { handler, possibleKeys: [], score: 0 };
      this.#methods = [m];
    }
    this.#patterns = [];
  }
  insert(method, path, handler) {
    this.#order = ++this.#order;
    let curNode = this;
    const parts = splitRoutingPath(path);
    const possibleKeys = [];
    for (let i = 0, len = parts.length; i < len; i++) {
      const p = parts[i];
      const nextP = parts[i + 1];
      const pattern = getPattern(p, nextP);
      const key = Array.isArray(pattern) ? pattern[0] : p;
      if (key in curNode.#children) {
        curNode = curNode.#children[key];
        if (pattern) {
          possibleKeys.push(pattern[1]);
        }
        continue;
      }
      curNode.#children[key] = new _Node2();
      if (pattern) {
        curNode.#patterns.push(pattern);
        possibleKeys.push(pattern[1]);
      }
      curNode = curNode.#children[key];
    }
    curNode.#methods.push({
      [method]: {
        handler,
        possibleKeys: possibleKeys.filter((v, i, a) => a.indexOf(v) === i),
        score: this.#order
      }
    });
    return curNode;
  }
  #getHandlerSets(node, method, nodeParams, params) {
    const handlerSets = [];
    for (let i = 0, len = node.#methods.length; i < len; i++) {
      const m = node.#methods[i];
      const handlerSet = m[method] || m[METHOD_NAME_ALL];
      const processedSet = {};
      if (handlerSet !== void 0) {
        handlerSet.params = /* @__PURE__ */ Object.create(null);
        handlerSets.push(handlerSet);
        if (nodeParams !== emptyParams || params && params !== emptyParams) {
          for (let i2 = 0, len2 = handlerSet.possibleKeys.length; i2 < len2; i2++) {
            const key = handlerSet.possibleKeys[i2];
            const processed = processedSet[handlerSet.score];
            handlerSet.params[key] = params?.[key] && !processed ? params[key] : nodeParams[key] ?? params?.[key];
            processedSet[handlerSet.score] = true;
          }
        }
      }
    }
    return handlerSets;
  }
  search(method, path) {
    const handlerSets = [];
    this.#params = emptyParams;
    const curNode = this;
    let curNodes = [curNode];
    const parts = splitPath(path);
    const curNodesQueue = [];
    for (let i = 0, len = parts.length; i < len; i++) {
      const part = parts[i];
      const isLast = i === len - 1;
      const tempNodes = [];
      for (let j = 0, len2 = curNodes.length; j < len2; j++) {
        const node = curNodes[j];
        const nextNode = node.#children[part];
        if (nextNode) {
          nextNode.#params = node.#params;
          if (isLast) {
            if (nextNode.#children["*"]) {
              handlerSets.push(
                ...this.#getHandlerSets(nextNode.#children["*"], method, node.#params)
              );
            }
            handlerSets.push(...this.#getHandlerSets(nextNode, method, node.#params));
          } else {
            tempNodes.push(nextNode);
          }
        }
        for (let k = 0, len3 = node.#patterns.length; k < len3; k++) {
          const pattern = node.#patterns[k];
          const params = node.#params === emptyParams ? {} : { ...node.#params };
          if (pattern === "*") {
            const astNode = node.#children["*"];
            if (astNode) {
              handlerSets.push(...this.#getHandlerSets(astNode, method, node.#params));
              astNode.#params = params;
              tempNodes.push(astNode);
            }
            continue;
          }
          const [key, name, matcher] = pattern;
          if (!part && !(matcher instanceof RegExp)) {
            continue;
          }
          const child = node.#children[key];
          const restPathString = parts.slice(i).join("/");
          if (matcher instanceof RegExp) {
            const m = matcher.exec(restPathString);
            if (m) {
              params[name] = m[0];
              handlerSets.push(...this.#getHandlerSets(child, method, node.#params, params));
              if (Object.keys(child.#children).length) {
                child.#params = params;
                const componentCount = m[0].match(/\//)?.length ?? 0;
                const targetCurNodes = curNodesQueue[componentCount] ||= [];
                targetCurNodes.push(child);
              }
              continue;
            }
          }
          if (matcher === true || matcher.test(part)) {
            params[name] = part;
            if (isLast) {
              handlerSets.push(...this.#getHandlerSets(child, method, params, node.#params));
              if (child.#children["*"]) {
                handlerSets.push(
                  ...this.#getHandlerSets(child.#children["*"], method, params, node.#params)
                );
              }
            } else {
              child.#params = params;
              tempNodes.push(child);
            }
          }
        }
      }
      curNodes = tempNodes.concat(curNodesQueue.shift() ?? []);
    }
    if (handlerSets.length > 1) {
      handlerSets.sort((a, b) => {
        return a.score - b.score;
      });
    }
    return [handlerSets.map(({ handler, params }) => [handler, params])];
  }
};

// worker/node_modules/hono/dist/router/trie-router/router.js
var TrieRouter = class {
  static {
    __name(this, "TrieRouter");
  }
  name = "TrieRouter";
  #node;
  constructor() {
    this.#node = new Node2();
  }
  add(method, path, handler) {
    const results = checkOptionalParameter(path);
    if (results) {
      for (let i = 0, len = results.length; i < len; i++) {
        this.#node.insert(method, results[i], handler);
      }
      return;
    }
    this.#node.insert(method, path, handler);
  }
  match(method, path) {
    return this.#node.search(method, path);
  }
};

// worker/node_modules/hono/dist/hono.js
var Hono2 = class extends Hono {
  static {
    __name(this, "Hono");
  }
  /**
   * Creates an instance of the Hono class.
   *
   * @param options - Optional configuration options for the Hono instance.
   */
  constructor(options = {}) {
    super(options);
    this.router = options.router ?? new SmartRouter({
      routers: [new RegExpRouter(), new TrieRouter()]
    });
  }
};

// worker/node_modules/hono/dist/middleware/cors/index.js
var cors = /* @__PURE__ */ __name((options) => {
  const defaults = {
    origin: "*",
    allowMethods: ["GET", "HEAD", "PUT", "POST", "DELETE", "PATCH"],
    allowHeaders: [],
    exposeHeaders: []
  };
  const opts = {
    ...defaults,
    ...options
  };
  const findAllowOrigin = ((optsOrigin) => {
    if (typeof optsOrigin === "string") {
      if (optsOrigin === "*") {
        return () => optsOrigin;
      } else {
        return (origin) => optsOrigin === origin ? origin : null;
      }
    } else if (typeof optsOrigin === "function") {
      return optsOrigin;
    } else {
      return (origin) => optsOrigin.includes(origin) ? origin : null;
    }
  })(opts.origin);
  const findAllowMethods = ((optsAllowMethods) => {
    if (typeof optsAllowMethods === "function") {
      return optsAllowMethods;
    } else if (Array.isArray(optsAllowMethods)) {
      return () => optsAllowMethods;
    } else {
      return () => [];
    }
  })(opts.allowMethods);
  return /* @__PURE__ */ __name(async function cors2(c, next) {
    function set2(key, value) {
      c.res.headers.set(key, value);
    }
    __name(set2, "set");
    const allowOrigin = await findAllowOrigin(c.req.header("origin") || "", c);
    if (allowOrigin) {
      set2("Access-Control-Allow-Origin", allowOrigin);
    }
    if (opts.credentials) {
      set2("Access-Control-Allow-Credentials", "true");
    }
    if (opts.exposeHeaders?.length) {
      set2("Access-Control-Expose-Headers", opts.exposeHeaders.join(","));
    }
    if (c.req.method === "OPTIONS") {
      if (opts.origin !== "*") {
        set2("Vary", "Origin");
      }
      if (opts.maxAge != null) {
        set2("Access-Control-Max-Age", opts.maxAge.toString());
      }
      const allowMethods = await findAllowMethods(c.req.header("origin") || "", c);
      if (allowMethods.length) {
        set2("Access-Control-Allow-Methods", allowMethods.join(","));
      }
      let headers = opts.allowHeaders;
      if (!headers?.length) {
        const requestHeaders = c.req.header("Access-Control-Request-Headers");
        if (requestHeaders) {
          headers = requestHeaders.split(/\s*,\s*/);
        }
      }
      if (headers?.length) {
        set2("Access-Control-Allow-Headers", headers.join(","));
        c.res.headers.append("Vary", "Access-Control-Request-Headers");
      }
      c.res.headers.delete("Content-Length");
      c.res.headers.delete("Content-Type");
      return new Response(null, {
        headers: c.res.headers,
        status: 204,
        statusText: "No Content"
      });
    }
    await next();
    if (opts.origin !== "*") {
      c.header("Vary", "Origin", { append: true });
    }
  }, "cors2");
}, "cors");

// worker/node_modules/hono/dist/utils/color.js
function getColorEnabled() {
  const { process: process2, Deno } = globalThis;
  const isNoColor = typeof Deno?.noColor === "boolean" ? Deno.noColor : process2 !== void 0 ? (
    // eslint-disable-next-line no-unsafe-optional-chaining
    "NO_COLOR" in process2?.env
  ) : false;
  return !isNoColor;
}
__name(getColorEnabled, "getColorEnabled");
async function getColorEnabledAsync() {
  const { navigator } = globalThis;
  const cfWorkers = "cloudflare:workers";
  const isNoColor = navigator !== void 0 && navigator.userAgent === "Cloudflare-Workers" ? await (async () => {
    try {
      return "NO_COLOR" in ((await import(cfWorkers)).env ?? {});
    } catch {
      return false;
    }
  })() : !getColorEnabled();
  return !isNoColor;
}
__name(getColorEnabledAsync, "getColorEnabledAsync");

// worker/node_modules/hono/dist/middleware/logger/index.js
var humanize = /* @__PURE__ */ __name((times) => {
  const [delimiter, separator] = [",", "."];
  const orderTimes = times.map((v) => v.replace(/(\d)(?=(\d\d\d)+(?!\d))/g, "$1" + delimiter));
  return orderTimes.join(separator);
}, "humanize");
var time = /* @__PURE__ */ __name((start) => {
  const delta = Date.now() - start;
  return humanize([delta < 1e3 ? delta + "ms" : Math.round(delta / 1e3) + "s"]);
}, "time");
var colorStatus = /* @__PURE__ */ __name(async (status) => {
  const colorEnabled = await getColorEnabledAsync();
  if (colorEnabled) {
    switch (status / 100 | 0) {
      case 5:
        return `\x1B[31m${status}\x1B[0m`;
      case 4:
        return `\x1B[33m${status}\x1B[0m`;
      case 3:
        return `\x1B[36m${status}\x1B[0m`;
      case 2:
        return `\x1B[32m${status}\x1B[0m`;
    }
  }
  return `${status}`;
}, "colorStatus");
async function log(fn, prefix, method, path, status = 0, elapsed) {
  const out = prefix === "<--" ? `${prefix} ${method} ${path}` : `${prefix} ${method} ${path} ${await colorStatus(status)} ${elapsed}`;
  fn(out);
}
__name(log, "log");
var logger = /* @__PURE__ */ __name((fn = console.log) => {
  return /* @__PURE__ */ __name(async function logger2(c, next) {
    const { method, url } = c.req;
    const path = url.slice(url.indexOf("/", 8));
    await log(fn, "<--", method, path);
    const start = Date.now();
    await next();
    await log(fn, "-->", method, path, c.res.status, time(start));
  }, "logger2");
}, "logger");

// worker/src/index.ts
init_routes();

// worker/src/queue-consumer.ts
init_db3();
init_storage();
init_territory();
async function handleQueueBatch(batch, env) {
  for (const message of batch.messages) {
    try {
      await processMessage(message.body, env);
      message.ack();
    } catch (err) {
      console.error(`[QUEUE] Error processing message:`, err);
      message.retry();
    }
  }
}
__name(handleQueueBatch, "handleQueueBatch");
async function processMessage(msg, env) {
  const db = createDb(env.DATABASE_URL, env.TURSO_AUTH_TOKEN);
  const storage = new WorkerStorage(db);
  switch (msg.type) {
    case "process_route_territory":
      await handleProcessRouteTerritory(storage, msg, env);
      break;
    case "reprocess_friend_group":
      await handleReprocessFriendGroup(storage, msg);
      break;
    default:
      console.error(`[QUEUE] Unknown message type:`, msg.type);
  }
}
__name(processMessage, "processMessage");
async function handleProcessRouteTerritory(storage, msg, env) {
  const { userId, routeId, startedAt, completedAt, distance: distance2, duration, earlyFeedEventId } = msg;
  console.log(`[QUEUE] Processing territory for route ${routeId} (user ${userId})`);
  const coords = JSON.parse(msg.coordinates);
  if (coords.length < 10) {
    console.log(`[QUEUE] Route ${routeId} has < 10 coords, skipping territory processing`);
    await updateFeedEventSafe(storage, userId, routeId, distance2, duration, {
      newAreaConquered: 0,
      victims: [],
      ranTogetherWith: [],
      treasuresCollected: []
    }, earlyFeedEventId);
    return;
  }
  const enclosedPoly = routeToEnclosedPolygon(coords, 150);
  if (!enclosedPoly) {
    console.warn(`[QUEUE] routeToEnclosedPolygon returned null for route ${routeId}`);
    let treasuresCollected = [];
    try {
      const competition = await storage.getActiveCompetition();
      if (competition && isCompetitionActive(competition)) {
        treasuresCollected = await autoCollectTreasures(storage, userId, competition.id, coords);
      }
    } catch (e) {
      console.error("[QUEUE] Treasure collect failed:", e);
    }
    await updateFeedEventSafe(storage, userId, routeId, distance2, duration, {
      newAreaConquered: 0,
      victims: [],
      ranTogetherWith: [],
      treasuresCollected
    }, earlyFeedEventId);
    return;
  }
  try {
    const { processTerritoryConquest: processTerritoryConquest2, generateFeedEvents: generateFeedEvents2 } = await Promise.resolve().then(() => (init_routes(), routes_exports));
    const conquestResult = await processTerritoryConquest2(
      storage,
      userId,
      routeId,
      enclosedPoly.geometry,
      env,
      startedAt,
      completedAt,
      coords
    );
    if (conquestResult.ranTogetherWith.length > 0) {
      await storage.updateRouteRanTogether(routeId, conquestResult.ranTogetherWith);
    }
    try {
      const actualArea = await storage.getUserTotalAreaFromTerritories(userId);
      if (Math.abs(actualArea - conquestResult.totalArea) > 100) {
        console.warn(`[QUEUE] Area mismatch: conquest=${conquestResult.totalArea}, actual=${actualArea}`);
        await storage.updateUserTotalArea(userId, actualArea);
      }
    } catch (_) {
    }
    await generateFeedEvents2(
      storage,
      userId,
      routeId,
      distance2,
      duration,
      conquestResult,
      false,
      earlyFeedEventId
    );
    try {
      const { notifyFriendNewActivity: notifyFriendNewActivity2, notifyAreaOvertake: notifyAreaOvertake2 } = await Promise.resolve().then(() => (init_notifications(), notifications_exports));
      const distanceKm = distance2 ? distance2 / 1e3 : 0;
      const newAreaKm2 = conquestResult.newAreaConquered / 1e6;
      await notifyFriendNewActivity2(storage, userId, distanceKm, newAreaKm2, env);
      const friendIds = await storage.getFriendIds(userId);
      const userAreaKm2 = conquestResult.totalArea / 1e6;
      const previousAreaKm2 = (conquestResult.totalArea - conquestResult.newAreaConquered) / 1e6;
      const MAX_FRIEND_CHECKS = 10;
      for (const friendId of friendIds.slice(0, MAX_FRIEND_CHECKS)) {
        try {
          const friend = await storage.getUser(friendId);
          if (friend?.totalArea != null) {
            const friendAreaKm2 = friend.totalArea / 1e6;
            if (previousAreaKm2 < friendAreaKm2 && userAreaKm2 >= friendAreaKm2) {
              await notifyAreaOvertake2(storage, userId, friendId, userAreaKm2, env);
            }
          }
        } catch (_) {
        }
      }
    } catch (notifErr) {
      console.error("[QUEUE] Notification error (non-critical):", notifErr);
    }
    console.log(`[QUEUE] \u2705 Route ${routeId} territory processed: ${(conquestResult.newAreaConquered / 1e6).toFixed(4)} km\xB2 new, ${conquestResult.victims.length} victims`);
  } catch (error) {
    console.error(`[QUEUE] \u274C Territory processing failed for route ${routeId}:`, error);
    try {
      const actualArea = await storage.getUserTotalAreaFromTerritories(userId);
      await storage.updateUserTotalArea(userId, actualArea);
    } catch (_) {
    }
    if (earlyFeedEventId) {
      try {
        await storage.updateFeedEvent(earlyFeedEventId, {
          metadata: JSON.stringify({ territoryError: true })
        });
      } catch (_) {
      }
    }
    throw error;
  }
}
__name(handleProcessRouteTerritory, "handleProcessRouteTerritory");
async function handleReprocessFriendGroup(storage, msg) {
  console.log(`[QUEUE] Reprocessing friend group for user ${msg.userId}`);
  const { reprocessFriendGroupTerritoriesChronologically: reprocessFriendGroupTerritoriesChronologically2 } = await Promise.resolve().then(() => (init_routes(), routes_exports));
  await reprocessFriendGroupTerritoriesChronologically2(storage, msg.userId);
  console.log(`[QUEUE] \u2705 Friend group reprocess complete for user ${msg.userId}`);
}
__name(handleReprocessFriendGroup, "handleReprocessFriendGroup");
async function updateFeedEventSafe(storage, userId, routeId, distance2, duration, conquestResult, earlyFeedEventId) {
  try {
    const { generateFeedEvents: generateFeedEvents2 } = await Promise.resolve().then(() => (init_routes(), routes_exports));
    await generateFeedEvents2(
      storage,
      userId,
      routeId,
      distance2,
      duration,
      conquestResult,
      false,
      earlyFeedEventId
    );
  } catch (err) {
    console.error("[QUEUE] Feed event update failed:", err);
  }
}
__name(updateFeedEventSafe, "updateFeedEventSafe");
async function autoCollectTreasures(storage, userId, competitionId, coords) {
  const { autoCollectTreasuresAlongRoute: autoCollectTreasuresAlongRoute2 } = await Promise.resolve().then(() => (init_routes(), routes_exports));
  return autoCollectTreasuresAlongRoute2(storage, userId, competitionId, coords);
}
__name(autoCollectTreasures, "autoCollectTreasures");

// worker/src/index.ts
var app = new Hono2();
app.use("*", logger());
app.use("*", cors({
  origin: /* @__PURE__ */ __name((origin) => {
    const allowedOrigins = [
      "https://runna-io.pages.dev",
      "https://runna-io-web.runna-io-api.workers.dev",
      "http://localhost:5000",
      "http://localhost:3000"
    ];
    if (origin && (allowedOrigins.includes(origin) || origin.endsWith(".runna-io.pages.dev") || origin.endsWith(".workers.dev"))) {
      return origin;
    }
    return allowedOrigins[0];
  }, "origin"),
  allowMethods: ["GET", "POST", "PATCH", "DELETE", "OPTIONS"],
  allowHeaders: ["Content-Type", "Authorization"],
  credentials: false
}));
registerRoutes(app);
app.get("/", (c) => {
  return c.json({ message: "Runna.io API - Cloudflare Workers" });
});
var index_default = {
  fetch: app.fetch,
  queue: handleQueueBatch
};
export {
  index_default as default
};
//# sourceMappingURL=index.js.map
